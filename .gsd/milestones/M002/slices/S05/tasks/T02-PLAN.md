---
estimated_steps: 16
estimated_files: 6
skills_used: []
---

# T02: Update active recovery guidance to gw

---
estimated_steps: 5
estimated_files: 4
skills_used:
  - python-testing-patterns
  - verify-before-complete
---

Why: After S04, a focused active-source scan still found stale user-facing recovery instructions that say to run `graph-wiki-agent bootstrap <path>`. These are executable recovery instructions, not historical notes, and must now point at `gw bootstrap <path>`.

Do:
1. Update `packages/workspace-io/src/workspace_io/config.py` missing-manifest error text from `graph-wiki-agent bootstrap <path>` to `gw bootstrap <path>`.
2. Update matching expectations in `packages/workspace-io/tests/test_config.py`.
3. Update `packages/wiki-io/src/wiki_io/_workspace.py` docstring/user-facing wording that names the old bootstrap command.
4. Update matching expectations in `packages/wiki-io/tests/test_ports_importable.py`.
5. Classify `packages/wiki-io/src/wiki_io/init_vault.py`'s `generated by graph-wiki-agent scan` comment as provenance/plugin-identity text, not an executable recovery command, unless tests or runtime docs prove it should change. If preserved, make sure later stale-reference gates use a narrow executable-guidance pattern or explicit allowlist so this allowed provenance string does not hide real command guidance.

Negative tests: Existing tests should continue to exercise unresolved workspace/error paths and assert the new `gw bootstrap` recovery message rather than accepting the obsolete executable name.

Done when: Touched error-path tests pass and no active recovery text in these modules tells users to run `graph-wiki-agent bootstrap`.

## Inputs

- `packages/workspace-io/src/workspace_io/config.py`
- `packages/workspace-io/tests/test_config.py`
- `packages/wiki-io/src/wiki_io/_workspace.py`
- `packages/wiki-io/tests/test_ports_importable.py`
- `packages/wiki-io/src/wiki_io/init_vault.py`
- `packages/wiki-io/tests/test_init_vault.py`

## Expected Output

- `packages/workspace-io/src/workspace_io/config.py`
- `packages/workspace-io/tests/test_config.py`
- `packages/wiki-io/src/wiki_io/_workspace.py`
- `packages/wiki-io/tests/test_ports_importable.py`

## Verification

uv run python -m pytest packages/workspace-io/tests/test_config.py packages/wiki-io/tests/test_ports_importable.py -q

## Observability Impact

Improves failure-path diagnostics by ensuring unresolved-workspace errors give the current executable recovery command.
