---
id: T02
parent: S05
milestone: M002
key_files:
  - packages/workspace-io/src/workspace_io/config.py
  - packages/workspace-io/tests/test_config.py
  - packages/wiki-io/src/wiki_io/_workspace.py
  - packages/wiki-io/tests/test_ports_importable.py
key_decisions:
  - Preserved `packages/wiki-io/src/wiki_io/init_vault.py`'s `generated by graph-wiki-agent scan` sentinel as provenance/plugin-identity text rather than executable recovery guidance.
duration: 
verification_result: passed
completed_at: 2026-05-31T17:17:34.253Z
blocker_discovered: false
---

# T02: Updated active missing-workspace recovery guidance to tell users to run `gw bootstrap <path>` instead of the obsolete `graph-wiki-agent bootstrap <path>`.

**Updated active missing-workspace recovery guidance to tell users to run `gw bootstrap <path>` instead of the obsolete `graph-wiki-agent bootstrap <path>`.**

## What Happened

Changed the missing-manifest RuntimeError in `workspace_io.config.resolve()` to emit `Run: gw bootstrap <path>`. Updated the corresponding workspace-io negative test expectation and refreshed the wiki-io workspace docstring/user-facing wording to name the current `gw bootstrap <path>` command. Updated the wiki-io unresolved-workspace smoke tests so they assert the new recovery message. Reviewed `init_vault.py` and preserved the `generated by graph-wiki-agent scan` sentinel comment because it is provenance/plugin-identity text, not executable bootstrap recovery guidance.

## Verification

Ran a focused scan across the touched modules to confirm active executable bootstrap guidance now uses `gw bootstrap` and that the only preserved `graph-wiki-agent` hit in scope is the init_vault generated-by provenance comment. Ran the task's targeted pytest command; all 22 selected tests passed with one existing pytest collection warning about `.hypothesis` norecursedirs configuration.

## Verification Evidence

| # | Command | Exit Code | Verdict | Duration |
|---|---------|-----------|---------|----------|
| 1 | `python - <<'PY'
from pathlib import Path
paths = [
    Path('packages/workspace-io/src/workspace_io/config.py'),
    Path('packages/workspace-io/tests/test_config.py'),
    Path('packages/wiki-io/src/wiki_io/_workspace.py'),
    Path('packages/wiki-io/tests/test_ports_importable.py'),
    Path('packages/wiki-io/src/wiki_io/init_vault.py'),
]
for p in paths:
    text = p.read_text(encoding='utf-8')
    hits = []
    for i, line in enumerate(text.splitlines(), 1):
        if 'graph-wiki-agent bootstrap' in line or 'gw bootstrap' in line or 'generated by graph-wiki-agent scan' in line:
            hits.append((i, line.strip()))
    print(f'{p}:')
    for i, line in hits:
        print(f'  {i}: {line}')
    if not hits:
        print('  (no hits)')
PY` | 0 | ✅ pass | 64ms |
| 2 | `uv run python -m pytest packages/workspace-io/tests/test_config.py packages/wiki-io/tests/test_ports_importable.py -q` | 0 | ✅ pass | 2120ms |

## Deviations

None.

## Known Issues

Targeted pytest emitted an existing warning about pytest's `norecursedirs` replacing default ignores for `.hypothesis`; not caused by this task.

## Files Created/Modified

- `packages/workspace-io/src/workspace_io/config.py`
- `packages/workspace-io/tests/test_config.py`
- `packages/wiki-io/src/wiki_io/_workspace.py`
- `packages/wiki-io/tests/test_ports_importable.py`
