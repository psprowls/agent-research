---
estimated_steps: 17
estimated_files: 4
skills_used: []
---

# T03: Refresh repo guardrails for package-era boundaries

---
estimated_steps: 6
estimated_files: 4
skills_used:
  - uv-package-manager
  - python-testing-patterns
  - verify-before-complete
---

Why: Repo-level gates still contain package-pre-split assumptions, including integration-gate wording that references old agent tests and brand checks scoped to `agents/graph-wiki-agent`. These guardrails need to enforce the new world instead of silently depending on a deleted directory.

Do:
1. Update `tests/test_integration_gate.py` stale assertion/failure messaging so it describes package-local integration tests under `packages/*/tests/integration/*` plus the intentional repo-level meta-test, not `agents/graph-wiki-agent/tests/integration/*`.
2. Update `scripts/check-brand.sh` scopes and comments to package-era roots. In particular, make CHECK 3 inspect `packages/graph-wiki-cli/src/graph_wiki_cli/cli.py` for the old init-command hazard instead of the deleted agent CLI path.
3. Update `.brand-grep-allow` to remove `agents/graph-wiki-agent/` allowances that are no longer valid. Preserve only allowances that correspond to active, intentionally classified plugin identity/provenance/negative-test strings.
4. If the brand/runtime docs guard needs an explicit allowlist for `generated by graph-wiki-agent scan`, document it narrowly as provenance so it cannot mask executable command guidance.
5. Run the integration meta-test and the brand script.

Negative tests: Guardrails should fail on stale executable guidance or old workspace paths, while still allowing negative boundary tests and preserved plugin identity strings.

Done when: The repo-level integration gate wording is current, the brand script checks live package paths, and both guardrails pass in the package-only workspace.

## Inputs

- `tests/test_integration_gate.py`
- `scripts/check-brand.sh`
- `.brand-grep-allow`
- `packages/graph-wiki-cli/src/graph_wiki_cli/cli.py`
- `tests/test_package_split_workspace.py`

## Expected Output

- `tests/test_integration_gate.py`
- `scripts/check-brand.sh`
- `.brand-grep-allow`

## Verification

uv run python -m pytest tests/test_integration_gate.py -q && bash scripts/check-brand.sh

## Observability Impact

Keeps guardrail failures actionable for future agents by pointing checks at live package paths instead of deleted agent paths.
