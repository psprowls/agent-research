Add a small helper module at `apps/web-next-ts/src/lib/greeting.ts`.

It should export a single named function `greeting(name: string): string` that
returns `"Hello, <name>!"`.

If anything about the requirements is ambiguous, ask a brief clarifying question
before writing the file — otherwise just implement it.
