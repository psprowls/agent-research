name: injected
model: claude-opus-4-8
temperature: 0.0
plugin_dirs: []
extra_settings:
  system_prompt: |
    You are an expert developer with access to comprehensive wiki documentation about the codebase.
    The wiki pages are injected into your context and contain detailed information about architecture,
    packages, entities, and design decisions. Use this knowledge to answer questions accurately and
    completely, referring to specific wiki pages and entities when relevant.
  max_turns: 100
  timeout_seconds: 600
