---
milestone: v1.4 — Workspace Path Resolution Cleanup
phases: [22, 23, 24, 25]
status: gaps_found
date: 2026-05-21
---

# v1.4 Milestone Integration Audit

**Status: GAPS FOUND** — one BLOCKER (detection-workflow.md D-08 mirror drift introduced by Phase 25), one WARNING (eval integration tests live path deferred/unrun). All other cross-phase connections verified end-to-end.

---

## Wiring Summary

**Connected:** 28 exports/APIs properly wired across phases  
**Orphaned:** 0 exports created but unused  
**Missing:** 1 expected connection not made (detection-workflow.md prompt-source mirror)

## API Coverage

**Consumed:** All 6 MCP tool routes wired to callers  
**Orphaned:** 0 routes without callers

## Auth Protection

Not applicable — this milestone is a rename/refactor; no auth surfaces introduced.

## E2E Flows

**Complete:** 5 flows verified end-to-end  
**Broken:** 0 flows broken  
**Deferred (not broken):** 1 live integration run (UAT-01, WSMCP-06)

---

## Detailed Findings

### Flow 1: CLI → MCP → command boundary (WIRED)

Chain: `--workspace` Typer flag → `workspace_path = Path(workspace) if workspace else None` → `run_*(workspace_path=workspace_path)` → `resolve_wiki_and_repo(workspace_path)` → `wiki_dir(workspace_path)`

- All 7 Typer commands carry `"--workspace"` (0 `"--vault"` survivors in `cli.py`)
- Bridge `workspace_path = Path(workspace) if workspace else None` present in all 7 command bodies
- All 6 MCP handlers construct `vault = Path(input.workspace_path) if input.workspace_path else None` then call `run_*(workspace_path=vault)`
- All 6 `run_*` signatures accept `workspace_path: Path | None = None`
- `resolve_wiki_and_repo(workspace_path, repo_path)` signature confirmed in `wiki_io/_workspace.py`
- `wiki_dir(workspace_path)` derivation in sweep.py (2 sites), baseline.py (1 site), structural.py (2 sites)
- `EvalWorktree(wiki)` called with derived `wiki`, not `workspace_path`, in sweep.py and baseline.py — CR-01 fix intact

**REQ mapping:** WSAPI-01 WIRED, WSAPI-02 WIRED, WSAPI-03 WIRED, WSMCP-01 WIRED, WSMCP-02 WIRED, WSAPI-06 WIRED, WSEVAL-01 WIRED, WSEVAL-02 WIRED

### Flow 2: Scan → ingest pipeline contract (WIRED)

- `scan_monorepo.py` emits `"wiki_relative_path"` at 3 sites (0 `"vault_path"` emissions)
- `commands/scan.py` consumes `pkg.get("wiki_relative_path", ...)` at line 369; `existing_rec["wiki_relative_path"]` at lines 399 and 416
- 0 `"vault_path"` key reads in `commands/scan.py`

**REQ mapping:** WSMCP-04 WIRED

### Flow 3: Plugin↔mirror byte-identicality (SPLIT — scan-workflow WIRED, detection-workflow BROKEN)

- `scan-workflow.md`: `diff plugins/.../scan-workflow.md packages/prompt-sources/references/scan-workflow.md` → exit 0 (IDENTICAL) ✓
- `detection-workflow.md`: `diff` exits non-zero — 39 lines diverged (BLOCKER — see below)
- `ingest-workflow.md`, `lint-workflow.md`, `query-workflow.md`: diverged, but these predate Phase 22 and are not regressions introduced by this milestone

**REQ mapping:** WSMCP-05 PARTIAL (scan-workflow updated; detection-workflow mirror gap is new regression introduced by Phase 25)

### Flow 4: Plugin shim → wiki_io.detect_containers (WIRED)

- `plugins/graph-wiki/skills/graph-wiki/scripts/detect_containers.py` contains `from wiki_io.detect_containers import main` and calls `main()` under `__main__`
- `wiki_io.detect_containers._classify_dir` Rule 3 is the updated permissive branch (lines 112–135)
- Shim is 9 lines, unchanged by Phase 25, auto-inherits new classifier

**REQ mapping:** PKGCLS-01 WIRED, PKGCLS-02 WIRED

### Flow 5: Brand-gate hardening (WIRED)

- `scripts/check-brand.sh` contains CHECK 4 (Phase 23) and CHECK 5 (Phase 24)
- Clean-tree run exits 0 (verified live during this audit)
- CHECK 4 bans `vault_path: (str|Path|int|bool)` Pydantic fields, `"--vault"` flags, `"vault_path"` dict keys in `packages/ agents/ plugins/`
- CHECK 5 bans `vault_path: Path`, `vault: Path` param annotations, `"--vault"` argparse literals in `packages/eval-harness/{src,tests}`
- All allowlist entries carry `# rationale:` comments
- Phase 23 carry-forward entries for `structural.py` and `sweep.py` correctly removed from `.brand-grep-allow` after Phase 24 shipped

**REQ mapping:** WSMCP-07 WIRED, WSEVAL-04 (brand-gate side) WIRED

### Flow 6: eval-harness ↔ wiki-io coupling (WIRED)

- `EvalWorktree.__init__(self, source_wiki: Path)` confirmed in `isolation.py` line 36
- `sweep.py`: `wiki = wiki_dir(workspace_path)` at lines 262 and 531; `EvalWorktree(wiki)` at lines 268 and 545
- `baseline.py`: `wiki = wiki_dir(self._workspace_path)` at line 354; `EvalWorktree(wiki)` at line 355
- CR-01 fix verified: worktree receives the derived wiki path, not the workspace root

**REQ mapping:** WSEVAL-01 WIRED, WSEVAL-02 WIRED, WSAPI-05 (EvalWorktree D-04) WIRED

### Flow 7: YAML key hard-cut (WIRED)

- `packages/workspace-io/src/workspace_io/config.py`: `WORKSPACE_DIRECTORY_KEY = "workspace-directory"` at line 21
- Only reference to the key is `local.get(WORKSPACE_DIRECTORY_KEY, "").strip()` at line 41
- Zero references to `"graph-wiki-directory"` in `config.py`
- `resolve_workspace(repo_root)` is public at line 39; consumed by `workspace_io/init.py` line 32

**REQ mapping:** WSAPI-05 WIRED, WSAPI-06 WIRED

### Flow 8: Pytest (WIRED — all tests green)

Full workspace `uv run pytest` result at milestone boundary:  
**596 passed, 32 skipped, 0 failed** (run completed 2026-05-21)

The 5 ANSI-help test failures documented in Phase 22–25 SUMMARYs as "pre-existing" are resolved by quick task `260521-ans-typer-help-ansi-strip` (commit `fda4971`). All previously-failing tests now pass.

---

## Orphaned Exports

None. All phase exports are consumed.

---

## Missing Connections

### BLOCKER: detection-workflow.md prompt-source mirror not updated (Phase 25)

- **REQ:** WSMCP-05 (partial), no dedicated PKGCLS REQ-ID for mirror sync
- **From:** Phase 25 Task 3 (`1db8b6d`) updated `plugins/graph-wiki/skills/graph-wiki/references/detection-workflow.md`
- **Missing:** `packages/prompt-sources/references/detection-workflow.md` was NOT updated in the same commit
- **Evidence:** `diff` between the two files exits non-zero with 39-line diff; plugin copy has `graph-wiki` references and permissive Rule 3 language; mirror copy still has `lattice-wiki` references and old all-or-nothing Rule 3 description
- **Why it matters:** Per Phase 23 PATTERNS.md D-08: "the mirrors are loaded into agent system prompts at runtime. Drift between the pair = silent agent-behavior regression." The mirror is the file actually loaded by the `prompt-sources` package at inference time, not the plugin copy.
- **Invariant source:** `23-PATTERNS.md` §"Plugin-doc ↔ Prompt-source mirror pattern" explicitly lists `detection-workflow.md` as a known mirror pair and states "Whenever a `plugins/graph-wiki/skills/graph-wiki/references/<X>.md` file changes, the corresponding `packages/prompt-sources/references/<X>.md` mirror must change in the same commit."
- **Note:** Phase 25's own CONTEXT defined "D-08" differently (adding a Rules subsection) without referencing the Phase 23 D-08 mirror-invariant. The plan did not include `packages/prompt-sources/references/detection-workflow.md` in `key_files.modified`.
- **Fix:** Copy `plugins/graph-wiki/skills/graph-wiki/references/detection-workflow.md` → `packages/prompt-sources/references/detection-workflow.md` and commit.

---

## Broken Flows

None. All traced flows complete end-to-end (see BLOCKER above for the mirror invariant violation, which is a doc-sync issue, not a runtime flow break).

---

## Unprotected Routes

Not applicable.

---

## Deferred Items (not blockers)

### WARNING: UAT-01 live integration test unrun (WSMCP-06)

- `GRAPH_WIKI_RUN_INTEGRATION=1 uv run pytest agents/graph-wiki-agent/tests/integration/test_mcp_e2e.py -x -q` was not executed during Phase 23 execution (AWS credentials / cost reason per D-04)
- Mechanical test updates verified: `test_mcp_e2e.py` has 6 `"workspace_path"` JSON keys, 0 `"vault_path"` keys
- SC#2 smoke test (`test_mcp_schema_forbid_extra.py`) passes in default pytest and exercises the schema reject path
- The deferred live run is the only gap in WSMCP-06 verification

### WARNING: eval/baselines/*.json carry old `vault_content_hash` key (WSEVAL-02)

- 8 baseline JSON files still contain the old key name `vault_content_hash` (pre-Phase-24 recordings)
- No read site exists in-package today — the key is only emitted, not consumed
- Will surface as `KeyError` if a baseline-replay consumer is added before regeneration
- Intentional per Phase 24 D-11; regeneration requires `GRAPH_WIKI_RUN_EVAL=1` with live Bedrock

### NOTE: ingest-workflow.md, lint-workflow.md, query-workflow.md mirrors diverged

These three mirrors were diverged before Phase 22 started (last modified by `c5a47ba refactor: rename cores/ workspace tier to packages/`). Phase 23 fixed `scan-workflow.md` but did not sweep all mirrors. These are pre-existing and outside this milestone's scope.

---

## Requirements Integration Map

| Requirement | Integration Path | Status | Issue |
|-------------|-----------------|--------|-------|
| WSAPI-01 | `wiki_io/_workspace.py` `resolve_wiki_and_repo(workspace_path, repo_path)` | WIRED | — |
| WSAPI-02 | 6 `run_*` signatures in `commands/` accept `workspace_path: Path | None` | WIRED | — |
| WSAPI-03 | `cli.py` + `server.py` call `run_*(workspace_path=...)` | WIRED | — |
| WSAPI-04 | ~30 test call sites use `workspace_path=` kwarg | WIRED | — |
| WSAPI-05 | `WORKSPACE_DIRECTORY_KEY = "workspace-directory"` in `config.py`; old key absent | WIRED | — |
| WSAPI-06 | `resolve_workspace(repo_root)` public; `init.py` calls it | WIRED | — |
| WSMCP-01 | 6 MCP input classes have `workspace_path: str` + `extra='forbid'` | WIRED | — |
| WSMCP-02 | 7 Typer commands have `"--workspace"` flag; 0 `"--vault"` survivors | WIRED | — |
| WSMCP-03 | `bootstrap` has `"--repo"` flag; wired to `run_init(repo_path=repo_path)` | WIRED | — |
| WSMCP-04 | `scan_monorepo.py` emits `"wiki_relative_path"`; `commands/scan.py` reads it | WIRED | — |
| WSMCP-05 | `scan-workflow.md` byte-identical; `detection-workflow.md` mirror NOT updated | PARTIAL | **BLOCKER** — Phase 25 updated plugin copy but not `packages/prompt-sources/references/detection-workflow.md` |
| WSMCP-06 | Mechanical test update verified; live `GRAPH_WIKI_RUN_INTEGRATION` run deferred | PARTIAL | WARNING — UAT-01 unrun |
| WSMCP-07 | CHECK 4 in `check-brand.sh`; allowlist entries with rationale; clean-tree exits 0 | WIRED | — |
| WSEVAL-01 | `sweep.py` 7 public fns accept `workspace_path: Path`; `wiki_dir` derivation | WIRED | — |
| WSEVAL-02 | `baseline.py` `_wiki_content_hash`; `--workspace` argparse; `structural.py` | WIRED | — |
| WSEVAL-03 | 7 divergence modules accept bare `wiki: Path` | WIRED | — |
| WSEVAL-04 | 194 test references swept; brand-gate CHECK 5 active; 596 tests pass | WIRED | — |
| WSEVAL-05 | `eval/README.md` updated; `EvalWorktree(source_wiki=...)` | WIRED | — |
| PKGCLS-01 | `_classify_dir` Rule 3 permissive `>=1 manifested child → package` | WIRED | — |
| PKGCLS-02 | Plugin shim `from wiki_io.detect_containers import main` auto-inherits | WIRED | — |
| PKGCLS-03 | `--interactive` flag deferred per D-12; doc-side only (detection-workflow.md) | DEFERRED | Backlog item; explicitly out of Phase 25 scope per D-12 |
| PKGCLS-04 | 5 new unit tests; 9/9 wiki-io tests pass; operational repro verified | WIRED | — |
| PKGCLS-05 | Pending todo moved to `resolved/` via `git mv` | WIRED | — |

**Requirements with no cross-phase wiring:**
- PKGCLS-03 — self-contained in Phase 25 doc side; flag implementation explicitly deferred to backlog. No cross-phase integration needed for the deferred portion.

---

## Summary Counts

| Category | Count |
|----------|-------|
| WIRED (end-to-end verified) | 23 |
| PARTIAL (mechanically correct, live path deferred) | 2 (WSMCP-05, WSMCP-06) |
| DEFERRED (explicitly out of scope) | 1 (PKGCLS-03) |
| BLOCKER findings | 1 |
| WARNING findings | 2 |
| Full test suite result | 596 passed, 32 skipped, 0 failed |
