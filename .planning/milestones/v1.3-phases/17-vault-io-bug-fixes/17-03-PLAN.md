---
phase: 17-vault-io-bug-fixes
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/vault-io/src/vault_io/init_vault.py
  - packages/vault-io/src/vault_io/detect_containers.py
  - packages/vault-io/tests/test_detect_containers.py
autonomous: true
requirements: [WSRES-01, WSRES-02, WSRES-03]
tags: [vault-io, workspace, repo-resolution, detect-containers, python, pytest]

must_haves:
  truths:
    - "`init_vault.main()` resolves the repo via `_, repo = resolve_wiki_and_repo()` — NOT `wiki.parent`."
    - "`detect_containers.main()` resolves the repo via `_, repo = resolve_wiki_and_repo()` — NOT `wiki.parent` — and passes `workspace_path=wiki.parent` (or equivalent: the wiki's parent directory, which is the workspace dir under v2 layout) to `detect()`."
    - "`detect()` signature becomes `def detect(repo_root, workspace_path=None) -> list[dict]`."
    - "When `workspace_path` is set AND `workspace_path.resolve() != repo_root.resolve()` AND `workspace_path.parent.resolve() == repo_root.resolve()`, the matching immediate subdir is excluded from classification."
    - "When `workspace_path.resolve() == repo_root.resolve()` (v1 layout — workspace IS the repo), the exclusion is skipped — `detect()` returns the same containers as if no workspace was supplied."
    - "On a synthetic v2 monorepo fixture (`tmp_path/repo/graph-wiki/wiki/`, `tmp_path/repo/packages/pkg-a/pyproject.toml`, `tmp_path/repo/packages/pkg-b/pyproject.toml`, `.git/`, `GRAPH_WIKI_WORKSPACE=tmp_path/repo/graph-wiki`), `detect(tmp_path/'repo', workspace_path=tmp_path/'repo'/'graph-wiki')` returns at least one record with `source == 'packages'` AND no record with `source == 'graph-wiki'`."
    - "On a v1-layout fixture where `workspace_path == repo_root`, the exclusion guard prevents self-skip and `detect()` returns its normal classification list (regression-locked)."
  artifacts:
    - path: packages/vault-io/src/vault_io/init_vault.py
      provides: "WSRES-01 fix: use `_, repo = resolve_wiki_and_repo()` instead of `wiki.parent`."
      contains: "resolve_wiki_and_repo"
    - path: packages/vault-io/src/vault_io/detect_containers.py
      provides: "WSRES-01 + WSRES-02 fixes: workspace-aware repo resolution AND optional `workspace_path` param on `detect()` with D-11 guard."
      contains: "workspace_path"
    - path: packages/vault-io/tests/test_detect_containers.py
      provides: "Synthetic-fixture unit tests covering v2 layout (workspace excluded, packages found) and v1 layout (guard prevents self-exclusion)."
      exports: ["test_v2_layout_finds_repo_containers", "test_workspace_path_excluded", "test_v1_layout_guard", "test_v2_synthetic_repo"]
  key_links:
    - from: "packages/vault-io/src/vault_io/detect_containers.py::detect"
      to: "_immediate_subdirs(repo_root)"
      via: "list comprehension that filters by `d.resolve() != exclude`"
      pattern: "_immediate_subdirs"
    - from: "packages/vault-io/src/vault_io/detect_containers.py::main"
      to: "_workspace.resolve_wiki_and_repo()"
      via: "tuple unpack `wiki, repo = resolve_wiki_and_repo()` then `detect(repo, workspace_path=wiki.parent)`"
      pattern: "resolve_wiki_and_repo"
    - from: "packages/vault-io/tests/test_detect_containers.py"
      to: "GRAPH_WIKI_WORKSPACE env var"
      via: "monkeypatch.setenv"
      pattern: "monkeypatch.setenv\\(.GRAPH_WIKI_WORKSPACE."

reconciliations: []
---

<objective>
Fix workspace/repo resolution in `init_vault.py` and `detect_containers.py` so both scripts find repo-root containers under the v2 workspace layout (wiki at `<repo>/graph-wiki/wiki/`), and stop self-classifying the workspace directory as a `docs` container. Add a synthetic tmp_path test that exercises both fixes.

Purpose: Address WSRES-01 (use the correct `resolve_wiki_and_repo()` return value), WSRES-02 (add `workspace_path` exclusion on `detect()` with the v1-layout guard), and WSRES-03 (test coverage). After this plan, `detect_containers --json` returns the real repo containers when run from a v2-layout repo, and the workspace dir is no longer listed as a self-classified `docs` container.

Output: Two small source edits (one in `init_vault.py`, one in `detect_containers.py`), and a new `tests/test_detect_containers.py` covering the v2-layout positive path and the v1-layout guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-vault-io-bug-fixes/17-CONTEXT.md
@.planning/phases/17-vault-io-bug-fixes/17-RESEARCH.md
@.planning/phases/17-vault-io-bug-fixes/17-PATTERNS.md
@packages/vault-io/src/vault_io/init_vault.py
@packages/vault-io/src/vault_io/detect_containers.py
@packages/vault-io/src/vault_io/_workspace.py
@packages/vault-io/tests/test_truncated_frontmatter.py

<interfaces>
<!-- Key contracts extracted from the codebase. Do not re-discover. -->

From `packages/vault-io/src/vault_io/_workspace.py:23-38`:
```python
def resolve_wiki_and_repo(vault_path: Path | None = None) -> tuple[Path, Path | None]:
    if vault_path is not None:
        return vault_path.resolve(), _find_repo_root(vault_path)
    cfg = _ws_config.resolve()
    return _ws_paths.wiki_dir(cfg.workspace), cfg.repo_root
```
Second return value is the workspace-aware repo root. Honors `GRAPH_WIKI_WORKSPACE` env var via `_ws_config.resolve()`.

From `packages/vault-io/src/vault_io/init_vault.py:305-306` (current buggy state):
```python
wiki, _ = resolve_wiki_and_repo()
repo = wiki.parent  # v1: repo is always wiki's parent directory
```

From `packages/vault-io/src/vault_io/detect_containers.py:148-180` (current buggy state):
- `detect(repo_root: Path) -> list[dict]` — signature changes to add `workspace_path`
- `_immediate_subdirs(repo_root)` — existing helper at line 56; returns list of immediate subdir Paths
- `SKIP_DIRS` constant at line 33 — static skip-set; conceptual neighbor but DO NOT extend (workspace exclusion is dynamic-per-call)
- `_classify_dir(d)` — existing helper; returns the dict record with `source`, `classification`, `children_count`, `reason`
- Lines 174-175 (in `main()`): `wiki, _ = resolve_wiki_and_repo(); repo = wiki.parent`

Record dict shape (from `_classify_dir`): `{"source": str, "classification": str, "children_count": int, "reason": str, ...}`. `source` is the immediate subdir name relative to repo_root.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix repo resolution + add workspace_path exclusion (WSRES-01, WSRES-02)</name>
  <files>packages/vault-io/src/vault_io/init_vault.py, packages/vault-io/src/vault_io/detect_containers.py</files>
  <read_first>
    - packages/vault-io/src/vault_io/init_vault.py (lines 290-320 — `main()` and surrounding code to confirm `wiki` is still needed by `init_wiki(...)`)
    - packages/vault-io/src/vault_io/detect_containers.py (lines 1-50 for constants + `SKIP_DIRS`; lines 50-100 for `_immediate_subdirs` and `_classify_dir`; lines 140-200 for `detect()` and `main()`)
    - packages/vault-io/src/vault_io/_workspace.py (lines 1-50 — confirm `resolve_wiki_and_repo` return shape)
    - .planning/phases/17-vault-io-bug-fixes/17-PATTERNS.md (`detect_containers.detect()` and `init_vault.main()` sections, lines 139-220)
    - .planning/phases/17-vault-io-bug-fixes/17-RESEARCH.md (Pattern 3, Pitfall 3 — D-11 guard semantics)
  </read_first>
  <behavior>
    - `init_vault.main()`: the `repo` variable comes from the second return value of `resolve_wiki_and_repo()`, not from `wiki.parent`. The `wiki` variable is still bound if the surrounding code needs it for the `init_wiki(wiki, repo, ...)` call.
    - `detect_containers.detect(repo, workspace_path=None)`: when called with `workspace_path=None`, behavior is identical to the pre-change version (no regression).
    - `detect_containers.detect(repo, workspace_path=ws)`: when `ws.resolve() != repo.resolve()` AND `ws.parent.resolve() == repo.resolve()`, the immediate subdir at `ws` is excluded from `_immediate_subdirs` iteration before classification.
    - When `ws.resolve() == repo.resolve()` (v1 layout, wiki at repo root, workspace IS the repo), the exclusion guard prevents self-skip — the function returns its normal container list.
    - `detect_containers.main()`: passes `workspace_path=wiki.parent` (the actual workspace dir under v2; under v1 this equals `repo`, and the guard handles that case).
  </behavior>
  <action>
    Two source edits:

    **A. `packages/vault-io/src/vault_io/init_vault.py:305-306`** — replace the two-line `wiki, _ = resolve_wiki_and_repo(); repo = wiki.parent` block with `wiki, repo = resolve_wiki_and_repo()` (tuple unpack of both return values). The `wiki` variable is still needed because the surrounding `init_wiki(wiki, repo, ...)` call consumes it; only the `repo = wiki.parent` line is wrong. Drop the `# v1: repo is always wiki's parent directory` comment as it's now stale.

    **B. `packages/vault-io/src/vault_io/detect_containers.py`** — two edits:

       B1. Modify `detect()` (lines 148-166) signature and body:
       - Change signature to `def detect(repo_root: Path, workspace_path: Path | None = None) -> list[dict]:`.
       - Inside the function, after the existing `repo_root = Path(repo_root).resolve()` and `if not repo_root.exists(): return []` lines, add the D-11-guarded exclusion logic:
         - Compute `exclude: Path | None = None`.
         - If `workspace_path is not None`: resolve it to `wp = Path(workspace_path).resolve()`; if `wp != repo_root` AND `wp.parent == repo_root`, set `exclude = wp`. (This guards the v1 layout where workspace IS repo, AND guards against passing an arbitrary non-subdir path.)
       - Change the existing `top = _immediate_subdirs(repo_root)` line to `top = [d for d in _immediate_subdirs(repo_root) if exclude is None or d.resolve() != exclude]`. The rest of `detect()` is unchanged.

       Implementation freedom on path equality (D-11): per the discretion list, either `Path.resolve()` equality or `Path.samefile()` is acceptable. Default to `.resolve()` equality — symlinks are not a known concern here.

       B2. Modify `main()` (lines 174-180):
       - Change `wiki, _ = resolve_wiki_and_repo(); repo = wiki.parent` to `wiki, repo = resolve_wiki_and_repo()`.
       - Change the `records = detect(repo)` call to `records = detect(repo, workspace_path=wiki.parent)`.
       - Drop the stale `# v1: repo is always wiki's parent directory` comment if present.

    Do NOT modify `SKIP_DIRS`, `_immediate_subdirs`, `_classify_dir`, or any other helper. The workspace exclusion is a dynamic per-call parameter, not a constant.

    Do NOT call `resolve_wiki_and_repo()` inside `detect()` itself — keeping `detect()` a pure path classifier is explicitly required (per CONTEXT.md D-10 + RESEARCH.md "Anti-Patterns to Avoid").
  </action>
  <verify>
    <automated>cd packages/vault-io &amp;&amp; grep -n "wiki.parent" src/vault_io/init_vault.py src/vault_io/detect_containers.py | grep -v '^#'</automated>
    <automated>uv run --package vault-io python -c "from vault_io.detect_containers import detect; import inspect; sig = inspect.signature(detect); assert 'workspace_path' in sig.parameters, sig.parameters; print('OK', sig)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "repo = wiki.parent" packages/vault-io/src/vault_io/init_vault.py | grep -v '^#'` returns 0 matches.
    - `grep -n "repo = wiki.parent" packages/vault-io/src/vault_io/detect_containers.py | grep -v '^#'` returns 0 matches.
    - `grep -n "wiki, repo = resolve_wiki_and_repo()" packages/vault-io/src/vault_io/init_vault.py | grep -v '^#'` returns 1 match.
    - `grep -n "wiki, repo = resolve_wiki_and_repo()" packages/vault-io/src/vault_io/detect_containers.py | grep -v '^#'` returns 1 match.
    - `grep -n "workspace_path" packages/vault-io/src/vault_io/detect_containers.py | grep -v '^#'` returns at least 3 matches (signature, guard logic, main() call site).
    - The Python introspection check above prints `OK` and the signature contains `workspace_path`.
    - `uv run --package vault-io python -c "import vault_io.init_vault; import vault_io.detect_containers"` exits 0.
    - The full vault-io suite still passes: `uv run --package vault-io pytest -x` exits 0 (no regression).
    - `detect(repo, workspace_path=None)` produces the same output as `detect(repo)` for any input (no positional/default regression) — verified by running pytest after edit; the existing test suite must remain green.
  </acceptance_criteria>
  <done>
    Both source files use `_, repo = resolve_wiki_and_repo()` (or `wiki, repo = ...` where wiki is still needed); `detect()` has the optional `workspace_path` parameter with the D-11 guard; `detect_containers.main()` passes `wiki.parent` as the workspace; no module-level constants were touched beyond the function body changes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Synthetic-fixture unit tests for v2 layout and v1 guard (WSRES-03)</name>
  <files>packages/vault-io/tests/test_detect_containers.py</files>
  <read_first>
    - packages/vault-io/tests/test_truncated_frontmatter.py (lines 1-30 — `tmp_path` + targeted file build header pattern)
    - packages/vault-io/tests/conftest.py (existing fixtures and `write_file` helper)
    - packages/vault-io/src/vault_io/detect_containers.py (after Task 1 — confirm signature and behavior)
    - packages/vault-io/src/vault_io/_workspace.py (lines 1-50 — confirm `GRAPH_WIKI_WORKSPACE` env var honoring path)
    - packages/workspace-io/src/workspace_io/config.py (skim — confirm `_find_repo_root` walks up looking for `.git`)
    - .planning/phases/17-vault-io-bug-fixes/17-PATTERNS.md (`tests/test_detect_containers.py` section, lines 397-461)
    - .planning/phases/17-vault-io-bug-fixes/17-VALIDATION.md (Per-Task Verification Map — exact test names)
  </read_first>
  <behavior>
    - `test_v2_layout_finds_repo_containers`: with a synthetic v2 fixture (repo with `.git/`, `graph-wiki/wiki/`, `packages/pkg-a/pyproject.toml`, `packages/pkg-b/pyproject.toml`, and `GRAPH_WIKI_WORKSPACE` set to the graph-wiki dir), calling `detect(repo, workspace_path=repo/'graph-wiki')` returns at least one record with `source == 'packages'`.
    - `test_workspace_path_excluded`: same fixture; the returned records do NOT contain any with `source == 'graph-wiki'` (workspace dir is excluded).
    - `test_v1_layout_guard`: with a v1-layout fixture (workspace == repo_root, wiki at `<repo>/wiki/`), calling `detect(repo, workspace_path=repo)` returns records that DO include the same containers as a no-workspace call — the guard prevents the self-exclusion footgun.
    - `test_v2_synthetic_repo`: optional end-to-end test that exercises the call path via `main()` or directly via `detect()` against the v2 fixture; asserts both positive (packages found) and negative (graph-wiki not listed) outcomes in a single test. May overlap with the above two; provided as the named test from VALIDATION.md.
  </behavior>
  <action>
    Create `packages/vault-io/tests/test_detect_containers.py` as a new file. Follow the header pattern from `test_truncated_frontmatter.py:1-12` (`from __future__ import annotations`, `from pathlib import Path`, `import pytest`).

    Build the v2 synthetic fixture either as a module-level `@pytest.fixture` (`v2_workspace`) returning a dict of `{"repo": Path, "workspace": Path}` OR inline inside each test — executor's call. The fixture must:
    - Create `repo` at `tmp_path / "repo"`.
    - `mkdir(parents=True)` for `repo / "graph-wiki" / "wiki"`, `repo / "packages" / "pkg-a"`, `repo / "packages" / "pkg-b"`, and `repo / ".git"`.
    - `write_text` for `repo / "graph-wiki" / ".graph-wiki.yaml"` with `"plugins: []\n"`, and for both `pyproject.toml` files with minimal `[project]\nname="a"\n` content.
    - `monkeypatch.setenv("GRAPH_WIKI_WORKSPACE", str(repo / "graph-wiki"))`.

    Build the v1 fixture similarly but with wiki at `repo / "wiki"` and workspace_path passed to detect equal to `repo`.

    Implement the four test functions per the `<behavior>` block. The exact test names listed in `17-VALIDATION.md` must exist as discoverable pytest functions: `test_v2_layout_finds_repo_containers`, `test_workspace_path_excluded`, `test_v1_layout_guard`, `test_v2_synthetic_repo`. (`test_v2_layout_finds_repo_containers` and `test_v2_synthetic_repo` may be one assertion or two — they may overlap in coverage, but BOTH names must appear in the file.)

    Do NOT mock `resolve_wiki_and_repo` — the tests call `detect()` directly with synthetic paths. The `GRAPH_WIKI_WORKSPACE` monkeypatch is for any code path that internally invokes the resolver; the direct `detect()` calls don't need it but it's good hygiene.

    Use `monkeypatch.setenv` rather than `os.environ[...] = ...` — pytest auto-restores monkeypatch on test exit.

    All tests must be sync (no `async def`).
  </action>
  <verify>
    <automated>uv run --package vault-io pytest tests/test_detect_containers.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/vault-io/tests/test_detect_containers.py` exists.
    - `grep -c "^def test_" packages/vault-io/tests/test_detect_containers.py` returns at least 4.
    - `grep -n "test_v2_layout_finds_repo_containers\|test_workspace_path_excluded\|test_v1_layout_guard\|test_v2_synthetic_repo" packages/vault-io/tests/test_detect_containers.py` returns exactly 4 matches (one per required test name from VALIDATION.md).
    - `grep -n "monkeypatch.setenv" packages/vault-io/tests/test_detect_containers.py | grep -v '^#'` returns at least 1 match.
    - `grep -n "GRAPH_WIKI_WORKSPACE" packages/vault-io/tests/test_detect_containers.py | grep -v '^#'` returns at least 1 match.
    - `uv run --package vault-io pytest tests/test_detect_containers.py -x` exits 0 with all 4 tests passing.
    - The full vault-io suite passes: `uv run --package vault-io pytest -x` exits 0.
  </acceptance_criteria>
  <done>
    Four passing unit tests exist in `tests/test_detect_containers.py` covering the v2 positive path (packages found, workspace excluded) and the v1 guard (no self-exclusion). The synthetic fixtures use `tmp_path` + `monkeypatch.setenv` per the canonical pattern.
  </done>
</task>

</tasks>

<verification>
- `uv run --package vault-io pytest -x` passes (existing + new tests).
- `uv run --package vault-io python -c "from vault_io.detect_containers import detect; import inspect; assert 'workspace_path' in inspect.signature(detect).parameters"` exits 0.
- Static checks against `init_vault.py` and `detect_containers.py` per Task 1 acceptance_criteria.
</verification>

<success_criteria>
- `init_vault.main()` and `detect_containers.main()` resolve the repo via the second return value of `resolve_wiki_and_repo()` — `wiki.parent` is gone from both files.
- `detect()` accepts an optional `workspace_path` parameter and excludes the matching immediate subdir, with the D-11 guard preventing v1-layout self-exclusion.
- Synthetic-fixture tests cover both v2 (positive path: packages found, workspace excluded) and v1 (guard prevents self-exclusion).
- Phase requirements covered: WSRES-01 (resolver usage), WSRES-02 (`detect()` exclusion + guard), WSRES-03 (synthetic-fixture test coverage).
</success_criteria>

<output>
Create `.planning/phases/17-vault-io-bug-fixes/17-03-SUMMARY.md` when done.
</output>
