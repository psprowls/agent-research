---
phase: 17-vault-io-bug-fixes
plan: 04
type: execute
wave: 2
depends_on: [17-02]
files_modified:
  - .planning/phases/17-vault-io-bug-fixes/17-VERIFICATION.md
autonomous: false
requirements: [TOK-03]
tags: [vault-io, bedrock, live-re-stamp, operational-closure, wiki-self-update]

user_setup:
  - service: aws-bedrock
    why: "TOK-03 live re-stamp invokes real Bedrock CountTokens against `us.anthropic.claude-haiku-4-5-20251001-v1:0` in `us-east-1` to refresh the `tokens:` frontmatter on 35 pages in `~/Personal/wiki/deep-agents`."
    env_vars:
      - name: AWS_ACCESS_KEY_ID
        source: "Local AWS credentials (or IAM role via SSO) — must have `bedrock:CountTokens` for the target model in `us-east-1`."
      - name: AWS_SECRET_ACCESS_KEY
        source: "Same as AWS_ACCESS_KEY_ID."
    dashboard_config:
      - task: "Confirm Bedrock model access for `us.anthropic.claude-haiku-4-5-20251001-v1:0` in `us-east-1` is enabled for the account."
        location: "AWS Console → Bedrock → Model access (us-east-1 region)"

must_haves:
  truths:
    - "`~/Personal/wiki/deep-agents` git working tree is clean BEFORE the re-stamp run (no unrelated uncommitted state to be mixed into the re-stamp commit)."
    - "After running `uv run python -m vault_io.update_tokens` against `~/Personal/wiki/deep-agents`, `grep -rn '^tokens: 0' ~/Personal/wiki/deep-agents` returns 0 matches."
    - "The 35 pages that were previously stubbed at `tokens: 0` now have a non-zero integer `tokens:` value in their YAML frontmatter."
    - "A commit in the WIKI repo (NOT the deep-agents repo) captures the re-stamped pages."
    - "The diff transcript (before/after counts + a sample of re-stamped pages) is recorded in `.planning/phases/17-vault-io-bug-fixes/17-VERIFICATION.md` per the Phase 15 D-08/D-09 live-vault transcript pattern."
  artifacts:
    - path: .planning/phases/17-vault-io-bug-fixes/17-VERIFICATION.md
      provides: "Per-SC sections (#1-#5) citing unit/integration test pytest output AND the live re-stamp transcript for TOK-03."
      contains: "TOK-03"
  key_links:
    - from: "~/Personal/wiki/deep-agents/**/*.md frontmatter"
      to: "Bedrock CountTokens API"
      via: "uv run python -m vault_io.update_tokens (CLI invocation)"
      pattern: "tokens:"
    - from: ".planning/phases/17-vault-io-bug-fixes/17-VERIFICATION.md"
      to: "live re-stamp transcript"
      via: "embedded code block citing actual CLI output"
      pattern: "TOK-03"

reconciliations: []
---

<objective>
Operational closure: with the TOK fix from plan 17-02 in place, run `uv run python -m vault_io.update_tokens` against `~/Personal/wiki/deep-agents` to re-stamp the 35 pages currently at `tokens: 0`. Capture the before/after transcript in `17-VERIFICATION.md` per the Phase 15 live-vault pattern. Commit the resulting frontmatter updates in the wiki repo (separate git repo from deep-agents).

Purpose: Address TOK-03 — the phase does not close until the live wiki shows non-zero `tokens:` on every page that was previously stubbed.

Output: Updated `17-VERIFICATION.md` with the TOK-03 section populated (before count, transcript, after count, sample diff), and a commit in `~/Personal/wiki/deep-agents` capturing the page updates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-vault-io-bug-fixes/17-CONTEXT.md
@.planning/phases/17-vault-io-bug-fixes/17-RESEARCH.md
@.planning/phases/17-vault-io-bug-fixes/17-VALIDATION.md
@.planning/phases/17-vault-io-bug-fixes/17-02-PLAN.md
@packages/vault-io/src/vault_io/update_tokens.py

<interfaces>
<!-- TOK-03 operational contract -->

Target wiki: `~/Personal/wiki/deep-agents` (a separate git repo from `~/Personal/deep-agents`).

CLI: `uv run python -m vault_io.update_tokens` — invoked from anywhere; uses `_workspace.resolve_wiki_and_repo()` under the hood (which honors `GRAPH_WIKI_WORKSPACE`).

Defaults (from `update_tokens.py:34-35`):
- `DEFAULT_MODEL_ID = "us.anthropic.claude-haiku-4-5-20251001-v1:0"`
- `DEFAULT_REGION = "us-east-1"`

Idempotency: `update_tokens.py` strips any existing `tokens:` field before re-counting; re-running is safe.

Verification probe: `grep -rn "^tokens: 0" ~/Personal/wiki/deep-agents` returns matches iff stubbed pages remain. After the re-stamp this MUST return 0 matches.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Preflight checkpoint — confirm wiki repo clean + AWS creds available</name>
  <files>—</files>
  <read_first>
    - .planning/phases/17-vault-io-bug-fixes/17-RESEARCH.md (Open Question 3 — pre-condition: `git status` in wiki repo is clean)
    - .planning/phases/17-vault-io-bug-fixes/17-CONTEXT.md (D-08 — "If the re-stamp fails for operational reasons (AWS creds, network), the phase doesn't close")
  </read_first>
  <action>
    This is a CHECKPOINT task (type: checkpoint:human-verify) — no automated action. The executor pauses and presents the &lt;what-built&gt; / &lt;how-to-verify&gt; probes to the user. Wait for the resume signal before invoking Task 2. See the &lt;what-built&gt;, &lt;how-to-verify&gt;, and &lt;resume-signal&gt; sections below for the full checkpoint contract.
  </action>
  <verify>
    <human-check>User confirms preflight per the &lt;resume-signal&gt; protocol below.</human-check>
  </verify>
  <done>
    User has confirmed: (1) wiki repo working tree is clean, (2) AWS creds resolve to an identity with Bedrock CountTokens access in us-east-1, (3) BEFORE count from `grep -rn "^tokens: 0" ~/Personal/wiki/deep-agents | wc -l` is recorded.
  </done>
  <what-built>
    Verification probes that must pass BEFORE the live re-stamp runs:
    1. `cd ~/Personal/wiki/deep-agents &amp;&amp; git status --porcelain` — output must be empty (clean working tree, no untracked unrelated files).
    2. `aws sts get-caller-identity` (or equivalent) — confirms AWS creds are loaded in the current shell and resolve to an identity with Bedrock access in `us-east-1`.
    3. `grep -rn "^tokens: 0" ~/Personal/wiki/deep-agents | wc -l` — record the BEFORE count (expected ~35 per the source todo and SC#2).
  </what-built>
  <how-to-verify>
    User runs (or confirms output of) the three probes above.

    Expected outcomes:
    1. `git status --porcelain` in `~/Personal/wiki/deep-agents` returns empty stdout.
    2. `aws sts get-caller-identity` returns a JSON identity with a known account/role; `bedrock:CountTokens` for the target model in `us-east-1` is granted (model access enabled in the AWS Console → Bedrock → Model access page).
    3. The BEFORE count is captured (typically ~35; the exact number is recorded into `17-VERIFICATION.md` in Task 3).

    If ANY probe fails:
    - Dirty git tree → user commits or stashes unrelated changes in the wiki repo first; the phase pauses.
    - Missing creds → user authenticates (SSO refresh, profile switch, etc.); the phase pauses.
    - Model access denied → user enables the model in the Bedrock console; the phase pauses.

    None of these are automatable from inside the deep-agents repo — they require user action in the wiki repo and the AWS account.
  </how-to-verify>
  <acceptance_criteria>
    - User confirms in the resume signal: "wiki clean", "AWS creds resolved", and BEFORE count from `grep -rn "^tokens: 0" ~/Personal/wiki/deep-agents | wc -l` (a number, typically ~35).
    - If any precondition is unmet, the user describes the blocker and the plan pauses for remediation; do NOT proceed to Task 2 until all three probes pass.
  </acceptance_criteria>
  <resume-signal>Reply with: "preflight ok, before=<N>" where N is the integer from the BEFORE grep — OR describe the blocker.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Run live re-stamp against ~/Personal/wiki/deep-agents</name>
  <files>—</files>
  <read_first>
    - packages/vault-io/src/vault_io/update_tokens.py (confirm CLI entry, default model + region)
    - .planning/phases/17-vault-io-bug-fixes/17-VALIDATION.md (Per-Task Verification Map — TOK-03 row)
  </read_first>
  <action>
    Invoke the update_tokens CLI against the wiki repo. Capture the full stdout+stderr transcript to a temp file (for later embedding into VERIFICATION.md in Task 3).

    Commands to run, in order (each absolute or with cwd noted):

    1. From the deep-agents repo root: `uv run python -m vault_io.update_tokens 2>&amp;1 | tee /tmp/17-tok03-restamp.log`
       - This invokes the CLI; the resolver honors `GRAPH_WIKI_WORKSPACE` or the default workspace discovery to locate `~/Personal/wiki/deep-agents` as the target wiki.
       - If the resolver does NOT locate the wiki automatically (because the deep-agents workspace points to its own wiki location), set the env var explicitly for the invocation: `GRAPH_WIKI_WORKSPACE=~/Personal/wiki/deep-agents uv run python -m vault_io.update_tokens 2>&amp;1 | tee /tmp/17-tok03-restamp.log`. The executor reads `update_tokens.py` to confirm which discovery path applies and chooses the correct invocation. If `update_tokens.py` accepts a `--wiki` or `--vault` CLI argument, prefer that over env-var hijacking. Check the CLI surface in `update_tokens.py` before running.

    2. Probe AFTER count: `AFTER=$(grep -rn "^tokens: 0" ~/Personal/wiki/deep-agents | wc -l) &amp;&amp; echo "AFTER=$AFTER"`. Expected: `AFTER=0`.

    3. If `AFTER != 0`, the re-stamp partially failed (some pages were skipped or hit transient errors). Inspect `/tmp/17-tok03-restamp.log` for error rows. Do not proceed to Task 3 until `AFTER=0`. Re-running is idempotent (per the module docstring at `update_tokens.py`) — the executor may invoke the CLI again to retry failed pages. If retries still fail, surface the error log to the user and STOP — the phase does not close.

    4. Commit the wiki-side changes: `cd ~/Personal/wiki/deep-agents &amp;&amp; git add -A &amp;&amp; git diff --cached --stat &amp;&amp; git commit -m "$(cat <<'EOF'\nchore(tokens): re-stamp pages after Bedrock CountTokens fix\n\nFollows graph-wiki-agent phase 17 (vault-io-bug-fixes) TOK-01/02 fix.\n\nCo-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>\nEOF\n)"`. This commit lives in the wiki repo, NOT in deep-agents.

    Do NOT modify any code in this task — Task 2 is operational execution only. Do NOT commit anything in `~/Personal/deep-agents` during this task.
  </action>
  <verify>
    <automated>grep -rn "^tokens: 0" ~/Personal/wiki/deep-agents | wc -l</automated>
    <automated>cd ~/Personal/wiki/deep-agents &amp;&amp; git log -1 --pretty=%B | head -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "^tokens: 0" ~/Personal/wiki/deep-agents | wc -l` returns `0`.
    - `cd ~/Personal/wiki/deep-agents &amp;&amp; git log -1 --pretty=%s` returns a commit subject containing `re-stamp` (the re-stamp commit exists in the wiki repo HEAD).
    - `/tmp/17-tok03-restamp.log` exists and contains the full transcript of the run (used as the source for Task 3's transcript paste).
    - No new commits were created in `~/Personal/deep-agents` during this task (the re-stamp commit goes in the WIKI repo only).
  </acceptance_criteria>
  <done>
    The 35 stubbed pages in `~/Personal/wiki/deep-agents` have non-zero `tokens:` frontmatter; a single commit in the wiki repo captures the change; the run transcript is saved to `/tmp/17-tok03-restamp.log` for Task 3.
  </done>
</task>

<task type="auto">
  <name>Task 3: Populate 17-VERIFICATION.md with TOK-03 transcript + SC evidence</name>
  <files>.planning/phases/17-vault-io-bug-fixes/17-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/17-vault-io-bug-fixes/17-VALIDATION.md (per-SC verification map — gives the structure of the per-SC sections to fill in)
    - .planning/milestones/v1.2-phases/15-wiki-self-update/ (any 15-VERIFICATION.md or equivalent — for the live-vault transcript paste shape)
    - .planning/phases/17-vault-io-bug-fixes/17-CONTEXT.md (the five Success Criteria in ROADMAP.md §Phase 17; the verification doc must cover all five)
    - /tmp/17-tok03-restamp.log (the transcript captured in Task 2)
  </read_first>
  <action>
    Create or update `.planning/phases/17-vault-io-bug-fixes/17-VERIFICATION.md`. The file documents per-Success-Criterion evidence for ALL FIVE Phase 17 SCs (from ROADMAP.md §Phase 17). Structure the file as:

    ```
    # Phase 17 — Verification

    **Verified:** <today's date>

    ## SC#1 — `/graph-wiki:scan` reports 0 deleted entries for companion pages

    Evidence: <pytest output from `uv run --package vault-io pytest tests/test_scan_companion_fold.py -v`>
    (Optional bonus evidence: live `/graph-wiki:scan` output against `~/Personal/wiki/deep-agents` — record this if the executor or user runs it.)

    ## SC#2 — All 35 wiki pages previously at `tokens: 0` show non-zero token count

    BEFORE: `grep -rn "^tokens: 0" ~/Personal/wiki/deep-agents | wc -l` returned <N> (recorded at preflight, Task 1).

    Re-stamp transcript:
    ```
    <paste of /tmp/17-tok03-restamp.log, possibly truncated to first 20 + last 20 lines if very long>
    ```

    AFTER: `grep -rn "^tokens: 0" ~/Personal/wiki/deep-agents | wc -l` returns 0.

    Sample of re-stamped pages (3-5 examples): <paste of `git diff HEAD~1 -- '*.md' | grep -A 2 '^+tokens:' | head -30` from the wiki repo, or equivalent sampling>.

    Wiki-side commit: <SHA from `cd ~/Personal/wiki/deep-agents && git rev-parse HEAD`>.

    ## SC#3 — `detect_containers --json` returns repo-root containers under v2 layout

    Evidence: <pytest output from `uv run --package vault-io pytest tests/test_detect_containers.py -v`>

    ## SC#4 — The workspace dir does not appear in its own layout block as a `docs` container

    Evidence: <same test file output covers this — specifically `test_workspace_path_excluded`>.

    ## SC#5 — Unit and integration tests pass under `uv run --package vault-io pytest`

    Evidence:
    - Unit suite: `uv run --package vault-io pytest` → exit 0, <K> tests passing.
    - Integration suite (gated): `GRAPH_WIKI_RUN_INTEGRATION=1 uv run --package vault-io pytest -m integration` → exit 0, <M> tests passing.

    ---

    *Phase 17 verification complete: <today's date>.*
    ```

    Fill in the placeholders with the actual values:
    - `<today's date>` — current ISO date.
    - `<N>` — the BEFORE count from Task 1's resume signal.
    - The paste of `/tmp/17-tok03-restamp.log` — truncate to first 20 and last 20 lines if the log is longer than ~80 lines; include the truncation marker `... (N lines elided) ...` between them.
    - The sample diff — pull 3-5 pages from the wiki commit using `git diff HEAD~1 -- '*.md' | grep -B 2 -A 2 '^+tokens:' | head -40` in the wiki repo.
    - The wiki commit SHA — `cd ~/Personal/wiki/deep-agents &amp;&amp; git rev-parse HEAD`.
    - The pytest output for SC#1, SC#3, SC#4, SC#5 — run the commands and paste the relevant summary lines (don't paste the full verbose output; the test summary line and pass count are sufficient).

    If a prior partial version of `17-VERIFICATION.md` exists from earlier execution (the file may have a draft created during a previous attempt), overwrite it with the full per-SC structure above.
  </action>
  <verify>
    <automated>test -f .planning/phases/17-vault-io-bug-fixes/17-VERIFICATION.md &amp;&amp; echo OK</automated>
    <automated>grep -c "^## SC#" .planning/phases/17-vault-io-bug-fixes/17-VERIFICATION.md</automated>
    <automated>grep -n "TOK-03\|re-stamp" .planning/phases/17-vault-io-bug-fixes/17-VERIFICATION.md | grep -v '^#' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/17-vault-io-bug-fixes/17-VERIFICATION.md` exists.
    - `grep -c "^## SC#" .planning/phases/17-vault-io-bug-fixes/17-VERIFICATION.md` returns exactly 5 (one section per SC).
    - The SC#2 section includes BOTH a BEFORE count AND the explicit string "AFTER" with a 0 result; the embedded transcript references real CLI output (not a placeholder).
    - The SC#2 section cites the wiki-side commit SHA.
    - The SC#5 section cites either `uv run --package vault-io pytest` exit status or the test-pass summary line.
    - `grep -rn "<placeholder>\|<N>\|<paste of\|<SHA from" .planning/phases/17-vault-io-bug-fixes/17-VERIFICATION.md` returns 0 matches (all placeholders filled in with real values).
  </acceptance_criteria>
  <done>
    `17-VERIFICATION.md` contains per-SC evidence for all five Phase 17 Success Criteria. The TOK-03 section embeds the actual re-stamp transcript, BEFORE/AFTER counts, sample diffs, and wiki-side commit SHA — the live-vault transcript pattern from Phase 15 D-08/D-09 is honored.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AWS Bedrock API | The `count_tokens` call sends wiki page content to AWS Bedrock for tokenization. Bedrock CountTokens does NOT invoke model inference and does NOT return generated text — only an integer count. Content cannot influence anything beyond the count value. |
| Wiki repo git boundary | The re-stamp commit lands in `~/Personal/wiki/deep-agents` (a separate repo). No deep-agents repo state is modified during the re-stamp. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Information disclosure | Wiki page content sent to Bedrock CountTokens | accept | Wiki content is non-sensitive (project documentation, no PII or secrets); CountTokens does not echo content back; same data already flows to Bedrock via other graph-wiki-agent calls. |
| T-17-02 | Tampering | Dirty wiki repo state mixed into re-stamp commit | mitigate | Preflight checkpoint (Task 1) verifies `git status --porcelain` is empty before invocation; phase pauses if the wiki repo is dirty. |
| T-17-03 | Repudiation | Unauthorized re-stamp commit attribution | mitigate | Commit is co-authored with `Claude Opus 4.7 (1M context) <noreply@anthropic.com>` per repo convention; commit message names the originating phase (17) for traceability. |
| T-17-04 | Denial of service | Bedrock rate-limiting or transient failure interrupts re-stamp | accept | `update_tokens.py` is idempotent (strips existing `tokens:` field before re-counting); Task 2 documents the retry path; partial failures leave the wiki in a recoverable state (some pages stamped, others still 0 — re-running converges). |
</threat_model>

<verification>
- TOK-03 SC: `grep -rn "^tokens: 0" ~/Personal/wiki/deep-agents | wc -l` returns `0`.
- Wiki repo has exactly one new commit since the start of the phase reflecting the re-stamp.
- `17-VERIFICATION.md` exists with 5 SC sections and embedded TOK-03 transcript.
</verification>

<success_criteria>
- Live wiki at `~/Personal/wiki/deep-agents` has no pages stuck at `tokens: 0`.
- The re-stamp commit lives in the WIKI repo (not in deep-agents).
- `17-VERIFICATION.md` documents evidence for all 5 Phase 17 Success Criteria, with the TOK-03 transcript pattern matching Phase 15 D-08/D-09.
- Phase requirement covered: TOK-03 (operational closure).
</success_criteria>

<output>
Create `.planning/phases/17-vault-io-bug-fixes/17-04-SUMMARY.md` when done.
</output>
