---
phase: 18-plugin-command-rename
plan: 06
type: execute
wave: 3
depends_on: [18-01, 18-02, 18-03, 18-04, 18-05]
files_modified:
  - scripts/check-brand.sh
  - .brand-grep-allow
  - .planning/todos/pending/2026-05-19-rename-graph-wiki-init-command-to-init-wiki.md
  - .planning/todos/resolved/2026-05-19-rename-graph-wiki-init-command-to-init-wiki.md
autonomous: true
requirements: [CMD-03]
must_haves:
  truths:
    - "scripts/check-brand.sh greps for graph-wiki:init, wiki_init, and def init( (in cli.py); fails CI on any hit not covered by .brand-grep-allow"
    - ".brand-grep-allow allowlists ONLY the Phase 18 meta dir and the folded todo (in resolved/) — no broader exemptions"
    - "Red-then-green sanity: injecting /graph-wiki:init into a random non-allowlisted file makes the gate fail; removing it makes the gate pass"
    - "Folded todo is moved via git mv to .planning/todos/resolved/"
  artifacts:
    - path: "scripts/check-brand.sh"
      provides: "Extended brand-gate enforcing the rename"
      contains: "graph-wiki:init"
    - path: ".brand-grep-allow"
      provides: "Allowlist with narrow Phase 18 exemptions"
      contains: "phases/18-plugin-command-rename"
  key_links:
    - from: "scripts/check-brand.sh"
      to: ".brand-grep-allow"
      via: "grep -vF -f filter"
      pattern: "grep -vF -f"
---

<objective>
Lock in the rename by extending `scripts/check-brand.sh` so it fails CI on any reintroduction of the old slugs `/graph-wiki:init`, `wiki_init` (MCP tool), or `def init(` (Typer subcommand in `cli.py`). Update `.brand-grep-allow` with narrow exemptions for files that legitimately retain old-name references (this phase's `18-CONTEXT.md` + `18-DISCUSSION-LOG.md` as quoted documentation, plus the folded todo file which retains the old slug in its filename). Run a red-then-green sanity check. Fold the resolved todo by moving it to `.planning/todos/resolved/`.

Purpose: D-06 step 6, the final step in the staged cutover. Closes the brand-gate-enforcement portion of CMD-03 per D-05. After this commit, future regressions are caught at the CI / pre-commit boundary.

Output: One commit, brand-gate extended + sanity-tested, allowlist surgically expanded, folded todo moved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-plugin-command-rename/18-CONTEXT.md
@CLAUDE.md
@scripts/check-brand.sh
@.brand-grep-allow

<interfaces>
Current `scripts/check-brand.sh` (lines 37–40) runs a single `grep -rEl` over `packages/ agents/ plugins/ .planning/ CLAUDE.md` for `lattice|LATTICE|lattice_workspace|lattice_wiki_core`, filters via `grep -vF -f .brand-grep-allow`, fails on unallowlisted hits.

Three checks to add (executor may choose the exact structure; acceptance criteria pins observable behavior):

CHECK 2 — CMD-rename pattern:
- Extended regex: `graph-wiki:init\b|\bwiki_init\b`
- Scope: `packages/ agents/ plugins/ .planning/ scripts/ docs/ README.md CLAUDE.md`
- Filter through `.brand-grep-allow` with the same BSD-grep blank-line workaround
- Fail message contains the literal string `BRAND-CMD FAIL`
- Exclude `__pycache__` and `*.pyc` (same as lattice check)

CHECK 3 — Typer `def init(` regression:
- Regex: `^\s*def init\(`
- Scope: ONLY `agents/code-wiki-agent/src/code_wiki_agent/cli.py`
- No allowlist filter
- Fail message contains the literal string `BRAND-CMD-CLI FAIL`
- Use `2>/dev/null` so missing file does not error

`.brand-grep-allow` additions — two surgical entries:
1. Path fragment `.planning/phases/18-plugin-command-rename/` (covers both 18-CONTEXT.md and 18-DISCUSSION-LOG.md)
2. Path of the folded todo. Initially `.planning/todos/pending/2026-05-19-rename-graph-wiki-init-command-to-init-wiki.md`; Task 4 updates this to `.planning/todos/resolved/...` after the git mv.

Existing allowlist semantic: `grep -vF` literal substring match. Path fragments work under that semantic.

Red-then-green sanity (Task 3):
- RED: create temp file `scripts/_brand_gate_test.tmp` containing `/graph-wiki:init`. Run `bash scripts/check-brand.sh`. Expect exit non-zero.
- GREEN: delete temp file. Run `bash scripts/check-brand.sh`. Expect exit 0.
- Temp file must not be committed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend scripts/check-brand.sh with three CMD-rename enforcement checks</name>
  <files>scripts/check-brand.sh</files>
  <read_first>
    - scripts/check-brand.sh (full file — current structure, BSD-grep blank-line fix, exclusions)
    - .brand-grep-allow (full file — semantic and rationale-comment convention)
    - .planning/phases/18-plugin-command-rename/18-CONTEXT.md (D-05)
  </read_first>
  <action>
    Step 1: Preserve the existing `lattice` check unchanged. After its `if [ -n "$HITS" ]` block ends, add CHECK 2: a `HITS2=$(grep -rEl --exclude-dir=__pycache__ --exclude='*.pyc' 'graph-wiki:init\b|\bwiki_init\b' packages/ agents/ plugins/ .planning/ scripts/ docs/ README.md CLAUDE.md 2>/dev/null | grep -vF -f <(grep -vE '^[[:space:]]*(#|$)' "$ALLOWLIST") || true)` style invocation. On non-empty `$HITS2`: print the hits, print a message containing the literal `BRAND-CMD FAIL`, `exit 1`.

    Step 2: Add CHECK 3: `CLI_HITS=$(grep -nE '^\s*def init\(' agents/code-wiki-agent/src/code_wiki_agent/cli.py 2>/dev/null || true)`. On non-empty `$CLI_HITS`: print the hit, print a message containing the literal `BRAND-CMD-CLI FAIL`, `exit 1`. No allowlist filter — the file has zero `def init(` lines post-rename.

    Step 3: Replace the trailing `echo "BRAND-04 OK: zero unallowlisted hits"` with a message that names all three checks passing. Exact wording at executor discretion; minimum requirement: still contains the word `OK` and exits 0.

    Step 4: Preserve `set -euo pipefail` at top; preserve the existing `--exclude-dir=__pycache__` `--exclude='*.pyc'` flags for both `grep -rEl` invocations; preserve the `|| true` idiom for "no hits is OK".

    Step 5: Syntax-check: `bash -n scripts/check-brand.sh` exits 0.
  </action>
  <verify>
    <automated>bash -n scripts/check-brand.sh && grep -q 'graph-wiki:init' scripts/check-brand.sh && grep -q 'wiki_init' scripts/check-brand.sh && grep -q 'BRAND-CMD' scripts/check-brand.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/check-brand.sh` exits 0.
    - `grep -c 'graph-wiki:init' scripts/check-brand.sh` returns at least 1.
    - `grep -cE '\bwiki_init\b' scripts/check-brand.sh` returns at least 1.
    - `grep -c 'def init' scripts/check-brand.sh` returns at least 1 (the CHECK 3 regex literal).
    - `grep -c 'agents/code-wiki-agent/src/code_wiki_agent/cli.py' scripts/check-brand.sh` returns at least 1.
    - `grep -cE 'lattice\|LATTICE' scripts/check-brand.sh` returns at least 1 (existing lattice check still present).
    - `grep -c 'BRAND-CMD' scripts/check-brand.sh` returns at least 1 (new failure message present).
    - `grep -c '__pycache__' scripts/check-brand.sh` returns at least 2 (excludes preserved across both grep invocations).
    - `set -euo pipefail` line still present near top: `grep -c 'set -euo pipefail' scripts/check-brand.sh` returns 1.
  </acceptance_criteria>
  <done>
    Brand-gate script syntax-clean with three CMD-rename checks added; existing lattice check and BSD-grep workaround preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add surgical exemptions to .brand-grep-allow</name>
  <files>.brand-grep-allow</files>
  <read_first>
    - .brand-grep-allow (full file — existing Phase 12/13/14 sections show the path-fragment + rationale-comment convention)
    - .planning/phases/18-plugin-command-rename/18-CONTEXT.md (D-05 — "narrow exemptions only")
  </read_first>
  <action>
    Step 1: Append a new section to `.brand-grep-allow` (do NOT modify existing entries). Mirror the format of the existing Phase 12 / 13 / 14 sections: header comment block naming the phase, then path-fragment entries each preceded by a `# rationale: …` comment.

    Step 2: Add exactly TWO path-fragment entries:

    Entry A — Phase 18 own meta files. Comment line above: `# rationale: Phase 18 meta — quoted documentation of the /graph-wiki:init → /graph-wiki:bootstrap rename direction.` Path fragment line: `.planning/phases/18-plugin-command-rename/`

    Entry B — Folded todo at its CURRENT location (pre-move). Comment line above: `# rationale: Phase 18 folded todo — filename slug references the older "init-wiki" rename direction; this file is moved to resolved/ in Task 4 of plan 18-06, and entry B is updated there.` Path fragment line: `.planning/todos/pending/2026-05-19-rename-graph-wiki-init-command-to-init-wiki.md`

    Step 3: Do NOT add any broader exemptions. No `.planning/phases/` parent wildcard, no `.planning/todos/` parent wildcard, no agent-package wildcards.

    Step 4: After editing, sanity-grep: `grep -cF '.planning/phases/18-plugin-command-rename/' .brand-grep-allow` returns exactly 1.
  </action>
  <verify>
    <automated>grep -qF '.planning/phases/18-plugin-command-rename/' .brand-grep-allow && grep -qF '2026-05-19-rename-graph-wiki-init-command-to-init-wiki' .brand-grep-allow</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cF '.planning/phases/18-plugin-command-rename/' .brand-grep-allow` returns exactly 1.
    - `grep -cF '2026-05-19-rename-graph-wiki-init-command-to-init-wiki.md' .brand-grep-allow` returns exactly 1.
    - The two new path-fragment lines each have a `# rationale:` comment immediately above: `grep -B1 '.planning/phases/18-plugin-command-rename/' .brand-grep-allow | grep -c '# rationale'` returns at least 1.
    - No accidental broader exemption: `grep -cE '^\.planning/phases/$|^\.planning/todos/$' .brand-grep-allow` returns 0.
    - Existing entries (e.g., `packages/vault-io/tests/fixtures/round-trip-vault/`, `.planning/phases/14-plugin-port-m3b/`) are still present (unchanged): `grep -c '.planning/phases/14-plugin-port-m3b/' .brand-grep-allow` returns at least 1.
  </acceptance_criteria>
  <done>
    Two narrow path-fragment exemptions added with rationale comments; no broader exemptions; existing allowlist entries untouched.
  </done>
</task>

<task type="auto">
  <name>Task 3: Red-then-green sanity check of the extended brand-gate</name>
  <files>(no source modifications; temp file created and deleted during the task)</files>
  <read_first>
    - scripts/check-brand.sh (verify the script as edited in Task 1)
    - .brand-grep-allow (verify the allowlist as edited in Task 2)
  </read_first>
  <action>
    Step 1: GREEN baseline. Run `bash scripts/check-brand.sh` from repo root. Expect exit 0. Capture output. If the gate already fails at this point, STOP — earlier plans left residue. Surface the failure to the orchestrator naming the offending files.

    Step 2: RED injection. Create temp file at a non-allowlisted path. Recommended path: `scripts/_brand_gate_test.tmp`. Write the literal string `/graph-wiki:init` into it (single line, single occurrence is enough). Do NOT add `.tmp` to `.gitignore`; do NOT commit. Run `bash scripts/check-brand.sh`. Expect exit non-zero. Capture output and confirm the failure message includes `BRAND-CMD FAIL` and names `scripts/_brand_gate_test.tmp`.

    Step 3: GREEN recovery. Delete `scripts/_brand_gate_test.tmp` (e.g., `rm scripts/_brand_gate_test.tmp`). Run `bash scripts/check-brand.sh`. Expect exit 0.

    Step 4: Additional RED check for CHECK 3 (Typer `def init`). Use a non-destructive `git stash` + `git stash pop` pattern (WARNING 2 fix per plan-checker feedback) — `git checkout --` would silently wipe any uncommitted edits in cli.py.

      (a) Confirm clean working tree for cli.py before injection: `git status --porcelain agents/code-wiki-agent/src/code_wiki_agent/cli.py` must return empty. If non-empty, STOP — there are uncommitted edits that the injection-then-revert pattern would risk; surface to orchestrator and skip the CHECK 3 RED phase entirely (the static `grep -E 'def init' scripts/check-brand.sh` assertion in Task 1's acceptance criteria already proves the pattern is present in the gate).

      (b) Inject: append a single line `def init(): pass` (no indent, no decorator) to the END of `agents/code-wiki-agent/src/code_wiki_agent/cli.py` via `echo 'def init(): pass' >> agents/code-wiki-agent/src/code_wiki_agent/cli.py`. (Appending to end is safer than editing the existing `def bootstrap(` line.)

      (c) Run `bash scripts/check-brand.sh`. Expect exit non-zero with a message containing `BRAND-CMD-CLI FAIL`. Capture the output.

      (d) Revert via stash, NOT checkout: `git stash push -m "phase18-06-task3-red-injection" -- agents/code-wiki-agent/src/code_wiki_agent/cli.py`. This stores the injection in a stash entry rather than discarding it. Run `git stash drop` immediately afterward to discard the stash entry — the injection is now gone from both working tree and stash.

      (e) Confirm clean: `git status --porcelain agents/code-wiki-agent/src/code_wiki_agent/cli.py` returns empty AND `grep -cE '^\s*def init\(' agents/code-wiki-agent/src/code_wiki_agent/cli.py` returns 0.

      (f) Run `bash scripts/check-brand.sh` once more. Expect exit 0.

      Why `stash push` + `stash drop` instead of `checkout --`: if the executor accidentally has unrelated uncommitted edits in cli.py at the start of Step 4, `git checkout --` discards them silently; `stash push` either stashes them with the injection (recoverable via `git stash pop`) or fails cleanly. If `stash drop` fails for any reason (rare — the stash entry was just created), abort the task with a clear error message rather than continuing and risk leaving residual state.

    Step 5: After both red-then-green cycles pass, confirm `git status --porcelain` shows no residual untracked or modified files from the sanity check (the temp `.tmp` file is deleted; the CHECK 3 injection was reverted).

    Step 6: This task creates NO commit. Tasks 1–4 fold into the single commit at the end of Task 4.
  </action>
  <verify>
    <automated>bash scripts/check-brand.sh && [ -z "$(git status --porcelain scripts/_brand_gate_test.tmp 2>/dev/null)" ] && [ -z "$(git status --porcelain agents/code-wiki-agent/src/code_wiki_agent/cli.py 2>/dev/null)" ]</automated>
  </verify>
  <acceptance_criteria>
    - Final `bash scripts/check-brand.sh` (after all injections reverted) exits 0 and emits a message containing `OK`.
    - During the red phase (temp file present), the gate exits non-zero with output containing `BRAND-CMD FAIL`.
    - During the red phase for the def-init injection, the gate exits non-zero with output containing `BRAND-CMD-CLI FAIL`.
    - `git status --porcelain scripts/_brand_gate_test.tmp` returns empty (file deleted).
    - `git status --porcelain agents/code-wiki-agent/src/code_wiki_agent/cli.py` returns empty (revert successful; no diff vs HEAD after Task 4's todo move which does not touch this file).
    - The Task 3 sanity check produces NO commit on its own.
  </acceptance_criteria>
  <done>
    Red-then-green observed for both CHECK 2 (slug regex) and CHECK 3 (def-init regex); no residue left in the working tree.
  </done>
</task>

<task type="auto">
  <name>Task 4: Move folded todo to resolved/, update .brand-grep-allow entry, commit Tasks 1+2+4</name>
  <files>.planning/todos/pending/2026-05-19-rename-graph-wiki-init-command-to-init-wiki.md, .planning/todos/resolved/2026-05-19-rename-graph-wiki-init-command-to-init-wiki.md, .brand-grep-allow</files>
  <read_first>
    - .planning/todos/pending/2026-05-19-rename-graph-wiki-init-command-to-init-wiki.md (full file — the file content is moved verbatim; no body edits)
    - .planning/phases/18-plugin-command-rename/18-CONTEXT.md ("Folded Todos" section: filename slug retained, body unchanged, file moves to resolved/)
    - .brand-grep-allow (the entry B added in Task 2 needs the path updated from pending/ to resolved/)
  </read_first>
  <action>
    Step 1: Confirm `.planning/todos/resolved/` directory exists. If not, create it: `mkdir -p .planning/todos/resolved`. (It usually exists from prior phase work; this is a safety net.)

    Step 2: Run `git mv .planning/todos/pending/2026-05-19-rename-graph-wiki-init-command-to-init-wiki.md .planning/todos/resolved/2026-05-19-rename-graph-wiki-init-command-to-init-wiki.md`. Plain `mv` is forbidden — must record as a rename so git log preserves provenance.

    Step 3: Update `.brand-grep-allow` entry B (added in Task 2). Change the path fragment line from `.planning/todos/pending/2026-05-19-rename-graph-wiki-init-command-to-init-wiki.md` to `.planning/todos/resolved/2026-05-19-rename-graph-wiki-init-command-to-init-wiki.md`. Update the corresponding rationale comment to drop the "moved to resolved/ in Task 4" parenthetical (the move is now done). Recommended new rationale: `# rationale: Phase 18 folded todo — filename slug references the older "init-wiki" rename direction (now superseded by /graph-wiki:bootstrap); content unchanged for traceability.`

    Step 4: Do NOT edit the body of the todo file. The filename and content reference the old direction (`init-wiki`); that's intentional for historical traceability per the "Folded Todos" decision in 18-CONTEXT.md.

    Step 5: Final gate run. From repo root: `bash scripts/check-brand.sh`. Expect exit 0 with the OK message. If non-zero, the allowlist edit did not correctly cover the moved file path — diagnose and fix before commit.

    Step 6: Per-commit gate. `uv run --package code-wiki-agent pytest agents/code-wiki-agent/tests/ -m "not integration" -q`. Expect exit 0 (no Python source changed in this entire plan, so the suite is unaffected; this is a discipline check matching D-06's per-commit gate).

    Step 7: Stage and commit Tasks 1 + 2 + 4 changes (Task 3 produced no diff). `git add scripts/check-brand.sh .brand-grep-allow .planning/todos/pending/2026-05-19-rename-graph-wiki-init-command-to-init-wiki.md .planning/todos/resolved/2026-05-19-rename-graph-wiki-init-command-to-init-wiki.md`. Commit subject: `chore(18): extend brand-gate to enforce /graph-wiki:bootstrap rename; fold todo`. Body references CMD-03 + D-05 + D-06 step 6; notes the red-then-green sanity result from Task 3; notes the git mv of the folded todo.

    Step 8: Confirm `git log -1 --name-status` shows the todo rename as an `R{score}` entry plus modifications to `scripts/check-brand.sh` and `.brand-grep-allow`.
  </action>
  <verify>
    <automated>test ! -e .planning/todos/pending/2026-05-19-rename-graph-wiki-init-command-to-init-wiki.md && test -e .planning/todos/resolved/2026-05-19-rename-graph-wiki-init-command-to-init-wiki.md && grep -qF '.planning/todos/resolved/2026-05-19-rename-graph-wiki-init-command-to-init-wiki.md' .brand-grep-allow && bash scripts/check-brand.sh</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/todos/pending/2026-05-19-rename-graph-wiki-init-command-to-init-wiki.md` no longer exists.
    - File `.planning/todos/resolved/2026-05-19-rename-graph-wiki-init-command-to-init-wiki.md` exists.
    - `git log -1 --name-status` shows a rename line of form `R{score}\t.planning/todos/pending/...md\t.planning/todos/resolved/...md`.
    - `.brand-grep-allow` contains the resolved/ path: `grep -cF '.planning/todos/resolved/2026-05-19-rename-graph-wiki-init-command-to-init-wiki.md' .brand-grep-allow` returns 1.
    - `.brand-grep-allow` no longer contains the pending/ path: `grep -cF '.planning/todos/pending/2026-05-19-rename-graph-wiki-init-command-to-init-wiki.md' .brand-grep-allow` returns 0.
    - `bash scripts/check-brand.sh` exits 0 with output containing `OK`.
    - `uv run --package code-wiki-agent pytest agents/code-wiki-agent/tests/ -m "not integration"` exits 0.
    - Single commit subject contains `chore(18)` and the words `brand-gate` and `bootstrap`.
    - The todo body content is byte-identical to its pre-move state: `git log -1 --follow -p .planning/todos/resolved/2026-05-19-rename-graph-wiki-init-command-to-init-wiki.md` shows the rename with `similarity index 100%` (or equivalent indicator of unchanged content).
  </acceptance_criteria>
  <done>
    Brand-gate green on clean tree; folded todo moved via git mv; allowlist updated to track resolved/ path; per-commit pytest gate green; single commit lands. Phase 18 complete — all three SCs satisfied modulo SC#3's manual smoke check (owned by verifier / UAT).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI / pre-commit gate → repo state | `scripts/check-brand.sh` is the enforcement boundary preventing rename regression |
| Allowlist → gate scope | `.brand-grep-allow` is the only place broader scope can leak in; surgical entries only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-18 | Tampering | Brand-gate could be silently weakened (over-broad allowlist) | mitigate | Acceptance criteria Task 2 asserts no parent-directory wildcards; rationale comments make intent reviewable. |
| T-18-19 | Denial of Service | Brand-gate false positives that block legitimate commits | mitigate | Word-boundary regexes; Task 3 red-then-green sanity proves the gate is tight enough to clear the green baseline. |
| T-18-20 | Tampering | Folded todo body edited unintentionally during move | mitigate | Acceptance criteria Task 4 requires `similarity index 100%` (byte-identical) on the git rename. |
| T-18-21 | Repudiation | Future regression with no audit trail | mitigate | Brand-gate exit messages name specific files; CI failure surfaces the exact offending path. |
| T-18-22 | Elevation of Privilege | Bypassing the gate via `--no-verify` | accept | Out of scope for this plan; gate-bypass discipline is a team-norm concern. CLAUDE.md "Never skip hooks (--no-verify)" rule documents the prohibition. |
</threat_model>

<verification>
- `bash scripts/check-brand.sh` exits 0 on the clean post-rename tree.
- Red-then-green observed for both CHECK 2 and CHECK 3 in Task 3.
- Folded todo moved via git mv with `similarity index 100%`.
- `.brand-grep-allow` surgical: two new entries only, both with rationale comments.
- Per-commit pytest gate green.
- One commit lands.
</verification>

<success_criteria>
- Brand-gate extended with three CMD-rename checks; teeth verified via red-then-green sanity.
- Allowlist surgically expanded; no parent-directory wildcards.
- Folded todo moved via git mv with content unchanged.
- CMD-03 brand-gate-enforcement portion closed.
- Phase 18 done: all three SCs satisfied at the artifact level; manual SC#3 smoke check (typing `/init` in Claude Code) deferred to the verifier / UAT pass.
</success_criteria>

<output>
Create `.planning/phases/18-plugin-command-rename/18-06-SUMMARY.md` when done. Capture: scripts/check-brand.sh diff summary (new lines added; exit-message strings); .brand-grep-allow diff summary (2 path-fragment entries added with rationale); red-then-green sanity transcript (exit codes from both phases of each CHECK); folded-todo rename `git log --name-status` line; final `bash scripts/check-brand.sh` output; commit SHA.
</output>
