---
phase: 18-plugin-command-rename
plan: 04
type: execute
wave: 2
depends_on: [18-01, 18-02, 18-03]
files_modified:
  - plugins/graph-wiki/README.md
  - plugins/graph-wiki/CLAUDE.md
  - plugins/graph-wiki/agents/linter.md
  - plugins/graph-wiki/commands/scan.md
  - plugins/graph-wiki/commands/bootstrap.md
  - plugins/graph-wiki/skills/graph-wiki/SKILL.md
  - plugins/graph-wiki/skills/graph-wiki/README.md
  - plugins/graph-wiki/skills/graph-wiki/references/cross-tool-setup.md
  - plugins/graph-wiki/skills/graph-wiki/references/detection-workflow.md
  - packages/vault-io/src/vault_io/scan_monorepo.py
  - packages/vault-io/src/vault_io/lint/container.py
autonomous: true
requirements: [CMD-03]
must_haves:
  truths:
    - "Zero `/graph-wiki:init\\b` literal references remain in plugins/, packages/, agents/, scripts/, docs/, README.md, or repo-root CLAUDE.md"
    - "The plugin README has a one-sentence reinstall note for prior installs (Q2 resolution)"
    - "Active-source error/prompt strings emitted by vault-io (`scan_monorepo.py`, `lint/container.py`) tell users to run `/graph-wiki:bootstrap`, not `/graph-wiki:init`"
  artifacts:
    - path: "plugins/graph-wiki/README.md"
      provides: "Plugin top-level docs with renamed slash command"
      contains: "/graph-wiki:bootstrap"
  key_links:
    - from: "packages/vault-io/src/vault_io/lint/container.py:35"
      to: "/graph-wiki:bootstrap user-typed command"
      via: "Error message string"
      pattern: "/graph-wiki:bootstrap"
    - from: "packages/vault-io/src/vault_io/scan_monorepo.py:1157"
      to: "/graph-wiki:bootstrap user-typed command"
      via: "Print-statement hint string"
      pattern: "/graph-wiki:bootstrap"
---

<objective>
Sweep all 11 active-source consumer references to `/graph-wiki:init` and replace them with `/graph-wiki:bootstrap`. After this plan, grep across `plugins/`, `packages/`, `agents/`, `scripts/`, `docs/`, `README.md`, and repo-root `CLAUDE.md` for `\bgraph-wiki:init\b` must return zero hits. Historical `.planning/` references are swept by plan 18-05 in parallel.

Purpose: D-06 step 4 in the staged cutover. Closes the active-source portion of CMD-03. Also resolves open question Q2 (yes, add a one-sentence reinstall note to the plugin README — recommended in the planning_context).

This plan depends on plans 18-01 / 18-02 / 18-03 so that the renamed command file (`bootstrap.md`) and renamed CLI/MCP surfaces exist before downstream consumer references are pointed at them. Same wave as plan 18-05 (historical sweep) which touches a disjoint file set, so they can run in parallel.

Output: One commit, repo-wide grep clean of the old slug across active-source paths, vault-io error messages tell users the new command name.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-plugin-command-rename/18-CONTEXT.md
@CLAUDE.md
@plugins/graph-wiki/CLAUDE.md
@plugins/graph-wiki/README.md
@plugins/graph-wiki/commands/scan.md
@packages/vault-io/src/vault_io/lint/container.py
@packages/vault-io/src/vault_io/scan_monorepo.py

<interfaces>
Exact in-scope hit list (from `grep -rln '\bgraph-wiki:init\b' plugins/ packages/ agents/ scripts/ docs/ README.md CLAUDE.md` taken 2026-05-19):

1. `plugins/graph-wiki/README.md` — 2 hits (lines 30, 63)
2. `plugins/graph-wiki/CLAUDE.md` — 1 hit (line 74, namespacing example list)
3. `plugins/graph-wiki/agents/linter.md` — 1 hit (line 39, prose mention)
4. `plugins/graph-wiki/commands/scan.md` — 3 hits (lines 8, 46, 67)
5. `plugins/graph-wiki/commands/bootstrap.md` — already renamed in plan 18-01; should already be clean (verify; do not re-edit if grep is already 0)
6. `plugins/graph-wiki/skills/graph-wiki/README.md` — 1 hit (line 25, slash-command table)
7. `plugins/graph-wiki/skills/graph-wiki/SKILL.md` — 1 hit (line 105, command table)
8. `plugins/graph-wiki/skills/graph-wiki/references/cross-tool-setup.md` — 2 hits (lines 56, 58)
9. `plugins/graph-wiki/skills/graph-wiki/references/detection-workflow.md` — 4 hits (lines 7, 17, 36, 41)
10. `packages/vault-io/src/vault_io/scan_monorepo.py` — 1 hit (line 1157, print-statement hint)
11. `packages/vault-io/src/vault_io/lint/container.py` — 1 hit (line 35, lint-error string)

Total active-source hits: 16 across 10 files (plus the renamed `bootstrap.md` which should already be clean).

Transformation: word-boundary replace `/graph-wiki:init` → `/graph-wiki:bootstrap`. Word-boundary so that `/graph-wiki:ingest` is NOT a false positive (it contains `init`-prefix but as part of `ingest`); regex `\bgraph-wiki:init\b` does not match `ingest`.

NOTE on the two Python files (`scan_monorepo.py`, `lint/container.py`): these are runtime user-facing strings, not just docs. After the sweep, when a user runs `/graph-wiki:lint` and a container is missing its layout block, the error reads `"no layout block found in CLAUDE.md (run /graph-wiki:bootstrap)"`. When `/graph-wiki:scan` warns about layout drift, the print hint reads `"(Re-run /graph-wiki:bootstrap or hand-edit the layout block to update.)"`. These messages MUST match the surface the user actually types.

NOTE on `init_vault` (Python identifier): the function name `init_vault` and script `init_vault.py` are out of scope per D-02. Do NOT rename them anywhere. The word-boundary regex `\bgraph-wiki:init\b` does not match `init_vault` (matches only the slash-command literal).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep active-source markdown + Python references; add reinstall note</name>
  <files>plugins/graph-wiki/README.md, plugins/graph-wiki/CLAUDE.md, plugins/graph-wiki/agents/linter.md, plugins/graph-wiki/commands/scan.md, plugins/graph-wiki/skills/graph-wiki/SKILL.md, plugins/graph-wiki/skills/graph-wiki/README.md, plugins/graph-wiki/skills/graph-wiki/references/cross-tool-setup.md, plugins/graph-wiki/skills/graph-wiki/references/detection-workflow.md, packages/vault-io/src/vault_io/scan_monorepo.py, packages/vault-io/src/vault_io/lint/container.py</files>
  <read_first>
    - plugins/graph-wiki/README.md (full file — find line ~30 usage example and line ~63 command table)
    - plugins/graph-wiki/CLAUDE.md (full file — line 74 namespacing list)
    - plugins/graph-wiki/agents/linter.md (full file — line 39 prose mention)
    - plugins/graph-wiki/commands/scan.md (full file — lines 8, 46, 67)
    - plugins/graph-wiki/skills/graph-wiki/SKILL.md (lines around 105 command-table entry)
    - plugins/graph-wiki/skills/graph-wiki/README.md (lines around 25 command-table entry)
    - plugins/graph-wiki/skills/graph-wiki/references/cross-tool-setup.md (lines around 56, 58)
    - plugins/graph-wiki/skills/graph-wiki/references/detection-workflow.md (lines around 7, 17, 36, 41)
    - packages/vault-io/src/vault_io/scan_monorepo.py (line 1157 — print hint)
    - packages/vault-io/src/vault_io/lint/container.py (line 35 — lint error string)
    - .planning/phases/18-plugin-command-rename/18-CONTEXT.md (D-01 verb, D-02 surface coverage, Q2 reinstall note recommendation)
  </read_first>
  <action>
    Step 0: Capture pre-sweep baseline counts (WARNING 3 fix per plan-checker feedback) — these are invariants that the sweep must NOT change:
      `INGEST_BASELINE=$(grep -rcE '/graph-wiki:ingest\b' plugins/ packages/ 2>/dev/null | awk -F: 'BEGIN{s=0}{s+=$2}END{print s}')`
      `INIT_VAULT_BASELINE=$(grep -rcE '\binit_vault\b' packages/ agents/ 2>/dev/null | awk -F: 'BEGIN{s=0}{s+=$2}END{print s}')`
      Store both values in shell variables (or write to a temp file like `/tmp/18-04-baselines.txt`) for retrieval after the sweep. Both must remain stable across the sweep — if either drifts, the word-boundary regex over-matched and the sweep is wrong.

    Step 1: For each file in the in-scope hit list (see `<interfaces>`), apply the substitution `/graph-wiki:init` → `/graph-wiki:bootstrap` with word boundary protection. The simplest mechanism is `sed -i.bak -E 's@/graph-wiki:init\b@/graph-wiki:bootstrap@g' <file>` followed by removing the `.bak` backup file — but an executor may use any tool that respects the word-boundary anchor. The acceptance criteria pins the observable end state, not the mechanism.

    Step 2: In two files, the slash-command literal sits inside non-slash contexts that also need consideration:
      - `plugins/graph-wiki/CLAUDE.md` line 74: a markdown list of commands `/graph-wiki:init, /graph-wiki:scan, …`. Replace the `/graph-wiki:init` entry only; sibling entries (`/graph-wiki:scan`, `/graph-wiki:ingest`, etc.) stay unchanged.
      - `plugins/graph-wiki/skills/graph-wiki/README.md` line 25: table cell listing 6 slash commands. Same — replace only the `init` cell with `bootstrap`.

    Step 3: In `packages/vault-io/src/vault_io/lint/container.py` line 35, the string literal currently reads:
      `"no layout block found in CLAUDE.md (run /graph-wiki:init)"`
      Replace with:
      `"no layout block found in CLAUDE.md (run /graph-wiki:bootstrap)"`
      No other change. The function/lint-rule name is not the user-typed slug.

    Step 4: In `packages/vault-io/src/vault_io/scan_monorepo.py` line 1157, the print statement currently reads:
      `print("(Re-run /graph-wiki:init or hand-edit the layout block to update.)")`
      Replace with:
      `print("(Re-run /graph-wiki:bootstrap or hand-edit the layout block to update.)")`

    Step 5: Add a one-sentence reinstall note to `plugins/graph-wiki/README.md` (resolves open question Q2). Pick a natural location — typically the "Installation" / setup section. Wording suggestion (executor may rephrase): `Note: if you previously installed an older version of this plugin, remove and reinstall it to pick up the renamed /graph-wiki:bootstrap command (formerly /graph-wiki:init).` Place it as a callout or short paragraph. Do NOT add it to multiple files — README.md only.

    Step 6: After all substitutions, run the active-source grep gate from repo root:
      `grep -rEn '\bgraph-wiki:init\b' plugins/ packages/ agents/ scripts/ docs/ README.md CLAUDE.md 2>/dev/null`
      Must return zero output (exit 1 from grep).

    Step 7: Assert non-targets are unchanged via pre/post baseline comparison (strengthened per WARNING 3):
      `INGEST_POST=$(grep -rcE '/graph-wiki:ingest\b' plugins/ packages/ 2>/dev/null | awk -F: 'BEGIN{s=0}{s+=$2}END{print s}')`
      `INIT_VAULT_POST=$(grep -rcE '\binit_vault\b' packages/ agents/ 2>/dev/null | awk -F: 'BEGIN{s=0}{s+=$2}END{print s}')`
      Assert: `[ "$INGEST_BASELINE" = "$INGEST_POST" ]` AND `[ "$INIT_VAULT_BASELINE" = "$INIT_VAULT_POST" ]`. If either equality fails, STOP — the sweep over-matched; surface to orchestrator with both counts.

      Alternative robust mechanism (executor may pick either): `git diff -- 'plugins/**' 'packages/**' | grep -E '^[+-].*(\binit_vault\b|/graph-wiki:ingest\b)' | wc -l` must return 0 — no addition or deletion of either invariant token. Both mechanisms prove the regex did not over-match.

    Step 8: Stage and commit all sweep changes as a single commit. Subject: `refactor(18): sweep active-source /graph-wiki:init → /graph-wiki:bootstrap references`. Body lists the 10 files touched and the reinstall note added to the README; references CMD-03.
  </action>
  <verify>
    <automated>[ "$(grep -rEn '\bgraph-wiki:init\b' plugins/ packages/ agents/ scripts/ docs/ README.md CLAUDE.md 2>/dev/null | wc -l | tr -d ' ')" = "0" ] && [ "$(grep -cE '/graph-wiki:bootstrap\b' plugins/graph-wiki/README.md plugins/graph-wiki/CLAUDE.md plugins/graph-wiki/agents/linter.md plugins/graph-wiki/commands/scan.md plugins/graph-wiki/skills/graph-wiki/SKILL.md plugins/graph-wiki/skills/graph-wiki/README.md plugins/graph-wiki/skills/graph-wiki/references/cross-tool-setup.md plugins/graph-wiki/skills/graph-wiki/references/detection-workflow.md packages/vault-io/src/vault_io/scan_monorepo.py packages/vault-io/src/vault_io/lint/container.py | awk -F: 'BEGIN{s=0}{s+=$2}END{print s}')" -ge 16 ] && grep -qiE 'reinstall|remove and reinstall' plugins/graph-wiki/README.md && uv run --package code-wiki-agent pytest agents/code-wiki-agent/tests/ -m "not integration" -q 2>&1 | tail -3 | grep -qE 'passed'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rEn '\bgraph-wiki:init\b' plugins/ packages/ agents/ scripts/ docs/ README.md CLAUDE.md` returns zero output (exit code 1, "no match").
    - `grep -cE '/graph-wiki:bootstrap\b' plugins/graph-wiki/README.md` returns ≥ 2 (replacing the 2 prior `init` hits).
    - `grep -cE '/graph-wiki:bootstrap\b' plugins/graph-wiki/commands/scan.md` returns ≥ 3 (replacing the 3 prior `init` hits).
    - `grep -cE '/graph-wiki:bootstrap\b' plugins/graph-wiki/skills/graph-wiki/references/detection-workflow.md` returns ≥ 4 (replacing the 4 prior `init` hits).
    - `grep -nE 'run /graph-wiki:bootstrap' packages/vault-io/src/vault_io/lint/container.py` matches line ~35.
    - `grep -nE 'Re-run /graph-wiki:bootstrap' packages/vault-io/src/vault_io/scan_monorepo.py` matches line ~1157.
    - `plugins/graph-wiki/README.md` contains a reinstall note: `grep -ciE 'reinstall' plugins/graph-wiki/README.md` returns ≥ 1.
    - **WARNING 3 invariant — init_vault identifier:** total occurrences of `\binit_vault\b` across `packages/` and `agents/` are IDENTICAL pre- and post-sweep. Specifically: `INIT_VAULT_POST = INIT_VAULT_BASELINE` (captured per Step 0 / Step 7). Mechanism: `grep -rcE '\binit_vault\b' packages/ agents/ 2>/dev/null | awk -F: 'BEGIN{s=0}{s+=$2}END{print s}'` evaluated before and after the sweep returns the same number.
    - **WARNING 3 invariant — /graph-wiki:ingest sibling slug:** total occurrences of `/graph-wiki:ingest\b` across `plugins/` and `packages/` are IDENTICAL pre- and post-sweep. Specifically: `INGEST_POST = INGEST_BASELINE`. Mechanism: `grep -rcE '/graph-wiki:ingest\b' plugins/ packages/ 2>/dev/null | awk -F: 'BEGIN{s=0}{s+=$2}END{print s}'` evaluated before and after the sweep returns the same number.
    - Pre-existing minimum sanity (kept for backward compatibility): `grep -cE '\binit_vault\b' packages/vault-io/src/vault_io/init_vault.py` returns ≥ 1 (the canonical definition file still defines the function).
    - Per-commit gate: `uv run --package code-wiki-agent pytest agents/code-wiki-agent/tests/ -m "not integration"` exits 0.
    - Single commit lands with subject containing `refactor(18)` and the word `sweep`.
  </acceptance_criteria>
  <done>
    Active-source grep clean of `/graph-wiki:init`; vault-io error messages tell users to run the new command name; README has reinstall note; per-commit gate green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plugin docs → user mental model | Stale `/graph-wiki:init` references would mislead users into typing a command that no longer resolves |
| vault-io runtime errors → user terminal | Error/print strings instruct users to run a specific slash command; must match the actual surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-11 | Information Disclosure | Lint/scan output strings | mitigate | Steps 3 + 4 explicitly rewrite the two known runtime strings; acceptance criteria asserts the new strings are present at the correct lines. |
| T-18-12 | Tampering | Word-boundary regex catching false positives (e.g., `init_vault`) | mitigate | Acceptance criteria asserts `init_vault` is still present unchanged AND `/graph-wiki:ingest` is still present unchanged — proving the regex did not over-match. |
| T-18-13 | Spoofing | Documentation drift between surfaces | mitigate | Sweep is comprehensive across all known consumer files; brand-gate (plan 18-06) will lock in zero `\bgraph-wiki:init\b` going forward. |
</threat_model>

<verification>
- `grep -rEn '\bgraph-wiki:init\b' plugins/ packages/ agents/ scripts/ docs/ README.md CLAUDE.md` exits with no output (status 1).
- `grep -rEn '\binit_vault\b' packages/ agents/` count unchanged from pre-sweep baseline (sanity).
- Per-commit gate green.
- One commit lands.
</verification>

<success_criteria>
- 11 active-source files swept; renamed command file from plan 18-01 confirmed clean.
- vault-io runtime error strings updated.
- README reinstall note present.
- `init_vault` Python identifier and `/graph-wiki:ingest` sibling slug untouched.
- CMD-03 active-source portion closed.
</success_criteria>

<output>
Create `.planning/phases/18-plugin-command-rename/18-04-SUMMARY.md` when done. Capture: per-file substitution counts; confirmation that `grep -rEn '\bgraph-wiki:init\b' plugins/ packages/ agents/ scripts/ docs/ README.md CLAUDE.md` returns 0; pre/post counts of `init_vault` references (must be equal); pre/post counts of `/graph-wiki:ingest` references (must be equal); commit SHA.
</output>
