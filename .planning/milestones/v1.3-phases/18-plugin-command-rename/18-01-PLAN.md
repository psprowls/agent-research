---
phase: 18-plugin-command-rename
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/graph-wiki/commands/init.md
  - plugins/graph-wiki/commands/bootstrap.md
autonomous: true
requirements: [CMD-01]
must_haves:
  truths:
    - "plugins/graph-wiki/commands/bootstrap.md exists with the renamed front matter and body"
    - "plugins/graph-wiki/commands/init.md does NOT exist (file removed via git mv)"
    - "Renamed file body uses /graph-wiki:bootstrap in all usage examples (no /graph-wiki:init remains in the body)"
  artifacts:
    - path: "plugins/graph-wiki/commands/bootstrap.md"
      provides: "Renamed slash-command markdown invoked by Claude Code as /graph-wiki:bootstrap"
      contains: "name: bootstrap"
  key_links:
    - from: "plugins/graph-wiki/commands/bootstrap.md"
      to: "plugins/graph-wiki/skills/graph-wiki/scripts/init_vault.py"
      via: "Script reference"
      pattern: "init_vault\\.py"
---

<objective>
Rename the plugin slash-command file `plugins/graph-wiki/commands/init.md` to `plugins/graph-wiki/commands/bootstrap.md` (preserving git history via `git mv`) and update the file body so all `/graph-wiki:init` invocations and the front-matter `name:` / `description:` reflect `bootstrap`. This restores Claude Code's native `/init` command by removing the namespace collision at the Claude Code surface (the plugin's name + the filename together produce the slash command).

Purpose: First commit in the staged cutover (D-06 step 1). Closes CMD-01. Underlying `init_vault.py` Python script keeps its name (out of scope per D-02).

Output: One renamed file with internal references self-updated; commit lands green on per-commit test gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-plugin-command-rename/18-CONTEXT.md
@CLAUDE.md
@plugins/graph-wiki/commands/init.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: git mv init.md to bootstrap.md and rewrite body</name>
  <files>plugins/graph-wiki/commands/init.md, plugins/graph-wiki/commands/bootstrap.md</files>
  <read_first>
    - plugins/graph-wiki/commands/init.md (full file — current body, front-matter, usage examples)
    - plugins/graph-wiki/commands/scan.md (sibling command for tone/format reference)
    - .planning/phases/18-plugin-command-rename/18-CONTEXT.md (decisions D-01, D-02, D-04, D-06)
  </read_first>
  <action>
    Step 1: Run `git mv plugins/graph-wiki/commands/init.md plugins/graph-wiki/commands/bootstrap.md` to preserve rename history. Do NOT use a plain `mv` then re-add — git must record this as a rename.

    Step 2: Edit the file body of the renamed `bootstrap.md`:
      - Front-matter `name:` field: change `init` to `bootstrap`.
      - Front-matter `description:` field: replace the substring `/graph-wiki:init` with `/graph-wiki:bootstrap`.
      - H1 heading: change `# /graph-wiki:init` to `# /graph-wiki:bootstrap`.
      - Usage code block (lines around 14–18): replace every `/graph-wiki:init` with `/graph-wiki:bootstrap`.
      - Examples code block (lines around 22–26): replace every `/graph-wiki:init` with `/graph-wiki:bootstrap`.
      - Any narrative reference inside the body (e.g. "before initializing the wiki, run …") may keep semantic verbs like "initialize / bootstrap" but the slash-command literal MUST be `/graph-wiki:bootstrap`.

    Step 3: The reference to `${CLAUDE_PLUGIN_ROOT}/skills/graph-wiki/scripts/init_vault.py` in the body stays unchanged — the underlying Python script is out of scope per D-02. Same for any prose "bootstrap a wiki vault" wording — those are descriptive verbs, not the literal command slug.

    Step 4: Do NOT leave a stub `init.md` redirector — hard cut per D-04. Confirm `git status` shows the file as renamed (R) not deleted-then-added.

    Hard rule: zero occurrences of the literal `/graph-wiki:init` (with word boundary) may remain inside `plugins/graph-wiki/commands/bootstrap.md` after this task. The slug `/graph-wiki:bootstrap` MUST appear at minimum in the front-matter description, H1 heading, and usage block.
  </action>
  <verify>
    <automated>test ! -e plugins/graph-wiki/commands/init.md && test -e plugins/graph-wiki/commands/bootstrap.md && grep -cE '^name: bootstrap$' plugins/graph-wiki/commands/bootstrap.md | grep -qx 1 && [ "$(grep -cE '/graph-wiki:init\b' plugins/graph-wiki/commands/bootstrap.md)" = "0" ] && [ "$(grep -cE '/graph-wiki:bootstrap\b' plugins/graph-wiki/commands/bootstrap.md)" -ge 3 ] && git status --porcelain plugins/graph-wiki/commands/ | grep -qE '^R'</automated>
  </verify>
  <acceptance_criteria>
    - File `plugins/graph-wiki/commands/init.md` does not exist (exit 1 on `test -e`).
    - File `plugins/graph-wiki/commands/bootstrap.md` exists (exit 0 on `test -e`).
    - `grep -cE '^name: bootstrap$' plugins/graph-wiki/commands/bootstrap.md` returns exactly 1.
    - `grep -cE '/graph-wiki:init\b' plugins/graph-wiki/commands/bootstrap.md` returns 0.
    - `grep -cE '/graph-wiki:bootstrap\b' plugins/graph-wiki/commands/bootstrap.md` returns ≥ 3 (front-matter description, H1, usage block minimum).
    - `git status --porcelain plugins/graph-wiki/commands/` shows a line beginning with `R` (renamed), proving `git mv` was used (not delete+add).
    - The body still references `init_vault.py` (script name unchanged per D-02): `grep -c 'init_vault\.py' plugins/graph-wiki/commands/bootstrap.md` returns ≥ 1.
  </acceptance_criteria>
  <done>
    Renamed file in place with internal `/graph-wiki:init` literals replaced by `/graph-wiki:bootstrap`; git history preserved via `git mv`; no stub redirector left behind.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run per-commit test gate and stage the rename commit</name>
  <files>(no source modifications; verification + commit only)</files>
  <read_first>
    - .planning/phases/18-plugin-command-rename/18-CONTEXT.md (D-06 per-commit gate definition)
  </read_first>
  <action>
    Step 1: Run the D-06 per-commit gate from repo root: `uv run --package code-wiki-agent pytest agents/code-wiki-agent/tests/ -m "not integration"`. The integration-tagged tests are explicitly excluded. The suite must exit 0.

    Step 2: If pytest fails, do NOT proceed to commit — surface the failure to the orchestrator. Likely cause if it fails here would be an unrelated regression (no Python code changed in this plan); investigate before pressing on.

    Step 3: Stage and commit only the rename: `git add plugins/graph-wiki/commands/init.md plugins/graph-wiki/commands/bootstrap.md` (the deleted path must be staged to record the `git mv`). Commit message subject line: `refactor(18): rename plugin slash command /graph-wiki:init → /graph-wiki:bootstrap`. Body should reference CMD-01 and note that the underlying `init_vault.py` script name is intentionally unchanged (per D-02).

    Step 4: Confirm with `git log -1 --stat` that the commit shows the file as renamed (`R100` or similar) rather than a delete-plus-add pair.
  </action>
  <verify>
    <automated>uv run --package code-wiki-agent pytest agents/code-wiki-agent/tests/ -m "not integration" -q 2>&1 | tail -5 && git log -1 --name-status | grep -qE '^R[0-9]+\s+plugins/graph-wiki/commands/init\.md\s+plugins/graph-wiki/commands/bootstrap\.md$'</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --package code-wiki-agent pytest agents/code-wiki-agent/tests/ -m "not integration"` exits 0.
    - `git log -1 --name-status` shows a single rename line of the form `R{score}\tplugins/graph-wiki/commands/init.md\tplugins/graph-wiki/commands/bootstrap.md`.
    - Commit subject line contains the literal string `refactor(18)` and references the rename direction `init → bootstrap` (or `init -> bootstrap`).
  </acceptance_criteria>
  <done>
    Test gate green, single rename commit landed cleanly, git history preserved.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude Code host → plugin slash command | Claude Code resolves `/graph-wiki:bootstrap` against the filename in `plugins/graph-wiki/commands/` |
| Plugin command body → bundled scripts | Body references `${CLAUDE_PLUGIN_ROOT}/skills/graph-wiki/scripts/init_vault.py` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering | plugins/graph-wiki/commands/bootstrap.md | mitigate | Acceptance criteria asserts exact `name: bootstrap` front-matter line + zero `/graph-wiki:init` literals; per-commit test gate runs before commit. |
| T-18-02 | Repudiation | Git history of the rename | mitigate | Require `git mv` (not delete+add); acceptance criteria asserts `git log` shows an `R{score}` rename entry so future blame/log surfaces the rename atomically. |
| T-18-03 | Denial of Service | Claude Code native `/init` workflow | mitigate | This rename IS the mitigation (the threat is the pre-existing namespace collision); restoring native `/init` is the phase goal. Plan 18-04 sweep + 18-06 brand-gate prevent regression. |
</threat_model>

<verification>
After both tasks pass:
- `test -e plugins/graph-wiki/commands/bootstrap.md && ! test -e plugins/graph-wiki/commands/init.md` exits 0.
- `grep -E '/graph-wiki:init\b' plugins/graph-wiki/commands/bootstrap.md` finds zero matches (exit 1).
- `git log --diff-filter=R --summary -1` shows the rename.
- Per-commit pytest gate green.

Deferred to Phase verifier: the manual SC#3 check (typing `/init` in Claude Code with plugin installed invokes Claude's native CLAUDE.md initialization, not graph-wiki bootstrap). That manual check is owned by `/gsd:verify-phase` / UAT, not this plan.
</verification>

<success_criteria>
- File renamed via `git mv`, history preserved.
- Body self-consistently references `/graph-wiki:bootstrap`.
- Underlying `init_vault.py` script name unchanged (per D-02 — out of scope).
- No backwards-compat stub at `init.md` (per D-04 — hard cut).
- CMD-01 closed.
</success_criteria>

<output>
Create `.planning/phases/18-plugin-command-rename/18-01-SUMMARY.md` when done. Capture: exact `git log -1 --name-status` line (proving rename, not delete+add); count of `/graph-wiki:bootstrap` literals introduced; confirmation that `init_vault.py` references inside the body are unchanged.
</output>
