---
phase: 18-plugin-command-rename
plan: 05
type: execute
wave: 2
depends_on: [18-01, 18-02, 18-03]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/spec/13-plugin-contract/scan.md
  - .planning/spec/13-plugin-contract/init.md
  - .planning/spec/13-plugin-contract/CONTRACT-INDEX.md
  - .planning/milestones/v1.2-ROADMAP.md
  - .planning/milestones/v1.2-phases/12-drift-backport-ecosystem-rebrand-m2/12-VERIFICATION.md
  - .planning/milestones/v1.2-phases/12-drift-backport-ecosystem-rebrand-m2/12-REVIEW.md
  - .planning/milestones/v1.2-phases/14-plugin-port-m3b/14-CONTEXT.md
  - .planning/milestones/v1.2-phases/14-plugin-port-m3b/14-VERIFICATION.md
  - .planning/milestones/v1.2-phases/14-plugin-port-m3b/14-PATTERNS.md
  - .planning/milestones/v1.2-phases/14-plugin-port-m3b/14-03-PLAN.md
  - .planning/milestones/v1.2-phases/13-plugin-spec-m3a/13-01-SUMMARY.md
  - .planning/milestones/v1.2-phases/13-plugin-spec-m3a/13-VERIFICATION.md
  - .planning/milestones/v1.2-phases/13-plugin-spec-m3a/13-CONTEXT.md
  - .planning/milestones/v1.2-phases/13-plugin-spec-m3a/13-01-PLAN.md
  - .planning/todos/pending/2026-05-19-fix-workspace-repo-resolution-in-init-vault-and-detect-conta.md
  - .planning/phases/17-wiki-io-bug-fixes/17-05-PLAN.md
autonomous: true
requirements: [CMD-03]
must_haves:
  truths:
    - "Across all historical `.planning/` files (except this phase's own CONTEXT.md and DISCUSSION-LOG.md, which are allowlisted by the brand-gate in plan 18-06), `\\bgraph-wiki:init\\b` returns 0 matches"
    - "This phase's CONTEXT.md and DISCUSSION-LOG.md retain their references intact (they quote the rename direction; allowlisted in plan 18-06)"
    - "The folded todo file `.planning/todos/pending/2026-05-19-rename-graph-wiki-init-command-to-init-wiki.md` is NOT swept here — it gets moved to resolved/ in plan 18-06 untouched"
    - "Git log/commit messages are NOT rewritten (no rebase/filter-branch) — they retain the old name immutably per D-03 acknowledged trade-off"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Roadmap text with historical SC#1/SC#2 references self-updated"
      contains: "/graph-wiki:bootstrap"
  key_links:
    - from: ".planning/phases/18-plugin-command-rename/18-CONTEXT.md"
      to: "Brand-gate allowlist (plan 18-06)"
      via: "Allowlist exemption (this CONTEXT.md retains old name as quoted documentation of the rename direction)"
      pattern: "allowlist"
---

<objective>
Sweep all historical `.planning/` references to `/graph-wiki:init` and replace them with `/graph-wiki:bootstrap`. This matches Phase 21's `code-wiki-agent` rename norm — full historical consistency, future grep for `/graph-wiki:init` outside this phase's own CONTEXT.md / DISCUSSION-LOG.md returns 0.

Purpose: D-06 step 5 in the staged cutover. Closes the historical-sweep portion of CMD-03. Parallel with plan 18-04 (active-source sweep) — disjoint file sets, no contention.

**Q3 resolved:** Single bundled commit. The transformation is uniform across all 18 historical files (verbatim word-boundary replace), the diff is mechanically identical per file, and one commit is easier to revert if needed than 18 micro-commits. Phase 21 D-05 set the same precedent.

Output: One commit, `grep -rEn '\bgraph-wiki:init\b' .planning/` returns ≤ 2 hits (this phase's `18-CONTEXT.md` and `18-DISCUSSION-LOG.md`, which retain references as quoted documentation of the rename direction; these are allowlisted by the brand-gate in plan 18-06).

**Acknowledged trade-off (D-03):** Historical record alteration. Git commit history, however, is NOT rewritten — old commit messages naming `/graph-wiki:init` remain immutable. Anyone reviewing git log will see the rename happened on a specific commit; anyone grepping `.planning/` will see consistent post-rename names everywhere except this phase's own quoted documentation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-plugin-command-rename/18-CONTEXT.md

<interfaces>
Exact in-scope hit list (from `grep -rln '\bgraph-wiki:init\b' .planning/` taken 2026-05-19; 21 files total, of which 3 are EXCLUDED from this sweep per D-03 and the brand-gate self-allowlist policy):

INCLUDED — 18 files (all renamed in this plan):
- `.planning/REQUIREMENTS.md`
- `.planning/ROADMAP.md`
- `.planning/spec/13-plugin-contract/scan.md`
- `.planning/spec/13-plugin-contract/init.md`
- `.planning/spec/13-plugin-contract/CONTRACT-INDEX.md`
- `.planning/milestones/v1.2-ROADMAP.md`
- `.planning/milestones/v1.2-phases/12-drift-backport-ecosystem-rebrand-m2/12-VERIFICATION.md`
- `.planning/milestones/v1.2-phases/12-drift-backport-ecosystem-rebrand-m2/12-REVIEW.md`
- `.planning/milestones/v1.2-phases/14-plugin-port-m3b/14-CONTEXT.md`
- `.planning/milestones/v1.2-phases/14-plugin-port-m3b/14-VERIFICATION.md`
- `.planning/milestones/v1.2-phases/14-plugin-port-m3b/14-PATTERNS.md`
- `.planning/milestones/v1.2-phases/14-plugin-port-m3b/14-03-PLAN.md`
- `.planning/milestones/v1.2-phases/13-plugin-spec-m3a/13-01-SUMMARY.md`
- `.planning/milestones/v1.2-phases/13-plugin-spec-m3a/13-VERIFICATION.md`
- `.planning/milestones/v1.2-phases/13-plugin-spec-m3a/13-CONTEXT.md`
- `.planning/milestones/v1.2-phases/13-plugin-spec-m3a/13-01-PLAN.md`
- `.planning/todos/pending/2026-05-19-fix-workspace-repo-resolution-in-init-vault-and-detect-conta.md`
- `.planning/phases/17-wiki-io-bug-fixes/17-05-PLAN.md`

EXCLUDED — 3 files (allowlisted by plan 18-06 brand-gate, quoted documentation of the rename direction):
- `.planning/phases/18-plugin-command-rename/18-CONTEXT.md`
- `.planning/phases/18-plugin-command-rename/18-DISCUSSION-LOG.md`
- `.planning/todos/pending/2026-05-19-rename-graph-wiki-init-command-to-init-wiki.md` — NOT swept; the todo filename slug references the older "init-wiki" rename direction (now superseded). The file is moved verbatim to `.planning/todos/resolved/` in plan 18-06 without content rewrite. The slug in the FILENAME stays for traceability.

NOTE on ROADMAP.md: Phase 18's bullet currently reads `"Rename /graph-wiki:init → /graph-wiki:init-wiki to restore Claude Code's native /init"`. **Resolution (BLOCKER ambiguity fix per plan-checker WARNING 1): pin option B — rewrite the Phase 18 bullet so it reads `/graph-wiki:init → /graph-wiki:bootstrap`.** Allowlisting ROADMAP.md by line range is NOT permitted — `.brand-grep-allow` uses path-fragment substring matching, not line ranges; adding ROADMAP.md to the allowlist would mask legitimate future regressions across the entire 180+ line file. Rewriting the bullet keeps the file gate-clean and grep-clean. The trailing `Claude Code's native /init` text remains intact (that's a reference to Claude Code's native command, not the graph-wiki slug). Same rule applies to the SC#1/SC#2 lines under the Phase 18 Phase Details section.

Transformation rule: identical to plan 18-04 — word-boundary replace `/graph-wiki:init` → `/graph-wiki:bootstrap`. Word-boundary so `/graph-wiki:ingest` is not touched.

NOTE on `init-wiki` and `init_wiki` literals: Some historical files (esp. ROADMAP.md, REQUIREMENTS.md, the folded todo's content) reference the OLDER planned rename direction `/graph-wiki:init-wiki`. Per D-01, the final verb is `bootstrap`, not `init-wiki`. After the sweep, also run `grep -rEn '/graph-wiki:init-wiki\b|graph-wiki:init_wiki\b' .planning/` and replace the `init-wiki` literal with `bootstrap` where it appears outside this phase's own meta files. Justification: the older `init-wiki` plan was superseded; leaving stale `init-wiki` literals in roadmap/requirements creates downstream confusion. The folded todo file's CONTENT is excluded (filename retains the old slug; file moves to resolved/ in plan 18-06).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep historical `.planning/` references via word-boundary replace</name>
  <files>(18 files listed in frontmatter; see <interfaces> for exact list)</files>
  <read_first>
    - .planning/ROADMAP.md (full file — Phase 18 bullet currently mentions the OLD `init-wiki` direction; needs rewrite per <interfaces> note)
    - .planning/REQUIREMENTS.md (CMD section — currently mentions both `/init` and `/init-wiki`; update CMD-01/CMD-02/CMD-03 wording to reference `bootstrap`)
    - .planning/milestones/v1.2-ROADMAP.md (where the historical Phase 18 bullet may also live)
    - At least one milestone-phase file from each cluster (`12-*`, `13-*`, `14-*`) to understand the prose context before doing the bulk sweep
    - .planning/phases/18-plugin-command-rename/18-CONTEXT.md (the EXCLUDED files; do NOT modify; verify excluded files are not in the file list before running the bulk substitution)
  </read_first>
  <action>
    Step 1: Build the precise file list. Run `grep -rln '\bgraph-wiki:init\b' .planning/` to get all hits, then filter out the 3 EXCLUDED files (`18-CONTEXT.md`, `18-DISCUSSION-LOG.md`, the folded todo file). Use a guarded shell expression like:
      `grep -rln '\bgraph-wiki:init\b' .planning/ | grep -v '.planning/phases/18-plugin-command-rename/' | grep -v '2026-05-19-rename-graph-wiki-init-command-to-init-wiki'`
    Confirm the resulting list contains exactly the 18 INCLUDED files in `<interfaces>`. If the count or paths differ, STOP and surface the discrepancy to the orchestrator (the on-disk state may have drifted from the planning_context baseline).

    Step 2: Apply word-boundary replace to each file in the filtered list. Mechanism options:
      - `sed -i.bak -E 's@/graph-wiki:init\b@/graph-wiki:bootstrap@g' <file>` per file, then `find .planning -name "*.bak" -delete`.
      - Or scripted via `xargs -I{} sed -i.bak ...`.
    Executor picks the cleanest mechanism. After replacement, verify each file has been touched by checking `git status`.

    Step 3: Also sweep the OLDER planned-rename literal `/graph-wiki:init-wiki` (which appears in the original ROADMAP.md and REQUIREMENTS.md text for Phase 18 / CMD-01..03). For each file in the INCLUDED list that contains it, replace `/graph-wiki:init-wiki` → `/graph-wiki:bootstrap` (no word-boundary issue here — `init-wiki` is unambiguous). Same exclusion list applies.

    Step 4: Also sweep `graph-wiki:init` (without the leading `/`) in the same INCLUDED files — `graph-wiki:init` appears as a non-slash-prefix form in some prose (e.g., `the graph-wiki:init command`). Same transformation, same word-boundary regex `\bgraph-wiki:init\b` (which already matches the no-slash form).

    Step 5: Update Phase 18 bullet in `.planning/ROADMAP.md` so it reads accurately post-rename. The current bullet:
      `**Phase 18: Plugin Command Rename** - Rename /graph-wiki:init → /graph-wiki:init-wiki to restore Claude Code's native /init`
    Should become (executor may rephrase, but the intent must match):
      `**Phase 18: Plugin Command Rename** - Rename /graph-wiki:init → /graph-wiki:bootstrap to restore Claude Code's native /init`
    The trailing `Claude Code's native /init` is descriptive prose about Claude Code's command, NOT a `/graph-wiki:init` reference. Leave it intact.

    Same for the Phase Details section: SC#1 currently says `init-wiki.md`, SC#2 mentions `/init-wiki`. Both rewrite to `bootstrap.md` and `/graph-wiki:bootstrap`. SC#3 says "with the plugin installed, typing `/init` invokes the native …" — leave `/init` here (that IS Claude Code's native command).

    Step 6: Rewrite `.planning/REQUIREMENTS.md` CMD-01 / CMD-02 / CMD-03 requirement bodies to match as-built scope (BLOCKER 2 fix per plan-checker feedback). The existing bodies describe the older `init-wiki` direction AND the scope-to-requirement mapping doesn't match the as-planned plan-to-requirement mapping (CMD-02 was previously written as covering markdown sweep; that's actually CMD-03's plan-04 scope).

    Apply these exact replacements to the three requirement bodies in REQUIREMENTS.md under the "### Plugin Command Rename (CMD)" section:

    - **CMD-01**: replace the existing body line with: `- [ ] **CMD-01**: \`plugins/graph-wiki/commands/init.md\` renamed to \`plugins/graph-wiki/commands/bootstrap.md\` (file rename via \`git mv\` to preserve history; body updated).`

    - **CMD-02**: replace the existing body line with: `- [ ] **CMD-02**: Bedrock CLI subcommand (\`cli.py\` Typer \`init\` → \`bootstrap\`) and MCP tool (\`server.py\` \`wiki_init\` → \`wiki_bootstrap\` + Pydantic \`WikiInitInput\`/\`WikiInitOutput\` → \`WikiBootstrapInput\`/\`WikiBootstrapOutput\`) renamed for vocabulary consistency.`

    - **CMD-03**: replace the existing body line with: `- [ ] **CMD-03**: All 11 active-source \`/graph-wiki:init\` references swept (plugin docs, agent docs, wiki-io consumers); all 18 historical \`.planning/\` references swept (per D-03 historical-consistency norm); \`scripts/check-brand.sh\` extended with \`\\bgraph-wiki:init\\b\` / \`\\bwiki_init\\b\` / \`\\bdef init\\(\` enforcement regexes; \`.brand-grep-allow\` updated for narrow exemptions. SC#3 manual smoke ("typing \`/init\` in Claude Code invokes native CLAUDE.md workflow") is owned by the phase verifier / UAT pass.`

    Also update the section preamble paragraph above CMD-01 in REQUIREMENTS.md (currently: "Source: \`.planning/todos/pending/2026-05-19-rename-graph-wiki-init-command-to-init-wiki.md\`. The \`graph-wiki\` plugin ships a \`/init\` command (\`plugins/graph-wiki/commands/init.md\`) that shadows Claude Code's built-in \`/init\`. With the plugin installed, the native 'initialize CLAUDE.md' workflow is unreachable.") to keep the Source pointer accurate AFTER plan 18-06 moves the todo to resolved/: change \`pending/\` → \`resolved/\` in the Source line. Leave the rest of the preamble paragraph intact (the slug retention rationale was already documented in CONTEXT.md "Folded Todos").

    The frontmatter `requirements:` mapping across the six plans (CMD-01 → 18-01; CMD-02 → 18-02 + 18-03; CMD-03 → 18-04 + 18-05 + 18-06) is unchanged by this rewrite — the new bodies match the existing plan-to-requirement mapping.

    Step 7: After all substitutions, run the historical grep gate:
      `grep -rEn '\bgraph-wiki:init\b|/graph-wiki:init-wiki\b' .planning/ 2>/dev/null`
    Expected output: matches ONLY in the 3 EXCLUDED files. Specifically `.planning/phases/18-plugin-command-rename/18-CONTEXT.md`, `.planning/phases/18-plugin-command-rename/18-DISCUSSION-LOG.md`, and `.planning/todos/pending/2026-05-19-rename-graph-wiki-init-command-to-init-wiki.md`. If hits appear in any other file, surface the discrepancy.

    Step 8: Sanity-check non-targets via pre/post baseline (WARNING 3 fix per plan-checker feedback):
      Before Step 2 starts (capture as shell variable): `INGEST_BASELINE=$(grep -rcE '/graph-wiki:ingest\b' .planning/ 2>/dev/null | awk -F: 'BEGIN{s=0}{s+=$2}END{print s}')`
      After Step 7 grep gate: `INGEST_POST=$(grep -rcE '/graph-wiki:ingest\b' .planning/ 2>/dev/null | awk -F: 'BEGIN{s=0}{s+=$2}END{print s}')`
      Assert: `[ "$INGEST_BASELINE" = "$INGEST_POST" ]` — sibling command counts must be identical. If not equal, the word-boundary regex over-matched; STOP and surface to orchestrator.

      Alternative mechanism (executor may pick either): `git diff -- '.planning/**' | grep -E '^[+-].*/graph-wiki:ingest' | wc -l` must return 0. Both mechanisms prove the sibling slug is untouched; the pre/post baseline is more robust to staged-vs-unstaged state.

    Step 9: Run per-commit gate `uv run --package code-wiki-agent pytest agents/code-wiki-agent/tests/ -m "not integration" -q`. Should still pass (no Python changes in this plan; this is a sanity check against parallel execution interference with plan 18-04).

    Step 10: Stage all .planning/ changes and commit as a single bundled commit. Subject: `docs(18): sweep historical .planning/ references /graph-wiki:init → /graph-wiki:bootstrap`. Body lists the 18 files swept; notes the 3 exclusions (this phase's CONTEXT.md + DISCUSSION-LOG.md + the folded todo file); references CMD-03 and D-03 (acknowledged historical-record trade-off).
  </action>
  <verify>
    <automated>[ "$(grep -rlE '\bgraph-wiki:init\b|/graph-wiki:init-wiki\b' .planning/ 2>/dev/null | grep -vE '\.planning/phases/18-plugin-command-rename/|2026-05-19-rename-graph-wiki-init-command-to-init-wiki' | wc -l | tr -d ' ')" = "0" ] && [ "$(grep -lE '\bgraph-wiki:bootstrap\b' .planning/ROADMAP.md .planning/REQUIREMENTS.md | wc -l | tr -d ' ')" = "2" ] && uv run --package code-wiki-agent pytest agents/code-wiki-agent/tests/ -m "not integration" -q 2>&1 | tail -3 | grep -qE 'passed'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rlE '\bgraph-wiki:init\b|/graph-wiki:init-wiki\b' .planning/` returns ONLY paths inside `.planning/phases/18-plugin-command-rename/` AND the folded todo file `.planning/todos/pending/2026-05-19-rename-graph-wiki-init-command-to-init-wiki.md`. No other file should appear.
    - `.planning/ROADMAP.md` contains `/graph-wiki:bootstrap`: `grep -c '/graph-wiki:bootstrap' .planning/ROADMAP.md` returns ≥ 1.
    - `.planning/REQUIREMENTS.md` Plugin Command Rename section references `bootstrap`: `grep -c '/graph-wiki:bootstrap\|graph-wiki:bootstrap' .planning/REQUIREMENTS.md` returns ≥ 1.
    - **BLOCKER 2 closure:** `.planning/REQUIREMENTS.md` no longer references the older `init-wiki` rename direction at the verb level. The folded-todo filename `2026-05-19-rename-graph-wiki-init-command-to-init-wiki.md` legitimately contains `init-wiki` as a path fragment (Source pointer; also allowlisted by plan 18-06 `.brand-grep-allow`); exclude it from the assertion: `grep 'init-wiki' .planning/REQUIREMENTS.md | grep -v '2026-05-19-rename-graph-wiki-init-command-to-init-wiki.md' | wc -l` returns 0.
    - CMD-01 body matches the rewritten text (BLOCKER 2): `grep -cE 'CMD-01.*bootstrap\.md.*git mv' .planning/REQUIREMENTS.md` returns 1.
    - CMD-02 body matches the rewritten text (BLOCKER 2): `grep -cE 'CMD-02.*wiki_bootstrap.*WikiBootstrapInput' .planning/REQUIREMENTS.md` returns 1.
    - CMD-03 body matches the rewritten text (BLOCKER 2): `grep -cE 'CMD-03.*scripts/check-brand\.sh' .planning/REQUIREMENTS.md` returns 1.
    - **WARNING 3 invariant — sibling slug:** `grep -rcE '/graph-wiki:ingest\b' .planning/ | awk -F: 'BEGIN{s=0}{s+=$2}END{print s}'` returns the same total before and after the sweep (capture `INGEST_BASELINE` per Step 8).
    - **WARNING 3 invariant — init_vault identifier:** `grep -rcE '\binit_vault\b' .planning/ | awk -F: 'BEGIN{s=0}{s+=$2}END{print s}'` returns the same total before and after the sweep. Capture `INIT_VAULT_BASELINE` via the same pre/post technique in Step 8. The Python identifier `init_vault` is out of scope per D-02; if pre-sweep != post-sweep, the regex over-matched.
    - `.planning/spec/13-plugin-contract/init.md` (the historical contract spec for the old init command) contains `bootstrap`: `grep -c 'bootstrap' .planning/spec/13-plugin-contract/init.md` returns ≥ 1. (The FILENAME `init.md` stays; the file is at a historical phase 13 path and renaming it would also rewrite git history paths — out of scope for this sweep.)
    - The folded todo file is NOT modified by this plan: `git status --porcelain .planning/todos/pending/2026-05-19-rename-graph-wiki-init-command-to-init-wiki.md` returns empty (file untouched). Its move to `.planning/todos/resolved/` happens in plan 18-06.
    - `.planning/phases/18-plugin-command-rename/18-CONTEXT.md` is NOT modified by this plan: `git status --porcelain .planning/phases/18-plugin-command-rename/18-CONTEXT.md` returns empty.
    - `.planning/phases/18-plugin-command-rename/18-DISCUSSION-LOG.md` is NOT modified by this plan: same git status check returns empty.
    - Sibling slug intact: `grep -rcE '/graph-wiki:ingest\b' .planning/ | grep -vE ':0$' | wc -l` returns ≥ 1 (sibling command literal still present somewhere).
    - Phase 18 ROADMAP bullet rewrites correctly: `grep -E 'Phase 18:.*bootstrap' .planning/ROADMAP.md` matches at least once. The descriptive `/init` reference for Claude Code's native command remains intact: `grep -c "Claude Code's native /init\|native \`/init\`\|native /init" .planning/ROADMAP.md` returns ≥ 1.
    - Per-commit gate: `uv run --package code-wiki-agent pytest agents/code-wiki-agent/tests/ -m "not integration"` exits 0.
    - Single bundled commit lands with subject containing `docs(18)` and the word `sweep`.
  </acceptance_criteria>
  <done>
    Historical `.planning/` references swept (18 files), exclusions verified untouched (3 files), per-commit gate green, single bundled commit lands.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Historical planning docs → future grep / search by Claude or developer | Stale references would lead future agents to recommend the wrong command |
| ROADMAP/REQUIREMENTS → traceability matrix | Requirement bodies must be consistent with the implementation surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-14 | Tampering | Word-boundary regex over-matching (e.g., catching `init-wiki` substring of something unintended) | mitigate | Step 7 grep gate uses an explicit anchored regex `\bgraph-wiki:init\b|/graph-wiki:init-wiki\b`; acceptance criteria asserts only the 3 EXCLUDED files remain as residue. |
| T-18-15 | Repudiation | Historical record alteration | accept | Acknowledged trade-off per D-03; git log retains old names immutably. Phase 21 D-05 set the precedent. |
| T-18-16 | Information Disclosure | Accidental sweep of an unrelated `init` token | mitigate | Word-boundary regex protects `/graph-wiki:ingest`; sanity check in Step 8 + acceptance criteria assert sibling slugs unchanged. |
| T-18-17 | Tampering | EXCLUDED files inadvertently swept | mitigate | Acceptance criteria asserts `git status --porcelain` shows the 3 EXCLUDED files as untouched. |
</threat_model>

<verification>
- `grep -rlE '\bgraph-wiki:init\b|/graph-wiki:init-wiki\b' .planning/` returns only the 3 allowlisted/excluded files.
- Per-commit gate green.
- Single commit lands.
- Sibling slugs untouched.
</verification>

<success_criteria>
- 18 historical `.planning/` files swept.
- 3 EXCLUDED files untouched.
- ROADMAP/REQUIREMENTS Phase 18 / CMD section updated to reference `bootstrap`.
- CMD-03 historical-sweep portion closed.
</success_criteria>

<output>
Create `.planning/phases/18-plugin-command-rename/18-05-SUMMARY.md` when done. Capture: pre-sweep hit count (21), post-sweep hit count (≤ 3 — only the 3 EXCLUDED files); list of files modified; confirmation that EXCLUDED files were untouched (via `git diff --stat`); commit SHA.
</output>
