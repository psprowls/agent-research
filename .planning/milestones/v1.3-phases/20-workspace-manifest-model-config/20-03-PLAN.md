---
phase: 20-workspace-manifest-model-config
plan: 03
type: execute
wave: 3
depends_on: ["20-02"]
files_modified:
  - agents/graph-wiki-agent/src/graph_wiki_agent/config.py
  - agents/graph-wiki-agent/src/graph_wiki_agent/cli.py
  - agents/graph-wiki-agent/src/graph_wiki_mcp/server.py
  - agents/graph-wiki-agent/tests/unit/test_config.py
autonomous: true
requirements: [WMC-03]
tags: [graph-wiki-agent, cli, mcp, cleanup, deletion]

must_haves:
  truths:
    - "`WikiConfig` dataclass no longer has a `models_path` field. The remaining fields are `vault_path: str | None` and `state_gate_enabled: bool`."
    - "`load_config(path)` still parses TOML and silently drops unknown keys (including `models_path` if a user passes a legacy file) — Karpathy §3 surgical: no migration shim, no warning, just drop. The CLI no longer calls `load_config` because `--config` is gone, so the only remaining caller surface is direct programmatic use by tests; the function stays as-is for now to avoid touching tests that exercise its TOML parsing semantics."
    - "The Typer `--config` global option is removed from `cli.py`. The `@app.callback()` decorator on `main_callback` is deleted (Typer is fine without one when the app carries a `help` string)."
    - "`graph_wiki_mcp/server.py:442-455` no longer reads `GRAPH_WIKI_CONFIG`, no longer imports `graph_wiki_agent.config`, and no longer imports `set_models_path` (which was deleted in Plan 02 — leaving the import would fail at MCP startup)."
    - "`agents/graph-wiki-agent/tests/unit/test_config.py` is updated: assertions referencing `models_path` are removed, the Typer-callback test for `--config` is removed, and the remaining `load_config`/`get_config` tests still pass."
    - "All agent-side docs (`agents/graph-wiki-agent/**/*.md`, `agents/graph-wiki-agent/README*`) drop residual `--config` / `GRAPH_WIKI_CONFIG` references — verified by a recursive grep gate."
    - "`uv run --package graph-wiki-agent pytest` exits 0. `graph-wiki-agent --help` runs without error and does not list `--config`. The MCP server starts (import-time check via `python -c \"from graph_wiki_mcp import server\"`)."
  artifacts:
    - path: agents/graph-wiki-agent/src/graph_wiki_agent/config.py
      provides: "WikiConfig dataclass without models_path; load_config silently drops unknown keys; docstring purged of @app.callback / --config / GRAPH_WIKI_CONFIG / set_models_path references."
      contains: "vault_path"
    - path: agents/graph-wiki-agent/src/graph_wiki_agent/cli.py
      provides: "Typer app with no --config global option; no set_models_path import."
    - path: agents/graph-wiki-agent/src/graph_wiki_mcp/server.py
      provides: "MCP main() that no longer reads GRAPH_WIKI_CONFIG and no longer imports set_models_path."
    - path: agents/graph-wiki-agent/tests/unit/test_config.py
      provides: "Updated unit tests with models_path/--config assertions removed."
  key_links:
    - from: "agents/graph-wiki-agent/src/graph_wiki_agent/cli.py"
      to: "model_adapter.loader"
      via: "the set_models_path import is REMOVED (it does not exist post-Plan-02)"
      pattern: "set_models_path"
    - from: "agents/graph-wiki-agent/src/graph_wiki_mcp/server.py"
      to: "os.environ.get('GRAPH_WIKI_CONFIG')"
      via: "REMOVED — no env-var read"
      pattern: "GRAPH_WIKI_CONFIG"
---

<objective>
Delete the orphan `wiki-config.toml` pathway from the agent side: remove `WikiConfig.models_path`, the `--config` Typer option (per CONTEXT.md recommendation: drop entirely, do not repurpose), the `GRAPH_WIKI_CONFIG` env var read in the MCP server, and the now-broken `set_models_path` imports left dangling by Plan 02. Update affected tests AND sweep residual `--config` / `GRAPH_WIKI_CONFIG` references from `agents/graph-wiki-agent/**/*.md`.

Purpose: Closes SC#3 of Phase 20 — `WikiConfig.models_path`, `set_models_path()`, `--config`, and `GRAPH_WIKI_CONFIG` are all removed; no code path remains that reads `wiki-config.toml`. Also closes the agent-side portion of SC#5 (CLI help / docs drop all `--config` references).

Output: A surgical deletion sweep across `config.py`, `cli.py`, `server.py`, `test_config.py`, and any agent-side `*.md` carrying residual references, with the affected tests passing and the CLI / MCP server still importing cleanly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-workspace-manifest-model-config/20-CONTEXT.md
@.planning/phases/20-workspace-manifest-model-config/20-02-PLAN.md
@agents/graph-wiki-agent/src/graph_wiki_agent/config.py
@agents/graph-wiki-agent/src/graph_wiki_agent/cli.py
@agents/graph-wiki-agent/src/graph_wiki_mcp/server.py
@agents/graph-wiki-agent/tests/unit/test_config.py

<interfaces>
<!-- Contracts the executor must honor. Surgical scope. -->

From `agents/graph-wiki-agent/src/graph_wiki_agent/config.py` (current — to be edited):
- `WikiConfig` dataclass fields: `models_path: str | None = None`, `vault_path: str | None = None`, `state_gate_enabled: bool = True`. **DELETE `models_path`.**
- `load_config(path: Path) -> WikiConfig` — TOML parser, drops unknown keys via `known = {k: v for k, v in data.items() if k in WikiConfig.__dataclass_fields__}`. After `models_path` is removed from the dataclass, any TOML file still carrying `models_path = "..."` will be silently dropped. No migration warning needed (TOML files are already `git rm`'d per CONTEXT.md §3).
- `_active_config` module-level singleton, `get_config()` accessor — these stay; they are still useful for `vault_path` / `state_gate_enabled` even though `--config` is gone. (Other code paths may still set `_active_config` programmatically; do not delete them.)
- Module docstring (lines 3-25) carries TWO obsolete code-fence examples — one for `@app.callback()` `--config` AND one for `os.environ.get("GRAPH_WIKI_CONFIG")`. The replacement docstring text is specified verbatim in Task 1 step 1a below.

From `agents/graph-wiki-agent/src/graph_wiki_agent/cli.py` (current — to be edited):
- Lines 35-45: the `@app.callback()` decorator on `main_callback`. The body reads `--config`, calls `load_config`, and calls `set_models_path` (which no longer exists post-Plan-02). **DELETE the option and the callback body entirely.**

From `agents/graph-wiki-agent/src/graph_wiki_mcp/server.py:442-455`:
- Current `main()` reads `GRAPH_WIKI_CONFIG` env var, calls `load_config`, calls `set_models_path`. After Plan 02 deleted `set_models_path`, this code is BROKEN at import time. **DELETE the env-var read + the two imports.** The final `main()` should be reduced to the comment + `mcp.run(transport="stdio")` call only.

From `agents/graph-wiki-agent/tests/unit/test_config.py`:
- `test_load_config_parses_all_three_fields` references `cfg.models_path == "/some/models"` — REMOVE that assertion, rename to `test_load_config_parses_remaining_fields`, drop the `models_path = "/some/models"` line from the TOML fixture.
- `test_load_config_defaults_for_missing_fields` references `cfg.models_path is None` — REMOVE that line.
- `test_typer_callback_sets_active_config` invokes `--config` — DELETE this test entirely (the callback it tested no longer exists).
- `test_load_config_drops_unknown_keys` — keep as-is; this already validates the unknown-key drop semantics that now incidentally cover the legacy-`models_path` case.

Agent-side docs sweep (warning #6 from plan-check):
- Run `grep -rln '\-\-config\|GRAPH_WIKI_CONFIG' agents/graph-wiki-agent/ --include='*.md' --include='README*'` BEFORE editing to enumerate the files to touch. As of the planning snapshot the project has no top-level `agents/graph-wiki-agent/README.md` (`find` confirmed); the sweep may still surface `*.md` files inside `agents/graph-wiki-agent/` (command help excerpts, design notes, plugin docs). For each match, delete the reference or replace it with the post-Phase-20 equivalent ("set `GRAPH_WIKI_WORKSPACE` and edit `<workspace>/.graph-wiki.yaml`"). The acceptance criterion is a 0-count recursive grep — see `<acceptance_criteria>`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete models_path / --config / GRAPH_WIKI_CONFIG plumbing + docstring rewrite</name>
  <files>agents/graph-wiki-agent/src/graph_wiki_agent/config.py, agents/graph-wiki-agent/src/graph_wiki_agent/cli.py, agents/graph-wiki-agent/src/graph_wiki_mcp/server.py</files>
  <read_first>
    - agents/graph-wiki-agent/src/graph_wiki_agent/config.py (full file — 77 lines)
    - agents/graph-wiki-agent/src/graph_wiki_agent/cli.py (lines 1-50 — the imports and the `main_callback`; the rest of the file is irrelevant to this edit)
    - agents/graph-wiki-agent/src/graph_wiki_mcp/server.py (lines 440-460 — the `main()` function body)
  </read_first>
  <action>
    Three surgical edits. Touch ONLY the documented lines — do not "improve" surrounding code per Karpathy §3.

    1. `agents/graph-wiki-agent/src/graph_wiki_agent/config.py`:

       a. **Replace the module docstring** (currently lines 3-25; contains two code-fence examples for `@app.callback()` `--config` AND `os.environ.get("GRAPH_WIKI_CONFIG")`) verbatim with the following text. Do not paraphrase — use this exact string so the grep gates land deterministically:

          ```python
          """WikiConfig — shared configuration module for graph-wiki-agent.

          Provides:
          - WikiConfig dataclass with fields for vault_path and state_gate_enabled
          - load_config(path) — reads a TOML file and returns a WikiConfig
            (unknown keys silently dropped for forward-compatibility)
          - get_config() — returns the module-level active config singleton
          - _active_config — module-level mutable singleton; available for
            programmatic configuration (no longer set by any CLI / MCP entry
            point — the `--config` / GRAPH_WIKI_CONFIG pathway was removed in
            Phase 20 / WMC-03; per-workspace config now lives in
            `<workspace>/.graph-wiki.yaml`).
          """
          ```

          That docstring has been deliberately scrubbed of `@app.callback`, `--config`, `GRAPH_WIKI_CONFIG`, and `set_models_path` — see the grep gate in `<acceptance_criteria>`.

       b. Delete the `models_path: str | None = None` field from the `WikiConfig` dataclass (line 42). Update the dataclass docstring (lines 33-44) to drop the `models_path:` bullet. The final dataclass docstring should read:
          ```
          """Runtime configuration for graph-wiki-agent.

          Fields:
              vault_path: Default vault path (overrides GRAPH_WIKI_WORKSPACE env var).
              state_gate_enabled: Whether git state-gate checks are enforced (default: True).
          """
          ```

       c. Leave `load_config`, `get_config`, `_active_config` in place — the unknown-key drop in `load_config` already handles the legacy `models_path` TOML key by silently ignoring it. No code change needed in those functions.

    2. `agents/graph-wiki-agent/src/graph_wiki_agent/cli.py`:

       a. Delete the import `Optional` from `typing` if it is no longer used after the callback removal — verify by grepping the file for other `Optional[` usages first. (Spoiler: it is used in many subcommand signatures; KEEP the import.)

       b. Delete the entire `@app.callback()` decorator and `main_callback` function (lines 35-45 in the current file). Verify by reading the file that no other code references `main_callback`.

       c. Remove the now-unused imports: `from model_adapter.loader import set_models_path` is inside the callback body and goes with it. Confirm that `graph_wiki_agent.config` is still imported elsewhere in the file (it is not; the only reference was inside the callback). The `import graph_wiki_agent.config as _cfg_module` line inside the callback is removed alongside the callback body.

       d. Do NOT touch any subcommand (`query`, `scan`, `log`, `init`, `lint`, `ingest`, `trace`, `version`). They stay as-is.

    3. `agents/graph-wiki-agent/src/graph_wiki_mcp/server.py` (lines 442-455 — the `main()` function body):

       a. Replace the current `main()` body with a minimal version that only calls `mcp.run(transport="stdio")`. Drop the `import os` line if it is no longer used elsewhere in the function (grep the rest of `server.py` first; if other code uses `os`, keep the import at module scope where it lives). Drop the `GRAPH_WIKI_CONFIG` env-var read, the `graph_wiki_agent.config` import, and the `set_models_path` import. The end-state `main()` should look like:
          ```python
          def main() -> None:
              # Be explicit about transport — do not rely on the default (RESEARCH A2).
              mcp.run(transport="stdio")
          ```
          Preserve the RESEARCH A2 comment — it is load-bearing context for why `transport="stdio"` is explicit.

       b. If the `from pathlib import Path` import was added solely for the `Path(config_path_str)` call in the deleted block AND is not used elsewhere in `server.py`, drop it. Grep first.

    4. Do NOT delete `graph_wiki_agent.config.WikiConfig`, `load_config`, `get_config`, or `_active_config` — they remain part of the public Python API even though no CLI / MCP code reads them right now. (They are still imported in tests, and `_active_config` is the singleton for programmatic configuration in tests / future surfaces.)

    5. Do NOT add any new code in this task — this is a deletion-only edit per Karpathy §3.
  </action>
  <verify>
    <automated>grep -c 'models_path' agents/graph-wiki-agent/src/graph_wiki_agent/config.py</automated>
    <automated>grep -c '@app.callback\|--config\|GRAPH_WIKI_CONFIG\|set_models_path' agents/graph-wiki-agent/src/graph_wiki_agent/config.py</automated>
    <automated>grep -c 'models_path\|--config\|set_models_path\|main_callback' agents/graph-wiki-agent/src/graph_wiki_agent/cli.py</automated>
    <automated>grep -c 'GRAPH_WIKI_CONFIG\|set_models_path\|load_config' agents/graph-wiki-agent/src/graph_wiki_mcp/server.py</automated>
    <automated>uv run --package graph-wiki-agent python -c "from graph_wiki_agent.cli import app; from graph_wiki_mcp import server; print('imports OK')"</automated>
    <automated>uv run --package graph-wiki-agent graph-wiki-agent --help 2>&amp;1 | grep -c -- '--config' | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'models_path' agents/graph-wiki-agent/src/graph_wiki_agent/config.py` returns 0.
    - `grep -c '@app.callback\|--config\|GRAPH_WIKI_CONFIG\|set_models_path' agents/graph-wiki-agent/src/graph_wiki_agent/config.py` returns 0 (warning #5 fix — docstring is fully scrubbed).
    - `grep -c 'models_path\|--config\|set_models_path\|main_callback' agents/graph-wiki-agent/src/graph_wiki_agent/cli.py` returns 0.
    - `grep -c 'GRAPH_WIKI_CONFIG\|set_models_path' agents/graph-wiki-agent/src/graph_wiki_mcp/server.py` returns 0.
    - `uv run --package graph-wiki-agent python -c "from graph_wiki_agent.cli import app"` exits 0 (no ImportError on the removed `set_models_path` symbol).
    - `uv run --package graph-wiki-agent python -c "from graph_wiki_mcp import server"` exits 0 (no ImportError; server module imports cleanly).
    - `graph-wiki-agent --help` exit code is 0 and the output does NOT contain `--config` (use `uv run --package graph-wiki-agent graph-wiki-agent --help`).
    - The `WikiConfig`, `load_config`, `get_config` symbols are still importable: `python -c "from graph_wiki_agent.config import WikiConfig, load_config, get_config"` exits 0.
  </acceptance_criteria>
  <done>
    `WikiConfig.models_path` is deleted, the `--config` Typer option is gone, the MCP server no longer reads `GRAPH_WIKI_CONFIG`, and no module imports `set_models_path` (which no longer exists). The CLI and MCP entrypoints still import cleanly. The `config.py` module docstring is fully scrubbed of obsolete references.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update test_config.py — drop models_path and --config assertions</name>
  <files>agents/graph-wiki-agent/tests/unit/test_config.py</files>
  <read_first>
    - agents/graph-wiki-agent/tests/unit/test_config.py (full file — 91 lines)
    - agents/graph-wiki-agent/src/graph_wiki_agent/config.py (post-Task-1 state — for the new test invariants)
  </read_first>
  <behavior>
    - `test_load_config_parses_remaining_fields` (renamed from `test_load_config_parses_all_three_fields`): TOML carries `vault_path` and `state_gate_enabled` only; assertions check those two fields. No reference to `models_path`.
    - `test_load_config_drops_unknown_keys`: unchanged behavior — still validates the silent-drop semantic. Bonus: now also doubles as the implicit "legacy `models_path` key is silently dropped" test.
    - `test_load_config_defaults_for_missing_fields`: drop the `cfg.models_path is None` line; the remaining `vault_path is None` + `state_gate_enabled is True` assertions stay.
    - `test_typer_callback_sets_active_config`: DELETED. The callback no longer exists.
    - After edits, `uv run --package graph-wiki-agent pytest tests/unit/test_config.py -x` passes with three tests (down from four), all green.
  </behavior>
  <action>
    Surgical test-file edit. Do NOT add new tests; only update / delete the existing ones to reflect the post-deletion contract.

    Implementation steps:

    1. Update the module docstring (lines 1-6). The current docstring claims "CLI-05 (--config global flag), D-11, D-12, D-13". Replace with: "Unit tests for graph_wiki_agent.config module — exercises load_config TOML parsing and the _active_config singleton. (CLI-05 / --config plumbing was removed in Phase 20 / WMC-03.)"

    2. Rename `test_load_config_parses_all_three_fields` → `test_load_config_parses_remaining_fields`. Drop `'models_path = "/some/models"\n'` from the TOML fixture so the input is:
       ```
       'vault_path = "/my/vault"\nstate_gate_enabled = false\n'
       ```
       Drop the `assert cfg.models_path == "/some/models"` line. The two remaining assertions on `vault_path` and `state_gate_enabled` stay.

    3. In `test_load_config_defaults_for_missing_fields`, delete the line `assert cfg.models_path is None`. The TOML fixture stays unchanged (it already only sets `state_gate_enabled`), and the remaining two assertions stay.

    4. Delete `test_typer_callback_sets_active_config` entirely (lines 76-91). The callback it tested no longer exists; keeping the test would either fail at the missing `--config` option or test stale behavior.

    5. Remove the now-unused import `from typer.testing import CliRunner` from inside the deleted function — it has no other use in the file. Also drop the section-header comment `# Typer callback test` (lines 72-75) that introduces only the deleted test.

    6. Leave `test_load_config_drops_unknown_keys` unchanged.

    7. Do NOT add new assertions, fixtures, or tests in this pass. The only goal is to bring `test_config.py` in sync with the post-deletion `config.py` and `cli.py`.
  </action>
  <verify>
    <automated>uv run --package graph-wiki-agent pytest tests/unit/test_config.py -x -v</automated>
    <automated>grep -c 'models_path' agents/graph-wiki-agent/tests/unit/test_config.py</automated>
    <automated>grep -c 'test_typer_callback_sets_active_config\|--config' agents/graph-wiki-agent/tests/unit/test_config.py</automated>
    <automated>grep -c '^def test_' agents/graph-wiki-agent/tests/unit/test_config.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'models_path' agents/graph-wiki-agent/tests/unit/test_config.py` returns 0.
    - `grep -c 'test_typer_callback_sets_active_config' agents/graph-wiki-agent/tests/unit/test_config.py` returns 0.
    - `grep -c -- '--config' agents/graph-wiki-agent/tests/unit/test_config.py` returns 0.
    - `grep -c '^def test_' agents/graph-wiki-agent/tests/unit/test_config.py` returns exactly 3.
    - `uv run --package graph-wiki-agent pytest tests/unit/test_config.py -x` exits 0 with 3 passing tests.
    - Full agent suite stays green: `uv run --package graph-wiki-agent pytest -x` exits 0 (no regression elsewhere).
  </acceptance_criteria>
  <done>
    `test_config.py` no longer references `models_path` or `--config`. Three tests cover the remaining surface (parse, drop-unknown, defaults). The full graph-wiki-agent test suite passes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Agent-side docs sweep — purge --config / GRAPH_WIKI_CONFIG from agents/graph-wiki-agent/**/*.md</name>
  <files>agents/graph-wiki-agent/</files>
  <read_first>
    - Run `grep -rln -- '--config\|GRAPH_WIKI_CONFIG' agents/graph-wiki-agent/ --include='*.md' --include='README*' 2>/dev/null` to enumerate matching files. As of the planning snapshot, no top-level `agents/graph-wiki-agent/README.md` exists, but `*.md` files may live under subdirectories. Read each match before editing — do not assume the file list.
  </read_first>
  <action>
    Recursive doc sweep across `agents/graph-wiki-agent/`. Touch ONLY the documented surface — Markdown files (`*.md`) and `README*` files. Do not edit Python source (Task 1 already covered that) or test files (Task 2 covered those).

    Implementation steps:

    1. Enumerate the working set:
       ```bash
       grep -rln -- '--config\|GRAPH_WIKI_CONFIG' agents/graph-wiki-agent/ --include='*.md' --include='README*' 2>/dev/null
       ```

    2. For each file in the working set, read it fully and decide:
       - **Help-text excerpt referencing the removed `--config` option:** rewrite the excerpt to reflect the current CLI shape. The remaining global options after Plan 03 are none — drop the entire excerpt or replace it with a pointer to `graph-wiki-agent --help` for the live truth.
       - **Usage examples like `graph-wiki-agent --config wiki-config.toml scan ...`:** rewrite to the post-Phase-20 equivalent: `graph-wiki-agent scan ...` (with `GRAPH_WIKI_WORKSPACE=<path>` env-var export shown alongside if vault selection is being demonstrated).
       - **Design notes referencing `GRAPH_WIKI_CONFIG` or the `--config` design rationale:** mark the section as historical context with a one-line note: `(Removed in Phase 20 / WMC-03 — per-workspace config now lives in <workspace>/.graph-wiki.yaml.)` and delete the now-invalid example code or commands. Preserve the surrounding narrative if it still makes sense without the deleted lines (Karpathy §3 — surgical).

    3. If a file's ENTIRE purpose was documenting `--config` / `GRAPH_WIKI_CONFIG` (unlikely but possible), delete it. Surface the deletion in the summary so it is reviewable; do NOT delete silently.

    4. Do NOT edit files under `agents/graph-wiki-agent/tests/`. Tests are surgical-fixture territory; if a fixture `*.md` still references `--config`, leave it (these are frozen test artifacts, not live docs). Confirm the recursive grep below excludes the tests dir; if it does not, narrow the gate explicitly:
       ```bash
       grep -rln -- '--config\|GRAPH_WIKI_CONFIG' agents/graph-wiki-agent/ \
         --include='*.md' --include='README*' \
         --exclude-dir='tests' --exclude-dir='.pytest_cache' 2>/dev/null
       ```

    5. After edits, re-run the enumeration command (with the test-dir exclusion) and confirm 0 matches. Record the list of files touched in the SUMMARY.

    6. Do NOT touch:
       - `agents/graph-wiki-agent/tests/**` (fixtures / test artifacts).
       - `agents/graph-wiki-agent/.pytest_cache/**` (auto-generated).
       - `.planning/**` history files (historical context, not live docs).
  </action>
  <verify>
    <automated>grep -rln -- '--config\|GRAPH_WIKI_CONFIG' agents/graph-wiki-agent/ --include='*.md' --include='README*' --exclude-dir='tests' --exclude-dir='.pytest_cache' 2>/dev/null | wc -l | tr -d ' '</automated>
    <automated>grep -rln 'wiki-config\.toml' agents/graph-wiki-agent/ --include='*.md' --exclude-dir='tests' --exclude-dir='.pytest_cache' 2>/dev/null | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rln -- '--config\|GRAPH_WIKI_CONFIG' agents/graph-wiki-agent/ --include='*.md' --include='README*' --exclude-dir='tests' --exclude-dir='.pytest_cache' 2>/dev/null` returns NO matches (the line-count gate above returns `0`).
    - `grep -rln 'wiki-config\.toml' agents/graph-wiki-agent/ --include='*.md' --exclude-dir='tests' --exclude-dir='.pytest_cache' 2>/dev/null` returns NO matches (line-count gate returns `0`). This catches stale TOML-file mentions in usage examples even when the `--config` flag itself is not present.
    - The agent's runtime test suite still passes: `uv run --package graph-wiki-agent pytest -x` exits 0 (a defensive cross-check that doc edits did not accidentally touch a fixture).
  </acceptance_criteria>
  <done>
    All live agent-side docs (`*.md`, `README*` under `agents/graph-wiki-agent/`, excluding the tests directory and pytest cache) are free of `--config`, `GRAPH_WIKI_CONFIG`, and `wiki-config.toml` references. The agent-side SC#5 docs portion is closed.
  </done>
</task>

</tasks>

<verification>
- `uv run --package graph-wiki-agent pytest -x` exits 0.
- `grep -rn "set_models_path\|GRAPH_WIKI_CONFIG\|models_path" agents/graph-wiki-agent/src/` returns 0 matches.
- `grep -rln -- '--config\|GRAPH_WIKI_CONFIG' agents/graph-wiki-agent/ --include='*.md' --include='README*' --exclude-dir='tests' --exclude-dir='.pytest_cache'` returns 0 file matches.
- `graph-wiki-agent --help` exits 0 and does not advertise `--config`.
- `python -c "from graph_wiki_mcp import server"` exits 0 (MCP main() imports cleanly).
- Cross-package check: `uv run --package model-adapter pytest -x` still exits 0 (Plan 02 invariants hold).
- Cross-package check: `uv run --package workspace-io pytest -x` still exits 0 (Plan 01 invariants hold).
</verification>

<success_criteria>
- SC#3 (deletion of `WikiConfig.models_path`, `set_models_path()`, `--config`, `GRAPH_WIKI_CONFIG`; no code path remains that reads a `wiki-config.toml`): satisfied by Task 1 and verified by the cross-source greps in `<verification>`.
- SC#5 agent-side docs portion (CLI help / docs drop all `--config` references; warning #6 from plan-check): satisfied by Task 3's recursive grep gate.
- Test suites continue to pass across `workspace-io`, `model-adapter`, and `graph-wiki-agent`.
</success_criteria>

<output>
Create `.planning/phases/20-workspace-manifest-model-config/20-03-SUMMARY.md` when done. Include the list of `*.md` files touched in Task 3.
</output>
