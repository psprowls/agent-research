---
phase: 19-phase-16-code-review-burndown
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/subagent-runtime/src/subagent_runtime/pool.py
  - agents/graph-wiki-agent/src/graph_wiki_agent/commands/ingest.py
autonomous: true
requirements: [REVIEW-01]
must_haves:
  truths:
    - "`inspect.signature(task)` is computed once per fan-out, not once per item"
    - "`ingest._route_target_path` uses `Path.is_relative_to` for containment checks (codebase idiom)"
  artifacts:
    - path: "packages/subagent-runtime/src/subagent_runtime/pool.py"
      provides: "SubagentPool with hoisted signature inspection"
      contains: "inspect.signature"
    - path: "agents/graph-wiki-agent/src/graph_wiki_agent/commands/ingest.py"
      provides: "Ingest with `is_relative_to`-based containment check"
      contains: "is_relative_to"
  key_links:
    - from: "pool.py fan-out dispatch"
      to: "task signature arity check"
      via: "hoisted single computation with try/except fallback"
      pattern: "inspect.signature"
    - from: "ingest.py `_route_target_path`"
      to: "Path.is_relative_to"
      via: "pathlib idiom (matches query.py:356)"
      pattern: "is_relative_to"
---

<objective>
Land two core-runtime warning-severity fixes from CONTEXT.md D-05 and D-06. The first hoists a per-item `inspect.signature(task)` call out of the inner loop in `SubagentPool._run_one`. The second converges `commands/ingest.py::_route_target_path` on the `Path.is_relative_to` idiom already used by `commands/query.py:356`.

Purpose: Remove a per-item reflection overhead in the fan-out hot path (D-05) and align `ingest.py` with the codebase's pathlib idiom (D-06) — both warning-severity, neither user-facing.
Output: Two source files updated; existing subagent-runtime and graph-wiki-agent unit tests pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-phase-16-code-review-burndown/19-CONTEXT.md
@.planning/milestones/v1.2-phases/16-carry-forward-debt-cleanup/16-REVIEW.md

@packages/subagent-runtime/src/subagent_runtime/pool.py
@agents/graph-wiki-agent/src/graph_wiki_agent/commands/ingest.py
@agents/graph-wiki-agent/src/graph_wiki_agent/commands/query.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-05 — hoist `inspect.signature(task)` out of `_run_one` in pool.py</name>
  <files>packages/subagent-runtime/src/subagent_runtime/pool.py</files>
  <read_first>
    - packages/subagent-runtime/src/subagent_runtime/pool.py (full file; locate `_run_one` and the `inspect.signature(task)` call around lines 133-137)
    - .planning/milestones/v1.2-phases/16-carry-forward-debt-cleanup/16-REVIEW.md (WR-05 narrative + proposed fix)
    - packages/subagent-runtime/tests/ (grep `_run_one`, `inspect`, `signature` to locate coverage)
  </read_first>
  <action>
    Per CONTEXT.md D-05: hoist the `inspect.signature(task)` call from inside `_run_one` (per-item, called for every fan-out item) to the enclosing dispatch site (per-fan-out, called once). Compute the signature once with `try/except (ValueError, TypeError)` falling back to the single-arg form (existing fallback behavior must be preserved). Pass the arity decision (or the resolved Signature object) into `_run_one`, or close over it via the enclosing function — whichever yields the smallest delta.

    Preserve existing semantics exactly:
    - Callable with `(item, config)` signature → invoked with `(item, config)`.
    - Callable without recognizable signature (ValueError/TypeError) → invoked with `(item,)` only.
    - Any other arity behavior currently in `_run_one` must continue to work.

    Do not change `SubagentPool` public API or `_run_one` external behavior. Do not add new error types.
  </action>
  <verify>
    <automated>uv sync && uv run pytest packages/subagent-runtime/tests/ -m "not integration" -x</automated>
  </verify>
  <done>
    - `grep -c "inspect.signature" packages/subagent-runtime/src/subagent_runtime/pool.py` shows the call appears exactly once (was N+ before for N items in a fan-out, conceptually).
    - The single `inspect.signature(...)` call sits OUTSIDE `_run_one`'s body (i.e., at the enclosing dispatch level — verifiable by reading the function structure).
    - `try/except (ValueError, TypeError)` wraps the signature call with the single-arg fallback preserved.
    - `uv run pytest packages/subagent-runtime/tests/ -m "not integration"` exits 0.
  </done>
  <acceptance_criteria>
    - The `inspect.signature(task)` call appears exactly once and is located in the dispatch path that calls `_run_one`, not in `_run_one` itself.
    - The fallback behavior on `ValueError`/`TypeError` matches the prior behavior (single-arg invocation).
    - All existing `packages/subagent-runtime/tests/test_pool*.py` (or equivalent) pass without modification.
    - `uv run pytest packages/subagent-runtime/tests/ -m "not integration"` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: D-06 — switch `_route_target_path` to `Path.is_relative_to` in ingest.py</name>
  <files>agents/graph-wiki-agent/src/graph_wiki_agent/commands/ingest.py</files>
  <read_first>
    - agents/graph-wiki-agent/src/graph_wiki_agent/commands/ingest.py (full file; locate `_route_target_path` at lines ~101-106)
    - agents/graph-wiki-agent/src/graph_wiki_agent/commands/query.py (line ~356 — canonical `Path.is_relative_to` usage in this codebase; the analog/source-of-truth idiom)
    - .planning/milestones/v1.2-phases/16-carry-forward-debt-cleanup/16-REVIEW.md (WR-06 narrative + proposed fix)
    - agents/graph-wiki-agent/tests/ (grep `_route_target_path` and `ingest` for coverage)
  </read_first>
  <action>
    Per CONTEXT.md D-06: replace the current containment check in `_route_target_path` (whatever string-comparison or `commonpath`-style check is there) with a `Path.is_relative_to` call. Mirror the idiom used in `agents/graph-wiki-agent/src/graph_wiki_agent/commands/query.py:356`. Per CONTEXT.md this is NOT a runtime bug today (project is macOS/Linux only and CLAUDE.md confirms platform constraint) — the fix is purely codebase-idiom convergence per WR-06's locked disposition.

    Do not introduce a backwards-compat shim or fallback path for non-`is_relative_to` Pythons — Python 3.11+ is the floor (CLAUDE.md). Do not extend the function's contract; preserve the existing return shape and error behavior exactly.

    If the old implementation imported anything (e.g., `os.path`) that is now unused after the swap, remove the dead import per CLAUDE.md "no dead code" rule.
  </action>
  <verify>
    <automated>uv sync && uv run pytest agents/graph-wiki-agent/tests/ -m "not integration" -x</automated>
  </verify>
  <done>
    - `grep -n "is_relative_to" agents/graph-wiki-agent/src/graph_wiki_agent/commands/ingest.py` shows `_route_target_path` (or a helper it calls) uses `Path.is_relative_to`.
    - No `commonpath`, no `str(...).startswith(str(...))`-style containment check remains in `_route_target_path` (verifiable via reading the updated function body).
    - `uv run pytest agents/graph-wiki-agent/tests/ -m "not integration"` exits 0.
  </done>
  <acceptance_criteria>
    - `_route_target_path` uses `Path.is_relative_to` (matching `query.py:356` idiom).
    - The function's return type, return values, and raised exceptions are unchanged from the prior version (smoke-checked against existing tests).
    - Existing `agents/graph-wiki-agent/tests/` coverage for ingest's route-target-path behavior continues to pass without modification.
    - `uv run pytest agents/graph-wiki-agent/tests/ -m "not integration"` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | D-05 is a hot-path refactor with identical semantics; D-06 is an idiom swap with identical semantics on macOS/Linux (project's only supported targets per CLAUDE.md) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-02 | (n/a) | `packages/subagent-runtime/src/subagent_runtime/pool.py` | accept | Pure refactor — signature inspection moved up one stack frame; no behavior change |
| T-19-03 | (n/a) | `agents/graph-wiki-agent/src/graph_wiki_agent/commands/ingest.py` | accept | Idiom swap to `Path.is_relative_to`; identical semantics on macOS/Linux (CLAUDE.md confirms platform support) |
| T-19-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies installed by this plan |
</threat_model>

<verification>
- `uv run pytest packages/subagent-runtime/tests/ -m "not integration"` exits 0 after task 1.
- `uv run pytest agents/graph-wiki-agent/tests/ -m "not integration"` exits 0 after task 2.
- Per-commit gate at plan close: `uv sync && uv run pytest packages/eval-harness/tests/ packages/subagent-runtime/tests/ agents/graph-wiki-agent/tests/ -m "not integration"` exits 0.
</verification>

<success_criteria>
- D-05 and D-06 implemented per CONTEXT.md decisions.
- Per-commit gate (D-18) passes at plan completion.
- 19-REVIEW-BURNDOWN.md rows for WR-05 and WR-06 will be populated by plan 05 (table consolidation).
</success_criteria>

<output>
Create `.planning/phases/19-phase-16-code-review-burndown/19-02-SUMMARY.md` when done.
Record commit SHA for plan 05's burndown table.
</output>
