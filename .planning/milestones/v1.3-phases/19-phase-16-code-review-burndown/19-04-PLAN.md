---
phase: 19-phase-16-code-review-burndown
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/graph-wiki-agent/src/graph_wiki_agent/commands/query.py
  - docs/cancellation.md
autonomous: true
requirements: [REVIEW-02]
must_haves:
  truths:
    - "`_extract_usage_tokens`'s docstring in query.py points at the canonical home `subagent_runtime.trace_io.write_trace_record:56-66`"
    - "Synth trace filenames are distinguishable: regular path uses `synth_librarian_{query_id}.jsonl`, code-fallback path uses `synth_codefallback_{query_id}.jsonl`"
    - "Both JSON example blocks in `docs/cancellation.md` include `\"schema_version\": 1`"
    - "`apply_guardrails` G1 path calls `_compute_unresolved_wikilinks` instead of re-implementing it inline"
  artifacts:
    - path: "agents/graph-wiki-agent/src/graph_wiki_agent/commands/query.py"
      provides: "Query module with corrected docstring, qualified synth filenames, deduplicated G1 logic"
      contains: "_compute_unresolved_wikilinks"
    - path: "docs/cancellation.md"
      provides: "Cancellation doc with schema_version-complete JSON examples"
      contains: "schema_version"
  key_links:
    - from: "query.py `_extract_usage_tokens` docstring"
      to: "subagent_runtime.trace_io.write_trace_record:56-66"
      via: "post-D-04 extraction reference"
      pattern: "trace_io"
    - from: "query.py `apply_guardrails` G1 branch"
      to: "query.py `_compute_unresolved_wikilinks` helper (lines 551-570)"
      via: "function call instead of inline duplication"
      pattern: "_compute_unresolved_wikilinks"
    - from: "query.py:532 (code-fallback synth path)"
      to: "trace file `synth_codefallback_{query_id}.jsonl`"
      via: "filename literal change"
      pattern: "synth_codefallback_"
    - from: "query.py:968 (regular synth path)"
      to: "trace file `synth_librarian_{query_id}.jsonl`"
      via: "filename literal change"
      pattern: "synth_librarian_"
---

<objective>
Land the four query.py + docs cleanup fixes from CONTEXT.md D-07 (IN-01), D-12 (IN-06), D-14 (IN-08), and D-15 (IN-09).

Purpose: Restore one stale docstring reference (D-07), prevent future synth-trace filename collisions (D-12), make the cancellation-doc JSON examples schema-complete (D-14), and remove a drift-bait duplicate G1 algorithm (D-15).
Output: Two files updated; existing tests pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-phase-16-code-review-burndown/19-CONTEXT.md
@.planning/milestones/v1.2-phases/16-carry-forward-debt-cleanup/16-REVIEW.md

@agents/graph-wiki-agent/src/graph_wiki_agent/commands/query.py
@packages/subagent-runtime/src/subagent_runtime/trace_io.py
@docs/cancellation.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-07 + D-15 — fix docstring + dedupe G1 in query.py apply_guardrails</name>
  <files>agents/graph-wiki-agent/src/graph_wiki_agent/commands/query.py</files>
  <read_first>
    - agents/graph-wiki-agent/src/graph_wiki_agent/commands/query.py (full file; locate `_extract_usage_tokens` docstring at lines ~283-286, `_compute_unresolved_wikilinks` helper at lines ~551-570, and `apply_guardrails` G1 logic at lines ~663-670)
    - packages/subagent-runtime/src/subagent_runtime/trace_io.py (the canonical post-D-04 home of `write_trace_record`; confirm line range 56-66 — adjust to actual current line range if drifted)
    - .planning/milestones/v1.2-phases/16-carry-forward-debt-cleanup/16-REVIEW.md (IN-01 and IN-09 narratives)
  </read_first>
  <action>
    **D-07:** Update the `_extract_usage_tokens` docstring at lines ~283-286 in `query.py` so it points at `subagent_runtime.trace_io.write_trace_record:56-66` (the canonical home after Phase 16 D-04 extraction). If the actual line range in the current `trace_io.py` has drifted from 56-66, use the current correct range — read `trace_io.py` first to confirm. Follow Phase 10's `# Source: / # Anchor:` provenance norm: restore a correct line reference, do not drop the reference entirely.

    **D-15:** Refactor `apply_guardrails` at lines ~663-670 to call `_compute_unresolved_wikilinks` (defined at lines ~551-570) instead of duplicating the G1 resolution algorithm inline. After the swap, the G1 branch in `apply_guardrails` should consist of a single call to `_compute_unresolved_wikilinks` plus whatever surrounding bookkeeping `apply_guardrails` needs (assignment / decision-making on the result). The behavior — what counts as "unresolved" — must remain identical, because both algorithms produce identical output on every input.

    Do not change `_compute_unresolved_wikilinks`'s signature or behavior. Do not add new helpers.
  </action>
  <verify>
    <automated>uv sync && uv run pytest agents/graph-wiki-agent/tests/ -m "not integration" -x</automated>
  </verify>
  <done>
    - `grep -n "trace_io" agents/graph-wiki-agent/src/graph_wiki_agent/commands/query.py` shows the `_extract_usage_tokens` docstring references `subagent_runtime.trace_io.write_trace_record` with a line range.
    - `grep -c "_compute_unresolved_wikilinks" agents/graph-wiki-agent/src/graph_wiki_agent/commands/query.py` shows at least 2 references (one definition + one new call from `apply_guardrails`).
    - The G1 branch of `apply_guardrails` no longer contains an inline duplicate of the resolution algorithm — verifiable by reading the function body (should be short / delegating).
    - `uv run pytest agents/graph-wiki-agent/tests/ -m "not integration"` exits 0.
  </done>
  <acceptance_criteria>
    - `_extract_usage_tokens` docstring names `subagent_runtime.trace_io.write_trace_record` with a current line range (read `trace_io.py` to verify the range you write is accurate as of this commit).
    - `apply_guardrails` G1 branch calls `_compute_unresolved_wikilinks` exactly once and contains no parallel inline implementation of the same algorithm.
    - Existing `query.py` test coverage for `apply_guardrails` G1 continues to pass without modification.
    - Per-commit gate passes.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: D-12 — qualify synth trace filenames in query.py</name>
  <files>agents/graph-wiki-agent/src/graph_wiki_agent/commands/query.py</files>
  <read_first>
    - agents/graph-wiki-agent/src/graph_wiki_agent/commands/query.py (locate both synth-call sites: code-fallback path at line ~532 and regular-path at line ~968; confirm the current synth trace filename literal each one uses)
    - .planning/milestones/v1.2-phases/16-carry-forward-debt-cleanup/16-REVIEW.md (IN-06 narrative)
  </read_first>
  <action>
    Per CONTEXT.md D-12: change the synth trace filename literal at each call site so the two paths write to distinguishable files:
    - Regular path (query.py:~968): `synth_librarian_{query_id}.jsonl`
    - Code-fallback path (query.py:~532): `synth_codefallback_{query_id}.jsonl`

    The `{query_id}` placeholder follows the current f-string / format convention at each call site — preserve whatever interpolation style is already in use. Surgical change to just the filename literals; do not refactor the surrounding synth call.

    Cheap insurance against future retry-after-fallback collisions where both paths would write to the same trace file.
  </action>
  <verify>
    <automated>uv sync && uv run pytest agents/graph-wiki-agent/tests/ -m "not integration" -x</automated>
  </verify>
  <done>
    - `grep -n "synth_librarian_" agents/graph-wiki-agent/src/graph_wiki_agent/commands/query.py` returns the regular-path call site at ~line 968.
    - `grep -n "synth_codefallback_" agents/graph-wiki-agent/src/graph_wiki_agent/commands/query.py` returns the code-fallback call site at ~line 532.
    - No surviving generic / unqualified `synth_{query_id}` filename remains (verifiable via grep — should not match the old pattern at either call site).
    - `uv run pytest agents/graph-wiki-agent/tests/ -m "not integration"` exits 0.
  </done>
  <acceptance_criteria>
    - Both synth call sites use the two distinct filename prefixes per CONTEXT.md D-12.
    - Existing query.py tests that assert on synth trace filename presence (if any — check via grep) are updated only if necessary; otherwise tests pass unchanged.
    - Per-commit gate passes.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: D-14 — add `schema_version: 1` to JSON example blocks in cancellation.md</name>
  <files>docs/cancellation.md</files>
  <read_first>
    - docs/cancellation.md (full file; locate both trace JSON example blocks — at lines ~103-115 and ~121-131)
    - .planning/milestones/v1.2-phases/16-carry-forward-debt-cleanup/16-REVIEW.md (IN-08 narrative)
  </read_first>
  <action>
    Per CONTEXT.md D-14: add a `"schema_version": 1,` field to both example JSON blocks in `docs/cancellation.md` at lines ~103-115 and ~121-131. Place the field at the top of each object (conventional position for schema-version fields) and preserve indentation / trailing comma style consistent with the surrounding JSON. Doc readers using these blocks as scaffolding for a trace consumer will then produce schema-complete writers.

    Surgical edit — do not modify any other field, comment, or block content.
  </action>
  <verify>
    <automated>grep -c '"schema_version": 1' docs/cancellation.md</automated>
  </verify>
  <done>
    - `grep -c '"schema_version": 1' docs/cancellation.md` returns `2` (one occurrence per example block).
    - Both occurrences are inside JSON example blocks (verifiable by inspecting the file — they sit within the fenced JSON code blocks at lines ~103-115 and ~121-131, not in prose).
  </done>
  <acceptance_criteria>
    - Both JSON example blocks now contain `"schema_version": 1`.
    - No other field, comment, or surrounding prose modified.
    - `grep -c '"schema_version": 1' docs/cancellation.md` returns `2`.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | D-07 is docstring-only; D-12 is filename literal change with identical semantics; D-14 is doc-only; D-15 is a dedup refactor with identical output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-05 | (n/a) | `agents/graph-wiki-agent/src/graph_wiki_agent/commands/query.py` | accept | Docstring repair + helper-call dedup + filename qualifier — none changes any guardrail decision or trace contents beyond filenames |
| T-19-06 | (n/a) | `docs/cancellation.md` | accept | Documentation example adds a schema_version field; no executable surface |
| T-19-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies installed by this plan |
</threat_model>

<verification>
- Per-commit gate at plan close: `uv sync && uv run pytest packages/eval-harness/tests/ packages/subagent-runtime/tests/ agents/graph-wiki-agent/tests/ -m "not integration"` exits 0.
- `grep -c '"schema_version": 1' docs/cancellation.md` returns `2`.
- `grep -n "synth_librarian_\|synth_codefallback_" agents/graph-wiki-agent/src/graph_wiki_agent/commands/query.py` returns hits for both prefixes.
</verification>

<success_criteria>
- D-07, D-12, D-14, D-15 all implemented per CONTEXT.md decisions.
- Per-commit gate passes.
- 19-REVIEW-BURNDOWN.md rows for IN-01, IN-06, IN-08, IN-09 will be populated by plan 05.
</success_criteria>

<output>
Create `.planning/phases/19-phase-16-code-review-burndown/19-04-SUMMARY.md` when done.
Record commit SHA for plan 05's burndown table.
</output>
