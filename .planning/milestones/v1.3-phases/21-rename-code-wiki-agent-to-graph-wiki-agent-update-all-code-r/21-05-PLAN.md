---
phase: 21-rename-code-wiki-agent-to-graph-wiki-agent-update-all-code-r
plan: 05
type: execute
wave: 5
depends_on:
  - 21-04
files_modified:
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/PROJECT.md
  - .planning/RETROSPECTIVE.md
  - .planning/MILESTONES.md
  - .planning/intel/stack.json
  - .planning/threads/**/*.md
  - .planning/phases/17-vault-io-bug-fixes/**/*.md
  - .planning/phases/20-workspace-manifest-model-config/**/*.md
  - .planning/milestones/v1.0-phases/**/*.md
  - .planning/milestones/v1.1-phases/**/*.md
  - .planning/milestones/v1.2-phases/**/*.md
  - .planning/sketches/**/*.md
  - .planning/sweep/**/*.md
  - .planning/research/**/*.md
  - .planning/spikes/001-subagent-context-audit/README.md
  - .planning/spikes/002-lattice-drift-inventory/README.md
  - .planning/spikes/WRAP-UP-SUMMARY.md
  - .planning/spikes/MANIFEST.md
  - .planning/spikes/CONVENTIONS.md
  - README.md
  - CLAUDE.md
  - plugins/graph-wiki/README.md
  - plugins/graph-wiki/CLAUDE.md
  - plugins/graph-wiki/skills/graph-wiki/README.md
  - plugins/graph-wiki/.claude-plugin/plugin.json
  - .claude/skills/spike-findings-agent-research/SKILL.md
  - .claude/skills/spike-findings-agent-research/references/**/*.md
  - scripts/check-brand.sh
  - .brand-grep-allow
autonomous: true
requirements: []
must_haves:
  truths:
    - "All `.planning/` files (full historical scope per D-05 user override of R-03) have the six target tokens (`code-wiki-agent`, `code_wiki_agent`, `code-wiki-mcp`, `code_wiki_mcp`, `CODE_WIKI_`, `.code-wiki/`) rebranded — except the Phase 21 phase dir itself (allowlisted; discusses the rename) and spike-source dirs (D-07 exclusion)."
    - "Repo `README.md` + `CLAUDE.md`, plugin `README.md` + `CLAUDE.md`, `plugins/graph-wiki/skills/graph-wiki/README.md`, and `plugins/graph-wiki/.claude-plugin/plugin.json` reference `graph-wiki-agent` for current-state prose."
    - "`.claude/skills/spike-findings-agent-research/SKILL.md` + active reference files are swept per D-07; raw spike sources under `.planning/spikes/00{1,2}/sources/**` stay verbatim."
    - "`scripts/check-brand.sh` regex EXTENDS the existing Phase 12 lattice patterns with `code-wiki-agent|code_wiki_agent|code-wiki-mcp|code_wiki_mcp` per D-12 — single gate, single allowlist."
    - "`.brand-grep-allow` adds a Phase 21 section per D-13 with tight entries (only paths that would actively break if rewritten); each entry carries a `# rationale:` comment per SP-6."
    - "Final gate (SP-2): `bash scripts/check-brand.sh` exits 0 (GATE_RC=0) AND `uv run pytest` exits 0 (TEST_RC=0 via PIPESTATUS) — both stages staged with independent named exit-code capture."
    - "Phase 21 goal SC#5 satisfied: full-repo grep for the four `code-wiki-*` / `code_wiki_*` slugs returns 0 hits outside the new Phase 21 allowlist entries."
  artifacts:
    - path: "scripts/check-brand.sh"
      provides: "Extended grep gate enforcing the new brand"
      contains: "code-wiki-agent|code_wiki_agent|code-wiki-mcp|code_wiki_mcp"
    - path: ".brand-grep-allow"
      provides: "Tight Phase 21 allowlist section appended to existing Phase 12 entries"
  key_links:
    - from: "scripts/check-brand.sh"
      to: ".brand-grep-allow (Phase 21 section)"
      via: "grep -vF -f .brand-grep-allow"
      pattern: "\\.brand-grep-allow"
    - from: ".brand-grep-allow Phase 21 entries"
      to: "D-13 (tight allowlist discipline)"
      via: "every entry carries a `# rationale:` comment per SP-6"
      pattern: "# rationale:"
---

<objective>
D-09 layer 5 — final layer. Sweep the full `.planning/` historical surface (188 files per D-05 override of Phase 12's R-03), the live repo + plugin docs, and the spike-findings skill (D-07). Extend `scripts/check-brand.sh` + `.brand-grep-allow` so the new brand is enforced going forward. Run the final two-stage gate (SP-2): brand-grep + pytest.

Sub-commit cadence inside this plan (matches 21-PATTERNS.md `planning-doc-sweep` 3-way split + one gate-extension commit):

1. **Task 1:** Live planning surface (top-level `.planning/*.md`) + repo + plugin docs + spike-findings skill.
2. **Task 2:** Current-milestone phase docs (`.planning/phases/17/`, `.planning/phases/20/`) + `.planning/intel/stack.json` + `.planning/threads/`. Phase 21's own phase dir is NOT swept (it discusses the rename and naturally references both names; Task 4 allowlists it).
3. **Task 3:** Historical archives (`.planning/milestones/v1.{0,1,2}-phases/**`, `.planning/sketches/`, `.planning/sweep/`, `.planning/research/`, `.planning/spikes/{MANIFEST,CONVENTIONS,WRAP-UP-SUMMARY}.md`, `.planning/spikes/00{1,2}/README.md`). Excludes `.planning/spikes/00{1,2}/sources/**` per D-07.
4. **Task 4:** Extend `scripts/check-brand.sh` + author Phase 21 section of `.brand-grep-allow`; run SP-2 two-stage final gate.

SP-1 PIPESTATUS gate runs after each of Tasks 1-3 (pytest is trivially green for doc-only changes — the gate is the audit trail). SP-2 two-stage gate (independent `GATE_RC` + `TEST_RC` capture) runs in Task 4. SP-3 ("no fix-forward") applies between sub-commits.

Sed scripting is acceptable here per 21-PATTERNS.md `planning-doc-sweep`: "all four search terms are unambiguous (no false-positive risk — they don't appear inside other tokens)."

**Why gate-extension lands last, not first:** if `check-brand.sh` is extended before the planning sweep lands, the gate would fail catastrophically on every intermediate sub-commit (hundreds of unallowlisted hits across 188 planning files). Extending after the sweep means the gate's first run is meaningful proof the sweep is complete.

**Worktree merge-back is intentionally NOT in this plan.** After all 5 plans land green on `rename-21`, the user decides when/how to merge into `main` (per D-08 worktree posture). The phase is complete when `rename-21` is green; the merge is a separate operator action outside Phase 21's scope.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/21-rename-code-wiki-agent-to-graph-wiki-agent-update-all-code-r/21-CONTEXT.md
@.planning/phases/21-rename-code-wiki-agent-to-graph-wiki-agent-update-all-code-r/21-PATTERNS.md
@.planning/phases/21-rename-code-wiki-agent-to-graph-wiki-agent-update-all-code-r/21-04-SUMMARY.md
@scripts/check-brand.sh
@.brand-grep-allow
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep live planning surface + repo/plugin docs + spike-findings skill (first sub-commit)</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/PROJECT.md, .planning/RETROSPECTIVE.md, .planning/MILESTONES.md, README.md, CLAUDE.md, plugins/graph-wiki/README.md, plugins/graph-wiki/CLAUDE.md, plugins/graph-wiki/skills/graph-wiki/README.md, plugins/graph-wiki/.claude-plugin/plugin.json, .claude/skills/spike-findings-agent-research/SKILL.md, .claude/skills/spike-findings-agent-research/references/**/*.md</files>
  <read_first>
    - 21-PATTERNS.md §"Class: `planning-doc-sweep`" sub-commit 1 ("Live planning surface")
    - 21-PATTERNS.md §"Class: `repo-doc` + `plugin-doc`" (folded into this sub-commit)
    - 21-PATTERNS.md §"Class: `skill-doc-sweep`" (D-07 inclusion: SKILL.md + active refs; exclusion: raw spike sources)
    - 21-CONTEXT.md D-05 (full historical sweep override of R-03), D-07 (skill scope), D-13 (tight allowlist)
    - .planning/milestones/v1.2-phases/12-drift-backport-ecosystem-rebrand-m2/12-03-rebrand-sweep-PLAN.md Task 4 — analog (live planning surface rebrand) with disambiguation rule INVERTED for Phase 21 (default: rebrand)
    - Each file with a hit, before editing (Read once per file)
  </read_first>
  <action>
    Working directory: `../agent-research-rename`. Start every Bash call with `cd ../agent-research-rename`.

    Step 1 — Scope grep:

    ```bash
    cd ../agent-research-rename
    grep -rEln 'code-wiki-agent|code_wiki_agent|code-wiki-mcp|code_wiki_mcp|CODE_WIKI_|\.code-wiki/' \
      .planning/STATE.md .planning/ROADMAP.md .planning/REQUIREMENTS.md \
      .planning/PROJECT.md .planning/RETROSPECTIVE.md .planning/MILESTONES.md \
      2>/dev/null | tee /tmp/21-05-t1-planning.txt
    grep -rEln 'code-wiki-agent|code_wiki_agent|code-wiki-mcp|code_wiki_mcp|CODE_WIKI_|\.code-wiki/' \
      README.md CLAUDE.md \
      plugins/graph-wiki/README.md plugins/graph-wiki/CLAUDE.md \
      plugins/graph-wiki/skills/graph-wiki/README.md \
      plugins/graph-wiki/.claude-plugin/plugin.json \
      2>/dev/null | tee /tmp/21-05-t1-docs.txt
    grep -rEln 'code-wiki-agent|code_wiki_agent|code-wiki-mcp|code_wiki_mcp|CODE_WIKI_|\.code-wiki/' \
      .claude/skills/spike-findings-agent-research/SKILL.md \
      .claude/skills/spike-findings-agent-research/references/ \
      2>/dev/null | tee /tmp/21-05-t1-skill.txt
    ```

    Step 2 — Six-pattern sed sweep:

    ```bash
    cd ../agent-research-rename
    FILES=$(cat /tmp/21-05-t1-planning.txt /tmp/21-05-t1-docs.txt /tmp/21-05-t1-skill.txt | sort -u)
    for f in $FILES; do
      [ -z "$f" ] && continue
      sed -i '' \
        -e 's/code-wiki-agent/graph-wiki-agent/g' \
        -e 's/code-wiki-mcp/graph-wiki-mcp/g' \
        -e 's/code_wiki_agent/graph_wiki_agent/g' \
        -e 's/code_wiki_mcp/graph_wiki_mcp/g' \
        -e 's/CODE_WIKI_/GRAPH_WIKI_/g' \
        -e 's|\.code-wiki/|.graph-wiki/|g' \
        "$f"
    done
    ```

    Step 3 — Verify, gate (SP-1 PIPESTATUS), commit:

    ```bash
    cd ../agent-research-rename
    for f in .planning/STATE.md .planning/ROADMAP.md .planning/REQUIREMENTS.md \
             .planning/PROJECT.md .planning/RETROSPECTIVE.md .planning/MILESTONES.md \
             README.md CLAUDE.md \
             plugins/graph-wiki/README.md plugins/graph-wiki/CLAUDE.md \
             plugins/graph-wiki/skills/graph-wiki/README.md \
             plugins/graph-wiki/.claude-plugin/plugin.json; do
      [ -f "$f" ] || continue
      if grep -qE 'code-wiki-agent|code_wiki_agent|code-wiki-mcp|code_wiki_mcp|CODE_WIKI_|\.code-wiki/' "$f"; then
        echo "RESIDUAL in $f"
        grep -nE 'code-wiki-agent|code_wiki_agent|code-wiki-mcp|code_wiki_mcp|CODE_WIKI_|\.code-wiki/' "$f"
        exit 1
      fi
    done
    RS=$(grep -rEl 'code-wiki-agent|code_wiki_agent|code-wiki-mcp|code_wiki_mcp|CODE_WIKI_|\.code-wiki/' \
      .claude/skills/spike-findings-agent-research/SKILL.md \
      .claude/skills/spike-findings-agent-research/references/ 2>/dev/null || true)
    test -z "$RS" || { echo "RESIDUAL in skill: $RS"; exit 1; }

    uv sync 2>&1 | tail -3
    SYNC_RC=${PIPESTATUS[0]}
    test "$SYNC_RC" -eq 0 || exit 1
    uv run pytest -m "not integration" 2>&1 | tail -10
    PYTEST_RC=${PIPESTATUS[0]}
    test "$PYTEST_RC" -eq 0 || { echo "PYTEST FAILED rc=$PYTEST_RC — SP-3 revert"; exit 1; }

    git add -A
    git status
    git commit -m "docs(21): rebrand code-wiki-agent in live planning + repo/plugin docs + skill"
    ```
  </action>
  <verify>
    <automated>cd ../agent-research-rename && for f in .planning/STATE.md .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/PROJECT.md .planning/RETROSPECTIVE.md .planning/MILESTONES.md README.md CLAUDE.md plugins/graph-wiki/README.md plugins/graph-wiki/CLAUDE.md plugins/graph-wiki/skills/graph-wiki/README.md plugins/graph-wiki/.claude-plugin/plugin.json; do [ -f "$f" ] || continue; if grep -qE 'code-wiki-agent|code_wiki_agent|code-wiki-mcp|code_wiki_mcp|CODE_WIKI_|\.code-wiki/' "$f"; then echo "RESIDUAL in $f"; grep -nE 'code-wiki-agent|code_wiki_agent|code-wiki-mcp|code_wiki_mcp|CODE_WIKI_|\.code-wiki/' "$f"; exit 1; fi; done; RS=$(grep -rEl 'code-wiki-agent|code_wiki_agent|code-wiki-mcp|code_wiki_mcp|CODE_WIKI_|\.code-wiki/' .claude/skills/spike-findings-agent-research/SKILL.md .claude/skills/spike-findings-agent-research/references/ 2>/dev/null || true); test -z "$RS" || { echo "RESIDUAL skill: $RS"; exit 1; }; uv sync 2>&1 | tail -3; SYNC_RC=${PIPESTATUS[0]}; test "$SYNC_RC" -eq 0 || exit 1; uv run pytest -m "not integration" 2>&1 | tail -10; PYTEST_RC=${PIPESTATUS[0]}; test "$PYTEST_RC" -eq 0 || { echo "PYTEST FAILED rc=$PYTEST_RC"; exit 1; }; HEAD_SUBJECT=$(git log -1 --format='%s'); echo "$HEAD_SUBJECT" | grep -qE '^docs\(21\): rebrand code-wiki-agent in live planning' || { echo "HEAD subject mismatch: $HEAD_SUBJECT"; exit 1; }</automated>
  </verify>
  <acceptance_criteria>
    - The 12 listed live-surface files contain zero of the six target tokens.
    - `.claude/skills/spike-findings-agent-research/SKILL.md` + everything under `references/` is clean.
    - Spike-source dirs untouched (D-07) — verified implicitly because the scope grep excluded them.
    - `uv run pytest -m "not integration"` exits 0 (PIPESTATUS-captured).
    - HEAD commit subject matches `^docs\(21\): rebrand code-wiki-agent in live planning`.
  </acceptance_criteria>
  <done>Sub-commit 1 landed; live surfaces clean; pytest non-integration green.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep current-milestone phase docs (17 + 20) + intel/stack.json + threads/ (second sub-commit)</name>
  <files>.planning/phases/17-vault-io-bug-fixes/**/*.md, .planning/phases/20-workspace-manifest-model-config/**/*.md, .planning/intel/stack.json, .planning/threads/**/*.md</files>
  <read_first>
    - 21-PATTERNS.md §"Class: `planning-doc-sweep`" sub-commit 2 ("Current-milestone phase docs")
    - 21-CONTEXT.md D-05 — full historical sweep; Phase 21's own dir is NOT swept (it documents the rename); Task 4 adds it to the allowlist (mirroring Phase 12's `.brand-grep-allow` entry for its own dir, lines 73-75 of current `.brand-grep-allow`)
    - 21-PATTERNS.md §"SP-3" — no fix-forward if pytest reds between sub-commits
    - Sample files from each phase dir (Phase 17 + 20 — each has CONTEXT/PLAN/SUMMARY shape) before editing
  </read_first>
  <action>
    Working directory: `../agent-research-rename`.

    Step 1 — Scope grep (explicitly excluding Phase 21's own phase dir):

    ```bash
    cd ../agent-research-rename
    grep -rEln 'code-wiki-agent|code_wiki_agent|code-wiki-mcp|code_wiki_mcp|CODE_WIKI_|\.code-wiki/' \
      .planning/phases/17-vault-io-bug-fixes/ \
      .planning/phases/20-workspace-manifest-model-config/ \
      .planning/intel/stack.json \
      .planning/threads/ \
      2>/dev/null | tee /tmp/21-05-t2-scope.txt
    ```

    Step 2 — Sweep:

    ```bash
    cd ../agent-research-rename
    FILES=$(cat /tmp/21-05-t2-scope.txt | sort -u)
    for f in $FILES; do
      [ -z "$f" ] && continue
      sed -i '' \
        -e 's/code-wiki-agent/graph-wiki-agent/g' \
        -e 's/code-wiki-mcp/graph-wiki-mcp/g' \
        -e 's/code_wiki_agent/graph_wiki_agent/g' \
        -e 's/code_wiki_mcp/graph_wiki_mcp/g' \
        -e 's/CODE_WIKI_/GRAPH_WIKI_/g' \
        -e 's|\.code-wiki/|.graph-wiki/|g' \
        "$f"
    done
    ```

    Step 3 — Verify, gate, commit:

    ```bash
    cd ../agent-research-rename
    RESIDUAL=$(grep -rEl 'code-wiki-agent|code_wiki_agent|code-wiki-mcp|code_wiki_mcp|CODE_WIKI_|\.code-wiki/' \
      .planning/phases/17-vault-io-bug-fixes/ \
      .planning/phases/20-workspace-manifest-model-config/ \
      .planning/intel/stack.json \
      .planning/threads/ 2>/dev/null || true)
    test -z "$RESIDUAL" || { echo "RESIDUAL: $RESIDUAL"; exit 1; }

    uv sync 2>&1 | tail -3
    SYNC_RC=${PIPESTATUS[0]}
    test "$SYNC_RC" -eq 0 || exit 1
    uv run pytest -m "not integration" 2>&1 | tail -10
    PYTEST_RC=${PIPESTATUS[0]}
    test "$PYTEST_RC" -eq 0 || { echo "PYTEST FAILED rc=$PYTEST_RC"; exit 1; }

    git add -A
    git status
    git commit -m "docs(21): rebrand code-wiki-agent in current-milestone phase docs"
    ```
  </action>
  <verify>
    <automated>cd ../agent-research-rename && RS=$(grep -rEl 'code-wiki-agent|code_wiki_agent|code-wiki-mcp|code_wiki_mcp|CODE_WIKI_|\.code-wiki/' .planning/phases/17-vault-io-bug-fixes/ .planning/phases/20-workspace-manifest-model-config/ .planning/intel/stack.json .planning/threads/ 2>/dev/null || true); test -z "$RS" || { echo "RESIDUAL: $RS"; exit 1; }; uv sync 2>&1 | tail -3; SYNC_RC=${PIPESTATUS[0]}; test "$SYNC_RC" -eq 0 || exit 1; uv run pytest -m "not integration" 2>&1 | tail -10; PYTEST_RC=${PIPESTATUS[0]}; test "$PYTEST_RC" -eq 0 || { echo "PYTEST FAILED rc=$PYTEST_RC"; exit 1; }; HEAD_SUBJECT=$(git log -1 --format='%s'); echo "$HEAD_SUBJECT" | grep -qE '^docs\(21\): rebrand code-wiki-agent in current-milestone phase docs' || { echo "HEAD subject mismatch: $HEAD_SUBJECT"; exit 1; }; CHANGED=$(git show --stat HEAD --format= | awk 'NF>0 \&\& !/^ [0-9]+ file/ {print $1}' | grep -v '^$' | sort -u); PH21=$(echo "$CHANGED" | grep '^\.planning/phases/21-' || true); test -z "$PH21" || { echo "Task 2 leaked into Phase 21 own dir: $PH21"; exit 1; }</automated>
  </verify>
  <acceptance_criteria>
    - Phase 17 + Phase 20 phase dirs + `.planning/intel/stack.json` + `.planning/threads/` contain zero of the six target tokens.
    - HEAD commit does NOT touch any file under `.planning/phases/21-rename-code-wiki-agent.../` (allowlisted in Task 4, not swept).
    - `uv run pytest -m "not integration"` exits 0.
    - HEAD commit subject matches `^docs\(21\): rebrand code-wiki-agent in current-milestone phase docs`.
  </acceptance_criteria>
  <done>Sub-commit 2 landed; current-milestone planning surface clean; pytest non-integration green.</done>
</task>

<task type="auto">
  <name>Task 3: Sweep historical archives (milestones v1.0 / v1.1 / v1.2; sketches; sweep; research; spike READMEs + MANIFEST + CONVENTIONS + WRAP-UP) (third sub-commit)</name>
  <files>.planning/milestones/v1.0-phases/**/*.md, .planning/milestones/v1.1-phases/**/*.md, .planning/milestones/v1.2-phases/**/*.md, .planning/sketches/**/*.md, .planning/sweep/**/*.md, .planning/research/**/*.md, .planning/spikes/MANIFEST.md, .planning/spikes/CONVENTIONS.md, .planning/spikes/WRAP-UP-SUMMARY.md, .planning/spikes/001-subagent-context-audit/README.md, .planning/spikes/002-lattice-drift-inventory/README.md</files>
  <read_first>
    - 21-PATTERNS.md §"Class: `planning-doc-sweep`" sub-commit 3 ("Historical archives") + the D-07 exclusion (raw spike sources stay verbatim)
    - 21-CONTEXT.md D-05 override (Phase 21 sweeps history; Phase 12's R-03 did not) + D-07 (spike-source carveout)
    - The current `.brand-grep-allow` (Phase 12 entries) — read to understand what's already allowlisted under R-03 (e.g., `.planning/milestones/v1.0-`, `.planning/milestones/v1.1-`, `.planning/spikes/001-.../README.md`, etc.); Phase 21 D-05 SWEEPS these despite R-03 allowlist — the Phase 12 allowlist entries will eventually be reviewed in Task 4 (R-03 lattice entries stay; Phase 21 brand entries are net-new in their own section)
    - Sample of each archive dir before editing
  </read_first>
  <action>
    Working directory: `../agent-research-rename`.

    Step 1 — Scope grep (explicitly EXCLUDING `.planning/spikes/00{1,2}/sources/**` per D-07):

    ```bash
    cd ../agent-research-rename
    grep -rEln 'code-wiki-agent|code_wiki_agent|code-wiki-mcp|code_wiki_mcp|CODE_WIKI_|\.code-wiki/' \
      .planning/milestones/ \
      .planning/sketches/ \
      .planning/sweep/ \
      .planning/research/ \
      .planning/spikes/MANIFEST.md \
      .planning/spikes/CONVENTIONS.md \
      .planning/spikes/WRAP-UP-SUMMARY.md \
      .planning/spikes/001-subagent-context-audit/README.md \
      .planning/spikes/002-lattice-drift-inventory/README.md \
      2>/dev/null | grep -v '/sources/' \
      | tee /tmp/21-05-t3-scope.txt
    ```

    The `grep -v '/sources/'` post-filter enforces D-07 — raw spike sources stay verbatim.

    Step 2 — Sweep:

    ```bash
    cd ../agent-research-rename
    FILES=$(cat /tmp/21-05-t3-scope.txt | sort -u)
    for f in $FILES; do
      [ -z "$f" ] && continue
      sed -i '' \
        -e 's/code-wiki-agent/graph-wiki-agent/g' \
        -e 's/code-wiki-mcp/graph-wiki-mcp/g' \
        -e 's/code_wiki_agent/graph_wiki_agent/g' \
        -e 's/code_wiki_mcp/graph_wiki_mcp/g' \
        -e 's/CODE_WIKI_/GRAPH_WIKI_/g' \
        -e 's|\.code-wiki/|.graph-wiki/|g' \
        "$f"
    done
    ```

    Step 3 — Verify (including the D-07 spike-source exclusion), gate, commit:

    ```bash
    cd ../agent-research-rename
    RESIDUAL=$(grep -rEl 'code-wiki-agent|code_wiki_agent|code-wiki-mcp|code_wiki_mcp|CODE_WIKI_|\.code-wiki/' \
      .planning/milestones/ .planning/sketches/ .planning/sweep/ .planning/research/ \
      .planning/spikes/MANIFEST.md .planning/spikes/CONVENTIONS.md \
      .planning/spikes/WRAP-UP-SUMMARY.md \
      .planning/spikes/001-subagent-context-audit/README.md \
      .planning/spikes/002-lattice-drift-inventory/README.md 2>/dev/null \
      | grep -v '/sources/' || true)
    test -z "$RESIDUAL" || { echo "RESIDUAL in archives: $RESIDUAL"; exit 1; }

    uv sync 2>&1 | tail -3
    SYNC_RC=${PIPESTATUS[0]}
    test "$SYNC_RC" -eq 0 || exit 1
    uv run pytest -m "not integration" 2>&1 | tail -10
    PYTEST_RC=${PIPESTATUS[0]}
    test "$PYTEST_RC" -eq 0 || { echo "PYTEST FAILED rc=$PYTEST_RC"; exit 1; }

    git add -A
    git status
    git commit -m "docs(21): rebrand code-wiki-agent in historical archives (D-05 override of R-03)"
    ```
  </action>
  <verify>
    <automated>cd ../agent-research-rename && RESIDUAL=$(grep -rEl 'code-wiki-agent|code_wiki_agent|code-wiki-mcp|code_wiki_mcp|CODE_WIKI_|\.code-wiki/' .planning/milestones/ .planning/sketches/ .planning/sweep/ .planning/research/ .planning/spikes/MANIFEST.md .planning/spikes/CONVENTIONS.md .planning/spikes/WRAP-UP-SUMMARY.md .planning/spikes/001-subagent-context-audit/README.md .planning/spikes/002-lattice-drift-inventory/README.md 2>/dev/null | grep -v '/sources/' || true); test -z "$RESIDUAL" || { echo "RESIDUAL archives: $RESIDUAL"; exit 1; }; uv sync 2>&1 | tail -3; SYNC_RC=${PIPESTATUS[0]}; test "$SYNC_RC" -eq 0 || exit 1; uv run pytest -m "not integration" 2>&1 | tail -10; PYTEST_RC=${PIPESTATUS[0]}; test "$PYTEST_RC" -eq 0 || { echo "PYTEST FAILED rc=$PYTEST_RC"; exit 1; }; HEAD_SUBJECT=$(git log -1 --format='%s'); echo "$HEAD_SUBJECT" | grep -qE '^docs\(21\): rebrand code-wiki-agent in historical archives' || { echo "HEAD subject mismatch: $HEAD_SUBJECT"; exit 1; }; CHANGED=$(git show --stat HEAD --format= | awk 'NF>0 \&\& !/^ [0-9]+ file/ {print $1}' | grep -v '^$' | sort -u); SRC_LEAK=$(echo "$CHANGED" | grep -E '\.planning/spikes/00[12].*/sources/' || true); test -z "$SRC_LEAK" || { echo "Task 3 leaked into spike sources (D-07 violation): $SRC_LEAK"; exit 1; }</automated>
  </verify>
  <acceptance_criteria>
    - All scoped historical archives contain zero of the six target tokens.
    - HEAD commit does NOT touch any file under `.planning/spikes/00{1,2}/sources/` (D-07 enforced).
    - `uv run pytest -m "not integration"` exits 0.
    - HEAD commit subject matches `^docs\(21\): rebrand code-wiki-agent in historical archives`.
  </acceptance_criteria>
  <done>Sub-commit 3 landed; historical planning archives clean (sweep complete except for Phase 21's own dir and spike sources); pytest non-integration green.</done>
</task>

<task type="auto">
  <name>Task 4: Extend scripts/check-brand.sh + author .brand-grep-allow Phase 21 section + run SP-2 final two-stage gate (fourth sub-commit)</name>
  <files>scripts/check-brand.sh, .brand-grep-allow</files>
  <read_first>
    - 21-PATTERNS.md §"Class: `brand-grep-gate-extension`" — full extension recipe (regex + heredoc comment) + the SP-2 two-stage gate snippet
    - 21-PATTERNS.md §"SP-2: `bash scripts/check-brand.sh` as second-stage gate" + §"SP-6: Tight allowlist discipline (D-13)"
    - 21-CONTEXT.md D-12 (single gate extends, not replaces, lattice regex) + D-13 (tight allowlist with `# rationale:` per entry)
    - .planning/milestones/v1.2-phases/12-drift-backport-ecosystem-rebrand-m2/12-04-grep-gate-and-verification-PLAN.md Task 2 lines 156-187 — direct precedent for the SP-2 two-stage gate
    - Current `scripts/check-brand.sh` (50 lines; the HITS= block on line 37-40 is what gets extended)
    - Current `.brand-grep-allow` (202 lines; Phase 12 entries; this task APPENDS a Phase 21 section without modifying existing entries)
  </read_first>
  <action>
    Working directory: `../agent-research-rename`.

    Step 1 — Extend `scripts/check-brand.sh`. Modify ONLY the regex inside the HITS= block. Add a heredoc comment line near the top (after the existing comment block) per 21-PATTERNS.md.

    Before (lines 37-40):
    ```bash
    HITS=$(grep -rEl --exclude-dir=__pycache__ --exclude='*.pyc' \
        'lattice|LATTICE|lattice_workspace|lattice_wiki_core' \
        packages/ agents/ plugins/ .planning/ CLAUDE.md 2>/dev/null \
        | grep -vF -f <(grep -vE '^[[:space:]]*(#|$)' "$ALLOWLIST") || true)
    ```

    After (extend the regex; everything else stays):
    ```bash
    HITS=$(grep -rEl --exclude-dir=__pycache__ --exclude='*.pyc' \
        'lattice|LATTICE|lattice_workspace|lattice_wiki_core|code-wiki-agent|code_wiki_agent|code-wiki-mcp|code_wiki_mcp' \
        packages/ agents/ plugins/ .planning/ CLAUDE.md 2>/dev/null \
        | grep -vF -f <(grep -vE '^[[:space:]]*(#|$)' "$ALLOWLIST") || true)
    ```

    Also update the script's top comment block (lines 3-12) to note the Phase 21 extension. Add a single line:

    ```
    # Per Phase 21 §D-12: extended to also catch `code-wiki-agent` / `code_wiki_agent`
    # / `code-wiki-mcp` / `code_wiki_mcp` after the rename to `graph-wiki-agent`.
    ```

    Step 2 — Author the Phase 21 section of `.brand-grep-allow`. APPEND to the existing file (do NOT modify Phase 12 entries). The new section per D-13 / SP-6 (tight; every entry carries `# rationale:`):

    ```
    # ---------------------------------------------------------------------------
    # Phase 21 — `code-wiki-agent` rename to `graph-wiki-agent`. Per D-13, the
    # allowlist stays tight: only entries that would actively break things if
    # rewritten. Most historical references are eliminated by the .planning/
    # sweep (D-05 override of R-03), not allowlisted.
    # ---------------------------------------------------------------------------
    # rationale: scripts/check-brand.sh contains 'code-wiki-agent' / 'code_wiki_agent'
    # / 'code-wiki-mcp' / 'code_wiki_mcp' as grep patterns themselves; self-allowlisted.
    # (NOTE: scripts/check-brand.sh is ALREADY in the Phase 12 self-allowlist section
    # above — no duplicate entry needed; this comment documents the dual coverage.)
    # rationale: .brand-grep-allow self-allowlists these literals as pattern documentation;
    # this file is ALREADY in the Phase 12 self-allowlist section above — no duplicate
    # entry needed; this comment documents the dual coverage.
    # rationale: graph-wiki/wiki/ is out of scope per D-06 — healed by next /graph-wiki:scan run.
    graph-wiki/wiki/
    # rationale: raw spike sources stay verbatim per D-07 (historic untouched material).
    .planning/spikes/001-subagent-context-audit/sources/
    .planning/spikes/002-lattice-drift-inventory/sources/
    # rationale: Phase 21 phase dir itself discusses the rename and naturally references
    # both old and new names side-by-side — mirrors Phase 12's self-allowlist pattern for
    # its own phase dir.
    .planning/phases/21-rename-code-wiki-agent-to-graph-wiki-agent-update-all-code-r/
    # rationale: pytest runtime artifacts embed renamed test node IDs as substrings;
    # gitignored, present locally after `uv run pytest`. (The Phase 12 entry covers this
    # same path under .pytest_cache/v/cache/nodeids — duplicate entry intentional for
    # documentation clarity; allowlist matching is by substring so no harm.)
    .pytest_cache/v/cache/nodeids
    # rationale: gitignored test-output dir — agent test runs may emit stale references.
    agents/graph-wiki-agent/test-out/
    ```

    Step 3 — Iteration loop per SP-2 (if first gate run reveals legitimate carry-forward refs that planner missed):

    ```bash
    cd ../agent-research-rename
    bash scripts/check-brand.sh
    GATE_RC=$?
    ```

    If `GATE_RC != 0`: read the printed unallowlisted hits. For each:
    - If a genuine missed rebrand (code/test/doc that should have been swept by 21-03/04 or 21-05 Tasks 1-3): STOP, surface to operator, do NOT add to allowlist. Operator decides whether to amend an earlier sub-commit or extend allowlist.
    - If a legitimate carry-forward (e.g., a phase doc that references a commit SHA quoting the old name in a commit-message-style line, or a path that mirrors an existing Phase 12 R-class but for Phase 21 brand): append a `# rationale: <class>` + path entry. Repeat the gate until `GATE_RC == 0`. Each allowlist amendment is folded into the Task 4 commit.

    Step 4 — SP-2 final two-stage gate with independent named exit codes (copy verbatim from 21-PATTERNS.md SP-2 / Phase 12 plan 04 line 187):

    ```bash
    cd ../agent-research-rename
    bash scripts/check-brand.sh
    GATE_RC=$?
    test "$GATE_RC" -eq 0 || { echo "GATE FAILED rc=$GATE_RC"; exit "$GATE_RC"; }
    uv run pytest 2>&1 | tail -10
    TEST_RC=${PIPESTATUS[0]}
    test "$TEST_RC" -eq 0 || { echo "TESTS FAILED rc=$TEST_RC"; exit "$TEST_RC"; }
    ```

    Note: Stage 2 runs `uv run pytest` (FULL suite, no `-m "not integration"` filter) because (a) the integration tests are env-gated by `GRAPH_WIKI_RUN_INTEGRATION` (which is unset in this environment, so they skip not fail) and (b) we want `tests/test_integration_gate.py` (the meta integration-gate test) to run — it walks `tests/integration/test_*.py` files; under the renamed agent dir it now finds `agents/graph-wiki-agent/tests/integration/*` files. That meta-test will FAIL if the integration test files don't have the canonical `GRAPH_WIKI_RUN_INTEGRATION` skipif marker — and per Task 2 of plan 21-04 they were rewritten, so it should pass.

    Step 5 — Commit:

    ```bash
    cd ../agent-research-rename
    git add scripts/check-brand.sh .brand-grep-allow
    git status
    git commit -m "chore(21): extend brand grep-gate for code-wiki-agent → graph-wiki-agent"
    ```

    Step 5.5 — M4 deletion-guard check (pre-finalization):

    ```bash
    cd ../agent-research-rename
    # M4: assert no pre-existing Phase 12 allowlist entry was deleted by this commit.
    # All Phase 21 work APPENDS to .brand-grep-allow; never deletes lines from earlier sections.
    DEL_PHASE12=$(git diff HEAD~1 HEAD -- .brand-grep-allow | grep -E '^-[^-]' | grep -v 'Phase 21' || true)
    test -z "$DEL_PHASE12" || { echo "M4 VIOLATION: HEAD deleted pre-existing Phase 12 allowlist entries:"; echo "$DEL_PHASE12"; exit 1; }
    echo "M4 OK: no Phase 12 allowlist entries removed"
    ```

    Step 6 — Phase-final assertion (run after the commit; logs the result):

    ```bash
    cd ../agent-research-rename
    bash scripts/check-brand.sh
    FINAL_GATE_RC=$?
    test "$FINAL_GATE_RC" -eq 0 || { echo "FINAL GATE FAILED rc=$FINAL_GATE_RC"; exit 1; }
    grep -rE 'code-wiki-agent|code_wiki_agent|code-wiki-mcp|code_wiki_mcp' \
      packages/ agents/ plugins/ .planning/ CLAUDE.md README.md 2>/dev/null \
      | grep -vF -f <(grep -vE '^[[:space:]]*(#|$)' .brand-grep-allow) \
      | tee /tmp/21-05-final-residual.txt
    test ! -s /tmp/21-05-final-residual.txt || { echo "FINAL RESIDUAL HITS — gate did not catch these:"; cat /tmp/21-05-final-residual.txt; exit 1; }
    echo "PHASE 21 COMPLETE — gate green, full pytest green, zero unallowlisted hits"
    ```
  </action>
  <verify>
    <automated>cd ../agent-research-rename && grep -qE "lattice\|LATTICE\|lattice_workspace\|lattice_wiki_core\|code-wiki-agent\|code_wiki_agent\|code-wiki-mcp\|code_wiki_mcp" scripts/check-brand.sh || { echo "check-brand.sh regex not extended"; exit 1; }; grep -qF 'Phase 21' .brand-grep-allow || { echo "Phase 21 section header missing from .brand-grep-allow"; exit 1; }; grep -qF 'graph-wiki/wiki/' .brand-grep-allow || { echo "graph-wiki/wiki/ allowlist entry missing"; exit 1; }; grep -qF '.planning/spikes/001-subagent-context-audit/sources/' .brand-grep-allow || { echo "spike-source allowlist entry missing (D-07)"; exit 1; }; grep -qF '.planning/phases/21-rename-code-wiki-agent-to-graph-wiki-agent-update-all-code-r/' .brand-grep-allow || { echo "Phase 21 self-allowlist entry missing"; exit 1; }; bash -n scripts/check-brand.sh || { echo "check-brand.sh syntax error"; exit 1; }; bash scripts/check-brand.sh; GATE_RC=$?; test "$GATE_RC" -eq 0 || { echo "GATE FAILED rc=$GATE_RC"; exit "$GATE_RC"; }; uv run pytest 2>&1 | tail -15; TEST_RC=${PIPESTATUS[0]}; test "$TEST_RC" -eq 0 || { echo "TESTS FAILED rc=$TEST_RC"; exit "$TEST_RC"; }; HEAD_SUBJECT=$(git log -1 --format='%s'); echo "$HEAD_SUBJECT" | grep -qE '^chore\(21\): extend brand grep-gate for code-wiki-agent' || { echo "HEAD subject mismatch: $HEAD_SUBJECT"; exit 1; }; CHANGED=$(git show --stat HEAD --format= | awk 'NF>0 \&\& !/^ [0-9]+ file/ {print $1}' | grep -v '^$' | sort -u); DEL_PHASE12=$(git diff HEAD~1 HEAD -- .brand-grep-allow | grep -E "^-[^-]" | grep -v "Phase 21" || true); test -z "$DEL_PHASE12" || { echo "M4 VIOLATION: HEAD deleted pre-existing Phase 12 allowlist entries: $DEL_PHASE12"; exit 1; }; EXTRA=$(echo "$CHANGED" | grep -vE '^(scripts/check-brand\.sh|\.brand-grep-allow)$' | grep -v '^$' || true); test -z "$EXTRA" || { echo "HEAD touches unexpected files: $EXTRA"; exit 1; }; FINAL_RES=$(grep -rE 'code-wiki-agent|code_wiki_agent|code-wiki-mcp|code_wiki_mcp' packages/ agents/ plugins/ .planning/ CLAUDE.md README.md 2>/dev/null | grep -vF -f <(grep -vE '^[[:space:]]*(#|$)' .brand-grep-allow) || true); test -z "$FINAL_RES" || { echo "FINAL RESIDUAL: $FINAL_RES"; exit 1; }; echo "PHASE 21 COMPLETE"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/check-brand.sh` regex contains all four new patterns: `code-wiki-agent|code_wiki_agent|code-wiki-mcp|code_wiki_mcp`. Phase 12 lattice patterns are PRESERVED (extension, not replacement, per D-12).
    - `scripts/check-brand.sh` passes `bash -n` (syntax valid).
    - `.brand-grep-allow` contains a `Phase 21` section header comment + the required entries: `graph-wiki/wiki/`, `.planning/spikes/001-subagent-context-audit/sources/`, `.planning/spikes/002-lattice-drift-inventory/sources/`, `.planning/phases/21-rename-code-wiki-agent-to-graph-wiki-agent-update-all-code-r/`.
    - Each Phase 21 allowlist entry preceded by a `# rationale:` comment per SP-6.
    - Existing Phase 12 allowlist entries are UNCHANGED (verify via `git diff` showing only additions to `.brand-grep-allow`, no deletions/modifications).
    - SP-2 two-stage gate: `bash scripts/check-brand.sh` exits 0 (GATE_RC=0); `uv run pytest` exits 0 (TEST_RC=0 via PIPESTATUS); failures are distinguishable.
    - HEAD commit subject is `chore(21): extend brand grep-gate for code-wiki-agent → graph-wiki-agent`.
    - HEAD commit touches exactly 2 files: `scripts/check-brand.sh`, `.brand-grep-allow` (Surgical Changes).
    - Phase-final assertion: full-repo grep for the four `code-wiki-*` slugs returns zero unallowlisted hits.
  </acceptance_criteria>
  <done>Brand gate extended; Phase 21 allowlist section landed; SP-2 two-stage gate green (GATE_RC=0, TEST_RC=0); full-repo final-residual grep clean. Phase 21 goal SC#5 satisfied.</done>
</task>

</tasks>

<verification>
- Four sub-commits on `rename-21` from this plan:
  1. `docs(21): rebrand code-wiki-agent in live planning + repo/plugin docs + skill`
  2. `docs(21): rebrand code-wiki-agent in current-milestone phase docs`
  3. `docs(21): rebrand code-wiki-agent in historical archives (D-05 override of R-03)`
  4. `chore(21): extend brand grep-gate for code-wiki-agent → graph-wiki-agent`
- After all four: `bash scripts/check-brand.sh` exits 0; `uv run pytest` (full suite) exits 0.
- D-06 enforced: `graph-wiki/wiki/` untouched throughout (allowlisted).
- D-07 enforced: spike sources under `.planning/spikes/00{1,2}/sources/` untouched (allowlisted).
- Phase 21 own dir allowlisted (mirrors Phase 12's self-allowlist pattern).
- SP-1 pytest gate after Tasks 1-3; SP-2 two-stage gate (GATE_RC + TEST_RC) at Task 4.
- SP-3 observed: no fix-forward commits between sub-commits.
- SP-6 enforced: every Phase 21 allowlist entry carries a `# rationale:` comment.
</verification>

<success_criteria>
Phase 21 goal SC#5 satisfied (`scripts/check-brand.sh` extended; `.brand-grep-allow` updated; full-repo grep for the four `code-wiki-*` slugs returns 0 hits outside allowlist).

All Phase 21 goal SCs satisfied in aggregate across the five plans:
- SC#1 (dir rename with git history preserved) — plan 21-01.
- SC#2 (Python pkgs renamed; imports updated; agent-pkg pytest non-integration green) — plan 21-03.
- SC#3 (console scripts work end-to-end) — plans 21-02 + 21-03 smoke tests; final whole-repo run in plan 21-05.
- SC#4 (plugin shellouts invoke graph-wiki-agent; trace dir is .graph-wiki/) — plan 21-04.
- SC#5 (brand-grep extended; allowlist updated; zero unallowlisted hits) — plan 21-05.

The phase is complete when `rename-21` branch is green on the final SP-2 gate. Merging `rename-21` → `main` is an operator decision OUTSIDE Phase 21 scope per D-08 worktree posture.
</success_criteria>

<output>
Create `.planning/phases/21-rename-code-wiki-agent-to-graph-wiki-agent-update-all-code-r/21-05-SUMMARY.md` with:
- All 4 sub-commit SHAs + subjects
- `git show --stat <each-SHA>` proving Surgical Changes per sub-commit
- Output of the final SP-2 two-stage gate: `GATE_RC=0` + `TEST_RC=0`
- Final-residual grep output (proving zero unallowlisted hits)
- Phase 21 allowlist section contents inline (for audit)
- Discretion notes:
  - Phase 21 own dir allowlisted vs. swept (rationale: mirrors Phase 12 self-allowlist; dir documents the rename and references both names)
  - Spike sources NOT swept per D-07
  - Phase 12 lattice allowlist entries UNTOUCHED (Phase 21 extends, does not modify)
- Phase-completion note pointing back at all 5 plans + reminder that `rename-21` → `main` merge is operator-scoped
- Pointer to Phase 19 (next in v1.3 execution queue per ROADMAP — the code review burndown depends on Phase 21's rename being shipped)
</output>
