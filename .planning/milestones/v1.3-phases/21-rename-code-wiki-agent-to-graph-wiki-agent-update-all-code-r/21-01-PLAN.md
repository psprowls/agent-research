---
phase: 21-rename-code-wiki-agent-to-graph-wiki-agent-update-all-code-r
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/code-wiki-agent/
  - agents/graph-wiki-agent/src/code_wiki_agent/
  - agents/graph-wiki-agent/src/code_wiki_mcp/
autonomous: false
requirements: []
must_haves:
  truths:
    - "A git worktree exists at `../agent-research-rename` on branch `rename-21` (D-08). All Phase 21 work happens inside the worktree; main checkout stays usable."
    - "Inside the worktree, the directory `agents/code-wiki-agent/` no longer exists; `agents/graph-wiki-agent/` exists with the full src + tests subtree."
    - "Inside `agents/graph-wiki-agent/src/`, the Python module directories are `graph_wiki_agent/` and `graph_wiki_mcp/` (NOT `code_wiki_agent/` / `code_wiki_mcp/`)."
    - "`git log --follow agents/graph-wiki-agent/pyproject.toml` shows the file's full prior history under `agents/code-wiki-agent/pyproject.toml` — proving `git mv` preserved blame/history (SP-5)."
    - "Layer 1 commit lands green under the relaxed D-11 gate: `uv sync` succeeds. `uv run pytest` is **expected red** at this layer (imports still say `code_wiki_agent`); pytest-green resumes at layer 3."
  artifacts:
    - path: "agents/graph-wiki-agent/"
      provides: "Renamed agent package directory (replaces agents/code-wiki-agent/)"
    - path: "agents/graph-wiki-agent/src/graph_wiki_agent/"
      provides: "Renamed Python package module root (replaces src/code_wiki_agent/)"
    - path: "agents/graph-wiki-agent/src/graph_wiki_mcp/"
      provides: "Renamed MCP server module root (replaces src/code_wiki_mcp/)"
  key_links:
    - from: "worktree branch `rename-21`"
      to: "main checkout (read-only during phase)"
      via: "git worktree add ../agent-research-rename rename-21"
      pattern: "agent-research-rename"
    - from: "agents/graph-wiki-agent/pyproject.toml history"
      to: "agents/code-wiki-agent/pyproject.toml (pre-move)"
      via: "git mv preserves file identity (SP-5)"
      pattern: "git log --follow"
---

<objective>
D-09 layer 1: stand up a worktree (D-08), then use `git mv` to rename the agent package directory and its two Python module subdirs in ONE commit. This is the foundational move — every later layer operates inside `agents/graph-wiki-agent/`.

Purpose: preserve `git log --follow` blame/history across the rename (SP-5). Tangling `git mv` with import sweeps obscures which line moved vs. which line changed; isolating the move in its own commit makes it independently revertable (SP-3).

Output: a clean worktree at `../agent-research-rename`, branch `rename-21`, with one commit on top of main whose entire diff is renames (no content edits). The codebase is **intentionally broken at the end of layer 1** — imports still say `code_wiki_agent`. Pytest-green resumes at layer 3 per D-11 ("or equivalent scope"); layer-1 gate is `uv sync` only.

**SP-3 ("no fix-forward") reminder:** if any `git mv` in this layer goes wrong (wrong source/dest path, accidental plain `mv`), `git reset --hard HEAD~1` and redo from a clean state. Do not stack a "fix" commit on top of a broken move.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/21-rename-code-wiki-agent-to-graph-wiki-agent-update-all-code-r/21-CONTEXT.md
@.planning/phases/21-rename-code-wiki-agent-to-graph-wiki-agent-update-all-code-r/21-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Checkpoint — confirm worktree location and branch name (D-08)</name>
  <files>(checkpoint — no files modified; gates the worktree creation in Task 2)</files>
  <read_first>
    - .planning/phases/21-rename-code-wiki-agent-to-graph-wiki-agent-update-all-code-r/21-CONTEXT.md §"Cutover Strategy" (D-08 — worktree is a hard constraint, not a suggestion)
    - 21-PATTERNS.md §"SP-4: Worktree containment (D-08)" — entry-step of layer 1, before any `git mv`
  </read_first>
  <action>
    Pause execution and present the proposed worktree command to the operator. Do NOT execute `git worktree add` until the operator approves the path + branch. If alternatives are supplied, record them in the SUMMARY and use the operator-supplied values in Task 2.

    Proposed default (defer to operator):

    ```
    git worktree add ../agent-research-rename rename-21
    ```

    Surface the three verification questions in `<what-built>` / `<how-to-verify>` to the operator and wait for `approved` (or alternative names).
  </action>
  <what-built>
    Proposed worktree command:
    ```bash
    git worktree add ../agent-research-rename rename-21
    ```
    This creates a sibling directory `~/Personal/agent-research-rename/` on a new branch `rename-21` forked from the current HEAD of `main`. All subsequent Phase 21 tasks `cd` into that worktree before running any command.
  </what-built>
  <how-to-verify>
    1. Confirm the worktree parent directory (`~/Personal/`) is the correct location — no name collision with an existing directory.
    2. Confirm the branch name `rename-21` is acceptable (or supply an alternative; CONTEXT says "or similar").
    3. Confirm the main checkout (`~/Personal/agent-research/`) should remain on its current branch and be left alone for the duration of Phase 21.
    4. Reply `approved` to proceed with the `git worktree add` command and the subsequent `git mv` task. To change the path or branch, reply with the alternative names.
  </how-to-verify>
  <verify>
    <human-check>Operator replies "approved" (or supplies alternative path/branch names). No automated verification — this gate exists to confirm D-08 user-explicit constraints before the worktree is created.</human-check>
  </verify>
  <resume-signal>Type "approved" to proceed with `git worktree add ../agent-research-rename rename-21`, or supply alternative path/branch names.</resume-signal>
  <acceptance_criteria>
    - User confirms worktree path + branch name OR provides alternatives.
    - No silent default — D-08 is user-explicit; planner does not assume.
  </acceptance_criteria>
  <done>Operator approval recorded; Task 2 proceeds with the approved path + branch.</done>
</task>

<task type="auto">
  <name>Task 2: Create worktree and run three `git mv` operations in one commit</name>
  <files>agents/code-wiki-agent/ (entire tree), agents/graph-wiki-agent/ (new), agents/graph-wiki-agent/src/code_wiki_agent/ → graph_wiki_agent/, agents/graph-wiki-agent/src/code_wiki_mcp/ → graph_wiki_mcp/</files>
  <read_first>
    - 21-PATTERNS.md §"Class: `dir-move-git-mv`" — full pattern + `uv sync`-only gate rationale (the codebase is intentionally broken at this layer)
    - 21-PATTERNS.md §"SP-5: `git mv` over `mv` + `git add`" — preserves blame/history
    - 21-CONTEXT.md §"Cutover Strategy" D-08, D-09 (layer 1), D-11 ("or equivalent scope" — pytest deferred to layer 3)
    - .planning/milestones/v1.2-phases/12-drift-backport-ecosystem-rebrand-m2/12-03-rebrand-sweep-PLAN.md §"<objective>" (line 70: "no 'fix forward' — revert and retry" → SP-3 inheritance)
    - Output of `git status` and `ls agents/code-wiki-agent/src/` before starting (so any uncommitted local change is noticed before the worktree is created)
  </read_first>
  <action>
    From the **main checkout** (`~/Personal/agent-research/`), create the worktree (using the path + branch approved in Task 1; defaults shown):

    ```bash
    git worktree add ../agent-research-rename rename-21
    cd ../agent-research-rename
    ```

    All remaining commands in this task and all subsequent Phase 21 tasks run from `../agent-research-rename/` (the worktree). Any per-task `Bash` invocation MUST start with an absolute `cd` to the worktree (the harness resets cwd between calls).

    Inside the worktree, run the three `git mv` operations in order:

    ```bash
    git mv agents/code-wiki-agent agents/graph-wiki-agent
    git mv agents/graph-wiki-agent/src/code_wiki_agent agents/graph-wiki-agent/src/graph_wiki_agent
    git mv agents/graph-wiki-agent/src/code_wiki_mcp   agents/graph-wiki-agent/src/graph_wiki_mcp
    ```

    Use **only** `git mv` — never `mv` followed by `git add`. SP-5 is non-negotiable for blame/history preservation. If a `git mv` errors (e.g., target exists), STOP, surface the error, and revert via `git reset --hard HEAD` from a clean state — do NOT attempt a plain-`mv` workaround.

    Stage and commit (no `--amend`; this is the first Phase 21 commit on `rename-21`):

    ```bash
    git status        # sanity — should show 3 renames, zero content edits
    git diff --cached --stat
    git commit -m "refactor(21): git mv code-wiki-agent → graph-wiki-agent (dir + src modules)"
    ```

    **Relaxed D-11 gate at layer 1** (per 21-PATTERNS.md `dir-move-git-mv`): the per-commit gate is `uv sync` only — NOT `uv run pytest`. The codebase is broken at this layer because imports still say `from code_wiki_agent...`. Pytest will fail; that is expected. The gate command:

    ```bash
    uv sync 2>&1 | tail -20
    SYNC_RC=${PIPESTATUS[0]}
    test "$SYNC_RC" -eq 0 || { echo "UV SYNC FAILED rc=$SYNC_RC"; exit 1; }
    ```

    `uv sync` resyncs the workspace to reflect the renamed directory. Some warnings about missing entry-point modules are tolerable until layer 2 fixes `pyproject.toml`; what is NOT tolerable is a non-zero exit from `uv sync` itself.

    Document the layer-1 expected-red pytest state in the SUMMARY so future readers understand why pytest is not gated at this layer.
  </action>
  <verify>
    <automated>cd ../agent-research-rename && test ! -d agents/code-wiki-agent || { echo "agents/code-wiki-agent still exists"; exit 1; }; test -d agents/graph-wiki-agent || { echo "agents/graph-wiki-agent missing"; exit 1; }; test -d agents/graph-wiki-agent/src/graph_wiki_agent || { echo "src/graph_wiki_agent missing"; exit 1; }; test -d agents/graph-wiki-agent/src/graph_wiki_mcp || { echo "src/graph_wiki_mcp missing"; exit 1; }; test ! -d agents/graph-wiki-agent/src/code_wiki_agent || { echo "stale src/code_wiki_agent still present"; exit 1; }; test ! -d agents/graph-wiki-agent/src/code_wiki_mcp || { echo "stale src/code_wiki_mcp still present"; exit 1; }; HEAD_SUBJECT=$(git log -1 --format='%s'); echo "$HEAD_SUBJECT" | grep -qE '^refactor\(21\): git mv code-wiki-agent' || { echo "HEAD subject mismatch: $HEAD_SUBJECT"; exit 1; }; RENAME_COUNT=$(git show --stat HEAD | grep -cE '=>|rename') ; test "$RENAME_COUNT" -gt 0 || { echo "no renames detected in HEAD commit (git mv may have been bypassed)"; exit 1; }; CONTENT_DIFF_LINES=$(git show --numstat HEAD | awk '$1+$2 > 0 {print}' | wc -l | tr -d ' '); test "$CONTENT_DIFF_LINES" -eq 0 || { echo "HEAD commit has content edits ($CONTENT_DIFF_LINES non-zero numstat lines) — layer 1 must be moves-only"; exit 1; }; uv sync 2>&1 | tail -20; SYNC_RC=${PIPESTATUS[0]}; test "$SYNC_RC" -eq 0 || { echo "UV SYNC FAILED rc=$SYNC_RC"; exit 1; }; FOLLOW_LINES=$(git log --follow --format=%H agents/graph-wiki-agent/pyproject.toml | wc -l | tr -d ' '); test "$FOLLOW_LINES" -gt 1 || { echo "git log --follow returned $FOLLOW_LINES entries — expected prior history before the rename commit (SP-5 failed)"; exit 1; }; echo "LAYER-1 OK (pytest deliberately not run — expected red per D-11 relaxation)"</automated>
  </verify>
  <acceptance_criteria>
    - Working dir is the worktree (`../agent-research-rename`), branch `rename-21`. (Verify by `git rev-parse --show-toplevel` ending in `agent-research-rename`.)
    - `agents/code-wiki-agent/` no longer exists.
    - `agents/graph-wiki-agent/` exists; contains `src/graph_wiki_agent/` and `src/graph_wiki_mcp/`.
    - No stale `src/code_wiki_agent/` or `src/code_wiki_mcp/` directories remain under `agents/graph-wiki-agent/src/`.
    - HEAD commit subject matches `^refactor\(21\): git mv code-wiki-agent`.
    - HEAD commit `git show --numstat HEAD` shows zero non-rename content edits (every line had 0 insertions + 0 deletions, since `git mv` is a pure rename).
    - `git log --follow agents/graph-wiki-agent/pyproject.toml` returns >1 commit (SP-5 — blame preserved across the rename).
    - `uv sync` exits 0 (captured via `${PIPESTATUS[0]}`).
    - `uv run pytest` is **not** run at this layer (D-11 "or equivalent scope" relaxation); the SUMMARY documents this explicitly.
  </acceptance_criteria>
  <done>Worktree exists; one commit landed with three `git mv` operations and zero content edits; `uv sync` green; `git log --follow` proves history preservation; pytest deferred to layer 3.</done>
</task>

</tasks>

<verification>
- Worktree at `../agent-research-rename/`, branch `rename-21`.
- HEAD commit is moves-only (three renames, zero content edits).
- `git log --follow agents/graph-wiki-agent/pyproject.toml` shows prior history.
- `uv sync` green; `uv run pytest` deliberately skipped at this layer (D-11 relaxation).
- SP-3 ("no fix-forward") observed throughout: any error during `git mv` triggers `git reset --hard` + retry, not a stacked fix commit.
</verification>

<success_criteria>
Phase 21 goal SC#1 surface (`agents/code-wiki-agent/` gone, `agents/graph-wiki-agent/` exists with full git history preserved via `git mv`) is established. Subsequent layers fix imports, pyproject, consumers, and docs on top of this foundation.
</success_criteria>

<output>
Create `.planning/phases/21-rename-code-wiki-agent-to-graph-wiki-agent-update-all-code-r/21-01-SUMMARY.md` with:
- Worktree location + branch confirmation
- HEAD commit SHA + subject
- Output of `git show --stat HEAD` (the three renames)
- Output of `git log --follow agents/graph-wiki-agent/pyproject.toml | head -5` (proves SP-5)
- Explicit note: "pytest deferred to layer 3 per D-11 relaxation; `uv sync` was the layer-1 gate"
- Confirmation that all subsequent Phase 21 tasks `cd ../agent-research-rename/` first
</output>
