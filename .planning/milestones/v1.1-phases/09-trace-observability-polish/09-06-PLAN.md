---
phase: 09-trace-observability-polish
plan: 06
type: execute
wave: 1
depends_on:
  - 09-05
files_modified:
  - agents/graph-wiki-agent/src/graph_wiki_agent/cli.py
  - agents/graph-wiki-agent/tests/unit/test_trace_viewer.py
  - agents/graph-wiki-agent/tests/unit/__snapshots__/test_trace_viewer.ambr
autonomous: true
requirements:
  - OBS-05
  - OBS-06
tags:
  - gap-closure
  - renderer
  - trace
gap_closure: true

must_haves:
  truths:
    - "Two consecutive same-role records carrying different `model_id` values render as TWO distinct collapsed-group lines (CR-01), and the model id is surfaced in each collapsed-group header so an operator reading the timeline can attribute every line to a specific model."
    - "`kind: query_summary` records (and other event/kind discriminator records) do NOT appear in the `Per-role breakdown` output (WR-02) — no synthetic `unknown` role bucket is emitted."
    - "A collapsed-run containing any non-{success,error,cancelled} status (e.g., `timeout`, `expired`, or any future producer-added status) reports it under an `other` bucket in the breakdown rather than silently dropping it; the misleading `0 success` fallback for an N-record group is replaced (WR-03)."
    - "The renamed/updated `test_query_summary_interleaved_breaks_group_snapshot` snapshot no longer contains `unknown: count=1 tokens_in=0 tokens_out=0` after WR-02 is fixed."
    - "The full `agents/graph-wiki-agent/tests/unit/test_trace_viewer.py` suite passes; no .ambr drift beyond the expected `unknown` bucket removal in `test_query_summary_interleaved_breaks_group_snapshot` and any new regression snapshots added by this plan."
  artifacts:
    - path: "agents/graph-wiki-agent/src/graph_wiki_agent/cli.py"
      provides: "Fixed collapse grouping key (role, model_id), event/kind-filtered by_role aggregation, `other`-bucket-aware status breakdown"
      contains: "model_id"
    - path: "agents/graph-wiki-agent/tests/unit/test_trace_viewer.py"
      provides: "Regression tests for CR-01 (mixed-model fan-out), WR-02 (no unknown bucket), WR-03 (unknown-status surfacing)"
      contains: "test_mixed_model_same_role_breaks_collapse"
    - path: "agents/graph-wiki-agent/tests/unit/__snapshots__/test_trace_viewer.ambr"
      provides: "Re-recorded snapshot for the interleaved test (without phantom `unknown` bucket) plus any new snapshots from this plan"
      contains: "test_query_summary_interleaved_breaks_group_snapshot"
  key_links:
    - from: "collapse loop in trace command"
      to: "_render_collapsed_group"
      via: "group key (role, model_id) parity with cost rollup (role, model_id)"
      pattern: "model_id"
    - from: "_aggregate_trace by_role pass"
      to: "Per-role breakdown rendering"
      via: "_is_groupable filter (event/kind exclusion)"
      pattern: "event|kind"
    - from: "_render_collapsed_group status breakdown"
      to: "other-bucket surfacing"
      via: "status not in {success,error,cancelled}"
      pattern: "other"
---

<objective>
Close the three advisory gaps surfaced by `09-VERIFICATION.md` (status: gaps_found): CR-01 (collapsed-group loses model_id attribution on mixed-model fan-outs), WR-02 (`_aggregate_trace` bucketizes event/kind records under a synthetic `unknown` role), and WR-03 (`_render_collapsed_group` silently drops non-{success,error,cancelled} statuses and prints a misleading `0 success` fallback). All three are localised to `cli.py` and close cleanly with one snapshot update at the end.

Purpose: Lock OBS-06 (`collapses repeated subagent-role groups`) and OBS-05 (`per-subagent cost` per `(role, model_id)`) to their *intent*, not just their literal wording. SC#3 literal wording is already satisfied — but the model-attribution refinement (CR-01) is meaningfully aligned with the project's cost-frontier story and was explicitly flagged for fix-now in the verification recommendation ("should not be deferred to v1.2"). WR-02 cleans a baked-in defect already visible in a committed snapshot; WR-03 hardens the renderer against the additive-shape rule (`docs/trace-schema.md §4`) so a future producer-added status won't silently corrupt the breakdown.

Output: `cli.py` `_render_collapsed_group` consumes both `role` and `model_id` and surfaces `model_id_short` in its header; the collapse loop keys runs by `(role, model_id)` (mirroring the cost rollup at `cli.py:329-338`); `_aggregate_trace` excludes event/kind records from the `by_role` pass (mirroring `_is_groupable` at `cli.py:218-220`); `_render_collapsed_group` adds an `other` bucket for unknown statuses and replaces the `'0 success'` fallback with `'{N} unknown'`. Three regression tests added; one existing snapshot re-recorded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-trace-observability-polish/09-CONTEXT.md
@.planning/phases/09-trace-observability-polish/09-PATTERNS.md
@.planning/phases/09-trace-observability-polish/09-VERIFICATION.md
@.planning/phases/09-trace-observability-polish/09-REVIEW.md
@agents/graph-wiki-agent/src/graph_wiki_agent/cli.py
@agents/graph-wiki-agent/tests/unit/test_trace_viewer.py
@agents/graph-wiki-agent/tests/unit/__snapshots__/test_trace_viewer.ambr

<interfaces>
<!-- Existing relevant signatures and invariants (from current cli.py): -->

<!-- `_aggregate_trace(records: list[dict]) -> dict` returns: -->
<!--   { "by_role": {role: {count, tokens_in, tokens_out}}, -->
<!--     "by_role_model": {f"{role}|{model_id}": {role, model_id, count, tokens_in, tokens_out, cost_usd_sum, unknown_cost_count}}, -->
<!--     "total_records", "total_tokens_in", "total_tokens_out" } -->

<!-- `_is_groupable(record: dict) -> bool` at cli.py:218-220: -->
<!--   `return "event" not in record and "kind" not in record` -->
<!--   THIS IS THE EXACT FILTER WR-02 NEEDS — reuse it in the by_role pass. -->

<!-- `_render_collapsed_group(records: list[dict]) -> str` at cli.py:164-215 currently: -->
<!--   - Reads `role = records[0].get("role", "-")` but never reads `model_id`. -->
<!--   - Hardcodes `counts = {"success": 0, "error": 0, "cancelled": 0}` and the canonical-order list `("success", "error", "cancelled")`. -->
<!--   - Falls back to `"0 success"` when no canonical-status counts are nonzero. -->

<!-- Cost rollup key at cli.py:329-338 keys by `(role, model_id)` and uses `model_short = model_id[-30:]` for display. Mirror this convention for the collapsed-group header. -->

<!-- Collapse extend-or-flush at cli.py:303 currently: -->
<!--   `if current_run and current_run[-1].get("role") == record.get("role"):` -->
<!--   MUST BECOME: -->
<!--   `if current_run and current_run[-1].get("role") == record.get("role") and current_run[-1].get("model_id") == record.get("model_id"):` -->

<!-- Decisions this plan reaffirms (from 09-CONTEXT.md): -->
<!-- D-11: collapse applies only to groupable (non-event/kind) records — extend to by_role aggregation per WR-02. -->
<!-- D-13: collapsed-group header shape — extend with model attribution per CR-01. -->
<!-- D-15: cost rollup ordering — unchanged; this plan does not touch the cost rollup section. -->

<!-- Test file conventions (test_trace_viewer.py): -->
<!--   - `_run_trace_cmd(args)` helper for subprocess invocation. -->
<!--   - `_write_*_fixture(tmp_path)` helpers create JSONL files. -->
<!--   - Snapshot tests use `syrupy`'s `SnapshotAssertion` fixture. -->
<!--   - All fixtures stamp `schema_version: 1` (Task 1 of 09-05 already pins this). -->

<!-- Snapshot file (`test_trace_viewer.ambr`) line 94 currently bakes in `    unknown: count=1 tokens_in=0 tokens_out=0` — this line MUST disappear after WR-02. -->

<!-- Out of scope (per planning_context — DO NOT plan tasks for these): -->
<!--   - WR-01 (bool-as-int coercion) — INFO severity, latent producer-side bug class. -->
<!--   - WR-04 (missing-timestamp dash fallback) — INFO cosmetic. -->
<!--   - IN-01 (bare ValueError on float(cost) for bad input) — operator-friendliness, not correctness. -->
<!--   - IN-04 (docs/cancellation.md inline examples lack schema_version) — minor doc drift. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix CR-01 — collapse by (role, model_id) and surface model attribution in the collapsed-group header</name>
  <files>agents/graph-wiki-agent/src/graph_wiki_agent/cli.py, agents/graph-wiki-agent/tests/unit/test_trace_viewer.py</files>
  <read_first>
    - agents/graph-wiki-agent/src/graph_wiki_agent/cli.py L164-215 (`_render_collapsed_group` — current header line shape; never reads `model_id`)
    - agents/graph-wiki-agent/src/graph_wiki_agent/cli.py L297-308 (the collapse extend-or-flush loop; L303 is the role-only group key per VERIFICATION.md)
    - agents/graph-wiki-agent/src/graph_wiki_agent/cli.py L329-345 (cost rollup at `(role, model_id)` — the parity target; note `model_short = model_id[-30:]`)
    - agents/graph-wiki-agent/tests/unit/test_trace_viewer.py L498-529 (`_write_fan_out_fixture` — base for the new mixed-model fixture)
    - agents/graph-wiki-agent/tests/unit/test_trace_viewer.py L548-605 (existing collapse tests for shape conventions and assertion patterns)
    - .planning/phases/09-trace-observability-polish/09-VERIFICATION.md (Gaps section, CR-01)
    - .planning/phases/09-trace-observability-polish/09-CONTEXT.md (D-13 collapsed-group header shape)
  </read_first>
  <behavior>
    - The collapse loop keys consecutive-run extension on the tuple `(record.get("role"), record.get("model_id"))`, not on `role` alone. Two same-role records with different `model_id` start two distinct runs.
    - `_render_collapsed_group` reads `model_id = records[0].get("model_id", "-")` and includes a `model_id_short = model_id[-30:] if model_id and model_id != "-" else "-"` in the header (mirroring the cost-rollup convention at `cli.py:345`).
    - The collapsed-group header line includes the model id somewhere unambiguous and parseable, e.g.: `[<ts_first> .. <ts_last>] <role> / <model_short> x<N>: <breakdown>, <tin>-><tout> tokens, <cost>`. Exact format details (separator choice — ` / `, ` (`, etc.) are planner discretion, but the model id MUST appear on the line.
    - Existing snapshot tests `test_default_mode_collapses_consecutive_same_role`, `test_collapsed_default_snapshot`, `test_mixed_status_in_run_snapshot`, and `test_query_summary_interleaved_breaks_group_snapshot` all currently use single-model fan-outs — their fan-out tokens (e.g., `scanner x4`) will gain a `/ <model_short>` segment. Snapshots for these tests WILL regenerate on `--snapshot-update` (deferred to Task 3 to avoid thrashing the .ambr file). Numeric assertions (token sums, cost sums, counts) MUST remain unchanged.
    - New regression test `test_mixed_model_same_role_breaks_collapse(tmp_path)`: two consecutive `role: scanner` records, first with `model_id: us.anthropic.claude-haiku-4-5-20251001-v1:0`, second with `model_id: us.anthropic.claude-sonnet-4-5-20251001-v1:0` — both isolated (run-of-1 after the split), so each renders via `_render_trace_record` (the existing isolated-record path); OR if the run is length-1 each, both fall through to per-record rendering. Assertion: stdout contains both model_id substrings on separate lines OR (if the chosen header format still names both models on one collapsed line via some future variant) at minimum both model_id substrings appear in the timeline. Recommended assertion: `result.stdout.count("haiku-4-5") >= 1` AND `result.stdout.count("sonnet-4-5") >= 1` AND the two model substrings appear on distinct lines.
    - The cost rollup section (`Cost rollup (per role/model):`) is unchanged — it already keys by `(role, model_id)`. This task only fixes the timeline collapse.
  </behavior>
  <action>
    Step A — Modify `_render_collapsed_group` in `agents/graph-wiki-agent/src/graph_wiki_agent/cli.py` (L164-215):

    - Inside the function, after reading `role = records[0].get("role", "-")`, read `model_id = records[0].get("model_id", "-")` and compute `model_short = model_id[-30:] if model_id and model_id != "-" else "-"`.
    - Update the returned f-string to include `model_short` in the header. Suggested shape (planner discretion within these constraints): `f"[{ts_first} .. {ts_last}] {role} / {model_short} x{n}: {breakdown}, {sum_tin}->{sum_tout} tokens, {cost_str}"`. The header line MUST surface the model id; the separator is planner discretion but should match the cost-rollup's `/` convention for visual consistency.
    - Do NOT change the numeric aggregation (token sums, cost sums, counts, breakdown computation). Only the header string composition changes.

    Step B — Modify the collapse extend-or-flush loop in the `trace` command (current L303):

    - Replace `if current_run and current_run[-1].get("role") == record.get("role"):` with a tuple-key comparison:
      `if (current_run and current_run[-1].get("role") == record.get("role") and current_run[-1].get("model_id") == record.get("model_id")):`
    - No other changes to the collapse-loop control flow. `_flush()` still handles `len(current_run) >= 2` vs `== 1` correctly.

    Step C — Add `test_mixed_model_same_role_breaks_collapse(tmp_path: Path)` to `agents/graph-wiki-agent/tests/unit/test_trace_viewer.py`:

    - Write a JSONL fixture with TWO consecutive records, both `schema_version: 1`, both `role: scanner`, both `status: success`, but with different `model_id`:
      - Record 1: `model_id: "us.anthropic.claude-haiku-4-5-20251001-v1:0"`, `item_id: "page-0"`, `timestamp: "2026-05-17T10:00:00Z"`, `tokens_in: 10`, `tokens_out: 5`, `cost_usd: 0.0001`, `prompt_hash: null`, `latency_ms: 100`.
      - Record 2: `model_id: "us.anthropic.claude-sonnet-4-5-20251001-v1:0"`, `item_id: "page-1"`, `timestamp: "2026-05-17T10:00:01Z"`, `tokens_in: 20`, `tokens_out: 10`, `cost_usd: 0.0002`, `prompt_hash: null`, `latency_ms: 110`.
    - Run `_run_trace_cmd([str(fixture)])` (NO `--expand` flag — default mode).
    - Assert `result.returncode == 0`.
    - Assert `"haiku-4-5" in result.stdout` AND `"sonnet-4-5" in result.stdout`.
    - Find the timeline portion of `result.stdout` (everything before `=== Summary ===`):
      `timeline = result.stdout.split("=== Summary ===")[0]`
    - Assert that `haiku-4-5` and `sonnet-4-5` appear on DISTINCT non-empty lines of `timeline` (i.e., not on the same line). Implementation:
      ```python
      haiku_lines = [ln for ln in timeline.splitlines() if "haiku-4-5" in ln]
      sonnet_lines = [ln for ln in timeline.splitlines() if "sonnet-4-5" in ln]
      assert haiku_lines, f"expected haiku line in timeline; got:\n{timeline}"
      assert sonnet_lines, f"expected sonnet line in timeline; got:\n{timeline}"
      assert haiku_lines != sonnet_lines, (
          f"expected haiku and sonnet on distinct lines; both matched same line(s):\n"
          f"haiku_lines={haiku_lines}\nsonnet_lines={sonnet_lines}"
      )
      ```
    - This regression test pins CR-01: two same-role records on different `model_id`s must NOT collapse into a single role-only line.

    Step D — Do NOT regenerate the existing collapse snapshots in this task. Existing snapshots `test_collapsed_default_snapshot`, `test_mixed_status_in_run_snapshot`, `test_query_summary_interleaved_breaks_group_snapshot`, and inline-asserted `test_default_mode_collapses_consecutive_same_role` will diverge after this change. The snapshot regeneration is deferred to Task 3 (single `--snapshot-update` pass at the end of the plan). Numeric inline assertions in `test_default_mode_collapses_consecutive_same_role` (token sums, cost sums, counts, `scanner x4`) may need adjustment in Task 3 if the chosen header format moves the `x4` token relative to other anchors; if a test using literal-substring assertions fails, fix it in Task 3 alongside snapshot regeneration.
  </action>
  <verify>
    <automated>grep -n "model_id" agents/graph-wiki-agent/src/graph_wiki_agent/cli.py | grep -E "_render_collapsed_group|model_short" | head -5</automated>
    <automated>grep -q "current_run\[-1\].get(\"model_id\") == record.get(\"model_id\")" agents/graph-wiki-agent/src/graph_wiki_agent/cli.py</automated>
    <automated>uv run --package graph-wiki-agent pytest agents/graph-wiki-agent/tests/unit/test_trace_viewer.py::test_mixed_model_same_role_breaks_collapse -x</automated>
  </verify>
  <done>
    - `_render_collapsed_group` reads `model_id` from `records[0]` and emits a `model_short` segment in its header line.
    - The collapse extend-or-flush loop in `trace` keys runs by `(role, model_id)` rather than `role` alone.
    - `test_mixed_model_same_role_breaks_collapse` exists and passes; it asserts the haiku and sonnet model substrings appear on DISTINCT timeline lines.
    - Existing snapshot tests are expected to fail at this point — Task 3 will regenerate them in a single pass. This task does NOT run `--snapshot-update`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix WR-02 (filter event/kind from by_role) and WR-03 (other-bucket for unknown statuses)</name>
  <files>agents/graph-wiki-agent/src/graph_wiki_agent/cli.py, agents/graph-wiki-agent/tests/unit/test_trace_viewer.py</files>
  <read_first>
    - agents/graph-wiki-agent/src/graph_wiki_agent/cli.py L120-161 (`_aggregate_trace`; L124-132 is the by_role pass that runs BEFORE the event/kind filter at L135-136 per VERIFICATION.md)
    - agents/graph-wiki-agent/src/graph_wiki_agent/cli.py L182-189 (`_render_collapsed_group` hardcoded status counts and `"0 success"` fallback per VERIFICATION.md)
    - agents/graph-wiki-agent/src/graph_wiki_agent/cli.py L218-220 (`_is_groupable` — reuse this filter in the by_role pass)
    - agents/graph-wiki-agent/tests/unit/__snapshots__/test_trace_viewer.ambr L94 (the baked-in phantom `unknown: count=1 tokens_in=0 tokens_out=0` line that must disappear)
    - agents/graph-wiki-agent/tests/unit/test_trace_viewer.py L676-785 (`_write_interleaved_fixture` — the fixture that drives the interleaved snapshot)
    - agents/graph-wiki-agent/tests/unit/test_trace_viewer.py L144-275 (existing `test_aggregate_trace_by_role_model_groups_and_costs` and adjacent direct-`_aggregate_trace` tests for assertion patterns; check whether any directly asserts on `by_role["unknown"]` — if so the assertion needs revision in this task too)
    - .planning/phases/09-trace-observability-polish/09-VERIFICATION.md (Gaps section, WR-02 and WR-03)
    - docs/trace-schema.md §4 (additive-shape rule — justifies the WR-03 `other`-bucket design)
  </read_first>
  <behavior>
    WR-02 fix:
    - `_aggregate_trace`'s by_role pass excludes event/kind records (records where `"event" in record or "kind" in record`). It can either (a) skip the entire by_role mutation for non-groupable records — recommended for symmetry with the by_role_model pass below it — or (b) gate via `if _is_groupable(record):`. Either approach is acceptable; recommended is to reuse `_is_groupable(record)` for textual symmetry with L218-220.
    - The `Per-role breakdown` rendering at `cli.py:317-322` continues unchanged — the upstream aggregator now never produces an `unknown` bucket from event/kind records, so no synthetic `unknown` line appears in stdout for clean traces.
    - Per-item records that genuinely lack a `role` field (malformed producer — should not occur in production but the fallback `record.get("role", "unknown")` remains in the code path) still bucket under `unknown` — the lenient policy is preserved. WR-02 only excludes event/kind discriminator records, not malformed-shape records.
    - If any existing direct-`_aggregate_trace` unit test (`test_aggregate_trace_by_role_model_groups_and_costs` or siblings) asserts on `by_role["unknown"]` from an event/kind record in its fixture, that assertion is updated to reflect the new (correct) behavior.

    WR-03 fix:
    - `_render_collapsed_group` adds an `other` bucket for statuses outside `{success, error, cancelled}`. Suggested implementation:
      ```python
      counts = {"success": 0, "error": 0, "cancelled": 0, "other": 0}
      for r in records:
          status = r.get("status")
          if status in ("success", "error", "cancelled"):
              counts[status] += 1
          else:
              counts["other"] += 1
      breakdown_parts = [f"{counts[k]} {k}" for k in ("success", "error", "cancelled", "other") if counts[k]]
      breakdown = " / ".join(breakdown_parts) if breakdown_parts else f"{n} unknown"
      ```
    - The fallback `breakdown` (when ALL records have a falsy status — e.g., missing `status` key entirely) becomes `f"{n} unknown"` rather than the misleading `"0 success"`. This makes a future producer-side bug visible rather than silently mis-reporting.
    - Records with `status: None` or missing `status` count toward `other` (they are not `success`/`error`/`cancelled`). If `counts["other"] > 0` for an all-statusless run, the breakdown shows e.g. `"3 other"`, not `"3 unknown"`. The `n unknown` fallback fires ONLY when `breakdown_parts` is empty — which can no longer happen if any record has any status (success/error/cancelled/other-value) — i.e., it fires only on a record where `r.get("status")` returns a falsy value AND the `other` branch increments. Re-read: in the suggested code above, the `else` branch ALWAYS increments `other` for any non-canonical status (including `None` and missing), so `breakdown_parts` will be empty only when `n == 0`. That means the `f"{n} unknown"` fallback is unreachable for a real N≥1 run. Acceptable outcome — the fallback now acts purely as a defensive guard rather than the actively-wrong `"0 success"` from the current code.

    Regression tests:
    - `test_aggregate_excludes_event_kind_from_by_role(tmp_path)`: build a fixture with one per-item record AND one `kind: query_summary` record. After running `graph-wiki-agent trace`, assert `result.stdout` contains `Per-role breakdown:` and the per-role section does NOT contain `unknown:` (or `unknown: count=` — pick the strongest substring that survives format changes). Additionally assert it DOES contain the legitimate role from the per-item record (e.g., `scanner: count=1`).
    - `test_collapsed_group_surfaces_unknown_status_in_other_bucket(tmp_path)`: build a fixture with 3 consecutive records all `role: scanner`, all `model_id` identical, all `status: "timeout"` (or another non-canonical status). Run default mode. Assert the timeline contains a collapsed-group line (it's a run of 3 same-role+same-model) and that the line contains `"3 other"` (or whatever exact substring the chosen format produces for 3 records in the `other` bucket). Assert the line does NOT contain `"0 success"`.
  </behavior>
  <action>
    Step A — Modify `_aggregate_trace` in `agents/graph-wiki-agent/src/graph_wiki_agent/cli.py` (L124-132):

    - Wrap the by_role mutation block (the four lines `by_role[role][...]+=...` plus the role lookup) in an `if _is_groupable(record):` guard. Keep `total_tokens_in`/`total_tokens_out` accumulation OUTSIDE that guard (they are file-level totals and should include event/kind records' token contribution if any — though event/kind records typically have `tokens_in=None`/`tokens_out=None`, the existing `or 0` coercion handles that). Actually, re-examine: at L131-132 `total_tokens_in += tin` happens after the by_role bucketing. Recommended structure:
      ```python
      for record in records:
          tin = record.get("tokens_in") or 0
          tout = record.get("tokens_out") or 0
          total_tokens_in += tin
          total_tokens_out += tout
          if not _is_groupable(record):
              # Skip both by_role and by_role_model passes for event/kind records.
              continue
          role = record.get("role", "unknown")
          by_role[role]["count"] += 1
          by_role[role]["tokens_in"] += tin
          by_role[role]["tokens_out"] += tout
          # ... existing by_role_model pass continues below ...
      ```
      This collapses the now-duplicate event/kind filter at L135-136 into a single early-continue and unifies both bucket passes under one filter. Verify the by_role_model section (currently L138-153) still runs only for groupable records — it does, by virtue of being below the early-continue.

    Step B — Modify `_render_collapsed_group` in `agents/graph-wiki-agent/src/graph_wiki_agent/cli.py` (L182-189):

    - Change the `counts` dict to include `"other": 0`.
    - In the status-counting loop, route non-canonical statuses to `counts["other"] += 1` in an `else` branch.
    - Extend the canonical order tuple to `("success", "error", "cancelled", "other")`.
    - Change the empty-`breakdown_parts` fallback from `"0 success"` to `f"{n} unknown"`.

    Step C — Add regression tests to `agents/graph-wiki-agent/tests/unit/test_trace_viewer.py`:

    - `test_aggregate_excludes_event_kind_from_by_role(tmp_path: Path) -> None`:
      - Write a JSONL fixture with two records (both `schema_version: 1`):
        1. Per-item: `{"schema_version": 1, "role": "scanner", "model_id": "us.anthropic.claude-haiku-4-5-20251001-v1:0", "prompt_hash": null, "item_id": "page-0", "status": "success", "latency_ms": 100, "tokens_in": 10, "tokens_out": 5, "cost_usd": 0.0001, "timestamp": "2026-05-17T10:00:00Z"}`
        2. Query summary: `{"schema_version": 1, "kind": "query_summary", "timestamp": "2026-05-17T10:00:01Z"}` (minimal valid query_summary — no role field).
      - Run `_run_trace_cmd([str(fixture)])`.
      - Assert `result.returncode == 0`.
      - Extract the Per-role section: `per_role_section = result.stdout.split("Per-role breakdown:")[1].split("Cost rollup")[0]`.
      - Assert `"scanner:" in per_role_section` (the legitimate per-item role IS bucketed).
      - Assert `"unknown:" not in per_role_section` (the phantom bucket is GONE).

    - `test_collapsed_group_surfaces_unknown_status_in_other_bucket(tmp_path: Path) -> None`:
      - Write a JSONL fixture with 3 consecutive records, all `schema_version: 1`, all `role: scanner`, all `model_id: "us.anthropic.claude-haiku-4-5-20251001-v1:0"`, all `status: "timeout"` (a status not in the canonical set), all with `cost_usd: 0.0001`, `tokens_in: 10`, `tokens_out: 5`, distinct `item_id`s and monotonically-increasing timestamps.
      - Run `_run_trace_cmd([str(fixture)])` (default mode — no `--expand`).
      - Assert `result.returncode == 0`.
      - Extract the timeline: `timeline = result.stdout.split("=== Summary ===")[0]`.
      - Assert `"x3" in timeline` (a collapsed group of 3 was emitted).
      - Assert `"3 other" in timeline` (the unknown-status records are surfaced under `other`).
      - Assert `"0 success" not in timeline` (the misleading fallback is NOT emitted).

    - If `test_aggregate_trace_by_role_model_groups_and_costs` (or any other direct-`_aggregate_trace` test in `test_trace_viewer.py`) asserts on an `unknown` bucket arising from a fixture that includes an event/kind record, update the assertion to expect the absence of that bucket. (Read the test before changing it — it may not currently fixture an event/kind record at all.)
  </action>
  <verify>
    <automated>grep -q "counts\[\"other\"\]" agents/graph-wiki-agent/src/graph_wiki_agent/cli.py</automated>
    <automated>grep -q "n} unknown" agents/graph-wiki-agent/src/graph_wiki_agent/cli.py</automated>
    <automated>grep -c "0 success" agents/graph-wiki-agent/src/graph_wiki_agent/cli.py | awk '{exit ($1==0)?0:1}'</automated>
    <automated>uv run --package graph-wiki-agent pytest agents/graph-wiki-agent/tests/unit/test_trace_viewer.py::test_aggregate_excludes_event_kind_from_by_role -x</automated>
    <automated>uv run --package graph-wiki-agent pytest agents/graph-wiki-agent/tests/unit/test_trace_viewer.py::test_collapsed_group_surfaces_unknown_status_in_other_bucket -x</automated>
  </verify>
  <done>
    - `_aggregate_trace` no longer buckets event/kind records under `by_role`.
    - `_render_collapsed_group` surfaces non-canonical statuses under an `other` bucket and uses `{n} unknown` as the all-empty fallback in place of the actively-wrong `0 success`.
    - The literal substring `"0 success"` no longer appears in `cli.py`.
    - Both new regression tests pass.
    - Existing direct-`_aggregate_trace` tests still pass (or are updated to reflect the new by_role filtering if any of them fixtured an event/kind record).
    - Existing snapshot tests are still expected to be out-of-date (Task 3 regenerates them).
  </done>
</task>

<task type="auto">
  <name>Task 3: Single-pass snapshot regeneration and full-suite green check</name>
  <files>agents/graph-wiki-agent/tests/unit/__snapshots__/test_trace_viewer.ambr, agents/graph-wiki-agent/tests/unit/test_trace_viewer.py</files>
  <read_first>
    - agents/graph-wiki-agent/tests/unit/__snapshots__/test_trace_viewer.ambr (full read — identify the snapshots that will regenerate: `test_collapsed_default_snapshot`, `test_mixed_status_in_run_snapshot`, `test_query_summary_interleaved_breaks_group_snapshot`, `test_cost_rollup_snapshot` (verify whether unchanged), `test_expand_snapshot` (verify whether unchanged — expand mode is NOT affected by collapse-key changes))
    - agents/graph-wiki-agent/tests/unit/test_trace_viewer.py L548-605 (`test_default_mode_collapses_consecutive_same_role` and `test_expand_mode_renders_every_record_full_line` — inline-asserted tests that may need substring tweaks if the chosen Task 1 header format moves anchor text)
    - .planning/phases/09-trace-observability-polish/09-VERIFICATION.md (Recommendation paragraph — "would close cleanly with one update to `test_query_summary_interleaved_breaks_group_snapshot.ambr`"; this task delivers that promise)
  </read_first>
  <action>
    Step A — Run the full trace-viewer test suite WITHOUT `--snapshot-update` first, to enumerate the diff:
    ```
    uv run --package graph-wiki-agent pytest agents/graph-wiki-agent/tests/unit/test_trace_viewer.py -x --no-header --tb=short 2>&1 | head -100
    ```
    Note which snapshot tests fail and which inline-asserted tests (if any) fail. Expected failures:
    - `test_collapsed_default_snapshot` — header gains `/ <model_short>` per CR-01.
    - `test_mixed_status_in_run_snapshot` — same.
    - `test_query_summary_interleaved_breaks_group_snapshot` — same PLUS `unknown: count=1 tokens_in=0 tokens_out=0` line disappears per WR-02.
    - `test_default_mode_collapses_consecutive_same_role` (inline-asserted) — may fail if its inline substring assertions anchor on `scanner x4:` exactly (with no model segment between role and `x4`). Acceptable adjustment: change the assertion to substring-match on `scanner` AND ` x4` separately (allowing the `/ <model_short>` segment between them), OR match on `scanner / ` ... ` x4`.

    Likely-unaffected:
    - `test_cost_rollup_snapshot` — cost rollup section is untouched.
    - `test_expand_snapshot` — expand mode does not use `_render_collapsed_group`.

    Step B — Adjust any inline-asserted test that fails because the chosen Task 1 header format moved a substring anchor. Prefer the smallest possible change that preserves the test's original intent (the test pins the collapse-occurred property; adding a `/ <model_short>` segment in the middle should not break that pin). Suggested pattern: replace `assert "scanner x4:" in result.stdout` with `assert "scanner" in result.stdout and " x4:" in result.stdout` (both true on the same line is implicit by visual inspection of the timeline format).

    Step C — Regenerate snapshots in ONE pass:
    ```
    uv run --package graph-wiki-agent pytest agents/graph-wiki-agent/tests/unit/test_trace_viewer.py --snapshot-update
    ```

    Step D — Diff the resulting `.ambr` file (`git diff agents/graph-wiki-agent/tests/unit/__snapshots__/test_trace_viewer.ambr`) and verify:
    - In every changed collapsed-group line, the model_id (or its 30-char suffix) now appears in the header.
    - In the `test_query_summary_interleaved_breaks_group_snapshot` snapshot, the previously-baked-in line `    unknown: count=1 tokens_in=0 tokens_out=0` is GONE.
    - No numeric drift in token sums, cost sums, counts, or `=== Summary ===` block totals (the aggregation arithmetic did not change; only header composition and event/kind filtering for by_role).
    - No snapshot for unrelated tests (e.g., `test_cost_rollup_snapshot`, `test_expand_snapshot`) was modified.

    Step E — Final green check WITHOUT `--snapshot-update`:
    ```
    uv run --package graph-wiki-agent pytest agents/graph-wiki-agent/tests/unit/test_trace_viewer.py -x
    ```
    Must exit 0. If any test still fails, do NOT re-run `--snapshot-update` blindly — investigate; a non-snapshot test failure indicates a regression in Task 1 or Task 2 that needs to be fixed at its source.

    Step F — Wider regression sanity check (cheap):
    ```
    uv run --package graph-wiki-agent pytest agents/graph-wiki-agent/tests/unit -x
    ```
    The unit-test directory has other test files (e.g., `test_pool.py`, `test_query_summary_schema_version.py`). They should all still pass — this plan does not modify any producer code or schema-version constants.
  </action>
  <verify>
    <automated>uv run --package graph-wiki-agent pytest agents/graph-wiki-agent/tests/unit/test_trace_viewer.py -x</automated>
    <automated>grep -c "unknown: count=1 tokens_in=0 tokens_out=0" agents/graph-wiki-agent/tests/unit/__snapshots__/test_trace_viewer.ambr | awk '{exit ($1==0)?0:1}'</automated>
    <automated>uv run --package graph-wiki-agent pytest agents/graph-wiki-agent/tests/unit -x</automated>
  </verify>
  <done>
    - `uv run --package graph-wiki-agent pytest agents/graph-wiki-agent/tests/unit/test_trace_viewer.py -x` exits 0 WITHOUT `--snapshot-update`.
    - The literal substring `unknown: count=1 tokens_in=0 tokens_out=0` no longer appears in `test_trace_viewer.ambr`.
    - All collapsed-group snapshot lines now include a model_id segment in the header.
    - No numeric drift in any snapshot's token sums, cost sums, counts, or Summary totals (only header composition + the WR-02 line removal).
    - The wider `agents/graph-wiki-agent/tests/unit` suite passes (no upstream regression from this plan's code changes).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User-supplied `<file>` path → renderer parse loop | Same boundary as 09-05. This plan adds NO new untrusted-input surface; it modifies pure-function aggregation/rendering of already-parsed records. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-16 | Tampering | A trace record with `model_id` set to a very long string (e.g., 10 KB) crafted to bloat the collapsed-group header | accept | The existing cost-rollup at `cli.py:345` already truncates `model_id[-30:]` for display; this plan adopts the same convention. Header length is bounded. No new exposure. |
| T-09-17 | Information Disclosure | The `model_id` is now surfaced in two places (cost rollup + timeline header) instead of one | accept | `model_id` is operator-supplied configuration (`models-qwen.toml`, `wiki-config.toml`); it is not sensitive and is already echoed in the cost rollup, error messages, and producer log lines. Surfacing it on the timeline header is consistent. |
| T-09-18 | Tampering | A producer emits `status: "<script>"` or another XSS-like value, surfaced via the `other` bucket in the breakdown | accept | The renderer writes to stdout via `typer.echo`; downstream consumption is a terminal, not a browser. No HTML rendering surface exists. Acceptance: the project ships no web UI in scope. |
| T-09-19 | Repudiation | After WR-02, event/kind records no longer appear in the Per-role breakdown — an operator might miss that the file contained a `query_summary` record | accept | The query_summary record still renders as a full-line per-record entry in the timeline (via `_render_trace_record`, since it's non-groupable per `_is_groupable`) AND its existence is visible in `Total records:` of the Summary block. WR-02 removes ONLY the misleading `unknown` bucket; it does not hide the record's existence. |
</threat_model>

<verification>
- `uv run --package graph-wiki-agent pytest agents/graph-wiki-agent/tests/unit/test_trace_viewer.py -x` exits 0 WITHOUT `--snapshot-update`.
- `uv run --package graph-wiki-agent pytest agents/graph-wiki-agent/tests/unit -x` exits 0 (no upstream regression).
- `_render_collapsed_group` reads `model_id` and emits it in the header (`grep -E "model_id|model_short" agents/graph-wiki-agent/src/graph_wiki_agent/cli.py | grep -E "_render_collapsed_group" -A 5` shows the wiring).
- Collapse extend-or-flush keys on `(role, model_id)` (`grep -q 'current_run\[-1\].get("model_id") == record.get("model_id")' agents/graph-wiki-agent/src/graph_wiki_agent/cli.py`).
- `_aggregate_trace` by_role pass is event/kind-filtered (verified via the new `test_aggregate_excludes_event_kind_from_by_role` test).
- `_render_collapsed_group` status breakdown handles `other` and replaces `"0 success"` fallback with `"{n} unknown"` (verified via the new `test_collapsed_group_surfaces_unknown_status_in_other_bucket` test).
- `test_trace_viewer.ambr` no longer contains the phantom `unknown: count=1 tokens_in=0 tokens_out=0` line from the interleaved snapshot.
- The literal substring `"0 success"` is gone from `cli.py`.
- Renderer still has zero `eval_harness` imports (D-10 invariant).
</verification>

<success_criteria>
1. **CR-01 closed:** Two consecutive same-role records on different `model_id`s render as TWO distinct lines (run-of-1 each, falling through to `_render_trace_record`), and any collapsed group of ≥2 records surfaces `model_id` in its header line. Regression test `test_mixed_model_same_role_breaks_collapse` pins this.
2. **WR-02 closed:** `_aggregate_trace`'s `by_role` pass excludes event/kind records. The interleaved snapshot no longer contains `unknown: count=1 tokens_in=0 tokens_out=0`. Regression test `test_aggregate_excludes_event_kind_from_by_role` pins this.
3. **WR-03 closed:** `_render_collapsed_group` adds an `other` bucket for non-canonical statuses and the `"0 success"` fallback is gone, replaced with `f"{n} unknown"`. Regression test `test_collapsed_group_surfaces_unknown_status_in_other_bucket` pins this.
4. **Snapshot hygiene:** `test_trace_viewer.ambr` was re-recorded in a SINGLE `--snapshot-update` pass (Task 3), not three times. No numeric drift in any snapshot; only header composition changes (CR-01) and the WR-02 phantom-bucket line removal.
5. **No regression:** The full `agents/graph-wiki-agent/tests/unit` suite passes after all three tasks complete.
6. **Scope discipline:** No changes to producer code (`pool.py`, `query.py`), no changes to `docs/trace-schema.md`, no changes to the cost rollup section of `cli.py`. WR-01, WR-04, IN-01, IN-04 from the verification report are explicitly out of scope per the planning-context directive.
</success_criteria>

<output>
Create `.planning/phases/09-trace-observability-polish/09-06-SUMMARY.md` when done. Summary must list:
- The chosen collapsed-group header format string (verbatim, with the `model_short` segment).
- The exact `cli.py` line numbers where the three fixes landed (CR-01 collapse-key, WR-02 by_role filter, WR-03 other-bucket).
- The three new regression test names and their assertion strategies.
- The list of snapshots in `test_trace_viewer.ambr` that regenerated, and confirmation that the phantom `unknown: count=1` line is gone.
- Confirmation that no producer code, schema docs, or cost-rollup logic was touched.
</output>
