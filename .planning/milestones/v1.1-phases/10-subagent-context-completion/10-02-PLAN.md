---
phase: 10-subagent-context-completion
plan: 02
type: execute
wave: 2
depends_on: []
files_modified:
  - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/architecture_overview.py
autonomous: true
requirements:
  - CTX-01
must_haves:
  truths:
    - "A new shared fragment ARCHITECTURE_OVERVIEW is importable from graph_wiki_agent.prompts._fragments.architecture_overview"
    - "Its 3-line provenance header cites cores/prompt-sources/SKILL.md §Architecture L34-L69 and resolves on disk"
    - "Its content is a compact rewrite of SKILL.md §Architecture (~600 tokens) preserving the vault directory tree, the conditional-containers note, and the 'code is source of truth' sentence"
  artifacts:
    - path: "agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/architecture_overview.py"
      provides: "ARCHITECTURE_OVERVIEW string constant + 3-line provenance header"
      contains: "ARCHITECTURE_OVERVIEW = \"\"\""
  key_links:
    - from: "agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/architecture_overview.py"
      to: "cores/prompt-sources/SKILL.md"
      via: "# Source: cores/prompt-sources/SKILL.md provenance header"
      pattern: "^# Source: cores/prompt-sources/SKILL.md$"
---

<objective>
Add `agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/architecture_overview.py` — a new shared fragment carrying a compact rewrite of `cores/prompt-sources/SKILL.md §Architecture (L34-L69)`. This is the first of four new fragments and is the largest by token count (~600 tokens), so it lands alone to keep the diff focused and the token-budget math observable.

Purpose: Subagents currently lack any explicit vault-layout context. Phase 6's port did not include the §Architecture section. Adding `ARCHITECTURE_OVERVIEW` to the shared fragment library lets later plans (10-05) wire it into the scanner and ingestor system prompts so those agents know what the vault tree looks like.

Output: One new Python module under `prompts/_fragments/` exporting the `ARCHITECTURE_OVERVIEW` string constant, with the mandatory 3-line provenance header. The module is pure data — no imports, no functions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-subagent-context-completion/10-CONTEXT.md
@.planning/phases/10-subagent-context-completion/10-PATTERNS.md
@cores/prompt-sources/SOURCE-COMMIT
@cores/prompt-sources/SKILL.md

<interfaces>
<!-- Pattern to replicate exactly. -->

From agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/iron_rules.py (the canonical analog — 15 lines total):
```
# Source: cores/prompt-sources/SKILL.md
# Anchor: ## Iron rules (L193-L201)
# Source-commit: ef05d99

IRON_RULES = """\
## Iron rules

1. **The code is the source of truth.** ...
...
"""
```

From agents/graph-wiki-agent/tests/prompts/test_provenance.py (the invariant enforced):
- `_PROVENANCE_RE` requires three consecutive lines: `# Source: <anything>\n# Anchor: <anything>\n# Source-commit: <hex>`
- The `Source:` path must start with `cores/prompt-sources/` AND the resolved file must exist on disk.
- The Source-commit regex accepts any lowercase hex digit sequence (`[a-f0-9]+`) — short or long SHA both pass.

From cores/prompt-sources/SKILL.md (the content source — L34-L69):
- L34: `## Architecture`
- L36: leading paragraph about workspace location (skip in the compact rewrite — user-facing prose)
- L38-L65: the vault directory tree code fence
- L67: the "conditional containers" note about apps/, packages/, domains/ being conditional
- L69: "Source of truth is the code itself."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create _fragments/architecture_overview.py with provenance header and compact rewrite of SKILL.md §Architecture</name>
  <read_first>
    - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/iron_rules.py (exact analog — 15 lines; copy the shape, change header values and constant name)
    - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/page_categories.py (second example of the same pattern)
    - cores/prompt-sources/SKILL.md (the source — L34-L69 is the anchor range to compact-rewrite)
    - agents/graph-wiki-agent/tests/prompts/test_provenance.py (the invariant; lines 53-58 show the exact regex)
    - cores/prompt-sources/SOURCE-COMMIT (single line; current value = `ef05d991a9ab1ea12b1bc7ebc1fb20ba70074030`)
  </read_first>
  <files>agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/architecture_overview.py</files>
  <action>
    Create the new module agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/architecture_overview.py.

    Required provenance header (exact format — three lines, no blank line between them, immediately followed by one blank line):
    - Line 1: `# Source: cores/prompt-sources/SKILL.md`
    - Line 2: `# Anchor: ## Architecture (L34-L69)`
    - Line 3: `# Source-commit: ef05d991a9ab1ea12b1bc7ebc1fb20ba70074030`

    After the blank line, declare the module-level constant `ARCHITECTURE_OVERVIEW` as a triple-quoted string (use the `"""\` opening-backslash idiom matching iron_rules.py so the first line of the rendered string is `## Vault layout`, not a blank line). No imports, no functions, no `__all__`.

    Content of `ARCHITECTURE_OVERVIEW` — compact rewrite of SKILL.md §Architecture L34-L69. Preserve these load-bearing elements (per CONTEXT.md §Fragment curation and PATTERNS.md):
    1. A top-level `## Vault layout` heading (preferred over `## Architecture` since the fragment's purpose is layout context for subagents, not a general arch overview).
    2. The vault directory tree from SKILL.md L38-L65 — keep it as a fenced code block. Trim user-facing prose lines (e.g., the leading workspace-location paragraph) but keep every line that names a vault subdirectory (`index.md`, `log.md`, `apps/<app>/`, `packages/<pkg>/`, `domains/<domain>/`, `concepts/`, `dependencies/`, `sources/`, `architecture/`, `adrs/`, `.templates/`, `CLAUDE.md`, `AGENTS.md`) along with its inline `# [conditional]` annotation where present.
    3. The conditional-containers note (SKILL.md L67): a sentence stating that `apps/`, `packages/`, `domains/` are created only when the repo has matching containers, and that pinned containers are recorded in `wiki/CLAUDE.md` / `wiki/AGENTS.md`. Compress freely — the load-bearing fact is "these subdirectories are conditional and pinned in wiki/CLAUDE.md".
    4. The "code is source of truth" sentence (SKILL.md L69): preserve verbatim or near-verbatim — this is a load-bearing iron-rule echo.

    Drop:
    - The opening paragraph about `<workspace>/wiki/` location, `lattice-workspace`, `.lattice.local.yaml`, Obsidian vault path — user-facing prose, no rule content.
    - Anything outside L34-L69 in SKILL.md.

    Target rendered length: ~600 tokens (~2,400 characters). Stay under 700 tokens. The compact rewrite is at the executor's discretion (per CONTEXT.md §Claude's Discretion) subject to preserving items 1-4 above.

    No project_context wiring in this plan. No imports into scanner.py / ingestor.py / etc. Wiring happens in plan 10-05.
  </action>
  <verify>
    <automated>test -f agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/architecture_overview.py &amp;&amp; uv run --package graph-wiki-agent pytest agents/graph-wiki-agent/tests/prompts/test_provenance.py -x &amp;&amp; uv run --package graph-wiki-agent python -c "from graph_wiki_agent.prompts._fragments.architecture_overview import ARCHITECTURE_OVERVIEW; assert ARCHITECTURE_OVERVIEW.startswith('## Vault layout'); assert 'code is source of truth' in ARCHITECTURE_OVERVIEW.lower() or 'code wins' in ARCHITECTURE_OVERVIEW.lower(); assert len(ARCHITECTURE_OVERVIEW) // 4 &lt;= 700"</automated>
  </verify>
  <acceptance_criteria>
    - File agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/architecture_overview.py exists.
    - First three lines of the file are exactly: `# Source: cores/prompt-sources/SKILL.md`, `# Anchor: ## Architecture (L34-L69)`, `# Source-commit: ef05d991a9ab1ea12b1bc7ebc1fb20ba70074030`.
    - `grep -c "^ARCHITECTURE_OVERVIEW = \"\"\"" agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/architecture_overview.py` reports 1.
    - `from graph_wiki_agent.prompts._fragments.architecture_overview import ARCHITECTURE_OVERVIEW` succeeds in a `uv run --package graph-wiki-agent python` shell.
    - `ARCHITECTURE_OVERVIEW.startswith("## Vault layout")` is True.
    - The string contains the substrings `packages/<pkg>/`, `apps/<app>/`, `domains/<domain>/`, `adrs/`, `[conditional]` (case-sensitive on the bracket form).
    - The string contains either "code is the source of truth" or "code wins" (the load-bearing iron-rule echo).
    - `len(ARCHITECTURE_OVERVIEW) // 4 &lt;= 700` (token budget guard rail; ~600 target).
    - `uv run --package graph-wiki-agent pytest agents/graph-wiki-agent/tests/prompts/test_provenance.py -x` exits 0 (both `test_all_fragments_have_provenance_header` and `test_provenance_source_paths_resolve` pass for the new file).
    - No imports in the new file other than the (optional, conventional) `from __future__ import annotations` — verify `grep -c "^from graph_wiki_agent" agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/architecture_overview.py` reports 0.
    - No edits to any file outside `agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/architecture_overview.py` (verify via `git diff --name-only HEAD`).
  </acceptance_criteria>
  <done>The fragment file exists with the correct 3-line provenance header, exports `ARCHITECTURE_OVERVIEW` containing the load-bearing vault-layout content, stays within the ~700-token budget, and passes test_provenance.</done>
</task>

</tasks>

<verification>
1. Provenance test passes against the new file.
2. Token budget check: `len(ARCHITECTURE_OVERVIEW) // 4 <= 700`.
3. Required substrings (`## Vault layout`, conditional-containers annotation, code-source-of-truth echo) present.
4. No other files in the repo are modified.
</verification>

<success_criteria>
- New fragment exists with correct provenance, compact content, and import works cleanly.
- Token cost stays under 700 (target 600).
- This plan is wire-free — the fragment is not yet consumed by any prompt builder. That comes in 10-05.
- Scope fences honored: no deepagents import, no pyproject.toml change, no subagent-runtime/pool.py change.
</success_criteria>

<output>
Create `.planning/phases/10-subagent-context-completion/10-02-SUMMARY.md` when done.
</output>
