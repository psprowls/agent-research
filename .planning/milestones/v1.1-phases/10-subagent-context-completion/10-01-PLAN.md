---
phase: 10-subagent-context-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cores/prompt-sources/wiki-claude-md-template.md
autonomous: true
requirements:
  - CTX-01
must_haves:
  truths:
    - "The vendored upstream CLAUDE.md.template exists at cores/prompt-sources/wiki-claude-md-template.md"
    - "Subsequent fragments (style_rules, log_format) can anchor a `# Source: cores/prompt-sources/wiki-claude-md-template.md` provenance line that resolves on disk"
  artifacts:
    - path: "cores/prompt-sources/wiki-claude-md-template.md"
      provides: "Vendored verbatim copy of upstream lattice-wiki CLAUDE.md.template (159 lines)"
      min_lines: 150
  key_links:
    - from: "agents/graph-wiki-agent/tests/prompts/test_provenance.py"
      to: "cores/prompt-sources/wiki-claude-md-template.md"
      via: "PROMPT_SOURCES_DIR / 'wiki-claude-md-template.md' .exists()"
      pattern: "wiki-claude-md-template.md"
---

<objective>
Vendor the upstream lattice-wiki `CLAUDE.md.template` into `cores/prompt-sources/wiki-claude-md-template.md` so that Phase 10 fragments anchored to it (`style_rules.py`, `log_format.py`) satisfy the `test_provenance.py` invariant at every commit boundary.

Purpose: The provenance test enforces that every fragment's `# Source: cores/prompt-sources/<path>` cites a file that exists on disk under `cores/prompt-sources/`. Style/log fragments anchor to the wiki-side template — that template must land before the fragments do, or the provenance test would fail on the intermediate commit. This plan ships the file with no code changes elsewhere.

Output: A single new file `cores/prompt-sources/wiki-claude-md-template.md` — a verbatim copy of `/Users/pat/Personal/lattice/dist/lattice-wiki/skills/lattice-wiki/scripts/vendor/assets/CLAUDE.md.template` (the upstream tracked at `cores/prompt-sources/SOURCE-COMMIT` = `ef05d991a9ab1ea12b1bc7ebc1fb20ba70074030`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-subagent-context-completion/10-CONTEXT.md
@.planning/phases/10-subagent-context-completion/10-PATTERNS.md
@cores/prompt-sources/SOURCE-COMMIT

<interfaces>
<!-- Provenance test invariant the executor must preserve. -->

From agents/graph-wiki-agent/tests/prompts/test_provenance.py:
- Constant `PROMPT_SOURCES_DIR = Path(__file__).resolve().parents[4] / "cores" / "prompt-sources"`
- Test `test_provenance_source_paths_resolve` asserts that for every fragment, `source_rel.startswith("cores/prompt-sources/")` AND `(PROMPT_SOURCES_DIR / suffix).exists()`.
- This plan lands the file at `cores/prompt-sources/wiki-claude-md-template.md` so future fragment provenance headers naming that path resolve cleanly.

From cores/prompt-sources/ (existing vendored layout):
- `cores/prompt-sources/SKILL.md` — analog: another verbatim vendored upstream file, no header, no transformation
- `cores/prompt-sources/SOURCE-COMMIT` — single line, current value `ef05d991a9ab1ea12b1bc7ebc1fb20ba70074030`
- `cores/prompt-sources/agents/` — vendored per-role markdown sources
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vendor CLAUDE.md.template verbatim into cores/prompt-sources/</name>
  <read_first>
    - cores/prompt-sources/SKILL.md (analog: another verbatim vendored upstream file; sets the precedent — no header, no transformation)
    - cores/prompt-sources/SOURCE-COMMIT (confirms current pinned commit value)
    - agents/graph-wiki-agent/tests/prompts/test_provenance.py (provenance invariant the vendored file enables — line 86-111 is the test_provenance_source_paths_resolve check)
    - /Users/pat/Personal/lattice/dist/lattice-wiki/skills/lattice-wiki/scripts/vendor/assets/CLAUDE.md.template (the source to copy)
  </read_first>
  <files>cores/prompt-sources/wiki-claude-md-template.md</files>
  <action>
    Create cores/prompt-sources/wiki-claude-md-template.md as a byte-for-byte copy of /Users/pat/Personal/lattice/dist/lattice-wiki/skills/lattice-wiki/scripts/vendor/assets/CLAUDE.md.template (159 lines).

    Implementation: Use the Read tool to load the source file in full, then use the Write tool to emit the same contents at the destination path. Do NOT add a vendoring header, a provenance comment, or any wrapper — the file is a passive data asset cited by fragment provenance headers, not a Python module.

    Do NOT bump cores/prompt-sources/SOURCE-COMMIT. The current value (ef05d991a9ab1ea12b1bc7ebc1fb20ba70074030) already corresponds to the upstream commit being vendored — no edit required.

    The §Style heading must land at line 153 and the §Log format heading at line 124 in the destination so that future fragment anchors (`## Style (L153-L159)`, `## Log format (L124-L133)`) reference the correct line ranges.

    No code changes outside this single file. No imports added anywhere. No pyproject.toml changes.
  </action>
  <verify>
    <automated>test -f cores/prompt-sources/wiki-claude-md-template.md &amp;&amp; diff -q cores/prompt-sources/wiki-claude-md-template.md /Users/pat/Personal/lattice/dist/lattice-wiki/skills/lattice-wiki/scripts/vendor/assets/CLAUDE.md.template &amp;&amp; uv run --package graph-wiki-agent pytest agents/graph-wiki-agent/tests/prompts/test_provenance.py -x</automated>
  </verify>
  <acceptance_criteria>
    - File cores/prompt-sources/wiki-claude-md-template.md exists.
    - `diff -q cores/prompt-sources/wiki-claude-md-template.md /Users/pat/Personal/lattice/dist/lattice-wiki/skills/lattice-wiki/scripts/vendor/assets/CLAUDE.md.template` exits 0 (byte-identical).
    - `wc -l cores/prompt-sources/wiki-claude-md-template.md` reports 159 lines.
    - `sed -n '124p' cores/prompt-sources/wiki-claude-md-template.md` contains `## Log format`.
    - `sed -n '153p' cores/prompt-sources/wiki-claude-md-template.md` contains `## Style`.
    - `uv run --package graph-wiki-agent pytest agents/graph-wiki-agent/tests/prompts/test_provenance.py -x` exits 0 (existing tests still pass; this commit adds no fragments so the only check is that the existing fragments still resolve).
    - `grep -rn "^from deepagents" agents/ cores/ 2>/dev/null | wc -l` reports 0 (Scope Fence #1).
    - `git diff --stat HEAD | grep -E '^ (agents/|cores/subagent-runtime/)' | wc -l` reports 0 (no changes to agent code or subagent-runtime pool — Scope Fence #6).
  </acceptance_criteria>
  <done>The vendored template file exists at the canonical location, is byte-identical to the upstream source, has §Style on L153 and §Log format on L124 so Phase 10 fragment anchors will resolve correctly, and the existing test_provenance suite still passes.</done>
</task>

</tasks>

<verification>
After this plan lands:
1. `cores/prompt-sources/wiki-claude-md-template.md` exists and is byte-identical to upstream.
2. Existing `agents/graph-wiki-agent/tests/prompts/test_provenance.py` still passes (no fragments were added, so no new anchor resolution checks were introduced).
3. No source files under `agents/` or `cores/` (other than the new vendored file) are modified.
4. No `from deepagents` import was added anywhere.
</verification>

<success_criteria>
- The vendored file exists, byte-identical to upstream, with §Style and §Log format on the expected line numbers.
- `cores/prompt-sources/SOURCE-COMMIT` is unchanged.
- No code changes outside the vendored file.
- Provenance test passes.
- This plan unblocks plan 10-03 (style_rules, log_format fragments).
</success_criteria>

<output>
Create `.planning/phases/10-subagent-context-completion/10-01-SUMMARY.md` when done.
</output>
