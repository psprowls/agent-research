---
phase: 10-subagent-context-completion
plan: 07
type: execute
wave: 5
depends_on:
  - 10-05
  - 10-06
files_modified:
  - agents/graph-wiki-agent/tests/prompts/test_prompt_snapshots.py
  - agents/graph-wiki-agent/tests/prompts/test_token_budget.py
autonomous: false
requirements:
  - CTX-04
  - CTX-05
must_haves:
  truths:
    - "Snapshot tests exist for each of scanner/ingestor/linter-3-groups with project_context=<fixture> AND without project_context"
    - "A missing-CLAUDE.md degradation test asserts render_project_context returns empty string and build_*_system still produces a non-empty prompt"
    - "A token-budget test asserts each role's assembled prompt is within +1500 tokens of its recorded pre-Phase-10 baseline"
    - "The Phase 6 divergence eval has been re-run against the recorded baseline and shows no regression (or the developer has explicitly accepted the new baseline)"
  artifacts:
    - path: "agents/graph-wiki-agent/tests/prompts/test_prompt_snapshots.py"
      provides: "Additional snapshot tests for with-context and without-context per subagent + degradation test"
      contains: "test_scanner_system_with_project_context"
    - path: "agents/graph-wiki-agent/tests/prompts/test_token_budget.py"
      provides: "Token-budget regression test enforcing the +1500-tokens-per-role ceiling"
      contains: "PRE_PHASE_10_BASELINE"
  key_links:
    - from: "agents/graph-wiki-agent/tests/prompts/test_prompt_snapshots.py"
      to: "agents/graph-wiki-agent/src/graph_wiki_agent/prompts/project_context.py"
      via: "import render_project_context + tmp_path fixture with stub CLAUDE.md"
      pattern: "render_project_context"
    - from: "agents/graph-wiki-agent/tests/prompts/test_token_budget.py"
      to: "agents/graph-wiki-agent/src/graph_wiki_agent/prompts/scanner.py"
      via: "import build_scanner_system and assert len // 4 stays under baseline + 1500"
      pattern: "PRE_PHASE_10_BASELINE"
---

<objective>
Close Phase 10 with the verification harness: (1) snapshot tests on every wired subagent prompt covering both the with-project-context and without-project-context paths plus the missing-CLAUDE.md degradation case (CTX-04); (2) a token-budget regression test enforcing the +1,500-tokens-per-role ceiling (CTX-05); (3) re-run the Phase 6 divergence eval baseline (without `--accept-divergence-baseline`) and confirm no regression — or capture the developer's explicit acceptance if there is an intentional shift.

Purpose: Without (1) the wiring contract is unenforced — a future refactor could silently strip the project-context block. Without (2) the +1,500-token budget is unenforced — a future fragment-bloat could blow the cost budget. Without (3) we cannot claim the new context did not degrade output quality. CONTEXT.md LOCKED these three requirements.

This plan is `autonomous: false` because it ends with a `checkpoint:human-verify` gate on the divergence eval result — the developer either approves (no regression, ship it) or rejects (regression found, file follow-up).

Output: One modified test file (`test_prompt_snapshots.py`) plus one new test file (`test_token_budget.py`). Snapshot files under `__snapshots__/`. A documented Phase 6 divergence-eval result at the checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-subagent-context-completion/10-CONTEXT.md
@.planning/phases/10-subagent-context-completion/10-PATTERNS.md
@agents/graph-wiki-agent/tests/prompts/test_prompt_snapshots.py
@agents/graph-wiki-agent/tests/prompts/test_project_context.py

<interfaces>
Builder functions available after plan 10-05:
- `from graph_wiki_agent.prompts.scanner import build_scanner_system` — signature `(project_context: str = "") -> str`
- `from graph_wiki_agent.prompts.ingestor import build_ingestor_system` — signature `(project_context: str = "") -> str`
- `from graph_wiki_agent.prompts.linter import build_linter_page_quality_system, build_linter_adr_chain_system, build_linter_stale_claims_system` — signatures `(project_context: str = "") -> str`
- `from graph_wiki_agent.prompts.librarian import build_librarian_system` — signature `() -> str` (no project_context kwarg per CONTEXT.md §Wiring)
- `from graph_wiki_agent.prompts.project_context import render_project_context` — signature `(wiki_path: Path) -> str`

Existing test pattern in test_prompt_snapshots.py:
- The 7 existing snapshot tests use `try: from ... except ImportError: pytest.skip(...); assert X == snapshot`. New tests should follow the same pattern.

Phase 6 divergence eval entry point:
- Re-run via the same harness used in Phase 6. Check `.planning/phases/06-prompt-content-port-divergence-eval/` for the recorded baseline file (typically a JSON) and the test that exercises it. The default invocation should fail loud if divergence increases.
- The developer-acknowledgement flag is `--accept-divergence-baseline` (per ROADMAP.md Phase 6 Success Criteria 5).

Token-budget baseline values (per CONTEXT.md §Token budget):
- Measured via `len(prompt) / 4` rule-of-thumb tokenizer.
- The pre-Phase-10 baselines for each role need to be measured ONCE before Phase 10 wiring was applied. Since 10-05 has already shipped, the executor recovers these values from git: `git show <commit-before-10-05>:agents/graph-wiki-agent/src/graph_wiki_agent/prompts/scanner.py` etc. The executor records the integer floor of `len(constant) / 4` for each of scanner / ingestor / linter-3-groups / librarian as the baseline. The ceiling for the test is `baseline + 1500`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend test_prompt_snapshots.py with project-context-aware snapshot tests + degradation test</name>
  <read_first>
    - agents/graph-wiki-agent/tests/prompts/test_prompt_snapshots.py (existing 7 snapshot tests — pattern to extend)
    - agents/graph-wiki-agent/tests/prompts/test_project_context.py (existing fixture pattern from plan 10-04 — FIXTURE_CLAUDE_MD constant should be referenced or duplicated)
    - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/project_context.py (the render_project_context signature)
    - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/scanner.py (post-10-05 — build_scanner_system signature)
  </read_first>
  <files>agents/graph-wiki-agent/tests/prompts/test_prompt_snapshots.py</files>
  <action>
    Extend agents/graph-wiki-agent/tests/prompts/test_prompt_snapshots.py with the following ADDITIONAL tests (do NOT remove the existing 7 tests):

    Step 1: Add a module-level fixture constant `FIXTURE_CLAUDE_MD_FOR_SNAPSHOTS` near the top of the file. It must be a triple-quoted markdown string containing a minimal valid `<!-- lattice-wiki:layout:start -->` block (with two containers, e.g. `apps` and `cores`, with deterministic ordering) plus a `## Style` section with 2-3 bullets and a `## Log format` section. This can be a copy of the FIXTURE_CLAUDE_MD used in test_project_context.py — duplication is acceptable to keep each test module self-contained (per CONTEXT.md §Claude's Discretion on test placement), OR import the existing constant if test_project_context.py exposes it as a module-level name without underscore-prefix.

    Step 2: Add a private helper `_render_ctx_from_tmp(tmp_path) -> str` that creates `tmp_path / "wiki"`, writes FIXTURE_CLAUDE_MD_FOR_SNAPSHOTS into `wiki/CLAUDE.md`, calls `render_project_context(wiki)` and returns the result. This keeps the per-test setup short.

    Step 3: Add five new snapshot tests covering the with-context path. Each follows the existing pattern (try/except ImportError, pytest.skip, syrupy snapshot assertion). The tests are:
    - `test_scanner_system_with_project_context(snapshot, tmp_path)` — calls `build_scanner_system(project_context=_render_ctx_from_tmp(tmp_path))`, asserts equal to snapshot.
    - `test_ingestor_system_with_project_context(snapshot, tmp_path)` — same shape for build_ingestor_system.
    - `test_linter_page_quality_system_with_project_context(snapshot, tmp_path)` — same shape for build_linter_page_quality_system.
    - `test_linter_adr_chain_system_with_project_context(snapshot, tmp_path)` — same shape for build_linter_adr_chain_system.
    - `test_linter_stale_claims_system_with_project_context(snapshot, tmp_path)` — same shape for build_linter_stale_claims_system.

    Note: The librarian does NOT get a with-context variant (per CONTEXT.md §Wiring: librarian does not receive project_context). The existing `test_librarian_system_snapshot` covers it.

    Step 4: Add the degradation tests. These verify that on a vault with no CLAUDE.md or AGENTS.md, render_project_context returns the empty string AND every builder still produces a non-empty prompt without crashing. One consolidated test is sufficient:
    - `test_all_builders_degrade_without_project_context(tmp_path)` — call `render_project_context(tmp_path / "nonexistent")`, assert it equals `""`, then call each builder with `project_context=""` and assert each returns a non-empty string (this is the explicit missing-CLAUDE.md degradation contract).

    Step 5: Run pytest with `--snapshot-update` to record the new snapshots, then run again without to verify stability.

    The test file may grow from ~89 lines to ~180 lines. Do not delete or rename any existing test in this file.
  </action>
  <verify>
    <automated>uv run --package graph-wiki-agent pytest agents/graph-wiki-agent/tests/prompts/test_prompt_snapshots.py --snapshot-update -x &amp;&amp; uv run --package graph-wiki-agent pytest agents/graph-wiki-agent/tests/prompts/test_prompt_snapshots.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'def test_scanner_system_with_project_context' agents/graph-wiki-agent/tests/prompts/test_prompt_snapshots.py` reports 1.
    - `grep -c 'def test_ingestor_system_with_project_context' agents/graph-wiki-agent/tests/prompts/test_prompt_snapshots.py` reports 1.
    - `grep -c 'def test_linter_page_quality_system_with_project_context' agents/graph-wiki-agent/tests/prompts/test_prompt_snapshots.py` reports 1.
    - `grep -c 'def test_linter_adr_chain_system_with_project_context' agents/graph-wiki-agent/tests/prompts/test_prompt_snapshots.py` reports 1.
    - `grep -c 'def test_linter_stale_claims_system_with_project_context' agents/graph-wiki-agent/tests/prompts/test_prompt_snapshots.py` reports 1.
    - `grep -c 'def test_all_builders_degrade_without_project_context' agents/graph-wiki-agent/tests/prompts/test_prompt_snapshots.py` reports 1.
    - The existing 7 tests are still defined: `grep -cE '^def test_(librarian|ingestor|linter_page_quality|linter_adr_chain|linter_stale_claims|scanner|synthesizer|code_reader)_system_snapshot' agents/graph-wiki-agent/tests/prompts/test_prompt_snapshots.py` reports 8.
    - `uv run --package graph-wiki-agent pytest agents/graph-wiki-agent/tests/prompts/test_prompt_snapshots.py -x -v` exits 0 with the new tests PASSED and the existing 8 tests still PASSED.
    - Snapshot artifacts for the new tests exist under `agents/graph-wiki-agent/tests/prompts/__snapshots__/`.
    - The new with-context snapshots are visibly larger than the without-context snapshots: extract the assembled scanner string with and without context in a python -c invocation and assert `len(build_scanner_system(project_context=ctx)) > len(build_scanner_system())` is True.
  </acceptance_criteria>
  <done>Six new tests in test_prompt_snapshots.py (five with-context snapshots plus one degradation test). All tests pass on snapshot regeneration AND on a clean re-run.</done>
</task>

<task type="auto">
  <name>Task 2: Create test_token_budget.py enforcing the +1500 token ceiling per role</name>
  <read_first>
    - agents/graph-wiki-agent/tests/prompts/test_prompt_snapshots.py (the file-layout pattern; this new file lives alongside it)
    - .planning/phases/10-subagent-context-completion/10-CONTEXT.md §Token budget (LOCKED contract — +1500 tokens per role above the pre-Phase-10 baseline; len // 4 tokenizer)
    - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/scanner.py / ingestor.py / linter.py / librarian.py (post-10-05 state — for measuring current builder output)
  </read_first>
  <files>agents/graph-wiki-agent/tests/prompts/test_token_budget.py</files>
  <action>
    Create agents/graph-wiki-agent/tests/prompts/test_token_budget.py.

    Step 1: Determine the pre-Phase-10 baseline token count for each role. Since plan 10-05 has already shipped, recover the baseline values from git history. Run `git log --oneline -- agents/graph-wiki-agent/src/graph_wiki_agent/prompts/scanner.py` to find the last commit BEFORE plan 10-05's wiring landed. For each role, run a small one-off python snippet against that pre-10-05 file content (e.g., `git show <SHA>:agents/graph-wiki-agent/src/graph_wiki_agent/prompts/scanner.py > /tmp/old_scanner.py; python -c "exec(open('/tmp/old_scanner.py').read()); print(len(SCANNER_SYSTEM) // 4)"`) to compute the baseline integer for each of: scanner, ingestor, linter_page_quality, linter_adr_chain, linter_stale_claims, librarian. Record those six integers as a module-level dict named `PRE_PHASE_10_BASELINE` in the new test file. Include a comment block above the dict documenting which git SHA was used to derive the baseline and the exact command.

    Step 2: Set a module-level constant `TOKEN_CEILING_DELTA = 1500` per CONTEXT.md §Token budget LOCKED.

    Step 3: Define a private helper `_count_tokens(s: str) -> int` that returns `len(s) // 4` (the rule-of-thumb tokenizer mandated by CONTEXT.md).

    Step 4: Add six test functions, one per role:
    - `test_scanner_token_budget()` — imports `build_scanner_system` and asserts `_count_tokens(build_scanner_system(project_context="")) <= PRE_PHASE_10_BASELINE["scanner"] + TOKEN_CEILING_DELTA`. The assertion uses the empty-context build because the ceiling is for the role-shaped prompt floor; per-vault project_context is additive but capped by render_project_context's own size.
    - `test_ingestor_token_budget()` — same pattern with build_ingestor_system.
    - `test_linter_page_quality_token_budget()` — build_linter_page_quality_system.
    - `test_linter_adr_chain_token_budget()` — build_linter_adr_chain_system.
    - `test_linter_stale_claims_token_budget()` — build_linter_stale_claims_system.
    - `test_librarian_token_budget()` — build_librarian_system (no project_context kwarg).

    Step 5: Each test uses a try/except ImportError pattern with pytest.skip identical to the snapshot test pattern. Assertion failure message includes the role name, the measured token count, the baseline, and the ceiling so a regression is immediately diagnosable.

    Step 6: Run the full test file. All six tests must PASS at green commit. If any role's current builder exceeds baseline + 1500, the executor MUST stop and flag a token-budget violation — this is a hard CONTEXT.md LOCKED constraint. Reduction options: trim ARCHITECTURE_OVERVIEW further (still ≥ 400 tokens), or split out CLAUDE_MD_DISAMBIGUATION from a role that doesn't truly need it. Document the chosen reduction in the SUMMARY.
  </action>
  <verify>
    <automated>test -f agents/graph-wiki-agent/tests/prompts/test_token_budget.py &amp;&amp; uv run --package graph-wiki-agent pytest agents/graph-wiki-agent/tests/prompts/test_token_budget.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - File agents/graph-wiki-agent/tests/prompts/test_token_budget.py exists.
    - `grep -c 'PRE_PHASE_10_BASELINE = {' agents/graph-wiki-agent/tests/prompts/test_token_budget.py` reports 1.
    - `grep -c 'TOKEN_CEILING_DELTA = 1500' agents/graph-wiki-agent/tests/prompts/test_token_budget.py` reports 1.
    - Six test functions defined: scanner, ingestor, linter_page_quality, linter_adr_chain, linter_stale_claims, librarian. Verify with `grep -cE '^def test_(scanner|ingestor|linter_page_quality|linter_adr_chain|linter_stale_claims|librarian)_token_budget' agents/graph-wiki-agent/tests/prompts/test_token_budget.py` reports 6.
    - `uv run --package graph-wiki-agent pytest agents/graph-wiki-agent/tests/prompts/test_token_budget.py -x -v` exits 0 with all 6 tests PASSED.
    - The PRE_PHASE_10_BASELINE dict comment block names a specific git SHA used to derive the values (verified by `grep -E 'git (show|log).*[a-f0-9]{7,40}' agents/graph-wiki-agent/tests/prompts/test_token_budget.py | wc -l` reports at least 1).
    - No edits to any prompt or command file in this task: `git diff --name-only HEAD -- agents/graph-wiki-agent/src/graph_wiki_agent/prompts/ agents/graph-wiki-agent/src/graph_wiki_agent/commands/ | wc -l` reports 0. (If the token-budget test fails, the executor may need to trim a fragment — record that as a separate adjustment in the SUMMARY, but Task 2's primary deliverable is the test file alone.)
    - The full test suite for the phase passes: `uv run --package graph-wiki-agent pytest agents/graph-wiki-agent/tests/prompts/ -x` exits 0.
  </acceptance_criteria>
  <done>test_token_budget.py exists with the six tests, all PASSING. Baseline values are sourced from a specific pre-10-05 git SHA documented in the file. The +1500-token-per-role ceiling is enforced in CI.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Re-run Phase 6 divergence eval against the recorded baseline and confirm no regression</name>
  <what-built>
    All Phase 10 prompt-context wiring is in place: four new shared fragments (architecture_overview, style_rules, log_format, claude_md_disambiguation), the project_context.py renderer, prompt-builder refactors with project_context kwarg, and the command-side wiring. The Phase 6 divergence-detection eval was the gate the v1.1 prompt port shipped against. CTX-05 requires that Phase 10 not regress that baseline without an explicit developer acknowledgement.
  </what-built>
  <how-to-verify>
    1. Locate the Phase 6 divergence-eval invocation. Look for it in `.planning/phases/06-prompt-content-port-divergence-eval/06-11-PLAN.md` (the end-to-end eval-gated test plan) or via `grep -rn "accept-divergence-baseline" agents/ cores/ tests/`. The test typically lives under `cores/eval-harness/tests/` or `agents/graph-wiki-agent/tests/eval/`.

    2. Re-run the divergence eval WITHOUT the `--accept-divergence-baseline` flag against the current `HEAD` after plans 10-01..10-06 have shipped. Use `GRAPH_WIKI_RUN_EVAL=1` if the eval gate requires the live-Bedrock opt-in (see Phase 7 BED-01 pattern). A typical invocation is `GRAPH_WIKI_RUN_EVAL=1 uv run --package graph-wiki-agent pytest cores/eval-harness/tests/ -k divergence -x` — adjust the path/marker once you locate the test.

    3. Inspect the output:
       - If the test PASSES, divergence has not regressed against the recorded baseline. Approve.
       - If the test FAILS, capture the per-role divergence delta from the test output. If the delta is small and clearly attributable to legitimately added context (e.g., one fewer iron-rule omission because the new ARCHITECTURE_OVERVIEW grounds the model), the developer may explicitly approve and re-run with `--accept-divergence-baseline` to refresh the recorded baseline. If the delta is large or clearly a regression (more wikilink hallucinations, more wrong page_type routing), reject this checkpoint — file a follow-up task to investigate which fragment or wiring change caused the regression.

    4. Record the result and decision in the resume-signal message below.

    If the divergence eval gate is itself broken or unavailable (e.g., live Bedrock unreachable in this session), this checkpoint may be deferred — but the SUMMARY must document the deferral and a follow-up task must be filed in REQUIREMENTS.md before merging Phase 10.
  </how-to-verify>
  <resume-signal>Type "approved: no regression" if the divergence test passed. Type "approved: baseline refreshed" if the developer ran with --accept-divergence-baseline. Type "deferred: <reason>" if the eval is unrunnable in this session. Type "rejected: <details>" if a real regression was observed — this triggers a follow-up investigation before Phase 10 is considered complete.</resume-signal>
</task>

</tasks>

<verification>
1. New snapshot tests cover with-context and without-context paths for scanner, ingestor, linter (3 groups), plus a missing-CLAUDE.md degradation test — CTX-04 done.
2. New token-budget test enforces +1500-token-per-role ceiling on the six role-shaped prompts — CTX-05 first half done.
3. Phase 6 divergence eval re-run completed and result documented at the checkpoint — CTX-05 second half done.
4. All existing tests (test_provenance, test_project_context, the existing 8 snapshot tests in test_prompt_snapshots.py) still pass.
5. Scope fences: no deepagents, no pyproject.toml change, no pool.py change, no Phase-6 baseline regen unless the developer explicitly accepted it at the checkpoint.
</verification>

<success_criteria>
- 6 new tests in test_prompt_snapshots.py (5 with-context + 1 degradation) all pass.
- 6 new tests in test_token_budget.py all pass.
- Phase 6 divergence eval re-run result is recorded (approved / approved-refreshed / deferred / rejected).
- Phase 10 is end-to-end verified: CTX-01..CTX-05 are all satisfied.
</success_criteria>

<output>
Create `.planning/phases/10-subagent-context-completion/10-07-SUMMARY.md` when done. The SUMMARY should include the divergence-eval verdict captured at Task 3, plus the PRE_PHASE_10_BASELINE token counts for each role (so future phases have a documented reference point).
</output>
