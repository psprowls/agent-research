---
phase: 10-subagent-context-completion
plan: 03
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/style_rules.py
  - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/log_format.py
  - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/claude_md_disambiguation.py
autonomous: true
requirements:
  - CTX-01
must_haves:
  truths:
    - "Three new shared fragments (STYLE_RULES, LOG_FORMAT, CLAUDE_MD_DISAMBIGUATION) are importable from graph_wiki_agent.prompts._fragments"
    - "STYLE_RULES and LOG_FORMAT anchor to cores/prompt-sources/wiki-claude-md-template.md (the file vendored in plan 10-01)"
    - "CLAUDE_MD_DISAMBIGUATION anchors to cores/prompt-sources/SKILL.md L141 (cross-tool compatibility note)"
    - "All three pass test_provenance.py"
  artifacts:
    - path: "agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/style_rules.py"
      provides: "STYLE_RULES string constant (~150 tokens) anchored to vendored template §Style L153-L159"
      contains: "STYLE_RULES = \"\"\""
    - path: "agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/log_format.py"
      provides: "LOG_FORMAT string constant (~120 tokens) anchored to vendored template §Log format L124-L133"
      contains: "LOG_FORMAT = \"\"\""
    - path: "agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/claude_md_disambiguation.py"
      provides: "CLAUDE_MD_DISAMBIGUATION string constant (~80 tokens) anchored to SKILL.md L141"
      contains: "CLAUDE_MD_DISAMBIGUATION = \"\"\""
  key_links:
    - from: "agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/style_rules.py"
      to: "cores/prompt-sources/wiki-claude-md-template.md"
      via: "# Source: cores/prompt-sources/wiki-claude-md-template.md provenance header"
      pattern: "^# Source: cores/prompt-sources/wiki-claude-md-template.md$"
    - from: "agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/log_format.py"
      to: "cores/prompt-sources/wiki-claude-md-template.md"
      via: "# Source: cores/prompt-sources/wiki-claude-md-template.md provenance header"
      pattern: "^# Source: cores/prompt-sources/wiki-claude-md-template.md$"
    - from: "agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/claude_md_disambiguation.py"
      to: "cores/prompt-sources/SKILL.md"
      via: "# Source: cores/prompt-sources/SKILL.md provenance header"
      pattern: "^# Source: cores/prompt-sources/SKILL.md$"
---

<objective>
Add three small shared fragments under `prompts/_fragments/`: `style_rules.py`, `log_format.py`, `claude_md_disambiguation.py`. Combined they are ~350 tokens — small enough to ship in a single plan. All three carry the mandatory 3-line provenance header.

Purpose: Subagents currently lack project-style guidance (concise prose, cite aggressively), log-format awareness (the `## [YYYY-MM-DD] <op>` line format), and cross-tool disambiguation (root `CLAUDE.md` vs. wiki `CLAUDE.md`). These three fragments unblock the wiring in plan 10-05 (which prepends them to scanner / linter / ingestor / librarian).

Output: Three new `_fragments/*.py` modules, each pure data with a provenance header. No wiring in this plan.

Depends on plan 10-01 because `style_rules.py` and `log_format.py` anchor to `cores/prompt-sources/wiki-claude-md-template.md`, which 10-01 vendors. Without 10-01, `test_provenance.py::test_provenance_source_paths_resolve` would fail when this plan lands.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-subagent-context-completion/10-CONTEXT.md
@.planning/phases/10-subagent-context-completion/10-PATTERNS.md
@cores/prompt-sources/SOURCE-COMMIT
@cores/prompt-sources/wiki-claude-md-template.md
@cores/prompt-sources/SKILL.md

<interfaces>
<!-- Pattern to replicate exactly. -->

From agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/iron_rules.py (canonical analog for the file shape):
- 3-line provenance header followed by one blank line.
- Constant name in SCREAMING_SNAKE matching the file basename.
- `CONSTANT = """\` opening idiom so the first body line is not blank.

From cores/prompt-sources/wiki-claude-md-template.md (vendored by plan 10-01):
- §Log format starts at L124 with `## Log format` heading.
  L126-L129 contain the fenced log-line format and the "Valid ops" list.
  L133 has the `grep` example. The L124-L133 range covers the heading + fenced format + valid ops list + grep hint.
- §Style starts at L153 with `## Style` heading.
  L154-L158 are five `- ` bullet items (concise, short paragraphs, cite, when unsure, update updated:).
  L159 in the upstream is the trailing newline / next blank — the L153-L159 anchor range covers the heading + all five bullets.

From cores/prompt-sources/SKILL.md L141:
- One paragraph: `**Note:** your repo's root `CLAUDE.md` is separate from the wiki's `CLAUDE.md`. The root file defines the repo's build/style conventions; the wiki file defines how the vault is structured. Both are active simultaneously when working from the repo root.`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create _fragments/style_rules.py anchored to vendored template §Style L153-L159</name>
  <read_first>
    - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/iron_rules.py (exact analog — 15 lines; copy the shape)
    - cores/prompt-sources/wiki-claude-md-template.md (the source — read lines 150-160 to capture §Style verbatim or near-verbatim)
    - agents/graph-wiki-agent/tests/prompts/test_provenance.py (the invariant; PROMPT_SOURCES_DIR resolution check on lines 86-111)
  </read_first>
  <files>agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/style_rules.py</files>
  <action>
    Create agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/style_rules.py.

    Provenance header (lines 1-3, exact):
    - `# Source: cores/prompt-sources/wiki-claude-md-template.md`
    - `# Anchor: ## Style (L153-L159)`
    - `# Source-commit: ef05d991a9ab1ea12b1bc7ebc1fb20ba70074030`

    After one blank line, declare `STYLE_RULES = """\` followed by the rendered string. The rendered body must:
    1. Start with a `## Style` heading.
    2. Preserve all five bullets from the vendored template §Style (L154-L158) — these are load-bearing: concise / short-paragraphs+bullets / cite-with-wikilinks-and-code-paths / say-when-unsure / update-`updated:`-on-touch.
    3. Stay under ~200 tokens (target 150). Verbatim copy is acceptable.

    No imports beyond an optional `from __future__ import annotations`. No functions, no `__all__`.
  </action>
  <verify>
    <automated>uv run --package graph-wiki-agent python -c "from graph_wiki_agent.prompts._fragments.style_rules import STYLE_RULES; assert STYLE_RULES.startswith('## Style'); assert 'cite' in STYLE_RULES.lower(); assert 'updated' in STYLE_RULES.lower(); assert len(STYLE_RULES) // 4 &lt;= 200"</automated>
  </verify>
  <acceptance_criteria>
    - File agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/style_rules.py exists.
    - First three lines are exactly the provenance triple specified above.
    - `grep -c "^STYLE_RULES = \"\"\"" agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/style_rules.py` reports 1.
    - `STYLE_RULES.startswith("## Style")` is True.
    - `len(STYLE_RULES) // 4 &lt;= 200` (~150 token target).
    - The five load-bearing bullets are present (substring checks: "concise", "cite", "wikilinks" or "code-paths", "not sure" or "unsure" or "invent", "updated").
  </acceptance_criteria>
  <done>style_rules.py exists with correct provenance, contains all five load-bearing style bullets, stays under 200 tokens, imports cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Create _fragments/log_format.py anchored to vendored template §Log format L124-L133</name>
  <read_first>
    - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/iron_rules.py (analog)
    - cores/prompt-sources/wiki-claude-md-template.md (lines 120-135 — captures §Log format)
  </read_first>
  <files>agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/log_format.py</files>
  <action>
    Create agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/log_format.py.

    Provenance header (lines 1-3, exact):
    - `# Source: cores/prompt-sources/wiki-claude-md-template.md`
    - `# Anchor: ## Log format (L124-L133)`
    - `# Source-commit: ef05d991a9ab1ea12b1bc7ebc1fb20ba70074030`

    After one blank line, declare `LOG_FORMAT = """\` followed by the rendered string. The rendered body must:
    1. Start with a `## Log format` heading.
    2. Preserve the fenced log-line format from L126-L129 of the vendored template (the `## [YYYY-MM-DD] <op> | <title>` line plus the `<optional detail …>` line, inside a triple-backtick fenced block).
    3. Preserve the "Valid ops" sentence from L131 of the vendored template, listing all eight ops: scan, ingest, query, lint, create, update, delete, note.
    4. Stay under ~150 tokens (target 120). Verbatim is acceptable; the grep hint on L133 is optional.

    No imports beyond optional future-annotations.
  </action>
  <verify>
    <automated>uv run --package graph-wiki-agent python -c "from graph_wiki_agent.prompts._fragments.log_format import LOG_FORMAT; assert LOG_FORMAT.startswith('## Log format'); assert '[YYYY-MM-DD]' in LOG_FORMAT; assert all(op in LOG_FORMAT for op in ['scan','ingest','query','lint','create','update','delete','note']); assert len(LOG_FORMAT) // 4 &lt;= 150"</automated>
  </verify>
  <acceptance_criteria>
    - File agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/log_format.py exists.
    - First three lines are exactly the provenance triple specified above.
    - `grep -c "^LOG_FORMAT = \"\"\"" agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/log_format.py` reports 1.
    - `LOG_FORMAT.startswith("## Log format")` is True.
    - String contains `[YYYY-MM-DD]` literal.
    - String contains all eight valid ops as substrings: scan, ingest, query, lint, create, update, delete, note.
    - `len(LOG_FORMAT) // 4 &lt;= 150`.
  </acceptance_criteria>
  <done>log_format.py exists with correct provenance, contains the log-line format and all eight valid ops, stays under 150 tokens.</done>
</task>

<task type="auto">
  <name>Task 3: Create _fragments/claude_md_disambiguation.py anchored to SKILL.md L141</name>
  <read_first>
    - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/page_categories.py (PATTERNS.md names this as the analog — same 4-line preamble shape)
    - cores/prompt-sources/SKILL.md (line 141 — the single paragraph being extracted)
  </read_first>
  <files>agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/claude_md_disambiguation.py</files>
  <action>
    Create agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/claude_md_disambiguation.py.

    Provenance header (lines 1-3, exact):
    - `# Source: cores/prompt-sources/SKILL.md`
    - `# Anchor: ## Cross-tool compatibility (L141)`
    - `# Source-commit: ef05d991a9ab1ea12b1bc7ebc1fb20ba70074030`

    After one blank line, declare `CLAUDE_MD_DISAMBIGUATION = """\` followed by a compact rewrite of SKILL.md L141. The rendered body must:
    1. Convey the load-bearing distinction: the repo's root `CLAUDE.md` (build/style conventions) is separate from the wiki's `CLAUDE.md` (vault schema), and both are active simultaneously when working from the repo root.
    2. Stay under ~120 tokens (target 80). Verbatim copy of the source paragraph is acceptable; a heading is not required (SKILL.md L141 is body text, not a section heading).

    No imports beyond optional future-annotations.
  </action>
  <verify>
    <automated>uv run --package graph-wiki-agent python -c "from graph_wiki_agent.prompts._fragments.claude_md_disambiguation import CLAUDE_MD_DISAMBIGUATION; assert 'root' in CLAUDE_MD_DISAMBIGUATION.lower() and 'wiki' in CLAUDE_MD_DISAMBIGUATION.lower(); assert 'CLAUDE.md' in CLAUDE_MD_DISAMBIGUATION; assert len(CLAUDE_MD_DISAMBIGUATION) // 4 &lt;= 120"</automated>
  </verify>
  <acceptance_criteria>
    - File agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/claude_md_disambiguation.py exists.
    - First three lines are exactly the provenance triple specified above.
    - `grep -c "^CLAUDE_MD_DISAMBIGUATION = \"\"\"" agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/claude_md_disambiguation.py` reports 1.
    - String contains both "root" and "wiki" (case-insensitive) and the literal `CLAUDE.md`.
    - `len(CLAUDE_MD_DISAMBIGUATION) // 4 &lt;= 120`.
  </acceptance_criteria>
  <done>claude_md_disambiguation.py exists with correct provenance, conveys the root-vs-wiki distinction, stays under 120 tokens.</done>
</task>

<task type="auto">
  <name>Task 4: Run test_provenance against all three new fragments</name>
  <read_first>
    - agents/graph-wiki-agent/tests/prompts/test_provenance.py (the test that must pass for both `test_all_fragments_have_provenance_header` and `test_provenance_source_paths_resolve`)
  </read_first>
  <files>(no file changes — verification only)</files>
  <action>
    Run the provenance test suite against the three new fragments plus all pre-existing ones. The combined run must pass. If `test_provenance_source_paths_resolve` fails for `style_rules.py` or `log_format.py`, the most likely cause is that plan 10-01 did not land or the vendored template is at the wrong path — re-check `cores/prompt-sources/wiki-claude-md-template.md` exists before troubleshooting the fragment files.
  </action>
  <verify>
    <automated>uv run --package graph-wiki-agent pytest agents/graph-wiki-agent/tests/prompts/test_provenance.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --package graph-wiki-agent pytest agents/graph-wiki-agent/tests/prompts/test_provenance.py -x -v` exits 0.
    - Test output reports both `test_all_fragments_have_provenance_header` PASSED and `test_provenance_source_paths_resolve` PASSED (not skipped — the new fragments are now present so the skip branch should not trigger).
    - The three new fragment files appear in the pytest stdout / collected file set (indirect check: total fragment count goes up by 3 from the pre-plan baseline).
  </acceptance_criteria>
  <done>All four fragment files (architecture_overview from plan 10-02, style_rules, log_format, claude_md_disambiguation from this plan) pass the provenance gate. The scope fence on `pool.py` and deepagents is observed (no edits to those files).</done>
</task>

</tasks>

<verification>
1. Three new files under `_fragments/` exist with provenance headers.
2. Token budgets respected: style ≤200, log ≤150, disambiguation ≤120 (combined ≤350 tokens).
3. test_provenance.py passes (both tests, no skips for fragment-presence reasons).
4. No wiring into any prompt builder yet — that is plan 10-05.
</verification>

<success_criteria>
- All three fragments import successfully.
- All three carry the mandatory provenance header pointing to a file under `cores/prompt-sources/` that exists on disk.
- Combined added bytes/tokens for this plan ≤350 tokens.
- Scope fences honored: no deepagents import, no pyproject.toml change, no `subagent-runtime/pool.py` edit.
- This plan unblocks 10-05 wiring.
</success_criteria>

<output>
Create `.planning/phases/10-subagent-context-completion/10-03-SUMMARY.md` when done.
</output>
