---
phase: 06-prompt-content-port-divergence-eval
plan: 04
type: execute
wave: 3
depends_on: [06-03]
files_modified:
  - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/librarian.py
  - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/synthesizer.py
  - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/code_reader.py
  - agents/graph-wiki-agent/src/graph_wiki_agent/commands/query.py
autonomous: true
requirements: [PORT-02]
tags: [prompt-port, librarian, synthesizer, code-reader, query-command]

must_haves:
  truths:
    - "LIBRARIAN_SYSTEM is composed from shared fragments + librarian-local prose at import time"
    - "SYNTHESIZER_SYSTEM and CODE_READER_SYSTEM are relocated verbatim from commands/query.py with no content change (D-14)"
    - "commands/query.py imports all three constants from graph_wiki_agent.prompts.* instead of defining them inline"
    - "Existing query.py behavior (SystemMessage(content=LIBRARIAN_SYSTEM) call sites) is unchanged"
    - "LIBRARIAN_SYSTEM preserves the existing NO_RELEVANT_CONTENT sentinel contract and incorporates the canonical iron rules + citation rules + librarian red flags"
  artifacts:
    - path: "agents/graph-wiki-agent/src/graph_wiki_agent/prompts/librarian.py"
      provides: "LIBRARIAN_SYSTEM composed string"
      contains: "LIBRARIAN_SYSTEM"
    - path: "agents/graph-wiki-agent/src/graph_wiki_agent/prompts/synthesizer.py"
      provides: "SYNTHESIZER_SYSTEM (relocated verbatim, no content port)"
      contains: "SYNTHESIZER_SYSTEM"
    - path: "agents/graph-wiki-agent/src/graph_wiki_agent/prompts/code_reader.py"
      provides: "CODE_READER_SYSTEM (relocated verbatim, no content port)"
      contains: "CODE_READER_SYSTEM"
    - path: "agents/graph-wiki-agent/src/graph_wiki_agent/commands/query.py"
      provides: "Query command with import-based prompt sourcing (no inline *_SYSTEM constants)"
  key_links:
    - from: "prompts/librarian.py"
      to: "_fragments/{iron_rules, page_categories, citation_rules}"
      via: "Python imports at module top"
      pattern: "from graph_wiki_agent.prompts._fragments"
    - from: "commands/query.py"
      to: "prompts/{librarian, synthesizer, code_reader}"
      via: "Python imports replacing inline constants"
      pattern: "from graph_wiki_agent.prompts"
---

<objective>
Port the librarian system prompt content from `cores/prompt-sources/agents/librarian.md` (plus shared fragments) into `prompts/librarian.py`, and relocate `SYNTHESIZER_SYSTEM` and `CODE_READER_SYSTEM` verbatim out of `commands/query.py` into `prompts/synthesizer.py` and `prompts/code_reader.py` per D-14. Then swap the inline constants in `commands/query.py` for imports.

Purpose: Completes PORT-02 (librarian content port) and the D-14 relocation of synthesizer + code_reader for module uniformity. Adapts host-specific librarian references per RESEARCH §Adaptation Map while preserving the existing `NO_RELEVANT_CONTENT` sentinel contract that downstream code relies on.

Output: Three new prompt files + a refactored query.py importing them. Snapshot tests for all three constants are recorded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-prompt-content-port-divergence-eval/06-CONTEXT.md
@.planning/phases/06-prompt-content-port-divergence-eval/06-RESEARCH.md
@.planning/phases/06-prompt-content-port-divergence-eval/06-PATTERNS.md
@cores/prompt-sources/agents/librarian.md
@cores/prompt-sources/SKILL.md
@agents/graph-wiki-agent/src/graph_wiki_agent/commands/query.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create prompts/synthesizer.py and prompts/code_reader.py (verbatim relocation)</name>
  <files>
    agents/graph-wiki-agent/src/graph_wiki_agent/prompts/synthesizer.py,
    agents/graph-wiki-agent/src/graph_wiki_agent/prompts/code_reader.py
  </files>
  <read_first>
    - agents/graph-wiki-agent/src/graph_wiki_agent/commands/query.py (lines 150-179 — current SYNTHESIZER_SYSTEM and CODE_READER_SYSTEM definitions)
    - .planning/phases/06-prompt-content-port-divergence-eval/06-PATTERNS.md §`prompts/synthesizer.py` and §`prompts/code_reader.py`
    - .planning/phases/06-prompt-content-port-divergence-eval/06-CONTEXT.md §D-14
  </read_first>
  <action>
    Per D-14, these are relocations only — no content port, no provenance header (no canonical lattice source exists for them yet). Each file: `from __future__ import annotations` line 1, optional one-line docstring, then the single constant assignment.

    `prompts/synthesizer.py`: Copy the current `SYNTHESIZER_SYSTEM = """..."""` block from `commands/query.py` (lines ~150-163) verbatim. One export: `SYNTHESIZER_SYSTEM`.

    `prompts/code_reader.py`: Copy the current `CODE_READER_SYSTEM = """..."""` block from `commands/query.py` (lines ~165-179) verbatim. One export: `CODE_READER_SYSTEM`.

    No edits to the string content. No fragment composition. These exist for module-layout uniformity (D-14) so all role prompts live in the same place.
  </action>
  <verify>
    <automated>uv run python -c "from graph_wiki_agent.prompts.synthesizer import SYNTHESIZER_SYSTEM; from graph_wiki_agent.prompts.code_reader import CODE_READER_SYSTEM; from graph_wiki_agent.commands.query import SYNTHESIZER_SYSTEM as Q_SYN, CODE_READER_SYSTEM as Q_CR; assert SYNTHESIZER_SYSTEM == Q_SYN, 'synthesizer drift'; assert CODE_READER_SYSTEM == Q_CR, 'code_reader drift'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `prompts/synthesizer.py` exists and exports `SYNTHESIZER_SYSTEM` byte-identical to the current `commands/query.py` definition.
    - `prompts/code_reader.py` exists and exports `CODE_READER_SYSTEM` byte-identical to the current `commands/query.py` definition.
    - No provenance header on either file (no canonical lattice source per D-14).
    - Both files importable from `graph_wiki_agent.prompts.*`.
  </acceptance_criteria>
  <done>
    Both files exist; constants are byte-identical to the current inline `commands/query.py` definitions; verify command above passes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create prompts/librarian.py composing fragments + role-local content (PORT-02)</name>
  <files>
    agents/graph-wiki-agent/src/graph_wiki_agent/prompts/librarian.py
  </files>
  <read_first>
    - cores/prompt-sources/agents/librarian.md (full file — workflow, rules, red flags)
    - cores/prompt-sources/SKILL.md (iron rules and page categories for context — already in fragments)
    - agents/graph-wiki-agent/src/graph_wiki_agent/commands/query.py (lines 137-148 — existing LIBRARIAN_SYSTEM constant that MUST be a strict superset semantically)
    - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/iron_rules.py (the fragment landed in 06-03)
    - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/page_categories.py
    - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/citation_rules.py
    - .planning/phases/06-prompt-content-port-divergence-eval/06-PATTERNS.md §`prompts/librarian.py`
    - .planning/phases/06-prompt-content-port-divergence-eval/06-RESEARCH.md §Source File Content Analysis -> Librarian-local
    - .planning/phases/06-prompt-content-port-divergence-eval/06-RESEARCH.md §Adaptation Map
    - agents/graph-wiki-agent/tests/prompts/test_prompt_snapshots.py (the snapshot test scaffolding from 06-02)
  </read_first>
  <behavior>
    - LIBRARIAN_SYSTEM is built at import time via `"\n\n".join([...])` (no runtime templating per D-02)
    - LIBRARIAN_SYSTEM contains the substring `NO_RELEVANT_CONTENT` (sentinel contract is preserved)
    - LIBRARIAN_SYSTEM contains the substring from IRON_RULES (e.g., "Iron rules"), substring from PAGE_CATEGORIES (e.g., "page categories" or "package" or category names), substring from CITATION_RULES (e.g., "wikilink" or "[[")
    - LIBRARIAN_SYSTEM does NOT contain `${CLAUDE_PLUGIN_ROOT}`, `/lattice-wiki:`, `obsidian-markdown`, `Offer to file` — all per RESEARCH §Adaptation Map
    - Snapshot test `test_librarian_system_snapshot` (in 06-02 scaffold) records on first run
  </behavior>
  <action>
    Create `prompts/librarian.py`: `from __future__ import annotations` on line 1, optional module docstring. Imports:
      `from graph_wiki_agent.prompts._fragments.iron_rules import IRON_RULES`
      `from graph_wiki_agent.prompts._fragments.page_categories import PAGE_CATEGORIES`
      `from graph_wiki_agent.prompts._fragments.citation_rules import CITATION_RULES`

    Build `LIBRARIAN_SYSTEM = "\n\n".join([ ... ])` per PATTERNS.md §`prompts/librarian.py`. Sections in order:
      1. Role intro paragraph (role-local, adapted from `librarian.md` §Role). Must preserve the existing `commands/query.py` LIBRARIAN_SYSTEM intro behavior: "wiki librarian", "extract every passage from the page that is directly relevant to the query", quote verbatim, never invent paths/symbols.
      2. `IRON_RULES` fragment.
      3. `PAGE_CATEGORIES` fragment.
      4. `CITATION_RULES` fragment.
      5. Workflow section (role-local, adapted from `librarian.md` §Workflow). REMOVE references to `${CLAUDE_PLUGIN_ROOT}/.../wiki_search.py` (search results are provided by the harness — per RESEARCH §Adaptation Map). REMOVE "offer to file back" / "vote to file" interactive patterns (RESEARCH §Adaptation Map). KEEP: "Read the index first", drill-3-to-10-pages guidance, "follow wikilinks", "fall back to code", synthesize-from-supplied-content rule.
      6. Red flags section (role-local, adapted from `librarian.md` §Red flags — 4 items, all portable verbatim per RESEARCH).
      7. Output format paragraph (role-local). MUST preserve: the `NO_RELEVANT_CONTENT` sentinel contract from current `commands/query.py` LIBRARIAN_SYSTEM (line ~144); this is consumed by downstream synthesis code and cannot drift. Either include the sentinel paragraph verbatim from the existing constant, or rewrite ensuring the exact substring `NO_RELEVANT_CONTENT` appears with the same semantics ("respond with exactly the sentinel string `NO_RELEVANT_CONTENT` and nothing else when the page contains no relevant passage").

    Token budget: target <400 tokens for the composed prompt per RESEARCH §Pitfall 1.

    After writing, run `pytest agents/graph-wiki-agent/tests/prompts/test_prompt_snapshots.py::test_librarian_system_snapshot --snapshot-update -x` once to record the initial snapshot. Subsequent runs must pass without `--snapshot-update`.
  </action>
  <verify>
    <automated>uv run python -c "from graph_wiki_agent.prompts.librarian import LIBRARIAN_SYSTEM; assert 'NO_RELEVANT_CONTENT' in LIBRARIAN_SYSTEM; assert 'Iron rules' in LIBRARIAN_SYSTEM; assert 'wikilink' in LIBRARIAN_SYSTEM.lower(); assert '\${CLAUDE_PLUGIN_ROOT}' not in LIBRARIAN_SYSTEM; assert '/lattice-wiki:' not in LIBRARIAN_SYSTEM; print('OK len=', len(LIBRARIAN_SYSTEM))" && uv run pytest agents/graph-wiki-agent/tests/prompts/test_prompt_snapshots.py::test_librarian_system_snapshot -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `prompts/librarian.py` importable; exports `LIBRARIAN_SYSTEM` as a non-empty string.
    - `LIBRARIAN_SYSTEM` is built via `"\n\n".join([...])` at import time (no runtime template substitution).
    - Contains substrings: `NO_RELEVANT_CONTENT`, `Iron rules`, at least one page-category name from PAGE_CATEGORIES, and `wikilink` or `[[`.
    - Does NOT contain: `${CLAUDE_PLUGIN_ROOT}`, `/lattice-wiki:`, `obsidian-markdown`, `Offer to file`, `vote to file`.
    - Snapshot test `test_librarian_system_snapshot` passes (after one-time `--snapshot-update`).
    - Total composed length < 4000 characters (proxy for token budget — RESEARCH §Pitfall 1).
  </acceptance_criteria>
  <done>
    `prompts/librarian.py` composes from fragments + librarian-local prose; snapshot recorded and re-run passes; adaptation map honored (no host-specific references remain); NO_RELEVANT_CONTENT sentinel preserved.
  </done>
</task>

<task type="auto">
  <name>Task 3: Swap inline constants in commands/query.py for imports from prompts/</name>
  <files>
    agents/graph-wiki-agent/src/graph_wiki_agent/commands/query.py
  </files>
  <read_first>
    - agents/graph-wiki-agent/src/graph_wiki_agent/commands/query.py (full file — note the module docstring at top, the call sites that use each constant)
    - .planning/phases/06-prompt-content-port-divergence-eval/06-PATTERNS.md §`commands/query.py (modify — replace inline with import)`
    - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/librarian.py (just created)
    - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/synthesizer.py (just created)
    - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/code_reader.py (just created)
    - agents/graph-wiki-agent/tests/commands/ (or tests/unit/test_commands_query.py if it exists — to know what tests must still pass)
  </read_first>
  <action>
    In `commands/query.py`, delete the three inline `*_SYSTEM = """..."""` blocks (lines ~136-179 — verify by reading the file). Replace with three import lines near the top of the file (after stdlib/third-party imports, with the other internal imports):

      `from graph_wiki_agent.prompts.librarian import LIBRARIAN_SYSTEM  # noqa: F401`
      `from graph_wiki_agent.prompts.synthesizer import SYNTHESIZER_SYSTEM  # noqa: F401`
      `from graph_wiki_agent.prompts.code_reader import CODE_READER_SYSTEM  # noqa: F401`

    The `noqa: F401` is required because the module docstring lists these as part of the public API even though the names are re-exported via star-or-explicit imports rather than used directly inside this file.

    If the module docstring at the top of `commands/query.py` lists a "Public API" section that mentions these constants, update those lines to note "(re-exported from `graph_wiki_agent.prompts.*`)". Do NOT change anything else in query.py — no behavior change, no refactor of call sites. All `SystemMessage(content=LIBRARIAN_SYSTEM)` references remain identical.

    Run the full graph-wiki-agent test suite to confirm no regressions.
  </action>
  <verify>
    <automated>uv run pytest agents/graph-wiki-agent/tests/ -x -q -k 'query or commands' 2>&1 | tail -20 && uv run python -c "from graph_wiki_agent.commands.query import LIBRARIAN_SYSTEM, SYNTHESIZER_SYSTEM, CODE_READER_SYSTEM; from graph_wiki_agent.prompts.librarian import LIBRARIAN_SYSTEM as P_LIB; assert LIBRARIAN_SYSTEM is P_LIB, 'commands.query.LIBRARIAN_SYSTEM must be the same object as prompts.librarian.LIBRARIAN_SYSTEM'; print('OK')" && grep -E '^(LIBRARIAN_SYSTEM|SYNTHESIZER_SYSTEM|CODE_READER_SYSTEM)\s*=' agents/graph-wiki-agent/src/graph_wiki_agent/commands/query.py; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - `commands/query.py` no longer defines `LIBRARIAN_SYSTEM`, `SYNTHESIZER_SYSTEM`, `CODE_READER_SYSTEM` as inline string literals (the `grep ^LIBRARIAN_SYSTEM\s*=` etc. returns no matches in that file).
    - The three names are imported from `graph_wiki_agent.prompts.*` and are re-exposed from `commands.query` for backward compatibility.
    - `commands.query.LIBRARIAN_SYSTEM is prompts.librarian.LIBRARIAN_SYSTEM` (Python identity — proves import wiring, not duplication).
    - Existing tests under `agents/graph-wiki-agent/tests/` that touch the query command continue to pass.
    - Behavior is unchanged: no call site or runtime logic in `query.py` is modified.
  </acceptance_criteria>
  <done>
    Inline constants deleted; imports in place; query-related tests green; identity check passes proving no duplication.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Prompt source -> LLM input | Prompts assembled at import time as immutable strings; no user-injected content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | Tampering | NO_RELEVANT_CONTENT sentinel contract drift | mitigate | Task action explicitly requires the substring `NO_RELEVANT_CONTENT` and downstream consumers in `commands/query.py` continue to compare to that literal; snapshot test freezes the composed prompt |
| T-06-08 | Information Disclosure | Host-specific references leaking into composed prompt | mitigate | Acceptance criteria assert absence of `${CLAUDE_PLUGIN_ROOT}`, `/lattice-wiki:`, `Offer to file` substrings per RESEARCH §Adaptation Map |
</threat_model>

<verification>
- `pytest agents/graph-wiki-agent/tests/prompts/test_prompt_snapshots.py::test_librarian_system_snapshot -x` passes
- `commands/query.py` no longer defines the three constants inline
- `commands.query.LIBRARIAN_SYSTEM is prompts.librarian.LIBRARIAN_SYSTEM` (identity)
- Existing query-related tests still pass
</verification>

<success_criteria>
- PORT-02 delivered: librarian prompt composed from canonical fragments + adapted role-local prose
- D-14 delivered: synthesizer + code_reader relocated for module uniformity (no content port)
- query.py refactored to import-based prompt sourcing
- All adaptation-map host-specific references removed from composed prompt
</success_criteria>

<output>
Create `.planning/phases/06-prompt-content-port-divergence-eval/06-04-SUMMARY.md` when done.
</output>
