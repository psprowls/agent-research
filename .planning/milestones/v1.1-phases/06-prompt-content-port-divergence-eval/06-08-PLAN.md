---
phase: 06-prompt-content-port-divergence-eval
plan: 08
type: execute
wave: 4
depends_on: [06-02, 06-04, 06-05, 06-06, 06-07]
files_modified:
  - cores/eval-harness/src/eval_harness/divergence/__init__.py
  - cores/eval-harness/src/eval_harness/divergence/check.py
  - cores/eval-harness/src/eval_harness/divergence/librarian.py
  - cores/eval-harness/src/eval_harness/divergence/ingestor.py
  - cores/eval-harness/src/eval_harness/divergence/linter.py
  - cores/eval-harness/src/eval_harness/divergence/scanner.py
  - cores/eval-harness/src/eval_harness/divergence/rubrics/librarian.md
  - cores/eval-harness/src/eval_harness/divergence/rubrics/ingestor.md
  - cores/eval-harness/src/eval_harness/divergence/rubrics/linter.md
  - cores/eval-harness/src/eval_harness/divergence/rubrics/scanner.md
  - cores/eval-harness/tests/test_divergence_checks.py
autonomous: true
requirements: [EVAL-11]
tags: [divergence-eval, programmatic-checks, rubrics, judge-rubrics]

must_haves:
  truths:
    - "DivergenceCheck dataclass exists with the locked schema from D-08 (id, source_anchor, severity, check)"
    - "Verdict NamedTuple exists with (passed: bool, excerpt: str)"
    - "AgentOutputProxy dataclass exists with (answer: str, page_type: str = '')"
    - "Each of the 4 role modules exports {ROLE_PREFIX}_CHECKS: list[DivergenceCheck] with the rule IDs from RESEARCH §Divergence Check Inventory"
    - "Each rule's check callable returns Verdict(passed=True) on valid output and Verdict(passed=False, excerpt=...) on violation"
    - "Per-role rubric .md files exist with provenance comment headers pointing at source anchors (D-10)"
    - "Unit tests for at least one passing + one failing case per programmatic rule pass under uv run pytest"
  artifacts:
    - path: "cores/eval-harness/src/eval_harness/divergence/__init__.py"
      provides: "Subpackage marker; exports ROLE_CHECKS dict mapping role name to its checks list"
    - path: "cores/eval-harness/src/eval_harness/divergence/check.py"
      provides: "DivergenceCheck dataclass, Verdict NamedTuple, AgentOutputProxy dataclass"
      contains: "@dataclass\\nclass DivergenceCheck"
    - path: "cores/eval-harness/src/eval_harness/divergence/librarian.py"
      provides: "LIBRARIAN_CHECKS list with LIB-001..LIB-004 programmatic checks"
      contains: "LIBRARIAN_CHECKS"
    - path: "cores/eval-harness/src/eval_harness/divergence/ingestor.py"
      provides: "INGESTOR_CHECKS list with ING-001..ING-004 programmatic checks"
      contains: "INGESTOR_CHECKS"
    - path: "cores/eval-harness/src/eval_harness/divergence/linter.py"
      provides: "LINTER_CHECKS list with LNT-001..LNT-003 programmatic checks"
      contains: "LINTER_CHECKS"
    - path: "cores/eval-harness/src/eval_harness/divergence/scanner.py"
      provides: "SCANNER_CHECKS list with SCN-001..SCN-004 programmatic checks"
      contains: "SCANNER_CHECKS"
    - path: "cores/eval-harness/src/eval_harness/divergence/rubrics/{librarian,ingestor,linter,scanner}.md"
      provides: "Per-role LLM-judge rubric files with provenance headers"
  key_links:
    - from: "divergence/librarian.py"
      to: "eval_harness.structural._resolve_citation"
      via: "private function reuse (per PATTERNS §`from eval_harness.structural import _resolve_citation`)"
      pattern: "from eval_harness.structural import _resolve_citation"
    - from: "rubrics/*.md"
      to: "cores/prompt-sources/agents/*.md and SKILL.md"
      via: "provenance comment header at top of each rubric"
      pattern: "^# Source: cores/prompt-sources/"
---

<objective>
Build the divergence-detection rule infrastructure: dataclasses (DivergenceCheck, Verdict, AgentOutputProxy), per-role check modules (4 files with rule lists), per-role LLM-judge rubric files (4 .md files), and unit tests covering each programmatic check.

Purpose: Delivers EVAL-11 (the divergence metric flags violations of skill-content expectations). The check callables and rubrics are the substrate the metric class in 06-09 wraps and the baseline gate in 06-10 reads.

Output: Eight Python files (1 check.py + 4 role modules + 4 rubric markdown files + __init__.py) plus expanded `test_divergence_checks.py` unit tests covering each rule.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-prompt-content-port-divergence-eval/06-CONTEXT.md
@.planning/phases/06-prompt-content-port-divergence-eval/06-RESEARCH.md
@.planning/phases/06-prompt-content-port-divergence-eval/06-PATTERNS.md
@cores/eval-harness/src/eval_harness/structural.py
@cores/eval-harness/src/eval_harness/__init__.py
@cores/eval-harness/tests/test_structural.py
@cores/eval-harness/tests/conftest.py
@cores/prompt-sources/SKILL.md
@cores/prompt-sources/agents/librarian.md
@cores/prompt-sources/agents/ingestor.md
@cores/prompt-sources/agents/linter.md
@cores/prompt-sources/agents/scanner.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create divergence/check.py with dataclasses + divergence/__init__.py</name>
  <files>
    cores/eval-harness/src/eval_harness/divergence/__init__.py,
    cores/eval-harness/src/eval_harness/divergence/check.py
  </files>
  <read_first>
    - cores/eval-harness/src/eval_harness/structural.py (lines 1-30 — module structure, imports, regex constants)
    - cores/eval-harness/src/eval_harness/__init__.py (sibling package init style)
    - .planning/phases/06-prompt-content-port-divergence-eval/06-PATTERNS.md (section: divergence/check.py)
    - .planning/phases/06-prompt-content-port-divergence-eval/06-CONTEXT.md (D-08 schema lock)
  </read_first>
  <behavior>
    - `DivergenceCheck` is a `@dataclass` with fields exactly per D-08: `id: str`, `source_anchor: str`, `severity: str`, `check: Callable[["AgentOutputProxy", Path], Verdict]`
    - `Verdict` is a `NamedTuple` with fields `passed: bool` and `excerpt: str`
    - `AgentOutputProxy` is a `@dataclass` with `answer: str` and `page_type: str = ""` (default for non-ingestor roles)
    - `severity` is validated only at use sites (no runtime enum) but the docstring documents the two valid values: `"hard"`, `"soft"`
    - All three types are importable: `from eval_harness.divergence.check import DivergenceCheck, Verdict, AgentOutputProxy`
  </behavior>
  <action>
    Create `cores/eval-harness/src/eval_harness/divergence/__init__.py`: empty file initially (will be populated in Task 6 with `ROLE_CHECKS` dict for re-export convenience).

    Create `cores/eval-harness/src/eval_harness/divergence/check.py`: `from __future__ import annotations` line 1, then a module docstring (one-line summary + "Implements DivergenceCheck dataclass schema locked in CONTEXT.md D-08. Used by per-role rule modules (librarian.py, ingestor.py, linter.py, scanner.py) and DivergenceMetric (metric.py)." plus a Security note: "Inputs to check callables are LLM-generated text; checks must not eval/exec the input.").

    Imports: `from dataclasses import dataclass, field`; `from pathlib import Path`; `from typing import Callable, NamedTuple`.

    Define `class Verdict(NamedTuple): passed: bool; excerpt: str` (NamedTuple because verdicts are immutable result objects; matches PATTERNS).

    Define `@dataclass class DivergenceCheck:` with fields `id: str`, `source_anchor: str`, `severity: str`, `check: Callable[["AgentOutputProxy", Path], Verdict]`. No `__post_init__` validation (severity values are constrained by usage, not by runtime check — keeps the code simple per RESEARCH §Don't Hand-Roll).

    Define `@dataclass class AgentOutputProxy:` with fields `answer: str`, `page_type: str = ""`. The default lets non-ingestor checks ignore page_type without specifying it. (RESEARCH §Open Questions #2 resolves this shape.)
  </action>
  <verify>
    <automated>uv run python -c "from eval_harness.divergence.check import DivergenceCheck, Verdict, AgentOutputProxy; v = Verdict(passed=True, excerpt=''); assert v.passed is True and v.excerpt == ''; o = AgentOutputProxy(answer='hi'); assert o.page_type == ''; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `cores/eval-harness/src/eval_harness/divergence/__init__.py` exists.
    - `check.py` defines `DivergenceCheck` (dataclass) with the 4 D-08-locked fields, `Verdict` (NamedTuple) with `passed` and `excerpt`, `AgentOutputProxy` (dataclass) with `answer: str` and `page_type: str = ""`.
    - All three types are importable from `eval_harness.divergence.check`.
    - `uv sync` still succeeds.
  </acceptance_criteria>
  <done>
    Subpackage created; dataclass schema locked per D-08; verify command above passes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create per-role programmatic check modules (librarian, ingestor, linter, scanner)</name>
  <files>
    cores/eval-harness/src/eval_harness/divergence/librarian.py,
    cores/eval-harness/src/eval_harness/divergence/ingestor.py,
    cores/eval-harness/src/eval_harness/divergence/linter.py,
    cores/eval-harness/src/eval_harness/divergence/scanner.py
  </files>
  <read_first>
    - cores/eval-harness/src/eval_harness/structural.py (full file — `_resolve_citation`, regex patterns, function style)
    - .planning/phases/06-prompt-content-port-divergence-eval/06-PATTERNS.md (section: divergence/librarian.py — pattern applies to all 4 role files)
    - .planning/phases/06-prompt-content-port-divergence-eval/06-RESEARCH.md (Divergence Check Inventory table — exact rule IDs, severity, source_anchor strings per role)
    - cores/eval-harness/src/eval_harness/divergence/check.py (just created in Task 1)
  </read_first>
  <behavior>
    Each role module exports a single list constant `{ROLE_PREFIX}_CHECKS: list[DivergenceCheck]` covering exactly the rule IDs in RESEARCH §Divergence Check Inventory (programmatic checks only — judge-only rules are NOT included here; they live in the rubric files).

    Librarian (`LIBRARIAN_CHECKS`): LIB-001-wikilink-resolves (hard), LIB-002-citation-present (hard), LIB-003-no-slug-only-wikilinks (hard), LIB-004-code-path-format (soft).
    Ingestor (`INGESTOR_CHECKS`): ING-001-frontmatter-present (hard), ING-002-required-fields (hard), ING-003-page-type-routing (hard), ING-004-page-type-valid-category (hard).
    Linter (`LINTER_CHECKS`): LNT-001-code-drift-first (soft), LNT-002-findings-nonempty-when-issues (hard), LNT-003-no-silent-fix (hard).
    Scanner (`SCANNER_CHECKS`): SCN-001-frontmatter-present (hard), SCN-002-required-fields (hard), SCN-003-no-file-map-section (hard), SCN-004-overview-present (hard).

    Every check callable returns `Verdict(passed=True, excerpt="")` on valid input and `Verdict(passed=False, excerpt="<short evidence string>")` on violation. Callables are pure functions (no side effects, no I/O beyond the `vault: Path` argument). Callables tolerate missing `page_type` (default empty string in AgentOutputProxy) — ingestor checks that need it test `if not output.page_type: return Verdict(passed=True, excerpt='')` to skip when context doesn't apply.
  </behavior>
  <action>
    Create each role file with `from __future__ import annotations`, a one-line module docstring naming the role, imports of `DivergenceCheck`, `Verdict`, `AgentOutputProxy` from `eval_harness.divergence.check`, plus role-specific imports.

    `librarian.py`:
      Reuse `from eval_harness.structural import _resolve_citation` per RESEARCH §Don't Hand-Roll.
      Define `_WIKILINK_RE = re.compile(r"\[\[([^\]]+)\]\]")` at module level.
      Define `_CITATION_RE` for any-citation detection (wikilink OR code-path backtick pattern — see structural.py for the existing `_CODE_PATH_RE`).
      Implement four check functions:
        - `_check_wikilink_resolves(output, vault)`: extract all `[[...]]` links; for each, call `_resolve_citation(link, vault)`; if any returns None, fail with `f"Unresolved: {first_unresolved}"`.
        - `_check_citation_present(output, vault)`: pass if at least one wikilink OR backtick-quoted `path:line` appears in `output.answer`; fail with `"No citation in answer"`.
        - `_check_no_slug_only_wikilinks(output, vault)`: scan wikilinks; if any matches `^[A-Z][A-Za-z]+$` (slug-only PackageName form without `/` path prefix), fail with the offending wikilink.
        - `_check_code_path_format(output, vault)`: scan for code paths NOT inside backticks (heuristic: lines like `src/foo.py:42` outside backticks); soft severity — fail with the first offending match.
      Build `LIBRARIAN_CHECKS: list[DivergenceCheck] = [...]` with all 4 IDs, severities per RESEARCH inventory, and `source_anchor` strings per inventory (e.g., `"cores/prompt-sources/SKILL.md#iron-rules"`).

    `ingestor.py`:
      Use `python-frontmatter` (already in deps): `import frontmatter`.
      Define check functions:
        - `_check_frontmatter_present(output, vault)`: pass if `output.answer.startswith("---")` and a second `---` delimiter exists; fail with `"Missing YAML frontmatter delimiters"`.
        - `_check_required_fields(output, vault)`: parse with `frontmatter.loads(output.answer)`; required fields = `["title", "category", "page_type", "target_slug", "summary"]`; fail with missing-field list if any absent.
        - `_check_page_type_routing(output, vault)`: parse `page_type`; must be in `{"package", "concept", "adr", "source"}`; fail with the invalid value.
        - `_check_page_type_valid_category(output, vault)`: parse `category` and `page_type`; if `page_type == "source"` then `category` must be `"source"`, else `category` must match `page_type` (e.g. `page_type:package` -> `category:package`); fail with mismatch description.
      Build `INGESTOR_CHECKS: list[DivergenceCheck]` with all 4 IDs.

    `linter.py`:
      Define check functions:
        - `_check_code_drift_first(output, vault)`: parse output as ordered findings list; if the first finding mentions `orphan` or `stale` while a later finding mentions `code drift` or `outdated claim`, soft-fail with "Code-drift finding should appear before orphan/stale finding". Heuristic-based (regex match on finding lines).
        - `_check_findings_nonempty(output, vault)`: trim whitespace from output; pass if non-empty (the fixture-anchored notion of "known issues" is enforced at integration test time in 06-11, not in this unit check; the unit check just guards against empty output on a vault that has been seeded with issues).
        - `_check_no_silent_fix(output, vault)`: scan output for words indicating write operations: `wrote`, `created`, `updated`, `edited`, `fixed`, `modified` paired with `.md` file references; fail with the offending line if present.
      Build `LINTER_CHECKS: list[DivergenceCheck]`.

    `scanner.py`:
      Use `frontmatter` for parsing.
      Define check functions:
        - `_check_frontmatter_present(output, vault)`: same shape as ingestor's, but for scanner stub output.
        - `_check_required_fields(output, vault)`: required = `["title", "category", "summary", "package_path", "language"]`; fail with missing-field list.
        - `_check_no_file_map_section(output, vault)`: fail if `## File map` appears in `output.answer`; this enforces the pipeline contract from SCANNER_SYSTEM.
        - `_check_overview_present(output, vault)`: fail if `## Overview` is absent.
      Build `SCANNER_CHECKS: list[DivergenceCheck]`.

    All four files: keep functions pure, no global state mutation, no LLM calls, no Bedrock dependency — these run in unit-test mode without `GRAPH_WIKI_RUN_EVAL`.
  </action>
  <verify>
    <automated>uv run python -c "from eval_harness.divergence.librarian import LIBRARIAN_CHECKS; from eval_harness.divergence.ingestor import INGESTOR_CHECKS; from eval_harness.divergence.linter import LINTER_CHECKS; from eval_harness.divergence.scanner import SCANNER_CHECKS; assert len(LIBRARIAN_CHECKS) == 4 and {c.id for c in LIBRARIAN_CHECKS} == {'LIB-001-wikilink-resolves', 'LIB-002-citation-present', 'LIB-003-no-slug-only-wikilinks', 'LIB-004-code-path-format'}; assert len(INGESTOR_CHECKS) == 4 and all(c.id.startswith('ING-') for c in INGESTOR_CHECKS); assert len(LINTER_CHECKS) == 3 and all(c.id.startswith('LNT-') for c in LINTER_CHECKS); assert len(SCANNER_CHECKS) == 4 and all(c.id.startswith('SCN-') for c in SCANNER_CHECKS); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Each of 4 role files exports the named `*_CHECKS: list[DivergenceCheck]` constant.
    - LIBRARIAN_CHECKS has exactly 4 entries with IDs LIB-001..LIB-004 per RESEARCH inventory.
    - INGESTOR_CHECKS has exactly 4 entries (ING-001..ING-004).
    - LINTER_CHECKS has exactly 3 entries (LNT-001..LNT-003).
    - SCANNER_CHECKS has exactly 4 entries (SCN-001..SCN-004).
    - Each check's `severity` is one of `"hard"` or `"soft"` per RESEARCH inventory.
    - Each check's `source_anchor` references `cores/prompt-sources/...`.
    - `librarian.py` imports `_resolve_citation` from `eval_harness.structural` (no re-implementation).
    - All four files importable without Bedrock or AWS credentials (pure-Python checks).
  </acceptance_criteria>
  <done>
    All 4 role files exist with the correct rule-ID inventories, severity assignments, and source_anchor references; pure-Python check callables with no Bedrock dependency.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create LLM-judge rubric .md files + populate divergence/__init__.py re-export</name>
  <files>
    cores/eval-harness/src/eval_harness/divergence/rubrics/librarian.md,
    cores/eval-harness/src/eval_harness/divergence/rubrics/ingestor.md,
    cores/eval-harness/src/eval_harness/divergence/rubrics/linter.md,
    cores/eval-harness/src/eval_harness/divergence/rubrics/scanner.md,
    cores/eval-harness/src/eval_harness/divergence/__init__.py
  </files>
  <read_first>
    - cores/prompt-sources/agents/librarian.md (Rules + Red flags sections for rubric content)
    - cores/prompt-sources/agents/ingestor.md (Rules + Red flags)
    - cores/prompt-sources/agents/linter.md (Rules + Red flags)
    - cores/prompt-sources/agents/scanner.md (Rules + Red flags)
    - cores/prompt-sources/SOURCE-COMMIT (SHA for provenance headers)
    - .planning/phases/06-prompt-content-port-divergence-eval/06-CONTEXT.md (D-10 — rubric file convention)
    - .planning/phases/06-prompt-content-port-divergence-eval/06-RESEARCH.md (Divergence Check Inventory -> LLM-judge checks section lists the judge-only rule IDs per role)
  </read_first>
  <action>
    Each rubric file is a markdown document the LLM judge consumes as the `criteria` parameter of `GEval` in `metric.py` (06-09). Each starts with a 3-line markdown-comment provenance header (HTML comment style since markdown doesn't have Python-style line comments):

    ```
    <!-- Source: cores/prompt-sources/agents/librarian.md -->
    <!-- Anchor: ## Rules + ## Red flags -->
    <!-- Source-commit: <SHA from SOURCE-COMMIT> -->
    ```

    Then a single `# Divergence Rubric — {Role}` H1 followed by `## Scoring Criteria` H2 listing binary pass/fail items.

    Per RESEARCH §Divergence Check Inventory -> LLM-judge checks, each rubric covers:

    `librarian.md`: LIB-005-refusal-pattern (when vault has no relevant content, answer should be the `NO_RELEVANT_CONTENT` sentinel or an explicit "vault doesn't know" statement, NOT an invented answer); LIB-006-no-invention (answer references only symbols/paths that appear in the supplied excerpts).

    `ingestor.md`: ING-005-citation-density (output cites at least one wikilink or code path per claim made); ING-006-flag-contradictions (when the source document contradicts existing vault content, ingestor flags the contradiction rather than silently writing).

    `linter.md`: LNT-004-semantic-completeness (findings list covers categories actually present in the vault — not just a subset); LNT-005-suggestions-present (each finding includes a concrete remediation suggestion).

    `scanner.md`: SCN-005-no-prose-overwrite (output does not overwrite prose in the existing stub file — for delta scans, scanner only adjusts frontmatter / structural sections, leaves freeform prose alone).

    Each criterion is phrased as a binary judge question, e.g.:
      `1. Does the answer either provide a citation-backed response OR explicitly state the vault has no relevant content? (pass/fail)`

    Per RESEARCH (Pitfall 2), soft-severity judge checks should be tolerant of noise — phrase criteria so a single ambiguous case doesn't fail the rubric. End each rubric with: `## Scoring\nReturn a score between 0.0 and 1.0 representing the fraction of criteria that pass.`

    Update `cores/eval-harness/src/eval_harness/divergence/__init__.py` to expose:
      `from pathlib import Path`
      `from eval_harness.divergence.check import DivergenceCheck, Verdict, AgentOutputProxy  # re-export`
      `from eval_harness.divergence.librarian import LIBRARIAN_CHECKS`
      `from eval_harness.divergence.ingestor import INGESTOR_CHECKS`
      `from eval_harness.divergence.linter import LINTER_CHECKS`
      `from eval_harness.divergence.scanner import SCANNER_CHECKS`
      `_RUBRICS_DIR = Path(__file__).parent / "rubrics"`
      `ROLE_CHECKS: dict[str, list[DivergenceCheck]] = {"librarian": LIBRARIAN_CHECKS, "ingestor": INGESTOR_CHECKS, "linter": LINTER_CHECKS, "scanner": SCANNER_CHECKS}`
      `ROLE_RUBRICS: dict[str, Path] = {"librarian": _RUBRICS_DIR / "librarian.md", "ingestor": _RUBRICS_DIR / "ingestor.md", "linter": _RUBRICS_DIR / "linter.md", "scanner": _RUBRICS_DIR / "scanner.md"}`
      `__all__ = ["DivergenceCheck", "Verdict", "AgentOutputProxy", "ROLE_CHECKS", "ROLE_RUBRICS"]`
  </action>
  <verify>
    <automated>uv run python -c "from eval_harness.divergence import ROLE_CHECKS, ROLE_RUBRICS, DivergenceCheck; assert set(ROLE_CHECKS.keys()) == {'librarian', 'ingestor', 'linter', 'scanner'}; assert set(ROLE_RUBRICS.keys()) == {'librarian', 'ingestor', 'linter', 'scanner'}; assert all(p.exists() for p in ROLE_RUBRICS.values()), [str(p) for p in ROLE_RUBRICS.values() if not p.exists()]; print('OK')" && for r in librarian ingestor linter scanner; do head -3 "cores/eval-harness/src/eval_harness/divergence/rubrics/$r.md" | grep -E 'Source:|Anchor:|Source-commit:' | wc -l | tr -d ' '; done | sort -u</automated>
  </verify>
  <acceptance_criteria>
    - All 4 rubric .md files exist at the expected paths.
    - Each rubric starts with the 3-line HTML-comment provenance header (`<!-- Source: -->`, `<!-- Anchor: -->`, `<!-- Source-commit: -->`).
    - Each rubric covers the judge-only rule IDs from RESEARCH inventory (LIB-005, LIB-006 for librarian; ING-005, ING-006 for ingestor; LNT-004, LNT-005 for linter; SCN-005 for scanner).
    - Each rubric ends with a `## Scoring` section instructing the judge to return 0.0-1.0.
    - `eval_harness.divergence` package re-exports `DivergenceCheck`, `Verdict`, `AgentOutputProxy`, `ROLE_CHECKS`, `ROLE_RUBRICS`.
    - `ROLE_RUBRICS` Path values all resolve to existing files.
  </acceptance_criteria>
  <done>
    4 rubric files with provenance headers; __init__.py exposes ROLE_CHECKS + ROLE_RUBRICS dicts; metric.py in 06-09 can import them cleanly.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Expand test_divergence_checks.py with passing + failing unit tests per programmatic rule</name>
  <files>
    cores/eval-harness/tests/test_divergence_checks.py
  </files>
  <read_first>
    - cores/eval-harness/tests/test_divergence_checks.py (scaffold from 06-02)
    - cores/eval-harness/tests/test_structural.py (style + fixture_vault_path usage analog)
    - cores/eval-harness/tests/conftest.py (fixtures available: fixture_vault_path or equivalent)
    - cores/eval-harness/src/eval_harness/divergence/librarian.py (just created)
    - cores/eval-harness/src/eval_harness/divergence/ingestor.py
    - cores/eval-harness/src/eval_harness/divergence/linter.py
    - cores/eval-harness/src/eval_harness/divergence/scanner.py
    - .planning/phases/06-prompt-content-port-divergence-eval/06-PATTERNS.md (section: eval-harness/tests/test_divergence_checks.py)
  </read_first>
  <behavior>
    For each programmatic rule (LIB-001..004, ING-001..004, LNT-001..003, SCN-001..004 — 15 rules total), at least one passing test and one failing test. Tests use a small in-memory fixture vault (tmp_path) or the existing `fixture_vault_path` fixture. No Bedrock, no judge calls.
  </behavior>
  <action>
    Edit `cores/eval-harness/tests/test_divergence_checks.py` from the 06-02 scaffold. Remove the module-level skip guard (or convert it to fail-fast). Add concrete tests per rule following the pattern in PATTERNS.md (section: eval-harness/tests/test_divergence_checks.py).

    Use a helper `_get_check(role, rule_id)` that scans the role module's `*_CHECKS` list and returns the matching `DivergenceCheck`. This isolates test code from the order of items in the list.

    For each rule write at least two tests: `test_{rule_id_lowered}_passes_on_{valid_case}` and `test_{rule_id_lowered}_fails_on_{violating_case}`. Examples:

      LIB-001 passing: AgentOutputProxy with `answer="See [[packages/lattice-wiki-core]]."` against a fixture vault where `packages/lattice-wiki-core.md` exists.
      LIB-001 failing: AgentOutputProxy with `answer="See [[nonexistent/page]]."` against the same vault — assert `verdict.passed is False` and `"Unresolved" in verdict.excerpt`.

      ING-001 passing: `answer="---\\ntitle: foo\\n---\\n# body"`.
      ING-001 failing: `answer="No frontmatter here"`.

      ING-002 passing: full frontmatter with all 5 required fields.
      ING-002 failing: missing `target_slug` — assert excerpt names `target_slug`.

      ING-003 passing: `page_type: package`. Failing: `page_type: invalid_type` — assert excerpt names the bad value.

      ING-004 passing: `category: source` + `page_type: source`. Failing: `category: package` + `page_type: source` — assert mismatch in excerpt.

      LNT-002 passing: non-empty findings. Failing: empty answer (after strip) — assert `verdict.passed is False`.

      LNT-003 passing: `"Found 3 issues: orphan page, stale claim, broken wikilink."`. Failing: `"Updated foo.md to fix the typo."` — assert "wrote" or "updated" detection.

      SCN-001 passing: `"---\\ntitle: foo\\n---\\n## Overview\\nstuff"`. Failing: no frontmatter.

      SCN-003 passing: `"## Overview\\nstuff\\n## Notable files\\nthings"`. Failing: `"## Overview\\n## File map\\n- foo.py"` — assert `verdict.passed is False`.

      SCN-004 passing: contains `## Overview`. Failing: missing.

    Use `fixture_vault_path` from existing conftest.py when a real vault is needed. For tests that need a minimal vault, create one in-test via tmp_path with 1-2 pages.
  </action>
  <verify>
    <automated>uv run pytest cores/eval-harness/tests/test_divergence_checks.py -x -q --tb=short 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `test_divergence_checks.py` no longer skip-stubs (the module-level skip guard from 06-02 is removed or converted to a hard fail if imports break).
    - At least 2 tests per rule (1 passing + 1 failing case), total at least 30 tests (15 rules × 2).
    - All tests pass under `uv run pytest cores/eval-harness/tests/test_divergence_checks.py -x` without `GRAPH_WIKI_RUN_EVAL` (no Bedrock).
    - For each failing-case test, the assertion includes both `verdict.passed is False` AND a substring check on `verdict.excerpt` (proves accepted_failures will carry useful evidence in 06-09).
  </acceptance_criteria>
  <done>
    All 15 programmatic rules have passing + failing unit tests; `pytest cores/eval-harness/tests/test_divergence_checks.py -x` reports green; no Bedrock dependency.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM output -> check callable | Untrusted text input; checks treat it as data, never eval/exec |
| Vendored .md content -> rubric file | Static text read into GEval criteria parameter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-15 | Tampering | LLM output evaluated as code | mitigate | All check callables use string operations + regex + `frontmatter.loads` only; no `eval`/`exec`; no shell calls |
| T-06-16 | Tampering | Path traversal via wikilink content | mitigate | `_resolve_citation` (reused from structural.py) uses `vault_path.glob()` anchored to vault root per RESEARCH §Security Domain |
| T-06-17 | Information Disclosure | Excerpt strings leak sensitive data | accept | Excerpts are first-violating-link snippets only, max ~100 chars; baseline JSON is committed to git, so any excerpt is by definition non-sensitive vault content |
</threat_model>

<verification>
- All 15 programmatic check rules covered by passing + failing tests
- `uv run pytest cores/eval-harness/tests/test_divergence_checks.py -x` green
- `eval_harness.divergence` package exports DivergenceCheck, Verdict, AgentOutputProxy, ROLE_CHECKS, ROLE_RUBRICS
- All 4 rubric files exist with 3-line HTML-comment provenance headers
- Path values in ROLE_RUBRICS resolve to existing files
</verification>

<success_criteria>
- EVAL-11 delivered: divergence rule infrastructure complete (dataclasses + 15 rules + 4 rubrics)
- Each check is a pure function with a Verdict return value and an excerpt suitable for the accepted_failures array
- All rule IDs match RESEARCH §Divergence Check Inventory exactly
- 06-09 (metric class) and 06-10 (baseline) can consume `ROLE_CHECKS` and `ROLE_RUBRICS` directly
</success_criteria>

<output>
Create `.planning/phases/06-prompt-content-port-divergence-eval/06-08-SUMMARY.md` when done.
</output>
