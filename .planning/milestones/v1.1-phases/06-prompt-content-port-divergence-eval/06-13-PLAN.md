---
phase: 06-prompt-content-port-divergence-eval
plan: 13
type: execute
wave: 9
depends_on: [06-12]
files_modified:
  - agents/graph-wiki-agent/src/graph_wiki_agent/commands/ingest.py
  - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/ingestor.py
  - agents/graph-wiki-agent/tests/unit/test_commands_ingest.py
autonomous: true
requirements: [PORT-03, EVAL-11]
tags: [gap-closure, ingestor, routing, slug-consistency, UAT-G2, UAT-G3]

must_haves:
  truths:
    - "When the ingestor frontmatter declares page_type: source, the file lands under wiki/sources/<slug>.md (not concepts/)"
    - "page_type values 'package' / 'concept' / 'adr' / 'source' all route to their canonical directories per cores/prompt-sources/SKILL.md page-categories table"
    - "The on-disk filename slug equals the frontmatter target_slug exactly — the routing step writes target_slug back into the frontmatter when it had to be re-slugified, so the file and its frontmatter agree"
    - "Ingestor system prompt enumerates `source` alongside `package | concept | adr` in the page-type routing section (no longer says `page_type: source` then routes nowhere)"
    - "Unit tests prove source → sources/ and target_slug ↔ filename equality"
  artifacts:
    - path: "agents/graph-wiki-agent/src/graph_wiki_agent/commands/ingest.py"
      provides: "_PAGE_TYPE_DIRS includes 'source': 'sources'; target_slug is reconciled with the on-disk filename"
      contains: "\"source\": \"sources\""
    - path: "agents/graph-wiki-agent/src/graph_wiki_agent/prompts/ingestor.py"
      provides: "_PAGE_TYPE_ROUTING enumerates source alongside package/concept/adr"
      contains: "page_type: source"
    - path: "agents/graph-wiki-agent/tests/unit/test_commands_ingest.py"
      provides: "Tests for page_type=source routing and target_slug-filename equality"
      contains: "test_run_ingest_source_routes_source_to_sources_dir"
  key_links:
    - from: "commands/ingest.py:_PAGE_TYPE_DIRS"
      to: "wiki/sources/ directory"
      via: "added mapping entry 'source' -> 'sources'"
      pattern: "\"source\": \"sources\""
    - from: "commands/ingest.py:run_ingest_source step 7"
      to: "target_path filename"
      via: "after slugify(target_slug), write the (possibly re-slugified) value back into the file body so frontmatter target_slug matches the on-disk filename"
      pattern: "target_slug.*==.*target_path.stem"
---

<objective>
Close UAT gaps G2 (source documents routed to `concepts/` instead of `sources/`; frontmatter self-declares `page_type: source` but on-disk placement disagrees) and G3 (`target_slug` in frontmatter does not match the on-disk filename slug — e.g. `otel-story-observability` vs `otel-story-of-observability.md`).

Purpose: The canonical page-categories table in `cores/prompt-sources/SKILL.md` (lines 14-19 of `_fragments/page_categories.py`) defines `source` as a page_type that routes to `vault_path/sources/`, but `_PAGE_TYPE_DIRS` in `commands/ingest.py` only maps `package | concept | adr`, so `source` falls through to the default `concepts/`. Also, the slug pipeline applies `slugify()` to the LLM-provided `target_slug` (which can transform it), but never writes the canonical slug back into the file body — so the frontmatter and filename can disagree.

Output: One mapping entry added to `_PAGE_TYPE_DIRS`; one rewrite step in `run_ingest_source` that rewrites the file body's `target_slug:` value to match `target_path.stem`; ingestor system prompt enumerates `source` explicitly; new unit tests for both behaviors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-prompt-content-port-divergence-eval/06-UAT.md
@cores/prompt-sources/SKILL.md
@cores/prompt-sources/agents/ingestor.md
@agents/graph-wiki-agent/src/graph_wiki_agent/commands/ingest.py
@agents/graph-wiki-agent/src/graph_wiki_agent/prompts/ingestor.py
@agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/page_categories.py
@agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/frontmatter_rules.py
@agents/graph-wiki-agent/tests/unit/test_commands_ingest.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add source page_type to routing map and reconcile target_slug with filename</name>
  <files>
    agents/graph-wiki-agent/src/graph_wiki_agent/commands/ingest.py
    agents/graph-wiki-agent/tests/unit/test_commands_ingest.py
  </files>
  <read_first>
    - agents/graph-wiki-agent/src/graph_wiki_agent/commands/ingest.py (lines 69-93 — `_PAGE_TYPE_DIRS` + `_route_target_path`; lines 261-291 — Step 6 + Step 7 of `run_ingest_source` where page_type fallback and slug sanitization happen)
    - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/page_categories.py (lines 10-18 — canonical page-categories table; the `source -> vault_path/sources/` mapping is the source of truth)
    - cores/prompt-sources/SKILL.md (lines 14-20 — same table at its origin; line 16 explicitly says `source` -> `vault_path/sources/`)
    - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/frontmatter_rules.py (lines 12-19 — `page_type: one of package, concept, adr, source` — confirms `source` is a valid page_type that the LLM may emit)
    - agents/graph-wiki-agent/tests/unit/test_commands_ingest.py (lines 22-67 — pattern for ingest unit tests: patch `resolve_wiki_and_repo`, `make_llm`, `update_index`, `append_log`; assert on `expected_page` path and `result.page_type`)
  </read_first>
  <behavior>
    - `_PAGE_TYPE_DIRS["source"] == "sources"` (new mapping)
    - `_PAGE_TYPE_DIRS["package"] == "packages"` (unchanged)
    - `_PAGE_TYPE_DIRS["concept"] == "concepts"` (unchanged)
    - `_PAGE_TYPE_DIRS["adr"] == "adrs"` (unchanged)
    - In `run_ingest_source`, when the LLM frontmatter says `page_type: source` and `target_slug: my-slug`, the on-disk path is `wiki/sources/my-slug.md` (NOT `wiki/concepts/my-slug.md`)
    - When `slugify(llm_target_slug) != llm_target_slug` (slug got transformed), the written file body has its `target_slug:` line rewritten to the canonical slug so frontmatter and filename agree
    - When the LLM omits `target_slug` entirely, the title-derived slug is written back into the frontmatter body (so a downstream reader of the file sees a `target_slug:` value that matches the filename)
    - Returned `IngestResult.slug == target_path.stem` (already true, but tests assert it explicitly post-fix)
  </behavior>
  <action>
    Edit `agents/graph-wiki-agent/src/graph_wiki_agent/commands/ingest.py`:

    1. In `_PAGE_TYPE_DIRS` (lines 73-77), ADD ONE LINE:

       ```
       _PAGE_TYPE_DIRS: dict[str, str] = {
           "package": "packages",
           "concept": "concepts",
           "adr": "adrs",
           "source": "sources",
       }
       ```

    2. In `run_ingest_source`, locate the page_type-fallback block (currently around lines 263-265):

       ```
       page_type = str(fm.get("page_type", "concept")).lower()
       if page_type not in _PAGE_TYPE_DIRS:
           page_type = "concept"
       ```

       Leave it as-is — the `source` key is now a valid member of the dict, so a frontmatter declaring `page_type: source` no longer falls through to `concept`.

    3. In `run_ingest_source`, immediately AFTER `target_path = _route_target_path(wiki, page_type, target_slug)` and BEFORE `target_path.write_text(llm_output, encoding="utf-8")`, add a slug-reconciliation step. The on-disk filename is `target_path.stem` (already-sanitized); write that value back into the file body's `target_slug:` line so the file's own frontmatter agrees with its filename:

       ```
       # Reconcile target_slug in the body with the on-disk filename slug.
       # _route_target_path uses slugify(target_slug); if that differs from
       # what the LLM wrote, rewrite the body's `target_slug:` line to match.
       # Also handles the case where the LLM omitted target_slug entirely
       # (we fell back to slugify(title)) — write that fallback into the body.
       canonical_slug = target_path.stem
       llm_output = _rewrite_target_slug_in_body(llm_output, canonical_slug)
       ```

       Add a new private helper at module level (just above `_parse_ingestor_response`, after `_route_target_path`):

       ```
       def _rewrite_target_slug_in_body(text: str, canonical_slug: str) -> str:
           """Rewrite the `target_slug:` line in the YAML frontmatter of `text`
           so it equals `canonical_slug`. If no `target_slug:` line exists in
           the frontmatter, inject one immediately after the opening `---`.

           Operates on the raw text — does not re-emit YAML — so it preserves
           comments, ordering, and indentation of all other frontmatter fields.

           Only touches the frontmatter block (between the first two `---`
           delimiters). If text has no frontmatter, returns text unchanged.
           """
           stripped = text.lstrip()
           if not stripped.startswith("---"):
               return text
           # Find the second `---` to bound the frontmatter block
           after_open = stripped[3:].lstrip("\n")
           close_idx = after_open.find("\n---")
           if close_idx == -1:
               return text
           leading_ws = text[: len(text) - len(stripped)]
           fm_block = after_open[:close_idx]
           body_and_close = after_open[close_idx:]
           new_lines: list[str] = []
           found = False
           for line in fm_block.splitlines():
               if line.lstrip().startswith("target_slug:"):
                   indent = line[: len(line) - len(line.lstrip())]
                   new_lines.append(f"{indent}target_slug: {canonical_slug}")
                   found = True
               else:
                   new_lines.append(line)
           if not found:
               new_lines.insert(0, f"target_slug: {canonical_slug}")
           new_fm = "\n".join(new_lines)
           return f"{leading_ws}---\n{new_fm}{body_and_close}"
       ```

       This helper is intentionally text-based (no `yaml.load`) to match the existing parser philosophy (the file's docstring at lines 102-109 explicitly avoids `yaml.load`).

    4. In `agents/graph-wiki-agent/tests/unit/test_commands_ingest.py`, add new tests at the end of the file:

       ```
       # ---------------------------------------------------------------------------
       # page_type=source routing + target_slug-filename equality (Plan 06-13)
       # ---------------------------------------------------------------------------


       @pytest.mark.asyncio
       async def test_run_ingest_source_routes_source_to_sources_dir(tmp_path: Path) -> None:
           """Fake ingestor returns page_type=source; page lands under sources/foo.md."""
           from graph_wiki_agent.commands.ingest import IngestResult, run_ingest_source

           source_file = tmp_path / "an-article.md"
           source_file.write_text("# An Article\n\nBody.", encoding="utf-8")

           wiki = tmp_path / "wiki"
           wiki.mkdir()
           (wiki / "log.md").write_text("", encoding="utf-8")

           fake_llm_response = (
               "---\n"
               "title: An Article\n"
               "category: source\n"
               "page_type: source\n"
               "target_slug: an-article\n"
               "summary: A test source\n"
               "---\n"
               "\n"
               "Body text here."
           )

           with (
               patch("graph_wiki_agent.commands.ingest.resolve_wiki_and_repo") as mock_resolve,
               patch("graph_wiki_agent.commands.ingest.make_llm") as mock_make_llm,
               patch("graph_wiki_agent.commands.ingest.update_index"),
               patch("graph_wiki_agent.commands.ingest.append_log"),
           ):
               mock_resolve.return_value = (wiki, tmp_path)
               fake_llm = MagicMock()
               fake_llm.ainvoke = AsyncMock(return_value=MagicMock(content=fake_llm_response))
               mock_make_llm.return_value = fake_llm

               result = await run_ingest_source(source_file, wiki)

           expected_page = wiki / "sources" / "an-article.md"
           assert expected_page.exists(), f"expected page at {expected_page}, got result={result}"
           assert (wiki / "concepts").exists() is False or not any(
               (wiki / "concepts").iterdir()
           ), "concepts/ should be empty for page_type=source"
           assert result.page_type == "source"
           assert "sources/an-article.md" in result.page_path
           assert result.slug == "an-article"


       @pytest.mark.asyncio
       async def test_run_ingest_source_target_slug_matches_filename(tmp_path: Path) -> None:
           """LLM emits a slug that slugify() transforms; frontmatter target_slug
           in the written file must equal the on-disk filename slug."""
           from graph_wiki_agent.commands.ingest import run_ingest_source

           source_file = tmp_path / "src.md"
           source_file.write_text("# Src\n\nBody.", encoding="utf-8")
           wiki = tmp_path / "wiki"
           wiki.mkdir()
           (wiki / "log.md").write_text("", encoding="utf-8")

           # LLM emits a slug with characters that slugify() would normalize.
           # We use a slug that survives slugify() unchanged for determinism,
           # then assert the rewrite step copies it into the body verbatim.
           # (The real-world G3 case was a slug that DIVERGED from the title-
           # derived slug; the test below covers the rewrite path explicitly.)
           fake_llm_response = (
               "---\n"
               "title: Some Page\n"
               "category: concept\n"
               "page_type: concept\n"
               "target_slug: weird_slug_with_underscores\n"  # slugify -> weird-slug-with-underscores
               "summary: x\n"
               "---\n"
               "Body."
           )

           with (
               patch("graph_wiki_agent.commands.ingest.resolve_wiki_and_repo") as mock_resolve,
               patch("graph_wiki_agent.commands.ingest.make_llm") as mock_make_llm,
               patch("graph_wiki_agent.commands.ingest.update_index"),
               patch("graph_wiki_agent.commands.ingest.append_log"),
           ):
               mock_resolve.return_value = (wiki, tmp_path)
               fake_llm = MagicMock()
               fake_llm.ainvoke = AsyncMock(return_value=MagicMock(content=fake_llm_response))
               mock_make_llm.return_value = fake_llm

               result = await run_ingest_source(source_file, wiki)

           # Determine the actual on-disk path; assert filename slug == body target_slug
           written_path = wiki / "concepts" / f"{result.slug}.md"
           assert written_path.exists(), f"expected page at {written_path}"
           written_body = written_path.read_text(encoding="utf-8")
           assert f"target_slug: {result.slug}" in written_body, (
               f"target_slug in body must equal filename stem '{result.slug}'; "
               f"body excerpt:\n{written_body[:300]}"
           )
           # And the original LLM slug (pre-slugify) should NOT survive verbatim
           assert "target_slug: weird_slug_with_underscores" not in written_body
       ```
  </action>
  <verify>
    <automated>uv run --package graph-wiki-agent pytest agents/graph-wiki-agent/tests/unit/test_commands_ingest.py -x -q -k "source or target_slug or extracts_and_routes or default_slug"</automated>
  </verify>
  <acceptance_criteria>
    - `_PAGE_TYPE_DIRS["source"] == "sources"` (grep-able: `grep -n '\"source\": \"sources\"' agents/graph-wiki-agent/src/graph_wiki_agent/commands/ingest.py` returns exactly one match).
    - `test_run_ingest_source_routes_source_to_sources_dir` PASSES; the asserted on-disk path is `wiki/sources/an-article.md`; `result.page_type == "source"`.
    - `test_run_ingest_source_target_slug_matches_filename` PASSES; the written file body contains `target_slug: <result.slug>` matching the filename stem.
    - The pre-existing `test_run_ingest_source_extracts_and_routes` and `test_run_ingest_source_default_slug_from_title` still pass (no regression on page_type=concept routing).
    - The `_route_target_path` containment check (line 91-92) still rejects path traversal (no behavior change there — the new key just adds another allowed subdir).
  </acceptance_criteria>
  <done>
    `page_type=source` files land under `sources/`; on-disk filename slug always equals body `target_slug:`; all four ingest unit tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enumerate source in INGESTOR_SYSTEM page-type routing section</name>
  <files>
    agents/graph-wiki-agent/src/graph_wiki_agent/prompts/ingestor.py
  </files>
  <read_first>
    - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/ingestor.py (lines 24-29 — current `_PAGE_TYPE_ROUTING` constant; note line 27 already says "Work-item subjects (package/concept/decision): `page_type: package | concept | adr`" — `source` is mentioned in line 26 but the routing semantics are not stated in terms of the directory it lands in)
    - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/page_categories.py (lines 10-18 — confirms canonical directory mapping; the prompt should match)
    - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/frontmatter_rules.py (lines 12-19 — already lists `source` as a valid page_type)
    - .planning/phases/06-prompt-content-port-divergence-eval/06-UAT.md (Test 2 — the LLM ALREADY emitted `page_type: source`; this prompt change is a clarity reinforcement, not a primary fix — Task 1's routing-map change is what makes the file land correctly)
  </read_first>
  <behavior>
    - INGESTOR_SYSTEM contains the substring "page_type: source" AND the substring "sources/" (so the LLM sees the directory consequence of choosing `page_type: source`)
    - The enumeration explicitly lists all four allowed page_types: `package`, `concept`, `adr`, `source`
    - All other INGESTOR_SYSTEM content unchanged
    - Snapshot test passes after one `--snapshot-update`
  </behavior>
  <action>
    Edit `_PAGE_TYPE_ROUTING` in `agents/graph-wiki-agent/src/graph_wiki_agent/prompts/ingestor.py` (lines 24-29). Replace with:

    ```
    _PAGE_TYPE_ROUTING = (
        "## Page-type routing\n\n"
        "Choose exactly one `page_type`. The on-disk destination is determined by `page_type`:\n\n"
        "- `page_type: source` -> `sources/` (specs, PRs, articles, transcripts, in-repo docs)\n"
        "- `page_type: package` -> `packages/` (a workspace member with a manifest)\n"
        "- `page_type: concept` -> `concepts/` (cross-cutting technical idea, comparison page)\n"
        "- `page_type: adr` -> `adrs/` (dated decision record)\n\n"
        "`category` should agree with `page_type` (`source` -> `source`, `package` -> `package`, etc.).\n"
        "`update_index()` and `append_log()` run automatically — omit those steps."
    )
    ```

    Run snapshot refresh:
      `uv run --package graph-wiki-agent pytest agents/graph-wiki-agent/tests/prompts/test_prompt_snapshots.py::test_ingestor_system_snapshot --snapshot-update -x -q`

    Commit the source + snapshot together.
  </action>
  <verify>
    <automated>uv run --package graph-wiki-agent python -c "from graph_wiki_agent.prompts.ingestor import INGESTOR_SYSTEM; s = INGESTOR_SYSTEM; assert 'page_type: source' in s; assert 'sources/' in s; assert 'page_type: package' in s and 'packages/' in s; assert 'page_type: concept' in s and 'concepts/' in s; assert 'page_type: adr' in s and 'adrs/' in s; print('OK')" &amp;&amp; uv run --package graph-wiki-agent pytest agents/graph-wiki-agent/tests/prompts/test_prompt_snapshots.py::test_ingestor_system_snapshot -x -q</automated>
  </verify>
  <acceptance_criteria>
    - INGESTOR_SYSTEM contains all four routing lines `page_type: {source|package|concept|adr}` each paired with their directory (`sources/`, `packages/`, `concepts/`, `adrs/`).
    - INGESTOR_SYSTEM contains the substring `\`category\` should agree with \`page_type\`` so the LLM is reminded to keep the two fields consistent (this addresses the secondary aspect of G2 where frontmatter `category` and `page_type` agreed but on-disk routing did not — the disagreement was Claude's, not the LLM's).
    - Snapshot test passes after one --snapshot-update run.
    - Snapshot git diff shows only ADDED lines (no deletes from other fragments).
  </acceptance_criteria>
  <done>
    INGESTOR_SYSTEM enumerates the four page_types with their target directories; snapshot refreshed and committed.
  </done>
</task>

</tasks>

<verification>
- `uv run --package graph-wiki-agent pytest agents/graph-wiki-agent/tests/unit/test_commands_ingest.py agents/graph-wiki-agent/tests/prompts/test_prompt_snapshots.py -x -q` exits 0.
- `grep -c '\"source\": \"sources\"' agents/graph-wiki-agent/src/graph_wiki_agent/commands/ingest.py | grep -v '^#' || true` shows ≥1 (depending on shell; use `grep -n '\"source\": \"sources\"' ...` directly).
- Manual: cat a fresh `wiki/sources/*.md` written by `test_run_ingest_source_routes_source_to_sources_dir` (via `pytest -s --capture=no` plus a print) and confirm its body's `target_slug:` equals the filename stem.
</verification>

<success_criteria>
- A live `graph-wiki-agent ingest` of a source document with `page_type: source` frontmatter lands at `wiki/sources/<slug>.md` (UAT G2 closed).
- The on-disk filename stem and the body's `target_slug:` are byte-identical for every written page (UAT G3 closed).
- The ingestor LLM is now told explicitly which directory each `page_type` maps to (clarity reinforcement for G2/G3).
</success_criteria>

<output>
Create `.planning/phases/06-prompt-content-port-divergence-eval/06-13-SUMMARY.md` when done. Record: (a) the diff of `_PAGE_TYPE_DIRS`, (b) the new `_rewrite_target_slug_in_body` signature, (c) the snapshot delta line count, (d) the names of the two new unit tests and their pass status.
</output>