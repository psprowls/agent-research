---
phase: 06-prompt-content-port-divergence-eval
plan: 05
type: execute
wave: 3
depends_on: [06-03]
files_modified:
  - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/ingestor.py
  - agents/graph-wiki-agent/src/graph_wiki_agent/commands/ingest.py
autonomous: true
requirements: [PORT-03]
tags: [prompt-port, ingestor, ingest-command]

must_haves:
  truths:
    - "INGESTOR_SYSTEM is composed from IRON_RULES + PAGE_CATEGORIES + FRONTMATTER_RULES + CITATION_RULES + ingestor-local rules at import time"
    - "INGESTOR_SYSTEM preserves canonical: page-type routing table, source_type discrimination, minimum-3-touches rule, cite-aggressively rule, required frontmatter fields"
    - "INGESTOR_SYSTEM removes host-specific references (slash commands, interactive discussion loops, skill invocations) per RESEARCH §Adaptation Map"
    - "commands/ingest.py imports INGESTOR_SYSTEM from prompts.ingestor instead of defining it inline"
    - "Behavior of commands/ingest.py is unchanged — only the source of the constant changes"
  artifacts:
    - path: "agents/graph-wiki-agent/src/graph_wiki_agent/prompts/ingestor.py"
      provides: "INGESTOR_SYSTEM composed string"
      contains: "INGESTOR_SYSTEM"
    - path: "agents/graph-wiki-agent/src/graph_wiki_agent/commands/ingest.py"
      provides: "Ingest command with import-based prompt sourcing"
  key_links:
    - from: "prompts/ingestor.py"
      to: "_fragments/{iron_rules, page_categories, frontmatter_rules, citation_rules}"
      via: "Python imports at module top"
      pattern: "from graph_wiki_agent.prompts._fragments"
    - from: "commands/ingest.py"
      to: "prompts.ingestor"
      via: "import replacing inline constant"
      pattern: "from graph_wiki_agent.prompts.ingestor import INGESTOR_SYSTEM"
---

<objective>
Port the ingestor system prompt content from `cores/prompt-sources/agents/ingestor.md` (plus shared fragments) into `prompts/ingestor.py`, then swap the inline `INGESTOR_SYSTEM` constant in `commands/ingest.py` for an import.

Purpose: Completes PORT-03. Adapts the canonical ingestor workflow to the graph-wiki-agent non-interactive tool surface (RESEARCH §Adaptation Map) while preserving the semantic rules (page-type routing, frontmatter, citation density, minimum-3-touches).

Output: One new `prompts/ingestor.py` + a refactored `commands/ingest.py` importing it. Snapshot test for `INGESTOR_SYSTEM` recorded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-prompt-content-port-divergence-eval/06-CONTEXT.md
@.planning/phases/06-prompt-content-port-divergence-eval/06-RESEARCH.md
@.planning/phases/06-prompt-content-port-divergence-eval/06-PATTERNS.md
@cores/prompt-sources/agents/ingestor.md
@cores/prompt-sources/SKILL.md
@agents/graph-wiki-agent/src/graph_wiki_agent/commands/ingest.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create prompts/ingestor.py composing fragments + ingestor-local content</name>
  <files>
    agents/graph-wiki-agent/src/graph_wiki_agent/prompts/ingestor.py
  </files>
  <read_first>
    - cores/prompt-sources/agents/ingestor.md (full file — workflow, rules, red flags)
    - cores/prompt-sources/SKILL.md (page categories context)
    - agents/graph-wiki-agent/src/graph_wiki_agent/commands/ingest.py (lines 42-66 — existing INGESTOR_SYSTEM constant)
    - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/iron_rules.py
    - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/page_categories.py
    - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/frontmatter_rules.py
    - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/citation_rules.py
    - .planning/phases/06-prompt-content-port-divergence-eval/06-PATTERNS.md (section: prompts/ingestor.py)
    - .planning/phases/06-prompt-content-port-divergence-eval/06-RESEARCH.md (sections: Source File Content Analysis -> Ingestor-local, and Adaptation Map)
  </read_first>
  <behavior>
    - INGESTOR_SYSTEM is built at import time via `"\n\n".join([...])` (per D-02)
    - INGESTOR_SYSTEM contains: page-type values `package`, `concept`, `adr`, `source` (page-type routing); the required frontmatter fields `title`, `category`, `page_type`, `target_slug`, `summary`; the cite-aggressively / wikilink convention
    - INGESTOR_SYSTEM contains a minimum-3-touches or equivalent phrasing for the ingest-touches-3-files rule
    - INGESTOR_SYSTEM contains the canonical "Output ONLY YAML frontmatter followed by a markdown body" instruction (preserve from existing inline constant)
    - INGESTOR_SYSTEM does NOT contain: `${CLAUDE_PLUGIN_ROOT}`, `/lattice-wiki:`, `Wait for confirmation`, `Discuss`, `obsidian-markdown`, interactive discussion loop phrasing
    - Snapshot test `test_ingestor_system_snapshot` records on first run
  </behavior>
  <action>
    Create `prompts/ingestor.py`: `from __future__ import annotations` line 1. Imports of IRON_RULES, PAGE_CATEGORIES, FRONTMATTER_RULES, CITATION_RULES from `graph_wiki_agent.prompts._fragments.*`.

    Build `INGESTOR_SYSTEM = "\n\n".join([...])` per PATTERNS.md (section: prompts/ingestor.py). Sections in order:
      1. Role intro (role-local). Preserve the existing constant's "Output ONLY YAML frontmatter followed by a markdown body" rule and the "Do NOT add commentary" rule (these are downstream-contract-relevant — parsers expect this shape).
      2. IRON_RULES
      3. PAGE_CATEGORIES
      4. FRONTMATTER_RULES
      5. CITATION_RULES
      6. Page-type routing block (role-local, adapted from ingestor.md Workflow). Must include the discrimination rule: source pages get `page_type: source`, work-item pages get `page_type: package|concept|adr` depending on content. This is what divergence check ING-003 in 06-08 validates.
      7. Ingestor rules (role-local, adapted from ingestor.md Rules). Preserve: cite-aggressively, contradiction-flagging, minimum-3-touches (the changed/new page, index.md, log.md — iron rule #5 already in IRON_RULES but state the implication for ingestor explicitly). Adapt: remove all `${CLAUDE_PLUGIN_ROOT}/.../ingest_source.py`, `update_index.py`, `append_log.py` script references (per RESEARCH Adaptation Map — those calls are made by the command layer, not the LLM).
      8. Red flags (role-local, adapted from ingestor.md Red flags — 4 items, paths replaced with graph-wiki-agent vault structure).
      9. Output format paragraph (role-local). Preserve from existing `commands/ingest.py` INGESTOR_SYSTEM the output schema: `## Summary` section, optional `## Key Concepts` or `## Decisions`, token budget under 1500 tokens, do not reproduce full source.

    Adapt away (per RESEARCH Adaptation Map): interactive discussion loops, `Wait for confirmation`, `obsidian-markdown` skill invocations, `skills: [lattice-wiki, ...]` frontmatter, slash-command references.

    Token budget: target <500 tokens for the composed prompt.

    After writing, run `pytest agents/graph-wiki-agent/tests/prompts/test_prompt_snapshots.py::test_ingestor_system_snapshot --snapshot-update -x` once to record the initial snapshot.
  </action>
  <verify>
    <automated>uv run python -c "from graph_wiki_agent.prompts.ingestor import INGESTOR_SYSTEM; assert 'page_type' in INGESTOR_SYSTEM; assert 'package' in INGESTOR_SYSTEM and 'concept' in INGESTOR_SYSTEM and 'adr' in INGESTOR_SYSTEM; assert 'title' in INGESTOR_SYSTEM and 'summary' in INGESTOR_SYSTEM; assert 'wikilink' in INGESTOR_SYSTEM.lower() or '[[' in INGESTOR_SYSTEM; assert 'CLAUDE_PLUGIN_ROOT' not in INGESTOR_SYSTEM; assert '/lattice-wiki:' not in INGESTOR_SYSTEM; assert 'Wait for confirmation' not in INGESTOR_SYSTEM; print('OK len=', len(INGESTOR_SYSTEM))" && uv run pytest agents/graph-wiki-agent/tests/prompts/test_prompt_snapshots.py::test_ingestor_system_snapshot -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `prompts/ingestor.py` importable; exports `INGESTOR_SYSTEM` non-empty string.
    - Composed via `"\n\n".join([...])` at import time.
    - Contains: `page_type`, all four page-type values (`package`, `concept`, `adr`, `source`), required frontmatter fields, wikilink/citation convention.
    - Contains the "Output ONLY YAML frontmatter" rule from the existing constant (downstream parser contract).
    - Does NOT contain: `CLAUDE_PLUGIN_ROOT`, `/lattice-wiki:`, `Wait for confirmation`, `obsidian-markdown` references.
    - Snapshot test passes (after one-time `--snapshot-update`).
    - Length < 5000 chars (token budget proxy).
  </acceptance_criteria>
  <done>
    `prompts/ingestor.py` composes from fragments + ingestor-local prose; snapshot recorded; adaptation map honored; existing-constant downstream contracts (YAML-frontmatter-only output) preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Swap inline INGESTOR_SYSTEM in commands/ingest.py for an import</name>
  <files>
    agents/graph-wiki-agent/src/graph_wiki_agent/commands/ingest.py
  </files>
  <read_first>
    - agents/graph-wiki-agent/src/graph_wiki_agent/commands/ingest.py (full file — note where INGESTOR_SYSTEM is defined and where it is consumed; module docstring at top)
    - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/ingestor.py (just created)
    - .planning/phases/06-prompt-content-port-divergence-eval/06-PATTERNS.md (section: commands/ingest.py)
  </read_first>
  <action>
    In `commands/ingest.py`, delete the inline `INGESTOR_SYSTEM = """\..."""` block (lines ~42-66 — verify by reading the file). Replace with a single import line in the imports section near the top of the file:

      `from graph_wiki_agent.prompts.ingestor import INGESTOR_SYSTEM  # noqa: F401`

    The `noqa: F401` is included for safety in case the name appears only in re-export but not in direct in-module usage. If `SystemMessage(content=INGESTOR_SYSTEM)` is used in this file, the noqa is harmless; remove it only if ruff flags it.

    If the module docstring lists INGESTOR_SYSTEM as part of the public API, update the docstring line to note "(re-exported from `graph_wiki_agent.prompts.ingestor`)". Do NOT change anything else in `commands/ingest.py` — no behavior change, no refactor of call sites. The `SystemMessage(content=INGESTOR_SYSTEM)` (or equivalent) usage at the call site remains identical.

    Run any existing ingest-related tests to confirm no regressions.
  </action>
  <verify>
    <automated>uv run pytest agents/graph-wiki-agent/tests/ -x -q -k 'ingest' 2>&1 | tail -15 && uv run python -c "from graph_wiki_agent.commands.ingest import INGESTOR_SYSTEM; from graph_wiki_agent.prompts.ingestor import INGESTOR_SYSTEM as P_ING; assert INGESTOR_SYSTEM is P_ING, 'identity mismatch'; print('OK')" && (! grep -E '^INGESTOR_SYSTEM[[:space:]]*=' agents/graph-wiki-agent/src/graph_wiki_agent/commands/ingest.py)</automated>
  </verify>
  <acceptance_criteria>
    - `commands/ingest.py` no longer defines `INGESTOR_SYSTEM` as an inline string literal (the grep above returns no matches in that file).
    - The name is imported from `graph_wiki_agent.prompts.ingestor` and re-exposed from `commands.ingest` for backward compatibility.
    - `commands.ingest.INGESTOR_SYSTEM is prompts.ingestor.INGESTOR_SYSTEM` (Python identity — proves no duplication).
    - Existing ingest-related tests continue to pass.
    - Behavior is unchanged: no call site or runtime logic is modified.
  </acceptance_criteria>
  <done>
    Inline constant deleted; import wired; ingest-related tests green; identity check passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Prompt source -> LLM input | Prompt assembled at import time as immutable string; no user-injected content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09 | Tampering | Output-shape contract (YAML-frontmatter-only) drift | mitigate | Acceptance criteria assert the "Output ONLY YAML frontmatter" instruction remains; snapshot test freezes the composed prompt byte-for-byte |
| T-06-10 | Information Disclosure | Host-specific references leaking into composed prompt | mitigate | Acceptance criteria assert absence of `CLAUDE_PLUGIN_ROOT`, `/lattice-wiki:`, `Wait for confirmation`, `obsidian-markdown` substrings per RESEARCH Adaptation Map |
</threat_model>

<verification>
- `pytest agents/graph-wiki-agent/tests/prompts/test_prompt_snapshots.py::test_ingestor_system_snapshot -x` passes
- `commands/ingest.py` no longer defines INGESTOR_SYSTEM inline
- `commands.ingest.INGESTOR_SYSTEM is prompts.ingestor.INGESTOR_SYSTEM` (identity)
- Existing ingest-related tests still pass
</verification>

<success_criteria>
- PORT-03 delivered: ingestor prompt composed from canonical fragments + adapted role-local prose
- commands/ingest.py refactored to import-based prompt sourcing
- All adaptation-map host-specific references removed from composed prompt
- Output-shape contract (YAML-only output) preserved
</success_criteria>

<output>
Create `.planning/phases/06-prompt-content-port-divergence-eval/06-05-SUMMARY.md` when done.
</output>
