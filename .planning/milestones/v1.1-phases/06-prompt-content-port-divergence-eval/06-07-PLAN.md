---
phase: 06-prompt-content-port-divergence-eval
plan: 07
type: execute
wave: 3
depends_on: [06-03]
files_modified:
  - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/scanner.py
  - agents/graph-wiki-agent/src/graph_wiki_agent/commands/scan.py
autonomous: true
requirements: [PORT-05]
tags: [prompt-port, scanner, scan-command]

must_haves:
  truths:
    - "SCANNER_SYSTEM is composed from IRON_RULES + FRONTMATTER_RULES + scanner-local rules at import time"
    - "SCANNER_SYSTEM preserves: required stub frontmatter fields (title, category, summary, package_path, language, version, depends_on, exports), `## Overview` + `## Notable files` section requirements, the explicit no-`## File map` rule (added by pipeline)"
    - "SCANNER_SYSTEM contains the don't-overwrite-prose rule, the confirm-renames-and-deletions rule, the only-stub-actual-workspace-entries rule from scanner.md"
    - "SCANNER_SYSTEM removes host-specific references (scan_monorepo.py, update_index.py, update_tokens.py, append_log.py script calls) per RESEARCH §Adaptation Map"
    - "commands/scan.py imports SCANNER_SYSTEM from prompts.scanner instead of defining it inline"
  artifacts:
    - path: "agents/graph-wiki-agent/src/graph_wiki_agent/prompts/scanner.py"
      provides: "SCANNER_SYSTEM composed string"
      contains: "SCANNER_SYSTEM"
    - path: "agents/graph-wiki-agent/src/graph_wiki_agent/commands/scan.py"
      provides: "Scan command with import-based prompt sourcing"
  key_links:
    - from: "prompts/scanner.py"
      to: "_fragments/{iron_rules, frontmatter_rules}"
      via: "Python imports at module top"
      pattern: "from graph_wiki_agent.prompts._fragments"
    - from: "commands/scan.py"
      to: "prompts.scanner"
      via: "import replacing inline constant"
      pattern: "from graph_wiki_agent.prompts.scanner import SCANNER_SYSTEM"
---

<objective>
Port the scanner system prompt content from `cores/prompt-sources/agents/scanner.md` (plus shared fragments) into `prompts/scanner.py`, then swap the inline `SCANNER_SYSTEM` constant in `commands/scan.py` for an import.

Purpose: Completes PORT-05. Scanner is the lowest token-budget role (~115 tokens currently). Port must preserve that calibration target (<400 tokens composed) while adding canonical package-detection and don't-overwrite-prose rules from scanner.md.

Output: One new `prompts/scanner.py` + refactored `commands/scan.py` importing it. Snapshot test for `SCANNER_SYSTEM` recorded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-prompt-content-port-divergence-eval/06-CONTEXT.md
@.planning/phases/06-prompt-content-port-divergence-eval/06-RESEARCH.md
@.planning/phases/06-prompt-content-port-divergence-eval/06-PATTERNS.md
@cores/prompt-sources/agents/scanner.md
@agents/graph-wiki-agent/src/graph_wiki_agent/commands/scan.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create prompts/scanner.py composing fragments + scanner-local content</name>
  <files>
    agents/graph-wiki-agent/src/graph_wiki_agent/prompts/scanner.py
  </files>
  <read_first>
    - cores/prompt-sources/agents/scanner.md (full file — workflow, rules, red flags)
    - agents/graph-wiki-agent/src/graph_wiki_agent/commands/scan.py (lines 92-115 — existing SCANNER_SYSTEM constant; this is the calibration baseline at ~115 tokens)
    - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/iron_rules.py
    - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/frontmatter_rules.py
    - .planning/phases/06-prompt-content-port-divergence-eval/06-PATTERNS.md (section: prompts/scanner.py)
    - .planning/phases/06-prompt-content-port-divergence-eval/06-RESEARCH.md (sections: Source File Content Analysis -> Scanner-local, Adaptation Map)
  </read_first>
  <behavior>
    - SCANNER_SYSTEM is built at import time via `"\n\n".join([...])` (per D-02)
    - SCANNER_SYSTEM contains all required stub frontmatter fields: `title`, `category`, `summary`, `package_path`, `language`, `version`, `depends_on`, `exports` (matches existing constant at lines 92-115)
    - SCANNER_SYSTEM contains `## Overview` and `## Notable files` section requirements
    - SCANNER_SYSTEM contains the explicit no-`## File map` rule (added by pipeline; LLM must NOT generate it — this is what SCN-003 divergence check in 06-08 validates)
    - SCANNER_SYSTEM contains "Don't overwrite prose" or equivalent canonical wording
    - SCANNER_SYSTEM does NOT contain `CLAUDE_PLUGIN_ROOT`, `/lattice-wiki:`, `scan_monorepo.py`, `update_index.py`, `update_tokens.py`, `append_log.py` script references
    - Total composed length <2500 chars (token budget proxy for ~400 tokens)
    - Snapshot test `test_scanner_system_snapshot` records on first run
  </behavior>
  <action>
    Create `prompts/scanner.py`: `from __future__ import annotations` line 1. Imports:
      `from graph_wiki_agent.prompts._fragments.iron_rules import IRON_RULES`
      `from graph_wiki_agent.prompts._fragments.frontmatter_rules import FRONTMATTER_RULES`

    Per RESEARCH, scanner does NOT use PAGE_CATEGORIES (scanner stubs are always `category: package` or `app`) or CITATION_RULES (scanner stubs do not contain freeform claims that need citation).

    Build `SCANNER_SYSTEM = "\n\n".join([...])` per PATTERNS.md (section: prompts/scanner.py). Sections in order:
      1. Role intro (role-local, adapted from existing scan.py constant). Preserve: "You are a code wiki scanner", "Your job is to write a concise stub page for a software package", and the explicit "Produce ONLY the page body with YAML frontmatter" rule.
      2. IRON_RULES.
      3. FRONTMATTER_RULES.
      4. Scanner stub schema (role-local). Preserve the exact field list from the existing constant: `title`, `category` (package or app), `summary`, `package_path`, `language`, `version` (optional), `depends_on` (list), `exports` (list). Preserve the explicit no-`## File map` rule — state both "do NOT include a `## File map` section" and the reason "that is added separately by the build pipeline".
      5. Output sections requirement: ONE short `## Overview` (3-5 sentences) + ONE short `## Notable files` (2-4 key files with one-line descriptions each).
      6. Scanner rules (role-local, adapted from scanner.md Rules). Preserve verbatim: "Don't overwrite prose", "Confirm renames and deletions", "Only stub actual workspace entries", "Dependency-only frontmatter updates don't need confirmation".
      7. Red flags (role-local, adapted from scanner.md Red flags — 3 items, portable verbatim per RESEARCH).
      8. Token budget reminder (role-local): "Keep total output under 380 tokens. Do NOT speculate beyond what the provided file listing shows." Preserve verbatim from existing constant — these constraints are downstream-relevant.

    Adapt away (per RESEARCH Adaptation Map): all `${CLAUDE_PLUGIN_ROOT}/.../scan_monorepo.py`, `update_index.py`, `update_tokens.py`, `append_log.py` script references — those calls are made by the command layer (`commands/scan.py::run_scan`).

    Per RESEARCH §Open Questions (1) and §Assumptions Log (A3-verified): `update_tokens.py` DOES exist in vault-io but is called by the command layer, not the LLM. The scanner prompt should NOT mention it (the LLM has no role in invoking it).

    Token budget: target <400 tokens (~2500 chars). Existing constant is ~115 tokens; composition with IRON_RULES + FRONTMATTER_RULES expands it but should still fit.

    After writing, run `pytest agents/graph-wiki-agent/tests/prompts/test_prompt_snapshots.py::test_scanner_system_snapshot --snapshot-update -x` once to record the initial snapshot.
  </action>
  <verify>
    <automated>uv run python -c "from graph_wiki_agent.prompts.scanner import SCANNER_SYSTEM; assert 'package_path' in SCANNER_SYSTEM; assert 'language' in SCANNER_SYSTEM; assert 'depends_on' in SCANNER_SYSTEM; assert 'Overview' in SCANNER_SYSTEM and 'Notable files' in SCANNER_SYSTEM; assert 'File map' in SCANNER_SYSTEM and 'do NOT' in SCANNER_SYSTEM or 'do not' in SCANNER_SYSTEM.lower(); assert 'Iron rules' in SCANNER_SYSTEM; assert 'prose' in SCANNER_SYSTEM.lower(); assert 'CLAUDE_PLUGIN_ROOT' not in SCANNER_SYSTEM; assert '/lattice-wiki:' not in SCANNER_SYSTEM; assert 'scan_monorepo.py' not in SCANNER_SYSTEM; assert 'append_log.py' not in SCANNER_SYSTEM; print('OK len=', len(SCANNER_SYSTEM))" && uv run pytest agents/graph-wiki-agent/tests/prompts/test_prompt_snapshots.py::test_scanner_system_snapshot -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `prompts/scanner.py` importable; exports `SCANNER_SYSTEM` non-empty string.
    - Composed via `"\n\n".join([...])` at import time.
    - Contains all stub frontmatter fields: `title`, `category`, `summary`, `package_path`, `language`, `depends_on`, `exports`.
    - Contains `## Overview` and `## Notable files` section requirements.
    - Contains the no-`## File map` rule (substring `File map` plus a negation context).
    - Contains the "don't overwrite prose" rule (substring `prose` in a negation context).
    - Contains IRON_RULES content (substring `Iron rules`).
    - Does NOT contain: `CLAUDE_PLUGIN_ROOT`, `/lattice-wiki:`, `scan_monorepo.py`, `update_index.py`, `update_tokens.py`, `append_log.py`.
    - Snapshot test passes (after one-time `--snapshot-update`).
    - Length <2500 chars (token budget proxy).
  </acceptance_criteria>
  <done>
    `prompts/scanner.py` composes from fragments + scanner-local prose; snapshot recorded; adaptation map honored; all canonical scanner invariants (frontmatter fields, no-File-map, don't-overwrite-prose) preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Swap inline SCANNER_SYSTEM in commands/scan.py for an import</name>
  <files>
    agents/graph-wiki-agent/src/graph_wiki_agent/commands/scan.py
  </files>
  <read_first>
    - agents/graph-wiki-agent/src/graph_wiki_agent/commands/scan.py (full file — note SCANNER_SYSTEM definition at lines ~92-115, the call site at line ~329 that consumes it)
    - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/scanner.py (just created)
    - .planning/phases/06-prompt-content-port-divergence-eval/06-PATTERNS.md (section: commands/scan.py)
  </read_first>
  <action>
    In `commands/scan.py`, delete the inline `SCANNER_SYSTEM = """\..."""` block (lines ~92-115). Replace with a single import line in the imports section:

      `from graph_wiki_agent.prompts.scanner import SCANNER_SYSTEM  # noqa: F401`

    The `noqa: F401` is included for safety; remove only if ruff flags it.

    If the module docstring lists SCANNER_SYSTEM as part of the public API, update it to note "(re-exported from `graph_wiki_agent.prompts.scanner`)". Do NOT change anything else in `commands/scan.py` — call site at line ~329 (`SystemMessage(content=SCANNER_SYSTEM)` or equivalent) is unchanged.

    Run scan-related tests to confirm no regressions.
  </action>
  <verify>
    <automated>uv run pytest agents/graph-wiki-agent/tests/ -x -q -k 'scan' 2>&1 | tail -15 && uv run python -c "from graph_wiki_agent.commands.scan import SCANNER_SYSTEM; from graph_wiki_agent.prompts.scanner import SCANNER_SYSTEM as P_SCN; assert SCANNER_SYSTEM is P_SCN, 'identity mismatch'; print('OK')" && (! grep -E '^SCANNER_SYSTEM[[:space:]]*=' agents/graph-wiki-agent/src/graph_wiki_agent/commands/scan.py)</automated>
  </verify>
  <acceptance_criteria>
    - `commands/scan.py` no longer defines `SCANNER_SYSTEM` as an inline string literal.
    - The name is imported from `graph_wiki_agent.prompts.scanner` and re-exposed.
    - Identity check passes: `commands.scan.SCANNER_SYSTEM is prompts.scanner.SCANNER_SYSTEM`.
    - Existing scan-related tests continue to pass.
    - Behavior is unchanged: no call site or runtime logic is modified.
  </acceptance_criteria>
  <done>
    Inline constant deleted; import wired; scan tests green; identity check passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Prompt source -> LLM input | Prompt assembled at import time as immutable string |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-13 | Tampering | Pipeline contract drift (LLM-generated `## File map`) | mitigate | SCANNER_SYSTEM explicitly states "do NOT include `## File map`"; SCN-003 divergence check in 06-08 enforces this on agent outputs; snapshot test freezes the composed prompt |
| T-06-14 | Information Disclosure | Token budget blow-up degrading scan throughput | mitigate | Acceptance criteria caps composed prompt at <2500 chars; existing constant calibration of ~115 tokens is the baseline RESEARCH §Pitfall 1 |
</threat_model>

<verification>
- Scanner snapshot test passes
- `commands/scan.py` no longer defines SCANNER_SYSTEM inline
- Identity check passes
- Existing scan-related tests still pass
</verification>

<success_criteria>
- PORT-05 delivered: scanner prompt composed from canonical fragments + adapted role-local prose
- commands/scan.py refactored to import-based prompt sourcing
- All adaptation-map host-specific references removed from composed prompt
- Pipeline contract (no LLM-generated `## File map`) and don't-overwrite-prose rule preserved
- Token budget stays under ~400 tokens
</success_criteria>

<output>
Create `.planning/phases/06-prompt-content-port-divergence-eval/06-07-SUMMARY.md` when done.
</output>
