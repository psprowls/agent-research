---
phase: 06-prompt-content-port-divergence-eval
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cores/prompt-sources/SKILL.md
  - cores/prompt-sources/agents/librarian.md
  - cores/prompt-sources/agents/ingestor.md
  - cores/prompt-sources/agents/linter.md
  - cores/prompt-sources/agents/scanner.md
  - cores/prompt-sources/references/
autonomous: true
requirements: [PORT-01]
tags: [prompt-port, vendoring, source-of-truth]

must_haves:
  truths:
    - "Canonical lattice-wiki SKILL.md and 4 per-role agent .md files exist verbatim under cores/prompt-sources/"
    - "The vendored directory is NOT a uv workspace member"
    - "The upstream lattice commit SHA at the time of vendoring is recorded for use in provenance headers"
  artifacts:
    - path: "cores/prompt-sources/SKILL.md"
      provides: "Canonical iron rules + page categories source-of-truth"
      contains: "## Iron rules"
    - path: "cores/prompt-sources/agents/librarian.md"
      provides: "Canonical librarian workflow + rules + red flags"
    - path: "cores/prompt-sources/agents/ingestor.md"
      provides: "Canonical ingestor workflow + page-type routing"
    - path: "cores/prompt-sources/agents/linter.md"
      provides: "Canonical 3-pass linter rule definitions"
    - path: "cores/prompt-sources/agents/scanner.md"
      provides: "Canonical scanner workflow + package-detection rules"
  key_links:
    - from: "cores/prompt-sources/"
      to: "workspace root pyproject.toml"
      via: "must NOT be matched as a workspace member"
      pattern: "no pyproject.toml in cores/prompt-sources/"
---

<objective>
Vendor the canonical lattice-wiki source-of-truth files verbatim into the agent-research repo so all subsequent prompt-port work in Phase 6 references in-repo paths (not the sibling lattice checkout). Record the upstream commit SHA for provenance headers.

Purpose: Decouples the agent package from the sibling lattice repo (D-04) and makes drift detection a trivial in-repo diff. Required prerequisite for every fragment file written in 06-03..06-07 (provenance headers point at `cores/prompt-sources/...`).

Output: `cores/prompt-sources/` directory with SKILL.md, agents/{librarian,ingestor,linter,scanner}.md, and a snapshot of `references/` — verbatim copies of files from `/Users/pat/Personal/lattice/plugins/lattice-wiki/`. Plus the upstream commit SHA captured in a top-of-tree `SOURCE-COMMIT` marker file for later headers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-prompt-content-port-divergence-eval/06-CONTEXT.md
@.planning/phases/06-prompt-content-port-divergence-eval/06-RESEARCH.md
@.planning/phases/06-prompt-content-port-divergence-eval/06-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vendor lattice-wiki canonical sources into cores/prompt-sources/</name>
  <files>
    cores/prompt-sources/SKILL.md,
    cores/prompt-sources/agents/librarian.md,
    cores/prompt-sources/agents/ingestor.md,
    cores/prompt-sources/agents/linter.md,
    cores/prompt-sources/agents/scanner.md,
    cores/prompt-sources/references/
  </files>
  <read_first>
    - /Users/pat/Personal/lattice/plugins/lattice-wiki/skills/lattice-wiki/SKILL.md
    - /Users/pat/Personal/lattice/plugins/lattice-wiki/agents/librarian.md
    - /Users/pat/Personal/lattice/plugins/lattice-wiki/agents/ingestor.md
    - /Users/pat/Personal/lattice/plugins/lattice-wiki/agents/linter.md
    - /Users/pat/Personal/lattice/plugins/lattice-wiki/agents/scanner.md
    - /Users/pat/Personal/agent-research/pyproject.toml (verify members glob)
  </read_first>
  <action>
    Create `cores/prompt-sources/` as a bare content directory (per D-04 and RESEARCH §Pitfall 3). Copy verbatim from `/Users/pat/Personal/lattice/plugins/lattice-wiki/`:
      - `skills/lattice-wiki/SKILL.md` -> `cores/prompt-sources/SKILL.md`
      - `agents/librarian.md` -> `cores/prompt-sources/agents/librarian.md`
      - `agents/ingestor.md` -> `cores/prompt-sources/agents/ingestor.md`
      - `agents/linter.md` -> `cores/prompt-sources/agents/linter.md`
      - `agents/scanner.md` -> `cores/prompt-sources/agents/scanner.md`
      - `skills/lattice-wiki/references/` -> `cores/prompt-sources/references/` (entire directory, recursive)
    Use `cp -R` so file mtimes are reasonable but content is byte-identical. Do NOT create a `pyproject.toml`, `__init__.py`, or any Python file in `cores/prompt-sources/` — the workspace member glob is `cores/*` and any package metadata file would force uv to treat this as a workspace member (RESEARCH §Pitfall 3, Anti-Patterns).
    Capture the upstream SHA: run `git -C /Users/pat/Personal/lattice log -1 --format=%H -- plugins/lattice-wiki` and write the 7-char prefix to `cores/prompt-sources/SOURCE-COMMIT` (a plain text marker file, one line, no trailing newline beyond the SHA-newline). This file is the canonical reference for the `# Source-commit:` line in every fragment header written in 06-03..06-07.
    Verify the workspace member glob from `pyproject.toml` (`members = ["cores/*", "agents/*"]`) does NOT pick up `prompt-sources` by running `uv sync` and confirming it succeeds.
  </action>
  <verify>
    <automated>test -f cores/prompt-sources/SKILL.md && test -f cores/prompt-sources/agents/librarian.md && test -f cores/prompt-sources/agents/ingestor.md && test -f cores/prompt-sources/agents/linter.md && test -f cores/prompt-sources/agents/scanner.md && test -f cores/prompt-sources/SOURCE-COMMIT && test ! -f cores/prompt-sources/pyproject.toml && test ! -f cores/prompt-sources/__init__.py && diff -q cores/prompt-sources/SKILL.md /Users/pat/Personal/lattice/plugins/lattice-wiki/skills/lattice-wiki/SKILL.md && diff -q cores/prompt-sources/agents/librarian.md /Users/pat/Personal/lattice/plugins/lattice-wiki/agents/librarian.md && diff -q cores/prompt-sources/agents/ingestor.md /Users/pat/Personal/lattice/plugins/lattice-wiki/agents/ingestor.md && diff -q cores/prompt-sources/agents/linter.md /Users/pat/Personal/lattice/plugins/lattice-wiki/agents/linter.md && diff -q cores/prompt-sources/agents/scanner.md /Users/pat/Personal/lattice/plugins/lattice-wiki/agents/scanner.md && uv sync 2>&1 | grep -v -i 'error\|fail' | head -1</automated>
  </verify>
  <acceptance_criteria>
    - All 5 source files exist under `cores/prompt-sources/` and are byte-identical to the lattice repo originals (verified via `diff -q`).
    - `cores/prompt-sources/SOURCE-COMMIT` contains the upstream lattice commit SHA (7-char prefix or full).
    - No `pyproject.toml`, `__init__.py`, or any Python file exists under `cores/prompt-sources/`.
    - `uv sync` completes without error (proves the directory is not erroneously picked up as a workspace member).
    - `cores/prompt-sources/references/` exists and contains the subdirectory tree from the lattice references folder.
  </acceptance_criteria>
  <done>
    All 5 vendored .md files diff-clean against lattice sources; `cores/prompt-sources/SOURCE-COMMIT` exists with valid SHA; `uv sync` succeeds; no Python package metadata in the vendored directory.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lattice sibling repo -> agent-research repo | One-time read of files from `/Users/pat/Personal/lattice/`, copied verbatim into versioned content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | Vendored .md content in `cores/prompt-sources/` | mitigate | Files are static content read by humans for provenance and copied verbatim from a known-good local checkout; no remote fetch; committed to git for integrity |
| T-06-02 | Tampering | Workspace collision (pyproject.toml in vendored dir) | mitigate | Acceptance criteria explicitly checks for absence of `pyproject.toml`/`__init__.py`; `uv sync` is run to confirm no collision (RESEARCH §Pitfall 3) |
</threat_model>

<verification>
- All 5 vendored files byte-identical to lattice sources (`diff -q`)
- `cores/prompt-sources/SOURCE-COMMIT` exists and contains a hex SHA
- `uv sync` succeeds (no workspace member collision)
- No Python metadata under `cores/prompt-sources/`
</verification>

<success_criteria>
- Vendored files in place at `cores/prompt-sources/` matching lattice source verbatim
- `SOURCE-COMMIT` marker file exists with upstream SHA captured (suggested current SHA prefix: `37a7e9a`)
- Workspace remains syncable; no Python module footprint in the vendored content
- Fragment files in 06-03 can reference `cores/prompt-sources/SKILL.md` etc. in their `# Source:` headers
</success_criteria>

<output>
Create `.planning/phases/06-prompt-content-port-divergence-eval/06-01-SUMMARY.md` when done.
</output>
