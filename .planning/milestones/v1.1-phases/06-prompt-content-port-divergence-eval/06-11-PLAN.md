---
phase: 06-prompt-content-port-divergence-eval
plan: 11
type: execute
wave: 7
depends_on: [06-10]
files_modified:
  - cores/eval-harness/tests/test_divergence.py
  - cores/eval-harness/tests/conftest.py
autonomous: true
requirements: [EVAL-12, EVAL-13]
tags: [divergence-eval, integration-test, eval-gate, pytest-evals, end-to-end]

must_haves:
  truths:
    - "test_divergence.py is parametrized over the 4 roles (librarian, ingestor, linter, scanner)"
    - "The test runs the full DivergenceMetric.run() — programmatic + judge — against the Phase 4 fixture corpus when GRAPH_WIKI_RUN_EVAL=1"
    - "Without --accept-divergence-baseline, the test calls check_regression which compares current results against the role's baseline JSON; gate fails if any hard-severity rule regresses"
    - "With --accept-divergence-baseline, the test calls write_baseline to overwrite the role's JSON with current results and returns without raising"
    - "The test produces a per-role report (printed or written) that includes per-rule failure counts and the first few accepted_failures excerpts (EVAL-12 'concrete examples' requirement)"
    - "Without GRAPH_WIKI_RUN_EVAL=1 the test SKIPs cleanly (no Bedrock calls attempted)"
  artifacts:
    - path: "cores/eval-harness/tests/test_divergence.py"
      provides: "End-to-end divergence eval gate test parametrized over 4 roles"
    - path: "cores/eval-harness/tests/conftest.py"
      modifies: "Adds fixtures for resolving the fixture vault corpus + role -> agent-output-producer mapping (if not already present)"
  key_links:
    - from: "test_divergence.py"
      to: "DivergenceMetric.run() + load_baseline() + check_regression() + write_baseline()"
      via: "import + parametrize on role"
      pattern: "from eval_harness.divergence"
    - from: "test_divergence.py"
      to: "cores/vault-io/tests/fixtures/round-trip-vault (Phase 4 fixture corpus)"
      via: "fixture path resolution"
      pattern: "Path(__file__).parent.parent.parent.parent"
---

<objective>
Wire the divergence metric end-to-end into a single eval-gated pytest test parametrized over the 4 roles. Without the env-var gate, the test skips; with it, the test produces real agent outputs (via the existing query/ingest/lint/scan commands), runs `DivergenceMetric.run()`, compares against the baseline (or rewrites it if --accept-divergence-baseline was passed), and reports per-role counts plus example failures.

Purpose: Closes EVAL-12 (per-role divergence counts + concrete examples in the report) and EVAL-13 (regression gate against recorded baseline). The first real run on Bedrock will produce the v1.1 starting baseline; Phase 7's sweep then measures against it.

Output: One new integration test file (replacing the 06-02 scaffold) gated by `GRAPH_WIKI_RUN_EVAL=1` plus minor conftest.py fixture additions if needed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-prompt-content-port-divergence-eval/06-CONTEXT.md
@.planning/phases/06-prompt-content-port-divergence-eval/06-RESEARCH.md
@.planning/phases/06-prompt-content-port-divergence-eval/06-PATTERNS.md
@cores/eval-harness/tests/eval/test_sweep_eval.py
@cores/eval-harness/tests/conftest.py
@cores/eval-harness/src/eval_harness/divergence/__init__.py
@cores/eval-harness/src/eval_harness/divergence/metric.py
@cores/eval-harness/tests/test_divergence.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add agent-output-producer fixtures to conftest.py (per-role)</name>
  <files>
    cores/eval-harness/tests/conftest.py
  </files>
  <read_first>
    - cores/eval-harness/tests/conftest.py (existing EVAL_GATE, fixture_vault_path, accept_baseline option from 06-02)
    - cores/eval-harness/tests/eval/test_sweep_eval.py (how the sweep test produces agent outputs — pattern to follow)
    - agents/graph-wiki-agent/src/graph_wiki_agent/commands/query.py (run_query signature)
    - agents/graph-wiki-agent/src/graph_wiki_agent/commands/ingest.py (run_ingest signature)
    - agents/graph-wiki-agent/src/graph_wiki_agent/commands/lint.py (run_lint signature)
    - agents/graph-wiki-agent/src/graph_wiki_agent/commands/scan.py (run_scan signature)
    - cores/eval-harness/src/eval_harness/divergence/check.py (AgentOutputProxy shape)
  </read_first>
  <action>
    Extend `cores/eval-harness/tests/conftest.py` with a fixture `divergence_outputs(role, fixture_vault_path)` that produces the `list[tuple[str, AgentOutputProxy, str]]` (fixture_id, output, query) shape expected by `DivergenceMetric.run()`. Implementation strategy:
      - The fixture is parametrized indirectly via the test that passes a role string.
      - For `librarian`: run the existing query command against 2-3 fixture queries from the Phase 4 corpus (reuse the cases used by `test_sweep_eval.py` if available), wrap each `QueryResult.answer` in `AgentOutputProxy(answer=result.answer)`. fixture_id is the case_id from the existing fixture corpus.
      - For `ingestor`: run `run_ingest` against 1-2 sample source documents from the fixture corpus; the third tuple element (query) is the source path or a short description; AgentOutputProxy.answer is the LLM output (frontmatter + body); page_type is extracted from the parsed frontmatter if present.
      - For `linter`: run `run_lint` against the round-trip vault; one output per group prompt (page_quality, adr_chain, stale_claims). The third tuple element (query) is the group name. AgentOutputProxy.answer is the findings text.
      - For `scanner`: run `run_scan` against a small fixture monorepo (use the existing `cores/eval-harness/tests/fixtures/` scanner fixture if present, else `cores/vault-io/tests/fixtures/round-trip-vault`'s associated source tree); AgentOutputProxy.answer is the generated stub page (frontmatter + body).

    Document the fixture corpus assumption explicitly: if a per-role corpus fixture is missing, the fixture should `pytest.skip(f"No fixture corpus for role {role}; add fixtures under cores/eval-harness/tests/fixtures/divergence/{role}/")`. Do NOT fabricate corpus content here.

    If `test_sweep_eval.py` already loads a query corpus from a JSON or YAML file, reuse that loader rather than reimplementing. Look for any existing `eval_cases.json` or `cases.yaml` style file under `cores/eval-harness/tests/`.

    The fixture is decorated as eval-gated (`@EVAL_GATE` if applied at fixture level via pytest.skipif, or guarded inside the fixture by `if not os.environ.get("GRAPH_WIKI_RUN_EVAL"): pytest.skip(...)`). This prevents accidental Bedrock invocations when running the broader suite.

    Add the fixture as a callable helper rather than a parametrized fixture if cleanest: `def _produce_outputs(role, vault) -> list[tuple[str, AgentOutputProxy, str]]: ...` exported from conftest.py and imported by `test_divergence.py`. Either pattern is acceptable; pick whichever fits the existing conftest.py style.
  </action>
  <verify>
    <automated>uv run pytest cores/eval-harness/tests/test_divergence.py --collect-only -q 2>&1 | tail -10 && uv run python -c "from cores.eval_harness.tests.conftest import _produce_outputs  # noqa" 2>&1 | head -3 || uv run python -c "import importlib; m = importlib.import_module('conftest'); print(dir(m))" 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `conftest.py` exposes either a fixture or a helper function that produces `list[tuple[str, AgentOutputProxy, str]]` per role.
    - The helper guards on `GRAPH_WIKI_RUN_EVAL=1` — calling it without the env var either skips the test or raises a clear error.
    - The helper documents (in docstring) what fixture corpus is expected at what path; missing fixtures produce a `pytest.skip` with a path-pointing message, not a crash.
    - Existing conftest.py content (EVAL_GATE, accept_baseline, fixture_vault_path) is preserved unchanged.
  </acceptance_criteria>
  <done>
    Per-role output-producer fixture / helper added to conftest.py; existing fixtures preserved; missing-corpus path skips gracefully.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement the eval-gated divergence regression test in test_divergence.py</name>
  <files>
    cores/eval-harness/tests/test_divergence.py
  </files>
  <read_first>
    - cores/eval-harness/tests/test_divergence.py (scaffold from 06-02)
    - cores/eval-harness/tests/eval/test_sweep_eval.py (full file — EVAL_GATE pattern, parametrize style, path resolution)
    - cores/eval-harness/tests/conftest.py (after Task 1 — the new helper/fixture)
    - cores/eval-harness/src/eval_harness/divergence/__init__.py (ROLE_CHECKS, ROLE_RUBRICS)
    - cores/eval-harness/src/eval_harness/divergence/metric.py (DivergenceMetric + load_baseline + write_baseline + check_regression)
    - .planning/phases/06-prompt-content-port-divergence-eval/06-PATTERNS.md (section: eval-harness/tests/test_divergence.py)
    - .planning/phases/06-prompt-content-port-divergence-eval/06-RESEARCH.md (pytest Divergence Gate Test code example)
  </read_first>
  <action>
    Replace the 06-02 scaffold body of `cores/eval-harness/tests/test_divergence.py` with the production test. Structure:

    Module-level:
      `from __future__ import annotations`
      `import os, json, subprocess`
      `from pathlib import Path`
      `import pytest`
      `from eval_harness.divergence import ROLE_CHECKS, ROLE_RUBRICS`
      `from eval_harness.divergence.metric import DivergenceMetric, load_baseline, write_baseline, check_regression, summarize`
      (Import the Task 1 helper from conftest, or use pytest fixture injection.)

      `EVAL_GATE = pytest.mark.skipif(not os.environ.get("GRAPH_WIKI_RUN_EVAL"), reason="Set GRAPH_WIKI_RUN_EVAL=1 to run divergence eval")`
      `BASELINES_DIR = Path(__file__).parent.parent / "baselines"`

    Helper to get agent commit SHA:
      `def _current_agent_commit() -> str: return subprocess.check_output(["git", "rev-parse", "--short", "HEAD"], text=True).strip()` (with try/except returning `"unknown"`).

    Main test:
      `@EVAL_GATE`
      `@pytest.mark.parametrize("role", ["librarian", "ingestor", "linter", "scanner"])`
      `def test_divergence_regression(role, fixture_vault_path, accept_baseline, capsys):`
        `outputs = _produce_outputs(role, fixture_vault_path)`  # from conftest
        `metric = DivergenceMetric(role=role, checks=ROLE_CHECKS[role], rubric_path=ROLE_RUBRICS[role], vault=fixture_vault_path)`
        `results = metric.run(outputs)`
        # EVAL-12 "concrete examples" — print the report so pytest -s shows it
        `print(f"\\n=== Divergence report: {role} ===")`
        `for rule_id, data in results.items():`
        `    print(f"  {rule_id}: runs={data['runs']} failures={data['failures']}")`
        `    for f in data['accepted_failures'][:3]:  # first 3 examples`
        `        print(f"    - [{f['fixture']}] {f['excerpt']}")`
        `if accept_baseline:`
        `    write_baseline(role, BASELINES_DIR, results, _current_agent_commit())`
        `    return`
        `baseline = load_baseline(role, BASELINES_DIR)`
        `check_regression(role, results, baseline)`

    No additional assertions — the gate is the AssertionError raised by check_regression. The test passes silently (with printed report visible under `-s`) when there is no hard-severity regression.

    Run the test once with `GRAPH_WIKI_RUN_EVAL=1 uv run pytest cores/eval-harness/tests/test_divergence.py --accept-divergence-baseline -s` to record the v1.1 starting baseline that replaces the empty initial baselines from 06-10. Commit the resulting baseline diffs as part of the SUMMARY.

    If running on Bedrock fails (credentials, model access, throttling), the test should surface the underlying error — do NOT swallow exceptions. The user can re-run after fixing the environment.
  </action>
  <verify>
    <automated>uv run pytest cores/eval-harness/tests/test_divergence.py --collect-only -q 2>&1 | tail -10 && uv run pytest cores/eval-harness/tests/test_divergence.py -x -q 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `test_divergence.py` defines exactly one `@EVAL_GATE @parametrize("role", ["librarian", "ingestor", "linter", "scanner"])` test function.
    - Test calls `DivergenceMetric.run()` and either `write_baseline` (on `--accept-divergence-baseline`) or `check_regression` (default).
    - Without `GRAPH_WIKI_RUN_EVAL=1`, all 4 parametrized cases SKIP (no Bedrock call attempted) — verified by the verify-block `uv run pytest cores/eval-harness/tests/test_divergence.py -x` reporting 4 skipped, 0 failed.
    - Test prints per-rule failure counts and example failures (EVAL-12 "concrete examples in the report" requirement).
    - After running once with `GRAPH_WIKI_RUN_EVAL=1 --accept-divergence-baseline`, the 4 baseline JSON files in `cores/eval-harness/baselines/` are updated with real failure counts and `agent_commit` set to the current git SHA.
  </acceptance_criteria>
  <done>
    End-to-end divergence test in place; collects under default pytest; skips cleanly without `GRAPH_WIKI_RUN_EVAL`; produces a real per-role report when run with the env var; baseline files are updated when `--accept-divergence-baseline` is passed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bedrock credentials -> agent calls in test | Required when `GRAPH_WIKI_RUN_EVAL=1`; sourced from environment |
| `--accept-divergence-baseline` flag -> file rewrite | Explicit human-confirmed action that overwrites the baseline |
| Test output (stdout) -> CI logs | Excerpts from accepted_failures appear in test output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-24 | Elevation of Privilege (cost) | Accidental judge invocation without env gate | mitigate | `EVAL_GATE` skipif on test + `GRAPH_WIKI_RUN_EVAL=1` guard in the output-producer fixture; without the env var, no Bedrock calls happen |
| T-06-25 | Information Disclosure | Bedrock errors leak credentials in test logs | mitigate | The test does not catch underlying exceptions; standard boto3 error messages do not include secrets; user is responsible for not committing trace logs |
| T-06-26 | Repudiation | Baseline acceptance without recorded SHA | mitigate | `_current_agent_commit()` writes the git SHA into every accepted baseline; reviewer can trace baseline drift |
</threat_model>

<verification>
- Without `GRAPH_WIKI_RUN_EVAL=1`: `uv run pytest cores/eval-harness/tests/test_divergence.py -x` reports 4 skipped, 0 failed
- With `GRAPH_WIKI_RUN_EVAL=1 --accept-divergence-baseline`: all 4 baseline JSON files are updated with real failure counts and current git SHA
- With `GRAPH_WIKI_RUN_EVAL=1` (no flag): test passes if no hard-severity regression; fails with a clear AssertionError citing the rule_id and the literal `--accept-divergence-baseline` instruction otherwise
- Per-rule failure counts + first 3 accepted_failures excerpts are visible under `pytest -s` (EVAL-12)
</verification>

<success_criteria>
- EVAL-12 + EVAL-13 closed: per-role divergence counts with concrete examples + regression gate
- 4 baselines recorded against the post-port agent — the starting point for Phase 7 sweep comparisons
- The full Phase 6 success-criteria #4 + #5 from ROADMAP are satisfied
- Phase 6 is ready for `/gsd:verify-work`
</success_criteria>

<output>
Create `.planning/phases/06-prompt-content-port-divergence-eval/06-11-SUMMARY.md` when done. Include the 4 baseline JSON snapshots' per-rule failure counts in the summary so reviewers see the v1.1 starting point at a glance.
</output>
