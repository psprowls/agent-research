---
phase: 06-prompt-content-port-divergence-eval
plan: 14
type: execute
wave: 10
depends_on: [06-13]
files_modified:
  - agents/graph-wiki-agent/src/graph_wiki_agent/commands/ingest.py
  - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/ingestor.py
  - agents/graph-wiki-agent/tests/unit/test_commands_ingest.py
autonomous: true
requirements: [PORT-03, EVAL-11]
tags: [gap-closure, ingestor, wikilinks, hallucination-guard, UAT-G4]

must_haves:
  truths:
    - "Wikilinks in the LLM body that do not resolve to any existing vault page are stripped (replaced with their bare text label) before the file is written"
    - "Wikilinks that DO resolve to an existing vault page are preserved verbatim"
    - "A summary of stripped wikilinks is appended to the ingest log entry so Pat can audit hallucination frequency"
    - "Ingestor system prompt restates the existing CITATION_RULES no-fabrication rule with explicit failure modes (`[[Person Name]]` and `[[directory/missing-page]]`) so the LLM has named anti-patterns"
    - "Resolution is case-sensitive on the slug, accepts both `[[slug]]` and `[[subdir/slug]]` forms, and treats a vault page as 'existing' iff `<vault>/<wikilink>.md` exists OR `<vault>/**/<basename>.md` exists somewhere in the vault tree"
  artifacts:
    - path: "agents/graph-wiki-agent/src/graph_wiki_agent/commands/ingest.py"
      provides: "_resolve_wikilinks() that strips unresolved wikilinks; called between fence-strip and file write"
      contains: "_resolve_wikilinks"
    - path: "agents/graph-wiki-agent/src/graph_wiki_agent/prompts/ingestor.py"
      provides: "Strengthened wikilink rule with named anti-patterns"
      contains: "Never fabricate"
    - path: "agents/graph-wiki-agent/tests/unit/test_commands_ingest.py"
      provides: "Tests for resolved-vs-unresolved wikilink behavior"
      contains: "test_run_ingest_source_strips_unresolved_wikilinks"
  key_links:
    - from: "commands/ingest.py:run_ingest_source"
      to: "_resolve_wikilinks"
      via: "called after _rewrite_target_slug_in_body, before target_path.write_text"
      pattern: "_resolve_wikilinks"
    - from: "commands/ingest.py:_resolve_wikilinks"
      to: "vault filesystem"
      via: "wiki.rglob('*.md') to enumerate existing pages once per ingest"
      pattern: "rglob"
---

<objective>
Close UAT gap G4 (LLM-generated pages contain hallucinated wikilinks: `[[Krishnan Sriram]]` for a person who has no vault page, `[[sources/otel-story-observability]]` for a path that does not exist because `sources/` is empty and the slug is wrong).

Per the anti-shallow rule, both layers are addressed:
  (a) **Prompt:** strengthen the existing CITATION_RULES "Wikilinks must point at existing vault pages. Never fabricate" rule with explicit named anti-patterns the LLM has been seen to produce (people names, non-existent subdirectory paths).
  (b) **Code:** add a post-LLM resolver in `commands/ingest.py` that enumerates existing vault pages, scans the LLM body for `[[…]]` wikilinks, and strips any that do not resolve — replacing them with their bare text label so the prose still reads. Log a count of stripped wikilinks for audit.

This depends on Plan 06-13 (page_type=source routing must work first, otherwise the resolver would see `[[sources/foo]]` as unresolved even when the file does exist under `sources/`).

Purpose: Iron rule (`cores/prompt-sources/SKILL.md` §Iron rules #6 + `_fragments/citation_rules.py`) says "Wikilinks must point at existing vault pages. Never fabricate." The LLM is currently violating this. A prompt-only fix is insufficient (UAT showed the LLM already had been told to cite real pages and still hallucinated); a code-level resolver is the only deterministic guard.

Output: One new private helper `_resolve_wikilinks(text, wiki) -> tuple[str, list[str]]`, one call site insertion in `run_ingest_source`, one log-detail extension, one prompt strengthening, and unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-prompt-content-port-divergence-eval/06-UAT.md
@cores/prompt-sources/agents/ingestor.md
@cores/prompt-sources/SKILL.md
@agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/citation_rules.py
@agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/iron_rules.py
@agents/graph-wiki-agent/src/graph_wiki_agent/prompts/ingestor.py
@agents/graph-wiki-agent/src/graph_wiki_agent/commands/ingest.py
@agents/graph-wiki-agent/tests/unit/test_commands_ingest.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add _resolve_wikilinks helper and call it in run_ingest_source</name>
  <files>
    agents/graph-wiki-agent/src/graph_wiki_agent/commands/ingest.py
    agents/graph-wiki-agent/tests/unit/test_commands_ingest.py
  </files>
  <read_first>
    - agents/graph-wiki-agent/src/graph_wiki_agent/commands/ingest.py (lines 1-50 — current imports; line 197-292 — full `run_ingest_source` flow; lines 277-281 — `append_log` call site we will extend)
    - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/citation_rules.py (lines 5-13 — the source-of-truth rule that the resolver enforces mechanically)
    - .planning/phases/06-prompt-content-port-divergence-eval/06-UAT.md (lines 100-107 — the two named hallucination cases: `[[Krishnan Sriram]]` (a person, no page) and `[[sources/otel-story-observability]]` (slug mismatch + empty dir))
    - agents/graph-wiki-agent/tests/unit/test_commands_ingest.py (lines 22-67 — pattern for ingest tests; the new tests follow the same patching style)
  </read_first>
  <behavior>
    - `_resolve_wikilinks("text with [[real-page]] and [[fake-person]]", wiki)` where `wiki/concepts/real-page.md` exists returns `("text with [[real-page]] and fake-person", ["fake-person"])` — the unresolved wikilink is replaced by its label text (no brackets), and the function returns the list of removed targets
    - `_resolve_wikilinks("text with [[sources/otel-story]]", wiki)` where `wiki/sources/otel-story.md` exists returns `("text with [[sources/otel-story]]", [])` — subdir-qualified wikilinks resolve when the file exists at that path
    - `_resolve_wikilinks("text with [[Krishnan Sriram]]", wiki)` returns `("text with Krishnan Sriram", ["Krishnan Sriram"])` — names with spaces are stripped (no `.md` for them anywhere in the vault)
    - The function does NOT modify wikilinks that appear inside fenced code blocks (defense against eating example snippets in summaries); use a simple state machine that toggles on `` ``` `` markers
    - In `run_ingest_source`, `_resolve_wikilinks` is called AFTER `_rewrite_target_slug_in_body` and BEFORE `target_path.write_text`
    - The `append_log` call's `detail` argument is extended to include the number of stripped wikilinks (e.g., `detail=f"source: {source_path}; stripped {len(stripped)} unresolved wikilink(s)"`)
    - The new page being written counts as an existing vault page for the purpose of self-references — pre-populate the resolver's "known pages" set with `target_path` before scanning the body
  </behavior>
  <action>
    Edit `agents/graph-wiki-agent/src/graph_wiki_agent/commands/ingest.py`.

    1. Near the other private helpers (just below `_rewrite_target_slug_in_body` from Plan 06-13), add:

       ```
       import re as _re  # local alias if `re` is not already imported at module level; if `re` is imported, drop this and use `re` directly

       # Matches [[…]] wikilinks. Does not match nested brackets (`[[a][b]]`) which
       # are not valid wikilink syntax. The captured group is the target.
       _WIKILINK_RE = _re.compile(r"\[\[([^\]\n]+)\]\]")


       def _resolve_wikilinks(text: str, wiki: Path) -> tuple[str, list[str]]:
           """Strip wikilinks that do not resolve to an existing vault page.

           For each `[[target]]` in `text`:
             - If `<wiki>/<target>.md` exists OR any `<wiki>/**/<basename>.md`
               exists where `basename` is the last path segment of `target`,
               keep the wikilink verbatim.
             - Otherwise, replace `[[target]]` with the bare label (i.e. the
               target string itself, no brackets, no `.md`).

           Wikilinks inside fenced code blocks (``` … ```) are NOT modified.

           Returns (rewritten_text, list_of_stripped_targets).

           Args:
             text:  the LLM body (after frontmatter has been written/rewritten).
             wiki:  vault root.
           """
           # Build the set of known page basenames (and known relative paths).
           # rglob is O(vault_size) — acceptable: vaults are <10k files.
           known_relpaths: set[str] = set()
           known_basenames: set[str] = set()
           for p in wiki.rglob("*.md"):
               rel = p.relative_to(wiki).as_posix()
               # Strip the .md suffix
               known_relpaths.add(rel[:-3])
               known_basenames.add(p.stem)

           stripped: list[str] = []

           # Walk the text line-by-line so we can track fence state.
           # (A multi-line regex with a negative-lookbehind for ``` is harder
           # to reason about than this state machine.)
           in_fence = False
           out_lines: list[str] = []
           for line in text.splitlines(keepends=True):
               # Toggle fence state on any line that starts with ```
               # (anywhere — also handles indented fences in lists).
               if line.lstrip().startswith("```"):
                   in_fence = not in_fence
                   out_lines.append(line)
                   continue
               if in_fence:
                   out_lines.append(line)
                   continue

               def _sub(m: "_re.Match[str]") -> str:
                   target = m.group(1).strip()
                   # Try exact relpath match first
                   if target in known_relpaths:
                       return m.group(0)
                   # Then basename match (handles `[[concepts/foo]]` resolving
                   # when foo.md exists somewhere — Obsidian-style fallback)
                   basename = target.rsplit("/", 1)[-1]
                   if basename in known_basenames:
                       return m.group(0)
                   stripped.append(target)
                   return target

               out_lines.append(_WIKILINK_RE.sub(_sub, line))

           return "".join(out_lines), stripped
       ```

       If `re` is already imported at module top (check `agents/graph-wiki-agent/src/graph_wiki_agent/commands/ingest.py` lines 1-50), drop the `import re as _re` and use `re` directly — only add the import if absent.

    2. In `run_ingest_source`, locate the block (after Plan 06-13's changes) that looks like:

       ```
       target_path = _route_target_path(wiki, page_type, target_slug)
       target_path.parent.mkdir(parents=True, exist_ok=True)
       canonical_slug = target_path.stem
       llm_output = _rewrite_target_slug_in_body(llm_output, canonical_slug)
       target_path.write_text(llm_output, encoding="utf-8")
       ```

       Replace with:

       ```
       target_path = _route_target_path(wiki, page_type, target_slug)
       target_path.parent.mkdir(parents=True, exist_ok=True)
       canonical_slug = target_path.stem
       llm_output = _rewrite_target_slug_in_body(llm_output, canonical_slug)
       # Write the file first so it is part of the "known pages" set when
       # resolving self-references in the body (e.g. an ADR linking to
       # itself or to a sibling created earlier in the same ingest).
       target_path.write_text(llm_output, encoding="utf-8")
       resolved_output, stripped_wikilinks = _resolve_wikilinks(llm_output, wiki)
       if stripped_wikilinks:
           target_path.write_text(resolved_output, encoding="utf-8")
       ```

       Two writes is acceptable here — vaults are local-disk, writes are <1ms, and the cleaner alternative would require pre-registering the new file's existence in the resolver's set, which doubles the API surface.

    3. Extend the `append_log` call (currently `append_log(wiki, "ingest", title_guess, detail=f"source: {source_path}")`):

       ```
       detail = f"source: {source_path}"
       if stripped_wikilinks:
           detail += f"; stripped {len(stripped_wikilinks)} unresolved wikilink(s): {stripped_wikilinks[:5]}"
       append_log(wiki, "ingest", title_guess, detail=detail)
       ```

       (Cap at 5 in the log detail for readability; full list lives in memory only for the duration of the ingest.)

    4. In `agents/graph-wiki-agent/tests/unit/test_commands_ingest.py`, append:

       ```
       # ---------------------------------------------------------------------------
       # _resolve_wikilinks unit tests (Plan 06-14)
       # ---------------------------------------------------------------------------


       def test_resolve_wikilinks_strips_unresolved(tmp_path: Path) -> None:
           from graph_wiki_agent.commands.ingest import _resolve_wikilinks

           wiki = tmp_path / "wiki"
           (wiki / "concepts").mkdir(parents=True)
           (wiki / "concepts" / "real-page.md").write_text("# Real", encoding="utf-8")

           text = "See [[real-page]] and [[fake-person]] for context."
           out, stripped = _resolve_wikilinks(text, wiki)
           assert "[[real-page]]" in out
           assert "[[fake-person]]" not in out
           assert "fake-person" in out  # label preserved
           assert stripped == ["fake-person"]


       def test_resolve_wikilinks_resolves_subdir_qualified(tmp_path: Path) -> None:
           from graph_wiki_agent.commands.ingest import _resolve_wikilinks

           wiki = tmp_path / "wiki"
           (wiki / "sources").mkdir(parents=True)
           (wiki / "sources" / "otel-story.md").write_text("# OTel", encoding="utf-8")

           # The UAT G4 case form: [[sources/otel-story]]
           text = "Per [[sources/otel-story]] the trace is propagated…"
           out, stripped = _resolve_wikilinks(text, wiki)
           assert "[[sources/otel-story]]" in out
           assert stripped == []


       def test_resolve_wikilinks_preserves_fenced_code(tmp_path: Path) -> None:
           from graph_wiki_agent.commands.ingest import _resolve_wikilinks

           wiki = tmp_path / "wiki"
           wiki.mkdir()
           text = (
               "Outside [[fake-page]] is stripped.\n"
               "\n"
               "```\n"
               "Inside [[fake-page]] is preserved.\n"
               "```\n"
               "After [[also-fake]] is stripped.\n"
           )
           out, stripped = _resolve_wikilinks(text, wiki)
           # The fenced occurrence is verbatim
           assert "Inside [[fake-page]] is preserved." in out
           # The unfenced occurrences are stripped
           assert "[[fake-page]]" not in out.replace("Inside [[fake-page]] is preserved.", "")
           assert "[[also-fake]]" not in out
           # Only the two unfenced unresolved targets are reported
           assert sorted(stripped) == ["also-fake", "fake-page"]


       @pytest.mark.asyncio
       async def test_run_ingest_source_strips_unresolved_wikilinks(tmp_path: Path) -> None:
           """End-to-end: ingestor emits a hallucinated wikilink; written file
           on disk has it stripped; append_log detail records the count."""
           from graph_wiki_agent.commands.ingest import run_ingest_source

           source_file = tmp_path / "src.md"
           source_file.write_text("# Src\n\nBody.", encoding="utf-8")
           wiki = tmp_path / "wiki"
           (wiki / "concepts").mkdir(parents=True)
           (wiki / "concepts" / "real-thing.md").write_text("# Real", encoding="utf-8")
           (wiki / "log.md").write_text("", encoding="utf-8")

           fake_llm_response = (
               "---\n"
               "title: My Page\n"
               "category: concept\n"
               "page_type: concept\n"
               "target_slug: my-page\n"
               "summary: x\n"
               "---\n"
               "Refers to [[real-thing]] and to [[Hallucinated Person]]."
           )

           with (
               patch("graph_wiki_agent.commands.ingest.resolve_wiki_and_repo") as mock_resolve,
               patch("graph_wiki_agent.commands.ingest.make_llm") as mock_make_llm,
               patch("graph_wiki_agent.commands.ingest.update_index"),
               patch("graph_wiki_agent.commands.ingest.append_log") as mock_append_log,
           ):
               mock_resolve.return_value = (wiki, tmp_path)
               fake_llm = MagicMock()
               fake_llm.ainvoke = AsyncMock(return_value=MagicMock(content=fake_llm_response))
               mock_make_llm.return_value = fake_llm

               result = await run_ingest_source(source_file, wiki)

           written = (wiki / "concepts" / "my-page.md").read_text(encoding="utf-8")
           assert "[[real-thing]]" in written
           assert "[[Hallucinated Person]]" not in written
           assert "Hallucinated Person" in written  # label preserved
           # append_log was called with a detail recording the strip count
           call_args = mock_append_log.call_args
           detail = call_args.kwargs.get("detail") or (call_args.args[3] if len(call_args.args) >= 4 else "")
           assert "stripped 1" in detail or "stripped 1 unresolved" in detail
       ```
  </action>
  <verify>
    <automated>uv run --package graph-wiki-agent pytest agents/graph-wiki-agent/tests/unit/test_commands_ingest.py -x -q -k "resolve_wikilinks or strips_unresolved"</automated>
  </verify>
  <acceptance_criteria>
    - `_resolve_wikilinks` is importable from `graph_wiki_agent.commands.ingest`.
    - All 4 new tests PASS.
    - The end-to-end test `test_run_ingest_source_strips_unresolved_wikilinks` shows the written file has `[[real-thing]]` preserved and `[[Hallucinated Person]]` removed (label `Hallucinated Person` retained).
    - `append_log` detail string contains `stripped 1 unresolved` (or close match).
    - Pre-existing tests in `test_commands_ingest.py` still pass — `pytest agents/graph-wiki-agent/tests/unit/test_commands_ingest.py -q` exits 0 for the whole file.
    - Wikilinks inside fenced code blocks are NEVER modified — verified by `test_resolve_wikilinks_preserves_fenced_code`.
  </acceptance_criteria>
  <done>
    Wikilink resolver in place; integrated into the ingest flow with two-pass write; log records strip count; tests exercise resolved, unresolved, subdir-qualified, and fenced-code-block paths.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Strengthen wikilink rule in ingestor prompt with named anti-patterns</name>
  <files>
    agents/graph-wiki-agent/src/graph_wiki_agent/prompts/ingestor.py
  </files>
  <read_first>
    - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/citation_rules.py (lines 5-13 — current shared rule "Wikilinks must point at existing vault pages. Never fabricate a wikilink target that doesn't exist in the vault." — DO NOT MODIFY this shared fragment; the strengthening goes ingestor-local because librarian already calls `validate_wikilinks` programmatically)
    - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/ingestor.py (the constants `_INGESTOR_RULES`, `_RED_FLAGS`; we extend `_INGESTOR_RULES` with named anti-patterns)
    - .planning/phases/06-prompt-content-port-divergence-eval/06-UAT.md (lines 100-107 — the exact hallucinated wikilink strings; quote them in the prompt so the LLM sees the anti-patterns by name)
  </read_first>
  <behavior>
    - INGESTOR_SYSTEM contains the substring "Never fabricate" (preserved from CITATION_RULES) AND a new ingestor-local section that lists at least two named anti-pattern shapes: (i) a person/entity name with no vault page, (ii) a path like `subdir/foo` whose file does not actually exist
    - INGESTOR_SYSTEM tells the LLM that unresolved wikilinks WILL be stripped post-hoc by the command layer — this is honest information that may change its behavior
    - No semantic change to any shared fragment
    - Snapshot test passes after one `--snapshot-update`
  </behavior>
  <action>
    Edit `agents/graph-wiki-agent/src/graph_wiki_agent/prompts/ingestor.py`.

    Replace the existing `_INGESTOR_RULES` constant. Current:

    ```
    _INGESTOR_RULES = (
        "## Ingestor rules\n\n"
        "- `raw/` and in-repo docs are read-only.\n"
        "- Code is the source of truth; update the vault when they disagree.\n"
        "- Touch ≥3 files: new page + `index.md` + `log.md` (handled by command layer).\n"
        "- Cite aggressively — every claim links to a source page or code path.\n"
        "- Flag contradictions: vault↔vault with `> ⚠️ Contradiction:` callouts; vault↔code with path.\n"
        "- Propose ADRs for decisions."
    )
    ```

    Replace with:

    ```
    _INGESTOR_RULES = (
        "## Ingestor rules\n\n"
        "- `raw/` and in-repo docs are read-only.\n"
        "- Code is the source of truth; update the vault when they disagree.\n"
        "- Touch ≥3 files: new page + `index.md` + `log.md` (handled by command layer).\n"
        "- Cite aggressively — every claim links to a source page or code path.\n"
        "- Flag contradictions: vault↔vault with `> ⚠️ Contradiction:` callouts; vault↔code with path.\n"
        "- Propose ADRs for decisions.\n\n"
        "## Wikilink discipline (named anti-patterns)\n\n"
        "DO NOT emit `[[wikilink]]` targets that do not already exist in the vault. "
        "Examples of forbidden output observed in past runs:\n"
        "- `[[Person Name]]` for an author/speaker/contributor who has no vault page — "
        "use prose (\"Person Name\") instead.\n"
        "- `[[subdir/some-slug]]` for a path that does not exist on disk — "
        "either omit the link entirely or use the `NO_RELEVANT_CONTENT` sentinel from the citation rules.\n\n"
        "The command layer post-processes the body and STRIPS any `[[…]]` that "
        "does not resolve to an existing vault page (the wikilink is replaced by its bare "
        "label text and the strip is recorded in `log.md`). If you cite something the "
        "vault doesn't have, the link will be silently removed — don't rely on it."
    )
    ```

    Refresh snapshot:
      `uv run --package graph-wiki-agent pytest agents/graph-wiki-agent/tests/prompts/test_prompt_snapshots.py::test_ingestor_system_snapshot --snapshot-update -x -q`
  </action>
  <verify>
    <automated>uv run --package graph-wiki-agent python -c "from graph_wiki_agent.prompts.ingestor import INGESTOR_SYSTEM; s = INGESTOR_SYSTEM; assert 'Wikilink discipline' in s; assert '[[Person Name]]' in s; assert 'subdir/some-slug' in s; assert 'STRIPS any' in s; assert 'Never fabricate' in s, 'shared citation rule must still be present'; print('OK')" &amp;&amp; uv run --package graph-wiki-agent pytest agents/graph-wiki-agent/tests/prompts/test_prompt_snapshots.py::test_ingestor_system_snapshot -x -q</automated>
  </verify>
  <acceptance_criteria>
    - INGESTOR_SYSTEM contains substring `Wikilink discipline (named anti-patterns)`.
    - INGESTOR_SYSTEM contains both example anti-patterns: `[[Person Name]]` and `subdir/some-slug`.
    - INGESTOR_SYSTEM warns the LLM that unresolved wikilinks WILL be stripped (substring `STRIPS any` or `stripped` in that context).
    - The pre-existing CITATION_RULES content is still composed in: substring `Never fabricate` still present.
    - `_fragments/citation_rules.py` is UNCHANGED in git diff (shared fragment, not edited).
    - Snapshot refreshed; git diff on the snapshot file shows only ADDED lines.
  </acceptance_criteria>
  <done>
    Ingestor prompt names the wikilink hallucination anti-patterns explicitly and informs the LLM that the resolver will strip bad links; snapshot refreshed and committed.
  </done>
</task>

</tasks>

<verification>
- `uv run --package graph-wiki-agent pytest agents/graph-wiki-agent/tests/unit/test_commands_ingest.py agents/graph-wiki-agent/tests/prompts/test_prompt_snapshots.py -x -q` exits 0.
- Manual: a re-run of `graph-wiki-agent ingest <real source>` against the fixture vault — confirm the resulting page has no `[[<unresolved>]]` substrings (Pat may run this if live Bedrock available; not required for autonomous verify).
- `grep -c "_fragments/citation_rules.py" .git diff` shows 0 changes to the shared fragment.
</verification>

<success_criteria>
- After this plan ships, no file written by `run_ingest_source` contains a `[[wikilink]]` whose target does not exist somewhere in the vault tree (UAT G4 closed at the code layer).
- The LLM is honestly told its bad wikilinks will be stripped (UAT G4 closed at the prompt layer).
- Audit trail: `log.md` records how many wikilinks were stripped per ingest, so Pat can spot recurring hallucination patterns over time.
- Iron rule #6 (every claim cites an existing page or code path) is now mechanically enforced for the ingestor path, matching the existing librarian behavior (`librarian.py:LIB-003 wikilink validity` programmatic check).
</success_criteria>

<output>
Create `.planning/phases/06-prompt-content-port-divergence-eval/06-14-SUMMARY.md` when done. Include: (a) the count of stripped wikilinks in the unit test fixtures (sanity check the resolver fires); (b) confirmation that `_fragments/citation_rules.py` was not modified; (c) the line count of the new prompt section.
</output>