---
phase: 06-prompt-content-port-divergence-eval
plan: 10
type: execute
wave: 6
depends_on: [06-09]
files_modified:
  - cores/eval-harness/src/eval_harness/divergence/metric.py
  - cores/eval-harness/baselines/divergence-librarian.json
  - cores/eval-harness/baselines/divergence-ingestor.json
  - cores/eval-harness/baselines/divergence-linter.json
  - cores/eval-harness/baselines/divergence-scanner.json
  - cores/eval-harness/tests/test_divergence_baseline.py
autonomous: true
requirements: [EVAL-13]
tags: [divergence-eval, baseline, regression-gate, accept-flag]

must_haves:
  truths:
    - "load_baseline(role, baselines_dir) returns the per-role JSON dict or {} when file is missing (per RESEARCH §Pitfall 5)"
    - "write_baseline(role, baselines_dir, results, agent_commit) writes the D-11 schema JSON with `json.dumps(..., indent=2) + '\\n'`"
    - "check_regression(role, current, baseline, severity_lookup) raises AssertionError when any hard-severity rule's failure count exceeds baseline; soft-severity differences are reported but never raise"
    - "Initial empty baseline JSON file exists for each of 4 roles so the first non-accept run does not hit the missing-file path"
    - "Unit tests cover: missing-baseline returns empty; write_baseline schema; hard-severity regression raises; soft-severity regression does not raise; --accept-divergence-baseline flag rewrites file"
  artifacts:
    - path: "cores/eval-harness/src/eval_harness/divergence/metric.py"
      modifies: "Adds load_baseline, write_baseline, check_regression module-level functions"
    - path: "cores/eval-harness/baselines/divergence-{librarian,ingestor,linter,scanner}.json"
      provides: "Initial per-role baseline JSON (zero failures across all rules); committed to git per D-12"
  key_links:
    - from: "divergence/metric.py::check_regression"
      to: "ROLE_CHECKS[role] severity lookup"
      via: "build {rule_id: severity} dict from the role's checks list"
      pattern: "severity in ('hard', 'soft')"
    - from: "test_divergence_baseline.py"
      to: "divergence/metric.py functions"
      via: "unit tests with synthetic baseline + results dicts"
      pattern: "from eval_harness.divergence.metric import"
---

<objective>
Implement the baseline persistence + regression-gate layer (D-11, D-12, EVAL-13): three module-level functions in `metric.py` (`load_baseline`, `write_baseline`, `check_regression`) plus initial empty baseline JSON files committed for the 4 roles, plus unit tests covering the full flow including the `--accept-divergence-baseline` flag behavior.

Purpose: Delivers EVAL-13. The regression gate (hard-severity-only per D-12) is what makes Phase 7's cost-frontier sweep meaningful — without it, a sweep that breaks divergence is undetectable.

Output: Three new functions in `divergence/metric.py`, 4 initial baseline JSON files, expanded unit test file. Eval-gated integration test stays as 06-11.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-prompt-content-port-divergence-eval/06-CONTEXT.md
@.planning/phases/06-prompt-content-port-divergence-eval/06-RESEARCH.md
@.planning/phases/06-prompt-content-port-divergence-eval/06-PATTERNS.md
@cores/eval-harness/src/eval_harness/divergence/metric.py
@cores/eval-harness/src/eval_harness/divergence/__init__.py
@cores/eval-harness/src/eval_harness/baseline.py
@cores/eval-harness/tests/test_divergence_baseline.py
@cores/eval-harness/tests/conftest.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add load_baseline / write_baseline / check_regression to divergence/metric.py</name>
  <files>
    cores/eval-harness/src/eval_harness/divergence/metric.py
  </files>
  <read_first>
    - cores/eval-harness/src/eval_harness/divergence/metric.py (from 06-09 — extend, do not rewrite)
    - cores/eval-harness/src/eval_harness/baseline.py (full file — `_make_snapshot`, `path.write_text` JSON pattern at line ~360)
    - cores/eval-harness/src/eval_harness/report.py (regression_check pattern at lines ~66-84 — the AssertionError message style)
    - cores/eval-harness/src/eval_harness/divergence/__init__.py (ROLE_CHECKS available for severity lookup)
    - .planning/phases/06-prompt-content-port-divergence-eval/06-PATTERNS.md (section: divergence/metric.py — Baseline load/write pattern + Regression check pattern)
    - .planning/phases/06-prompt-content-port-divergence-eval/06-CONTEXT.md (D-11, D-12, D-13 — locked schema and gate behavior)
    - .planning/phases/06-prompt-content-port-divergence-eval/06-RESEARCH.md (Pattern 4: Baseline Delta Logic, §Pitfall 5)
  </read_first>
  <behavior>
    - `load_baseline(role: str, baselines_dir: Path) -> dict` returns `json.loads(path.read_text())` when `baselines_dir / f"divergence-{role}.json"` exists; returns `{}` if missing (RESEARCH §Pitfall 5).
    - `write_baseline(role: str, baselines_dir: Path, results: dict, agent_commit: str) -> Path` builds the D-11 envelope via `summarize` and writes with `json.dumps(envelope, indent=2) + "\n"` and `path.write_text(..., encoding="utf-8")` (PATTERNS §JSON write pattern). Creates `baselines_dir` with `mkdir(parents=True, exist_ok=True)`. Returns the written path.
    - `check_regression(role: str, current: dict, baseline: dict) -> None` looks up severity from `eval_harness.divergence.ROLE_CHECKS[role]` to determine hard vs soft. For each rule_id in `current`:
        - If rule_id ends with `-JUDGE` (judge-only aggregate from 06-09), treat as severity=`"soft"` (judge non-determinism per RESEARCH §Pitfall 2).
        - Else lookup severity from `{c.id: c.severity for c in ROLE_CHECKS[role]}`. Missing rule_id (defensive) defaults to `"soft"`.
        - `baseline_failures = baseline.get("checks", {}).get(rule_id, {}).get("failures", 0)`
        - `current_failures = current[rule_id]["failures"]`
        - If `severity == "hard"` and `current_failures > baseline_failures`, raise `AssertionError(f"[{role}] {rule_id}: {current_failures} failures > baseline {baseline_failures}. Run with --accept-divergence-baseline to accept.")` per PATTERNS §Regression check pattern.
        - Soft-severity differences: do not raise. Optionally write to logger.
    - All three functions are module-level (not methods on DivergenceMetric) so they can be called from the test layer without instantiating a metric.
  </behavior>
  <action>
    Edit `cores/eval-harness/src/eval_harness/divergence/metric.py`. Do NOT rewrite the existing `DivergenceMetric` class — append the three new functions below it.

    Add module-level constants near the top:
      `_BASELINE_FILENAME_TPL = "divergence-{role}.json"`

    Implement the three functions per the behavior above:

    `def load_baseline(role: str, baselines_dir: Path) -> dict:` — read the JSON file or return `{}`. Use `path = baselines_dir / _BASELINE_FILENAME_TPL.format(role=role)`. Wrap `json.loads(path.read_text(encoding="utf-8"))` in `if not path.exists(): return {}`.

    `def write_baseline(role: str, baselines_dir: Path, results: dict, agent_commit: str) -> Path:` — uses `summarize(role, results, agent_commit)` (already in metric.py from 06-09) to build the envelope, then writes with `baselines_dir.mkdir(parents=True, exist_ok=True)` + `path.write_text(json.dumps(envelope, indent=2) + "\n", encoding="utf-8")`. Returns the path written.

    `def check_regression(role: str, current: dict, baseline: dict) -> None:` — builds the severity lookup:
      `from eval_harness.divergence import ROLE_CHECKS`  # lazy import to avoid circularity
      `severity_lookup = {c.id: c.severity for c in ROLE_CHECKS.get(role, [])}`
      For each rule_id in `current`, determine effective severity (`-JUDGE` suffix → `"soft"`; otherwise lookup; default `"soft"`), look up baseline failure count, raise AssertionError on hard-severity regression.

    Do NOT touch the existing `DivergenceMetric` class, `_JUDGE_THRESHOLD`, or `summarize` helpers.
  </action>
  <verify>
    <automated>uv run python -c "from eval_harness.divergence.metric import load_baseline, write_baseline, check_regression, summarize; from pathlib import Path; import tempfile, json; tmp = Path(tempfile.mkdtemp()); assert load_baseline('librarian', tmp) == {}; results = {'LIB-001-wikilink-resolves': {'runs': 5, 'failures': 0, 'accepted_failures': []}}; p = write_baseline('librarian', tmp, results, 'abc123'); assert p.exists(); env = json.loads(p.read_text()); assert env['role'] == 'librarian' and env['agent_commit'] == 'abc123' and 'LIB-001-wikilink-resolves' in env['checks']; b = load_baseline('librarian', tmp); assert b['role'] == 'librarian'; check_regression('librarian', {'LIB-001-wikilink-resolves': {'runs': 5, 'failures': 0, 'accepted_failures': []}}, b); print('no-regression OK'); raised = False;\nfrom eval_harness.divergence import ROLE_CHECKS  # noqa\ntry:\n    check_regression('librarian', {'LIB-001-wikilink-resolves': {'runs': 5, 'failures': 3, 'accepted_failures': []}}, b)\nexcept AssertionError as e:\n    raised = True; assert 'LIB-001' in str(e) and 'accept-divergence-baseline' in str(e)\nassert raised; print('hard-regression OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `load_baseline`, `write_baseline`, `check_regression` are importable from `eval_harness.divergence.metric`.
    - `load_baseline` returns `{}` when file does not exist.
    - `write_baseline` produces a JSON file with the D-11 envelope keys (`role`, `recorded_at`, `agent_commit`, `checks`) and uses the `json.dumps(..., indent=2) + "\n"` format.
    - `check_regression` raises `AssertionError` containing `<role>`, `<rule_id>`, and the literal `accept-divergence-baseline` substring when a hard-severity rule's failures exceed baseline.
    - `check_regression` does NOT raise on `-JUDGE` rule_id regressions (judge severity is treated as soft).
    - `check_regression` does NOT raise on soft-severity programmatic rule regressions (e.g., LIB-004-code-path-format which is soft per RESEARCH).
  </acceptance_criteria>
  <done>
    Three new module-level functions in metric.py; verify command above passes both no-regression and hard-regression cases.
  </done>
</task>

<task type="auto">
  <name>Task 2: Initialize 4 baseline JSON files + populate test_divergence_baseline.py</name>
  <files>
    cores/eval-harness/baselines/divergence-librarian.json,
    cores/eval-harness/baselines/divergence-ingestor.json,
    cores/eval-harness/baselines/divergence-linter.json,
    cores/eval-harness/baselines/divergence-scanner.json,
    cores/eval-harness/tests/test_divergence_baseline.py
  </files>
  <read_first>
    - cores/eval-harness/tests/test_divergence_baseline.py (scaffold from 06-02)
    - cores/eval-harness/tests/test_baseline.py (style + schema-test analog)
    - cores/eval-harness/src/eval_harness/divergence/metric.py (functions added in Task 1)
    - cores/eval-harness/src/eval_harness/divergence/__init__.py (ROLE_CHECKS for rule lists)
    - .planning/phases/06-prompt-content-port-divergence-eval/06-PATTERNS.md (section: eval-harness/tests/test_divergence_baseline.py)
  </read_first>
  <action>
    Create the 4 initial baseline JSON files, one per role. Use `write_baseline` programmatically to produce them — do NOT hand-write JSON. Run a one-off script (inline `uv run python -c "..."`) that:
      1. Imports `ROLE_CHECKS` from `eval_harness.divergence`.
      2. For each role in `["librarian", "ingestor", "linter", "scanner"]`, builds a results dict `{c.id: {"runs": 0, "failures": 0, "accepted_failures": []} for c in ROLE_CHECKS[role]}` plus an entry `f"{prefix}-JUDGE": {"runs": 0, "failures": 0, "accepted_failures": []}` (judge aggregate ID per 06-09).
      3. Calls `write_baseline(role, Path("cores/eval-harness/baselines"), results, agent_commit="initial-empty-baseline")`.
    These files are committed to git per D-12 so the first `pytest cores/eval-harness/tests/test_divergence.py` run after 06-11 lands has a non-empty baseline to compare against (zero failures = clean baseline).

    Edit `test_divergence_baseline.py` from the 06-02 scaffold. Remove the module-level skip guard or convert to fail-fast. Implement concrete tests per PATTERNS.md (section: test_divergence_baseline.py):

      - `test_load_baseline_returns_empty_when_missing(tmp_path)`: assert `load_baseline("librarian", tmp_path) == {}`.
      - `test_write_baseline_schema(tmp_path)`: call write_baseline, read back the JSON, assert all required keys (`role`, `recorded_at`, `agent_commit`, `checks`) are present and `checks` is a dict.
      - `test_write_baseline_json_format(tmp_path)`: assert the file ends with `\n` and is indented 2 spaces (verify by reading raw text).
      - `test_check_regression_raises_on_hard_increase(tmp_path)`: build a baseline with `LIB-001-wikilink-resolves` failures=1, current with failures=3; assert raises AssertionError matching `accept-divergence-baseline`.
      - `test_check_regression_does_not_raise_for_soft(tmp_path)`: build a baseline + current where the soft-severity `LIB-004-code-path-format` regresses; assert no exception.
      - `test_check_regression_does_not_raise_for_judge(tmp_path)`: build a baseline + current where `LIB-JUDGE` regresses; assert no exception (judge severity is soft per Task 1).
      - `test_check_regression_passes_when_equal(tmp_path)`: failures equal baseline; assert no exception.
      - `test_check_regression_passes_when_decreased(tmp_path)`: current failures < baseline; assert no exception.
      - `test_accept_baseline_flag_rewrites_file(tmp_path)`: simulate the flag flow by calling `write_baseline` twice with different results; assert the second write replaces the file content.

    All tests use synthetic dicts — no Bedrock, no real DivergenceMetric calls.
  </action>
  <verify>
    <automated>uv run pytest cores/eval-harness/tests/test_divergence_baseline.py -x -q 2>&1 | tail -15 && for r in librarian ingestor linter scanner; do test -f "cores/eval-harness/baselines/divergence-$r.json" || { echo "MISSING: $r"; exit 1; }; uv run python -c "import json; d = json.load(open('cores/eval-harness/baselines/divergence-$r.json')); assert d['role'] == '$r'; assert 'checks' in d; print('$r OK', list(d['checks'].keys())[:2])"; done</automated>
  </verify>
  <acceptance_criteria>
    - 4 baseline JSON files exist under `cores/eval-harness/baselines/`, one per role.
    - Each baseline JSON validates against the D-11 schema: keys `role`, `recorded_at`, `agent_commit`, `checks`.
    - Each baseline's `checks` dict has an entry for every programmatic rule ID in that role's `*_CHECKS` list, plus the `{PREFIX}-JUDGE` aggregate key.
    - Initial baseline failure counts are all 0 (clean starting baseline).
    - `test_divergence_baseline.py` has at least 8 tests covering the flow described above; all pass under `uv run pytest cores/eval-harness/tests/test_divergence_baseline.py -x`.
    - No test requires Bedrock or `GRAPH_WIKI_RUN_EVAL=1`.
  </acceptance_criteria>
  <done>
    4 baseline JSON files initialized via `write_baseline`; `test_divergence_baseline.py` covers load/write/regression for hard, soft, and judge severities; all unit tests green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Baseline JSON file -> regression check | Reads JSON from disk; integrity is git's concern |
| `--accept-divergence-baseline` flag -> file rewrite | User-confirmed write that overwrites the baseline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-21 | Tampering | Baseline JSON tamper masks real regression | mitigate | Baseline files are committed to git; tamper detection is git diff at PR review time (per RESEARCH §Security Domain); the `--accept-divergence-baseline` flag rewrite is an explicit human action |
| T-06-22 | Repudiation | Silent acceptance without trail | mitigate | The `agent_commit` field in each baseline records the agent SHA at acceptance time; PR reviewer can trace baseline drift to a specific commit |
| T-06-23 | Tampering | check_regression skips a hard rule unexpectedly | mitigate | Test `test_check_regression_raises_on_hard_increase` explicitly validates a hard-severity rule triggers the assertion |
</threat_model>

<verification>
- 4 initial baseline JSON files present and valid per D-11 schema
- `uv run pytest cores/eval-harness/tests/test_divergence_baseline.py -x` green
- check_regression raises on hard-severity increase, not on soft, not on judge
- Baseline JSON files use `json.dumps(..., indent=2) + "\n"` format
</verification>

<success_criteria>
- EVAL-13 delivered: regression gate functions exist; --accept-divergence-baseline flag (wired in 06-02 conftest.py) will consume them in 06-11
- 4 initial baselines committed to git with all-zero failure counts as the starting point
- Unit tests cover the full baseline schema + regression-gate flow without Bedrock
- 06-11 can wire the metric + baseline into a pytest gate immediately
</success_criteria>

<output>
Create `.planning/phases/06-prompt-content-port-divergence-eval/06-10-SUMMARY.md` when done.
</output>
