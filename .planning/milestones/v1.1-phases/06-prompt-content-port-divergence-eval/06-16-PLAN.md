---
phase: 06-prompt-content-port-divergence-eval
plan: 16
type: execute
wave: 9
depends_on: [06-15]
files_modified:
  - cores/eval-harness/tests/test_divergence.py
  - pyproject.toml
autonomous: true
requirements: [EVAL-13]
tags: [gap-closure, test-hygiene, sys-path, WR-05]

must_haves:
  truths:
    - "cores/eval-harness/tests/test_divergence.py has NO sys.path.insert call — the eval_helpers import works via standard test discovery"
    - "Root pyproject.toml [tool.pytest.ini_options] has `pythonpath = [\"cores/eval-harness/tests\"]` so eval_helpers is importable regardless of pytest invocation cwd (workspace root or package dir)"
    - "Existing per-package pythonpath in cores/eval-harness/pyproject.toml is preserved (it serves the case where pytest is invoked from inside the package dir)"
    - "All test_divergence.py imports continue to resolve; no test starts failing as a side-effect of this cleanup"
  artifacts:
    - path: "cores/eval-harness/tests/test_divergence.py"
      provides: "Import-only test header — no sys.path manipulation"
      not_contains: "sys.path.insert"
    - path: "pyproject.toml"
      provides: "Root pytest pythonpath including cores/eval-harness/tests"
      contains: "pythonpath"
  key_links:
    - from: "pyproject.toml [tool.pytest.ini_options]"
      to: "cores/eval-harness/tests/eval_helpers"
      via: "pythonpath entry"
      pattern: "cores/eval-harness/tests"
---

<objective>
Close the residual portion of WR-05 — the `sys.path.insert` workaround in `cores/eval-harness/tests/test_divergence.py:37-39`. The earlier commit `cc6c67c` moved `EVAL_GATE` and `produce_outputs` into `eval_helpers.py` (resolving the WR-06 EVAL_GATE-duplication issue and most of WR-05), but the `sys.path.insert` block remains because the eval-harness package's `pythonpath = ["tests"]` config only applies when pytest is invoked from inside the package dir, not from the workspace root.

The fix: hoist `pythonpath` to the root `pyproject.toml` so pytest finds `eval_helpers` regardless of cwd, then delete the sys.path hack from `test_divergence.py`.

Purpose: per the code-review note, the sys.path manipulation is brittle (depends on pytest's `--import-mode` not changing how conftest dirs are added to sys.path). Removing it leaves the test header with only standard `from … import …` statements — easier to read, less fragile across pytest versions.

Output: One pyproject.toml edit (root, add pythonpath entry), one source-file cleanup (delete 10 lines of sys.path manipulation + obsolete comment).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-prompt-content-port-divergence-eval/06-REVIEW.md
@pyproject.toml
@cores/eval-harness/pyproject.toml
@cores/eval-harness/tests/test_divergence.py
@cores/eval-harness/tests/eval_helpers.py
@cores/eval-harness/tests/conftest.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add cores/eval-harness/tests to root pytest pythonpath</name>
  <files>
    pyproject.toml
  </files>
  <read_first>
    - pyproject.toml (full file — 24 lines; `[tool.pytest.ini_options]` at line 22 currently only sets `addopts`)
    - cores/eval-harness/pyproject.toml (lines 25-29 — confirms `pythonpath = ["tests"]` is set at the package level for in-package invocation)
    - cores/eval-harness/tests/test_divergence.py (lines 29-39 — the brittle block we are about to delete; its comment block at lines 30-36 explains exactly why `pythonpath` at the workspace root is the right fix)
  </read_first>
  <behavior>
    - Root `pyproject.toml` `[tool.pytest.ini_options]` table contains a `pythonpath` key listing `"cores/eval-harness/tests"` (relative path — pytest resolves it against rootdir, which is the directory containing the pyproject.toml that defined the ini-options table)
    - Existing `addopts = "--import-mode=importlib"` is preserved
    - Per-package `pythonpath` in `cores/eval-harness/pyproject.toml` is NOT modified (covers the in-package-cwd case independently)
    - From the workspace root, `uv run --package eval-harness python -c "import eval_helpers; print(eval_helpers.EVAL_GATE)"` does NOT need to work (eval_helpers is on the pytest path, not the global Python path) — but `uv run --package eval-harness pytest cores/eval-harness/tests/test_divergence.py --collect-only` MUST succeed at the collection stage (i.e., the file's top-level `from eval_helpers import …` resolves)
  </behavior>
  <action>
    Edit `pyproject.toml` (workspace root). The `[tool.pytest.ini_options]` block currently reads:

    ```
    [tool.pytest.ini_options]
    addopts = "--import-mode=importlib"
    ```

    Replace with:

    ```
    [tool.pytest.ini_options]
    addopts = "--import-mode=importlib"
    pythonpath = ["cores/eval-harness/tests"]
    ```

    Do NOT touch `cores/eval-harness/pyproject.toml` — its local `pythonpath = ["tests"]` is still useful for the case where pytest is invoked from inside the package directory and there is no parent pyproject providing the pytest config (e.g., when CI tooling chdirs into the package before running tests).
  </action>
  <verify>
    <automated>uv run --package eval-harness pytest cores/eval-harness/tests/test_divergence.py --collect-only -q 2>&amp;1 | head -30 &amp;&amp; grep -c 'pythonpath = \["cores/eval-harness/tests"\]' pyproject.toml | grep -v '^#'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'pythonpath' pyproject.toml` returns one line under `[tool.pytest.ini_options]` listing `["cores/eval-harness/tests"]`.
    - `cores/eval-harness/pyproject.toml` is UNCHANGED in `git status`.
    - `uv run --package eval-harness pytest cores/eval-harness/tests/test_divergence.py --collect-only` exits 0 (no `ModuleNotFoundError: eval_helpers`).
    - The collect-only output lists the parametrized cases for librarian, ingestor, linter, scanner.
  </acceptance_criteria>
  <done>
    Workspace-level pythonpath in place; the import will succeed from any cwd; per-package config untouched.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Delete sys.path.insert hack from test_divergence.py</name>
  <files>
    cores/eval-harness/tests/test_divergence.py
  </files>
  <read_first>
    - cores/eval-harness/tests/test_divergence.py (lines 23-53 — full header; the deletion target is lines 29-39 inclusive; lines 41-53 must remain unchanged)
    - .planning/phases/06-prompt-content-port-divergence-eval/06-REVIEW.md (lines 175-201 — WR-05 + WR-06 — confirms this is the residual issue after cc6c67c)
  </read_first>
  <behavior>
    - test_divergence.py imports `EVAL_GATE` and `produce_outputs` from `eval_helpers` directly (unchanged behavior — already does this)
    - The 11-line block `# Ensure the tests/ directory is on sys.path …` through `sys.path.insert(0, _TESTS_DIR)` (currently lines 29-39) is deleted
    - The `import sys` at line 24 is removed only IF no other code in the file uses `sys` (verify before deletion: `grep -c '\\bsys\\b' cores/eval-harness/tests/test_divergence.py` should drop to 0 after the block is removed)
    - All other imports continue to work
    - `pytest cores/eval-harness/tests/test_divergence.py --collect-only` succeeds (uses the Task-1 pythonpath)
    - The GRAPH_WIKI_RUN_EVAL=1 full run still works (no regression in eval behavior)
  </behavior>
  <action>
    Edit `cores/eval-harness/tests/test_divergence.py`.

    1. Delete lines 29-39 inclusive. The block to delete reads:
       ```
       # Ensure the tests/ directory is on sys.path so eval_helpers is importable.
       # eval_helpers lives alongside this file in cores/eval-harness/tests/.
       # This is necessary because pytest does not add conftest directories to sys.path
       # under --import-mode=importlib, and pythonpath in pyproject.toml is only
       # applied when pytest is invoked from within the package directory (not from
       # the workspace root). A targeted path-insert for a stable, well-named module
       # is an acceptable approach — unlike importing conftest, which pytest does not
       # guarantee is importable as a plain module.
       _TESTS_DIR = str(Path(__file__).parent)
       if _TESTS_DIR not in sys.path:
           sys.path.insert(0, _TESTS_DIR)
       ```

    2. **After step 1's deletion is on disk**, check whether `sys` is still used anywhere in the file:
       `grep -n '\bsys\b' cores/eval-harness/tests/test_divergence.py`
       (Order matters: run this AFTER deleting L29-39, not before — otherwise the grep will count the references inside the block you're about to remove and mislead you into keeping `import sys`.)
       If grep returns zero matches, also delete the `import sys` line (was at line 24 pre-deletion). If `sys` is referenced elsewhere (unlikely — `subprocess` is the standard-library escape for git invocation), leave the import.

    3. The block of comments at lines 49-51 reading `# Eval gate and helpers — imported from eval_helpers (WR-05, WR-06)` and the subsequent `from eval_helpers import EVAL_GATE, produce_outputs as _produce_outputs  # noqa: E402` (lines 49-53) remain unchanged. (The `# noqa: E402` is now technically unnecessary because the late-import was required ONLY by the sys.path block — but leave it: future refactors may reorder imports, and the comment causes no harm.)

       Actually re-check: with the sys.path block gone, the `from eval_helpers import …` is no longer "after non-import code" — it CAN be moved to the top with the other imports, AND the `# noqa: E402` can be dropped. Make this change:
       - Move `from eval_helpers import EVAL_GATE, produce_outputs as _produce_outputs` up next to the other top-level `from eval_harness.divergence …` imports.
       - Drop the `# noqa: E402` suffix.
       - Keep the comment block "# Eval gate and helpers — imported from eval_helpers (WR-05, WR-06)" as an inline note above the import, since it traces the WR provenance.

    4. After edits, the file's top section should read approximately:
       ```
       import subprocess
       from pathlib import Path

       import pytest

       from eval_harness.divergence import ROLE_CHECKS, ROLE_RUBRICS
       from eval_harness.divergence.metric import (
           DivergenceMetric,
           check_regression,
           load_baseline,
           write_baseline,
       )

       # Eval gate and helpers — imported from eval_helpers (WR-05, WR-06)
       from eval_helpers import EVAL_GATE, produce_outputs as _produce_outputs
       ```

       (`import sys` removed; sys.path block removed; eval_helpers import hoisted to the top.)

       If the verify command surfaces a different layout (e.g., the file has additional imports we haven't enumerated), preserve them — only the four bullets above are required behavioral changes.
  </action>
  <verify>
    <automated>grep -c "sys.path.insert" cores/eval-harness/tests/test_divergence.py | xargs -I{} sh -c '[ "{}" = "0" ] || (echo "FAIL: sys.path.insert still present" &amp;&amp; exit 1)' &amp;&amp; uv run --package eval-harness pytest cores/eval-harness/tests/test_divergence.py --collect-only -q &amp;&amp; uv run --package eval-harness pytest cores/eval-harness/tests/ -x -q --ignore=cores/eval-harness/tests/test_divergence.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "sys.path.insert" cores/eval-harness/tests/test_divergence.py` returns 0.
    - `grep -c "^import sys" cores/eval-harness/tests/test_divergence.py` returns 0 (assuming `sys` had no other use — verify per the check in step 2).
    - `pytest cores/eval-harness/tests/test_divergence.py --collect-only -q` exits 0; output shows all four parametrized cases collected (librarian, ingestor, linter, scanner).
    - `pytest cores/eval-harness/tests/ -q --ignore=cores/eval-harness/tests/test_divergence.py` (the non-gated tests) still pass — no collateral damage.
    - The `from eval_helpers import EVAL_GATE, produce_outputs as _produce_outputs` line has no `# noqa: E402` suffix.
    - File has no `# noqa` related to import ordering left in test_divergence.py.
  </acceptance_criteria>
  <done>
    sys.path manipulation removed; eval_helpers import hoisted; test collection passes from workspace root.
  </done>
</task>

</tasks>

<verification>
- `grep -c "sys.path.insert\|^import sys" cores/eval-harness/tests/test_divergence.py` returns 0 (both patterns).
- `pytest cores/eval-harness/tests/test_divergence.py --collect-only -q` exits 0 from the workspace root.
- `pytest cores/eval-harness/tests/ -q --ignore=cores/eval-harness/tests/test_divergence.py` still green.
- Optional live verification (Pat may run if convenient): `GRAPH_WIKI_RUN_EVAL=1 uv run --package eval-harness pytest cores/eval-harness/tests/test_divergence.py -k librarian -s -x` — should reach Bedrock and execute the librarian role normally (gating regression check).
</verification>

<success_criteria>
- WR-05 fully resolved — no sys.path manipulation, no conftest-as-module import (already fixed by cc6c67c), no EVAL_GATE duplication (already fixed by cc6c67c).
- Root pytest config is the single source of truth for `eval_helpers` discoverability.
- Test header reads as plain stdlib + project imports — readable in a single scroll.
</success_criteria>

<output>
Create `.planning/phases/06-prompt-content-port-divergence-eval/06-16-SUMMARY.md` when done. Include:
- (a) the diff of pyproject.toml (the one added pythonpath line),
- (b) the line count delta in test_divergence.py header (should be roughly −12 lines after deleting the comment-block + sys.path block + standalone `import sys`),
- (c) confirmation that `cores/eval-harness/pyproject.toml` was NOT touched.
</output>