---
phase: 11-workspace-io-port-m1
plan: 05
type: execute
wave: 3
depends_on: [11-01, 11-02]
files_modified:
  - agents/code-wiki-agent/pyproject.toml
  - agents/code-wiki-agent/src/code_wiki_agent/commands/init.py
  - agents/code-wiki-agent/src/code_wiki_agent/commands/log.py
  - agents/code-wiki-agent/src/code_wiki_agent/commands/query.py
  - agents/code-wiki-agent/src/code_wiki_agent/config.py
  - agents/code-wiki-agent/src/code_wiki_agent/cli.py
  - agents/code-wiki-agent/src/code_wiki_mcp/server.py
autonomous: true
requirements: [WS-04, WS-07]
must_haves:
  truths:
    - "code-wiki-agent declares workspace-io as a workspace dependency"
    - "code-wiki-agent init <path> calls workspace_io.init(...) BEFORE the existing init_wiki path (D-07 two-phase init)"
    - "All 6 --vault Typer option help strings reference GRAPH_WIKI_WORKSPACE"
    - "All 5 MCP server Field descriptions and 2 tool descriptions reference GRAPH_WIKI_WORKSPACE"
    - "code_wiki_agent.config.WikiConfig docstring references GRAPH_WIKI_WORKSPACE"
    - "Docstrings in commands/log.py and commands/query.py reference GRAPH_WIKI_WORKSPACE"
    - "Zero CODE_WIKI_REAL_VAULT_PATH references remain anywhere under agents/code-wiki-agent/"
    - "D-12: workspace_io.init registers code-wiki-agent as a plugin with installed_version == applied_version (both set to importlib.metadata.version('code-wiki-agent'))."
  artifacts:
    - path: "agents/code-wiki-agent/pyproject.toml"
      provides: "workspace-io dependency"
      contains: "workspace-io"
    - path: "agents/code-wiki-agent/src/code_wiki_agent/commands/init.py"
      provides: "Two-phase init — workspace_io.init then init_wiki (D-07)"
      contains: "workspace_io"
  key_links:
    - from: "code-wiki-agent CLI init command"
      to: "workspace_io.init"
      via: "import + call in commands/init.py before resolve_wiki_and_repo"
      pattern: "from workspace_io import init"
    - from: "importlib.metadata.version('code-wiki-agent')"
      to: "workspace_io.init version kwarg"
      via: "D-13 — runtime version introspection"
      pattern: "importlib.metadata.version"
---

<objective>
Wire `workspace_io.init` into the `code-wiki-agent init` CLI command per D-07 (two-phase init: workspace bootstrap THEN wiki tree), add the workspace-io dep to the agent's pyproject, and rebrand all remaining `CODE_WIKI_REAL_VAULT_PATH` references across the agent's CLI help strings, MCP server Field descriptions, and module docstrings to `GRAPH_WIKI_WORKSPACE`.

Purpose: Close the agent-side half of WS-07 (env-var rebrand sweep) and complete WS-04 (init flow extended to call workspace_io.init). Plan 04 handles the vault-io half of WS-07; together they leave zero `CODE_WIKI_REAL_VAULT_PATH` references in the in-scope source tree.

Output: `code-wiki-agent` CLI and MCP server use `GRAPH_WIKI_WORKSPACE` in all user-visible text; `code-wiki-agent init <path>` bootstraps the workspace via `workspace_io.init` before delegating to the existing `init_wiki` flow; the agent test suite (where applicable) is green.

Note on parallelism: This plan touches ONLY files under `agents/code-wiki-agent/`. Plan 04 touches ONLY files under `packages/vault-io/`. No file overlap → both can run in Wave 3 in parallel after Plan 02 ships the workspace_io source.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-workspace-io-port-m1/11-CONTEXT.md
@.planning/phases/11-workspace-io-port-m1/11-RESEARCH.md
@.planning/phases/11-workspace-io-port-m1/11-PATTERNS.md

<interfaces>
workspace_io.init signature (Plan 02 output):

def init(repo_root: Path, *, plugin: str, version: str, workspace: Path | None = None) -> None

Version introspection (D-13, RESEARCH.md verified):

importlib.metadata.version("code-wiki-agent")  # returns "0.1.0" under uv editable install

Existing call surface preserved:

- commands/init.py::run_init(topic, tool, force, vault_path) — current signature unchanged
- resolve_wiki_and_repo(vault_path) — Plan 04 keeps signature bit-identical
- init_wiki(...) — existing vault-io function, unchanged

All CODE_WIKI_REAL_VAULT_PATH occurrences (38 total; this plan handles agent half — 19 hits):

CLI help strings (6 occurrences):
- agents/code-wiki-agent/src/code_wiki_agent/cli.py:433  --vault help text
- agents/code-wiki-agent/src/code_wiki_agent/cli.py:455  --vault help text
- agents/code-wiki-agent/src/code_wiki_agent/cli.py:476  --vault help text
- agents/code-wiki-agent/src/code_wiki_agent/cli.py:514  --vault help text
- agents/code-wiki-agent/src/code_wiki_agent/cli.py:539  --vault help text
- agents/code-wiki-agent/src/code_wiki_agent/cli.py:573  --vault help text

MCP server (8 occurrences across Field descriptions, tool descriptions, inline comments):
- agents/code-wiki-agent/src/code_wiki_mcp/server.py:105  inline comment
- agents/code-wiki-agent/src/code_wiki_mcp/server.py:121  tool description string
- agents/code-wiki-agent/src/code_wiki_mcp/server.py:154  Field description
- agents/code-wiki-agent/src/code_wiki_mcp/server.py:196  Field description
- agents/code-wiki-agent/src/code_wiki_mcp/server.py:243  Field description
- agents/code-wiki-agent/src/code_wiki_mcp/server.py:309  Field description
- agents/code-wiki-agent/src/code_wiki_mcp/server.py:328  tool description string
- agents/code-wiki-agent/src/code_wiki_mcp/server.py:375  Field description

Docstrings (4 occurrences):
- agents/code-wiki-agent/src/code_wiki_agent/commands/log.py:48
- agents/code-wiki-agent/src/code_wiki_agent/commands/query.py:788
- agents/code-wiki-agent/src/code_wiki_agent/commands/init.py:52
- agents/code-wiki-agent/src/code_wiki_agent/config.py:38
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add workspace-io dep to agent pyproject + wire workspace_io.init into commands/init.py</name>
  <files>agents/code-wiki-agent/pyproject.toml, agents/code-wiki-agent/src/code_wiki_agent/commands/init.py</files>
  <read_first>
    - /Users/pat/Personal/deep-agents/agents/code-wiki-agent/pyproject.toml (current — 34 lines; add deps + sources)
    - /Users/pat/Personal/deep-agents/agents/code-wiki-agent/src/code_wiki_agent/commands/init.py (current — full file; prepend workspace_io.init call)
    - /Users/pat/Personal/deep-agents/agents/code-wiki-agent/src/code_wiki_agent/cli.py (lines 1-100 — confirm `importlib.metadata` already imported)
    - /Users/pat/Personal/deep-agents/packages/workspace-io/src/workspace_io/init.py (Plan 02 output — confirm `init` signature)
    - /Users/pat/Personal/deep-agents/.planning/phases/11-workspace-io-port-m1/11-PATTERNS.md (sections on agents/code-wiki-agent/pyproject.toml and agents/code-wiki-agent/src/code_wiki_agent/commands/init.py)
    - /Users/pat/Personal/deep-agents/.planning/phases/11-workspace-io-port-m1/11-RESEARCH.md (Architectural Responsibility Map row "CLI init command orchestration"; agents/code-wiki-agent/src/code_wiki_agent/commands/init.py section)
    - /Users/pat/Personal/deep-agents/.planning/phases/11-workspace-io-port-m1/11-CONTEXT.md (D-07, D-12, D-13)
  </read_first>
  <action>
    Add workspace-io dependency to the agent pyproject and wire the two-phase init flow.

    `agents/code-wiki-agent/pyproject.toml` (per PATTERNS.md section): apply two additions:
    - Append `"workspace-io"` to `[project] dependencies` (alongside vault-io, model-adapter, subagent-runtime)
    - Append `workspace-io = { workspace = true }` to the existing `[tool.uv.sources]` block (which already has entries for vault-io, model-adapter, subagent-runtime)

    `agents/code-wiki-agent/src/code_wiki_agent/commands/init.py` (per D-07 + PATTERNS.md section):
    1. Add imports at the top of the file:
       - `import importlib.metadata`
       - `from workspace_io import init as _ws_init`
       (Place after existing stdlib imports per the file's import grouping convention)
    2. Update the `vault_path` arg docstring (line 52) to reference `GRAPH_WIKI_WORKSPACE` instead of `CODE_WIKI_REAL_VAULT_PATH` (rebrand sweep; same change applied in Task 2 grep).
    3. In `run_init()`, BEFORE the existing `resolve_wiki_and_repo(vault_path)` call (around line 60), insert the two-phase init bootstrap:
       - Determine `repo_root`: if `vault_path is not None` then `repo_root = vault_path.parent`; else `repo_root = Path.cwd()` — this is the repo containing the new workspace
       - Call `_ws_init(repo_root, plugin="code-wiki-agent", version=importlib.metadata.version("code-wiki-agent"))` (D-12 — plugin name; D-13 — version source)
       - Catch nothing — let `workspace_io.init` failures propagate
    4. Leave the existing `resolve_wiki_and_repo(vault_path)` call and all downstream logic (`init_wiki`, etc.) unchanged

    Critically, the existing CLI behavior must be PRESERVED:
    - When the user runs `code-wiki-agent init /some/path` with an explicit path, `vault_path = /some/path`. The new `_ws_init(repo_root=/some/path.parent, ...)` creates `.graph-wiki.yaml` at `<parent>/graph-wiki/.graph-wiki.yaml`. The subsequent `resolve_wiki_and_repo(vault_path=/some/path)` short-circuits to `(vault_path.resolve(), ...)` — does NOT use the new manifest. This is intentional: explicit `vault_path` bypasses the manifest discovery (Plan 04 D-02 short-circuit branch).
    - When the user runs `code-wiki-agent init` (no path), `vault_path = None`. The new `_ws_init(repo_root=Path.cwd(), ...)` creates `<cwd>/graph-wiki/.graph-wiki.yaml`. The subsequent `resolve_wiki_and_repo(None)` then walks the manifest via `workspace_io.config.resolve()` and returns the wiki path inside the new workspace. Two-phase init unblocks the strict resolve in this case.

    After the edits, run `uv sync` to pick up the new dependency. Confirm the import works: `uv run --package code-wiki-agent python -c "from workspace_io import init; from code_wiki_agent.commands.init import run_init; print('imports ok')"`.

    Do NOT run the agent's full pytest suite here — agent tests are not in scope for this phase (Phase 12 BACKPORT-XX covers any test re-touch). The verify gate is import-correctness + grep checks.
  </action>
  <acceptance_criteria>
    - `agents/code-wiki-agent/pyproject.toml` includes workspace-io in dependencies: `grep -c '"workspace-io"' agents/code-wiki-agent/pyproject.toml` returns at least `1`
    - `agents/code-wiki-agent/pyproject.toml` includes workspace-io in [tool.uv.sources]: `grep -c 'workspace-io.*workspace = true' agents/code-wiki-agent/pyproject.toml` returns at least `1`
    - `commands/init.py` imports workspace_io.init: `grep -c 'from workspace_io import init' agents/code-wiki-agent/src/code_wiki_agent/commands/init.py` returns at least `1`
    - `commands/init.py` imports importlib.metadata: `grep -c 'importlib.metadata' agents/code-wiki-agent/src/code_wiki_agent/commands/init.py` returns at least `1`
    - `commands/init.py` calls `_ws_init` (or `init` from workspace_io) before `resolve_wiki_and_repo`: verify by reading the file and confirming the call order, then `grep -c 'plugin="code-wiki-agent"' agents/code-wiki-agent/src/code_wiki_agent/commands/init.py` returns at least `1`
    - `commands/init.py` docstring references GRAPH_WIKI_WORKSPACE: `grep -c 'GRAPH_WIKI_WORKSPACE' agents/code-wiki-agent/src/code_wiki_agent/commands/init.py` returns at least `1`; `grep -c 'CODE_WIKI_REAL_VAULT_PATH' agents/code-wiki-agent/src/code_wiki_agent/commands/init.py` returns `0`
    - `uv sync` exits 0
    - Import smoke test passes: `uv run --package code-wiki-agent python -c "from workspace_io import init; from code_wiki_agent.commands.init import run_init"` exits 0
  </acceptance_criteria>
  <verify>
    <automated>uv sync && grep -q '"workspace-io"' agents/code-wiki-agent/pyproject.toml && grep -q 'from workspace_io import init' agents/code-wiki-agent/src/code_wiki_agent/commands/init.py && grep -q 'importlib.metadata' agents/code-wiki-agent/src/code_wiki_agent/commands/init.py && ! grep -q 'CODE_WIKI_REAL_VAULT_PATH' agents/code-wiki-agent/src/code_wiki_agent/commands/init.py && uv run --package code-wiki-agent python -c "from workspace_io import init; from code_wiki_agent.commands.init import run_init"</automated>
  </verify>
  <done>
    Agent depends on workspace-io. commands/init.py is the orchestrator for the two-phase init flow (D-07). commands/init.py docstring rebranded. uv sync succeeds.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rebrand CODE_WIKI_REAL_VAULT_PATH across cli.py, MCP server, and remaining docstrings</name>
  <files>agents/code-wiki-agent/src/code_wiki_agent/cli.py, agents/code-wiki-agent/src/code_wiki_mcp/server.py, agents/code-wiki-agent/src/code_wiki_agent/commands/log.py, agents/code-wiki-agent/src/code_wiki_agent/commands/query.py, agents/code-wiki-agent/src/code_wiki_agent/config.py</files>
  <read_first>
    - /Users/pat/Personal/deep-agents/agents/code-wiki-agent/src/code_wiki_agent/cli.py (lines 420-580 — 6 --vault help strings at lines 433, 455, 476, 514, 539, 573)
    - /Users/pat/Personal/deep-agents/agents/code-wiki-agent/src/code_wiki_mcp/server.py (lines 100-400 — 8 hits at lines 105, 121, 154, 196, 243, 309, 328, 375)
    - /Users/pat/Personal/deep-agents/agents/code-wiki-agent/src/code_wiki_agent/commands/log.py (line 48 — docstring)
    - /Users/pat/Personal/deep-agents/agents/code-wiki-agent/src/code_wiki_agent/commands/query.py (line 788 — docstring)
    - /Users/pat/Personal/deep-agents/agents/code-wiki-agent/src/code_wiki_agent/config.py (line 38 — docstring)
    - /Users/pat/Personal/deep-agents/.planning/phases/11-workspace-io-port-m1/11-PATTERNS.md (sections on agents/code-wiki-agent/src/code_wiki_agent/cli.py and agents/code-wiki-agent/src/code_wiki_mcp/server.py and Docstring-only changes)
    - /Users/pat/Personal/deep-agents/.planning/phases/11-workspace-io-port-m1/11-RESEARCH.md (Common Pitfalls #6 — MCP Field descriptions are user-visible)
    - /Users/pat/Personal/deep-agents/.planning/phases/11-workspace-io-port-m1/11-CONTEXT.md (D-01)
  </read_first>
  <action>
    Rebrand all remaining `CODE_WIKI_REAL_VAULT_PATH` strings in the agent source tree to `GRAPH_WIKI_WORKSPACE`.

    `agents/code-wiki-agent/src/code_wiki_agent/cli.py`: at each of the 6 `typer.Option` calls (lines 433, 455, 476, 514, 539, 573), replace `CODE_WIKI_REAL_VAULT_PATH` with `GRAPH_WIKI_WORKSPACE` in the `help=` string. The pattern across all six is identical: `help="Vault path (default: CODE_WIKI_REAL_VAULT_PATH env var)"` -> `help="Vault path (default: GRAPH_WIKI_WORKSPACE env var)"`. No other lines change.

    `agents/code-wiki-agent/src/code_wiki_mcp/server.py`: 8 hits to rebrand per RESEARCH.md Common Pitfalls #6 — these are user-visible (Field descriptions are emitted to MCP clients as tool schema). For each occurrence at lines 105, 121, 154, 196, 243, 309, 328, 375 replace `CODE_WIKI_REAL_VAULT_PATH` with `GRAPH_WIKI_WORKSPACE`. The contexts vary:
    - Line 105: inline comment `# empty -> resolve from CODE_WIKI_REAL_VAULT_PATH env var` -> `# empty -> resolve from GRAPH_WIKI_WORKSPACE env var`
    - Line 121, 328: tool description strings — `"vault_path defaults to CODE_WIKI_REAL_VAULT_PATH env var."` -> `"vault_path defaults to GRAPH_WIKI_WORKSPACE env var."`
    - Lines 154, 196, 243, 309, 375: Pydantic `Field` descriptions — `Field("", description="Vault path (default: CODE_WIKI_REAL_VAULT_PATH env var)")` -> `Field("", description="Vault path (default: GRAPH_WIKI_WORKSPACE env var)")`

    Docstrings (3 files; each is a one-line edit in a module docstring or function docstring):
    - `agents/code-wiki-agent/src/code_wiki_agent/commands/log.py:48` — replace `CODE_WIKI_REAL_VAULT_PATH` with `GRAPH_WIKI_WORKSPACE` in the docstring
    - `agents/code-wiki-agent/src/code_wiki_agent/commands/query.py:788` — same
    - `agents/code-wiki-agent/src/code_wiki_agent/config.py:38` — same

    Apply each replacement precisely; do NOT change any other content on these lines. Use the Edit tool with exact-match strings.

    After all edits, run a sweeping grep to confirm zero remaining hits anywhere under `agents/code-wiki-agent/`:

      grep -rn CODE_WIKI_REAL_VAULT_PATH agents/code-wiki-agent/

    Must return zero lines.

    Optional smoke check: `uv run --package code-wiki-agent code-wiki-agent --help 2>&1 | head -30` should display Typer help without errors (and without any `CODE_WIKI_REAL_VAULT_PATH` text). If the CLI fails to load due to an import error from earlier work, fix that first.
  </action>
  <acceptance_criteria>
    - Zero CODE_WIKI_REAL_VAULT_PATH references in agent source: `grep -rE 'CODE_WIKI_REAL_VAULT_PATH' agents/code-wiki-agent/ --include="*.py"` returns zero lines
    - GRAPH_WIKI_WORKSPACE appears in cli.py at least 6 times (one per --vault option): `grep -c 'GRAPH_WIKI_WORKSPACE' agents/code-wiki-agent/src/code_wiki_agent/cli.py` returns at least `6`
    - GRAPH_WIKI_WORKSPACE appears in server.py at least 8 times: `grep -c 'GRAPH_WIKI_WORKSPACE' agents/code-wiki-agent/src/code_wiki_mcp/server.py` returns at least `8`
    - Docstrings rebranded: `grep -c 'GRAPH_WIKI_WORKSPACE' agents/code-wiki-agent/src/code_wiki_agent/commands/log.py agents/code-wiki-agent/src/code_wiki_agent/commands/query.py agents/code-wiki-agent/src/code_wiki_agent/config.py` shows non-zero for each
    - CLI loads without import errors: `uv run --package code-wiki-agent code-wiki-agent --help` exits 0 (or `code-wiki-mcp --help` if `--help` differs)
  </acceptance_criteria>
  <verify>
    <automated>! grep -rE 'CODE_WIKI_REAL_VAULT_PATH' agents/code-wiki-agent/ --include="*.py" && test $(grep -c 'GRAPH_WIKI_WORKSPACE' agents/code-wiki-agent/src/code_wiki_agent/cli.py) -ge 6 && test $(grep -c 'GRAPH_WIKI_WORKSPACE' agents/code-wiki-agent/src/code_wiki_mcp/server.py) -ge 8 && uv run --package code-wiki-agent python -c "from code_wiki_agent import cli; from code_wiki_mcp import server"</automated>
  </verify>
  <done>
    All CODE_WIKI_REAL_VAULT_PATH occurrences across `agents/code-wiki-agent/` are renamed to GRAPH_WIKI_WORKSPACE (6 CLI help, 8 MCP server, 3 docstrings = 17 hits handled here; the 19th was commands/init.py docstring updated in Task 1). CLI and MCP server modules still import cleanly.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client -> Pydantic Field descriptions | User-visible tool schema (Pitfall #6) — incorrect text misleads MCP consumers but is not a code-execution boundary |
| CLI user -> Typer --vault arg | Documented env var change; user-facing surface |
| code-wiki-agent init -> workspace_io.init | Trusted internal call; repo_root derived from CLI args or cwd |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-09 | Information Disclosure | MCP Field description leakage | accept | Field descriptions name an env var — same risk profile as the previous text; no new sensitive data |
| T-11-10 | Tampering | workspace_io.init call from CLI | mitigate | repo_root is `vault_path.parent` (CLI-provided) or `Path.cwd()`; workspace_io.init does its own Path.resolve and only writes inside repo_root/graph-wiki |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No new external packages added — workspace-io is a workspace dep |
</threat_model>

<verification>
- `grep -rE 'CODE_WIKI_REAL_VAULT_PATH' agents/code-wiki-agent/ --include="*.py"` returns zero lines
- `commands/init.py` calls `workspace_io.init` BEFORE `resolve_wiki_and_repo` (confirmed by reading the file body)
- `uv sync` succeeds; agent depends on workspace-io
- CLI and MCP server modules import cleanly
- Plan 04 + Plan 05 together ensure: `grep -rE 'CODE_WIKI_REAL_VAULT_PATH' packages/ agents/ --include="*.py"` returns zero lines
</verification>

<success_criteria>
- WS-07 (agent half) satisfied: env-var rebrand sweep completes
- WS-04 fully satisfied: workspace_io.init is wired into the `code-wiki-agent init` flow per D-07
- Phase 11 SC #2 partially satisfied (the env-var rename half — full SC#2 requires Plan 06's end-to-end verification)
</success_criteria>

<output>
Create `.planning/phases/11-workspace-io-port-m1/11-05-SUMMARY.md` when done.
</output>
