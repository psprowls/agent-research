---
phase: 11-workspace-io-port-m1
plan: 04
type: execute
wave: 3
depends_on: [11-01, 11-02]
files_modified:
  - packages/vault-io/pyproject.toml
  - packages/vault-io/src/vault_io/_workspace.py
  - packages/vault-io/src/vault_io/append_log.py
  - packages/vault-io/src/vault_io/detect_containers.py
  - packages/vault-io/src/vault_io/graph_analyzer.py
  - packages/vault-io/tests/test_ports_importable.py
  - packages/vault-io/tests/conftest.py
autonomous: true
requirements: [WS-07, WS-08]
must_haves:
  truths:
    - "vault-io declares workspace-io as a workspace dependency in pyproject.toml"
    - "vault-io._workspace.resolve_wiki_and_repo signature is bit-identical to pre-port (no caller breaks)"
    - "vault-io._workspace.resolve_wiki_and_repo delegates to workspace_io.config.resolve() when vault_path is None"
    - "vault-io._workspace.resolve_wiki_and_repo short-circuits when vault_path is provided (MCP boundary contract — Phase 11 SC#3)"
    - "CODE_WIKI_REAL_VAULT_PATH is gone from vault-io src and tests; GRAPH_WIKI_WORKSPACE is the only honored env var"
    - "All 9 vault-io call sites of `wiki, _ = resolve_wiki_and_repo()` continue to work without changes"
  artifacts:
    - path: "packages/vault-io/pyproject.toml"
      provides: "workspace-io dependency declared"
      contains: "workspace-io"
    - path: "packages/vault-io/src/vault_io/_workspace.py"
      provides: "Delegation shim — workspace_io.config.resolve() backend"
      contains: "from workspace_io"
    - path: "packages/vault-io/tests/test_ports_importable.py"
      provides: "Updated tests for env-var rename and new strict-error test"
      contains: "GRAPH_WIKI_WORKSPACE"
    - path: "packages/vault-io/tests/conftest.py"
      provides: "Conftest fixture using GRAPH_WIKI_WORKSPACE (one-line update)"
      contains: "GRAPH_WIKI_WORKSPACE"
  key_links:
    - from: "vault-io._workspace.resolve_wiki_and_repo"
      to: "workspace_io.config.resolve"
      via: "import + call when vault_path is None"
      pattern: "from workspace_io import config"
    - from: "vault-io._workspace.resolve_wiki_and_repo (explicit vault_path branch)"
      to: "workspace_io.config._find_repo_root"
      via: "import + call for repo discovery on explicit path"
      pattern: "from workspace_io.config import _find_repo_root"
    - from: "vault-io pyproject.toml [tool.uv.sources]"
      to: "packages/workspace-io/"
      via: "workspace = true marker"
      pattern: "workspace-io"
---

<objective>
Rewrite `vault-io._workspace.resolve_wiki_and_repo` as a thin delegation shim over `workspace_io.config.resolve()` while preserving the bit-identical public signature so all 9 in-tree vault-io call sites and the agent's MCP boundary contract are unaffected. Update vault-io's pyproject to depend on workspace-io. Rename `CODE_WIKI_REAL_VAULT_PATH` to `GRAPH_WIKI_WORKSPACE` across vault-io src docstrings, test fixtures, and the existing two delegation tests. Add the new strict-raises test for D-03 inside vault-io's test suite.

Purpose: Close WS-08 (delegation) and the vault-io half of WS-07 (env-var rename). Plan 05 handles the agent half of WS-07. This plan is the bridge between workspace-io's new surface and the vault-io callers — once it merges, vault-io modules transparently gain `.graph-wiki.yaml` manifest discovery.

Output: vault-io passes its full test suite (`uv run --package vault-io pytest`) against the new delegation shim. The MCP boundary contract (explicit `vault_path` short-circuit) is verified intact.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-workspace-io-port-m1/11-CONTEXT.md
@.planning/phases/11-workspace-io-port-m1/11-RESEARCH.md
@.planning/phases/11-workspace-io-port-m1/11-PATTERNS.md

<interfaces>
Bit-identical signature (preserved post-port):

def resolve_wiki_and_repo(vault_path: Path | None = None) -> tuple[Path, Path | None]

Post-port delegation logic (per PATTERNS.md _workspace.py + RESEARCH.md Code Examples):

from workspace_io import config as _ws_config
from workspace_io import paths as _ws_paths
from workspace_io.config import _find_repo_root

if vault_path is not None: return vault_path.resolve(), _find_repo_root(vault_path)
else: cfg = _ws_config.resolve(); return _ws_paths.wiki_dir(cfg.workspace), cfg.repo_root

workspace_io public surface available to vault-io:

- workspace_io.config.GraphWikiConfig dataclass (frozen): workspace: Path, repo_root: Path
- workspace_io.config.resolve(cwd=None) — raises RuntimeError if no manifest and no env override
- workspace_io.config._find_repo_root — walks up looking for .git; returns Path or None
- workspace_io.paths.wiki_dir(workspace) — returns workspace / "wiki"

9 vault-io call sites that MUST continue to work without changes:

- vault_io/append_log.py:139         wiki, _ = resolve_wiki_and_repo()
- vault_io/detect_containers.py:174  wiki, _ = resolve_wiki_and_repo()
- vault_io/graph_analyzer.py:181     wiki, _ = resolve_wiki_and_repo()
- vault_io/init_vault.py:305         wiki, _ = resolve_wiki_and_repo()
- vault_io/scan_monorepo.py:1082     wiki, _ = resolve_wiki_and_repo()
- vault_io/update_index.py:328       wiki, _ = resolve_wiki_and_repo()
- vault_io/update_tokens.py:175      wiki, _ = resolve_wiki_and_repo()
- ingest_source.py and ingest_work_item.py — caller passes vault_path
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add workspace-io dep to vault-io pyproject + rewrite _workspace.py shim</name>
  <files>packages/vault-io/pyproject.toml, packages/vault-io/src/vault_io/_workspace.py</files>
  <read_first>
    - /Users/pat/Personal/deep-agents/packages/vault-io/pyproject.toml (current — 18 lines; add deps + sources)
    - /Users/pat/Personal/deep-agents/packages/vault-io/src/vault_io/_workspace.py (current — 34 lines; full rewrite)
    - /Users/pat/Personal/deep-agents/packages/workspace-io/src/workspace_io/config.py (Plan 02 output — confirm `_find_repo_root` exists for import)
    - /Users/pat/Personal/deep-agents/packages/workspace-io/src/workspace_io/paths.py (Plan 02 output — confirm `wiki_dir` exists)
    - /Users/pat/Personal/deep-agents/.planning/phases/11-workspace-io-port-m1/11-PATTERNS.md (section on packages/vault-io/src/vault_io/_workspace.py — complete post-port file body; section on packages/vault-io/pyproject.toml — two additions)
    - /Users/pat/Personal/deep-agents/.planning/phases/11-workspace-io-port-m1/11-RESEARCH.md (vault-io._workspace.resolve_wiki_and_repo Current Signature and Callers section; Code Examples)
    - /Users/pat/Personal/deep-agents/.planning/phases/11-workspace-io-port-m1/11-CONTEXT.md (D-01, D-02, D-03)
  </read_first>
  <action>
    Add the workspace-io dependency to vault-io's pyproject.toml and rewrite `_workspace.py` as a delegation shim.

    `packages/vault-io/pyproject.toml`: Apply the two additions from PATTERNS.md section on packages/vault-io/pyproject.toml:
    - Add `"workspace-io"` to `[project] dependencies` array (alongside `python-frontmatter>=1.1` and `boto3>=1.38`)
    - Add a new `[tool.uv.sources]` section with `workspace-io = { workspace = true }` so uv resolves it from the workspace, not from PyPI

    `packages/vault-io/src/vault_io/_workspace.py`: Replace the entire file body with the delegation shim per PATTERNS.md section on packages/vault-io/src/vault_io/_workspace.py (which copies RESEARCH.md Code Examples). The shim:
    - Preserves signature: `resolve_wiki_and_repo(vault_path: Path | None = None) -> tuple[Path, Path | None]`
    - Two-tier logic per D-02: if `vault_path` is provided then short-circuit `(vault_path.resolve(), _find_repo_root(vault_path))`; else call `_ws_config.resolve()` and return `(_ws_paths.wiki_dir(cfg.workspace), cfg.repo_root)`
    - Removes all `os.environ.get("CODE_WIKI_REAL_VAULT_PATH")` references (D-01)
    - Imports: `from workspace_io import config as _ws_config`, `from workspace_io import paths as _ws_paths`, `from workspace_io.config import _find_repo_root`
    - Updated docstring naming the MCP boundary contract (D-02) and the bootstrap command (D-03)

    After writing, run `uv sync` to pick up the new dependency edge.

    Confirm: `uv run --package vault-io python -c "from vault_io._workspace import resolve_wiki_and_repo; from vault_io import resolve_wiki_and_repo as re_export; assert resolve_wiki_and_repo is re_export"` — re-export from `vault_io/__init__.py` still works unchanged (no edit needed to `__init__.py`).
  </action>
  <acceptance_criteria>
    - `packages/vault-io/pyproject.toml` includes `"workspace-io"` in `[project] dependencies`: `grep -c '"workspace-io"' packages/vault-io/pyproject.toml` returns at least `1`
    - `packages/vault-io/pyproject.toml` includes the `[tool.uv.sources]` block with workspace-io: `grep -c 'workspace-io = { workspace = true }' packages/vault-io/pyproject.toml` returns `1`
    - `packages/vault-io/src/vault_io/_workspace.py` imports from workspace_io: `grep -cE 'from workspace_io' packages/vault-io/src/vault_io/_workspace.py` returns at least `2` (config + paths or _find_repo_root)
    - `packages/vault-io/src/vault_io/_workspace.py` does NOT reference `CODE_WIKI_REAL_VAULT_PATH`: `grep -c 'CODE_WIKI_REAL_VAULT_PATH' packages/vault-io/src/vault_io/_workspace.py` returns `0`
    - Signature preserved: shell check exits 0
    - `uv sync` succeeds: exits 0
    - Re-export intact: `uv run --package vault-io python -c "from vault_io import resolve_wiki_and_repo; print('ok')"` exits 0
  </acceptance_criteria>
  <verify>
    <automated>uv sync && grep -q 'workspace-io = { workspace = true }' packages/vault-io/pyproject.toml && ! grep -q CODE_WIKI_REAL_VAULT_PATH packages/vault-io/src/vault_io/_workspace.py && uv run --package vault-io python -c "from vault_io import resolve_wiki_and_repo; import inspect; sig = str(inspect.signature(resolve_wiki_and_repo)); assert 'vault_path' in sig"</automated>
  </verify>
  <done>
    vault-io depends on workspace-io. `_workspace.py` is a delegation shim. Signature is bit-identical so the 9 call sites continue to work. CODE_WIKI_REAL_VAULT_PATH is gone from `_workspace.py`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update vault-io tests (conftest + test_ports_importable) and rebrand vault-io src docstrings</name>
  <files>packages/vault-io/tests/conftest.py, packages/vault-io/tests/test_ports_importable.py, packages/vault-io/src/vault_io/append_log.py, packages/vault-io/src/vault_io/detect_containers.py, packages/vault-io/src/vault_io/graph_analyzer.py</files>
  <read_first>
    - /Users/pat/Personal/deep-agents/packages/vault-io/tests/conftest.py (current — 38 lines; one-line update at line 35)
    - /Users/pat/Personal/deep-agents/packages/vault-io/tests/test_ports_importable.py (current — 91 lines; two targeted updates + one new test)
    - /Users/pat/Personal/deep-agents/packages/vault-io/src/vault_io/append_log.py (line 9 — docstring CODE_WIKI_REAL_VAULT_PATH reference)
    - /Users/pat/Personal/deep-agents/packages/vault-io/src/vault_io/detect_containers.py (line 6 — docstring)
    - /Users/pat/Personal/deep-agents/packages/vault-io/src/vault_io/graph_analyzer.py (lines 5-6 — docstring)
    - /Users/pat/Personal/deep-agents/.planning/phases/11-workspace-io-port-m1/11-PATTERNS.md (sections on packages/vault-io/tests/test_ports_importable.py and packages/vault-io/tests/conftest.py and Docstring-only changes)
    - /Users/pat/Personal/deep-agents/.planning/phases/11-workspace-io-port-m1/11-RESEARCH.md (Common Pitfalls #1 — env-var test needs workspace/wiki path now; Common Pitfalls #6 — docstring breadth)
    - /Users/pat/Personal/deep-agents/.planning/phases/11-workspace-io-port-m1/11-CONTEXT.md (D-01, D-02, D-03)
  </read_first>
  <action>
    Update vault-io tests for the env-var rename + delegation semantics, and rebrand vault-io src docstrings.

    `packages/vault-io/tests/conftest.py` (line 35 — single change per PATTERNS.md): replace `os.environ.get("CODE_WIKI_REAL_VAULT_PATH")` with `os.environ.get("GRAPH_WIKI_WORKSPACE")`. No other lines change.

    `packages/vault-io/tests/test_ports_importable.py`: Apply three updates per PATTERNS.md section on test_ports_importable.py:
    1. `test_resolve_wiki_and_repo_raises_on_no_config` (lines 67-78): rename `monkeypatch.delenv("CODE_WIKI_REAL_VAULT_PATH", raising=False)` to `monkeypatch.delenv("GRAPH_WIKI_WORKSPACE", raising=False)`; update the error-string assertion from `assert "CODE_WIKI_REAL_VAULT_PATH" in str(exc)` to `assert "code-wiki-agent init" in str(exc)` (matches D-03 RuntimeError message). Also `monkeypatch.setattr("workspace_io.config._find_repo_root", lambda _: None)` so the test does not accidentally pick up the real repo's `.git` and find a real workspace.
    2. `test_resolve_wiki_and_repo_honors_env_var` (lines 81-91): per RESEARCH.md Pitfall #1, the assertion shape changes. Rewrite as: create `fake_workspace = tmp_path / "workspace"`; `fake_workspace.mkdir()`; `monkeypatch.setenv("GRAPH_WIKI_WORKSPACE", str(fake_workspace))`; call `resolve_wiki_and_repo()`; assert `wiki == (fake_workspace / "wiki").resolve()`. No manifest file needed because the env-override branch skips the strict check (Plan 02 sanity-check 1).
    3. Add a NEW test `test_resolve_wiki_and_repo_strict_raises_without_manifest` (per PATTERNS.md test_ports_importable.py "New test to add"): delenv GRAPH_WIKI_WORKSPACE, monkeypatch.chdir(tmp_path), monkeypatch.setattr("workspace_io.config._find_repo_root", lambda _: None), then `pytest.raises(RuntimeError, match="code-wiki-agent init")` around `resolve_wiki_and_repo()`.

    Rebrand vault-io src docstrings (per PATTERNS.md Docstring-only changes; behavior unchanged):
    - `packages/vault-io/src/vault_io/append_log.py` line 9: replace `CODE_WIKI_REAL_VAULT_PATH` with `GRAPH_WIKI_WORKSPACE` in the module docstring
    - `packages/vault-io/src/vault_io/detect_containers.py` line 6: same docstring rename
    - `packages/vault-io/src/vault_io/graph_analyzer.py` lines 5-6: same docstring rename

    After all edits, run the vault-io test suite to confirm the delegation shim + tests are green:

      uv run --package vault-io pytest -x

    If a test fails because the real environment has `GRAPH_WIKI_WORKSPACE` set, the conftest fixture should already neutralize that via `monkeypatch.delenv` in the affected tests. Otherwise investigate.
  </action>
  <acceptance_criteria>
    - `packages/vault-io/tests/conftest.py` references `GRAPH_WIKI_WORKSPACE` (not the old name): `grep -c 'GRAPH_WIKI_WORKSPACE' packages/vault-io/tests/conftest.py` returns at least `1`; `grep -c 'CODE_WIKI_REAL_VAULT_PATH' packages/vault-io/tests/conftest.py` returns `0`
    - `packages/vault-io/tests/test_ports_importable.py` references `GRAPH_WIKI_WORKSPACE`: `grep -c 'GRAPH_WIKI_WORKSPACE' packages/vault-io/tests/test_ports_importable.py` returns at least `2` (env-var test + strict-raises test); `grep -c 'CODE_WIKI_REAL_VAULT_PATH' packages/vault-io/tests/test_ports_importable.py` returns `0`
    - `packages/vault-io/tests/test_ports_importable.py` includes the new test: `grep -c 'test_resolve_wiki_and_repo_strict_raises_without_manifest' packages/vault-io/tests/test_ports_importable.py` returns `1`
    - vault-io docstring sweep is clean: `grep -rE 'CODE_WIKI_REAL_VAULT_PATH' packages/vault-io/src/ --include="*.py"` returns zero lines
    - vault-io test suite green: `uv run --package vault-io pytest` exits 0
  </acceptance_criteria>
  <verify>
    <automated>uv run --package vault-io pytest -x && ! grep -rE 'CODE_WIKI_REAL_VAULT_PATH' packages/vault-io/src/ packages/vault-io/tests/ --include="*.py" && grep -q 'test_resolve_wiki_and_repo_strict_raises_without_manifest' packages/vault-io/tests/test_ports_importable.py</automated>
  </verify>
  <done>
    vault-io test suite passes. conftest fixture uses `GRAPH_WIKI_WORKSPACE`. Existing two delegation tests updated for new shim semantics. New strict-raises test verifies D-03 error message at the vault-io boundary. Three vault-io src docstrings rebranded. No `CODE_WIKI_REAL_VAULT_PATH` references remain in `packages/vault-io/`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client -> vault-io.resolve_wiki_and_repo | `vault_path` arg from MCP tool call is untrusted; current behavior preserved (Path.resolve only) |
| env -> vault-io._workspace | `GRAPH_WIKI_WORKSPACE` handled inside workspace_io, not vault-io anymore (D-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-07 | Tampering | `vault_path` arg from MCP | mitigate | `vault_path.resolve()` normalizes; behavior bit-identical to pre-port (no regression introduced) |
| T-11-08 | Information Disclosure | Error message from D-03 | accept | Error names the bootstrap command — necessary UX; no sensitive paths leaked |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No new external packages introduced — workspace-io is a workspace dep |
</threat_model>

<verification>
- `uv sync` resolves the new vault-io -> workspace-io edge
- `uv run --package vault-io pytest -x` exits 0 (full vault-io suite green)
- `grep -rE 'CODE_WIKI_REAL_VAULT_PATH' packages/vault-io/ --include="*.py"` returns zero lines
- Signature of `resolve_wiki_and_repo` is bit-identical: still accepts `vault_path: Path | None = None` and returns `tuple[Path, Path | None]`
- 9 existing call sites in vault-io src are NOT modified by this plan (verify with `git diff` showing only the 7 listed files)
</verification>

<success_criteria>
- WS-07 (vault-io half) satisfied: env var renamed across vault-io src + tests + conftest
- WS-08 satisfied: vault-io._workspace delegates to workspace_io.config.resolve() with explicit-path short-circuit preserved
- Phase 11 SC #3 satisfied: MCP boundary contract intact (vault_path arg short-circuits before delegating)
</success_criteria>

<output>
Create `.planning/phases/11-workspace-io-port-m1/11-04-SUMMARY.md` when done.
</output>
