---
phase: 11-workspace-io-port-m1
plan: 03
type: execute
wave: 3
depends_on: [11-01, 11-02]
files_modified:
  - packages/workspace-io/tests/test_config.py
  - packages/workspace-io/tests/test_manifest.py
  - packages/workspace-io/tests/test_manifest_v2_roundtrip.py
  - packages/workspace-io/tests/test_init.py
  - packages/workspace-io/tests/test_init_records_version.py
  - packages/workspace-io/tests/test_init_bumps_version.py
  - packages/workspace-io/tests/test_paths.py
  - packages/workspace-io/tests/test_local_config.py
  - packages/workspace-io/tests/test_render.py
  - packages/workspace-io/tests/test_warn_if_stale.py
  - packages/workspace-io/tests/test_pending_updates.py
autonomous: true
requirements: [WS-09]
must_haves:
  truths:
    - "All ported workspace-io tests pass via uv run --package workspace-io pytest"
    - "test_schema.py is NOT created (D-06)"
    - "test_manifest_v1_read.py is NOT created — replaced with a single test_read_raises_on_v1 inside test_manifest.py (D-14)"
    - "test_warn_if_stale.py uses a v2 manifest with applied_version null to simulate the old v1-coercion behavior (D-14 rewrite)"
    - "test_config.py includes test_resolve_raises_when_no_manifest_found (D-03 new test)"
  artifacts:
    - path: "packages/workspace-io/tests/test_config.py"
      provides: "Config resolve() tests (9 ported + 1 new strict-raises)"
      contains: "def test_"
    - path: "packages/workspace-io/tests/test_manifest.py"
      provides: "Manifest read/write tests + new v1-raises test (D-14)"
      contains: "def test_read_raises_on_v1"
    - path: "packages/workspace-io/tests/test_init.py"
      provides: "Init bootstrap tests — workspace creation, gitignore, idempotency"
      contains: "def test_"
    - path: "packages/workspace-io/tests/test_warn_if_stale.py"
      provides: "Version drift tests — v1-coerced test rewritten as null-applied test"
      contains: "def test_null_applied_version_no_signal"
  key_links:
    - from: "tests"
      to: "workspace_io.config.GraphWikiConfig, resolve"
      via: "from workspace_io.config import GraphWikiConfig, resolve"
      pattern: "from workspace_io"
    - from: "test_render.py marker assertions"
      to: "workspace_io.render.AUTO_START / AUTO_END"
      via: "import constants (do not hardcode in tests)"
      pattern: "from workspace_io.render import AUTO_START, AUTO_END"
---

<objective>
Port the 11 active test files from lattice-workspace/tests/ to packages/workspace-io/tests/, applying the rebrand sweep, the v1-coercion rewrite (D-14), the work-schema test drop (D-06), and the new strict-resolve test (D-03). Verify the full ported suite is green.

Purpose: Pin the behavior of every ported `workspace_io` symbol against a regression suite, including the two intentional divergences from lattice (strict resolve, v1-raises).

Output: A green `uv run --package workspace-io pytest` suite of approximately 60 tests covering config, manifest, init, paths, local_config, render, versions, and pending_updates.

Note on wave placement: This plan is Wave 3 (after Plans 01 and 02 are complete) so the test run in Task 3 has live source to exercise. Test file creation in Tasks 1 and 2 only depends on the analog files; running pytest requires Plan 02's source modules to exist.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-workspace-io-port-m1/11-CONTEXT.md
@.planning/phases/11-workspace-io-port-m1/11-RESEARCH.md
@.planning/phases/11-workspace-io-port-m1/11-PATTERNS.md

<interfaces>
<!-- Test file mapping: lattice analog -> workspace-io ported -->

| Source file (read-only) | Target file (port + rebrand) | Action |
|-------------------------|------------------------------|--------|
| lattice-workspace/tests/test_config.py | tests/test_config.py | Port + add test_resolve_raises_when_no_manifest_found (D-03) |
| lattice-workspace/tests/test_manifest.py | tests/test_manifest.py | Port + add test_read_raises_on_v1 (D-14 replacement) |
| lattice-workspace/tests/test_manifest_v2_roundtrip.py | tests/test_manifest_v2_roundtrip.py | Port verbatim with rebrand |
| lattice-workspace/tests/test_manifest_v1_read.py | (DROP) | Drop entirely per D-14 |
| lattice-workspace/tests/test_init.py | tests/test_init.py | Port; drop test_creates_work_schema (D-06) |
| lattice-workspace/tests/test_init_records_version.py | tests/test_init_records_version.py | Port verbatim with rebrand |
| lattice-workspace/tests/test_init_bumps_version.py | tests/test_init_bumps_version.py | Port verbatim with rebrand |
| lattice-workspace/tests/test_paths.py | tests/test_paths.py | Port verbatim with rebrand |
| lattice-workspace/tests/test_local_config.py | tests/test_local_config.py | Port verbatim with rebrand |
| lattice-workspace/tests/test_render.py | tests/test_render.py | Port with marker-string rebrand |
| lattice-workspace/tests/test_warn_if_stale.py | tests/test_warn_if_stale.py | Port; rewrite test_v1_coerced_entry_no_signal -> test_null_applied_version_no_signal (D-14) |
| lattice-workspace/tests/test_pending_updates.py | tests/test_pending_updates.py | Port verbatim with rebrand |
| lattice-workspace/tests/test_schema.py | (DROP) | Drop entirely per D-06 |

<!-- Rebrand symbol map for tests -->

| lattice in test | workspace-io in test |
|-----------------|---------------------|
| from lattice_workspace.config import LatticeConfig, resolve | from workspace_io.config import GraphWikiConfig, resolve |
| tmp_path / "lattice" | tmp_path / "graph-wiki" |
| ".lattice.local.yaml" | ".graph-wiki.local.yaml" |
| ".lattice.yaml" | ".graph-wiki.yaml" |
| "lattice-directory:" | "graph-wiki-directory:" |
| monkeypatch.setattr("lattice_workspace.config._find_repo_root", ...) | "workspace_io.config._find_repo_root" |
| [sys.executable, "-m", "lattice_workspace.config"] | [sys.executable, "-m", "workspace_io.config"] |
| "lattice-wiki" (plugin name in test args) | "graph-wiki-agent" |
| "<!-- lattice-workspace:auto:plugins:start -->" | "<!-- workspace-io:auto:plugins:start -->" (or import from workspace_io.render) |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port verbatim-rebrand test files (6 files, 27 tests)</name>
  <files>packages/workspace-io/tests/test_paths.py, packages/workspace-io/tests/test_local_config.py, packages/workspace-io/tests/test_manifest_v2_roundtrip.py, packages/workspace-io/tests/test_init_records_version.py, packages/workspace-io/tests/test_init_bumps_version.py, packages/workspace-io/tests/test_pending_updates.py</files>
  <read_first>
    - /Users/pat/Personal/lattice/packages/lattice-workspace/tests/test_paths.py (analog — 7 tests)
    - /Users/pat/Personal/lattice/packages/lattice-workspace/tests/test_local_config.py (analog — 9 tests)
    - /Users/pat/Personal/lattice/packages/lattice-workspace/tests/test_manifest_v2_roundtrip.py (analog — 3 tests)
    - /Users/pat/Personal/lattice/packages/lattice-workspace/tests/test_init_records_version.py (analog — 3 tests)
    - /Users/pat/Personal/lattice/packages/lattice-workspace/tests/test_init_bumps_version.py (analog — 1 test)
    - /Users/pat/Personal/lattice/packages/lattice-workspace/tests/test_pending_updates.py (analog — 4 tests)
    - /Users/pat/Personal/deep-agents/.planning/phases/11-workspace-io-port-m1/11-PATTERNS.md (Test Pattern Assignments section)
    - /Users/pat/Personal/deep-agents/.planning/phases/11-workspace-io-port-m1/11-RESEARCH.md (Test Porting Strategy — Groups 3, 4, 5, 7)
  </read_first>
  <action>
    Port each of these 6 test files from the lattice source to packages/workspace-io/tests/ applying ONLY the rebrand symbol map from interfaces above.

    For each source file:
    1. Copy the file content verbatim from lattice source.
    2. Apply the rebrand symbol map (search-replace):
       - `lattice_workspace` -> `workspace_io` in all imports
       - `LatticeConfig` -> `GraphWikiConfig`
       - `tmp_path / "lattice"` -> `tmp_path / "graph-wiki"`
       - `".lattice.yaml"` -> `".graph-wiki.yaml"`
       - `".lattice.local.yaml"` -> `".graph-wiki.local.yaml"`
       - `"lattice-directory:"` -> `"graph-wiki-directory:"`
       - Plugin name strings: `"lattice-wiki"` -> `"graph-wiki-agent"`. Other `"lattice-*"` plugin names exist only to test multi-plugin accumulation — replace each with a distinct stand-in like `"code-wiki-second"`, `"code-wiki-third"` so the multi-entry test still exercises multiple plugins.
       - `monkeypatch.setattr("lattice_workspace.config._find_repo_root", ...)` -> `"workspace_io.config._find_repo_root"`
       - `[sys.executable, "-m", "lattice_workspace.config"]` -> `[sys.executable, "-m", "workspace_io.config"]`
    3. Do NOT add new tests in this task — that is Task 3.
    4. Do NOT touch the `assets/` dir from tests — tests only read manifest YAML files in tmp_path.

    Specific file notes:
    - `test_paths.py`: 7 tests covering each helper (manifest_path, wiki_dir, raw_dir, work_dir, knowledge_dir, graph_dir) + string-coercion test. Rebrand only.
    - `test_local_config.py`: 9 tests for the bespoke parser (missing file, key reads, inline comments, quote strip, blank/comment/malformed lines). Rebrand only; also rebrand the `lattice-directory:` key in YAML content fixtures.
    - `test_manifest_v2_roundtrip.py`: 3 tests (v2 write/read, key order preserved, block style not flow style). Rebrand only.
    - `test_init_records_version.py`: 3 tests (both version fields written on init, idempotent same-version no rewrite, missing version kwarg raises TypeError). Rebrand only — D-12 unchanged.
    - `test_init_bumps_version.py`: 1 test (re-init with newer version bumps both fields). Rebrand only.
    - `test_pending_updates.py`: 4 tests (only mismatched returned, no mutation, frozen dataclass, no-manifest returns empty). Rebrand only.

    Total this task: 27 tests across 6 files.
  </action>
  <acceptance_criteria>
    - All 6 files exist at the listed paths
    - No lattice references remain in these 6 files. `grep -lE 'lattice_workspace|LatticeConfig|\.lattice\.yaml|\.lattice\.local\.yaml|lattice-directory|"lattice-wiki"' packages/workspace-io/tests/test_paths.py packages/workspace-io/tests/test_local_config.py packages/workspace-io/tests/test_manifest_v2_roundtrip.py packages/workspace-io/tests/test_init_records_version.py packages/workspace-io/tests/test_init_bumps_version.py packages/workspace-io/tests/test_pending_updates.py` returns no matches
    - All 6 files import from `workspace_io.*`: `grep -l 'from workspace_io' packages/workspace-io/tests/test_paths.py packages/workspace-io/tests/test_local_config.py packages/workspace-io/tests/test_manifest_v2_roundtrip.py packages/workspace-io/tests/test_init_records_version.py packages/workspace-io/tests/test_init_bumps_version.py packages/workspace-io/tests/test_pending_updates.py` lists all 6 files
    - Test count matches analogs: total `def test_` count across these 6 files equals 27 (7+9+3+3+1+4)
  </acceptance_criteria>
  <verify>
    <automated>for f in packages/workspace-io/tests/test_paths.py packages/workspace-io/tests/test_local_config.py packages/workspace-io/tests/test_manifest_v2_roundtrip.py packages/workspace-io/tests/test_init_records_version.py packages/workspace-io/tests/test_init_bumps_version.py packages/workspace-io/tests/test_pending_updates.py; do test -f "$f" || exit 1; done; ! grep -rE 'lattice_workspace|LatticeConfig|\.lattice\.yaml' packages/workspace-io/tests/ --include="*.py"</automated>
  </verify>
  <done>
    27 tests across 6 verbatim-rebrand test files written. No lattice references remain. Tests are not yet executed — Task 3 runs the full suite.
  </done>
</task>

<task type="auto">
  <name>Task 2: Port behavior-rewrite test files (test_init, test_render, test_warn_if_stale)</name>
  <files>packages/workspace-io/tests/test_init.py, packages/workspace-io/tests/test_render.py, packages/workspace-io/tests/test_warn_if_stale.py</files>
  <read_first>
    - /Users/pat/Personal/lattice/packages/lattice-workspace/tests/test_init.py (analog — 15 tests; drop test_creates_work_schema per D-06)
    - /Users/pat/Personal/lattice/packages/lattice-workspace/tests/test_render.py (analog — 6 tests; rebrand AUTO marker strings)
    - /Users/pat/Personal/lattice/packages/lattice-workspace/tests/test_warn_if_stale.py (analog — 4 tests; rewrite test_v1_coerced_entry_no_signal per D-14)
    - /Users/pat/Personal/deep-agents/.planning/phases/11-workspace-io-port-m1/11-PATTERNS.md (test sections for test_init.py, test_render.py, test_warn_if_stale.py — includes the rewrite body for test_null_applied_version_no_signal)
    - /Users/pat/Personal/deep-agents/.planning/phases/11-workspace-io-port-m1/11-RESEARCH.md (Test Porting Strategy Group 5 + Group 6 + Group 7; Pitfall #4 — workspace name in test_init)
    - /Users/pat/Personal/deep-agents/.planning/phases/11-workspace-io-port-m1/11-CONTEXT.md (D-06, D-12, D-14)
  </read_first>
  <action>
    Port these 3 test files with behavior changes (not pure verbatim).

    `test_init.py`: Port from lattice source applying rebrand symbol map (same as Task 1) plus:
    - Drop `test_creates_work_schema` entirely (D-06 — write_schema removed in init.py)
    - All `tmp_path / "lattice"` -> `tmp_path / "graph-wiki"` (Pitfall #4 — default workspace name changed)
    - Plugin name args: first plugin -> `"graph-wiki-agent"`; second plugin in multi-plugin accumulation tests -> a distinct name like `"code-wiki-second"`
    - Gitignore assertion strings: `.lattice.local.yaml` -> `.graph-wiki.local.yaml`
    - Auto-marker assertions: `<!-- lattice-workspace:auto:plugins:start -->` -> `<!-- workspace-io:auto:plugins:start -->` (or import from `workspace_io.render`)

    Resulting test count: 14 (was 15; dropped test_creates_work_schema).

    `test_render.py`: Port from lattice source applying rebrand + the marker-import recommendation:
    - Import the markers from the module instead of hardcoding: `from workspace_io.render import AUTO_END, AUTO_START, render_workspace_claude_md`
    - Update the `_write_manifest` helper fixture per PATTERNS.md test_render.py: writes a `.graph-wiki.yaml` body with v2 schema
    - All assertions that previously hardcoded `<!-- lattice-workspace:auto -->` strings should reference the imported `AUTO_START`/`AUTO_END` constants
    - Plugin name strings: `"graph-wiki-agent"` for the known-pointer test (matches the single entry in `_PLUGIN_POINTERS`); other plugin names can be arbitrary unknowns to test the generic-pointer path

    Resulting test count: 6 (unchanged from analog).

    `test_warn_if_stale.py`: Port from lattice source applying rebrand plus the v1-coercion rewrite (per PATTERNS.md test_warn_if_stale.py):
    - Keep 3 tests unchanged (with rebrand): `test_no_entry_no_write`, `test_match_no_write`, `test_mismatch_writes_installed_only` (names approximate; map by behavior)
    - Replace `test_v1_coerced_entry_no_signal` with `test_null_applied_version_no_signal` per PATTERNS.md — writes a v2 manifest directly with `applied_version: null`, then asserts `warn_if_stale(...)` returns False and the file is byte-unchanged
    - All workspace paths: `tmp_path / "lattice"` -> `tmp_path / "graph-wiki"`
    - Manifest filename strings: `.lattice.yaml` -> `.graph-wiki.yaml`

    Resulting test count: 4 (unchanged from analog; one test rewritten).

    Do NOT run pytest in this task — Task 3 runs the full suite.
  </action>
  <acceptance_criteria>
    - `packages/workspace-io/tests/test_init.py` exists with 14 tests: `grep -c "^def test_" packages/workspace-io/tests/test_init.py` returns `14`
    - `packages/workspace-io/tests/test_init.py` does NOT include `test_creates_work_schema`: `grep -c 'test_creates_work_schema' packages/workspace-io/tests/test_init.py` returns `0`
    - `packages/workspace-io/tests/test_render.py` exists with 6 tests: `grep -c "^def test_" packages/workspace-io/tests/test_render.py` returns `6`
    - `packages/workspace-io/tests/test_render.py` imports markers from the module: `grep -c 'from workspace_io.render import' packages/workspace-io/tests/test_render.py` returns at least `1`
    - `packages/workspace-io/tests/test_warn_if_stale.py` exists with 4 tests including the rewritten one: `grep -c "^def test_" packages/workspace-io/tests/test_warn_if_stale.py` returns `4`; `grep -c 'test_null_applied_version_no_signal' packages/workspace-io/tests/test_warn_if_stale.py` returns `1`
    - `packages/workspace-io/tests/test_warn_if_stale.py` does NOT contain `test_v1_coerced_entry_no_signal`: `grep -c 'v1_coerced' packages/workspace-io/tests/test_warn_if_stale.py` returns `0`
    - No lattice references remain across these 3 files: `grep -lE 'lattice_workspace|LatticeConfig|\.lattice\.yaml|lattice-workspace:auto|tmp_path \\/ "lattice"' packages/workspace-io/tests/test_init.py packages/workspace-io/tests/test_render.py packages/workspace-io/tests/test_warn_if_stale.py` returns no matches
  </acceptance_criteria>
  <verify>
    <automated>test $(grep -c "^def test_" packages/workspace-io/tests/test_init.py) -eq 14 && test $(grep -c "^def test_" packages/workspace-io/tests/test_render.py) -eq 6 && test $(grep -c "^def test_" packages/workspace-io/tests/test_warn_if_stale.py) -eq 4 && grep -q "test_null_applied_version_no_signal" packages/workspace-io/tests/test_warn_if_stale.py && ! grep -rE 'lattice_workspace|LatticeConfig|lattice-workspace:auto' packages/workspace-io/tests/test_init.py packages/workspace-io/tests/test_render.py packages/workspace-io/tests/test_warn_if_stale.py</automated>
  </verify>
  <done>
    24 tests across 3 behavior-rewrite test files written. test_creates_work_schema dropped, test_v1_coerced_entry_no_signal rewritten as test_null_applied_version_no_signal, marker strings sourced from `workspace_io.render`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Port test_config + test_manifest with new strict/v1-raises tests, then run full suite</name>
  <files>packages/workspace-io/tests/test_config.py, packages/workspace-io/tests/test_manifest.py</files>
  <read_first>
    - /Users/pat/Personal/lattice/packages/lattice-workspace/tests/test_config.py (analog — 9 tests; add 1 new for D-03)
    - /Users/pat/Personal/lattice/packages/lattice-workspace/tests/test_manifest.py (analog — 5 tests; add 1 new for D-14)
    - /Users/pat/Personal/deep-agents/.planning/phases/11-workspace-io-port-m1/11-PATTERNS.md (test_config.py and test_manifest.py sections — includes new-test bodies)
    - /Users/pat/Personal/deep-agents/.planning/phases/11-workspace-io-port-m1/11-RESEARCH.md (Test Porting Strategy Groups 1 + 2; Phase Requirements -> Test Map for WS-02 strict-raises)
    - /Users/pat/Personal/deep-agents/.planning/phases/11-workspace-io-port-m1/11-CONTEXT.md (D-03, D-14)
    - /Users/pat/Personal/deep-agents/packages/workspace-io/src/workspace_io/config.py (Plan 02 output — verify error string includes "graph-wiki-agent init")
    - /Users/pat/Personal/deep-agents/packages/workspace-io/src/workspace_io/manifest.py (Plan 02 output — verify v1-raises message)
  </read_first>
  <action>
    Port the two remaining test files with both the rebrand sweep and the new tests for D-03 and D-14, then run the full suite.

    `test_config.py`: Port from lattice source applying rebrand symbol map plus:
    - All `(repo / "lattice").resolve()` -> `(repo / "graph-wiki").resolve()`
    - `".lattice.local.yaml"` -> `".graph-wiki.local.yaml"`, `"lattice-directory:"` -> `"graph-wiki-directory:"`
    - `monkeypatch.setattr("lattice_workspace.config._find_repo_root", ...)` -> `"workspace_io.config._find_repo_root"`
    - `[sys.executable, "-m", "lattice_workspace.config"]` -> `[sys.executable, "-m", "workspace_io.config"]`
    - For any test that uses `.resolve()` against a workspace expected to exist: ensure the test ALSO writes a minimal `.graph-wiki.yaml` to that workspace dir so the new strict check (D-03) does not raise unexpectedly. The lattice tests can return a config without a manifest existing; the ported version must create one. Pattern: `(workspace_dir / ".graph-wiki.yaml").write_text("version: 2\ninitialized_at: 2026-05-17\nplugins: []\n")`
    - For tests that exercise the `GRAPH_WIKI_WORKSPACE` env-override: do NOT add a manifest file — the env-override branch skips the strict check by design
    - Add the new test per PATTERNS.md test_config.py "New test required" snippet:

      def test_resolve_raises_when_no_manifest_found(tmp_path, monkeypatch):
          monkeypatch.delenv("GRAPH_WIKI_WORKSPACE", raising=False)
          repo = _make_repo(tmp_path)
          with pytest.raises(RuntimeError, match="graph-wiki-agent init"):
              resolve(repo)

      (use the actual Python syntax in the file; ensure pytest is imported)

    Resulting test count: 10 (9 ported + 1 new).

    `test_manifest.py`: Port from lattice source applying rebrand (`.lattice.yaml` -> `.graph-wiki.yaml`) plus the new D-14 test per PATTERNS.md test_manifest.py:

      def test_read_raises_on_v1(tmp_path):
          mpath = tmp_path / ".graph-wiki.yaml"
          mpath.write_text("version: 1\ninitialized_at: 2026-05-17\nplugins:\n  - graph-wiki-agent\n")
          with pytest.raises(RuntimeError):
              read(mpath)

    Resulting test count: 6 (5 ported + 1 new).

    After both files are written, run the FULL workspace-io test suite:

      uv run --package workspace-io pytest -x

    Expected total: 14 (test_init) + 6 (test_render) + 4 (test_warn_if_stale) + 7 (test_paths) + 9 (test_local_config) + 3 (test_manifest_v2_roundtrip) + 3 (test_init_records_version) + 1 (test_init_bumps_version) + 4 (test_pending_updates) + 10 (test_config) + 6 (test_manifest) = 67 tests, all green.

    If any test fails, fix the test (do NOT modify source modules — Plan 02's sanity checks verified the source is correct). Common failure modes:
    - Tests that assumed lattice's non-strict `resolve()` need a `.graph-wiki.yaml` written into the expected workspace dir (see fix above)
    - Tests that read `monkeypatch.delenv("GRAPH_WIKI_REAL_VAULT_PATH", ...)` need to update to `GRAPH_WIKI_WORKSPACE`
    - Marker-string mismatches between `render.py` constants and template/test expectations
  </action>
  <acceptance_criteria>
    - `packages/workspace-io/tests/test_config.py` exists with 10 tests: `grep -c "^def test_" packages/workspace-io/tests/test_config.py` returns `10`
    - `packages/workspace-io/tests/test_config.py` includes the new test: `grep -c 'test_resolve_raises_when_no_manifest_found' packages/workspace-io/tests/test_config.py` returns `1`
    - `packages/workspace-io/tests/test_manifest.py` exists with 6 tests: `grep -c "^def test_" packages/workspace-io/tests/test_manifest.py` returns `6`
    - `packages/workspace-io/tests/test_manifest.py` includes the new test: `grep -c 'test_read_raises_on_v1' packages/workspace-io/tests/test_manifest.py` returns `1`
    - Full workspace-io suite passes: `uv run --package workspace-io pytest` exits 0
    - Test collection matches expected: `uv run --package workspace-io pytest --collect-only -q` shows close to 67 tests collected (allow +/- 2 for plan-time count drift from analogs)
    - No `test_schema.py` exists in `packages/workspace-io/tests/`: `test ! -f packages/workspace-io/tests/test_schema.py`
    - No `test_manifest_v1_read.py` exists: `test ! -f packages/workspace-io/tests/test_manifest_v1_read.py`
  </acceptance_criteria>
  <verify>
    <automated>uv run --package workspace-io pytest -x && test ! -f packages/workspace-io/tests/test_schema.py && test ! -f packages/workspace-io/tests/test_manifest_v1_read.py && grep -q "test_resolve_raises_when_no_manifest_found" packages/workspace-io/tests/test_config.py && grep -q "test_read_raises_on_v1" packages/workspace-io/tests/test_manifest.py</automated>
  </verify>
  <done>
    All 67 ported tests are green. New tests for D-03 (strict resolve) and D-14 (v1-raises) included. Dropped test files (test_schema.py, test_manifest_v1_read.py) not present. WS-09 satisfied: every ported test from lattice-workspace/tests/ runs green under the new module path with .graph-wiki.yaml filename expectations.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (tests only) | Tests run in a controlled tmp_path; no untrusted input crosses any boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-06 | Tampering | test fixture files in tmp_path | accept | Tests are hermetic; tmp_path is pytest-managed and isolated per test |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan; pytest already in workspace dev dependencies |
</threat_model>

<verification>
- `uv run --package workspace-io pytest -x` exits 0
- Test files dropped per decisions: `test_schema.py` and `test_manifest_v1_read.py` do not exist
- New tests present: `test_resolve_raises_when_no_manifest_found` (test_config.py) and `test_read_raises_on_v1` (test_manifest.py)
- No `lattice_workspace`/`LatticeConfig`/`.lattice.yaml` references in any test file
</verification>

<success_criteria>
- WS-09 satisfied (all ported tests pass)
- Phase 11 SC #5 satisfied (every ported test runs green under new module path with .graph-wiki.yaml expectations)
- Decisions D-03, D-06, D-14 verified by tests
</success_criteria>

<output>
Create `.planning/phases/11-workspace-io-port-m1/11-03-SUMMARY.md` when done.
</output>
