---
phase: 14-plugin-port-m3b
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/wiki-io/src/wiki_io/lint_wiki.py
  - packages/wiki-io/tests/test_lint_wiki.py
autonomous: true
requirements: [PLUGIN-04]
tags: [port, wiki-io, lint, brand-rebrand]

must_haves:
  truths:
    - "`packages/wiki-io/src/wiki_io/lint_wiki.py` exists and is importable as `from wiki_io.lint_wiki import main, scan`"
    - "`uv run --package wiki-io pytest packages/wiki-io/tests/test_lint_wiki.py` exits 0"
    - "`scripts/check-brand.sh` exits 0 with the new file in tree (no `lattice_*` identifiers, no `lattice-wiki` brand strings)"
    - "Module shape matches in-tree convention (clean docstring + plain imports — NO `# Source: / # Anchor: / # Source-commit:` provenance headers)"
  artifacts:
    - path: "packages/wiki-io/src/wiki_io/lint_wiki.py"
      provides: "Wiki health-check (orphans, broken wikilinks, stale pages, frontmatter, duplicate titles, log gaps, code-drift)"
      exports: ["main", "scan"]
    - path: "packages/wiki-io/tests/test_lint_wiki.py"
      provides: "Importability + structural smoke for `wiki_io.lint_wiki`"
  key_links:
    - from: "packages/wiki-io/src/wiki_io/lint_wiki.py"
      to: "wiki_io._workspace.resolve_wiki_and_repo"
      via: "direct import"
      pattern: "from wiki_io\\._workspace import"
    - from: "packages/wiki-io/src/wiki_io/lint_wiki.py"
      to: "wiki_io.lint.{container,file_map,domain,source_sync,package_sync}"
      via: "dispatcher imports"
      pattern: "from wiki_io\\.lint\\."
---

<objective>
Verbatim port of upstream `lattice_wiki_core.lint_wiki` into `wiki_io.lint_wiki` under the Phase 12 SR-01 rubric (bug-fixes / helpers / behavior-preserving refactors come over verbatim) and the Phase 14 VP-01 prerequisite for the plugin port.

Purpose: VP-01 says the plugin's `lint_wiki.py` shim cannot land until `wiki_io.lint_wiki` exists. This plan satisfies that prerequisite by porting the module, retargeting imports from `lattice_wiki_core` → `wiki_io`, dropping the upstream `_version_check` dependency (not in `wiki_io` scope), and clearing the BRAND-04 grep gate.

Output: `packages/wiki-io/src/wiki_io/lint_wiki.py` + accompanying test file. The `main()` entry, CLI argparse surface, and `scan(...)` return shape match upstream byte-for-byte modulo brand strings and the dropped `_version_check` import.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-plugin-port-m3b/14-CONTEXT.md
@.planning/phases/14-plugin-port-m3b/14-PATTERNS.md
@.planning/phases/12-drift-backport-ecosystem-rebrand-m2/12-CONTEXT.md
@packages/wiki-io/src/wiki_io/_workspace.py
@packages/wiki-io/src/wiki_io/scan_monorepo.py
@packages/wiki-io/tests/test_lint_modules.py
@packages/wiki-io/tests/test_ports_importable.py

<interfaces>
<!-- Existing in-tree exports the port depends on -->

From packages/wiki-io/src/wiki_io/_workspace.py:
```python
def resolve_wiki_and_repo(
    *, wiki: Path | None = None, repo: Path | None = None
) -> tuple[Path, Path]: ...
```

From packages/wiki-io/src/wiki_io/lint/ (sibling modules already in tree):
- `wiki_io.lint.container` — exposes `check(...)` and `GROUP`
- `wiki_io.lint.file_map` — same shape
- `wiki_io.lint.domain` — same shape
- `wiki_io.lint.source_sync` — same shape
- `wiki_io.lint.package_sync` — same shape

In-tree env-var convention (must mirror in any new error strings):
- Env var name: `GRAPH_WIKI_WORKSPACE`
- Fix command referenced in errors: `graph-wiki-agent init <path>`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1.1: Port lint_wiki.py from upstream lattice_wiki_core to wiki_io</name>
  <files>packages/wiki-io/src/wiki_io/lint_wiki.py</files>
  <read_first>
    - /Users/pat/Personal/lattice/packages/lattice-wiki-core/src/lattice_wiki_core/lint_wiki.py (full file — verbatim port source, ~508 LOC)
    - packages/wiki-io/src/wiki_io/scan_monorepo.py (in-tree shape analog — module docstring + import block pattern)
    - packages/wiki-io/src/wiki_io/_workspace.py (delegation idiom + env-var phrasing for error strings)
    - .planning/phases/14-plugin-port-m3b/14-PATTERNS.md (§"Pattern Assignments" → lint_wiki section — brand-rename rules, no-provenance-comments rule)
    - .planning/phases/12-drift-backport-ecosystem-rebrand-m2/12-CONTEXT.md (§SR-01 PORT-vs-LEAVE rubric — verbatim port philosophy)
  </read_first>
  <action>
    Copy upstream `lint_wiki.py` into `packages/wiki-io/src/wiki_io/lint_wiki.py`. Apply the brand-rename token table from 14-PATTERNS.md across identifiers, prose, docstrings, and error strings:
      - `from lattice_wiki_core.scan_monorepo import ...` → `from wiki_io.scan_monorepo import ...`
      - `from lattice_wiki_core._workspace import ...` → `from wiki_io._workspace import ...`
      - `from lattice_wiki_core.lint.<x> import ...` → `from wiki_io.lint.<x> import ...`
      - `from lattice_wiki_core._version_check import check_for_updates` → DELETE the import and any call site (not in `wiki_io` scope per 14-PATTERNS.md)
      - `lattice-wiki` → `graph-wiki` (in prose / docstrings)
      - `LATTICE_WIKI_ROOT` / `LATTICE_WORKSPACE` → `GRAPH_WIKI_ROOT` / `GRAPH_WIKI_WORKSPACE` (in env-var references)
      - `.lattice-wiki.json` → `.graph-wiki.yaml` (in any prose ref to the manifest)
    Match in-tree module shape: clean module docstring + plain imports. Do NOT introduce `# Source: / # Anchor: / # Source-commit:` provenance headers (the pattern map confirmed zero such headers exist in `packages/wiki-io/`).
    Preserve the `main()` entry shape, `scan(...)` signature, argparse surface, and structured return verbatim modulo the renames above. SR-01 verbatim rubric applies.
  </action>
  <verify>
    <automated>uv run --package wiki-io python -c "from wiki_io.lint_wiki import main, scan; assert callable(main) and callable(scan)"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `packages/wiki-io/src/wiki_io/lint_wiki.py`
    - `grep -c "lattice" packages/wiki-io/src/wiki_io/lint_wiki.py` returns 0 (no lattice tokens anywhere — identifiers or prose)
    - `grep -c "_version_check" packages/wiki-io/src/wiki_io/lint_wiki.py` returns 0 (import dropped)
    - `grep -c "^# Source:" packages/wiki-io/src/wiki_io/lint_wiki.py` returns 0 (no provenance header — match in-tree shape)
    - `grep -E "^from wiki_io\." packages/wiki-io/src/wiki_io/lint_wiki.py | wc -l` is at least 2 (`_workspace` import + at least one `lint.` import)
    - `uv run --package wiki-io python -c "from wiki_io.lint_wiki import main, scan"` exits 0
    - `bash scripts/check-brand.sh` exits 0 (no new entries needed in `.brand-grep-allow`)
  </acceptance_criteria>
  <done>
    `wiki_io.lint_wiki` is importable, exports `main` and `scan` with the same signatures as upstream `lattice_wiki_core.lint_wiki`, and clears the BRAND-04 grep gate without allowlist additions.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 1.2: Add structural test for wiki_io.lint_wiki</name>
  <files>packages/wiki-io/tests/test_lint_wiki.py</files>
  <read_first>
    - packages/wiki-io/tests/test_ports_importable.py (importability pattern)
    - packages/wiki-io/tests/test_lint_modules.py (fixture-vault structural pattern — lines 1–40 for `FIXTURES = Path(__file__).parent / "fixtures"` and the existing fixture vault names)
    - packages/wiki-io/tests/conftest.py (any shared fixtures)
    - .planning/phases/14-plugin-port-m3b/14-PATTERNS.md (§"test_lint_wiki.py" — VP-02 scope: importability + structural smoke, NOT finding-count parity)
  </read_first>
  <action>
    Create `packages/wiki-io/tests/test_lint_wiki.py` with two test functions:
      1. `test_lint_wiki_importable()` — asserts `from wiki_io.lint_wiki import main, scan` and that both are callable. Mirrors the `test_ports_importable.py` shape.
      2. `test_lint_wiki_scan_runs_on_fixture_vault()` — uses the existing `FIXTURES = Path(__file__).parent / "fixtures"` and one of the fixtures already present under `packages/wiki-io/tests/fixtures/` (e.g., `edge-case-vault` or `round-trip-vault` — pick whichever has a runnable wiki shape). Calls `scan(wiki=<fixture>, repo=<fixture>)` and asserts the return is structurally well-formed (dict or named-tuple with expected top-level keys — match whatever upstream returns). Open scope explicitly per VP-02: do NOT assert finding-count parity with upstream `lattice_wiki_core.lint_wiki`; structural-correctness is sufficient.
    Module docstring should mirror `test_lint_modules.py`'s opener: a one-line note that finding-count parity with upstream is out of Phase 14 scope.
  </action>
  <verify>
    <automated>uv run --package wiki-io pytest packages/wiki-io/tests/test_lint_wiki.py -x</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `packages/wiki-io/tests/test_lint_wiki.py`
    - `uv run --package wiki-io pytest packages/wiki-io/tests/test_lint_wiki.py -x` exits 0
    - At least 2 test functions present (`grep -c '^def test_' packages/wiki-io/tests/test_lint_wiki.py` >= 2)
    - No `lattice_` tokens (`grep -c 'lattice_' packages/wiki-io/tests/test_lint_wiki.py` returns 0)
    - `bash scripts/check-brand.sh` still exits 0
  </acceptance_criteria>
  <done>
    Test file ships green; finding-count parity is explicitly scoped out per VP-02; brand gate clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cwd → fs reads | `wiki_io.lint_wiki` reads files under the resolved wiki + repo paths; trust boundary is the user's filesystem under `$GRAPH_WIKI_WORKSPACE` |
| argv → argparse | CLI args from user shell; standard argparse boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01-01 | Tampering | `wiki_io.lint_wiki` file reads (markdown frontmatter, wikilinks) | accept | Read-only operation over a user-owned vault; tampering surface is the user's own workspace. No new write paths introduced by this port. |
| T-14-01-02 | Information Disclosure | argparse `--json` output prints findings to stdout | accept | Same surface as upstream `lattice_wiki_core.lint_wiki`; output goes to user's terminal. Behavior-preserving port — no new disclosure channels. |
| T-14-01-03 | Denial of Service | Large fixture vault could slow `scan()` | accept | CLI tool invoked manually; user controls input size. No regression vs. upstream behavior. |
| T-14-01-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — this plan adds no new package dependencies (`wiki_io.lint_wiki` only imports from sibling `wiki_io.*` modules and stdlib). No legitimacy gate required. |

ASVS L1: no new attack surface introduced; verbatim port of an existing CLI tool.
</threat_model>

<verification>
After both tasks complete, run the full wiki-io test suite to confirm no regression in sibling modules:

```bash
uv run --package wiki-io pytest packages/wiki-io/tests/
bash scripts/check-brand.sh
grep -rn 'lattice_' packages/wiki-io/src/wiki_io/lint_wiki.py packages/wiki-io/tests/test_lint_wiki.py | wc -l  # must equal 0
```
</verification>

<success_criteria>
- [ ] `packages/wiki-io/src/wiki_io/lint_wiki.py` exists and exports `main` + `scan`
- [ ] `packages/wiki-io/tests/test_lint_wiki.py` exists with importability + structural smoke tests
- [ ] `uv run --package wiki-io pytest packages/wiki-io/tests/test_lint_wiki.py -x` passes
- [ ] No `lattice_` tokens in either file; no `_version_check` import; no `# Source:` provenance header
- [ ] `bash scripts/check-brand.sh` exits 0
- [ ] VP-01 prerequisite satisfied (the `lint_wiki.py` shim in Plan 3 can now import `from wiki_io.lint_wiki import main`)
</success_criteria>

<output>
Create `.planning/phases/14-plugin-port-m3b/14-01-SUMMARY.md` when done.
</output>
