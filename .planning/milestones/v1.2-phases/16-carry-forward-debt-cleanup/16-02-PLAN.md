---
phase: 16-carry-forward-debt-cleanup
plan: 02
type: execute
wave: 1
depends_on: ["16-01"]
gap_closure: true
files_modified:
  - packages/subagent-runtime/src/subagent_runtime/pool.py
  - packages/subagent-runtime/src/subagent_runtime/__init__.py
  - packages/subagent-runtime/tests/test_pool.py
  - agents/code-wiki-agent/src/code_wiki_agent/commands/query.py
  - agents/code-wiki-agent/src/code_wiki_agent/commands/scan.py
  - agents/code-wiki-agent/src/code_wiki_agent/commands/lint.py
  - agents/code-wiki-agent/tests/integration/test_trace_coverage.py
  - .planning/phases/16-carry-forward-debt-cleanup/16-VERIFICATION.md
autonomous: false
requirements: [TRACE-FU-01]
user_setup:
  - service: aws-bedrock
    why: "Re-running the gated TRACE-FU-01 regression test invokes real Bedrock (librarian + synthesizer fan-out) and incurs cost."
    env_vars:
      - name: CODE_WIKI_RUN_INTEGRATION
        source: "Set to 1 in shell before running the gated test (opt-in cost gate per docs/testing.md §3)."
      - name: AWS_PROFILE
        source: "Existing local AWS credentials with Bedrock access in us-east-1 (same setup as 16-01 gated runs)."

must_haves:
  truths:
    - "Every production trace JSONL record from commands/{scan,lint,ingest,query} — INCLUDING librarian fan-out — emits non-None tokens_in/tokens_out on status=success records."
    - "The gated TRACE-FU-01 regression test (agents/code-wiki-agent/tests/integration/test_trace_coverage.py) exits 0 against real Bedrock with no record asserting on None tokens."
    - "SubagentPool.run_all's task contract supports surfacing usage_metadata back into the trace record without forcing callers to re-implement trace writing."
    - "All four production fan-out call sites (scan.generate_stub, lint.run_linter_group, query.drill_page, query.code_drill) carry usage_metadata into the pool's trace record."
    - "A fast unit test (no Bedrock) in packages/subagent-runtime/tests/test_pool.py proves the pool writes tokens_in/tokens_out when a callback surfaces usage_metadata."
    - "16-VERIFICATION.md records G-01 → CLOSED with the passing gated-test transcript as evidence."
  artifacts:
    - path: "packages/subagent-runtime/src/subagent_runtime/pool.py"
      provides: "Extended task-result contract that threads usage_metadata into write_trace_record without breaking scalar-returning callbacks."
      contains: "TaskResult"
    - path: "packages/subagent-runtime/tests/test_pool.py"
      provides: "Regression unit test asserting pool writes tokens_in/tokens_out when callback returns TaskResult(value=..., response=stub_with_usage_metadata)."
      contains: "test_pool_writes_tokens_when_callback_returns_taskresult"
    - path: "agents/code-wiki-agent/src/code_wiki_agent/commands/query.py"
      provides: "drill_page + code_drill return TaskResult so the librarian + code_reader trace records carry usage_metadata."
      contains: "TaskResult("
    - path: "agents/code-wiki-agent/src/code_wiki_agent/commands/scan.py"
      provides: "generate_stub returns TaskResult so scanner trace records carry usage_metadata."
      contains: "TaskResult("
    - path: "agents/code-wiki-agent/src/code_wiki_agent/commands/lint.py"
      provides: "run_linter_group returns TaskResult so linter trace records carry usage_metadata."
      contains: "TaskResult("
    - path: ".planning/phases/16-carry-forward-debt-cleanup/16-VERIFICATION.md"
      provides: "G-01 marked CLOSED with transcript evidence and final 5/5 SC score."
      contains: "G-01"
  key_links:
    - from: "pool.SubagentPool._run_one"
      to: "trace_io.write_trace_record"
      via: "TaskResult.response is passed as the response= argument when the callback returns TaskResult; raw AIMessage / scalar fallbacks preserved"
      pattern: "TaskResult|response="
    - from: "query.drill_page"
      to: "pool.SubagentPool.run_all"
      via: "returns TaskResult(value=resp.content, response=resp)"
      pattern: "TaskResult\\(value=.*response=resp"
    - from: "tests/integration/test_trace_coverage.py"
      to: "librarian JSONL records"
      via: "assert rec.get('tokens_in') is not None for non-error/non-event/non-query_summary records"
      pattern: "tokens_in.*is not None"
---

<objective>
Close gap G-01 from 16-VERIFICATION.md: the librarian fan-out path
(`drill_page` in `query.py:903-914`) returns only `resp.content`, so
`SubagentPool.run_all` writes per-item trace records with
`tokens_in=None, tokens_out=None, cost_usd=None`. SC#1 ("every production
trace JSONL includes usage_metadata, verified by a regression test that runs
a real fan-out") therefore FAILED when the gated test was executed against
real Bedrock on 2026-05-19.

Purpose: Restore TRACE-FU-01 to fully satisfied. The remediation is a
backward-compatible extension of the `SubagentPool.run_all` task contract so
callbacks can surface the raw LLM response (carrying `usage_metadata`)
alongside the value they want downstream consumers to see — without forcing
the pool to know anything about ChatBedrockConverse internals. Every
production fan-out call site is migrated atomically.

Output:
- Extended pool task contract via a `TaskResult` dataclass (additive, opt-in)
- All 4 production fan-out callbacks migrated (scan, lint, query librarian,
  query code_reader)
- One new pool unit test locking the contract
- Gated TRACE-FU-01 regression test re-run green against real Bedrock
- 16-VERIFICATION.md updated: G-01 → CLOSED, status flips to `passed`
  (modulo the 3 still-pending judgment-call HUMAN-UAT items, which are not
  in scope for this plan)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-carry-forward-debt-cleanup/16-CONTEXT.md
@.planning/phases/16-carry-forward-debt-cleanup/16-VERIFICATION.md
@.planning/phases/16-carry-forward-debt-cleanup/16-HUMAN-UAT.md
@.planning/phases/16-carry-forward-debt-cleanup/16-01-SUMMARY.md
@.planning/phases/16-carry-forward-debt-cleanup/16-PATTERNS.md
@packages/subagent-runtime/src/subagent_runtime/pool.py
@packages/subagent-runtime/src/subagent_runtime/trace_io.py
@packages/subagent-runtime/src/subagent_runtime/__init__.py
@packages/subagent-runtime/tests/test_pool.py
@agents/code-wiki-agent/src/code_wiki_agent/commands/query.py
@agents/code-wiki-agent/src/code_wiki_agent/commands/scan.py
@agents/code-wiki-agent/src/code_wiki_agent/commands/lint.py
@agents/code-wiki-agent/src/code_wiki_agent/commands/ingest.py
@agents/code-wiki-agent/tests/integration/test_trace_coverage.py

<interfaces>
<!-- Current shapes (extracted before this plan runs) — executor uses these as the source-of-truth contracts, no codebase exploration needed. -->

From packages/subagent-runtime/src/subagent_runtime/pool.py (existing):
```python
@dataclass
class PerItemError:
    item: Any
    exception: Exception

@dataclass
class FanOutResult:
    successes: list[tuple[Any, Any]] = field(default_factory=list)
    errors: list[PerItemError] = field(default_factory=list)

class SubagentPool:
    def __init__(self, trace_dir: Path, *, default_recursion_limit: int = 100) -> None: ...

    async def run_all(
        self,
        items: list[Any],
        task: Callable[..., Awaitable[Any]],
        role: str,
        *,
        model_id: str,
        max_concurrency: int,
        recursion_limit: int | None = None,
    ) -> FanOutResult: ...

    def _write_trace(
        self,
        path: Path,
        role: str,
        model_id: str,
        item: Any,
        status: str,
        latency_ms: int,
        response: Any,           # <— currently the raw return-value of `task(item)`; if that is a string, write_trace_record's hasattr(response, "usage_metadata") returns False and tokens stay None.
        *,
        error: str | None = None,
    ) -> None:
        write_trace_record(path, role, model_id, item, status, latency_ms, response, error=error)
```

From packages/subagent-runtime/src/subagent_runtime/trace_io.py (existing):
```python
def write_trace_record(
    path: Path,
    role: str,
    model_id: str,
    item: Any,
    status: str,
    latency_ms: int,
    response: Any,
    *,
    error: str | None = None,
) -> None:
    # Extracts response.usage_metadata if isinstance(meta, dict).
    # Already correct — does not need changing.
```

Current callback shapes (all return scalars — strings or lists):
- scan.generate_stub:        async def generate_stub(pkg: dict) -> str:                      returns `resp.content`
- lint.run_linter_group:     async def run_linter_group(group_tuple: tuple) -> list[str]:    returns `findings`
- query.drill_page:          async def drill_page(page_path: str) -> str:                    returns `resp.content`
- query.code_drill:          async def code_drill(page_path: str) -> str:                    returns `getattr(resp, "content", "") or ""`

Consumers of fan_result.successes (these must keep seeing the same scalar values):
- scan.py:365 — `for pkg, llm_body in fan_result.successes:`   (llm_body is str)
- lint.py:477 — `for group_tuple, findings in fan_result.successes:`   (findings is list[str])
- query.py:927 — `(result or "").strip()` on librarian results   (result is str)
- query.py:498 — `(result or "").strip()` on code_reader results   (result is str)

Failing regression test (today):
```
CODE_WIKI_RUN_INTEGRATION=1 uv run pytest \
  agents/code-wiki-agent/tests/integration/test_trace_coverage.py -x -v
→ FAILED: librarian record has tokens_in=None, tokens_out=None.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend SubagentPool task contract with TaskResult dataclass</name>
  <files>
    packages/subagent-runtime/src/subagent_runtime/pool.py
    packages/subagent-runtime/src/subagent_runtime/__init__.py
    packages/subagent-runtime/tests/test_pool.py
  </files>
  <read_first>
    packages/subagent-runtime/src/subagent_runtime/pool.py
    packages/subagent-runtime/src/subagent_runtime/trace_io.py
    packages/subagent-runtime/src/subagent_runtime/__init__.py
    packages/subagent-runtime/tests/test_pool.py
  </read_first>
  <behavior>
    - Test 1 (new — RED first): A callback that returns `TaskResult(value="hello", response=stub_response_with_usage_metadata_dict)` causes the pool to write a JSONL trace record with `tokens_in == 5`, `tokens_out == 7` (matching the stub's `usage_metadata`). The `fan_result.successes` list still surfaces `("item-a", "hello")` so downstream code reading the value field is unchanged.
    - Test 2 (new): A callback that returns a bare scalar (today's contract — string, list, dict) preserves existing behavior — `fan_result.successes` carries the scalar verbatim; trace record has `tokens_in is None, tokens_out is None`. This is the backward-compat guarantee for legacy callbacks.
    - Test 3 (new): A callback that returns `TaskResult(value=None, response=None)` writes a trace record with `tokens_in is None`, `tokens_out is None`; `fan_result.successes` carries `(item, None)`.
    - All 13 existing tests in packages/subagent-runtime/tests/test_pool.py continue to pass without modification (they return scalars; backward-compat path).
  </behavior>
  <action>
    Add a `TaskResult` dataclass to `packages/subagent-runtime/src/subagent_runtime/pool.py`. Fields: `value: Any` (what downstream consumers see) and `response: Any = None` (the raw LLM response carrying `usage_metadata`). Place it next to `PerItemError` / `FanOutResult`.

    Modify `SubagentPool._run_one` (the inner async helper inside `run_all`) so that when `task(item)` returns a `TaskResult` instance:
      1. The `response` passed to `self._write_trace(...)` is `result.response` (so `write_trace_record` can extract `usage_metadata`).
      2. The success tuple appended to `FanOutResult.successes` is `(item, result.value)` — NOT the TaskResult wrapper. Downstream consumers must continue to see the scalar.

    When `task(item)` returns anything else (scalar — today's contract), behavior is unchanged: `response=result` flows into `_write_trace` (where `hasattr(response, "usage_metadata")` will be False on a string, so tokens stay None — matching prior behavior), and `(item, result)` lands in `successes`.

    Detection MUST use `isinstance(result, TaskResult)` (not duck-typing on `.value` / `.response`, which would collide with arbitrary user-returned objects). No changes to `_write_trace` itself — it already delegates to `write_trace_record`, which already handles `response.usage_metadata` correctly via the existing `isinstance(meta, dict)` guard.

    Export `TaskResult` from `packages/subagent-runtime/src/subagent_runtime/__init__.py` (add to the existing `from subagent_runtime.pool import ...` line and `__all__`). Keep the existing exports (`SubagentPool`, `FanOutResult`, `PerItemError`) intact.

    Write three new tests in `packages/subagent-runtime/tests/test_pool.py` per the `<behavior>` block. Use a simple stub object pattern for the LLM response:
      - Stub class with a `usage_metadata` attribute that is a real dict `{"input_tokens": 5, "output_tokens": 7, "total_tokens": 12}` — NOT a `MagicMock` (MagicMock auto-resolves attributes and poisons the trace; see 16-01 SUMMARY auto-fix #1).
      - For each test, parse the per-run JSONL trace file in `tmp_path / "traces"` and assert on the `tokens_in` / `tokens_out` fields of the success record.
    Mirror the file-discovery + parsing pattern from existing `test_pool.py` tests around lines 161-200 (the `traces_dir` glob + `json.loads(raw)`).

    Do NOT modify `trace_io.py`, `_write_trace`, or `_write_batch_terminal`. The contract change is localized to `_run_one`'s handling of the callback's return value.

    Commit message: `feat(16): extend SubagentPool task contract with TaskResult for usage_metadata pass-through (G-01 closure)`.
  </action>
  <verify>
    <automated>uv run --package subagent-runtime pytest packages/subagent-runtime/tests/test_pool.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class TaskResult" packages/subagent-runtime/src/subagent_runtime/pool.py` returns `1`.
    - `grep -c "TaskResult" packages/subagent-runtime/src/subagent_runtime/__init__.py` returns at least `2` (import line + `__all__` entry).
    - `python -c "from subagent_runtime import TaskResult; t = TaskResult(value='x', response=None); assert t.value == 'x' and t.response is None"` exits 0.
    - All three new tests pass; all 13 pre-existing tests in `test_pool.py` still pass.
    - The new test that constructs `TaskResult(value="hello", response=stub_with_usage_metadata)` and runs `pool.run_all` asserts the JSONL trace record has `rec["tokens_in"] == 5` and `rec["tokens_out"] == 7` AND `fan_result.successes == [("item-a", "hello")]` (NOT `[("item-a", TaskResult(...))]`).
    - `_write_trace` and `write_trace_record` are byte-identical to their pre-task state (no diffs in `trace_io.py`; `_write_trace` body unchanged — verify via `git diff` showing only `_run_one` and the new `TaskResult` class changed in `pool.py`).
  </acceptance_criteria>
  <done>Backward-compatible TaskResult contract landed and locked by 3 new + 13 existing unit tests; only `_run_one` changed in pool.py; `trace_io.py` untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate all four production fan-out callbacks to return TaskResult</name>
  <files>
    agents/code-wiki-agent/src/code_wiki_agent/commands/query.py
    agents/code-wiki-agent/src/code_wiki_agent/commands/scan.py
    agents/code-wiki-agent/src/code_wiki_agent/commands/lint.py
  </files>
  <read_first>
    agents/code-wiki-agent/src/code_wiki_agent/commands/query.py
    agents/code-wiki-agent/src/code_wiki_agent/commands/scan.py
    agents/code-wiki-agent/src/code_wiki_agent/commands/lint.py
    agents/code-wiki-agent/tests/unit/test_commands_scan.py
    agents/code-wiki-agent/tests/unit/test_commands_lint.py
    agents/code-wiki-agent/tests/unit/test_query_code_fallback.py
    agents/code-wiki-agent/tests/test_command_overrides.py
  </read_first>
  <behavior>
    - `query.drill_page` (lines 903-914) now returns `TaskResult(value=resp.content, response=resp)`. `useful_excerpts` filtering at query.py:927 sees the same string value it does today.
    - `query.code_drill` (the code-fallback callback near line 459) returns `TaskResult(value=<final_content_string>, response=<last_resp_with_usage_metadata>)`. The `NO_RELEVANT_CONTENT` early-exit and the iteration-cap branch each construct an explicit `TaskResult` (response may be `None` on the cap-exit branch since no successful AIMessage produced the terminal value).
    - `scan.generate_stub` (lines 344-351) returns `TaskResult(value=resp.content, response=resp)`. Downstream `for pkg, llm_body in fan_result.successes` at scan.py:365 still receives a string `llm_body`.
    - `lint.run_linter_group` (lines 444-464) returns `TaskResult(value=findings, response=response)`. The empty-group early-return (`if not pages_input: return []`) returns `TaskResult(value=[], response=None)` for consistency. Downstream `for group_tuple, findings in fan_result.successes` at lint.py:477 still receives a `list[str]`.
    - The existing unit-test suite for these commands (test_commands_scan.py, test_commands_lint.py, test_query_code_fallback.py, test_command_overrides.py, test_query_trace_unit.py, test_query_result.py) continues to pass without modification — because the pool unwraps `TaskResult.value` into `successes`, all the MagicMock-patched `pool.run_all` paths return `FanOutResult(successes=[...])` with scalar values, which is what the existing tests already construct.
    - No new tests required for this task — the contract is locked by Task 1's pool unit tests; this task's job is the per-callsite migration.
  </behavior>
  <action>
    Import `TaskResult` into each of the three command modules. Add to the existing `from subagent_runtime.pool import ...` line in:
      - `query.py:46` (already imports `FanOutResult, SubagentPool`)
      - `scan.py:22` (already imports `FanOutResult, SubagentPool`)
      - `lint.py:37` (already imports `FanOutResult, PerItemError, SubagentPool`)

    For each fan-out callback, replace the bare `return <value>` with `return TaskResult(value=<value>, response=<raw_response_or_None>)`. Specifically:

    1. `query.py:drill_page` — currently `return resp.content`. Change to `return TaskResult(value=resp.content, response=resp)`.

    2. `query.py:code_drill` — there are three return paths in this function:
       - Tool-call exhausted at line 465: `return getattr(resp, "content", "") or ""`. The `resp` here is the last AIMessage; wrap as `TaskResult(value=<that_expression>, response=resp)`.
       - Iteration-cap exit at line 487: `return "NO_RELEVANT_CONTENT"`. No usage_metadata available (the iteration loop did write tokens to traces via separate paths, but the terminal sentinel has no AIMessage). Return `TaskResult(value="NO_RELEVANT_CONTENT", response=None)`. Document this with a one-line inline comment: `# iteration-cap exit: no terminal AIMessage; tokens were already accounted by per-iteration calls if logged elsewhere`.
       - Any other implicit return path: audit by reading the function; ensure no naked scalar returns remain.

    3. `scan.py:generate_stub` — currently `return resp.content`. Change to `return TaskResult(value=resp.content, response=resp)`.

    4. `lint.py:run_linter_group` — has two return paths:
       - Empty group: `if not pages_input: return []`. Change to `return TaskResult(value=[], response=None)`.
       - Normal return: `return findings`. Change to `return TaskResult(value=findings, response=response)` (where `response` is the variable holding the AIMessage from `linter_llm.ainvoke(msgs)` at line 460).

    Do NOT change any downstream consumer code (the `for ... in fan_result.successes:` loops). The pool unwraps `TaskResult.value` into `successes`, so all downstream code sees the same scalars it sees today.

    Do NOT modify the `synth_*` direct trace writes in query.py (lines 966-980 + the code-fallback synth at lines 525-540) — those are direct `write_trace_record` calls outside the pool path and were correctly wired in 16-01. They continue to write usage_metadata via the `_extract_usage_tokens` helper.

    Do NOT modify `commands/ingest.py` — it does not use the pool. Its per-call trace writes via `write_trace_record` were correctly wired in 16-01 (success path at line 452, error path at line 464).

    Commit message: `fix(16): wrap fan-out callbacks in TaskResult so librarian + scanner + linter + code_reader traces carry usage_metadata (G-01)`.
  </action>
  <verify>
    <automated>uv run --package code-wiki-agent pytest agents/code-wiki-agent/tests -x -q --ignore=agents/code-wiki-agent/tests/integration</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "TaskResult(value=" agents/code-wiki-agent/src/code_wiki_agent/commands/query.py` returns at least `3` (drill_page + 2 paths in code_drill).
    - `grep -c "TaskResult(value=" agents/code-wiki-agent/src/code_wiki_agent/commands/scan.py` returns `1`.
    - `grep -c "TaskResult(value=" agents/code-wiki-agent/src/code_wiki_agent/commands/lint.py` returns `2`.
    - `grep -E "from subagent_runtime.pool import.*TaskResult" agents/code-wiki-agent/src/code_wiki_agent/commands/{query,scan,lint}.py | wc -l` returns `3`.
    - `git diff agents/code-wiki-agent/src/code_wiki_agent/commands/ingest.py` is empty (ingest is not in scope).
    - Existing non-integration code-wiki-agent test suite passes (`uv run --package code-wiki-agent pytest agents/code-wiki-agent/tests -x -q --ignore=agents/code-wiki-agent/tests/integration` exits 0). Expected: ~205+ tests pass, matching the 16-01 SUMMARY baseline.
    - Full cross-package non-integration suite still green: `uv run pytest -x -q --ignore=agents/code-wiki-agent/tests/integration --ignore=packages/subagent-runtime/tests/integration` exits 0 with no regressions vs. the 549/22 baseline cited in 16-01 SUMMARY.
  </acceptance_criteria>
  <done>All 4 production fan-out callbacks return TaskResult; no downstream consumer code changed; full non-integration suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Re-run gated TRACE-FU-01 regression against real Bedrock</name>
  <what-built>
    Task 1 extended `SubagentPool` with a `TaskResult` opt-in contract that threads the raw AIMessage (and its `usage_metadata`) into `write_trace_record` without breaking scalar-returning callbacks. Task 2 migrated all four production fan-out callsites (scanner, linter, librarian, code_reader) to return `TaskResult(value=..., response=resp)`. With both in place, the librarian fan-out trace records should now carry non-None `tokens_in`/`tokens_out` on `status=success` — closing G-01.
  </what-built>
  <how-to-verify>
    1. From the repo root, set the integration gate and AWS profile as you did when the original gap was surfaced on 2026-05-19:
       ```
       export CODE_WIKI_RUN_INTEGRATION=1
       # ensure AWS_PROFILE points at the account with Bedrock access in us-east-1
       ```
    2. Run the gated regression test (this will invoke real Bedrock and incur cost — same scope as the original failed run; expected wall-clock ~30-90s):
       ```
       uv run pytest agents/code-wiki-agent/tests/integration/test_trace_coverage.py -x -v
       ```
    3. Confirm the test exits 0. Expected output: `1 passed in <X>s` with no skips and no failures.
    4. As a belt-and-braces check, inspect at least one librarian JSONL record produced under the tmp_path the test used (the test logs the path on failure; on success, grep for it in pytest's `-v` output or re-point the assertion at a persistent dir). Specifically verify a `role: librarian`, `status: success` record has integer `tokens_in` and `tokens_out` (not `null` / `None`) and a numeric `cost_usd` (assuming the model is in the pricing table).
    5. Also re-run the full non-integration suite to confirm no regression: `uv run pytest -x -q --ignore=agents/code-wiki-agent/tests/integration --ignore=packages/subagent-runtime/tests/integration` should still pass at 549+/22 skipped.

    If the test FAILS with a librarian record still showing `tokens_in: null`: paste the failing record into the resume signal. The likely cause is that one of the four migrated callbacks was missed or that the pool's `isinstance(result, TaskResult)` detection did not fire — both diagnosable from the JSONL evidence.

    If a NON-librarian non-error record fails the assertion (e.g., a `query_summary` record with `tokens_in: null`): that's WR-04 territory — see the 16-VERIFICATION.md anti-pattern table. Document the case in the resume signal; the appropriate fix is either (a) ensure the upstream code populates zeros instead of None for legitimately-empty paths, or (b) loosen the test's assertion. Decide based on the failing record's `kind`.
  </how-to-verify>
  <resume-signal>
    Reply `approved` if the gated test exits 0 AND a librarian success record carries non-None tokens.
    Reply `failed: <paste failing record>` if the assertion still trips — include the JSONL line so Task 4's documentation can capture the residual gap precisely.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 4: Document G-01 closure in 16-VERIFICATION.md</name>
  <files>
    .planning/phases/16-carry-forward-debt-cleanup/16-VERIFICATION.md
  </files>
  <read_first>
    .planning/phases/16-carry-forward-debt-cleanup/16-VERIFICATION.md
    .planning/phases/16-carry-forward-debt-cleanup/16-HUMAN-UAT.md
  </read_first>
  <action>
    Update `.planning/phases/16-carry-forward-debt-cleanup/16-VERIFICATION.md` to record G-01 closure. Do NOT create a separate `16-02-VERIFICATION.md` — gap_closure plans append to the phase's existing verification artifact so the SC history is in one place.

    Specific edits:

    1. **Frontmatter `status` field**: flip `status: gaps_found` → `status: human_needed`. (Cannot go to `passed` because the 3 judgment-call human-verification items remain pending; per the verifier decision tree, any open human_verification entries take priority over `passed`.) Update `verified:` timestamp to the wall-clock date the gated test passed (2026-05-19+).

    2. **Frontmatter `score` field**: rewrite to: `5/5 must-haves verified — SC#1 restored to VERIFIED after Plan 16-02 closed G-01 via SubagentPool TaskResult contract extension; gated TRACE-FU-01 regression test exited 0 against real Bedrock on <date>; 3 remaining SCs still have documented substitutions requiring human ack (items 2/3/4 in human_verification)`.

    3. **Frontmatter `gaps` block**: rewrite G-01 to:
       ```yaml
       gaps:
         - id: G-01
           sc: 1
           status: CLOSED
           closed_by: "Plan 16-02"
           closed_on: <YYYY-MM-DD of passing run>
           summary: "Librarian fan-out trace records previously emitted tokens_in=None / tokens_out=None because drill_page returned only resp.content. Plan 16-02 added an opt-in TaskResult contract to SubagentPool.run_all and migrated all 4 production fan-out callbacks (scanner, linter, librarian, code_reader) to return TaskResult(value=..., response=resp). The pool now threads response.usage_metadata into write_trace_record without breaking scalar-returning callbacks."
           evidence: "CODE_WIKI_RUN_INTEGRATION=1 uv run pytest agents/code-wiki-agent/tests/integration/test_trace_coverage.py -x -v → PASSED on <date>. Librarian record sample: role=librarian model=us.anthropic.claude-haiku-4-5-20251001-v1:0 status=success tokens_in=<N> tokens_out=<N> cost_usd=<F>. Non-integration suite green: 549+/22 skipped."
           files_touched_by_fix: ["packages/subagent-runtime/src/subagent_runtime/pool.py", "packages/subagent-runtime/src/subagent_runtime/__init__.py", "packages/subagent-runtime/tests/test_pool.py", "agents/code-wiki-agent/src/code_wiki_agent/commands/query.py", "agents/code-wiki-agent/src/code_wiki_agent/commands/scan.py", "agents/code-wiki-agent/src/code_wiki_agent/commands/lint.py"]
       ```
       Replace concrete `<N>` / `<F>` / `<date>` placeholders with the actual values from the Task 3 run transcript captured in the resume-signal reply.

    4. **Goal-Achievement table row for SC#1**: flip the Status column from `✗ FAILED (G-01)` to `✓ VERIFIED` and rewrite the Evidence column to cite the passing Task 3 transcript and reference the TaskResult contract change in 16-02 (link the plan ID; do not paste the whole transcript).

    5. **Anti-Patterns table — WR-04 row**: re-evaluate whether the gated regression now requires the WR-04 caveat ("asserts `tokens_in/tokens_out is not None` on all `query_summary` records but code-fallback empty/empty legitimately writes None"). Two cases:
       - If Task 3's gated run PASSED on a vault that exercises both vault-rich and code-fallback paths and the query_summary records carry non-None tokens: mark WR-04 as obsoleted (the legitimate-empty case is not actually reachable in production runs against real vaults). Add a one-line note.
       - If Task 3's gated run only exercised the vault-rich path (likely, given the round-trip-vault fixture content): leave WR-04 as a known warning and note that any future vault-thin code-fallback run may still trip it.

    6. **Gaps Summary section** at the bottom: rewrite to state that all 5 must-haves are VERIFIED with code + tests, and the 3 remaining human-verification items (live model-sweep, calendar-vs-event trigger, code_reader non-trivial scores) are operator-judgment calls scoped to acceptance, not code closure. The phase is unblocked on TRACE-FU-01.

    7. Append a short audit footnote at the bottom: `_Updated 2026-05-19+ by Plan 16-02 (G-01 closure). See 16-02-SUMMARY.md for the contract-change rationale + commits._`

    Do NOT modify the `human_verification` block — items 2/3/4 remain `[pending]` per HUMAN-UAT.md and require Pat's ack outside this plan.

    Commit message: `docs(16): mark G-01 CLOSED in VERIFICATION after Plan 16-02 landed TaskResult contract`.
  </action>
  <verify>
    <automated>grep -E "status:[[:space:]]+CLOSED" .planning/phases/16-carry-forward-debt-cleanup/16-VERIFICATION.md | grep -c "G-01\|CLOSED"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "status: CLOSED" .planning/phases/16-carry-forward-debt-cleanup/16-VERIFICATION.md` returns at least `1` (the G-01 entry).
    - `grep -c "✗ FAILED (G-01)" .planning/phases/16-carry-forward-debt-cleanup/16-VERIFICATION.md` returns `0` (the SC#1 row was flipped to VERIFIED).
    - `grep -c "Plan 16-02" .planning/phases/16-carry-forward-debt-cleanup/16-VERIFICATION.md` returns at least `2` (frontmatter closed_by + footnote).
    - The frontmatter parses as valid YAML (`python -c "import yaml; yaml.safe_load(open('.planning/phases/16-carry-forward-debt-cleanup/16-VERIFICATION.md').read().split('---')[1])"` exits 0).
    - The frontmatter `status` field is `human_needed` (not `passed`, because items 2/3/4 in `human_verification` are still `[pending]`).
    - The Gaps Summary section no longer claims SC#1 FAILED; it explicitly says all 5 must-haves are VERIFIED.
    - No edits made to `16-HUMAN-UAT.md`, `16-01-PLAN.md`, `16-01-SUMMARY.md`, `16-CONTEXT.md`, or `16-PATTERNS.md` (verify via `git diff --name-only` showing only `16-VERIFICATION.md` plus the source files from Tasks 1-2).
  </acceptance_criteria>
  <done>16-VERIFICATION.md records G-01 → CLOSED with date + evidence + commit references; SC#1 row flipped to VERIFIED; status field flipped to `human_needed`; no other phase docs touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pool.SubagentPool ↔ task callback | Pool invokes caller-supplied async callable; callable returns a value that the pool writes into a JSONL trace record. Both sides are in-process Python — no network or cross-host trust crossing. |
| pool ↔ trace_io.write_trace_record | Pool passes the callback's return value (or its `response` field, post-Task-1) as the `response` argument; `write_trace_record` extracts `usage_metadata` and serializes the record to disk. Trust-internal boundary; no new attack surface. |
| Gated test ↔ real Bedrock | Task 3 invokes ChatBedrockConverse via librarian + synthesizer LLMs. AWS credentials carry across this boundary as they did in 16-01's gated runs. No new credential paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16.02-01 | Tampering | TaskResult dataclass field types | mitigate | `isinstance(result, TaskResult)` detection in `_run_one` — strict type check, not duck-typing on `.value` / `.response` fields (would collide with arbitrary user objects). Documented in pool.py inline + verified by Task 1 unit test that returns a scalar dict with `.value`/`.response`-like keys and confirms backward-compat path fires. |
| T-16.02-02 | Information Disclosure | usage_metadata dict logged to JSONL | accept | usage_metadata fields are token counts (integers) + optional total_tokens — no PII, no secrets. Already emitted today by ingest + synthesizer trace paths; this plan just extends coverage to librarian/scanner/linter/code_reader. Same disposition as the Phase 9 OBS-04 threat model. |
| T-16.02-03 | Denial of Service | Trace write OSError on a full disk during gated test | accept | `write_trace_record` already swallows OSError with WARNING log (existing invariant preserved). Test failure mode is "tokens still None" not "test crash" — surfaces as assertion failure with diagnostic JSONL, not a silent corruption. |
| T-16.02-04 | Repudiation | Migration of 4 callsites without an integration test catching a miss | mitigate | The gated TRACE-FU-01 test (Task 3) walks EVERY JSONL record in the trace dir and asserts non-None tokens on every non-error/non-event record — by construction it catches any missed callsite. The librarian path is exercised by every query; scanner + linter are exercised by the underlying `code-wiki-agent query` command's index-build prerequisites if the fixture vault requires them. (Note: the round-trip-vault fixture may not exercise scanner/linter; those paths are covered by their dedicated unit-test suites confirming the callback signature change.) |
| T-16.02-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new dependencies. Plan modifies only existing source files; no `uv add`, no `pyproject.toml` edits. The Package Legitimacy Audit from 16-01 RESEARCH.md still applies (no new packages introduced). |
</threat_model>

<verification>
Phase-level checks after all 4 tasks complete:

- `uv run --package subagent-runtime pytest packages/subagent-runtime/tests/test_pool.py -x -v` — all 13 pre-existing + 3 new tests PASS.
- `uv run --package code-wiki-agent pytest agents/code-wiki-agent/tests -x -q --ignore=agents/code-wiki-agent/tests/integration` — all non-integration code-wiki-agent tests PASS (baseline ~205+).
- `uv run pytest -x -q --ignore=agents/code-wiki-agent/tests/integration --ignore=packages/subagent-runtime/tests/integration` — full cross-package non-integration suite PASSES with no regression vs. 16-01's 549/22 baseline.
- `CODE_WIKI_RUN_INTEGRATION=1 uv run pytest agents/code-wiki-agent/tests/integration/test_trace_coverage.py -x -v` — gated regression PASSES against real Bedrock (Task 3).
- `git log --oneline -5` shows three new feature commits (Task 1 feat, Task 2 fix, Task 4 docs) on top of the existing 16-01 commit chain.
- `grep -c "status: CLOSED" .planning/phases/16-carry-forward-debt-cleanup/16-VERIFICATION.md` returns `1`.
- No edits to `16-01-PLAN.md`, `16-01-SUMMARY.md`, `16-CONTEXT.md`, `16-PATTERNS.md`, `16-HUMAN-UAT.md`, `16-DISCUSSION-LOG.md`, or `16-REVIEW.md`.
</verification>

<success_criteria>
- **G-01 closed:** `tokens_in` / `tokens_out` are non-None on every non-error / non-event JSONL trace record produced by `code-wiki-agent query` against a real Bedrock account, verified by `test_trace_coverage.py` exiting 0 under `CODE_WIKI_RUN_INTEGRATION=1`.
- **Backward compatibility preserved:** All 13 pre-existing `test_pool.py` tests pass without modification (callbacks returning scalars continue to work).
- **Contract change is opt-in and explicit:** `TaskResult` is the single way a callback can surface `usage_metadata`; detection is `isinstance(result, TaskResult)`, not duck-typing. Exported from `subagent_runtime.__init__` for downstream discoverability.
- **All four production fan-out callsites migrated atomically:** scanner, linter, librarian, code_reader all return `TaskResult` — no half-migrated state where one role still drops usage_metadata.
- **TRACE-FU-01 fully satisfied:** REQUIREMENTS.md line 43 contract met; 16-VERIFICATION.md SC#1 row flipped from FAILED → VERIFIED.
- **No new dependencies; no expanded threat surface:** Zero `uv add` calls, no new packages, T-16-SC mitigation from 16-01 carries forward unchanged.
- **Phase docs scoped:** Only 16-VERIFICATION.md updated; 16-01-PLAN.md, 16-01-SUMMARY.md, and all other phase docs untouched.
</success_criteria>

<output>
After all 4 tasks complete, create `.planning/phases/16-carry-forward-debt-cleanup/16-02-SUMMARY.md` recording:
- Performance (duration, commits)
- Accomplishment: G-01 closed via `TaskResult` contract extension
- Task commits (3: feat / fix / docs)
- Verification reference: link to updated 16-VERIFICATION.md + the Task 3 transcript
- Decisions made: rationale for option (a) `TaskResult` dataclass vs option (b) pool-side response-aware hook (chose (a) because explicit type-checked sentinel beats hidden duck-typing; backward-compat via scalar passthrough; 4 callsite migration is small enough that the contract change does not need to be invisible)
- Deviations from plan (if any)
- Known stubs: none expected
- Threat flags: none new (T-16.02-* all mitigated or accepted)
- Self-check: PASSED block confirming the gated test exited 0 and SC#1 is VERIFIED

Remaining human-verification items 2/3/4 in `16-HUMAN-UAT.md` are explicitly OUT of scope for this plan; they remain `[pending]` for Pat's ack.
</output>
</content>
</invoke>