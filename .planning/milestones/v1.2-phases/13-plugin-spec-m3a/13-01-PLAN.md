---
phase: 13-plugin-spec-m3a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/spec/13-plugin-contract/init.md
  - .planning/spec/13-plugin-contract/scan.md
autonomous: true
requirements:
  - PLUGIN-01

must_haves:
  truths:
    - "A reader of .planning/spec/13-plugin-contract/init.md can produce the /graph-wiki:bootstrap plugin shim without consulting any other source"
    - "A reader of .planning/spec/13-plugin-contract/scan.md can produce the /graph-wiki:scan plugin shim without consulting any other source"
    - "Both spec files carry the locked port_verdict from CONTEXT.md (rename for both)"
  artifacts:
    - path: ".planning/spec/13-plugin-contract/init.md"
      provides: "Per-command port spec for /graph-wiki:bootstrap (rename verdict)"
      contains: "port_verdict: rename"
    - path: ".planning/spec/13-plugin-contract/scan.md"
      provides: "Per-command port spec for /graph-wiki:scan (rename verdict)"
      contains: "port_verdict: rename"
  key_links:
    - from: ".planning/spec/13-plugin-contract/init.md"
      to: "packages/wiki-io/src/wiki_io/init_vault.py + detect_containers.py"
      via: "Shell-out contract section names the target Python modules"
      pattern: "wiki_io\\.(init_vault|detect_containers)"
    - from: ".planning/spec/13-plugin-contract/scan.md"
      to: "packages/wiki-io/src/wiki_io/scan_monorepo.py"
      via: "Shell-out contract section names the target Python module"
      pattern: "wiki_io\\.scan_monorepo"
---

<objective>
Author the per-command port specs for `/graph-wiki:bootstrap` and `/graph-wiki:scan` — the first two of six command-spec files Phase 13 must produce.

Purpose: Phase 14's plugin port executor reads these files to know exactly what shim to write, what Python module to import, what args to pass through, and what upstream prose to preserve byte-for-byte vs. paraphrase. The verdicts (`rename` for both) and shell-out invocation shape (SO-01..SO-04) are already locked in CONTEXT.md — this plan transcribes those decisions into the per-command file format mandated by SP-02.

Output: Two markdown files under a new directory `.planning/spec/13-plugin-contract/`, each conforming to the SP-02 mandatory template (frontmatter + Shell-out contract + Prose-preservation map + Agent/skill rename map + Reshape notes + Verification gate).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-plugin-spec-m3a/13-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author init.md (per-command spec for /graph-wiki:bootstrap)</name>
  <files>.planning/spec/13-plugin-contract/init.md</files>
  <read_first>
    - /Users/pat/Personal/agent-research/.planning/phases/13-plugin-spec-m3a/13-CONTEXT.md (locked decisions SP-02 template, SO-01..SO-04 invocation shape, P-01..P-03 inference path, row #1 in the verdict table)
    - /Users/pat/Personal/lattice/plugins/lattice-wiki/commands/init.md (87 lines — the upstream prose to preserve / rebrand section-by-section)
    - /Users/pat/Personal/lattice/plugins/lattice-wiki/skills/lattice-wiki/scripts/init_vault.py (upstream shim shape for SO-02 reference)
    - /Users/pat/Personal/agent-research/packages/wiki-io/src/wiki_io/init_vault.py (the target module the shim will import as `wiki_io.init_vault.main`)
    - /Users/pat/Personal/agent-research/packages/wiki-io/src/wiki_io/detect_containers.py (the pre-step module called as `wiki_io.detect_containers.main --json` before init_vault)
  </read_first>
  <action>
    Create `.planning/spec/13-plugin-contract/init.md` with these MANDATORY sections per SP-02:

    1. YAML frontmatter with exactly three keys:
       - `command: init`
       - `upstream_source: plugins/lattice-wiki/commands/init.md`
       - `port_verdict: rename`

    2. H1 title: `# /graph-wiki:bootstrap — Port Spec`

    3. `## Shell-out contract` section with bullets:
       - Invocation: literal string `uv run --project "$AGENT_RESEARCH_ROOT" python3 "${CLAUDE_PLUGIN_ROOT}/skills/graph-wiki/scripts/init_vault.py" $ARGUMENTS`
       - Target module (claude backend): `wiki_io.init_vault.main`
       - Target subprocess (bedrock backend): `graph-wiki-agent init <args>`
       - Args pass-through: list every flag the upstream init takes (read upstream init.md to enumerate — likely `--topic`, `--tool`, similar) and map 1:1 to wiki_io.init_vault
       - Pre-step: `wiki_io.detect_containers.main --json` runs first to enumerate packages for the initial scan list (note this is the upstream behavior preserved)

    4. `## Prose-preservation map` section. Walk every H2/H3 in upstream `commands/init.md` and assign each a verdict (verbatim except namespace rename `/lattice-wiki:` → `/graph-wiki:`, OR paraphrase + cite). Concrete sections to cover (check upstream file for exact list): Usage, What happens, Sub-agent, anything else present. For each: one line stating "verbatim except rename" or "reshape: <what changed>".

    5. `## Agent / skill rename map` section:
       - Agent files used by `/init`: identify from upstream init.md (likely scanner). For each: `agents/<name>.md` — name stays, namespace prose rebranded.
       - Skill: `skills/lattice-wiki/SKILL.md` → `skills/graph-wiki/SKILL.md` — rename + namespace rebrand.
       - Script file: `skills/lattice-wiki/scripts/init_vault.py` → `skills/graph-wiki/scripts/init_vault.py`.

    6. `## Reshape notes` section. Since verdict is `rename`, write a single line: "No behavior changes vs upstream. Pure rename of namespace and import target."

    7. `## Verification gate` section: state how Phase 14 confirms `/graph-wiki:bootstrap` works. Example: "Run `/graph-wiki:bootstrap --topic test --tool claude-code` in an empty directory; resulting wiki/ tree byte-identical to upstream output captured in a baseline (or manual diff against a fresh `/lattice-wiki:init` run with identical args)."

    Use the SP-02 example block in CONTEXT.md as the literal template. Reference SHELL-OUT-PATTERN.md (which Plan 04 will write) via "see SHELL-OUT-PATTERN.md §SO-01" rather than re-stating the SO-01 invocation rationale.
  </action>
  <verify>
    <automated>test -f .planning/spec/13-plugin-contract/init.md &amp;&amp; grep -q '^port_verdict: rename' .planning/spec/13-plugin-contract/init.md &amp;&amp; grep -q '^## Shell-out contract' .planning/spec/13-plugin-contract/init.md &amp;&amp; grep -q '^## Prose-preservation map' .planning/spec/13-plugin-contract/init.md &amp;&amp; grep -q '^## Agent / skill rename map' .planning/spec/13-plugin-contract/init.md &amp;&amp; grep -q '^## Reshape notes' .planning/spec/13-plugin-contract/init.md &amp;&amp; grep -q '^## Verification gate' .planning/spec/13-plugin-contract/init.md &amp;&amp; grep -q 'wiki_io.init_vault.main' .planning/spec/13-plugin-contract/init.md &amp;&amp; grep -q 'wiki_io.detect_containers.main' .planning/spec/13-plugin-contract/init.md &amp;&amp; grep -q 'uv run --project' .planning/spec/13-plugin-contract/init.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/spec/13-plugin-contract/init.md` exists
    - Frontmatter contains exactly `command: init`, `upstream_source: plugins/lattice-wiki/commands/init.md`, `port_verdict: rename`
    - All six MANDATORY H2 sections from SP-02 are present (Shell-out contract, Prose-preservation map, Agent / skill rename map, Reshape notes, Verification gate)
    - H1 reads `# /graph-wiki:bootstrap — Port Spec`
    - Shell-out contract section names target module `wiki_io.init_vault.main`, pre-step `wiki_io.detect_containers.main`, and contains the literal string `uv run --project`
    - Prose-preservation map enumerates every H2 found in the upstream `plugins/lattice-wiki/commands/init.md` with a verdict per section
    - Reshape notes section explicitly states "No behavior changes vs upstream" (or equivalent), since verdict is `rename`
  </acceptance_criteria>
  <done>
    init.md exists at the spec path, all six mandatory sections present, frontmatter locks port_verdict=rename, shell-out names the correct wiki_io modules, automated grep gate passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author scan.md (per-command spec for /graph-wiki:scan)</name>
  <files>.planning/spec/13-plugin-contract/scan.md</files>
  <read_first>
    - /Users/pat/Personal/agent-research/.planning/phases/13-plugin-spec-m3a/13-CONTEXT.md (SP-02 template, row #2 in verdict table — clean-tree-on-main gate preservation note)
    - /Users/pat/Personal/lattice/plugins/lattice-wiki/commands/scan.md (75 lines — upstream prose source)
    - /Users/pat/Personal/lattice/plugins/lattice-wiki/skills/lattice-wiki/scripts/scan_monorepo.py (upstream shim being retargeted)
    - /Users/pat/Personal/agent-research/packages/wiki-io/src/wiki_io/scan_monorepo.py (target module the shim will import as `wiki_io.scan_monorepo.main`)
    - /Users/pat/Personal/agent-research/.planning/spec/13-plugin-contract/init.md (peer file for consistency — written in Task 1 of this same plan)
  </read_first>
  <action>
    Create `.planning/spec/13-plugin-contract/scan.md` mirroring the structure of Task 1's init.md, with the following concrete values:

    1. Frontmatter:
       - `command: scan`
       - `upstream_source: plugins/lattice-wiki/commands/scan.md`
       - `port_verdict: rename`

    2. H1: `# /graph-wiki:scan — Port Spec`

    3. `## Shell-out contract`:
       - Invocation: `uv run --project "$AGENT_RESEARCH_ROOT" python3 "${CLAUDE_PLUGIN_ROOT}/skills/graph-wiki/scripts/scan_monorepo.py" $ARGUMENTS`
       - Target module (claude): `wiki_io.scan_monorepo.main`
       - Target subprocess (bedrock): `graph-wiki-agent scan <args>`
       - Args pass-through: enumerate upstream scan.md flags (e.g., `--repo`, package selectors) and map 1:1
       - Pre-step: NONE — but explicitly note "clean-tree-on-main git gate is enforced inside `wiki_io.scan_monorepo.main` (preserved from upstream); no additional pre-step required at the shim layer"

    4. `## Prose-preservation map`: walk every H2 in upstream `scan.md`. Each gets verdict "verbatim except namespace rename" unless behavior differs. The scanner sub-agent prose is verbatim; only the slash command namespace strings (`/lattice-wiki:scan` → `/graph-wiki:scan`) change.

    5. `## Agent / skill rename map`:
       - `agents/scanner.md` — name stays, namespace prose rebranded
       - `skills/lattice-wiki/SKILL.md` → `skills/graph-wiki/SKILL.md`
       - Script: `skills/lattice-wiki/scripts/scan_monorepo.py` → `skills/graph-wiki/scripts/scan_monorepo.py`

    6. `## Reshape notes`: "No behavior changes vs upstream. Pure rename of namespace and import target. The clean-tree-on-main gate (referenced in upstream `scan.md` under What happens) is preserved verbatim inside `wiki_io.scan_monorepo.main` (no shim-level enforcement required)."

    7. `## Verification gate`: e.g., "Run `/graph-wiki:scan --repo ~/Personal/graph-wiki/agent-research` from a clean main branch; output matches upstream `/lattice-wiki:scan` against the same repo (modulo brand string differences). Failure mode to test: dirty tree must abort with the upstream error string (verbatim preservation check)."

    Reference SHELL-OUT-PATTERN.md §SO-01 instead of re-explaining the invocation shape.
  </action>
  <verify>
    <automated>test -f .planning/spec/13-plugin-contract/scan.md &amp;&amp; grep -q '^port_verdict: rename' .planning/spec/13-plugin-contract/scan.md &amp;&amp; grep -q '^## Shell-out contract' .planning/spec/13-plugin-contract/scan.md &amp;&amp; grep -q '^## Prose-preservation map' .planning/spec/13-plugin-contract/scan.md &amp;&amp; grep -q '^## Agent / skill rename map' .planning/spec/13-plugin-contract/scan.md &amp;&amp; grep -q '^## Reshape notes' .planning/spec/13-plugin-contract/scan.md &amp;&amp; grep -q '^## Verification gate' .planning/spec/13-plugin-contract/scan.md &amp;&amp; grep -q 'wiki_io.scan_monorepo.main' .planning/spec/13-plugin-contract/scan.md &amp;&amp; grep -q 'uv run --project' .planning/spec/13-plugin-contract/scan.md &amp;&amp; grep -iq 'clean-tree' .planning/spec/13-plugin-contract/scan.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/spec/13-plugin-contract/scan.md` exists
    - Frontmatter contains exactly `command: scan`, `upstream_source: plugins/lattice-wiki/commands/scan.md`, `port_verdict: rename`
    - All six MANDATORY H2 sections from SP-02 are present
    - H1 reads `# /graph-wiki:scan — Port Spec`
    - Shell-out contract names `wiki_io.scan_monorepo.main` and contains literal `uv run --project`
    - Reshape notes mention the clean-tree-on-main gate preservation (the one explicit upstream-behavior callout in the verdict table)
    - Prose-preservation map covers every H2 in upstream `scan.md`
  </acceptance_criteria>
  <done>
    scan.md exists at the spec path, all six mandatory sections present, frontmatter locks port_verdict=rename, shell-out names `wiki_io.scan_monorepo.main`, clean-tree gate preservation called out, automated grep gate passes.
  </done>
</task>

</tasks>

<verification>
- `ls .planning/spec/13-plugin-contract/` shows `init.md` and `scan.md`
- Both files pass the per-task automated grep gates
- Frontmatter in both files locks `port_verdict: rename`
- Both files name the correct `wiki_io.*` target modules in their Shell-out contract sections
</verification>

<success_criteria>
Per-command spec files for `/graph-wiki:bootstrap` and `/graph-wiki:scan` exist under `.planning/spec/13-plugin-contract/`, each conforming to the SP-02 mandatory template, each transcribing the locked `rename` verdict from CONTEXT.md, each ready to be consumed by Phase 14's plugin port executor without further design decisions.
</success_criteria>

<output>
Create `.planning/phases/13-plugin-spec-m3a/13-01-SUMMARY.md` when done.
</output>
