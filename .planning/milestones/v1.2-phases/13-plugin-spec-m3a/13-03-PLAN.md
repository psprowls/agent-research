---
phase: 13-plugin-spec-m3a
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/spec/13-plugin-contract/query.md
  - .planning/spec/13-plugin-contract/log.md
autonomous: true
requirements:
  - PLUGIN-01

must_haves:
  truths:
    - "A reader of .planning/spec/13-plugin-contract/query.md can produce the /graph-wiki:query plugin behavior (LLM-driven + BM25 fallback) without consulting any other source"
    - "A reader of .planning/spec/13-plugin-contract/log.md can produce the /graph-wiki:log command (prose-only, no script) without consulting any other source"
    - "query.md locks the BM25 fallback shell-out target (vault_io.wiki_search, VP-01 prerequisite); log.md locks 'no script' parity with upstream"
  artifacts:
    - path: ".planning/spec/13-plugin-contract/query.md"
      provides: "Per-command port spec for /graph-wiki:query (rename verdict, LLM-driven + BM25 fallback)"
      contains: "port_verdict: rename"
    - path: ".planning/spec/13-plugin-contract/log.md"
      provides: "Per-command port spec for /graph-wiki:log (rename verdict, prose-only)"
      contains: "port_verdict: rename"
  key_links:
    - from: ".planning/spec/13-plugin-contract/query.md"
      to: "packages/vault-io (wiki_search.py port required as Phase 14 Plan 2)"
      via: "Shell-out contract names `vault_io.wiki_search.main` and VP-01 prerequisite"
      pattern: "vault_io\\.wiki_search"
    - from: ".planning/spec/13-plugin-contract/log.md"
      to: "wiki/log.md inside the workspace"
      via: "Documented as `grep + tail` against workspace log; no shell-out script"
      pattern: "wiki/log\\.md"
---

<objective>
Author the per-command port specs for `/graph-wiki:query` (LLM-driven with BM25 fallback) and `/graph-wiki:log` (prose-only, no script — upstream parity).

Purpose: These two complete the six per-command files Phase 13 must produce. `query` carries the second VP-01 prerequisite (wiki_search.py port) and the only command where the shell-out happens only on fallback (LLM path is in-session Claude Code inference per P-01). `log` is the simplest port — it has no script upstream, just prose with namespace + path strings.

Output: Two markdown files under `.planning/spec/13-plugin-contract/`, each conforming to the SP-02 mandatory template.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-plugin-spec-m3a/13-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author query.md (per-command spec for /graph-wiki:query)</name>
  <files>.planning/spec/13-plugin-contract/query.md</files>
  <read_first>
    - /Users/pat/Personal/agent-research/.planning/phases/13-plugin-spec-m3a/13-CONTEXT.md (SP-02 template, row #5 in verdict table — LLM-driven, only BM25 fallback shells out; VP-01 prerequisite wiki_search.py port; P-01 inference-path reframe)
    - /Users/pat/Personal/lattice/plugins/lattice-wiki/commands/query.md (56 lines — upstream prose source)
    - /Users/pat/Personal/lattice/plugins/lattice-wiki/skills/lattice-wiki/scripts/wiki_search.py (upstream shim being retargeted — BM25 fallback path)
    - /Users/pat/Personal/lattice/plugins/lattice-wiki/agents/librarian.md (the LLM-driven agent doing the heavy lifting; rename map entry)
  </read_first>
  <action>
    Create `.planning/spec/13-plugin-contract/query.md` with these concrete values:

    1. Frontmatter:
       - `command: query`
       - `upstream_source: plugins/lattice-wiki/commands/query.md`
       - `port_verdict: rename`

    2. H1: `# /graph-wiki:query — Port Spec`

    3. `## Shell-out contract`:
       - Primary path: LLM-driven via Claude Code session (the librarian sub-agent). No Python shell-out for the primary path. State this explicitly: "Primary path is in-session Claude Code inference per P-01; the librarian agent (`agents/librarian.md`) runs synthesis inside the Claude Code host. No `uv run` invocation on the primary path."
       - Fallback path (BM25): `uv run --project "$AGENT_RESEARCH_ROOT" python3 "${CLAUDE_PLUGIN_ROOT}/skills/graph-wiki/scripts/wiki_search.py" $ARGUMENTS`
       - Target module (claude backend, fallback only): `vault_io.wiki_search.main` — NOTE: this module must be ported into vault-io as Phase 14 Plan 2 (VP-01 prerequisite); does not exist today
       - Target subprocess (bedrock backend): `graph-wiki-agent query <args>` (note: bedrock branch covers the whole flow, not just fallback, since graph-wiki-agent's `query` already does the LLM synthesis itself)
       - Args pass-through: enumerate upstream query.md flags 1:1
       - VP-01 callout: "Prerequisite — `wiki_search.py` (~194 LOC) must be ported from `lattice_wiki_core` to `vault_io` as Phase 14 Plan 2 before this shim's fallback path works."

    4. `## Prose-preservation map`: walk every H2 in upstream `query.md`. Each gets verdict "verbatim except namespace rename" unless behavior differs. The librarian sub-agent prose is verbatim; only the namespace strings (`/lattice-wiki:query` → `/graph-wiki:query`) change.

    5. `## Agent / skill rename map`:
       - `agents/librarian.md` — name stays, namespace prose rebranded (this is the most-edited agent prose surface in /query)
       - `skills/lattice-wiki/SKILL.md` → `skills/graph-wiki/SKILL.md`
       - Script: `skills/lattice-wiki/scripts/wiki_search.py` → `skills/graph-wiki/scripts/wiki_search.py`

    6. `## Reshape notes`: even though verdict is `rename`, briefly explain the fallback-only shell-out shape: "Verdict is `rename` because user-facing behavior is byte-for-byte preserved. The shell-out invocation only fires on the BM25-fallback branch — the primary LLM-driven path stays inside the Claude Code host session (P-01). Mirrors upstream exactly. Prerequisite: VP-01 wiki_search.py port (Phase 14 Plan 2)."

    7. `## Verification gate`: e.g., "Run `/graph-wiki:query 'what is workspace-io?'` against `~/Personal/graph-wiki/agent-research`; returns a librarian answer citing the new code paths (matches the Phase 14 SC#4 smoke). Fallback test: trigger a no-LLM path (mechanism per upstream wiki_search) and confirm BM25 results match upstream output modulo brand strings."

    Reference SHELL-OUT-PATTERN.md §SO-01 and CONTEXT.md §P-01 / §VP-01 for the prerequisite + inference-path framing.
  </action>
  <verify>
    <automated>test -f .planning/spec/13-plugin-contract/query.md &amp;&amp; grep -q '^port_verdict: rename' .planning/spec/13-plugin-contract/query.md &amp;&amp; grep -q '^## Shell-out contract' .planning/spec/13-plugin-contract/query.md &amp;&amp; grep -q '^## Prose-preservation map' .planning/spec/13-plugin-contract/query.md &amp;&amp; grep -q '^## Agent / skill rename map' .planning/spec/13-plugin-contract/query.md &amp;&amp; grep -q '^## Reshape notes' .planning/spec/13-plugin-contract/query.md &amp;&amp; grep -q '^## Verification gate' .planning/spec/13-plugin-contract/query.md &amp;&amp; grep -q 'vault_io.wiki_search' .planning/spec/13-plugin-contract/query.md &amp;&amp; grep -q 'VP-01' .planning/spec/13-plugin-contract/query.md &amp;&amp; grep -iq 'bm25\|fallback' .planning/spec/13-plugin-contract/query.md &amp;&amp; grep -q 'librarian' .planning/spec/13-plugin-contract/query.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/spec/13-plugin-contract/query.md` exists
    - Frontmatter locks `port_verdict: rename`, `command: query`
    - All six MANDATORY H2 sections from SP-02 are present
    - Shell-out contract distinguishes primary (LLM, no shell-out) vs fallback (BM25, shells to `vault_io.wiki_search.main`)
    - VP-01 prerequisite for wiki_search.py is referenced explicitly
    - Librarian agent rename row is present in the Agent / skill rename map
    - The strings "BM25" or "fallback" and "librarian" appear in the file
  </acceptance_criteria>
  <done>
    query.md exists, all six mandatory sections present, port_verdict=rename locked, LLM-primary + BM25-fallback shape documented, VP-01 prerequisite called out, librarian rename row present, automated grep gate passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author log.md (per-command spec for /graph-wiki:log)</name>
  <files>.planning/spec/13-plugin-contract/log.md</files>
  <read_first>
    - /Users/pat/Personal/agent-research/.planning/phases/13-plugin-spec-m3a/13-CONTEXT.md (SP-02 template, row #6 in verdict table — prose-only, no script, mirrors upstream)
    - /Users/pat/Personal/lattice/plugins/lattice-wiki/commands/log.md (62 lines — upstream prose source; this is the entire command surface since there is no script)
  </read_first>
  <action>
    Create `.planning/spec/13-plugin-contract/log.md` with these concrete values:

    1. Frontmatter:
       - `command: log`
       - `upstream_source: plugins/lattice-wiki/commands/log.md`
       - `port_verdict: rename`

    2. H1: `# /graph-wiki:log — Port Spec`

    3. `## Shell-out contract`:
       - Invocation: NONE — this command has no script in the upstream plugin and the port preserves that.
       - Mechanism: prose-only command; the body instructs the user (or the Claude Code session) to run `grep + tail` against `<workspace>/wiki/log.md` directly.
       - No `uv run` line. Explicitly state: "No script ships under `plugins/graph-wiki/skills/graph-wiki/scripts/log.py` (parity with upstream which has no `log.py` either)."
       - Optional placeholder script: per Claude's Discretion in CONTEXT.md §decisions, the executor MAY choose to ship a tiny `log.py` that just wraps `grep + tail` if it improves UX — but the default is "no script" matching upstream.

    4. `## Prose-preservation map`: walk every H2 in upstream `log.md`. Each gets verdict "verbatim except namespace rename (`/lattice-wiki:log` → `/graph-wiki:log`) and path string updates if the upstream prose references `.lattice.yaml` or similar (rebrand to `.graph-wiki.yaml`)".

    5. `## Agent / skill rename map`:
       - No agent involved (prose-only command, no sub-agent dispatch)
       - `skills/lattice-wiki/SKILL.md` → `skills/graph-wiki/SKILL.md` (boilerplate inherited; not log-specific)
       - No script rename (no script ships)

    6. `## Reshape notes`: "No behavior changes vs upstream. Pure prose rename of namespace and any path strings referencing the manifest/log files. No script ships (parity with upstream)."

    7. `## Verification gate`: e.g., "Run `/graph-wiki:log` in a Claude Code session inside a workspace; output mirrors upstream `/lattice-wiki:log` against the same `wiki/log.md` file content (modulo brand strings)."

    Reference SHELL-OUT-PATTERN.md §SO-01 only in passing (this command does not actually use it, but the rename map link is useful).
  </action>
  <verify>
    <automated>test -f .planning/spec/13-plugin-contract/log.md &amp;&amp; grep -q '^port_verdict: rename' .planning/spec/13-plugin-contract/log.md &amp;&amp; grep -q '^## Shell-out contract' .planning/spec/13-plugin-contract/log.md &amp;&amp; grep -q '^## Prose-preservation map' .planning/spec/13-plugin-contract/log.md &amp;&amp; grep -q '^## Agent / skill rename map' .planning/spec/13-plugin-contract/log.md &amp;&amp; grep -q '^## Reshape notes' .planning/spec/13-plugin-contract/log.md &amp;&amp; grep -q '^## Verification gate' .planning/spec/13-plugin-contract/log.md &amp;&amp; grep -iq 'no script' .planning/spec/13-plugin-contract/log.md &amp;&amp; grep -q 'wiki/log.md' .planning/spec/13-plugin-contract/log.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/spec/13-plugin-contract/log.md` exists
    - Frontmatter locks `port_verdict: rename`, `command: log`, `upstream_source: plugins/lattice-wiki/commands/log.md`
    - All six MANDATORY H2 sections from SP-02 are present
    - Shell-out contract section explicitly states "no script" (case-insensitive) — preserving upstream parity
    - The string `wiki/log.md` appears as the target file the prose instructs `grep + tail` against
    - Reshape notes section explicitly states "No behavior changes vs upstream" (or equivalent)
  </acceptance_criteria>
  <done>
    log.md exists, all six mandatory sections present, port_verdict=rename locked, no-script parity captured, automated grep gate passes.
  </done>
</task>

</tasks>

<verification>
- `ls .planning/spec/13-plugin-contract/` shows `query.md` and `log.md` alongside files from Plans 01 and 02
- Both files pass the per-task automated grep gates
- After all three per-command-spec plans (01, 02, 03) run, the directory contains exactly 6 per-command files: init.md, scan.md, ingest.md, lint.md, query.md, log.md
</verification>

<success_criteria>
Per-command spec files for `/graph-wiki:query` and `/graph-wiki:log` exist under `.planning/spec/13-plugin-contract/`, each conforming to the SP-02 mandatory template. The query spec captures the LLM-primary + BM25-fallback shape and the VP-01 wiki_search.py prerequisite. The log spec captures the no-script prose-only parity with upstream.
</success_criteria>

<output>
Create `.planning/phases/13-plugin-spec-m3a/13-03-SUMMARY.md` when done.
</output>
