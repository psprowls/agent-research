---
phase: 13-plugin-spec-m3a
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/spec/13-plugin-contract/CONTRACT-INDEX.md
  - .planning/spec/13-plugin-contract/SHELL-OUT-PATTERN.md
autonomous: true
requirements:
  - PLUGIN-01

must_haves:
  truths:
    - "CONTRACT-INDEX.md is the single auditable summary of the contract — Phase 14's executor can scan one file to know what to do"
    - "SHELL-OUT-PATTERN.md owns the cross-cutting decisions (SO-01..SO-04) so per-command files can reference it without repeating boilerplate"
    - "The 3 work-layer commands (archive, regen-index, status) are visible in CONTRACT-INDEX.md with verdict=drop and rationale, even though no per-command file ships for them"
  artifacts:
    - path: ".planning/spec/13-plugin-contract/CONTRACT-INDEX.md"
      provides: "Single-table summary of all 9 upstream commands with verdicts and per-command spec links"
      contains: "9 rows in the verdict table"
    - path: ".planning/spec/13-plugin-contract/SHELL-OUT-PATTERN.md"
      provides: "Cross-cutting decisions: SO-01 uv-run invocation, SO-02 shim shape, SO-03 [plugin] manifest block, SO-04 _config.py backend selector + agent/skill rename map"
      contains: "All four SO-01..SO-04 anchors"
  key_links:
    - from: ".planning/spec/13-plugin-contract/CONTRACT-INDEX.md"
      to: "per-command files init.md, scan.md, ingest.md, lint.md, query.md, log.md"
      via: "Each table row in CONTRACT-INDEX links to the per-command spec file"
      pattern: "\\[(init|scan|ingest|lint|query|log)\\.md\\]"
    - from: ".planning/spec/13-plugin-contract/SHELL-OUT-PATTERN.md"
      to: "every per-command file (via the §SO-XX references each per-command file makes)"
      via: "Cross-cutting decision anchors SO-01..SO-04"
      pattern: "SO-0[1-4]"
---

<objective>
Author the two cross-cutting docs that anchor the Phase 13 spec directory: `CONTRACT-INDEX.md` (the single-table verdict summary) and `SHELL-OUT-PATTERN.md` (the cross-cutting SO-01..SO-04 decisions plus the agent/skill rename map).

Purpose: Per CONTEXT.md SP-03 and SP-04, these two files exist so (a) Phase 14's executor can audit the whole contract by reading ONE file (CONTRACT-INDEX.md), and (b) the six per-command files do not repeat the same invocation-shape boilerplate (they reference SHELL-OUT-PATTERN.md SO-NN instead). Both verdicts and shell-out shape are already locked in CONTEXT.md — this plan transcribes them into the locked artifact form.

Output: Two markdown files under `.planning/spec/13-plugin-contract/`. CONTRACT-INDEX.md is a 9-row verdict table. SHELL-OUT-PATTERN.md is a four-section cross-cutting doc plus a rename-map section.

NOTE on wave assignment: this plan is in wave 1 (parallel-safe with Plans 01-03) because verdicts and shell-out shape are already locked in CONTEXT.md — no information dependency on the per-command files exists. File-set is disjoint from Plans 01-03 (different filenames).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-plugin-spec-m3a/13-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author CONTRACT-INDEX.md (single-table summary of all 9 commands)</name>
  <files>.planning/spec/13-plugin-contract/CONTRACT-INDEX.md</files>
  <read_first>
    - /Users/pat/Personal/agent-research/.planning/phases/13-plugin-spec-m3a/13-CONTEXT.md (SP-03 single auditable summary; the full 9-row verdict table in the decisions section; C-01..C-02 verdict definitions)
    - /Users/pat/Personal/lattice/plugins/lattice-wiki/commands/ (directory listing — confirm all 9 upstream commands)
  </read_first>
  <action>
    Create `.planning/spec/13-plugin-contract/CONTRACT-INDEX.md` with this structure:

    1. H1: `# graph-wiki Plugin Contract — Index`

    2. Short intro paragraph (3-5 sentences):
       - State purpose: this is the single auditable summary of the Phase 13 contract; Phase 14's executor reads this file to scan the whole contract.
       - State foundational reframe: the ported graph-wiki plugin runs on Claude Code inference (per P-01); it is NOT a wrapper around graph-wiki-agent.
       - Link to SHELL-OUT-PATTERN.md for cross-cutting invocation shape.

    3. H2 `## Verdict vocabulary` — transcribe C-02 verbatim:
       - `rename` = byte-for-byte text swap of upstream slash and module references
       - `reshape` = command behavior changes (e.g., /lint loses work-layer pass 1b)
       - `drop` = no port (no file ships in plugins/graph-wiki/commands/)
       - `defer` = port the markdown but disable execution until a later phase (unused in v1.2; documented for future phases)

    4. H2 `## Command verdict table` — one markdown table with EXACTLY 9 rows (one per upstream slash command). Columns:
       - `#`
       - `Upstream command`
       - `Verdict`
       - `Target script` (path under `plugins/graph-wiki/skills/graph-wiki/scripts/` or `—` for drop/log)
       - `Target Python module` (or `—`)
       - `Per-command spec` (markdown link to `init.md` / `scan.md` / etc., or `—` for drop)
       - `One-line rationale`

       Each table row must use the format `| N | ... |` with N being 1..9 — the verify gate counts rows by that pattern.

       Rows (in order, matching CONTEXT.md decisions table):
       1. `/lattice-wiki:init` → rename → init_vault.py → vault_io.init_vault.main (+ detect_containers pre-step) → [init.md](init.md) → "Direct mapping; detection sub-step preserved verbatim."
       2. `/lattice-wiki:scan` → rename → scan_monorepo.py → vault_io.scan_monorepo.main → [scan.md](scan.md) → "Direct mapping; clean-tree-on-main gate preserved."
       3. `/lattice-wiki:ingest` → rename → ingest_source.py → vault_io.ingest_source.main → [ingest.md](ingest.md) → "source-ingest only; work-item ingest dropped per C-01."
       4. `/lattice-wiki:lint` → reshape → lint_wiki.py → vault_io.lint_wiki.main (VP-01 prereq) + vault_io.graph_analyzer.main → [lint.md](lint.md) → "Mechanical pass 1 + semantic pass 2 preserved; work-lint pass 1b dropped per C-01."
       5. `/lattice-wiki:query` → rename → wiki_search.py (BM25 fallback only) → vault_io.wiki_search.main (VP-01 prereq) → [query.md](query.md) → "LLM-driven (Claude Code inference per P-01); only BM25 fallback shells out."
       6. `/lattice-wiki:log` → rename → — (no script) → — → [log.md](log.md) → "Prose-only; mirrors upstream which has no script."
       7. `/lattice-wiki:archive` → drop → — → — → — → "Work-layer out of v1.2 scope per C-01 and PROJECT.md."
       8. `/lattice-wiki:regen-index` → drop → — → — → — → "Work-layer out of v1.2 scope per C-01 and PROJECT.md."
       9. `/lattice-wiki:status` → drop → — → — → — → "Work-layer out of v1.2 scope per C-01 and PROJECT.md."

    5. H2 `## Resulting plugin command surface` — bullet list:
       - 6 ported commands under `plugins/graph-wiki/commands/`: init, scan, ingest, lint, query, log.
       - 3 dropped: no `.md` files for archive, regen-index, status — they do not appear in `/graph-wiki:` autocomplete.
       - Total: 6 files in `plugins/graph-wiki/commands/`.

    6. H2 `## Phase 14 prerequisite ports` — bullet list referencing VP-01..VP-04:
       - `vault_io.lint_wiki` — port ~508 LOC from `lattice_wiki_core/lint_wiki.py` (Phase 14 Plan 1) BEFORE the `/graph-wiki:lint` shim is written.
       - `vault_io.wiki_search` — port ~194 LOC from `lattice_wiki_core/wiki_search.py` (Phase 14 Plan 2) BEFORE the `/graph-wiki:query` BM25 fallback works.

    7. H2 `## See also` — bullets linking to SHELL-OUT-PATTERN.md (for invocation shape / SO-01..SO-04 / agent rename map) and to CONTEXT.md (the source-of-truth decisions doc at `.planning/phases/13-plugin-spec-m3a/13-CONTEXT.md`).
  </action>
  <verify>
    <automated>test -f .planning/spec/13-plugin-contract/CONTRACT-INDEX.md &amp;&amp; grep -q '^# graph-wiki Plugin Contract' .planning/spec/13-plugin-contract/CONTRACT-INDEX.md &amp;&amp; grep -q '^## Verdict vocabulary' .planning/spec/13-plugin-contract/CONTRACT-INDEX.md &amp;&amp; grep -q '^## Command verdict table' .planning/spec/13-plugin-contract/CONTRACT-INDEX.md &amp;&amp; [ "$(grep -cE '^\| [1-9] \|' .planning/spec/13-plugin-contract/CONTRACT-INDEX.md)" = "9" ] &amp;&amp; grep -q 'archive' .planning/spec/13-plugin-contract/CONTRACT-INDEX.md &amp;&amp; grep -q 'regen-index' .planning/spec/13-plugin-contract/CONTRACT-INDEX.md &amp;&amp; grep -q 'status' .planning/spec/13-plugin-contract/CONTRACT-INDEX.md &amp;&amp; grep -q 'init.md' .planning/spec/13-plugin-contract/CONTRACT-INDEX.md &amp;&amp; grep -q 'lint.md' .planning/spec/13-plugin-contract/CONTRACT-INDEX.md &amp;&amp; grep -q 'reshape' .planning/spec/13-plugin-contract/CONTRACT-INDEX.md &amp;&amp; grep -q 'VP-01' .planning/spec/13-plugin-contract/CONTRACT-INDEX.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/spec/13-plugin-contract/CONTRACT-INDEX.md` exists
    - H1 reads `# graph-wiki Plugin Contract — Index` (or the no-dash variant; the verify only requires the prefix)
    - The command verdict table contains exactly 9 rows (one per upstream slash command), enforced by `grep -cE '^\| [1-9] \|'` returning 9
    - All four verdict-vocab terms `rename`, `reshape`, `drop`, `defer` appear at least once
    - The three dropped commands (archive, regen-index, status) are explicitly listed in the table
    - Per-command spec links to init.md, scan.md, ingest.md, lint.md, query.md, log.md all present
    - VP-01 prerequisite referenced in the Phase 14 prerequisite ports section
  </acceptance_criteria>
  <done>
    CONTRACT-INDEX.md exists at the spec path with all 9 rows, all 4 verdict terms, all 6 per-command links, the 3 drop verdicts visible, and VP-01 prereq called out. Automated grep gate passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author SHELL-OUT-PATTERN.md (cross-cutting SO-01..SO-04 + rename map)</name>
  <files>.planning/spec/13-plugin-contract/SHELL-OUT-PATTERN.md</files>
  <read_first>
    - /Users/pat/Personal/agent-research/.planning/phases/13-plugin-spec-m3a/13-CONTEXT.md (SP-04 cross-cutting decisions home; SO-01..SO-04 full text; PD-01..PD-03 plugin discovery requirements; the "Agent / skill rename map (cross-cutting)" framing)
    - /Users/pat/Personal/lattice/plugins/lattice-wiki/skills/lattice-wiki/scripts/_config.py (the upstream backend selector — SO-04 retargets this verbatim with a new manifest read)
    - /Users/pat/Personal/lattice/plugins/lattice-wiki/skills/lattice-wiki/scripts/init_vault.py (canonical shim shape — SO-02 retargets this pattern for every ported script)
    - /Users/pat/Personal/lattice/plugins/lattice-wiki/agents/ (directory listing of upstream agents — ingestor, librarian, linter, scanner — for the rename map)
  </read_first>
  <action>
    Create `.planning/spec/13-plugin-contract/SHELL-OUT-PATTERN.md` with this structure:

    1. H1: `# graph-wiki Plugin — Shell-Out Pattern & Rename Map`

    2. Short intro paragraph (2-3 sentences): state that this file owns the cross-cutting decisions referenced by every per-command spec file via "see SHELL-OUT-PATTERN.md §SO-NN" anchors.

    3. H2 `## SO-01: uv run invocation with $AGENT_RESEARCH_ROOT` — transcribe SO-01 from CONTEXT.md:
       - Full literal invocation line: `uv run --project "$AGENT_RESEARCH_ROOT" python3 "${CLAUDE_PLUGIN_ROOT}/skills/graph-wiki/scripts/<x>.py" "$@"`
       - State user setup: user sets `AGENT_RESEARCH_ROOT` once in shell rc (e.g., `export AGENT_RESEARCH_ROOT=/Users/pat/Personal/agent-research`).
       - State auto-set: `$CLAUDE_PLUGIN_ROOT` is auto-set by Claude Code at slash-command invocation (PD-02).
       - State rationale: `uv run --project` resolves the agent-research venv and makes `vault_io` + `workspace_io` importable without manual venv activation. Single-user-setup-friendly.

    4. H2 `## SO-02: Shim file contents (the upstream pattern, retargeted)` — embed the SO-02 shim template from CONTEXT.md verbatim as a Python fenced code block. The two changes vs upstream are: (a) import from `vault_io.<module>` instead of `lattice_wiki_core.<module>`, (b) bedrock branch shells to `graph-wiki-agent` CLI instead of importing `lattice_wiki_agent`. Both changes called out beneath the code block.

    5. H2 `## SO-03: Backend selector config — [plugin] block in .graph-wiki.yaml` — transcribe SO-03:
       - YAML example block showing the `plugin:` section with `backend_default: claude` and `backend_overrides: { ingest: bedrock }`.
       - State that `workspace_io.manifest.read` is extended (Phase 14 task) to expose the `[plugin]` block.
       - State default-when-missing: `claude` everywhere.

    6. H2 `## SO-04: _config.py helper in plugin scripts dir` — transcribe SO-04:
       - State file path: `plugins/graph-wiki/skills/graph-wiki/scripts/_config.py`
       - State the function signature: `backend_for(cmd, repo=None) -> Literal["claude", "bedrock"]`
       - State it reads `.graph-wiki.yaml` via `workspace_io.manifest.read` and returns the resolved backend.
       - State this mirrors upstream's `_config.py` shape with the manifest-read rewired.

    7. H2 `## Plugin discovery requirements (PD-01..PD-03)` — bullet list:
       - PD-01: `$AGENT_RESEARCH_ROOT` env var is the only required user config; documented in `plugins/graph-wiki/README.md` (Phase 14 will author this).
       - PD-02: `$CLAUDE_PLUGIN_ROOT` is auto-set by Claude Code at slash-command invocation (no work needed).
       - PD-03: `uv` must be installed; documented in plugin README. No fallback to bare `python3`.

    8. H2 `## Agent / skill rename map (cross-cutting)` — table or bullets:
       - Skill directory rename: `plugins/lattice-wiki/skills/lattice-wiki/` → `plugins/graph-wiki/skills/graph-wiki/` (wholesale rename).
       - Skill files: `skills/lattice-wiki/SKILL.md` → `skills/graph-wiki/SKILL.md`; `skills/lattice-wiki/README.md` → `skills/graph-wiki/README.md`.
       - Agent files (names stay, namespace prose rebranded inside each file):
         - `agents/ingestor.md` — name stays; rebrand prose
         - `agents/librarian.md` — name stays; rebrand prose
         - `agents/linter.md` — name stays; rebrand prose
         - `agents/scanner.md` — name stays; rebrand prose
       - Skill reference docs: contents of `skills/lattice-wiki/references/` move to `skills/graph-wiki/references/` (full inventory — list each file by name once Phase 14 ports them; Phase 13 captures the move as a directory rename).
       - Plugin metadata: `.claude-plugin/plugin.json` plugin id renamed `lattice-wiki` → `graph-wiki`; namespace strings updated.
       - Slash command namespace: every `/lattice-wiki:<cmd>` becomes `/graph-wiki:<cmd>` across command files, agent prose, skill prose.

    9. H2 `## See also` — link to CONTRACT-INDEX.md and the per-command spec files.
  </action>
  <verify>
    <automated>test -f .planning/spec/13-plugin-contract/SHELL-OUT-PATTERN.md &amp;&amp; grep -q '^# graph-wiki Plugin' .planning/spec/13-plugin-contract/SHELL-OUT-PATTERN.md &amp;&amp; grep -q '^## SO-01' .planning/spec/13-plugin-contract/SHELL-OUT-PATTERN.md &amp;&amp; grep -q '^## SO-02' .planning/spec/13-plugin-contract/SHELL-OUT-PATTERN.md &amp;&amp; grep -q '^## SO-03' .planning/spec/13-plugin-contract/SHELL-OUT-PATTERN.md &amp;&amp; grep -q '^## SO-04' .planning/spec/13-plugin-contract/SHELL-OUT-PATTERN.md &amp;&amp; grep -q 'uv run --project' .planning/spec/13-plugin-contract/SHELL-OUT-PATTERN.md &amp;&amp; grep -q 'AGENT_RESEARCH_ROOT' .planning/spec/13-plugin-contract/SHELL-OUT-PATTERN.md &amp;&amp; grep -q 'CLAUDE_PLUGIN_ROOT' .planning/spec/13-plugin-contract/SHELL-OUT-PATTERN.md &amp;&amp; grep -q '.graph-wiki.yaml' .planning/spec/13-plugin-contract/SHELL-OUT-PATTERN.md &amp;&amp; grep -q 'backend_default' .planning/spec/13-plugin-contract/SHELL-OUT-PATTERN.md &amp;&amp; grep -q 'backend_for' .planning/spec/13-plugin-contract/SHELL-OUT-PATTERN.md &amp;&amp; grep -q 'librarian' .planning/spec/13-plugin-contract/SHELL-OUT-PATTERN.md &amp;&amp; grep -q 'scanner' .planning/spec/13-plugin-contract/SHELL-OUT-PATTERN.md &amp;&amp; grep -q 'ingestor' .planning/spec/13-plugin-contract/SHELL-OUT-PATTERN.md &amp;&amp; grep -q 'linter' .planning/spec/13-plugin-contract/SHELL-OUT-PATTERN.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/spec/13-plugin-contract/SHELL-OUT-PATTERN.md` exists
    - All four anchors `## SO-01`, `## SO-02`, `## SO-03`, `## SO-04` present as H2 sections
    - Literal strings `uv run --project`, `AGENT_RESEARCH_ROOT`, `CLAUDE_PLUGIN_ROOT`, `.graph-wiki.yaml`, `backend_default`, `backend_for` all present
    - All four upstream agent names (ingestor, librarian, linter, scanner) present in the rename map
    - SO-02 section includes a Python fenced code block showing the shim shape
  </acceptance_criteria>
  <done>
    SHELL-OUT-PATTERN.md exists at the spec path; all SO-01..SO-04 sections present; literal invocation line, env vars, manifest filename, and config function signature all present; agent rename map complete with all 4 agents listed; automated grep gate passes.
  </done>
</task>

</tasks>

<verification>
- `ls .planning/spec/13-plugin-contract/` shows `CONTRACT-INDEX.md` and `SHELL-OUT-PATTERN.md` alongside the 6 per-command files from Plans 01-03
- CONTRACT-INDEX.md's command verdict table has exactly 9 rows
- SHELL-OUT-PATTERN.md has all four `## SO-0[1-4]` anchor sections
- The pattern strings `SO-01`/`SO-02`/`SO-03`/`SO-04` referenced from per-command files (Plans 01-03) all resolve to anchors in this file
</verification>

<success_criteria>
The two cross-cutting docs for `.planning/spec/13-plugin-contract/` exist and lock the contract surface at the spec-directory level. CONTRACT-INDEX.md gives Phase 14's executor a single-file audit view. SHELL-OUT-PATTERN.md houses SO-01..SO-04 plus the agent/skill rename map. Together with the six per-command files (Plans 01-03), the spec directory is complete except for the PROJECT.md / REQUIREMENTS.md updates (Plan 05).
</success_criteria>

<output>
Create `.planning/phases/13-plugin-spec-m3a/13-04-SUMMARY.md` when done.
</output>
