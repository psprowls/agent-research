---
phase: 13-plugin-spec-m3a
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/spec/13-plugin-contract/ingest.md
  - .planning/spec/13-plugin-contract/lint.md
autonomous: true
requirements:
  - PLUGIN-01

must_haves:
  truths:
    - "A reader of .planning/spec/13-plugin-contract/ingest.md can produce the /graph-wiki:ingest plugin shim without consulting any other source"
    - "A reader of .planning/spec/13-plugin-contract/lint.md can produce the /graph-wiki:lint plugin shim without consulting any other source"
    - "ingest.md locks the source-only scope (work-item ingest dropped per C-01) and lint.md locks the dropped work-layer pass 1b"
  artifacts:
    - path: ".planning/spec/13-plugin-contract/ingest.md"
      provides: "Per-command port spec for /graph-wiki:ingest (rename verdict, source-only)"
      contains: "port_verdict: rename"
    - path: ".planning/spec/13-plugin-contract/lint.md"
      provides: "Per-command port spec for /graph-wiki:lint (reshape verdict — drops work-layer pass 1b)"
      contains: "port_verdict: reshape"
  key_links:
    - from: ".planning/spec/13-plugin-contract/ingest.md"
      to: "packages/vault-io/src/vault_io/ingest_source.py"
      via: "Shell-out contract names the target Python module"
      pattern: "vault_io\\.ingest_source"
    - from: ".planning/spec/13-plugin-contract/lint.md"
      to: "packages/vault-io (lint_wiki.py port required as Phase 14 Plan 1) + vault_io.graph_analyzer.main"
      via: "Shell-out contract names target modules and the VP-01 prerequisite"
      pattern: "vault_io\\.(lint_wiki|graph_analyzer)"
---

<objective>
Author the per-command port specs for `/graph-wiki:ingest` (source-only) and `/graph-wiki:lint` (reshape — drops work-layer pass 1b).

Purpose: These two commands are where the verdict vocabulary actually does work. `ingest` is `rename` but scope-narrowed (no work-item branch); `lint` is the one explicit `reshape` of the v1.2 port (mechanical pass 1 + semantic pass 2 preserved, work-layer pass 1b dropped). The spec must capture both the locked target module and the explicit behavior-change rationale per SP-02.

Output: Two markdown files under `.planning/spec/13-plugin-contract/`, each conforming to the SP-02 mandatory template. lint.md's Reshape notes section is non-trivial; ingest.md's covers the source-only narrowing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-plugin-spec-m3a/13-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author ingest.md (per-command spec for /graph-wiki:ingest)</name>
  <files>.planning/spec/13-plugin-contract/ingest.md</files>
  <read_first>
    - /Users/pat/Personal/deep-agents/.planning/phases/13-plugin-spec-m3a/13-CONTEXT.md (SP-02 template, row #3 in verdict table — source-only scope, work-item ingest dropped per C-01)
    - /Users/pat/Personal/lattice/plugins/lattice-wiki/commands/ingest.md (69 lines — upstream prose; identify any work-item branches that must be stripped during port)
    - /Users/pat/Personal/lattice/plugins/lattice-wiki/skills/lattice-wiki/scripts/ingest_source.py (upstream shim being retargeted)
    - /Users/pat/Personal/deep-agents/packages/vault-io/src/vault_io/ingest_source.py (target module — `vault_io.ingest_source.main`)
  </read_first>
  <action>
    Create `.planning/spec/13-plugin-contract/ingest.md` with these concrete values:

    1. Frontmatter:
       - `command: ingest`
       - `upstream_source: plugins/lattice-wiki/commands/ingest.md`
       - `port_verdict: rename`

    2. H1: `# /graph-wiki:ingest — Port Spec`

    3. `## Shell-out contract`:
       - Invocation: `uv run --project "$DEEP_AGENTS_ROOT" python3 "${CLAUDE_PLUGIN_ROOT}/skills/graph-wiki/scripts/ingest_source.py" $ARGUMENTS`
       - Target module (claude): `vault_io.ingest_source.main`
       - Target subprocess (bedrock): `graph-wiki-agent ingest source <args>` (note: explicitly `ingest source`, NOT `ingest work-item`)
       - Args pass-through: enumerate upstream ingest.md source-only flags (read upstream for exact set)
       - Pre-step: NONE
       - Scope note: source-ingest only. Upstream's work-item ingest branch is DROPPED per C-01 (work-layer out of v1.2 scope).

    4. `## Prose-preservation map`: walk every H2 in upstream `ingest.md`. For any section discussing work-item ingest, the verdict is "DROP — section omitted from the ported command body (work-layer out of v1.2 per C-01)". All other sections: "verbatim except namespace rename".

    5. `## Agent / skill rename map`:
       - `agents/ingestor.md` — name stays, namespace prose rebranded
       - `skills/lattice-wiki/SKILL.md` → `skills/graph-wiki/SKILL.md`
       - Script: `skills/lattice-wiki/scripts/ingest_source.py` → `skills/graph-wiki/scripts/ingest_source.py`
       - NOTE: upstream `ingest_work_item.py` script is NOT ported (work-layer dropped); the file simply does not appear under `plugins/graph-wiki/skills/graph-wiki/scripts/`.

    6. `## Reshape notes`: even though port_verdict is `rename`, briefly explain the scope narrowing in one or two lines: "Verdict is `rename` because the source-ingest behavior is byte-for-byte preserved. The companion work-item ingest path is `drop`-verdict per C-01 and does not ship in graph-wiki — captured by absence of the `ingest_work_item.py` script and the dropped prose sections above."

    7. `## Verification gate`: e.g., "Run `/graph-wiki:ingest <source-file>` against a deep-agents source file; resulting wiki page diff against a baseline matches upstream `/lattice-wiki:ingest <same-file>` modulo brand strings. Negative test: any --work-item flag (if upstream supports one) must return a clear 'not supported in graph-wiki v1.2' error or simply be absent from the CLI."

    Reference SHELL-OUT-PATTERN.md §SO-01.
  </action>
  <verify>
    <automated>test -f .planning/spec/13-plugin-contract/ingest.md &amp;&amp; grep -q '^port_verdict: rename' .planning/spec/13-plugin-contract/ingest.md &amp;&amp; grep -q '^## Shell-out contract' .planning/spec/13-plugin-contract/ingest.md &amp;&amp; grep -q '^## Prose-preservation map' .planning/spec/13-plugin-contract/ingest.md &amp;&amp; grep -q '^## Agent / skill rename map' .planning/spec/13-plugin-contract/ingest.md &amp;&amp; grep -q '^## Reshape notes' .planning/spec/13-plugin-contract/ingest.md &amp;&amp; grep -q '^## Verification gate' .planning/spec/13-plugin-contract/ingest.md &amp;&amp; grep -q 'vault_io.ingest_source.main' .planning/spec/13-plugin-contract/ingest.md &amp;&amp; grep -iq 'work-item' .planning/spec/13-plugin-contract/ingest.md &amp;&amp; grep -q 'C-01' .planning/spec/13-plugin-contract/ingest.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/spec/13-plugin-contract/ingest.md` exists
    - Frontmatter locks `port_verdict: rename`, `command: ingest`, `upstream_source: plugins/lattice-wiki/commands/ingest.md`
    - All six MANDATORY H2 sections from SP-02 are present
    - Shell-out contract names `vault_io.ingest_source.main` and explicitly the bedrock target `graph-wiki-agent ingest source` (not `work-item`)
    - The string "work-item" appears in the file (in the drop-callout context)
    - The C-01 decision is referenced by ID (so the dropped scope traces back to CONTEXT.md)
  </acceptance_criteria>
  <done>
    ingest.md exists, all six mandatory sections present, frontmatter locks port_verdict=rename, source-only scope captured, work-item ingest explicitly excluded with C-01 reference, automated grep gate passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author lint.md (per-command spec for /graph-wiki:lint, reshape verdict)</name>
  <files>.planning/spec/13-plugin-contract/lint.md</files>
  <read_first>
    - /Users/pat/Personal/deep-agents/.planning/phases/13-plugin-spec-m3a/13-CONTEXT.md (SP-02 template, row #4 in verdict table — pass 1 + pass 2 preserved, pass 1b dropped; VP-01 prerequisite lint_wiki.py port)
    - /Users/pat/Personal/lattice/plugins/lattice-wiki/commands/lint.md (67 lines — upstream prose; identify pass 1, pass 1b work-layer, pass 2 sections explicitly)
    - /Users/pat/Personal/lattice/plugins/lattice-wiki/skills/lattice-wiki/scripts/lint_wiki.py (upstream shim being retargeted)
    - /Users/pat/Personal/deep-agents/packages/vault-io/src/vault_io/lint/ (existing lint rule modules — mechanical pass 1 already lives in vault-io)
    - /Users/pat/Personal/deep-agents/packages/vault-io/src/vault_io/graph_analyzer.py (semantic pass 2 target module)
  </read_first>
  <action>
    Create `.planning/spec/13-plugin-contract/lint.md` with these concrete values:

    1. Frontmatter:
       - `command: lint`
       - `upstream_source: plugins/lattice-wiki/commands/lint.md`
       - `port_verdict: reshape`

    2. H1: `# /graph-wiki:lint — Port Spec`

    3. `## Shell-out contract`:
       - Invocation: `uv run --project "$DEEP_AGENTS_ROOT" python3 "${CLAUDE_PLUGIN_ROOT}/skills/graph-wiki/scripts/lint_wiki.py" $ARGUMENTS`
       - Target module (claude, mechanical pass 1 + semantic pass 2): `vault_io.lint_wiki.main` — NOTE: this module must be ported into vault-io as Phase 14 Plan 1 (VP-01 prerequisite); does not exist today
       - Companion module (semantic pass 2): `vault_io.graph_analyzer.main`
       - Target subprocess (bedrock): `graph-wiki-agent lint <args>`
       - Args pass-through: enumerate upstream lint.md flags 1:1 except any pass-1b/work-layer flag is omitted
       - Pre-step: NONE (lint_wiki module orchestrates both passes internally)
       - VP-01 callout: "Prerequisite — `lint_wiki.py` (~508 LOC) must be ported from `lattice_wiki_core` to `vault_io` as Phase 14 Plan 1 before this shim can dispatch."

    4. `## Prose-preservation map`: walk every H2/H3 in upstream `lint.md`. For each:
       - Pass 1 (mechanical) sections: "verbatim except namespace rename"
       - Pass 1b (work-layer / work-item lint) sections: "DROP — section omitted from ported command body (work-layer out of v1.2 per C-01)"
       - Pass 2 (semantic / graph) sections: "verbatim except namespace rename"
       - Other sections: per-section verdict

    5. `## Agent / skill rename map`:
       - `agents/linter.md` — name stays, namespace prose rebranded
       - `skills/lattice-wiki/SKILL.md` → `skills/graph-wiki/SKILL.md`
       - Script: `skills/lattice-wiki/scripts/lint_wiki.py` → `skills/graph-wiki/scripts/lint_wiki.py`

    6. `## Reshape notes` — this is the load-bearing section for this command. State concretely:
       - Behavior change #1: work-layer lint (upstream pass 1b) is removed from the ported command. Rationale: work/ subsystem out of v1.2 scope per C-01.
       - Behavior change #2: prerequisite — `vault_io.lint_wiki` does not exist in this repo as of Phase 13; Phase 14 Plan 1 ports it from `lattice_wiki_core/lint_wiki.py` (~508 LOC) following the VP-01..VP-04 contract (verbatim port with brand grep gate).
       - No other behavior changes: mechanical pass 1 (existing `vault_io.lint/` rule modules) and semantic pass 2 (`vault_io.graph_analyzer`) preserved.

    7. `## Verification gate`: e.g., "Run `/graph-wiki:lint` against a known-dirty wiki page; mechanical pass 1 issues match upstream output (modulo brand strings), semantic pass 2 issues match upstream output. Negative: any work-item lint flag (if upstream supported one) must be absent."

    Reference SHELL-OUT-PATTERN.md §SO-01 and CONTEXT.md §VP-01..VP-04 for the prerequisite framing.
  </action>
  <verify>
    <automated>test -f .planning/spec/13-plugin-contract/lint.md &amp;&amp; grep -q '^port_verdict: reshape' .planning/spec/13-plugin-contract/lint.md &amp;&amp; grep -q '^## Shell-out contract' .planning/spec/13-plugin-contract/lint.md &amp;&amp; grep -q '^## Prose-preservation map' .planning/spec/13-plugin-contract/lint.md &amp;&amp; grep -q '^## Agent / skill rename map' .planning/spec/13-plugin-contract/lint.md &amp;&amp; grep -q '^## Reshape notes' .planning/spec/13-plugin-contract/lint.md &amp;&amp; grep -q '^## Verification gate' .planning/spec/13-plugin-contract/lint.md &amp;&amp; grep -q 'vault_io.lint_wiki' .planning/spec/13-plugin-contract/lint.md &amp;&amp; grep -q 'vault_io.graph_analyzer' .planning/spec/13-plugin-contract/lint.md &amp;&amp; grep -q 'VP-01' .planning/spec/13-plugin-contract/lint.md &amp;&amp; grep -iq 'work-layer\|pass 1b' .planning/spec/13-plugin-contract/lint.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/spec/13-plugin-contract/lint.md` exists
    - Frontmatter locks `port_verdict: reshape` (the only `reshape` verdict in v1.2)
    - All six MANDATORY H2 sections from SP-02 are present
    - Shell-out contract names both `vault_io.lint_wiki.main` and `vault_io.graph_analyzer.main`
    - VP-01 prerequisite referenced explicitly (so Phase 14 executor knows to port lint_wiki.py BEFORE writing the shim)
    - Reshape notes section explicitly identifies "work-layer pass 1b dropped" or equivalent and gives the C-01 rationale
    - Prose-preservation map distinguishes pass 1 / pass 1b / pass 2 sections (drop verdict applied to pass 1b)
  </acceptance_criteria>
  <done>
    lint.md exists, all six mandatory sections present, port_verdict=reshape locked, both target modules named, VP-01 prerequisite called out, work-layer pass 1b drop documented with C-01 rationale, automated grep gate passes.
  </done>
</task>

</tasks>

<verification>
- `ls .planning/spec/13-plugin-contract/` shows `ingest.md` and `lint.md` alongside whatever Plan 01 created
- Both files pass the per-task automated grep gates
- `ingest.md` frontmatter is `port_verdict: rename`; `lint.md` frontmatter is `port_verdict: reshape`
- VP-01 prerequisite appears in `lint.md` Reshape notes section
</verification>

<success_criteria>
Per-command spec files for `/graph-wiki:ingest` and `/graph-wiki:lint` exist under `.planning/spec/13-plugin-contract/`, each conforming to the SP-02 mandatory template. The two non-trivial v1.2 decisions — source-only ingest scope, lint's dropped work-layer pass 1b — are explicitly captured with C-01 / VP-01 traceability so Phase 14 executes without revisiting them.
</success_criteria>

<output>
Create `.planning/phases/13-plugin-spec-m3a/13-02-SUMMARY.md` when done.
</output>
