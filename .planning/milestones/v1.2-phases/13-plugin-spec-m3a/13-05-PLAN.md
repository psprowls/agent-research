---
phase: 13-plugin-spec-m3a
plan: 05
type: execute
wave: 2
depends_on:
  - 13-01
  - 13-02
  - 13-03
  - 13-04
files_modified:
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - PLUGIN-01

must_haves:
  truths:
    - "PROJECT.md Key Decisions log contains a new entry locking the Phase 13 contract surface and linking to .planning/spec/13-plugin-contract/CONTRACT-INDEX.md (satisfies Phase 13 SC#3)"
    - "REQUIREMENTS.md PLUGIN-01 line has an updated note stating that lint_wiki.py + wiki_search.py must be ported into vault-io as a prerequisite step inside Phase 14"
    - "Phase 14's load_prior_context can read the PROJECT.md entry and find every needed Phase 13 artifact via the CONTRACT-INDEX.md link"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Key Decisions entry locking the plugin contract surface (SP-05)"
      contains: "13-plugin-contract"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated PLUGIN-01 note flagging the VP-01 prerequisite ports for Phase 14"
      contains: "lint_wiki"
  key_links:
    - from: ".planning/PROJECT.md"
      to: ".planning/spec/13-plugin-contract/CONTRACT-INDEX.md"
      via: "Key Decisions entry text contains the relative path"
      pattern: "13-plugin-contract/CONTRACT-INDEX"
    - from: ".planning/REQUIREMENTS.md"
      to: "Phase 14 prerequisite (lint_wiki + wiki_search ports)"
      via: "PLUGIN-01 line note mentions both modules by name"
      pattern: "lint_wiki|wiki_search"
---

<objective>
Land the two non-spec-directory artifacts that Phase 13 owes: (1) the PROJECT.md Key Decisions entry that LOCKS the plugin contract surface (Phase 13 Success Criterion #3), and (2) the REQUIREMENTS.md note attached to PLUGIN-01 flagging the VP-01 prerequisite ports for Phase 14.

Purpose: PROJECT.md Key Decisions is the durable lock — Phase 14 reads it as part of `load_prior_context` and the contract survives any future re-planning. The REQUIREMENTS.md update ensures any future read of the requirements file sees that PLUGIN-01 has been spec-completed AND that the two prerequisite modules (lint_wiki, wiki_search) are Phase 14 work.

Output: PROJECT.md gets one new Key Decisions row; REQUIREMENTS.md gets one updated note under PLUGIN-01.

Wave assignment: wave 2 because the PROJECT.md entry text references the path `.planning/spec/13-plugin-contract/CONTRACT-INDEX.md` and ideally the spec dir is in place when the entry lands (cleaner commit, no dangling reference). Both writes are pure markdown edits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-plugin-spec-m3a/13-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append Key Decisions entry to PROJECT.md locking the plugin contract surface</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - /Users/pat/Personal/agent-research/.planning/PROJECT.md (the existing Key Decisions section — find the right insertion point and mirror the existing entry format used by Phase 11 / Phase 12)
    - /Users/pat/Personal/agent-research/.planning/phases/13-plugin-spec-m3a/13-CONTEXT.md (SP-05 — defines the lock requirement and what the entry should contain; the foundational reframe paragraph at the top of the domain section is the load-bearing context)
    - /Users/pat/Personal/agent-research/.planning/spec/13-plugin-contract/CONTRACT-INDEX.md (the file the entry must link to — produced by Plan 04)
    - /Users/pat/Personal/agent-research/.planning/ROADMAP.md (Phase 13 §Success Criteria #3 — "contract surface locked in PROJECT.md Key Decisions")
  </read_first>
  <action>
    Open `.planning/PROJECT.md`. Locate the Key Decisions section (or its equivalent named section — could be "Validated Decisions" or "Locked Decisions"; check the existing file structure).

    Identify the chronological/phase-ordered position for a new Phase 13 entry — after the Phase 12 entries, before any later content.

    Add a new entry in the existing format. Required content (paraphrase to match the existing entry style — do not invent a new format):

    - One-line summary: "Phase 13 (M3a) — graph-wiki plugin contract surface locked"
    - One-paragraph body covering:
      - The foundational reframe: ported graph-wiki plugin runs on Claude Code inference (per P-01); it is NOT a wrapper around graph-wiki-agent. graph-wiki-agent stays as the separate Bedrock-backed surface.
      - Verdicts: 6 commands rename/reshape (init, scan, ingest, lint, query, log) + 3 dropped (archive, regen-index, status — work-layer out of v1.2 per C-01).
      - The single shell-out pattern: `uv run --project "$DEEP_AGENTS_ROOT" python3 ...` (SO-01) with the `[plugin]` backend selector block in `.graph-wiki.yaml` (SO-03).
      - The two VP-01 prerequisite ports for Phase 14: `lint_wiki.py` and `wiki_search.py` must be ported into vault-io as Phase 14's first two plans before the corresponding shims can shell out.
    - One link line pointing at the source-of-truth files:
      - `.planning/spec/13-plugin-contract/CONTRACT-INDEX.md` (the audit summary)
      - `.planning/spec/13-plugin-contract/SHELL-OUT-PATTERN.md` (the cross-cutting decisions)

    Do NOT add a new section header. Do NOT rename existing sections. Append the entry inside the existing Key Decisions list using the existing bullet/numbered format you observe when reading the file.
  </action>
  <verify>
    <automated>grep -q '13-plugin-contract' .planning/PROJECT.md &amp;&amp; grep -q 'CONTRACT-INDEX' .planning/PROJECT.md &amp;&amp; grep -iq 'phase 13\|m3a\|plugin contract' .planning/PROJECT.md &amp;&amp; grep -iq 'claude code inference\|p-01\|claude.*inference' .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/PROJECT.md` contains the relative path string `13-plugin-contract` (anchoring the link to the spec dir)
    - `.planning/PROJECT.md` contains the string `CONTRACT-INDEX` (link to the audit summary)
    - The new entry mentions Phase 13 / M3a / plugin contract by name (case-insensitive)
    - The entry references the Claude Code inference reframe (P-01) — the load-bearing fact that distinguishes graph-wiki from graph-wiki-agent
    - No existing PROJECT.md sections renamed or deleted (use `git diff .planning/PROJECT.md` to visually confirm only an addition)
  </acceptance_criteria>
  <done>
    PROJECT.md has a new Key Decisions entry locking the plugin contract surface, linking to CONTRACT-INDEX.md and SHELL-OUT-PATTERN.md, and mentioning the P-01 inference reframe. Automated grep gate passes. Phase 13 SC#3 satisfied.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add VP-01 prerequisite note to REQUIREMENTS.md PLUGIN-01 entry</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - /Users/pat/Personal/agent-research/.planning/REQUIREMENTS.md (find the existing PLUGIN-01 bullet — line ~36 today reads: "- [ ] **PLUGIN-01**: Spec phase completed answering: *what do the upstream `lattice-wiki` plugin slash commands actually shell out to?* ... and contract surface locked before code is moved.")
    - /Users/pat/Personal/agent-research/.planning/phases/13-plugin-spec-m3a/13-CONTEXT.md (VP-01..VP-04 — defines the prerequisite ports framing)
  </read_first>
  <action>
    Open `.planning/REQUIREMENTS.md`. Locate the PLUGIN-01 bullet under the "Plugin Port (M3)" subsection.

    Append a one-line note to the existing PLUGIN-01 bullet body (or as a nested sub-bullet directly beneath it, matching the file's existing style). The note must state:

    "Phase 14 prerequisite: `vault_io.lint_wiki` (port from `lattice_wiki_core/lint_wiki.py`, ~508 LOC) and `vault_io.wiki_search` (port from `lattice_wiki_core/wiki_search.py`, ~194 LOC) must be ported into `packages/vault-io/` as Phase 14 Plan 1 and Plan 2 respectively, before the `/graph-wiki:lint` and `/graph-wiki:query` shims can shell out. See `.planning/spec/13-plugin-contract/CONTRACT-INDEX.md` (§Phase 14 prerequisite ports)."

    Do NOT mark PLUGIN-01 as checked yet — that is the orchestrator's call after Phase 13 close. Do NOT touch any other requirement line.

    If the existing file format uses a single-line bullet without sub-bullets, append the prerequisite note as a parenthetical or as a follow-up sentence on the same line. If it uses nested bullets elsewhere in the file, use a nested bullet here for consistency. Match local style.
  </action>
  <verify>
    <automated>grep -q 'lint_wiki' .planning/REQUIREMENTS.md &amp;&amp; grep -q 'wiki_search' .planning/REQUIREMENTS.md &amp;&amp; grep -q 'PLUGIN-01' .planning/REQUIREMENTS.md &amp;&amp; grep -B1 -A3 'PLUGIN-01' .planning/REQUIREMENTS.md | grep -iq 'phase 14\|prerequisite\|prereq'</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` contains the string `lint_wiki` (was absent before this plan — verify against pre-edit state)
    - `.planning/REQUIREMENTS.md` contains the string `wiki_search` (was absent before this plan)
    - The new note is co-located with the PLUGIN-01 bullet (the `grep -B1 -A3 'PLUGIN-01'` window contains the Phase 14 prerequisite framing)
    - PLUGIN-01 line is NOT marked checked (still `[ ]`) — Phase 13 orchestrator handles checkbox state at phase close
    - No other requirement lines modified (visually confirm via `git diff .planning/REQUIREMENTS.md`)
  </acceptance_criteria>
  <done>
    REQUIREMENTS.md has the VP-01 prerequisite note attached to PLUGIN-01, naming both `vault_io.lint_wiki` and `vault_io.wiki_search` as Phase 14 prerequisite ports and linking back to CONTRACT-INDEX.md. Automated grep gate passes.
  </done>
</task>

</tasks>

<verification>
- `git diff .planning/PROJECT.md` shows exactly one new Key Decisions entry, no other changes
- `git diff .planning/REQUIREMENTS.md` shows exactly one addition co-located with the PLUGIN-01 bullet, no other changes
- Both automated grep gates pass
- After all five Phase 13 plans (01-05) ship, the full deliverable list in CONTEXT.md §domain is satisfied:
  - `.planning/spec/13-plugin-contract/` directory with 6 per-command files + CONTRACT-INDEX.md + SHELL-OUT-PATTERN.md = 8 files total
  - PROJECT.md Key Decisions entry (SP-05) locking the surface
  - REQUIREMENTS.md note flagging VP-01 prereq for Phase 14
</verification>

<success_criteria>
Phase 13 SC#3 ("contract surface locked in PROJECT.md Key Decisions") satisfied via the new PROJECT.md entry. REQUIREMENTS.md updated so PLUGIN-01 carries the explicit VP-01 prerequisite for Phase 14. Both edits surgical — no other content touched.
</success_criteria>

<output>
Create `.planning/phases/13-plugin-spec-m3a/13-05-SUMMARY.md` when done.
</output>
