---
phase: 57-index-generation-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/graph-io/src/graph_io/queries.py
  - packages/graph-io/tests/test_queries.py
  - packages/wiki-io/src/wiki_io/index_generator.py
  - packages/wiki-io/tests/test_index_generator.py
autonomous: true
requirements: [IDX-01, IDX-02, IDX-03, IDX-04, IDX-05]

must_haves:
  truths:
    - "EXECUTION GATE: this plan must be executed AFTER Phase 56 (entity `summary:` frontmatter) lands. The code is correct regardless (summary reads degrade to empty), but IDX-03's rendered inline summaries and the live-graph syrupy snapshot are only meaningful once entity pages carry `summary:`. Phase 55 (depends_on_package edge + describe_package internal-deps) is already landed."
    - "IDX-01 / D-03: `BY_KIND_ORDER` becomes `('app','package','plugin')` — apps first, `test_suite`/`dependency` removed; `KIND_LABELS` gains `app: 'Apps'`. The flat By-Kind section renders only app/package/plugin groups."
    - "IDX-01 / D-03 / D-04: apps are placed by `_place_entities` via `list_apps` and routed by the SAME single-vs-multi-domain rule as packages (single qualifying domain → that domain section; zero/multi → By-Kind `### Apps`). `_compute_qualifying_domains` handles `kind='app'` (direct `belongs_to_domain`, identical to `package`) instead of raising."
    - "CRUX (D-01): placement kinds are DECOUPLED from render order. A new `_PLACEABLE_KINDS = ('app','package','test_suite','dependency','plugin')` drives the `_place_entities` iteration AND the `by_kind` sort key; `BY_KIND_ORDER` (app/package/plugin) drives ONLY the flat By-Kind render groups. If placement kept iterating `BY_KIND_ORDER`, test_suites/dependencies would never be discovered and could not nest — breaking IDX-04/05."
    - "IDX-02 / D-05: `_entity_wikilink` returns `[[wiki/entities/{stem}|{entity.name}]]` — piped, display text = `entity.name`. Stem derivation via `_short_filename` is unchanged (kind-aware, collision-aware)."
    - "IDX-03 / D-06 / D-07: `PlacedEntity` gains `summary: str = ''`, populated in `_place_entities` by reading the entity page file `wiki_root/entities/<stem>.md` frontmatter `summary:` (NOT the graph node attr — Phase 56 makes `summary:` human-editable). Stem uses the SAME `_short_filename(uri, collision_set, suite_kind=, pkg_for_suite=)` derivation as `_entity_wikilink` so file lookup agrees with the link. Missing dir/file/frontmatter → empty summary, no crash (tolerant reads like `_scan_curated_lane`). Every entity entry renders `- {link} — {summary}` (empty summary → no ` — …` suffix), matching `_render_curated_section`."
    - "IDX-03 plumbing: `_place_entities` gains `wiki_root: Path` and `collision_set: frozenset[str]` parameters; `_render` (which already computes both) passes them in. No other caller exists."
    - "IDX-04 / D-01 / D-02: test_suites nest under the package(s) they test in BOTH domain sections (existing) AND the By-Kind section (new). Flat `### Test Suites` By-Kind group is gone. `parent_pkg_names` is now populated for by-kind-placed test_suites too (domain-agnostic consumer/tested-package names), so `_render_by_kind` can nest them under their package/app bullet exactly like `_render_domain_section`."
    - "IDX-05 / D-08 / D-09: dependencies nest under the package(s) that use them in BOTH domain and By-Kind sections via `used_by` (`parent_pkg_names`). Flat `### Dependencies` By-Kind group is gone. UNDER EACH package/app, TWO separate nested sub-lists render: (1) `Dependencies` — external `used_by` deps → dependency entity links; (2) `Internal dependencies` — workspace package→package deps via `depends_on_package` → links to the internal PACKAGE/APP entity page. The two sub-lists are NEVER merged and the internal sub-list is NEVER dropped."
    - "IDX-05 / D-11: the `depends_on_package` source is reused from graph-io, NOT re-implemented as parallel SQL in wiki-io. Since `describe_package` is `kind='package'`-only and returns `None` for apps, a small graph-io query `internal_dependencies_of(conn, *, name)` is added (runs the same L437-447 outgoing query for `src.kind IN ('package','app')`) and reused by wiki-io for both packages and apps. Internal-dep names are resolved to wikilinks via a `name→PlacedEntity` index built for package+app kinds during placement; an internal-dep name with no matching entity is skipped defensively."
    - "D-10: duplication is expected and correct — a test_suite testing N packages appears nested under each; a dependency used by N packages appears under each; an internal dep depended-on by N packages appears under each."
    - "Determinism preserved: all nested sub-lists sort alphabetically by entity name/uri so permuted graph insertion order still yields byte-identical output (`test_determinism_across_permutations` stays green). Empty-section omission (D-08) is preserved: a sub-list with zero entries emits no sub-heading."
    - "Self-documenting code (Pat's value, carried 54/55/56): comment WHY By-Kind packages now nest (D-01 cross-cutting rationale — flat-removal safety) and WHY summary is read from the page file not the graph attr (D-06 / Phase 56 fill-when-empty)."
    - "All IDX-02 test fallout is fixed: every bare-stem `[[wiki/entities/<stem>]]` assertion → piped `[[wiki/entities/<stem>|<name>]]`; flat `### Test Suites` / `### Dependencies` assertions → nested form; `BY_KIND_ORDER` / `KIND_LABELS` constant assertions updated; the syrupy snapshot regenerated."
  artifacts:
    - path: packages/graph-io/src/graph_io/queries.py
      provides: "internal_dependencies_of(conn, *, name) — outgoing depends_on_package dst-names for a package OR app node, reusing the Phase 55 query shape"
      contains: "depends_on_package"
    - path: packages/graph-io/tests/test_queries.py
      provides: "test for internal_dependencies_of covering package and app source, and empty case"
      contains: "internal_dependencies_of"
    - path: packages/wiki-io/src/wiki_io/index_generator.py
      provides: "app By-Kind section, piped entity links, inline summaries, nested test-suites/deps + internal-deps sub-list under packages/apps in both domain and By-Kind contexts"
      contains: "Internal dependencies"
    - path: packages/wiki-io/tests/test_index_generator.py
      provides: "updated constant/order/piped-link assertions; new app-section, internal-deps, and inline-summary tests; regenerated snapshot"
      contains: "Internal dependencies"
  key_links: []
---

<objective>
Make `wiki/index.md` a genuinely readable projection of the graph by applying five locked changes
(IDX-01..IDX-05) to `packages/wiki-io/src/wiki_io/index_generator.py`, plus one small reused
graph-io query for the internal-dependency source:

1. **IDX-01** — distinct `app` By-Kind section, apps first (`BY_KIND_ORDER = app, package, plugin`); apps follow the same single-vs-multi-domain routing as packages.
2. **IDX-02** — piped human-readable entity links `[[wiki/entities/<stem>|<name>]]`.
3. **IDX-03** — inline one-line summary per entity entry, read from the entity page's `summary:` frontmatter (NOT the graph attr).
4. **IDX-04** — test-suites nested under the package(s) they test in BOTH domain AND By-Kind sections; flat By-Kind "Test Suites" removed.
5. **IDX-05** — dependencies nested under their packages (external `used_by` → "Dependencies"; internal `depends_on_package` → "Internal dependencies") in BOTH contexts; flat By-Kind "Dependencies" removed.

The cross-cutting fix (D-01) — decoupling placement kinds from render order so flat-section
removal never loses a multi-domain package's nested items — is the structural backbone of this plan.

Output: the rendered index has `### Apps` (before `### Packages`), piped+summarized entity links
everywhere, and every package/app shows nested "Test Suites", "Dependencies", and "Internal
dependencies" sub-lists wherever it renders. graph-io gains `internal_dependencies_of`; the wiki-io
test suite (incl. syrupy snapshot) is green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/57-index-generation-polish/57-CONTEXT.md
@.planning/phases/57-index-generation-polish/57-RESEARCH.md
@.planning/phases/57-index-generation-polish/57-VALIDATION.md
@packages/wiki-io/src/wiki_io/index_generator.py
@packages/graph-io/src/graph_io/queries.py
@packages/wiki-io/tests/test_index_generator.py
@packages/wiki-io/tests/conftest.py
@packages/graph-io/CLAUDE.md
@packages/wiki-io/CLAUDE.md

<interfaces>
<!-- CURRENT index_generator.py (line numbers verified 2026-05-28; CONTEXT.md numbers drifted): -->
<!--   BY_KIND_ORDER L67 = ("package","test_suite","dependency","plugin"); KIND_LABELS L69-74. -->
<!--   PlacedEntity L114-133: kind,name,uri,parent_pkg_names,suite_kind,pkg_for_suite (NO summary). -->
<!--   _compute_qualifying_domains L140-195: package=direct belongs_to_domain; test_suite/dependency=one-hop; plugin=empty; raises ValueError otherwise (incl. app). -->
<!--   _consumer_pkgs_in_domain L198-231: DOMAIN-SCOPED consumer/tested pkg names for dep/test_suite. -->
<!--   _place_entities L234-290: iterates BY_KIND_ORDER via kind_to_list_fn (no app key); single-domain→bucket else→by_kind; parent_pkg_names only when single-domain & kind in (dependency,test_suite); by_kind.sort uses BY_KIND_ORDER.index (L289). -->
<!--   _entity_wikilink L419-432: returns f"[[wiki/entities/{stem}]]"; stem=_short_filename(uri,collision_set,suite_kind=,pkg_for_suite=). -->
<!--   _render_domain_section L435-500: per-package nesting L464-479 — "  - Test Suites"/"    - {link}" then "  - Dependencies"/"    - {link}", grouped via sub_for_pkg from parent_pkg_names. -->
<!--   _render_by_kind L534-559: FLAT "### {label}" per BY_KIND_ORDER kind, bare "- {link}". -->
<!--   _render_curated_section L562-572: "- {link}{summary}", summary=f" — {e['summary']}" if e.get('summary') (THE inline-summary shape). -->
<!--   _scan_curated_lane L329-354: reads fm via _parse_frontmatter, summary=fm.get('summary','') (THE frontmatter-read pattern). -->
<!--   _render L580-630: computes collision_set L589, workspace_root=wiki_root.parent L594; calls _place_entities(conn). -->
<!--   __all__ L678-700 re-exports public + underscore helpers; add any new public helper. -->
<!-- graph_io.queries: list_apps EXISTS (L866). describe_package L367 is kind='package'-only (returns None for app). -->
<!--   The outgoing depends_on_package query lives at L437-447 inside describe_package (src.kind IN ('package','app')). -->
<!--   No __all__ in queries.py. Schema has NO CHECK on kinds — fixture builds app nodes + depends_on_package edges freely. -->
<!-- entity_writer: ADMITTED_KINDS includes 'app'; _kind_list_fns()['app']=list_apps; short_filename derives 'app_<name>'. _entity_wikilink already works for app PlacedEntity unchanged. -->
<!-- test fixture make_index_fixture_graph (conftest L171-205): builds in-memory graph from {nodes:[(kind,name,attrs)], edges:[(sk,sn,dk,dn,ek,attrs)]} via upsert.upsert_records. -->
<!-- test_queries.py seeding: from source_parser.projections.graph import GraphEdge,GraphNode,GraphRecords; upsert.upsert_records(conn, GraphRecords(nodes=[...], edges=[...])). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add internal_dependencies_of(conn, *, name) to graph-io queries (IDX-05 source, reused not re-implemented)</name>
  <files>packages/graph-io/src/graph_io/queries.py, packages/graph-io/tests/test_queries.py</files>
  <read_first>
    - packages/graph-io/src/graph_io/queries.py:367-460 (`describe_package` — the outgoing `depends_on_package` query at L437-447 is the EXACT shape to extract: SELECT dst.name WHERE e.kind='depends_on_package' AND src.kind IN ('package','app') AND src.name=? AND dst.kind IN ('package','app') ORDER BY dst.name; describe_package gates on kind='package' (L369) so it returns None for apps — that's why a standalone function is needed)
    - packages/graph-io/src/graph_io/queries.py:836-873 (`list_packages`/`list_apps` — module-level public fn style; no `__all__` to update)
    - packages/graph-io/tests/test_queries.py:9-11,82+ (imports `GraphEdge,GraphNode,GraphRecords` from `source_parser.projections.graph`; seeding via `upsert.upsert_records(conn, GraphRecords(...))`; find an existing `describe_package` test to mirror)
    - packages/graph-io/CLAUDE.md (read-only queries via `store.read_only_connect()`; stdout=JSON / stderr=errors; `?` placeholders only)
    - .planning/phases/57-index-generation-polish/57-RESEARCH.md §4 (why describe_package can't cover apps; D-11 reuse intent)
  </read_first>
  <behavior>
    - New module-level function `internal_dependencies_of(conn: sqlite3.Connection, *, name: str) -> list[str]` returns the OUTGOING `depends_on_package` dst node names for the node named `name` — i.e. the workspace packages/apps that `name` depends on — sorted alphabetically.
    - It runs the SAME query as `describe_package`'s internal-dependencies block (`e.kind='depends_on_package'`, `src.kind IN ('package','app')`, `src.name=?`, `dst.kind IN ('package','app')`, `ORDER BY dst.name`), but works for a `name` of kind `package` OR `app` because it does NOT gate on the node's own kind the way `describe_package` does.
    - A node with no outgoing `depends_on_package` edges (or a non-existent name) returns `[]` (empty, no crash).
    - `?` placeholders only — no string interpolation of `name`. Read-only.
    - This is the SINGLE source of internal-dependency truth reused by wiki-io (D-11) — wiki-io must NOT write its own `depends_on_package` SQL.
  </behavior>
  <action>Edit `packages/graph-io/src/graph_io/queries.py`: add a module-level `def internal_dependencies_of(conn, *, name)` near the other `list_*`/`describe_*` query functions. Body: `conn.execute(...)` with the SELECT extracted from `describe_package` L437-447 — select `dst.name` from `edges e JOIN nodes src ON e.src=src.id JOIN nodes dst ON e.dst=dst.id WHERE e.kind='depends_on_package' AND src.kind IN ('package','app') AND src.name = ? AND dst.kind IN ('package','app') ORDER BY dst.name` with param `(name,)`; return `[r[0] for r in rows]`. Add a one-line docstring noting it covers both `package` and `app` source nodes (unlike `describe_package`) and is the reuse point for wiki-io IDX-05 (D-11). Then add a test to `packages/graph-io/tests/test_queries.py` (mirror an existing `describe_package`/`depends_on_package` test): seed via `upsert.upsert_records(conn, GraphRecords(nodes=[GraphNode(kind='package',name='alpha',...), GraphNode(kind='package',name='beta',...), GraphNode(kind='app',name='myapp',...)], edges=[GraphEdge(src=('package','beta','beta'),dst=('package','alpha','alpha'),kind='depends_on_package',attrs={}), GraphEdge(src=('app','myapp','myapp'),dst=('package','alpha','alpha'),kind='depends_on_package',attrs={})]))`, then assert `internal_dependencies_of(conn, name='beta') == ['alpha']`, `internal_dependencies_of(conn, name='myapp') == ['alpha']` (APP source works), and `internal_dependencies_of(conn, name='alpha') == []` (no outgoing). Do NOT modify `describe_package` or any existing test.</action>
  <verify>
    <automated>uv run --package graph-io pytest packages/graph-io/tests/test_queries.py -q</automated>
  </verify>
  <done>
    - `grep -n "def internal_dependencies_of" packages/graph-io/src/graph_io/queries.py` matches
    - `internal_dependencies_of` returns outgoing `depends_on_package` dst-names for a `package` AND an `app` source node, sorted, `[]` when none
    - New test in `test_queries.py` asserts the package-source, app-source, and empty cases
    - `uv run --package graph-io pytest packages/graph-io/tests/test_queries.py -q` exits 0
    - `describe_package` and existing tests are unchanged
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply IDX-01..IDX-05 to index_generator.py (constants, placement, links, summaries, nested rendering)</name>
  <files>packages/wiki-io/src/wiki_io/index_generator.py</files>
  <read_first>
    - packages/wiki-io/src/wiki_io/index_generator.py (FULL file — every change is here; especially BY_KIND_ORDER L67, KIND_LABELS L69-74, PlacedEntity L114-133, _compute_qualifying_domains L140-195, _consumer_pkgs_in_domain L198-231, _place_entities L234-290, _entity_wikilink L419-432, _render_domain_section L435-500, _render_by_kind L534-559, _render_curated_section L562-572, _scan_curated_lane L329-354, _render L580-630, __all__ L678-700)
    - packages/graph-io/src/graph_io/queries.py (import `list_apps` and the new `internal_dependencies_of` from Task 1)
    - .planning/phases/57-index-generation-polish/57-CONTEXT.md (D-01..D-11 — locked decisions)
    - .planning/phases/57-index-generation-polish/57-RESEARCH.md (§2 the decoupling crux, §3 by-kind nesting data, §4 internal-deps resolution, §5 summary plumbing)
    - packages/wiki-io/CLAUDE.md (package conventions)
  </read_first>
  <behavior>
    **Constants (IDX-01/D-03, D-08):**
    - `BY_KIND_ORDER = ("app", "package", "plugin")` (apps first; test_suite/dependency removed).
    - `KIND_LABELS` gains `"app": "Apps"`; keep `package`/`plugin` (and you MAY drop or keep `test_suite`/`dependency` labels — they are still used for the NESTED sub-headings "Test Suites"/"Dependencies", which are literal strings in the render code, so either keep the dict entries or use literals consistently).
    - New `_PLACEABLE_KINDS = ("app", "package", "test_suite", "dependency", "plugin")` — the iteration/sort order for placement, decoupled from render order (D-01 crux). Add to `__all__` if other modules/tests import it (the constant test will assert `BY_KIND_ORDER`; expose `_PLACEABLE_KINDS` via `__all__` too for symmetry).

    **PlacedEntity (IDX-03/D-07):** add `summary: str = ""` field (after `pkg_for_suite`, with default so construction stays compatible).

    **_compute_qualifying_domains (IDX-01/D-03/D-04):** handle `kind == "app"` identically to `package` (direct `belongs_to_domain` edges where the src node kind is `app`). Add an `app` branch (or generalize the `package` branch to match `kind IN ('package','app')` while still filtering by the passed kind). Apps with no domain edge → empty set → By-Kind fallback (correct per D-04).

    **_consumer_pkgs / by-kind nesting (IDX-04/05, D-01):** `parent_pkg_names` must be populated for by-kind-placed deps/test_suites too, using a DOMAIN-AGNOSTIC consumer/tested-package lookup (the existing `_consumer_pkgs_in_domain` is domain-scoped). Add a domain-agnostic variant (e.g. `_consumer_pkgs(conn, *, kind, entity_name)`) returning ALL consumer/tested package (and app) names via `used_by`/`tests` edges, OR generalize. In `_place_entities`, populate `parent_pkg_names` for dependency/test_suite regardless of single-vs-multi-domain (single-domain still works for `_render_domain_section`; the domain-agnostic names are a superset that `_render_by_kind` uses). Keep deterministic ordering (alphabetical).

    **_place_entities (IDX-01/IDX-03, D-03/D-04/D-07):**
    - Add `wiki_root: Path` and `collision_set: frozenset[str]` parameters.
    - Add `"app": list_apps` to `kind_to_list_fn`.
    - Iterate `_PLACEABLE_KINDS` (NOT `BY_KIND_ORDER`).
    - For every entity, derive the page stem with `_short_filename(uri, collision_set, suite_kind=, pkg_for_suite=)` (SAME call `_entity_wikilink` makes) and read `wiki_root/"entities"/(stem+".md")` frontmatter `summary:` via `_parse_frontmatter` (tolerant: missing dir/file/frontmatter → `""`). Store on `PlacedEntity.summary`.
    - `by_kind.sort` key must use `_PLACEABLE_KINDS.index(e.kind)` (not `BY_KIND_ORDER.index`, which would `ValueError` on a by-kind test_suite/dependency).
    - Return ALSO a `name_to_entity: dict[str, PlacedEntity]` index for `package`+`app` kinds (for internal-dep link resolution) — extend the return tuple, or build it in `_render` from the placed entities. Pick the lower-churn option; if extending the return tuple, update the `_render` unpack at L591.

    **_entity_wikilink (IDX-02/D-05):** return `f"[[wiki/entities/{stem}|{entity.name}]]"` — piped, display text = `entity.name`. Stem derivation unchanged.

    **Inline summary render (IDX-03/D-06/D-07):** add a small helper or inline the `_render_curated_section` shape: every entity bullet becomes `- {link} — {summary}` where the ` — {summary}` suffix is omitted when summary is empty. Apply to package/app top-level bullets, nested test_suite bullets, nested dependency bullets, internal-dep bullets, and the flat By-Kind app/package/plugin bullets. Add a comment citing D-06 / Phase 56 fill-when-empty for WHY summary is read from the page file.

    **_render_domain_section + _render_by_kind nesting (IDX-04/05, D-01/D-02/D-09):**
    - Both functions render each package/app bullet followed by THREE conditional nested sub-lists (omit any that are empty):
      1. `  - Test Suites` + `    - {ts_link}{summary}` (sorted) — from `parent_pkg_names` grouping (existing in domain section; NEW in by-kind).
      2. `  - Dependencies` + `    - {dep_link}{summary}` (sorted) — external `used_by` deps, from `parent_pkg_names` grouping.
      3. `  - Internal dependencies` + `    - {pkg_or_app_link}{summary}` (sorted) — NEW: for the bullet's package/app name, call `internal_dependencies_of(conn, name=<pkg name>)`, map each returned name through `name_to_entity` to build the wikilink to the internal PACKAGE/APP entity page; skip names with no matching placed entity. NEVER merge with "Dependencies"; NEVER drop.
    - `_render_by_kind` iterates `BY_KIND_ORDER` (app/package/plugin) for flat top-level groups; under each package/app group member it nests the three sub-lists. Plugins have no nested sub-lists. There is NO flat `### Test Suites` or `### Dependencies` group anymore. By-kind test_suites/dependencies (multi/zero-domain) appear ONLY nested under their parent package/app bullet (their `parent_pkg_names`); a by-kind dep/suite whose parent package is NOT in the by-kind group still nests under that package WHERE the package renders (domain or by-kind) — duplication is expected (D-10).
    - Factor the per-package nesting into a shared helper (e.g. `_render_pkg_nested(conn, pkg, sub_for_pkg, name_to_entity, collision_set)`) used by BOTH `_render_domain_section` and `_render_by_kind` so the two contexts stay byte-identical and DRY. Add a comment in `_render_by_kind` explaining D-01 (cross-cutting packages now nest so flat-section removal is safe).
    - `_render_by_kind` needs `conn` and `name_to_entity` — thread them through (currently it takes only `by_kind_entities, collision_set`). Update the call site in `_render` (L618).
    - Preserve D-08 empty-omission: a package with no suites/deps/internal-deps emits just its bullet; an entirely empty By-Kind section is omitted.

    **_render (orchestration):** compute `collision_set` (exists L589), pass `wiki_root` + `collision_set` into `_place_entities`; build/obtain `name_to_entity`; pass `conn` + `name_to_entity` into `_render_by_kind`; pass `conn` + `name_to_entity` into `_render_domains`/`_render_domain_section` (thread as needed). `entity_count` semantics: keep counting placed entities (domain buckets + by_kind) — do not double-count nested duplicates.

    **Determinism:** every nested list sorted alphabetically (by name or uri, consistent). No reliance on dict/set iteration order for output.
  </behavior>
  <action>Edit `packages/wiki-io/src/wiki_io/index_generator.py` only. (1) Set `BY_KIND_ORDER = ("app","package","plugin")`; add `KIND_LABELS["app"]="Apps"`; add `_PLACEABLE_KINDS = ("app","package","test_suite","dependency","plugin")`. (2) Add `summary: str = ""` to `PlacedEntity`. (3) Import `list_apps` and `internal_dependencies_of` from `graph_io.queries`. (4) Add an `app` branch to `_compute_qualifying_domains` (direct `belongs_to_domain`, src kind `app`). (5) Add a domain-agnostic consumer/tested-package helper and populate `parent_pkg_names` for ALL dependency/test_suite placements. (6) Rework `_place_entities`: add `wiki_root`+`collision_set` params, add `"app":list_apps` to the fn map, iterate `_PLACEABLE_KINDS`, derive stem + read `wiki_root/entities/<stem>.md` frontmatter `summary` (tolerant) into `PlacedEntity.summary`, sort `by_kind` by `_PLACEABLE_KINDS.index`, and produce a `name→PlacedEntity` index for package+app. (7) Make `_entity_wikilink` return the piped `[[wiki/entities/{stem}|{entity.name}]]`. (8) Add a `_render_pkg_nested(...)` helper rendering the three sub-lists (Test Suites / Dependencies / Internal dependencies via `internal_dependencies_of` + `name_to_entity`), each bullet using the `- {link} — {summary}` shape, omitting empty sub-lists; use it from both `_render_domain_section` and `_render_by_kind`. (9) Rewrite `_render_by_kind` to iterate `BY_KIND_ORDER` flat groups (app/package/plugin) and nest via the helper; remove flat test_suite/dependency groups; add `conn`+`name_to_entity` params. (10) Thread `wiki_root`/`collision_set`/`conn`/`name_to_entity` through `_render`, `_render_domains`, `_render_domain_section`, `_render_by_kind` call sites. (11) Add `_PLACEABLE_KINDS` (and any new public helper) to `__all__`. (12) Add comments citing D-01 (by-kind nesting / flat-removal safety) and D-06 (summary from page file, Phase 56 fill-when-empty). Do NOT change `generate_index`'s write-if-changed logic, the curated-lane scan, or `_scan_work`.</action>
  <verify>
    <automated>uv run --package wiki-io pytest packages/wiki-io/tests/test_index_generator.py -q -k "not snapshot"</automated>
  </verify>
  <done>
    - `grep -n 'BY_KIND_ORDER = ("app", "package", "plugin")' packages/wiki-io/src/wiki_io/index_generator.py` matches
    - `grep -n "_PLACEABLE_KINDS" packages/wiki-io/src/wiki_io/index_generator.py` matches (iteration + sort)
    - `grep -n "wiki/entities/{stem}|" packages/wiki-io/src/wiki_io/index_generator.py` (or equivalent f-string) shows the piped link
    - `grep -n "Internal dependencies" packages/wiki-io/src/wiki_io/index_generator.py` matches
    - `grep -n "internal_dependencies_of" packages/wiki-io/src/wiki_io/index_generator.py` matches (reuse, not parallel SQL — `grep -c "depends_on_package" packages/wiki-io/src/wiki_io/index_generator.py` is 0)
    - `summary: str` present on `PlacedEntity`; `_place_entities` reads `entities/<stem>.md` frontmatter
    - `_render_by_kind` has no `### Test Suites` / `### Dependencies` flat group; `### Apps` rendered before `### Packages`
    - Non-snapshot tests pass after the test updates in Task 3 (this verify is the pre-snapshot gate; Task 3 finalizes assertions)
  </done>
</task>

<task type="auto">
  <name>Task 3: Update broken assertions, add new IDX tests, regenerate snapshot</name>
  <files>packages/wiki-io/tests/test_index_generator.py</files>
  <read_first>
    - packages/wiki-io/tests/test_index_generator.py (FULL — every bare-stem `[[wiki/entities/` assertion, the constant/order tests, and the snapshot test)
    - packages/wiki-io/tests/conftest.py:171-205 (`make_index_fixture_graph` — declarative `{nodes,edges}` spec; supports `app` nodes + `depends_on_package` edges; `_write_curated_page` at test L366-374 writes frontmatter pages)
    - packages/wiki-io/src/wiki_io/index_generator.py (final shape from Task 2 — exact rendered strings to assert)
    - .planning/phases/57-index-generation-polish/57-RESEARCH.md §6 (the full table of tests to change)
    - .planning/phases/57-index-generation-polish/57-CONTEXT.md (D-05 piped form, D-09 two sub-lists, D-10 duplication)
  </read_first>
  <behavior>
    **Fix IDX-02 fallout (D-05):** every assertion expecting a bare `[[wiki/entities/<stem>]]` becomes the piped `[[wiki/entities/<stem>|<name>]]`. Known sites (verify by `grep -n "\[\[wiki/entities/" test_index_generator.py`): `test_single_domain_with_one_package` (~L470 `pkg_pkg-a`→`pkg_pkg-a|pkg-a`), `test_cross_cutting_in_by_kind_only` (L772-775 `pkg_pkg-cross`→`pkg_pkg-cross|pkg-cross`), `test_empty_sections_omitted` (L853 `pkg_pkg-solo`→`pkg_pkg-solo|pkg-solo`), and any others the grep surfaces.

    **Fix constant/order tests:** `test_module_constants` (L76-86) → `BY_KIND_ORDER == ("app","package","plugin")`; assert `KIND_LABELS["app"]=="Apps"`; drop assertions on removed `test_suite`/`dependency` KIND_LABELS keys (or assert the nested sub-headings exist in render output instead).

    **Fix flat→nested tests:** `test_by_kind_section_order` (L510-527) — no `### Dependencies` flat group; assert `### Apps` < `### Packages` < `### Plugins` order and that a dependency renders nested under its package (`  - Dependencies` + `    - <dep link>`), not as a flat `### Dependencies`. `test_test_suites_subheading` (L545-565) and `test_multi_domain_entity_in_by_kind` (L785-802) — replace `### Test Suites` flat-heading assertions with the nested `  - Test Suites` form (and note the multi-package suite duplicates under each package per D-10). `test_generate_index_against_fixture_graph` (L568-633) — remove the `### Dependencies` flat assertion (L621); recompute `by_kind_count` for the new nesting (boto3 multi-consumer now nests under pkg-a & pkg-b; pkg-cross + graph-wiki remain in by_kind); add piped-link + (where a summary page is written) inline-summary assertions.

    **New tests (IDX-01/03/05):**
    - IDX-01 app section: seed an `app` node with zero domains → `### Apps` appears and precedes `### Packages`; seed a single-domain app → it renders under its `## Domain: X` section (not in By-Kind). Use `("app","myapp",{"uri":"app:agent-research/myapp","app_kind":"cli","app_signals":[]})`.
    - IDX-05 internal-deps: seed two packages with a `depends_on_package` edge (`("package","consumer",...,"package","target",...,"depends_on_package",{})`) both in a domain → under `consumer` a `  - Internal dependencies` sub-list links to `target`'s entity page (`[[wiki/entities/pkg_target|target]]`), DISTINCT from any `  - Dependencies` (external) sub-list. Assert the two sub-headings are separate.
    - IDX-03 inline summary: write an entity page at `wiki_root/"entities"/"pkg_pkg-a.md"` with `summary: Some summary` frontmatter (reuse `_write_curated_page` shape, or write directly), build a graph with `pkg-a`, render, and assert the index contains `[[wiki/entities/pkg_pkg-a|pkg-a]] — Some summary`. Also assert that a package with NO entity page (empty summary) renders `[[wiki/entities/...|name]]` with NO ` — ` suffix.

    **Snapshot (IDX-01..05):** regenerate the syrupy snapshot for `test_snapshot_against_agent_research` (only when a live `.graph-wiki/graph.db` is present) with `--snapshot-update`, then commit the updated `.ambr`. If no live graph exists in the execution environment, the test is skipped — note in the SUMMARY that the snapshot must be regenerated post-Phase-56 against the live graph.

    **Determinism tests** (`test_determinism_across_permutations`, `test_write_if_changed`) must remain green unchanged — the new sorted sub-lists preserve determinism; the fresh `tmp_path` wikis have no entity pages so all summaries are empty.
  </behavior>
  <action>Edit `packages/wiki-io/tests/test_index_generator.py` only. (1) Grep `[[wiki/entities/` and convert every bare-stem assertion to the piped `<stem>|<name>` form. (2) Update `test_module_constants` to the new `BY_KIND_ORDER`/`KIND_LABELS`. (3) Rewrite `test_by_kind_section_order`, `test_test_suites_subheading`, `test_multi_domain_entity_in_by_kind`, and `test_generate_index_against_fixture_graph` for the nested (no-flat) form and corrected counts, removing the `### Dependencies`/`### Test Suites` flat-group assertions. (4) Add three new tests: an `app` By-Kind/domain placement test (IDX-01), a `depends_on_package` internal-deps sub-list test asserting a separate `  - Internal dependencies` heading linking to the internal package entity (IDX-05/D-09), and an inline-summary test that writes an entity page with `summary:` frontmatter and asserts `- [[wiki/entities/<stem>|<name>]] — <summary>` plus the empty-summary no-suffix case (IDX-03). (5) Regenerate the syrupy snapshot (`--snapshot-update`) if a live graph is available; otherwise note it in the summary. Do NOT weaken determinism/write-if-changed tests.</action>
  <verify>
    <automated>uv run --package wiki-io pytest packages/wiki-io/tests/test_index_generator.py -q</automated>
  </verify>
  <done>
    - `grep -n "\[\[wiki/entities/[^|]*\]\]" packages/wiki-io/tests/test_index_generator.py` returns NO bare-stem (non-piped) entity-link assertions
    - `test_module_constants` asserts `BY_KIND_ORDER == ("app","package","plugin")` and `KIND_LABELS["app"]=="Apps"`
    - New tests exist for: app section placement (IDX-01), internal-dependencies sub-list (IDX-05), inline summary render incl. empty-summary case (IDX-03)
    - No test asserts a flat `### Test Suites` or `### Dependencies` By-Kind group
    - `uv run --package wiki-io pytest packages/wiki-io/tests/test_index_generator.py -q` exits 0 (snapshot regenerated or skipped-with-note)
    - `uv run --package wiki-io pytest` full suite exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan touches graph-io's read-only query layer (one new parameterized SELECT) and wiki-io's
index renderer (reads the local sqlite graph + reads entity-page frontmatter from the vault, writes
`wiki/index.md` via the unchanged atomic write-if-changed path). No network, no untrusted input, no
new execution surface, no writes outside the existing `index.md` generation.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-01-T1 | Tampering | New `internal_dependencies_of` SQL string-interpolating `name` (injection / wrong match) | mitigate | Task 1 mandates `?` placeholders only, mirroring `describe_package`; the test seeds a known edge and asserts exact dst-name match. |
| T-57-01-T2 | Information Disclosure | Reading entity-page frontmatter could read outside the vault if stem derivation is attacker-controlled | mitigate | Stems come from `_short_filename` over graph-derived URIs (not user input) and are joined under `wiki_root/entities/`; path is `wiki_root/"entities"/(stem+".md")` with no traversal segments. Missing/odd files degrade to empty summary (tolerant read), never raising or escaping the dir. |
| T-57-01-T3 | Denial of Service | Per-entity frontmatter file read adds I/O; a huge vault could slow index generation | mitigate | One small read per placed entity (bounded by entity count, same order as existing curated-lane scan); reads are `read_text(errors="replace")` like `_scan_curated_lane`, no recursion added. Determinism tests run on fresh tmp vaults (no entity pages) confirming the no-page fast path. |
| T-57-01-T4 | Repudiation/Correctness | A swapped JOIN direction or wrong `name_to_entity` mapping mislabels internal deps as external (or vice-versa) | mitigate | Task 3's IDX-05 test asserts the `Internal dependencies` sub-heading is SEPARATE from `Dependencies` and links to the internal PACKAGE entity page (`pkg_<name>`), not a dependency page — a swapped mapping fails the assertion. |
</threat_model>

<verification>
- `uv run --package graph-io pytest packages/graph-io/tests/test_queries.py -q` exits 0 (new `internal_dependencies_of` test green)
- `uv run --package wiki-io pytest packages/wiki-io/tests/test_index_generator.py -q` exits 0 (all updated + new tests green, snapshot regenerated or skipped-with-note)
- `uv run --package wiki-io pytest` full suite exits 0
- Rendered index contains `### Apps` before `### Packages`; a single-domain app renders under its domain (IDX-01)
- Entity links render as `[[wiki/entities/<stem>|<name>]]` everywhere (IDX-02) — no bare-stem links remain
- Each entity entry renders `- {link} — {summary}` when its entity page has a `summary:` frontmatter, and no suffix when empty (IDX-03)
- Test-suites nest under their package(s) in both domain and By-Kind sections; no flat `### Test Suites` group (IDX-04)
- Dependencies nest under their package(s) as a `Dependencies` sub-list, AND a SEPARATE `Internal dependencies` sub-list (from `depends_on_package` via `internal_dependencies_of`) links to internal package/app entity pages; no flat `### Dependencies` group (IDX-05)
- `grep -c "depends_on_package" packages/wiki-io/src/wiki_io/index_generator.py` is 0 (no parallel SQL — D-11 honored)
- Determinism + write-if-changed tests remain green
</verification>

<success_criteria>
- IDX-01: distinct `app` By-Kind section, separate from packages, apps listed first (ROADMAP SC#1)
- IDX-02: entity links render `[[wiki/entities/<stem>|<name>]]` with human-readable display text (SC#2)
- IDX-03: each entity entry shows a one-line inline summary from the entity page's `summary:` frontmatter (SC#3)
- IDX-04: test-suites nested under the package(s) they test, duplicated where appropriate; no flat By-Kind "Test Suites" (SC#4)
- IDX-05: dependencies nested under their packages (external "Dependencies" + internal "Internal dependencies"), duplicated where appropriate; no flat By-Kind "Dependencies" (SC#5)
- The wiki-io + graph-io test suites are green; the change is additive at the graph-io query layer
</success_criteria>

<execution_gate>
**EXECUTE AFTER PHASE 56 LANDS.** Phase 55 (depends_on_package edge + describe_package internal-deps)
is already Complete. Phase 56 (entity `summary:` frontmatter population) is planned but not yet
executed as of 2026-05-28. This plan's CODE is correct regardless (summary reads degrade to empty),
but IDX-03's rendered inline summaries and the live-graph syrupy snapshot are only meaningful once
entity pages carry `summary:`. Do not regenerate the live snapshot until Phase 56 has populated
entity-page summaries.
</execution_gate>

<output>
Create `.planning/phases/57-index-generation-polish/57-01-SUMMARY.md` when done. Summary captures:
- The new `graph_io.queries.internal_dependencies_of` function and why it (not `describe_package`) is used for apps (D-11)
- The `_PLACEABLE_KINDS` vs `BY_KIND_ORDER` decoupling (D-01 crux) and how it makes flat-section removal safe
- The five IDX changes as landed (constants, app placement, piped links, page-frontmatter summaries, three nested sub-lists incl. Internal dependencies)
- Which tests were updated for the piped-link/flat-removal fallout and which new tests were added
- Snapshot status (regenerated, or skipped-with-note pending live-graph + Phase 56)
- `uv run --package graph-io pytest ...` and `uv run --package wiki-io pytest ...` results
</output>
