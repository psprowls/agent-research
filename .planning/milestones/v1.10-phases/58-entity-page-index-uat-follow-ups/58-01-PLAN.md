---
phase: 58-entity-page-index-uat-follow-ups
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/wiki-io/src/wiki_io/assets/page-templates/entity-package.md
  - packages/wiki-io/src/wiki_io/assets/page-templates/entity-app.md
  - packages/wiki-io/src/wiki_io/assets/page-templates/entity-plugin.md
  - packages/wiki-io/src/wiki_io/entity_writer.py
  - packages/wiki-io/tests/test_entity_writer.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "Generated entity pages contain a clean Obsidian-safe Related marker with no literal `<...>` text (SC#1, D-01/D-02/D-03)"
    - "The empty-description summary placeholder has no leading `>`, no `<...>`, and no `:` (SC#2, D-04/D-05)"
    - "Test assertions referencing the old summary placeholder string pass against the new marker (D-06)"
  artifacts:
    - path: "packages/wiki-io/src/wiki_io/assets/page-templates/entity-package.md"
      provides: "Clean ## Related marker (no <...> wikilinks)"
      contains: "## Related"
    - path: "packages/wiki-io/src/wiki_io/assets/page-templates/entity-app.md"
      provides: "Clean ## Related marker"
      contains: "## Related"
    - path: "packages/wiki-io/src/wiki_io/assets/page-templates/entity-plugin.md"
      provides: "Clean ## Related marker"
      contains: "## Related"
    - path: "packages/wiki-io/src/wiki_io/entity_writer.py"
      provides: "Obsidian-safe summary placeholder string"
      contains: "summary"
  key_links:
    - from: "entity_writer.py:587"
      to: "fm['summary'] placeholder"
      via: "f-string assignment when description empty"
      pattern: "fm\\[.summary.\\] = description or"
---

<objective>
Fix the two independent wiki-io template/string defects from v1.10 UAT:

- **Item #1 (SC#1):** Replace the `<...>` placeholder wikilink lists in the `## Related` block of three entity page templates with a single clean Obsidian-safe fill-me-in marker that names the curated page types (concepts, ADRs, architecture). Per D-03 this is a template-content edit only — no `entity_writer` logic change (the two-token rule leaves `<...>` text untouched, so the placeholder survives into generated pages; editing the template content is the fix).
- **Item #2 (SC#2):** Replace the empty-description summary placeholder at `entity_writer.py:587` (`> TODO: <add a one-line summary for {name}>`) with a plain-text marker carrying no leading `>`, no `<...>`, and no `:` (D-04). Update the exact-string test assertion that references the old form (D-06).

Purpose: Generated entity pages currently break Obsidian inline rendering (the leading `>` makes a blockquote) and ship dead `<...>` placeholder links. This phase makes both render cleanly.

Output: Three edited template files, one edited source line, one updated test assertion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-entity-page-index-uat-follow-ups/58-CONTEXT.md
@.planning/phases/58-entity-page-index-uat-follow-ups/58-RESEARCH.md
@.planning/phases/58-entity-page-index-uat-follow-ups/58-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace ## Related placeholder block in three entity templates (Item #1)</name>
  <files>
    packages/wiki-io/src/wiki_io/assets/page-templates/entity-package.md
    packages/wiki-io/src/wiki_io/assets/page-templates/entity-app.md
    packages/wiki-io/src/wiki_io/assets/page-templates/entity-plugin.md
  </files>
  <read_first>
    - packages/wiki-io/src/wiki_io/assets/page-templates/entity-package.md (lines 36-39 carry the `<...>` Related block)
    - packages/wiki-io/src/wiki_io/assets/page-templates/entity-app.md (lines 42-46)
    - packages/wiki-io/src/wiki_io/assets/page-templates/entity-plugin.md (lines 24-26)
    - 58-RESEARCH.md §"Item #1: Related Section — Verified Findings" (template line map + recommended marker)
    - 58-CONTEXT.md D-01/D-02/D-03 (clean marker now; Obsidian-safe; scope = entity templates only)
  </read_first>
  <action>
    In each of the three templates, replace the bullet list under `## Related` (the `- [[concepts/<concept>]]` / `- [[packages/<other-pkg>]]` / `- [[dependencies/<lib>]]` style placeholder wikilinks) with a single plain-text line that names the future curated page types and contains NO `<...>`, NO leading `>`, and NO `:` per D-02. Recommended exact line: `No related concept, ADR, or architecture pages yet.` Keep the `## Related` heading itself. Do NOT touch `entity-test-suite.md` or `entity-dependency.md` (they have no `## Related`). Do NOT touch `## Related patterns` in `concept*.md` (D-03 — different section). This is a template-content edit only; do not modify any `entity_writer` logic for this item.
  </action>
  <verify>
    <automated>! grep -rn '&lt;\.\.\.&gt;\|&lt;concept&gt;\|&lt;other-pkg&gt;\|&lt;lib&gt;\|&lt;domain&gt;\|&lt;pkg&gt;' packages/wiki-io/src/wiki_io/assets/page-templates/entity-package.md packages/wiki-io/src/wiki_io/assets/page-templates/entity-app.md packages/wiki-io/src/wiki_io/assets/page-templates/entity-plugin.md | grep -i related</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '## Related' packages/wiki-io/src/wiki_io/assets/page-templates/entity-package.md` returns the heading (still present)
    - The `## Related` section in all three templates contains no `<` character on any of its body lines (no `<...>` survives)
    - The `## Related` body line has no leading `>` and no `:` character
    - `uv run --package wiki-io pytest packages/wiki-io/tests/ -k related -x` exits 0 (or reports no matching tests if none exist for Related; in that case the grep-based criteria above are the gate)
    - `entity-test-suite.md` and `entity-dependency.md` are unmodified (`git diff --name-only` does not list them)
  </acceptance_criteria>
  <done>All three entity templates show a clean Obsidian-safe `## Related` marker; no `<...>` placeholder wikilinks remain in any of the three Related blocks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace summary placeholder string and update test assertion (Item #2)</name>
  <files>
    packages/wiki-io/src/wiki_io/entity_writer.py
    packages/wiki-io/tests/test_entity_writer.py
  </files>
  <read_first>
    - packages/wiki-io/src/wiki_io/entity_writer.py (line 587: `fm["summary"] = description or f"> TODO: <add a one-line summary for {node.name}>"`)
    - packages/wiki-io/tests/test_entity_writer.py (line 482: `todo = "> TODO: <add a one-line summary for x>"` in test_merge_summary_todo_marker_when_description_empty)
    - 58-RESEARCH.md §"Item #2: Summary Placeholder — Verified Findings"
    - 58-CONTEXT.md D-04/D-05/D-06
  </read_first>
  <behavior>
    - test_merge_summary_todo_marker_when_description_empty: when a node has an empty description, the merged `summary:` frontmatter equals the new plain-text marker `TODO add a one-line summary for x` (no `>`, no `<`, no `:`)
    - The new marker string contains none of the characters `>`, `<`, `:`
    - The integration test test_no_unsubstituted_token_and_summary_populated (test_entity_writer_integration.py:240) still passes — it only asserts non-empty, and the new marker is non-empty (no change to that file)
  </behavior>
  <action>
    At `entity_writer.py:587`, change the f-string fallback for `fm["summary"]` from `f"> TODO: <add a one-line summary for {node.name}>"` to the plain-text form `f"TODO add a one-line summary for {node.name}"` — no leading `>`, no angle brackets, no colon (D-04). Scope strictly to this one line (D-05) — do NOT sweep sibling templates (`source.md`, `AGENTS.md.template`, `CLAUDE.md.template`). Then update the exact-string assertion at `test_entity_writer.py:482` from `todo = "> TODO: <add a one-line summary for x>"` to match the new marker `TODO add a one-line summary for x` (D-06), keeping the rest of the test logic intact.
  </action>
  <verify>
    <automated>uv run --package wiki-io pytest packages/wiki-io/tests/test_entity_writer.py::test_merge_summary_todo_marker_when_description_empty -x</automated>
  </verify>
  <acceptance_criteria>
    - The `fm["summary"]` placeholder region of entity_writer.py (the `description or` line) contains no `>`, no `<`, and no `:` character
    - `uv run --package wiki-io pytest packages/wiki-io/tests/test_entity_writer.py -x` exits 0
    - `uv run --package wiki-io pytest packages/wiki-io/tests/integration/test_entity_writer_integration.py -x` exits 0 (unchanged file; still passes on non-empty assertion)
    - `grep -n 'TODO: <add a one-line summary' packages/wiki-io/tests/test_entity_writer.py` returns nothing (old string fully replaced)
  </acceptance_criteria>
  <done>The empty-description summary placeholder is a clean plain-text marker with no `>`/`<`/`:`; the exact-string test assertion is updated and the wiki-io entity_writer test module is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | All changes are static template text and an f-string constant in a local code-generation tool; no untrusted input crosses any boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-01 | Tampering | entity templates / summary placeholder string | accept | No new attack surface — per RESEARCH §"Security Domain", these are static template strings rendered into local Markdown; no auth/input-validation/crypto change |
| T-58-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan; framework (pytest) already present per VALIDATION.md |
</threat_model>

<verification>
- `uv run --package wiki-io pytest packages/wiki-io/tests/ -x -q` is green
- No `<...>` survives in any of the three entity templates' `## Related` blocks
- The summary placeholder string carries no `>`, `<`, or `:`
</verification>

<success_criteria>
- SC#1: Generated entity pages contain a clean Obsidian-safe `## Related` marker; no literal `<...>` text survives (verified at template level; full regeneration validated in Plan 03's snapshot rebaseline)
- SC#2: The empty-description `summary:` placeholder has no leading `>`, no `<...>`, no `:`
</success_criteria>

<output>
Create `.planning/phases/58-entity-page-index-uat-follow-ups/58-01-SUMMARY.md` when done
</output>
