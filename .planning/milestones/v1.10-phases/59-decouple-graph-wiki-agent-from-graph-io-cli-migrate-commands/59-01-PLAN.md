---
phase: 59-decouple-graph-wiki-agent-from-graph-io-cli-migrate-commands
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/graph-io/src/graph_io/render.py
  - packages/graph-io/src/graph_io/cli/_format.py
  - packages/graph-io/src/graph_io/cli/q_describe_package.py
  - packages/graph-io/src/graph_io/cli/q_describe_path.py
  - packages/graph-io/src/graph_io/cli/q_describe_repo.py
  - packages/graph-io/src/graph_io/cli/q_describe_domain.py
  - packages/graph-io/src/graph_io/cli/q_describe_entry_point.py
  - packages/graph-io/src/graph_io/cli/q_describe_suite.py
  - packages/graph-io/src/graph_io/cli/q_find.py
autonomous: true
requirements: [SC-03]
must_haves:
  truths:
    - "A public graph_io.render module exists outside graph_io.cli and is importable as `from graph_io import render` (D-01)"
    - "render.py exposes render() plus six format_<kind>() functions"
    - "cg describe-* and cg find produce byte-identical human and json output after the refactor (graph-io test suite green) (D-03)"
    - "graph_io.cli._format still imports successfully (re-export shim) so the 7 existing cli callers are unbroken (D-02)"
  artifacts:
    - path: "packages/graph-io/src/graph_io/render.py"
      provides: "Public formatter: render() + format_package/path/repo/domain/entry_point/suite"
      contains: "def format_package"
    - path: "packages/graph-io/src/graph_io/cli/_format.py"
      provides: "Thin re-export shim from graph_io.render (keeps 7 cli callers working)"
      contains: "from graph_io.render import"
  key_links:
    - from: "packages/graph-io/src/graph_io/cli/q_describe_package.py"
      to: "graph_io.render.format_package"
      via: "import + call"
      pattern: "render.*format_package"
    - from: "packages/graph-io/src/graph_io/cli/q_find.py"
      to: "graph_io.render.render"
      via: "import + call"
      pattern: "render\\.render|render import render"
    - from: "packages/graph-io/src/graph_io/cli/_format.py"
      to: "graph_io.render"
      via: "re-export shim (from graph_io.render import render, _to_dict, ...)"
      pattern: "from graph_io.render import"
---

<objective>
Promote graph_io's formatting into a public `graph_io.render` module (D-01) and make it the single source of truth (D-02): relocate `_format.render` (+ its 4 private helpers) verbatim, add six `format_<kind>()` functions extracted byte-for-byte from the inline printers in the 6 `q_describe_*.py` modules, and refactor those 6 modules + `q_find.py` to call the public formatter. cg's own test suite is the byte-identical guard (D-03).

Purpose: SC#1 forbids the agent importing `graph_io.cli`; the shared formatter must live in a public module so both cg and the agent (Plan 02) consume one renderer. This plan lands the formatter so Plan 02 can depend on it.
Output: `graph_io/render.py` (new public module) + 7 refactored cli modules + a `_format.py` re-export shim.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-decouple-graph-wiki-agent-from-graph-io-cli-migrate-commands/59-CONTEXT.md
@.planning/phases/59-decouple-graph-wiki-agent-from-graph-io-cli-migrate-commands/59-RESEARCH.md
@.planning/phases/59-decouple-graph-wiki-agent-from-graph-io-cli-migrate-commands/59-PATTERNS.md

<interfaces>
<!-- Exact output shapes the formatter must reproduce byte-for-byte. Source: the inline printers in each q_describe_*.py. -->

graph_io.render public API (to create):
  def render(records, fmt, *, cap=None, on_truncate=None) -> str   # moved verbatim from cli/_format.py
  def format_package(desc, fmt) -> str
  def format_path(desc, fmt) -> str
  def format_repo(desc, fmt) -> str
  def format_domain(desc, packages: list[str], subdomains: list[str], fmt) -> str
  def format_entry_point(desc, fmt) -> str
  def format_suite(desc, fmt) -> str

Existing private helpers to move alongside render() (cli/_format.py lines 10-47):
  _to_dict, _is_importer_batch, _importer_human, _importer_json

Per-kind human output shapes are documented exactly in 59-PATTERNS.md lines 92-199 (label text,
field order, alignment spacing, '(none)'/'-' fallbacks, 'suite:' not 'test_suite:'). JSON output for
all six is `json.dumps(dataclasses.asdict(desc), default=str)`; domain JSON additionally merges
`packages` and `subdomains` keys.

IMPORTANT (audit correction): `graph_io.cli._format.render` is imported by SEVEN cli modules, not
just q_find.py — also q_imported_by, q_exported_by, q_exports, q_imports, q_callers, q_callees.
RESEARCH/PATTERNS recommend DELETING _format.py; that would break those 6 modules. Instead convert
_format.py into a re-export shim so they keep working untouched.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create public graph_io.render module + _format.py re-export shim</name>
  <files>packages/graph-io/src/graph_io/render.py, packages/graph-io/src/graph_io/cli/_format.py</files>
  <read_first>
    - packages/graph-io/src/graph_io/cli/_format.py (the full module being promoted — move lines 1-103 verbatim)
    - packages/graph-io/src/graph_io/cli/q_describe_package.py (lines 36-47: package human printer to extract)
    - packages/graph-io/src/graph_io/cli/q_describe_path.py (path printer)
    - packages/graph-io/src/graph_io/cli/q_describe_repo.py (repo printer)
    - packages/graph-io/src/graph_io/cli/q_describe_domain.py (domain printer + the two extra SQL queries that feed packages/subdomains)
    - packages/graph-io/src/graph_io/cli/q_describe_entry_point.py (entry-point printer)
    - packages/graph-io/src/graph_io/cli/q_describe_suite.py (suite printer; label is 'suite:')
    - .planning/phases/59-decouple-graph-wiki-agent-from-graph-io-cli-migrate-commands/59-PATTERNS.md (lines 92-199: exact extracted-function bodies)
  </read_first>
  <behavior>
    - render() relocated to graph_io.render behaves identically to the old _format.render (same human alignment, json output, cap/on_truncate, ImporterRecord batch handling, ValueError on unknown fmt)
    - format_package/path/repo/entry_point/suite(desc, fmt="human") return the exact multi-line string the corresponding q_describe_*.py inline printer currently produces (label text, field order, two-space alignment, '(none)'/'-' fallbacks)
    - format_suite uses label "suite:" NOT "test_suite:"
    - format_domain(desc, packages, subdomains, fmt) accepts packages/subdomains as explicit args (they are NOT in DomainDescription); human output includes the two indented bullet lists; json output merges {"packages": ..., "subdomains": ...} into asdict(desc)
    - Each format_*(..., fmt="json") returns json.dumps(dataclasses.asdict(desc), default=str)
    - `from graph_io.cli import _format` still works and `_format.render` resolves to graph_io.render.render
  </behavior>
  <action>
    Create `packages/graph-io/src/graph_io/render.py`. Move the entire body of `cli/_format.py` (module docstring, `from __future__ import annotations`, `import dataclasses`, `import json as _json` (preserve the `as _json` alias exactly — every `format_*`/`render` body calls `_json.dumps(...)`, so an unaliased `import json` would NameError), the four helpers `_to_dict`/`_is_importer_batch`/`_importer_human`/`_importer_json`, and `render`) into it verbatim. Then add the six per-kind formatters using the exact bodies in 59-PATTERNS.md lines 92-199 — preserve every label string, field-order, alignment-space count, and fallback token ('(none)', '-') exactly; do not normalize whitespace. Use `dataclasses.asdict(desc)` + `_json.dumps(..., default=str)` for the json branches; `format_domain` json merges `packages` and `subdomains` keys. Then replace `cli/_format.py` contents with a re-export shim: `from graph_io.render import render, _to_dict, _is_importer_batch, _importer_human, _importer_json` (plus the module docstring). This keeps the 7 existing `from graph_io.cli import _format` callers (q_find, q_imported_by, q_exported_by, q_exports, q_imports, q_callers, q_callees) working without edits. Do NOT delete `_format.py` (RESEARCH said delete — that is wrong; the shim is required to avoid breaking 6 unrelated cli modules). Do NOT import `graph_io.queries` at render.py module load (preserve the `_is_importer_batch` duck-type check comment).
  </action>
  <verify>
    <automated>cd packages/graph-io && uv run --package graph-io python -c "from graph_io import render; from graph_io.cli import _format; assert _format.render is render.render; assert all(hasattr(render, f) for f in ['format_package','format_path','format_repo','format_domain','format_entry_point','format_suite'])" && uv run --package graph-io pytest tests/test_cli_format.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `from graph_io import render` succeeds; `render.format_package`..`render.format_suite` all exist (6 functions)
    - `from graph_io.cli import _format` succeeds and `_format.render is graph_io.render.render` (shim identity)
    - `uv run --package graph-io pytest tests/test_cli_format.py -x` passes unchanged (render() behavior preserved)
    - `grep -n "from graph_io.render import" packages/graph-io/src/graph_io/cli/_format.py` returns a match (shim in place)
  </acceptance_criteria>
  <done>render.py exists with render() + 6 format_* functions; _format.py is a shim re-exporting from render; test_cli_format.py green.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor the 6 q_describe_*.py + q_find.py to call graph_io.render</name>
  <files>packages/graph-io/src/graph_io/cli/q_describe_package.py, packages/graph-io/src/graph_io/cli/q_describe_path.py, packages/graph-io/src/graph_io/cli/q_describe_repo.py, packages/graph-io/src/graph_io/cli/q_describe_domain.py, packages/graph-io/src/graph_io/cli/q_describe_entry_point.py, packages/graph-io/src/graph_io/cli/q_describe_suite.py, packages/graph-io/src/graph_io/cli/q_find.py</files>
  <read_first>
    - packages/graph-io/src/graph_io/cli/q_describe_package.py (the inline printer block to replace, lines 36-47)
    - packages/graph-io/src/graph_io/cli/q_describe_domain.py (keep the two extra SQL queries; only replace the format block)
    - packages/graph-io/src/graph_io/cli/q_describe_entry_point.py (keep the disambiguation logic; only replace the format block)
    - packages/graph-io/src/graph_io/cli/q_find.py (line 11 import + line 73 call to swap)
    - .planning/phases/59-decouple-graph-wiki-agent-from-graph-io-cli-migrate-commands/59-PATTERNS.md (lines 205-315: per-module change instructions)
  </read_first>
  <action>
    In each of the 6 `q_describe_*.py`: add `from graph_io import render as _render` and replace the inline `print(...)` formatting block with a single `print(_render.format_<kind>(desc, fmt=args.fmt))` (domain passes `(desc, packages, subdomains, fmt=args.fmt)` — keep its two existing inline SQL queries that build `packages`/`subdomains`; entry_point keeps its full disambiguation block unchanged, only the final format/print changes). Remove now-unused `import dataclasses` and `import json as _json` from each module where they become orphaned (Karpathy surgical-changes: only remove imports YOUR change orphaned). Leave the connect/error-handling/exit-code blocks untouched. In `q_find.py`: change line 11 `from graph_io.cli import _format` to `from graph_io import render as _render`, and line 73 `_format.render(...)` to `_render.render(...)`; no other change. Do NOT alter any exit-code constant, stderr string, or label text — the byte-identical bar (D-03) is enforced by the cg test suite in the verify step.
  </action>
  <verify>
    <automated>cd packages/graph-io && uv run --package graph-io pytest tests/test_cli_describe.py tests/test_cli_describe_entry_point.py tests/test_cli_exit_codes.py tests/test_cli_anti_regression.py tests/test_cli_format.py -x</automated>
  </verify>
  <acceptance_criteria>
    - All 7 modules import `graph_io.render` (or use `_render`) and no longer contain inline describe-format `print(f"...")` blocks for the promoted fields
    - `grep -rn "from graph_io.cli import _format" packages/graph-io/src/graph_io/cli/q_find.py` returns NOTHING (q_find migrated to graph_io.render)
    - cg regression suite green: `tests/test_cli_describe.py`, `tests/test_cli_describe_entry_point.py`, `tests/test_cli_exit_codes.py`, `tests/test_cli_anti_regression.py` all pass — proves cg output byte-identical (D-03)
    - No orphaned `import dataclasses` / `import json as _json` left in the refactored describe modules
  </acceptance_criteria>
  <done>All 7 cli modules call the public formatter; cg's full describe/find/exit-code/anti-regression suites pass byte-identical.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cg CLI argv → cli modules | Existing, unchanged by this plan; only the format step moves |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-01 | Information Disclosure | format_* output strings | accept | Refactor preserves byte-identical output (D-03); no new fields surfaced. cg anti-regression suite guards against accidental disclosure of new internal fields. |
| T-59-02 | Tampering | render.py formatting of DB-sourced strings | accept | Read-only rendering of already-stored data; no new input crosses a boundary; queries remain parameterized in callers (unchanged). |
| T-59-SC | Tampering | npm/pip/cargo installs | N/A | No package installs in this plan (RESEARCH Package Legitimacy Audit: zero new packages). |
</threat_model>

<verification>
- `from graph_io import render` works; `from graph_io.cli import _format` works (shim).
- cg describe/find/exit-code/anti-regression suites green (byte-identical guard).
- No cli module other than the shim references inline describe formatting.
</verification>

<success_criteria>
- D-01: public `graph_io.render` module exists outside `graph_io.cli`.
- D-02: single source of truth — `_format.py` re-exports from `render`; all 7 cli formatters call `graph_io.render`.
- D-03: cg output byte-identical (cg test suite green).
</success_criteria>

<output>
Create `.planning/phases/59-decouple-graph-wiki-agent-from-graph-io-cli-migrate-commands/59-01-SUMMARY.md` when done. Record: final public module name (`graph_io.render`), that `_format.py` is a shim (not deleted) and why (7 callers), and any output-shape subtleties discovered during extraction.
</output>
