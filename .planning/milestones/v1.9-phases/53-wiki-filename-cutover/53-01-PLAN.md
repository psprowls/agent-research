---
phase: 53-wiki-filename-cutover
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [WIKI-FN-05, WIKI-FN-06]

must_haves:
  truths:
    - "`.planning/ROADMAP.md` §Phase 53 Success Criteria 1 (`migrate-vault` atomic rewrite) is REMOVED."
    - "`.planning/ROADMAP.md` §Phase 53 Success Criteria 2 (idempotency via manifest marker) is REMOVED."
    - "`.planning/ROADMAP.md` §Phase 53 Success Criteria 3 is REWRITTEN to verification-language reflecting that `generate_index()` writes short filenames after `cg update --full` per Phase 52 — verified manually in `53-UAT.md`."
    - "`.planning/ROADMAP.md` §Phase 53 Success Criteria 4 is REWRITTEN to describe the manual vault regen procedure (delete entity dirs → `cg update --full` → `graph-wiki-agent scan` → inspect index)."
    - "`.planning/ROADMAP.md` §Phase 53 Goal line is loosened: 'single atomic operation' wording removed; replaced with cleanup + verification scope per CONTEXT D-01..D-03."
    - "`.planning/REQUIREMENTS.md` WIKI-FN-05 is REWRITTEN to verification-language: 'encode_slug / decode_slug removed; consumer call sites rewritten; grep returns zero hits; tests pass.'"
    - "`.planning/REQUIREMENTS.md` WIKI-FN-06 is REWRITTEN to verification-language: 'short filenames emitted by Phase 52 write_entities; vault regenerated manually; UAT in 53-UAT.md.'"
    - "The traceability table in REQUIREMENTS.md (WIKI-FN-05 → Phase 53, WIKI-FN-06 → Phase 53) is unchanged (status stays `Pending` until phase verification flips it)."
    - "No source code under `packages/` or `agents/` is touched by this plan."
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Reshaped §Phase 53 success criteria and goal (D-10)"
      contains: "Phase 53: Wiki Filename Cutover"
    - path: ".planning/REQUIREMENTS.md"
      provides: "WIKI-FN-05 and WIKI-FN-06 rewritten to verification-language"
      contains: "WIKI-FN-05"
  key_links:
    - from: ".planning/ROADMAP.md §Phase 53"
      to: ".planning/REQUIREMENTS.md WIKI-FN-05/06"
      via: "matching language between roadmap success criteria and requirements"
      pattern: "encode_slug|decode_slug|regenerated"
---

<objective>
Reshape `.planning/ROADMAP.md` §Phase 53 and `.planning/REQUIREMENTS.md` WIKI-FN-05/06 to
match the slimmed Phase 53 scope (CONTEXT D-01..D-10). Strike the `migrate-vault` /
idempotency / atomic-commit criteria (no longer applicable — no migration command, no vault
manifest marker, no rewriter) and rewrite the remaining criteria as verification-language
that flows from Phase 52's write-path correctness plus Phase 53's source cleanup plus the
manual UAT recorded in `53-UAT.md`.

Purpose: This plan is the docs deliverable for CONTEXT D-10. Keeping it as a dedicated plan
(rather than folding into the code-cleanup plan) preserves a clean reviewable commit so the
scope-shrink is auditable in git history. The plan touches only `.planning/*` markdown
files — no code, no tests.

Output:
- `.planning/ROADMAP.md` §Phase 53 with reshaped goal + success criteria (SC #1 / #2 removed; SC #3 / #4 reframed as verification observations).
- `.planning/REQUIREMENTS.md` WIKI-FN-05 and WIKI-FN-06 rewritten to verification-language.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/53-wiki-filename-cutover/53-CONTEXT.md
@.planning/phases/53-wiki-filename-cutover/53-RESEARCH.md
@.planning/phases/53-wiki-filename-cutover/53-DISCUSSION-LOG.md
</context>

<tasks>

<task type="auto">
  <name>Task 53-01-01: Rewrite `.planning/REQUIREMENTS.md` WIKI-FN-05 and WIKI-FN-06 to verification-language</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (target — read the full WIKI-FN-* block at lines 28-35; lines 34-35 specifically hold WIKI-FN-05 and WIKI-FN-06; the traceability table at lines 67-94 must stay unchanged for these two IDs except their description column if the table has descriptions)
    - .planning/phases/53-wiki-filename-cutover/53-CONTEXT.md §D-10 + §"Claude's Discretion" (default = rewrite to verification-language)
    - .planning/phases/53-wiki-filename-cutover/53-RESEARCH.md §"Roadmap + Requirements Edits (D-10)" (canonical new text for both requirements)
  </read_first>
  <action>
    Open `.planning/REQUIREMENTS.md` and replace the two requirement bullets exactly.

    **WIKI-FN-05 current text (line 34):**
    ```
    - [ ] **WIKI-FN-05**: `migrate-vault` (or equivalent one-shot cutover command) rewrites existing inbound `[[…]]` wikilinks in curated lanes (`/concepts/`, `/adrs/`, `/architecture/`, `/work/`, `/sources/`) from the old URI-fully-qualified filenames to the new short filenames, in a single atomic commit on the vault repo. CommonMark-aware tokenizer (code-block / inline-code excluded) per v1.8 precedent.
    ```

    **Replace with:**
    ```
    - [ ] **WIKI-FN-05**: `encode_slug` / `decode_slug` and the orphaned `_ADMITTED_URI_PREFIXES` scaffolding are removed from `wiki_io.entity_writer`. All consumer call sites (`link_rewriter.py`, `index_generator.py`, scanner) are rewritten to read entity `frontmatter.uri` directly or to call `short_filename(uri, collision_set, ...)` from Phase 52. Verified: `grep -rn 'encode_slug\|decode_slug' packages/ agents/ --include="*.py"` returns zero hits (excluding `.planning/` and archived milestones); full test suite passes. (Phase 53 reshape — superseded the original migrate-vault wording per `53-CONTEXT.md` D-01..D-10.)
    ```

    **WIKI-FN-06 current text (line 35):**
    ```
    - [ ] **WIKI-FN-06**: `generate_index()` writes the new short filenames in `wiki/index.md`. Existing exploratory `~/Personal/graph-wiki/agent-research` vault is re-scanned + migrated as part of milestone close.
    ```

    **Replace with:**
    ```
    - [ ] **WIKI-FN-06**: `generate_index()` and the rest of the write path emit the new short filenames per Phase 52's `write_entities` correctness. The exploratory `~/Personal/graph-wiki/agent-research` vault is regenerated from scratch by deleting `wiki/{packages,dependencies,domain,plugin,test-suites,app}/` then running `cg update --full` + `graph-wiki-agent scan`. Verified manually in `.planning/phases/53-wiki-filename-cutover/53-UAT.md`: short-form filename entries present in `wiki/index.md`; 2-3 entity files spot-checked; no `pkg__org__repo__name`-style files remain.
    ```

    Use the `Edit` tool with the full bullet text as `old_string` (including the leading `- [ ] **WIKI-FN-05**:` and `- [ ] **WIKI-FN-06**:` markers) and the new text as `new_string`. Each replacement is unique because the bullet IDs appear only once outside the traceability table.

    Do NOT modify the traceability table at lines 67-94 (WIKI-FN-05 → Phase 53 → Pending stays as-is; the status only flips when phase verification completes).

    Do NOT modify other WIKI-FN-01..04 bullets (they belong to Phase 52).
  </action>
  <verify>
    <automated>grep -c "WIKI-FN-05" .planning/REQUIREMENTS.md | xargs -I {} sh -c 'test {} -ge 2 && echo ok-05'; grep -c "WIKI-FN-06" .planning/REQUIREMENTS.md | xargs -I {} sh -c 'test {} -ge 2 && echo ok-06'; grep -F "encode_slug" .planning/REQUIREMENTS.md | head -1; grep -F "regenerated from scratch" .planning/REQUIREMENTS.md | head -1; grep -F "migrate-vault" .planning/REQUIREMENTS.md | grep -v "Phase 46\|v1.8\|historical" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -n "^- \[ \] \*\*WIKI-FN-05\*\*: " .planning/REQUIREMENTS.md` returns exactly one match.
    - Source: `grep -n "^- \[ \] \*\*WIKI-FN-06\*\*: " .planning/REQUIREMENTS.md` returns exactly one match.
    - Source: `grep -F "encode_slug" .planning/REQUIREMENTS.md` returns at least one match (the new WIKI-FN-05 verification text mentions it).
    - Source: `grep -F "regenerated from scratch" .planning/REQUIREMENTS.md` returns at least one match (the new WIKI-FN-06 verification text mentions it).
    - Source: `grep -F "migrate-vault" .planning/REQUIREMENTS.md` returns zero matches in the WIKI-FN-05 / WIKI-FN-06 bullets (the old wording is gone). Acceptable: matches elsewhere in REQUIREMENTS.md that reference `cg migrate-vault` historically (e.g. in deferred-items lists) — confirm by `grep -n -B1 -A1 "migrate-vault" .planning/REQUIREMENTS.md` showing no WIKI-FN-05/06 vicinity.
    - Source: `grep -c "single atomic commit" .planning/REQUIREMENTS.md` returns 0 (old wording gone).
    - Source: the WIKI-FN-05 → Phase 53 → Pending row in the traceability table at lines 67-94 is unchanged.
    - Behavior: the workspace builds and tests are unaffected (no code change).
  </acceptance_criteria>
  <done>
    WIKI-FN-05 and WIKI-FN-06 in `.planning/REQUIREMENTS.md` are rewritten to verification-language matching CONTEXT D-10. The traceability table is untouched. No other WIKI-FN-* bullet is modified.
  </done>
</task>

<task type="auto">
  <name>Task 53-01-02: Reshape `.planning/ROADMAP.md` §Phase 53 (strike SC #1 / #2; reframe SC #3 / #4; loosen goal)</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (target — read §Phase 53 at lines 223-233 in full; understand the goal line + 4 success criteria block)
    - .planning/phases/53-wiki-filename-cutover/53-CONTEXT.md §D-10 (canonical instructions for the rewrite)
    - .planning/phases/53-wiki-filename-cutover/53-RESEARCH.md §"Roadmap + Requirements Edits (D-10)" (canonical new text for the goal + each SC)
  </read_first>
  <action>
    Open `.planning/ROADMAP.md`. Locate §Phase 53 (around line 223). The current block is:

    ```
    ### Phase 53: Wiki Filename Cutover
    **Goal**: The existing exploratory vault's wikilinks are rewritten to use the new short filenames, the index is regenerated, and the vault is left in a fully consistent state after a single atomic operation.
    **Depends on**: Phase 52 (short filename scheme implemented and tested)
    **Requirements**: WIKI-FN-05, WIKI-FN-06
    **Success Criteria** (what must be TRUE):
      1. Running `graph-wiki-agent migrate-vault` (or equivalent cutover command) rewrites all `[[old-uri-slug-filename]]` wikilinks in curated lanes (`/concepts/`, `/adrs/`, `/architecture/`, `/work/`, `/sources/`) to `[[new-short-filename]]` in a single atomic git commit on the vault repo; code-block and inline-code spans are not modified.
      2. A second run of the cutover command is a no-op (idempotent via manifest marker), producing no new git commit.
      3. `generate_index()` writes the new short filenames in `wiki/index.md`; all entity links in the index resolve to existing files.
      4. The `~/Personal/graph-wiki/agent-research` vault is re-scanned after cutover and `graph-wiki-agent scan` completes without errors; `wiki/index.md` reflects the new short filenames throughout.
    **Plans**: TBD
    **UI hint**: no
    ```

    Replace the entire block with:

    ```
    ### Phase 53: Wiki Filename Cutover
    **Goal**: The wiki-io codebase no longer carries dead bidirectional-slug machinery (`encode_slug` / `decode_slug`); all consumer call sites read entity `frontmatter.uri` directly or call `short_filename` from Phase 52; the exploratory `~/Personal/graph-wiki/agent-research` vault is regenerated from scratch to reflect Phase 52's short filenames; UAT findings are documented in `53-UAT.md`.
    **Depends on**: Phase 52 (short filename scheme implemented and tested)
    **Requirements**: WIKI-FN-05, WIKI-FN-06
    **Scope reshape**: Per `53-CONTEXT.md` D-01..D-10 the original scope (migration command, wikilink rewriter, atomic cutover commit) was removed. No production wikis exist; single-user vault regen is manual. SC #1 and SC #2 from the original roadmap are struck; SC #3 and SC #4 are reframed below as verification observations.
    **Success Criteria** (what must be TRUE):
      1. `encode_slug` and `decode_slug` are removed from `packages/wiki-io/src/wiki_io/entity_writer.py`; `grep -rn 'encode_slug\|decode_slug' packages/ agents/ --include="*.py"` returns zero hits (excluding `.planning/` and archived milestones).
      2. Consumer call sites in `link_rewriter.py`, `index_generator.py`, and the scanner are rewritten to read `frontmatter.uri` directly or call `short_filename(uri, collision_set, ...)`. The full workspace test suite (`uv run pytest`) passes.
      3. `generate_index()` writes the new short filenames in `wiki/index.md` — verified observation from Phase 52's `write_entities` correctness; no Phase 53 code change required for this output. The `packages/wiki-io/tests/fixtures/round-trip-vault/` fixture reflects the short-form scheme.
      4. The exploratory `~/Personal/graph-wiki/agent-research` vault is manually regenerated by Pat: delete `wiki/{packages,dependencies,domain,plugin,test-suites,app}/`, run `cg update --full`, run `graph-wiki-agent scan`, inspect `wiki/index.md`. Findings recorded in `.planning/phases/53-wiki-filename-cutover/53-UAT.md`.
    **Plans**: 2 plans (TBD until planning lands)
    **UI hint**: no
    ```

    Use the `Edit` tool with the full block (from `### Phase 53: Wiki Filename Cutover` through `**UI hint**: no`) as `old_string` and the new block as `new_string`. The block is unique in the file (Phase 53 only appears once in the §Phase Details section).

    Do NOT modify the `Progress` table row for Phase 53 at line 297 (`| 53. Wiki Filename Cutover | v1.9 | 0/TBD | Not started | - |`) — that gets updated by the GSD state machine when planning completes.

    Do NOT modify the `<details>` summary block at lines 153-157 (just the high-level phase list).
  </action>
  <verify>
    <automated>awk '/^### Phase 53: Wiki Filename Cutover/{p=1} p; /^### Phase / && !/Phase 53/{p=0}' .planning/ROADMAP.md | grep -c "encode_slug\|regenerated from scratch\|frontmatter.uri" | xargs -I {} sh -c 'test {} -ge 2 && echo ok'; awk '/^### Phase 53: Wiki Filename Cutover/{p=1} p; /^### Phase / && !/Phase 53/{p=0}' .planning/ROADMAP.md | grep -c "single atomic commit\|idempotent via manifest marker" | xargs -I {} sh -c 'test {} -eq 0 && echo ok-removed'</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "^### Phase 53: Wiki Filename Cutover" .planning/ROADMAP.md` returns exactly 1.
    - Source: extract the §Phase 53 block via `awk '/^### Phase 53: Wiki Filename Cutover/{p=1} p; /^### Phase / && !/Phase 53/{p=0}'` and confirm:
      - `encode_slug` appears at least once in the block (new SC #1).
      - `regenerated from scratch` OR `manually regenerated` appears at least once (new SC #4).
      - `Scope reshape` header is present.
      - `single atomic commit` does NOT appear in the block.
      - `idempotent via manifest marker` does NOT appear in the block.
      - `migrate-vault` does NOT appear as a current Success Criterion (acceptable: appears in the "Scope reshape" sentence as the *original* feature now removed).
    - Source: the Progress table row at line 297 (`| 53. Wiki Filename Cutover | v1.9 | 0/TBD | Not started | - |`) is unchanged.
    - Source: the `<details>` v1.9 summary block at lines 153-157 is unchanged.
    - Behavior: no source code or test is affected.
  </acceptance_criteria>
  <done>
    `.planning/ROADMAP.md` §Phase 53 has a reshaped goal + 4 reframed success criteria + a "Scope reshape" header pointing at `53-CONTEXT.md` D-01..D-10. The Progress table and the milestone summary block are unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plan markdown → human reader (Pat, future contributors) | The reshaped requirements + roadmap are the contract that downstream agents (e.g. the verify-phase agent) check against. Inaccurate verification-language could let a non-cleanup state ship as "passed." Mitigated by the precise wording in `53-RESEARCH.md` and the explicit grep / pytest verification commands cited in both documents. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-01-T | Tampering | requirements/roadmap rewrite | mitigate | Edits are surgical (`Edit` tool with full bullet/block as `old_string`) and the planning artifacts are version-controlled; review at PR time catches drift. |
| T-53-01-R | Repudiation | rationale for scope shrink | mitigate | The "Scope reshape" sentence in the new ROADMAP block + the verification-language in WIKI-FN-05/06 both link back to `53-CONTEXT.md` D-01..D-10 so the decision trail is explicit. |
| T-53-01-V | Input Validation | none applicable | accept | Plan touches only checked-in markdown; no user input crosses any boundary. |
| T-53-01-I | Information Disclosure | none | accept | All edited content is non-secret project documentation. |
| T-53-01-D | Denial of Service | none | accept | Markdown edits have no runtime cost. |
| T-53-01-S | Spoofing | none | accept | No identity layer in this plan. |
</threat_model>

<verification>
After both tasks complete:

1. `grep -F "encode_slug" .planning/REQUIREMENTS.md` returns at least one match (new WIKI-FN-05 text mentions it).
2. `grep -F "regenerated from scratch" .planning/REQUIREMENTS.md` returns at least one match (new WIKI-FN-06).
3. `grep -F "single atomic commit" .planning/REQUIREMENTS.md` returns zero matches in WIKI-FN-05/06.
4. `awk '/^### Phase 53: Wiki Filename Cutover/{p=1} p; /^### Phase / && !/Phase 53/{p=0}' .planning/ROADMAP.md` extracts the new block and shows the four reframed success criteria + the Scope reshape line.
5. `uv run pytest` (or `uv run --package wiki-io pytest packages/wiki-io/tests/`) is unaffected — no code paths changed.
6. The `.planning/STATE.md` and `.planning/ROADMAP.md` Progress table for Phase 53 are unchanged (`0/TBD plans, Not started`); the GSD state machine flips these when planning completes.

This plan delivers the markdown reshape half of Phase 53 (D-10). Plan 53-02 delivers the
code cleanup half (D-04..D-07).
</verification>

<success_criteria>
- `.planning/REQUIREMENTS.md` WIKI-FN-05 is rewritten to mention `encode_slug` / `decode_slug` removal and the grep-zero verification gate.
- `.planning/REQUIREMENTS.md` WIKI-FN-06 is rewritten to mention `generate_index()`'s short-filename output (a Phase 52 consequence) and the manual vault regen + `53-UAT.md` UAT recording.
- `.planning/ROADMAP.md` §Phase 53 goal is loosened (no "single atomic operation" language) and includes a "Scope reshape" sentence referencing `53-CONTEXT.md` D-01..D-10.
- `.planning/ROADMAP.md` §Phase 53 Success Criteria 1 (was `migrate-vault` atomic rewrite) is REPLACED with the `encode_slug`/`decode_slug` removal grep criterion.
- `.planning/ROADMAP.md` §Phase 53 Success Criteria 2 (was idempotency via manifest marker) is REPLACED with the call-site rewrite + workspace test suite criterion.
- `.planning/ROADMAP.md` §Phase 53 Success Criteria 3 (was `generate_index()` short-filename emission) is REFRAMED as a verification observation flowing from Phase 52.
- `.planning/ROADMAP.md` §Phase 53 Success Criteria 4 (was vault re-scan after cutover) is REFRAMED as the manual UAT procedure.
- The REQUIREMENTS.md traceability table for WIKI-FN-05/06 is unchanged (Phase 53 → Pending stays until phase verification flips it).
- The ROADMAP.md Progress table for Phase 53 is unchanged.
- No source code is touched.
</success_criteria>

<output>
Create `.planning/phases/53-wiki-filename-cutover/53-01-SUMMARY.md` when done.
</output>
