---
phase: 51-package-family-removal-divergence-rule-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/graph-io/src/graph_io/queries.py
  - packages/graph-io/src/graph_io/uri.py
  - packages/graph-io/tests/test_uri.py
autonomous: true
requirements: [PKGFAM-01, PKGFAM-02, PKGFAM-04, PKGFAM-05]

must_haves:
  truths:
    - "`package_family` is absent from `_VALID_KINDS` in `graph_io.queries`."
    - "`package_family_uri` is no longer importable from `graph_io.uri`."
    - "`cg describe-package-family` and `cg list-package-families` are confirmed absent from `_SUBCOMMANDS` (verification-only — already absent per RESEARCH.md)."
    - "All existing `graph-io` tests still pass after deletion."
    - "Domain layer code (PKGFAM-05) is untouched — verified via regression run of `test_domain*.py` and `test_derived_edges.py`."
  artifacts:
    - path: "packages/graph-io/src/graph_io/queries.py"
      provides: "`_VALID_KINDS` frozenset without `package_family`"
      contains: "_VALID_KINDS"
    - path: "packages/graph-io/src/graph_io/uri.py"
      provides: "URI builder module without `package_family_uri`"
    - path: "packages/graph-io/tests/test_uri.py"
      provides: "URI tests with no `package_family_uri` reference + new `_VALID_KINDS` assertion"
  key_links:
    - from: "packages/graph-io/src/graph_io/queries.py"
      to: "_VALID_KINDS frozenset"
      via: "set membership"
      pattern: "_VALID_KINDS = frozenset"
    - from: "packages/graph-io/tests/test_uri.py"
      to: "graph_io.queries._VALID_KINDS"
      via: "import + negative-assertion"
      pattern: "package_family.*not in.*_VALID_KINDS"
---

<objective>
Remove `package_family` from graph-io's kind admission set and delete the `package_family_uri` builder; verify (no code change required) that `cg describe-package-family` / `cg list-package-families` are absent from the CLI subcommand registry.

Purpose: This is the foundational deletion — every downstream layer (wiki-io entity dispatch, fixture cleanup, eval-harness CLEANUP-01) depends on `package_family` being gone from the graph-io kind set first. Per CONTEXT.md D-02, no SCHEMA_VERSION bump or pre-flight migration is added; stale graph rows are user-rebuild territory (`cg update --full`).

Output:
- `packages/graph-io/src/graph_io/queries.py` with `"package_family"` removed from `_VALID_KINDS`
- `packages/graph-io/src/graph_io/uri.py` with `package_family_uri` deleted
- `packages/graph-io/tests/test_uri.py` with the `test_package_family_uri` test removed AND a new assertion that `"package_family" not in _VALID_KINDS`
- Verification grep confirming PKGFAM-04 (no CLI subcommands to delete)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/51-package-family-removal-divergence-rule-cleanup/51-CONTEXT.md
@.planning/phases/51-package-family-removal-divergence-rule-cleanup/51-RESEARCH.md
@.planning/phases/51-package-family-removal-divergence-rule-cleanup/51-VALIDATION.md
@packages/graph-io/src/graph_io/queries.py
@packages/graph-io/src/graph_io/uri.py
@packages/graph-io/src/graph_io/cli/main.py
@packages/graph-io/tests/test_uri.py
</context>

<tasks>

<task type="auto">
  <name>Task 51-01-01: Delete `package_family` from `_VALID_KINDS` and add negative-assertion test</name>
  <files>packages/graph-io/src/graph_io/queries.py, packages/graph-io/tests/test_uri.py</files>
  <read_first>
    - packages/graph-io/src/graph_io/queries.py (target — read full `_VALID_KINDS` definition at lines 9-29)
    - packages/graph-io/tests/test_uri.py (target — find an appropriate place to add the negative assertion)
    - .planning/phases/51-package-family-removal-divergence-rule-cleanup/51-RESEARCH.md §"Code Examples" — `_VALID_KINDS` removal (PKGFAM-01)
    - .planning/phases/51-package-family-removal-divergence-rule-cleanup/51-CONTEXT.md §D-02 (no SCHEMA_VERSION bump; no pre-flight check)
  </read_first>
  <action>
    Remove the literal string `"package_family"` entry from the `_VALID_KINDS` frozenset in `packages/graph-io/src/graph_io/queries.py` (currently at line 9-29 per RESEARCH.md). Leave all other entries (`function`, `class`, `method`, `file`, `package`, `repository`, `subpackage`, `entry_point`, `test_suite`, `domain`, `dependency`, `plugin`, `builtin`, `app`) unchanged. Do NOT bump `SCHEMA_VERSION`, do NOT add a pre-flight scan, do NOT add a migration command (per D-02). Add a new test function `test_valid_kinds_excludes_package_family` to `packages/graph-io/tests/test_uri.py` that imports `_VALID_KINDS` from `graph_io.queries` and asserts `"package_family" not in _VALID_KINDS`. Use `from graph_io.queries import _VALID_KINDS` (note the underscore — this is module-private but tests are allowed to import it; verify the existing test file's import style first).
  </action>
  <verify>
    <automated>uv run --package graph-io pytest packages/graph-io/tests/test_uri.py -x</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -E '^\s*"package_family"' packages/graph-io/src/graph_io/queries.py` returns zero lines.
    - Source: `grep -n "package_family.*not in.*_VALID_KINDS\|_VALID_KINDS.*not.*package_family" packages/graph-io/tests/test_uri.py` returns the new assertion.
    - Behavior: `pytest packages/graph-io/tests/test_uri.py -x` passes, including the new `test_valid_kinds_excludes_package_family` test.
    - Behavior: No `SCHEMA_VERSION` change in `packages/graph-io/src/graph_io/` (`git diff packages/graph-io | grep -i SCHEMA_VERSION` returns empty).
  </acceptance_criteria>
  <done>
    `_VALID_KINDS` no longer contains `"package_family"`; new negative-assertion test passes; no schema-version bump introduced.
  </done>
</task>

<task type="auto">
  <name>Task 51-01-02: Delete `package_family_uri` builder and its test</name>
  <files>packages/graph-io/src/graph_io/uri.py, packages/graph-io/tests/test_uri.py</files>
  <read_first>
    - packages/graph-io/src/graph_io/uri.py (target — read `package_family_uri` at lines 49-50 plus any docstring)
    - packages/graph-io/tests/test_uri.py (target — read lines 16 and 82-83 for the import and test function)
    - .planning/phases/51-package-family-removal-divergence-rule-cleanup/51-RESEARCH.md §"Phase Requirements" PKGFAM-02 (single import outside `uri.py` is `tests/test_uri.py`)
    - .planning/phases/51-package-family-removal-divergence-rule-cleanup/51-CONTEXT.md (canonical_refs — graph-io deletions section)
  </read_first>
  <action>
    Delete the `package_family_uri` function definition from `packages/graph-io/src/graph_io/uri.py` (lines 49-50 per RESEARCH.md). If a module-level `__all__` exists and lists `package_family_uri`, remove it from `__all__`. In `packages/graph-io/tests/test_uri.py`, delete the import of `package_family_uri` (line 16 per RESEARCH.md) and the entire `test_package_family_uri` test function (lines 82-83 per RESEARCH.md). Do not touch any other URI builders — verify with grep that no other module imports `package_family_uri` before deleting (RESEARCH.md confirms zero other importers, but re-verify at execution time).
  </action>
  <verify>
    <automated>uv run --package graph-io pytest packages/graph-io/tests/test_uri.py -x</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -rn "package_family_uri" packages/graph-io/` returns zero lines (excluding planning docs).
    - Source: `grep -rn "package_family_uri" packages/` returns zero lines outside `.planning/`.
    - Behavior: `python -c "from graph_io.uri import package_family_uri"` raises `ImportError`.
    - Behavior: `pytest packages/graph-io/tests/test_uri.py -x` passes (no `test_package_family_uri` reference remains).
  </acceptance_criteria>
  <done>
    `package_family_uri` is gone from `graph_io.uri`; its test is removed; no other module references it.
  </done>
</task>

<task type="auto">
  <name>Task 51-01-03: Verify PKGFAM-04 (CLI subcommand absence) and run graph-io regression suite</name>
  <files>packages/graph-io/tests/test_cli_main.py (optional new assertion file)</files>
  <read_first>
    - packages/graph-io/src/graph_io/cli/main.py (target — read `_SUBCOMMANDS` table lines 45-77)
    - .planning/phases/51-package-family-removal-divergence-rule-cleanup/51-RESEARCH.md §"Phase Requirements" PKGFAM-04 (VERIFIED: neither subcommand exists)
    - .planning/phases/51-package-family-removal-divergence-rule-cleanup/51-VALIDATION.md §"Wave 0 Requirements" (optional `test_no_package_family_subcommand`)
  </read_first>
  <action>
    Verify that `cg describe-package-family` and `cg list-package-families` are NOT in `_SUBCOMMANDS` at `packages/graph-io/src/graph_io/cli/main.py:45-77` (RESEARCH.md confirms absence — this task is verification-only; no code deletion expected). Run a grep gate: `grep -nE 'describe-package-family|list-package-families' packages/graph-io/src/graph_io/cli/` must return zero lines. Optionally add a small belt-and-suspenders test to `packages/graph-io/tests/test_cli_main.py` (create the file if it does not exist) named `test_no_package_family_subcommand` that imports `_SUBCOMMANDS` from `graph_io.cli.main` and asserts neither key exists. Then run the full graph-io regression suite to confirm PKGFAM-05 (domain layer unaffected): `pytest packages/graph-io/tests/test_domain*.py packages/graph-io/tests/test_derived_edges.py -x` (skip silently if those test paths do not exist; the broader suite covers them). Finally run `pytest packages/graph-io/tests/ -x` to confirm the entire graph-io package is green after Tasks 51-01-01 and 51-01-02.
  </action>
  <verify>
    <automated>uv run --package graph-io pytest packages/graph-io/tests/ -x</automated>
  </verify>
  <acceptance_criteria>
    - CLI: `grep -nE 'describe-package-family|list-package-families' packages/graph-io/src/graph_io/cli/` returns zero lines.
    - Behavior: `pytest packages/graph-io/tests/ -x` passes (all graph-io tests including domain + derived-edge regressions).
    - Behavior (PKGFAM-05): no test file under `packages/graph-io/tests/test_domain*.py` or `test_derived_edges.py` was modified by this plan (`git diff --name-only packages/graph-io/tests/ | grep -E 'domain|derived'` returns empty).
  </acceptance_criteria>
  <done>
    CLI subcommand absence verified via grep (no code change needed per RESEARCH.md); full graph-io suite passes; domain layer is untouched (regression-only verification of PKGFAM-05).
  </done>
</task>

</tasks>

<verification>
After all 3 tasks complete:

1. `uv run --package graph-io pytest packages/graph-io/tests/ -x` — full graph-io suite green.
2. `grep -rn "package_family\|package_family_uri" packages/graph-io/` — zero hits in `.py` files.
3. `grep -nE 'describe-package-family|list-package-families' packages/graph-io/src/graph_io/cli/` — zero hits.
4. `git diff packages/graph-io/src/graph_io/ | grep -i SCHEMA_VERSION` — empty (D-02 honored).

This plan is the foundation for Plan 02 (wiki-io) — `_VALID_KINDS` is the single source of truth and must be cleaned first so wiki-io's deletion does not collide with a stale upstream admission.
</verification>

<success_criteria>
- `_VALID_KINDS` frozenset in `graph_io.queries` no longer contains `"package_family"`.
- `package_family_uri` function is deleted from `graph_io.uri`; no remaining importers in `packages/`.
- `cg describe-package-family` and `cg list-package-families` are verified absent from `_SUBCOMMANDS` (PKGFAM-04 satisfied by grep gate; no code deletion).
- New negative-assertion test `test_valid_kinds_excludes_package_family` is present and passing.
- All existing graph-io tests pass after deletions.
- Domain-layer code (PKGFAM-05) is unmodified; domain tests pass as a regression check.
- No `SCHEMA_VERSION` bump; no pre-flight check; no migration command (D-02 honored).
</success_criteria>

<output>
Create `.planning/phases/51-package-family-removal-divergence-rule-cleanup/51-01-SUMMARY.md` when done.
</output>
