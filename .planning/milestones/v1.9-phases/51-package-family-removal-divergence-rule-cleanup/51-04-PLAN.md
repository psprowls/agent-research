---
phase: 51-package-family-removal-divergence-rule-cleanup
plan: 04
type: execute
wave: 3
depends_on: [51-02]
files_modified:
  - packages/wiki-io/tests/fixtures/round-trip-vault/.templates/package-family.md
  - packages/wiki-io/tests/fixtures/round-trip-vault/plugins/lattice-wiki/patterns.md
  - packages/wiki-io/tests/fixtures/round-trip-vault/plugins/lattice-wiki/context.md
  - packages/wiki-io/tests/fixtures/round-trip-vault/packages/lattice-wiki-core/overview.md
  - packages/wiki-io/tests/fixtures/round-trip-vault/concepts/code-wiki-pattern.md
  - packages/wiki-io/tests/fixtures/round-trip-vault/concepts/index.md
  - packages/wiki-io/tests/fixtures/round-trip-vault/concepts/lattice-vault-terminology.md
  - packages/wiki-io/tests/fixtures/round-trip-vault/concepts/lattice-dependencies-tiering.md
  - packages/wiki-io/tests/fixtures/round-trip-vault/sources/2026-05-lattice-wiki-core-tokens-frontmatter-field.md
  - packages/wiki-io/tests/fixtures/round-trip-vault/.graph-wiki/bm25/vocab.index.json
  - packages/wiki-io/tests/fixtures/round-trip-vault/.graph-wiki/bm25/vocab.tokenizer.json
autonomous: true
requirements: [PKGFAM-03]

must_haves:
  truths:
    - "The `round-trip-vault` fixture contains zero `package_family` / `package-family` references in markdown files."
    - "`.templates/package-family.md` is deleted from the fixture."
    - "`vocab.index.json` and `vocab.tokenizer.json` are regenerated via a BM25-only script (no Bedrock required per RESEARCH.md Pitfall 5)."
    - "`packages/wiki-io/tests/test_round_trip.py` passes against the regenerated fixture — the second-pass byte-equality invariant holds."
    - "Non-package-family coverage in the fixture is preserved (D-03: surgical edits only)."
  artifacts:
    - path: "packages/wiki-io/tests/fixtures/round-trip-vault/.graph-wiki/bm25/vocab.index.json"
      provides: "Regenerated BM25 vocab index consistent with the post-edit corpus"
    - path: "packages/wiki-io/tests/fixtures/round-trip-vault/.graph-wiki/bm25/vocab.tokenizer.json"
      provides: "Regenerated BM25 tokenizer vocab consistent with the post-edit corpus"
  key_links:
    - from: "packages/wiki-io/tests/fixtures/round-trip-vault/.graph-wiki/bm25/vocab.*.json"
      to: "fixture markdown corpus"
      via: "bm25s tokenizer + retriever derived from the edited markdowns"
      pattern: "vocab"
---

<objective>
Surgically scrub `package_family` references from the `round-trip-vault` test fixture's markdown files, delete the in-fixture `.templates/package-family.md`, and regenerate the BM25 vocab JSONs via an inline Bedrock-free script. After this plan, the round-trip test passes against a fixture that contains no `package_family` mentions but otherwise retains all non-package-family coverage (D-03).

Purpose: Closes the fixture coupling identified in Plan 51-02 Task 03 (the round-trip test failure deferred to this plan) and finishes PKGFAM-03 on the fixture side. Runs in Wave 3 parallel with Plan 51-03 because the two plans touch disjoint trees (`wiki-io/tests/fixtures/` vs `eval-harness/`). Per RESEARCH.md Pitfall 5, BM25 vocab regeneration does NOT require Bedrock; only the search.db embedding step does, and search.db is accepted as drift (Pitfall 5 / Assumption A3).

Output:
- `.templates/package-family.md` deleted from the fixture
- Plain-text `package_family` / `package-family` mentions stripped from concept/source/overview/patterns/context markdowns (surgical line edits, not whole-section rewrites)
- `vocab.index.json` and `vocab.tokenizer.json` regenerated via a BM25-only one-shot script (no Bedrock)
- `test_round_trip.py` passing against the regenerated fixture
- Phase exit gate G1 grep passes (zero `package_family|package-family|PKGFAM|package_family_uri` hits across `packages/` excluding planning + migration-log files)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/51-package-family-removal-divergence-rule-cleanup/51-CONTEXT.md
@.planning/phases/51-package-family-removal-divergence-rule-cleanup/51-RESEARCH.md
@.planning/phases/51-package-family-removal-divergence-rule-cleanup/51-VALIDATION.md
@.planning/phases/51-package-family-removal-divergence-rule-cleanup/51-02-PLAN.md
@packages/wiki-io/tests/test_round_trip.py
@packages/wiki-io/tests/conftest.py
@agents/graph-wiki-agent/src/graph_wiki_agent/commands/query.py
</context>

<tasks>

<task type="auto">
  <name>Task 51-04-01: Surgical markdown edits + delete fixture template</name>
  <files>packages/wiki-io/tests/fixtures/round-trip-vault/.templates/package-family.md, packages/wiki-io/tests/fixtures/round-trip-vault/plugins/lattice-wiki/patterns.md, packages/wiki-io/tests/fixtures/round-trip-vault/plugins/lattice-wiki/context.md, packages/wiki-io/tests/fixtures/round-trip-vault/packages/lattice-wiki-core/overview.md, packages/wiki-io/tests/fixtures/round-trip-vault/concepts/code-wiki-pattern.md, packages/wiki-io/tests/fixtures/round-trip-vault/concepts/index.md, packages/wiki-io/tests/fixtures/round-trip-vault/concepts/lattice-vault-terminology.md, packages/wiki-io/tests/fixtures/round-trip-vault/concepts/lattice-dependencies-tiering.md, packages/wiki-io/tests/fixtures/round-trip-vault/sources/2026-05-lattice-wiki-core-tokens-frontmatter-field.md</files>
  <read_first>
    - .planning/phases/51-package-family-removal-divergence-rule-cleanup/51-CONTEXT.md §D-03 (surgical fixture edits; preserve non-package-family coverage)
    - .planning/phases/51-package-family-removal-divergence-rule-cleanup/51-RESEARCH.md §"Layer 4: fixture surgical edits" diagram (full inventory)
    - packages/wiki-io/tests/fixtures/round-trip-vault/.templates/package-family.md (target — delete)
    - Each markdown file above (target — open and locate `package_family` / `package-family` mentions in situ)
    - packages/wiki-io/tests/test_round_trip.py (read to understand which fixture properties the test checks — file presence, frontmatter, etc.)
  </read_first>
  <action>
    Use grep first to enumerate all fixture lines: `grep -rn "package_family\|package-family" packages/wiki-io/tests/fixtures/round-trip-vault/`. Use this as the working inventory — RESEARCH.md's list is from a prior grep and may be incomplete.

    1. Delete the template: `rm packages/wiki-io/tests/fixtures/round-trip-vault/.templates/package-family.md`. Also delete any in-fixture `wiki/package-family/` directory if present (`find packages/wiki-io/tests/fixtures/round-trip-vault -type d -name "package-family" -exec rm -rf {} +`). Note: per D-01 the EXTERNAL vault is untouched — this is fixture-only deletion.

    2. For each markdown file listed in `files_modified`, perform SURGICAL line edits:
       - Remove standalone `package_family` / `package-family` mentions when they appear as list items or bare-line references.
       - When they appear in a sentence (e.g., "...includes `package_family` and `package` kinds..."), rewrite the sentence to drop just the `package_family` token. Preserve the surrounding sentence intact.
       - When they appear in a frontmatter array (e.g., `kinds: [package, package-family, plugin]`), remove only the `package-family` entry.
       - DO NOT delete entire paragraphs, sections, or files unless the entire paragraph is dedicated to `package_family`.

    3. After editing, run a final grep to confirm cleanup: `grep -rn "package_family\|package-family" packages/wiki-io/tests/fixtures/round-trip-vault/ --include="*.md"` must return zero lines (ignore the `.graph-wiki/bm25/vocab.*` JSON files for now — those will be regenerated in Task 51-04-02).

    4. Do NOT run `test_round_trip.py` yet — the regenerated BM25 files in Task 51-04-02 are required for the test to pass.
  </action>
  <verify>
    <automated>! grep -rn "package_family\|package-family" packages/wiki-io/tests/fixtures/round-trip-vault/ --include="*.md" | grep -q .</automated>
  </verify>
  <acceptance_criteria>
    - Source: `test -f packages/wiki-io/tests/fixtures/round-trip-vault/.templates/package-family.md` returns false.
    - Source: `grep -rn "package_family\|package-family" packages/wiki-io/tests/fixtures/round-trip-vault/ --include="*.md"` returns zero lines.
    - Source: `find packages/wiki-io/tests/fixtures/round-trip-vault -type d -name "package-family"` returns empty (in-fixture dirs gone; external vault untouched per D-01).
    - Coverage preservation: each modified markdown file still parses as valid frontmatter+body (verify with `python -c "import frontmatter, sys; [frontmatter.load(p) for p in sys.argv[1:]]" <each-edited-file>`).
  </acceptance_criteria>
  <done>
    All `package_family` / `package-family` mentions are removed from fixture markdowns via surgical edits; `.templates/package-family.md` is deleted; non-package-family fixture coverage is preserved; frontmatter parses cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 51-04-02: BM25-only regen of `vocab.index.json` + `vocab.tokenizer.json`, then run round-trip test</name>
  <files>packages/wiki-io/tests/fixtures/round-trip-vault/.graph-wiki/bm25/vocab.index.json, packages/wiki-io/tests/fixtures/round-trip-vault/.graph-wiki/bm25/vocab.tokenizer.json</files>
  <read_first>
    - .planning/phases/51-package-family-removal-divergence-rule-cleanup/51-RESEARCH.md §"Common Pitfalls" Pitfall 5 (BM25-only script is safe; search.db drift is acceptable per Assumption A3)
    - .planning/phases/51-package-family-removal-divergence-rule-cleanup/51-RESEARCH.md §"Code Examples" — "BM25-only fixture regen" snippet (the exact pattern to follow)
    - agents/graph-wiki-agent/src/graph_wiki_agent/commands/query.py (READ ONLY — lines 739-810 are the canonical `build_index()` BM25 portion; the script mirrors lines 753-769 specifically)
    - packages/wiki-io/tests/test_round_trip.py (target test — read to understand the byte-equality invariant the regen must preserve)
  </read_first>
  <action>
    Regenerate the two BM25 vocab JSONs using a BM25-only one-shot script (no Bedrock, no search.db). Per RESEARCH.md Pitfall 5, the BM25 path inside `build_index()` (lines 753-769 of `query.py`) is fully self-contained — it only uses `bm25s.Tokenizer` + `bm25s.BM25` + `save_vocab()`.

    Write an inline Python script (do not commit it; run it once, then discard) that:

    1. Imports `bm25s` and `pathlib.Path`.
    2. Sets `FIXTURE = Path("packages/wiki-io/tests/fixtures/round-trip-vault")` and `BM25_DIR = FIXTURE / ".graph-wiki" / "bm25"`.
    3. Discovers all `.md` files via `FIXTURE.rglob("*.md")` using the SAME filtering rules as `query.py::_discover_pages`:
       - Skip files named `index.md` or `log.md`.
       - Skip any path with a part starting with `.` (so `.templates/`, `.graph-wiki/`, etc. are excluded).
    4. Reads each file's text content; builds two parallel lists `corpus_paths` (relative POSIX-style paths) and `corpus_texts`.
    5. Instantiates `tokenizer = bm25s.Tokenizer(stopwords="english", stemmer=None)`.
    6. Tokenizes via `corpus_tokens = tokenizer.tokenize(corpus_texts, return_as="tuple")`.
    7. Instantiates `retriever = bm25s.BM25(method="lucene", k1=1.5, b=0.75)`, calls `retriever.index(corpus_tokens)`.
    8. Calls `retriever.save(str(BM25_DIR), corpus=corpus_paths)`, `tokenizer.save_vocab(str(BM25_DIR))`, `tokenizer.save_stopwords(str(BM25_DIR))`.

    These calls write `vocab.index.json` and `vocab.tokenizer.json` (plus `params.json`, `stopwords.txt`, and the index files). The exact filenames depend on the bm25s version (0.3.8 per CLAUDE.md); verify the produced filenames match what was already committed in the fixture before the edits. If the bm25s version produces additional sibling files that were already committed, leave them; if it omits files that were committed, investigate (probably means library version drift).

    Do NOT touch the fixture's `search.db` — per Pitfall 5 / Assumption A3 the embedded vectors are stale but no test reads embedding content for correctness.

    After regeneration, run the round-trip test:
    `uv run --package wiki-io pytest packages/wiki-io/tests/test_round_trip.py -x`

    The test performs a two-pass `update_vault` against the fixture and asserts byte-equality between passes (idempotency). If it fails, common causes:
    - bm25s version drift between regen environment and CI expectations (verify locally pinned version)
    - search.db content-coupling that RESEARCH.md A3 missed (read the test source carefully)
    - A markdown edit in Task 51-04-01 broke frontmatter parsing

    Finally, run BOTH phase exit gates:

    Gate G1: `! grep -rE "package_family|package-family|PKGFAM|package_family_uri" packages/ --include="*.py" --include="*.md" --include="*.json" --include="*.toml" | grep -v ".planning/" | grep -v "migration.log" | grep -q .`

    Gate G2: `! grep -rn "_SLUG_ONLY_RE|LIB-003" packages/eval-harness/ | grep -q .` AND `! grep -n "_check_no_slug_only_wikilinks" packages/eval-harness/src/eval_harness/divergence/librarian.py | grep -q .`

    Both must pass (return exit code 0 from the `!`-prefixed form). If G1 fails, investigate which file still contains the token and either delete it (legitimate residue) or add it to the exclusion list (genuine migration-log artifact).
  </action>
  <verify>
    <automated>uv run --package wiki-io pytest packages/wiki-io/tests/test_round_trip.py -x</automated>
  </verify>
  <acceptance_criteria>
    - Source: `python -c "import json; json.load(open('packages/wiki-io/tests/fixtures/round-trip-vault/.graph-wiki/bm25/vocab.index.json'))"` succeeds (valid JSON).
    - Source: `python -c "import json; json.load(open('packages/wiki-io/tests/fixtures/round-trip-vault/.graph-wiki/bm25/vocab.tokenizer.json'))"` succeeds (valid JSON).
    - Behavior: `pytest packages/wiki-io/tests/test_round_trip.py -x` passes (two-pass byte-equality invariant holds against the regenerated fixture).
    - Behavior: `pytest packages/wiki-io/tests/ -x` (full wiki-io suite) passes — closes the deferral from Plan 51-02 Task 03.
    - Phase gate G1: `bash -c '! grep -rE "package_family|package-family|PKGFAM|package_family_uri" packages/ --include="*.py" --include="*.md" --include="*.json" --include="*.toml" | grep -v ".planning/" | grep -v "migration.log" | grep -q .'` exits 0.
    - Phase gate G2: `bash -c '! grep -rn "_SLUG_ONLY_RE|LIB-003" packages/eval-harness/ | grep -q .'` exits 0 AND `bash -c '! grep -n "_check_no_slug_only_wikilinks" packages/eval-harness/src/eval_harness/divergence/librarian.py | grep -q .'` exits 0.
  </acceptance_criteria>
  <done>
    BM25 vocab JSONs are regenerated from the edited fixture corpus via a Bedrock-free script; round-trip test passes; full wiki-io suite green; phase exit gates G1 and G2 both pass.
  </done>
</task>

</tasks>

<verification>
After both tasks complete, the phase is fully done. Run the complete verification battery:

1. `uv run --package wiki-io pytest packages/wiki-io/tests/ -x` — full wiki-io green (including `test_round_trip`).
2. `uv run --package graph-io pytest packages/graph-io/tests/ -x` — full graph-io green (Plan 01 verification).
3. `uv run --package eval-harness pytest packages/eval-harness/tests/ -x` — full eval-harness green (Plan 03 verification).
4. Phase exit gate G1 (success criterion #1):
   ```
   ! grep -rE "package_family|package-family|PKGFAM|package_family_uri" packages/ \
     --include="*.py" --include="*.md" --include="*.json" --include="*.toml" \
     | grep -v ".planning/" | grep -v "migration.log" | grep -q .
   ```
5. Phase exit gate G2 (success criterion #5):
   ```
   ! grep -rn "_SLUG_ONLY_RE|LIB-003" packages/eval-harness/ | grep -q .
   ! grep -n "_check_no_slug_only_wikilinks" packages/eval-harness/src/eval_harness/divergence/librarian.py | grep -q .
   ```
6. SYN-002 sanity: `grep -c "_check_no_slug_only_wikilinks" packages/eval-harness/src/eval_harness/divergence/synthesizer.py` returns >= 1 (independent check retained per Pitfall 1).
</verification>

<success_criteria>
- `.templates/package-family.md` is deleted from the fixture.
- Fixture markdowns contain zero `package_family` / `package-family` references.
- `vocab.index.json` and `vocab.tokenizer.json` are regenerated from the edited corpus via a Bedrock-free script.
- `test_round_trip.py` passes against the regenerated fixture (two-pass byte-equality holds).
- Full wiki-io test suite passes.
- Phase exit gate G1 passes (zero `package_family|package-family|PKGFAM|package_family_uri` hits in `packages/` outside planning + migration-log files).
- Phase exit gate G2 passes (zero `_SLUG_ONLY_RE|LIB-003` hits in `packages/eval-harness/`; zero `_check_no_slug_only_wikilinks` in librarian.py specifically; SYN-002 in synthesizer.py is retained).
- The external vault at `~/Personal/graph-wiki/agent-research` is UNTOUCHED (D-01 honored — deferred to Phase 53).
</success_criteria>

<output>
Create `.planning/phases/51-package-family-removal-divergence-rule-cleanup/51-04-SUMMARY.md` when done.
</output>
