---
phase: 38-graph-wiki-agent-graph-subcommand
plan: 02
type: execute
wave: 2
depends_on: [38-01]
files_modified:
  - agents/graph-wiki-agent/src/graph_wiki_agent/mcp/server.py
  - agents/graph-wiki-agent/tests/unit/test_mcp_graph_tools.py
autonomous: true
requirements:
  - GRAPHCMD-04

must_haves:
  truths:
    - "`agents/graph-wiki-agent/src/graph_wiki_agent/mcp/server.py` registers exactly 3 new MCP tools via `@mcp.tool(...)`: `graph_build`, `graph_describe`, `graph_query` (SC#4 / GRAPHCMD-04)"
    - "Existing `wiki_*` MCP tools (`wiki_ping`, `wiki_query`, `wiki_log`, `wiki_bootstrap`, `wiki_scan`, `wiki_ingest`, `wiki_lint`) remain registered and importable (regression guard)"
    - "Each new tool has paired Pydantic Input + Output models with `model_config = ConfigDict(extra='forbid')` on the Input class (mirrors existing `WikiQueryInput` shape)"
    - "`GraphDescribeInput.kind` is a `Literal['package','path','repository','domain','entry_point','test_suite']` Pydantic field (D-09); `kind='bogus'` is rejected at the Pydantic validation layer with `ValidationError`"
    - "`GraphDescribeInput.identifier` is `str | None` (optional for `kind='repository'`, required-by-adapter for other kinds — adapter returns clean exit_code=2 stderr error when identifier is missing for a non-repository kind)"
    - "`graph_build` tool function calls into `commands.graph._capture_run(ops_update, _build_namespace(ops_update, repo=..., workspace=..., full=...))` — REUSES Plan 01's helpers, does NOT re-implement the namespace builder or capture logic"
    - "`graph_describe` tool function uses `commands.graph._DESCRIBE_DISPATCH[kind]` to find the right cg module + identifier attr, then calls `_capture_run` — same dispatch table as the CLI side (D-09 cross-surface parity)"
    - "`graph_query` tool function calls `_capture_run(q_find, ...)` with the Phase 36 named-flag shape (`name`, `kind`, `in_package`); returns a clean error in `stderr`/`exit_code=2` if all three are None (mirrors the CLI Typer-layer pre-validation)"
    - "All 3 MCP tools wrap their `_capture_run` call so that the cg modules' `print()` output is captured via `contextlib.redirect_stdout` (inside `_capture_run` from Plan 01) and NEVER reaches the actual MCP server stdout — _StdoutGuard is never tripped during normal operation"
    - "`GraphCommandOutput` uniform shape: `status: Literal['success','error']`, `exit_code: int`, `stdout: str`, `stderr: str`, `trace_path: str | None` (the absolute path of the trace file when `trace=True`, else None)"
    - "When `trace=True` on any of the 3 MCP tools, the same `commands.graph._write_trace_record` helper writes records to `<workspace>/.graph-wiki/traces/<ISO-Z>-<command>.jsonl`; the path is returned in `GraphCommandOutput.trace_path`"
    - "`uv run --package graph-wiki-agent pytest agents/graph-wiki-agent/tests/unit/test_mcp_graph_tools.py -q` exits 0"
    - "`uv run --package graph-wiki-agent pytest agents/graph-wiki-agent/tests/ -q` exits 0 (no regressions)"
    - "`uv run --package graph-wiki-agent python -c \"from graph_wiki_agent.mcp.server import mcp\"` exits 0 (server module imports cleanly with _StdoutGuard active)"
  artifacts:
    - path: agents/graph-wiki-agent/src/graph_wiki_agent/mcp/server.py
      provides: 3 new MCP tools (graph_build, graph_describe, graph_query) using Plan 01's helpers
      contains: "graph_build"
    - path: agents/graph-wiki-agent/tests/unit/test_mcp_graph_tools.py
      provides: 9 unit tests covering tool registration, input validation, dispatch, stdout-guard safety, error packaging
      contains: "test_three_graph_tools_registered"
      min_lines: 250
  key_links:
    - from: agents/graph-wiki-agent/src/graph_wiki_agent/mcp/server.py
      to: agents/graph-wiki-agent/src/graph_wiki_agent/commands/graph.py
      via: "from graph_wiki_agent.commands.graph import _build_namespace, _capture_run, _write_trace_record, _DESCRIBE_DISPATCH, _resolve_paths, _iso_utc_timestamp, _trace_path, _iso_utc_record_timestamp, ops_update, q_find"
      pattern: "from graph_wiki_agent.commands.graph import"
    - from: agents/graph-wiki-agent/tests/unit/test_mcp_graph_tools.py
      to: agents/graph-wiki-agent/src/graph_wiki_agent/mcp/server.py
      via: "direct import of the @mcp.tool functions for async invocation in tests"
      pattern: "from graph_wiki_agent.mcp.server import"
---

<objective>
Register exactly 3 new MCP tools — `graph_build`, `graph_describe`, `graph_query` — in `agents/graph-wiki-agent/src/graph_wiki_agent/mcp/server.py`, mirroring the existing `wiki_*` tool patterns (Pydantic Input + Output models + `@mcp.tool` decorator). Each tool's body is THIN: resolve workspace, optionally compute a trace timestamp, call into the helpers from Plan 01's `commands/graph.py` (`_DESCRIBE_DISPATCH`, `_build_namespace`, `_capture_run`, `_write_trace_record`), and pack the result into a uniform `GraphCommandOutput` model. The MCP path MUST NOT trip `_StdoutGuard` — all cg `print()` output is captured inside `_capture_run` and forwarded into the `stdout` field of the output model.

Output: `mcp/server.py` modifications (3 new tools + paired Pydantic models, ~90 lines added near the bottom before `def main()`), plus a net-new test file `tests/unit/test_mcp_graph_tools.py` (~250 lines).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/38-graph-wiki-agent-graph-subcommand/38-CONTEXT.md
@.planning/phases/38-graph-wiki-agent-graph-subcommand/38-RESEARCH.md
@.planning/phases/38-graph-wiki-agent-graph-subcommand/38-VALIDATION.md

# HARD DEPENDENCY — Plan 38-01 must be merged on the same branch:
@.planning/phases/38-graph-wiki-agent-graph-subcommand/38-01-PLAN.md

# Files written by Plan 38-01 (Plan 02 imports from these — do NOT modify):
@agents/graph-wiki-agent/src/graph_wiki_agent/commands/graph.py

# MCP server pattern reference (do NOT modify the existing wiki_* tools):
@agents/graph-wiki-agent/src/graph_wiki_agent/mcp/server.py

# Existing test patterns:
@agents/graph-wiki-agent/tests/unit/test_cli_help.py

<interfaces>
<!-- Helpers Plan 02 imports from Plan 01 (must exist by the time this plan runs): -->
From agents/graph-wiki-agent/src/graph_wiki_agent/commands/graph.py:
  graph_app: typer.Typer
  graph_describe_app: typer.Typer
  _DESCRIBE_DISPATCH: dict[str, tuple[ModuleType, str | None]]
  _build_namespace(module, *, repo: Path, workspace: Path, **extras) -> argparse.Namespace
  _capture_run(module, args: argparse.Namespace) -> tuple[int, str, str]
  _resolve_paths(workspace_arg: str) -> tuple[Path, Path]
  _write_trace_record(trace_dir, event, command, args_dict, exit_code, duration_ms, *, model_id=None, extra=None) -> None
  _iso_utc_timestamp() -> str   # filename-safe (no colons)
  _trace_path(workspace, command, shared_stamp) -> Path
  # Re-exported cg modules (already imported in graph.py):
  ops_update, q_find, q_describe_package, q_describe_path, q_describe_repo, q_describe_domain, q_describe_entry_point, q_describe_suite

From mcp.server.fastmcp (already imported in server.py):
  FastMCP, Context

From pydantic (already imported):
  BaseModel, ConfigDict, Field
  # AND additionally needed by this plan:
  Literal  # from typing
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Register 3 `graph_*` MCP tools in `mcp/server.py` (GRAPHCMD-04, D-04, D-05, D-09)</name>
  <files>agents/graph-wiki-agent/src/graph_wiki_agent/mcp/server.py</files>
  <read_first>agents/graph-wiki-agent/src/graph_wiki_agent/mcp/server.py, agents/graph-wiki-agent/src/graph_wiki_agent/commands/graph.py, .planning/phases/38-graph-wiki-agent-graph-subcommand/38-CONTEXT.md, .planning/phases/38-graph-wiki-agent-graph-subcommand/38-RESEARCH.md</read_first>
  <behavior>
    - The new tools are added at the BOTTOM of `server.py`, AFTER the existing `wiki_lint` tool block (line 411 in the pre-Phase-38 file) and BEFORE `def main()` (line 448).
    - New Pydantic models: `GraphBuildInput`, `GraphDescribeInput`, `GraphQueryInput`, `GraphCommandOutput` (uniform output).
    - `GraphBuildInput`: `full: bool = False`, `trace: bool = False`, `model: str | None = None`, `workspace_path: str = ""` — all with `Field(...)` descriptions.
    - `GraphDescribeInput`: `kind: Literal["package", "path", "repository", "domain", "entry_point", "test_suite"]` (required), `identifier: str | None = None`, `trace: bool = False`, `workspace_path: str = ""`.
    - `GraphQueryInput`: `name: str | None = None`, `kind: str | None = None`, `in_package: str | None = None`, `trace: bool = False`, `workspace_path: str = ""`.
    - `GraphCommandOutput`: `status: Literal["success", "error"]`, `exit_code: int`, `stdout: str`, `stderr: str`, `trace_path: str | None = None`.
    - Each tool function is async (mirrors `wiki_query` at line 125 of server.py); accepts `(input: <Input>, ctx: Context)` (the `ctx` is used by FastMCP for progress reports — optional for cg-proxy tools but kept for parity with `wiki_*` tools).
    - Each tool body imports the Plan 01 helpers via a single line: `from graph_wiki_agent.commands import graph as graph_module` (or specific names — choose the cleaner style consistent with the existing server.py imports).
    - Tool dispatch:
      - `graph_build`: resolve paths via `graph_module._resolve_paths(input.workspace_path)`; compute timestamp; if `input.trace`, write `graph_build_start` record; build namespace via `_build_namespace(ops_update, repo=..., workspace=..., full=input.full)`; call `_capture_run(ops_update, args)`; if `trace`, write `graph_build_complete` record (with `model_id=input.model`); pack into `GraphCommandOutput`.
      - `graph_describe`: validate `kind in _DESCRIBE_DISPATCH` (Pydantic Literal does this already); look up `(module, id_attr) = _DESCRIBE_DISPATCH[input.kind]`; if `id_attr is not None and input.identifier is None`, return `GraphCommandOutput(status="error", exit_code=2, stdout="", stderr=f"identifier required for kind={input.kind}", trace_path=None)`. Else build extras + namespace + capture; if `trace`, write `graph_describe` record; pack output.
      - `graph_query`: if `input.name is None and input.kind is None and input.in_package is None`, return `GraphCommandOutput(status="error", exit_code=2, stdout="", stderr="at least one of name, kind, in_package required", trace_path=None)`. Else build namespace + capture; if `trace`, write `graph_query` record; pack output.
    - Each tool's `_capture_run` call ABSORBS cg's `print()` output into a `StringIO` (per Plan 01); the `GraphCommandOutput.stdout` / `.stderr` fields carry the captured strings back to the MCP host. No `print()` reaches `_StdoutGuard`.
    - Tool descriptions in `@mcp.tool(description=...)` are concise (one or two sentences) and document the proxy behavior (e.g., `"Run cg update (build the code graph)."`).
  </behavior>
  <action>
Edit `agents/graph-wiki-agent/src/graph_wiki_agent/mcp/server.py`. Make ONLY two changes — do NOT modify the existing `wiki_*` tools or the `_StdoutGuard` setup:

A. **Add imports** at the top of the file in the import block (after line ~376 `from graph_wiki_agent.commands.lint import LintResult, run_lint`):

```
from typing import Literal  # already imported elsewhere — coalesce if already present
from graph_wiki_agent.commands import graph as graph_module  # noqa: E402
```

Place these noqa-marked imports consistently with the existing pattern (other `# noqa: E402` imports follow the FastMCP-init block).

B. **Add the 3 new tools at the bottom of the file**, between the existing `wiki_lint` tool block (ends at the closing `)` on line ~445) and `def main()` (line ~448). The structure:

```
# --- graph_build, graph_describe, graph_query tools (Phase 38) ---

class GraphBuildInput(BaseModel):
    model_config = ConfigDict(extra='forbid')
    full: bool = Field(False, description="Full rebuild from scratch (else incremental).")
    trace: bool = Field(False, description="Write JSONL trace to .graph-wiki/traces/.")
    model: str | None = Field(None, description="Model ID — recorded in trace; NOT invoked in v1.7.")
    workspace_path: str = Field("", description="Workspace path (default: GRAPH_WIKI_WORKSPACE env var).")


class GraphDescribeInput(BaseModel):
    model_config = ConfigDict(extra='forbid')
    kind: Literal[
        "package", "path", "repository", "domain", "entry_point", "test_suite"
    ] = Field(..., description="Entity kind (snake_case enum).")
    identifier: str | None = Field(
        None,
        description="Identifier (e.g. package name, file path). Required for all kinds except 'repository'.",
    )
    trace: bool = Field(False, description="Write JSONL trace to .graph-wiki/traces/.")
    workspace_path: str = Field("", description="Workspace path (default: GRAPH_WIKI_WORKSPACE env var).")


class GraphQueryInput(BaseModel):
    model_config = ConfigDict(extra='forbid')
    name: str | None = Field(None, description="Node name (exact match).")
    kind: str | None = Field(None, description="Node kind (e.g. class, function, file).")
    in_package: str | None = Field(None, description="Filter to nodes in named package (case-insensitive).")
    trace: bool = Field(False, description="Write JSONL trace to .graph-wiki/traces/.")
    workspace_path: str = Field("", description="Workspace path (default: GRAPH_WIKI_WORKSPACE env var).")


class GraphCommandOutput(BaseModel):
    status: Literal["success", "error"]
    exit_code: int
    stdout: str
    stderr: str
    trace_path: str | None = None


def _pack_output(exit_code: int, stdout: str, stderr: str, trace_path: str | None) -> GraphCommandOutput:
    return GraphCommandOutput(
        status="success" if exit_code == 0 else "error",
        exit_code=exit_code,
        stdout=stdout,
        stderr=stderr,
        trace_path=trace_path,
    )


@mcp.tool(
    name="graph_build",
    description="Build the code graph (cg update) for the workspace. Mirrors `graph-wiki-agent graph build`.",
)
async def graph_build(input: GraphBuildInput, ctx: Context) -> GraphCommandOutput:
    import time

    repo, workspace = graph_module._resolve_paths(input.workspace_path)
    trace_path_str: str | None = None
    shared_stamp = graph_module._iso_utc_timestamp()
    trace_dir = workspace / ".graph-wiki" / "traces"
    if input.trace:
        trace_dir.mkdir(parents=True, exist_ok=True)
        trace_file = trace_dir / f"{shared_stamp}-graph-build.jsonl"
        trace_path_str = str(trace_file.resolve())
        graph_module._write_trace_record(
            trace_dir,
            event="graph_build_start",
            command="graph build",
            args_dict={"full": input.full, "model": input.model},
            exit_code=0,
            duration_ms=0,
            model_id=input.model,
        )

    args = graph_module._build_namespace(
        graph_module.ops_update,
        repo=repo,
        workspace=workspace,
        full=input.full,
    )
    t0 = time.monotonic()
    exit_code, stdout, stderr = graph_module._capture_run(graph_module.ops_update, args)
    dur_ms = int((time.monotonic() - t0) * 1000)

    if input.trace:
        graph_module._write_trace_record(
            trace_dir,
            event="graph_build_complete",
            command="graph build",
            args_dict={"full": input.full, "model": input.model},
            exit_code=exit_code,
            duration_ms=dur_ms,
            model_id=input.model,
        )

    return _pack_output(exit_code, stdout, stderr, trace_path_str)


@mcp.tool(
    name="graph_describe",
    description="Describe a graph entity by kind and identifier. Mirrors `graph-wiki-agent graph describe <kind>`.",
)
async def graph_describe(input: GraphDescribeInput, ctx: Context) -> GraphCommandOutput:
    import time

    module, id_attr = graph_module._DESCRIBE_DISPATCH[input.kind]
    if id_attr is not None and input.identifier is None:
        return _pack_output(
            exit_code=2,
            stdout="",
            stderr=f"identifier required for kind={input.kind}",
            trace_path=None,
        )

    repo, workspace = graph_module._resolve_paths(input.workspace_path)
    extras = {} if id_attr is None else {id_attr: input.identifier}
    trace_path_str: str | None = None
    if input.trace:
        trace_dir = workspace / ".graph-wiki" / "traces"
        trace_dir.mkdir(parents=True, exist_ok=True)
        shared_stamp = graph_module._iso_utc_timestamp()
        trace_path_str = str((trace_dir / f"{shared_stamp}-graph-describe.jsonl").resolve())

    args = graph_module._build_namespace(module, repo=repo, workspace=workspace, **extras)
    t0 = time.monotonic()
    exit_code, stdout, stderr = graph_module._capture_run(module, args)
    dur_ms = int((time.monotonic() - t0) * 1000)

    if input.trace:
        graph_module._write_trace_record(
            workspace / ".graph-wiki" / "traces",
            event="graph_describe",
            command="graph describe",
            args_dict={"kind": input.kind, "identifier": input.identifier},
            exit_code=exit_code,
            duration_ms=dur_ms,
            # D-03 honest-omission: model_id=None signals proxy command; helper omits cost fields.
            model_id=None,
        )

    return _pack_output(exit_code, stdout, stderr, trace_path_str)


@mcp.tool(
    name="graph_query",
    description="Find graph nodes by name/kind/in-package. Mirrors `graph-wiki-agent graph query` (= `cg find`).",
)
async def graph_query(input: GraphQueryInput, ctx: Context) -> GraphCommandOutput:
    import time

    if input.name is None and input.kind is None and input.in_package is None:
        return _pack_output(
            exit_code=2,
            stdout="",
            stderr="at least one of name, kind, in_package required",
            trace_path=None,
        )

    repo, workspace = graph_module._resolve_paths(input.workspace_path)
    trace_path_str: str | None = None
    if input.trace:
        trace_dir = workspace / ".graph-wiki" / "traces"
        trace_dir.mkdir(parents=True, exist_ok=True)
        shared_stamp = graph_module._iso_utc_timestamp()
        trace_path_str = str((trace_dir / f"{shared_stamp}-graph-query.jsonl").resolve())

    args = graph_module._build_namespace(
        graph_module.q_find,
        repo=repo,
        workspace=workspace,
        name=input.name,
        kind=input.kind,
        in_package=input.in_package,
    )
    t0 = time.monotonic()
    exit_code, stdout, stderr = graph_module._capture_run(graph_module.q_find, args)
    dur_ms = int((time.monotonic() - t0) * 1000)

    if input.trace:
        graph_module._write_trace_record(
            workspace / ".graph-wiki" / "traces",
            event="graph_query",
            command="graph query",
            args_dict={"name": input.name, "kind": input.kind, "in_package": input.in_package},
            exit_code=exit_code,
            duration_ms=dur_ms,
            model_id=None,  # D-03 honest-omission
        )

    return _pack_output(exit_code, stdout, stderr, trace_path_str)
```

Notes for the executor:
- `Literal` is needed in the typing imports; check whether it's already imported at line 298 (`from typing import Literal`) — coalesce, do not duplicate.
- The `import time` lines inside the tool bodies are intentional (avoid pulling `time` into the module-init scope; mirrors how `wiki_query` style does not do this — alternatively, hoist `import time` to the top of the file alongside other stdlib imports, whichever is cleaner for the existing style).
- Do NOT define `_pack_output` as a top-level helper if a one-liner construction is clearer; the small helper above keeps each tool body symmetric.
- `_write_trace_record(trace_dir, ...)` — the helper from Plan 01 takes the trace DIRECTORY (not the filename) and reconstructs the filename internally. CONFIRM by reading Plan 01's `_write_trace_record` implementation when the executor processes Plan 02; if the signature uses the filename directly, adjust the MCP calls accordingly. (The behavior block of Plan 01 Task 3 specifies a `_trace_path` helper; Plan 02 may need to call `_trace_path(workspace, "graph-build", shared_stamp)` before `_write_trace_record` to get the right path — or simply pass the dir and let the helper reconstruct.)
- The `_StdoutGuard` (server.py:31-48) is NOT touched. It will continue to raise on any stray stdout write. The 3 new tools delegate their stdout capture to Plan 01's `_capture_run` (via `contextlib.redirect_stdout` inside the helper), so no print reaches the guard.
- After editing, verify the file still loads under the guard: `uv run --package graph-wiki-agent python -c "from graph_wiki_agent.mcp.server import mcp"` must exit 0. If it raises `RuntimeError: Illegal stdout write in MCP server: ...`, one of the new imports is emitting on stdout — fix the import order (place the new imports AFTER the guard is installed, i.e., AFTER `sys.stdout = _StdoutGuard()` on line 51).
  </action>
  <verify>uv run --package graph-wiki-agent python -c "from graph_wiki_agent.mcp.server import mcp, graph_build, graph_describe, graph_query, GraphBuildInput, GraphDescribeInput, GraphQueryInput, GraphCommandOutput; print('ok')" | grep -q "^ok$"</verify>
  <acceptance_criteria>
    - `agents/graph-wiki-agent/src/graph_wiki_agent/mcp/server.py` contains the literal substring `class GraphBuildInput(BaseModel)`
    - File contains `class GraphDescribeInput(BaseModel)`
    - File contains `class GraphQueryInput(BaseModel)`
    - File contains `class GraphCommandOutput(BaseModel)`
    - File contains `@mcp.tool(` followed (within a few lines) by `name="graph_build"`
    - File contains `name="graph_describe"`
    - File contains `name="graph_query"`
    - File contains `async def graph_build(`
    - File contains `async def graph_describe(`
    - File contains `async def graph_query(`
    - File contains the literal substring `Literal[` (the kind enum)
    - File contains `from graph_wiki_agent.commands import graph as graph_module` (or analogous import that brings the helpers into scope)
    - File contains the literal substring `_DESCRIBE_DISPATCH[input.kind]` (proves dispatch reuse)
    - File contains `_capture_run(graph_module.ops_update` (or `_capture_run(module` — proves the capture helper is invoked)
    - File still contains all 6 existing `@mcp.tool(name="wiki_*")` decorators (regression check)
    - `python -c "from graph_wiki_agent.mcp.server import mcp"` exits 0 (server module loads under _StdoutGuard without errors)
    - `python -c "from graph_wiki_agent.mcp.server import graph_build, graph_describe, graph_query"` exits 0
  </acceptance_criteria>
  <done>3 new MCP tools registered; existing wiki_* tools intact; server module loads cleanly under _StdoutGuard; the 3 graph_* tools all delegate to Plan 01's `commands/graph.py` helpers (no duplication of dispatch / namespace / capture logic).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit tests for the 3 `graph_*` MCP tools (GRAPHCMD-04, D-04, D-09)</name>
  <files>agents/graph-wiki-agent/tests/unit/test_mcp_graph_tools.py</files>
  <read_first>agents/graph-wiki-agent/src/graph_wiki_agent/mcp/server.py, agents/graph-wiki-agent/src/graph_wiki_agent/commands/graph.py, agents/graph-wiki-agent/tests/unit/test_commands_graph.py</read_first>
  <behavior>
    - `test_three_graph_tools_registered` — Inspect `mcp._tool_manager` (or the equivalent registry attribute exposed by FastMCP 1.27.1) and assert that exactly the names `graph_build`, `graph_describe`, `graph_query` are present in addition to the existing `wiki_*` names. If FastMCP doesn't expose a public list, assert directly that the three async tool callables are importable from `graph_wiki_agent.mcp.server`.
    - `test_graph_build_input_shape` — `GraphBuildInput(full=True, trace=False, model="x", workspace_path="/tmp")` parses cleanly; `GraphBuildInput(unknown_field=1)` raises `pydantic.ValidationError` (extra='forbid').
    - `test_graph_describe_input_kind_enum` — `GraphDescribeInput(kind="bogus", identifier="x")` raises `ValidationError`. `GraphDescribeInput(kind="repository")` (no identifier) parses cleanly. `GraphDescribeInput(kind="package", identifier="foo")` parses cleanly.
    - `test_graph_describe_mcp_dispatch` — monkeypatch `q_describe_package.run` (via `graph_module.q_describe_package.run`) with a recorder returning 0; build a `GraphDescribeInput(kind="package", identifier="foo")`; `await graph_describe(input, ctx)`; assert recorder called once; assert returned `GraphCommandOutput.status == "success"` and `exit_code == 0`. Also test `kind="repository"` (no identifier) dispatches to `q_describe_repo.run`.
    - `test_stdout_guard_not_tripped` — invoke `graph_describe` with a mocked cg module whose `run` calls `print("hello")` and returns 0; assert the MCP call returns successfully (no `RuntimeError: Illegal stdout write`). The `_StdoutGuard` should NEVER be triggered because the cg print is captured by `_capture_run`'s `contextlib.redirect_stdout`.
    - `test_output_shape_per_tool` — for each of the 3 tools, call with mocked cg module returning 0 and `print("captured")`; assert `GraphCommandOutput.stdout == "captured\n"` (or equivalent: contains the captured text), `stderr == ""`, `exit_code == 0`, `status == "success"`, `trace_path is None` (when `trace=False`).
    - `test_describe_missing_entity` — monkeypatch `q_describe_package.run` to return `exit_codes.GENERIC` (1) and print to stderr `"error: package not found: nonexistent"`; invoke `graph_describe(kind="package", identifier="nonexistent")`; assert `output.status == "error"`, `output.exit_code == exit_codes.GENERIC`, `output.stderr` contains the expected literal. The tool returns a value rather than raising.
    - `test_graph_build_uninitialized_returns_error` — monkeypatch `ops_update.run` to return `exit_codes.NOT_IN_GIT_REPO` (5); invoke `graph_build` with default input; assert `output.status == "error"`, `output.exit_code == 5`. Does NOT raise.
    - `test_wiki_tools_still_registered` — assert that `graph_wiki_agent.mcp.server` still exports `wiki_query`, `wiki_log`, `wiki_bootstrap`, `wiki_scan`, `wiki_ingest`, `wiki_lint`, `wiki_ping` (regression guard).
    - `test_graph_query_no_filters_returns_error` — invoke `graph_query` with `name=None, kind=None, in_package=None`; assert `output.status == "error"`, `output.exit_code == 2`, `output.stderr == "at least one of name, kind, in_package required"`; the cg `q_find.run` is NOT called.
    - `test_graph_build_trace_writes_file` — set `GRAPH_WIKI_WORKSPACE` env var to a tmp path; mock `ops_update.run` to return 0; invoke `graph_build(trace=True)`; assert `output.trace_path is not None` and the file exists and contains both `graph_build_start` and `graph_build_complete` records.
  </behavior>
  <action>
Create `agents/graph-wiki-agent/tests/unit/test_mcp_graph_tools.py`. The tests use direct async invocation of the `@mcp.tool`-decorated coroutines (this is the same pattern as the existing `wiki_*` tests — read `tests/unit/test_cli_help.py` for the local idioms; if a `tests/unit/test_mcp_wiki_query.py`-style file exists, mirror its structure).

File structure:

```
from __future__ import annotations
import asyncio
import json
import os
from pathlib import Path
from unittest.mock import patch, MagicMock

import pytest
from pydantic import ValidationError

# Import after the module loads (which installs _StdoutGuard).
from graph_wiki_agent.mcp.server import (
    mcp,
    graph_build,
    graph_describe,
    graph_query,
    GraphBuildInput,
    GraphDescribeInput,
    GraphQueryInput,
    GraphCommandOutput,
)
from graph_wiki_agent.commands import graph as graph_module
from graph_io import exit_codes


@pytest.fixture
def tmp_workspace(tmp_path, monkeypatch):
    ws = tmp_path / "workspace"
    ws.mkdir()
    monkeypatch.setenv("GRAPH_WIKI_WORKSPACE", str(ws))
    return ws


@pytest.fixture
def fake_ctx():
    """A minimal stand-in for FastMCP's Context.

    FastMCP's Context is used for progress reporting; the graph_* tools do not
    call `ctx.report_progress` so a MagicMock is sufficient.
    """
    ctx = MagicMock()
    ctx.report_progress = MagicMock(return_value=None)
    return ctx


def test_three_graph_tools_registered():
    # The exact FastMCP registry attribute name varies across mcp versions.
    # Robust approach: assert the 3 callables are importable from the server module.
    assert callable(graph_build)
    assert callable(graph_describe)
    assert callable(graph_query)
    # The tool is async (coroutine function):
    import inspect
    assert inspect.iscoroutinefunction(graph_build)
    assert inspect.iscoroutinefunction(graph_describe)
    assert inspect.iscoroutinefunction(graph_query)


def test_graph_build_input_shape():
    # Valid input parses cleanly
    GraphBuildInput(full=True, trace=False, model="x", workspace_path="/tmp")
    GraphBuildInput()  # all-defaults
    # Unknown field rejected (extra='forbid')
    with pytest.raises(ValidationError):
        GraphBuildInput(unknown_field=1)  # type: ignore[call-arg]


def test_graph_describe_input_kind_enum():
    # Bogus kind rejected at Pydantic layer
    with pytest.raises(ValidationError):
        GraphDescribeInput(kind="bogus", identifier="x")  # type: ignore[arg-type]
    # repository: identifier optional
    GraphDescribeInput(kind="repository")
    # package: identifier optional at the Pydantic layer; adapter rejects None at the dispatch layer
    GraphDescribeInput(kind="package", identifier="foo")
    GraphDescribeInput(kind="package")  # Pydantic accepts; adapter returns error


@pytest.mark.asyncio
async def test_graph_describe_mcp_dispatch(tmp_workspace, fake_ctx):
    recorder_pkg = MagicMock(return_value=exit_codes.SUCCESS)
    with patch.object(graph_module.q_describe_package, "run", recorder_pkg):
        out = await graph_describe(GraphDescribeInput(kind="package", identifier="foo"), fake_ctx)
    assert out.status == "success"
    assert out.exit_code == 0
    assert recorder_pkg.call_count == 1
    args_ns = recorder_pkg.call_args.args[0]
    assert args_ns.name == "foo"

    recorder_repo = MagicMock(return_value=exit_codes.SUCCESS)
    with patch.object(graph_module.q_describe_repo, "run", recorder_repo):
        out = await graph_describe(GraphDescribeInput(kind="repository"), fake_ctx)
    assert out.status == "success"
    assert recorder_repo.call_count == 1


@pytest.mark.asyncio
async def test_stdout_guard_not_tripped(tmp_workspace, fake_ctx):
    def fake_print_then_succeed(args):
        print("hello from cg")  # this MUST be captured by _capture_run, never reach _StdoutGuard
        return exit_codes.SUCCESS

    with patch.object(graph_module.q_describe_package, "run", side_effect=fake_print_then_succeed):
        # If _StdoutGuard is tripped, RuntimeError("Illegal stdout write") propagates
        out = await graph_describe(GraphDescribeInput(kind="package", identifier="foo"), fake_ctx)

    assert out.status == "success"
    assert "hello from cg" in out.stdout


@pytest.mark.asyncio
async def test_output_shape_per_tool(tmp_workspace, fake_ctx):
    def succeed_with_output(args):
        print("captured")
        return exit_codes.SUCCESS

    # graph_build
    with patch.object(graph_module.ops_update, "run", side_effect=succeed_with_output):
        out = await graph_build(GraphBuildInput(), fake_ctx)
    assert out.status == "success"
    assert out.exit_code == 0
    assert "captured" in out.stdout
    assert out.stderr == ""
    assert out.trace_path is None  # no --trace

    # graph_describe
    with patch.object(graph_module.q_describe_package, "run", side_effect=succeed_with_output):
        out = await graph_describe(GraphDescribeInput(kind="package", identifier="foo"), fake_ctx)
    assert out.status == "success"
    assert "captured" in out.stdout

    # graph_query
    with patch.object(graph_module.q_find, "run", side_effect=succeed_with_output):
        out = await graph_query(GraphQueryInput(name="x"), fake_ctx)
    assert out.status == "success"
    assert "captured" in out.stdout


@pytest.mark.asyncio
async def test_describe_missing_entity(tmp_workspace, fake_ctx):
    import sys as _sys

    def not_found(args):
        print("error: package not found: nonexistent", file=_sys.stderr)
        return exit_codes.GENERIC

    with patch.object(graph_module.q_describe_package, "run", side_effect=not_found):
        out = await graph_describe(GraphDescribeInput(kind="package", identifier="nonexistent"), fake_ctx)

    assert out.status == "error"
    assert out.exit_code == exit_codes.GENERIC
    assert "package not found" in out.stderr


@pytest.mark.asyncio
async def test_graph_build_uninitialized_returns_error(tmp_workspace, fake_ctx):
    with patch.object(graph_module.ops_update, "run", return_value=exit_codes.NOT_IN_GIT_REPO):
        out = await graph_build(GraphBuildInput(), fake_ctx)
    assert out.status == "error"
    assert out.exit_code == exit_codes.NOT_IN_GIT_REPO  # == 5


def test_wiki_tools_still_registered():
    # Regression guard: import all existing wiki_* tools to verify the Phase 38 changes
    # did not break the existing MCP surface.
    from graph_wiki_agent.mcp.server import (
        wiki_ping,
        wiki_query,
        wiki_log,
        wiki_bootstrap,
        wiki_scan,
        wiki_ingest,
        wiki_lint,
    )
    for tool in [wiki_ping, wiki_query, wiki_log, wiki_bootstrap, wiki_scan, wiki_ingest, wiki_lint]:
        assert callable(tool)


@pytest.mark.asyncio
async def test_graph_query_no_filters_returns_error(tmp_workspace, fake_ctx):
    recorder = MagicMock(return_value=exit_codes.SUCCESS)
    with patch.object(graph_module.q_find, "run", recorder):
        out = await graph_query(GraphQueryInput(), fake_ctx)
    assert out.status == "error"
    assert out.exit_code == 2
    assert "at least one of name, kind, in_package required" in out.stderr
    assert recorder.call_count == 0


@pytest.mark.asyncio
async def test_graph_build_trace_writes_file(tmp_workspace, fake_ctx):
    with patch.object(graph_module.ops_update, "run", return_value=exit_codes.SUCCESS):
        out = await graph_build(GraphBuildInput(trace=True), fake_ctx)
    assert out.trace_path is not None
    p = Path(out.trace_path)
    assert p.exists(), f"trace file {p} should exist"
    records = [json.loads(line) for line in p.read_text().splitlines() if line.strip()]
    events = [r.get("event") for r in records]
    assert "graph_build_start" in events
    assert "graph_build_complete" in events
```

Notes for the executor:
- `pytest-asyncio` is enabled via `asyncio_mode = "auto"` in `agents/graph-wiki-agent/pyproject.toml` `[tool.pytest.ini_options]` (verify via Read tool). The `@pytest.mark.asyncio` decorators above are belt-and-suspenders; remove if `asyncio_mode=auto` automatically picks up `async def test_*`.
- `_StdoutGuard` is installed at module-import time when `graph_wiki_agent.mcp.server` is first imported. Any test that imports from `server` triggers it. The tests above all rely on the guard being active; the `test_stdout_guard_not_tripped` test is the explicit assertion that `_capture_run`'s redirect handles cg's print correctly.
- If `FastMCP._tool_manager` (or whatever the registry attribute is named) becomes useful for verifying tool registration, the executor can extend `test_three_graph_tools_registered` to walk that registry; if FastMCP 1.27.1 doesn't expose a stable attribute, the import-callable check is sufficient.
- If the `Pydantic` v2 BaseModel still allows None for a non-Optional Literal field with `default=None` (unusual edge case), the `test_graph_describe_input_kind_enum` test for `GraphDescribeInput(kind="package")` may need adjustment — the spec says identifier is optional at Pydantic level and validated at adapter level. Adjust if Pydantic rejects.
  </action>
  <verify>uv run --package graph-wiki-agent pytest agents/graph-wiki-agent/tests/unit/test_mcp_graph_tools.py -q</verify>
  <acceptance_criteria>
    - File `agents/graph-wiki-agent/tests/unit/test_mcp_graph_tools.py` exists
    - File contains the 10 test function definitions: `test_three_graph_tools_registered`, `test_graph_build_input_shape`, `test_graph_describe_input_kind_enum`, `test_graph_describe_mcp_dispatch`, `test_stdout_guard_not_tripped`, `test_output_shape_per_tool`, `test_describe_missing_entity`, `test_graph_build_uninitialized_returns_error`, `test_wiki_tools_still_registered`, `test_graph_query_no_filters_returns_error`, `test_graph_build_trace_writes_file` (11 total)
    - File imports `from graph_wiki_agent.mcp.server import mcp, graph_build, graph_describe, graph_query, GraphBuildInput, GraphDescribeInput, GraphQueryInput, GraphCommandOutput`
    - File imports `from graph_wiki_agent.commands import graph as graph_module`
    - `uv run --package graph-wiki-agent pytest agents/graph-wiki-agent/tests/unit/test_mcp_graph_tools.py -q` exits 0 (all tests green)
    - `uv run --package graph-wiki-agent pytest agents/graph-wiki-agent/tests/ -q` exits 0 (no regressions in any existing tests)
  </acceptance_criteria>
  <done>All 11 MCP-side tests pass. The 3 `graph_*` MCP tools are validated: input shape, kind enum, dispatch via `_DESCRIBE_DISPATCH`, stdout-guard safety, error packaging, trace file writes. Regression guard for existing `wiki_*` tools holds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP host → MCP server | DeepAgents CLI (or other MCP host) calls `graph_build` / `graph_describe` / `graph_query` with JSON-RPC framed inputs. Pydantic `model_config = ConfigDict(extra='forbid')` rejects unknown fields at the parsing boundary. `Literal[...]` enum validation rejects bogus kinds. |
| MCP server → Plan 01 helpers | The tool bodies call into `graph_module._capture_run(...)` etc. The helpers were threat-modeled in Plan 01 (parameterized SQL, no shell-out). Plan 02 adds no new attack surface — it's a thin Pydantic-wrapped re-export. |
| MCP server → stdout | The `_StdoutGuard` rejects all stdout writes that don't come through FastMCP's framing. `_capture_run` inside the tools redirects cg's `print()` into `StringIO` — guard is never tripped during normal operation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-08 | Tampering | MCP host supplies malicious `kind` to `graph_describe` | mitigate | `Literal[...]` Pydantic validation rejects any value outside the 6 snake_case kinds before the tool body runs. Tested via `test_graph_describe_input_kind_enum`. |
| T-38-09 | Tampering | MCP host supplies extra unknown fields | mitigate | `model_config = ConfigDict(extra='forbid')` on all 3 Input models rejects them. Tested. |
| T-38-10 | Information Disclosure | `GraphCommandOutput.stdout` returns cg's rendered output to the MCP host | accept | The MCP host is the user's authorized client (DeepAgents CLI on their machine). Cg's output is the same data the user would see at the CLI. No cross-tenant boundary. |
| T-38-11 | DoS | MCP host repeatedly invokes `graph_build --trace` to fill disk | accept | Trace files are per-invocation; user's own workspace. Same disposition as T-38-05 (CLI side). |
| T-38-12 | Tampering | Cg's `print()` reaches `_StdoutGuard` and crashes the MCP server | mitigate | `_capture_run` wraps EVERY cg call in `contextlib.redirect_stdout(StringIO())`. Tested via `test_stdout_guard_not_tripped`. |
| T-38-13 | Tampering | `GraphCommandOutput.trace_path` exposes the absolute filesystem path to the MCP host | accept | Path is in the user's own workspace; same trust level as the rest of the output. The host can choose to display, ignore, or `cat` the file. |
| T-38-SC | Tampering | npm/pip/cargo supply-chain | accept | NO new third-party dependencies. `pydantic`, `mcp`, `typing` are all pre-existing. Legitimacy gate not triggered. |
</threat_model>

<verification>
End-of-plan checks (all must pass for Phase 38 to be complete):

1. `uv sync` exits 0 (no dependency changes).
2. `uv run --package graph-wiki-agent python -c "from graph_wiki_agent.mcp.server import mcp, graph_build, graph_describe, graph_query"` exits 0 (server loads under _StdoutGuard).
3. `uv run --package graph-wiki-agent pytest agents/graph-wiki-agent/tests/unit/test_mcp_graph_tools.py -q` exits 0 (all 11 tests green).
4. `uv run --package graph-wiki-agent pytest agents/graph-wiki-agent/tests/ -q` exits 0 (no regressions across the agent's test suite, including Plan 01's `test_commands_graph.py` and existing `test_mcp_*.py`/`test_cli_*.py` files).
5. `grep -c '@mcp.tool(' agents/graph-wiki-agent/src/graph_wiki_agent/mcp/server.py` returns at least 10 (7 existing `wiki_*` + 3 new `graph_*`).
6. `grep -c 'name="graph_' agents/graph-wiki-agent/src/graph_wiki_agent/mcp/server.py` returns 3 (exactly).
7. `grep -c 'name="wiki_' agents/graph-wiki-agent/src/graph_wiki_agent/mcp/server.py` returns at least 7 (regression guard).
8. End-to-end smoke (manual, before `gsd:verify-work`): launch the MCP server via `graph-wiki-mcp` (stdio transport); send a `tools/list` JSON-RPC request; assert the response includes all 10 tools (`wiki_ping`, `wiki_query`, `wiki_log`, `wiki_bootstrap`, `wiki_scan`, `wiki_ingest`, `wiki_lint`, `graph_build`, `graph_describe`, `graph_query`).
</verification>

<success_criteria>
- 3 new MCP tools registered: `graph_build`, `graph_describe`, `graph_query` (GRAPHCMD-04 / SC#4).
- Pydantic Input models enforce shape: `Literal` for `GraphDescribeInput.kind`, `extra='forbid'` on all 3, optional/required field separation per the input contract.
- All 3 MCP tools delegate dispatch to Plan 01's `commands/graph.py` helpers (`_DESCRIBE_DISPATCH`, `_build_namespace`, `_capture_run`, `_write_trace_record`) — no duplication of business logic between CLI and MCP surfaces.
- `_StdoutGuard` is never tripped during normal tool operation (verified by `test_stdout_guard_not_tripped`).
- Existing `wiki_*` tools remain registered and importable (regression guard via `test_wiki_tools_still_registered`).
- The MCP path returns errors as structured `GraphCommandOutput(status="error", exit_code=..., stderr=...)` — does NOT raise across the MCP boundary.
- Phase 38 is complete: SC#1..SC#4 from ROADMAP are all satisfied via Plan 01 + Plan 02.
</success_criteria>

<output>
Create `.planning/phases/38-graph-wiki-agent-graph-subcommand/38-02-SUMMARY.md` when done. Summary should include:
- Files modified (`server.py` line-count delta) and created (`test_mcp_graph_tools.py` line count).
- Test pass/fail status for the full agent suite (`uv run --package graph-wiki-agent pytest -q` exit code + counts).
- Confirmation that the `_StdoutGuard` was never tripped (no `RuntimeError: Illegal stdout write` in any test output).
- Confirmation that existing `wiki_*` MCP tools still register and import.
- Any executor-discovered ambiguities about FastMCP 1.27.1's tool registry inspection (e.g., did the executor find a stable way to list registered tools, or did they fall back to the import-callable check?).
- Whether the optional manual end-to-end smoke (verification step 8) was executed.
</output>
