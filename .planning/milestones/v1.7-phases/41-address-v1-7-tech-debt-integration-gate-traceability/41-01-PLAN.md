---
phase: 41-address-v1-7-tech-debt-integration-gate-traceability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/graph-wiki-agent/tests/integration/test_scan_graph_end_to_end.py
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: []
tags:
  - tech-debt
  - integration-gate
  - traceability
  - v1.7-closeout

must_haves:
  truths:
    - "`pytest tests/test_integration_gate.py` exits 0 (was failing before this phase)"
    - "All 20 named REQ-IDs (HYGIENE-01..14, CGFIND-01..03, INGESTOR-01..03) show `- [x]` in REQUIREMENTS.md checklist"
    - "All 20 corresponding rows in the Traceability table show Status = `Satisfied` (not `Pending`)"
    - "No other REQ-IDs or table rows are modified (LIBTOOLS-*, GRAPHCMD-*, SCANNER-* untouched)"
    - "No new attack surface, no new imports beyond stdlib `os`, no new dependencies"
  artifacts:
    - path: "agents/graph-wiki-agent/tests/integration/test_scan_graph_end_to_end.py"
      provides: "Canonical INTEGRATION_GATE constant + @INTEGRATION_GATE decorator on test function"
      contains: "pytest.mark.skipif"
    - path: ".planning/REQUIREMENTS.md"
      provides: "20 checkboxes flipped + 20 traceability rows updated"
      contains: "Satisfied"
  key_links:
    - from: "agents/graph-wiki-agent/tests/integration/test_scan_graph_end_to_end.py"
      to: "tests/test_integration_gate.py"
      via: "regex match against _CANONICAL_PATTERN"
      pattern: "pytest\\.mark\\.skipif.*GRAPH_WIKI_RUN_INTEGRATION"
  decisions_addressed:
    - "D-01: Skipif fix scope limited to test_scan_graph_end_to_end.py only — sample_monorepo offender deferred to v1.8 (Task 1)"
    - "D-02: No pre-commit / CI lint hook added in this phase — scope fence honored (no task)"
    - "D-03: Canonical INTEGRATION_GATE constant pattern copied verbatim from test_bedrock_iam.py (Task 1 Edit 2)"
    - "D-04: No `# integration-gate-allow` allowlist marker used — verified by Task 1 acceptance criterion grep=0"
    - "D-05: Flip exactly 20 named checkboxes (HYGIENE-01..14, CGFIND-01..03, INGESTOR-01..03); LIBTOOLS/GRAPHCMD/SCANNER untouched (Task 2 Edit 1)"
    - "D-06: Same 20 REQ-IDs flipped from Pending to Satisfied in traceability table (Task 2 Edit 2)"
    - "D-07: No new columns, no table restructure, no Future Requirements edits — verified by Task 2 final diff acceptance criterion"
    - "D-08: No retroactive UAT/VERIFICATION.md produced for Phases 39 or 40 — scope fence honored (no task)"
---

<objective>
Close v1.7 milestone tech debt by (A) restoring the canonical `GRAPH_WIKI_RUN_INTEGRATION` gate
on the Phase 39 scan-end-to-end integration test so `tests/test_integration_gate.py` passes,
and (B) syncing `.planning/REQUIREMENTS.md` to reflect the actual shipped state of v1.7 phases
35, 36, and 40 (20 stale `[ ]` checkboxes → `[x]`, 20 `Pending` traceability rows → `Satisfied`).

Purpose: Unblock `/gsd-complete-milestone` for v1.7 — the milestone audit
(`.planning/v1.7-MILESTONE-AUDIT.md`) flagged exactly these two items as gating closure.
Per CONTEXT.md D-01..D-08, this phase is **minimum-scope** remediation: no pre-commit hooks,
no evidence-column additions, no retro UAT for Phases 39/40, no v1.6 fixture cleanup.

Output: Two files modified; one meta-test transitions red → green; traceability matrix
matches reality.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.7-MILESTONE-AUDIT.md
@.planning/phases/41-address-v1-7-tech-debt-integration-gate-traceability/41-CONTEXT.md
@.planning/phases/41-address-v1-7-tech-debt-integration-gate-traceability/41-PATTERNS.md

<interfaces>
<!-- Canonical pattern executor must match — extracted from test_bedrock_iam.py lines 30-35. -->
<!-- Use this verbatim; the gate regex in tests/test_integration_gate.py tolerates whitespace. -->

From agents/graph-wiki-agent/tests/integration/test_bedrock_iam.py (lines 21-39):
```python
from __future__ import annotations

import os

import pytest

# Canonical GRAPH_WIKI_RUN_INTEGRATION gate — matches conftest.py:19-22 verbatim
# so the docs/testing.md grep gate sees this file as canonical (D-10).
INTEGRATION_GATE = pytest.mark.skipif(
    not os.environ.get("GRAPH_WIKI_RUN_INTEGRATION"),
    reason="Set GRAPH_WIKI_RUN_INTEGRATION=1 to run real integration scenarios",
)


@pytest.mark.integration
@INTEGRATION_GATE
def test_make_llm_preflight_invoke():
    ...
```

Regex contract the edit must satisfy (from tests/test_integration_gate.py lines 21-25):
```python
_CANONICAL_PATTERN = re.compile(
    r'pytest\.mark\.skipif\s*\(\s*'
    r'(?:not\s+)?os\.environ\.get\(\s*["\']GRAPH_WIKI_RUN_INTEGRATION["\']\s*\)',
    re.MULTILINE,
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add canonical INTEGRATION_GATE to scan-end-to-end test</name>
  <files>agents/graph-wiki-agent/tests/integration/test_scan_graph_end_to_end.py</files>
  <read_first>
    - agents/graph-wiki-agent/tests/integration/test_scan_graph_end_to_end.py (the file being modified — note existing imports and module-level `pytestmark = pytest.mark.integration` on line 31)
    - agents/graph-wiki-agent/tests/integration/test_bedrock_iam.py (canonical analog — copy the INTEGRATION_GATE block verbatim from lines 30-35; mirror decorator usage from lines 38-40)
    - tests/test_integration_gate.py (success oracle — `_CANONICAL_PATTERN` regex on lines 21-25 defines what counts as canonical)
    - .planning/phases/41-address-v1-7-tech-debt-integration-gate-traceability/41-PATTERNS.md (pattern-map summary of imports, constant placement, decorator placement)
  </read_first>
  <action>
    Apply the canonical `GRAPH_WIKI_RUN_INTEGRATION` gate to the scan-end-to-end integration
    test per D-03. Three concrete edits to `agents/graph-wiki-agent/tests/integration/test_scan_graph_end_to_end.py`:

    1. **Add `import os`** to the existing stdlib import block. Current file imports (lines 18-24)
       include `asyncio`, `shutil`, `sqlite3`, `subprocess`, `sys`, `pathlib.Path`, and `unittest.mock.MagicMock`.
       Insert `import os` in alphabetical order within that stdlib block (between `import asyncio` and `import shutil`).
       Do NOT touch any other imports.

    2. **Add the `INTEGRATION_GATE` constant** verbatim from `test_bedrock_iam.py` lines 30-35
       (only the `reason=` string adapts to this file's scenario). Place it immediately AFTER the
       existing module-level `pytestmark = pytest.mark.integration` on current line 31, separated by
       one blank line. Exact block to insert:

       ```
       # Canonical GRAPH_WIKI_RUN_INTEGRATION gate — matches conftest.py:19-22 verbatim
       # so the docs/testing.md grep gate sees this file as canonical (D-10).
       INTEGRATION_GATE = pytest.mark.skipif(
           not os.environ.get("GRAPH_WIKI_RUN_INTEGRATION"),
           reason="Set GRAPH_WIKI_RUN_INTEGRATION=1 to run real scan→graph integration scenarios",
       )
       ```

       Keep the comment block — it documents the D-10 anchor.

    3. **Apply `@INTEGRATION_GATE`** to the existing test function
       `test_run_scan_creates_graph_db_and_uri_derived_slug` (currently undecorated at line 83).
       Add the decorator on its own line immediately above `def test_run_scan_creates_graph_db_and_uri_derived_slug(...)`.
       Do NOT add a redundant `@pytest.mark.integration` decorator — the module-level `pytestmark` on
       line 31 already covers that (per D-03: "alongside, not replacement"; the existing module-level
       marker stays).

    Prohibitions (per D-04, D-07): Do NOT use the `# integration-gate-allow` allowlist marker.
    Do NOT modify any test logic, fixtures, assertions, or the `_setup_fixture_monorepo` helper.
    Do NOT change the docstring. Do NOT touch any other file in this task.
  </action>
  <verify>
    <automated>uv run --package graph-wiki-agent python -m pytest /Users/pat/Personal/agent-research/tests/test_integration_gate.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^import os$" agents/graph-wiki-agent/tests/integration/test_scan_graph_end_to_end.py` returns `1`
    - `grep -c "INTEGRATION_GATE = pytest.mark.skipif" agents/graph-wiki-agent/tests/integration/test_scan_graph_end_to_end.py` returns `1`
    - `grep -c "GRAPH_WIKI_RUN_INTEGRATION" agents/graph-wiki-agent/tests/integration/test_scan_graph_end_to_end.py` returns at least `1` (constant body)
    - `grep -c "^@INTEGRATION_GATE$" agents/graph-wiki-agent/tests/integration/test_scan_graph_end_to_end.py` returns `1`
    - `grep -c "# integration-gate-allow" agents/graph-wiki-agent/tests/integration/test_scan_graph_end_to_end.py` returns `0` (allowlist marker forbidden per D-04)
    - `grep -c "^pytestmark = pytest.mark.integration$" agents/graph-wiki-agent/tests/integration/test_scan_graph_end_to_end.py` returns `1` (existing module-level marker preserved)
    - `pytest tests/test_integration_gate.py -x` exits 0 (was failing before this task per the audit)
    - `python -c "import ast; ast.parse(open('agents/graph-wiki-agent/tests/integration/test_scan_graph_end_to_end.py').read())"` exits 0 (file remains valid Python)
  </acceptance_criteria>
  <done>The scan-end-to-end test carries the canonical INTEGRATION_GATE constant and decorator; `tests/test_integration_gate.py` exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Sync REQUIREMENTS.md — flip 20 checkboxes + 20 traceability rows</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (the file being modified — note exact line numbers of the 20 target REQ-IDs in both the checklist and the traceability table)
    - .planning/v1.7-MILESTONE-AUDIT.md §"Requirements Coverage (3-Source Cross-Reference)" (authoritative mapping of REQ-ID → phase → Final Status; the audit is what the sync must reflect)
    - .planning/phases/41-address-v1-7-tech-debt-integration-gate-traceability/41-PATTERNS.md §`REQUIREMENTS.md` section (exact line-number map of the 20 checkboxes and 20 table rows)
  </read_first>
  <action>
    Sync `.planning/REQUIREMENTS.md` to reflect the actual shipped state of v1.7. Two pure markdown edits, no structural changes (per D-05, D-06, D-07).

    **Edit 1 — Checkbox flip (`- [ ]` → `- [x]`) for exactly these 20 REQ-IDs:**

    - HYGIENE-01 (line 16)
    - HYGIENE-02 (line 17)
    - HYGIENE-03 (line 18)
    - HYGIENE-04 (line 19)
    - HYGIENE-05 (line 20)
    - HYGIENE-06 (line 21)
    - HYGIENE-07 (line 22)
    - HYGIENE-08 (line 23)
    - HYGIENE-09 (line 24)
    - HYGIENE-10 (line 25)
    - HYGIENE-11 (line 26)
    - HYGIENE-12 (line 27)
    - HYGIENE-13 (line 28)
    - HYGIENE-14 (line 29)
    - CGFIND-01 (line 33)
    - CGFIND-02 (line 34)
    - CGFIND-03 (line 35)
    - INGESTOR-01 (line 60)
    - INGESTOR-02 (line 61)
    - INGESTOR-03 (line 62)

    (Note: that gives 14 + 3 + 3 = 20 checkboxes listed. The CONTEXT.md text says "24". Cross-check against
    PATTERNS.md and the file itself; flip only those that are currently `[ ]` in the HYGIENE/CGFIND/INGESTOR
    blocks, leaving LIBTOOLS-* / GRAPHCMD-* / SCANNER-* untouched per D-05.)

    For each line: change `- [ ] **{REQ-ID}** —` → `- [x] **{REQ-ID}** —`. The text after the
    REQ-ID label is preserved verbatim. Do NOT alter wording, punctuation, or backreferences.

    **Edit 2 — Traceability table Status column (`Pending` → `Satisfied`) for the same 20 REQ-IDs:**

    In the table starting at line 93 (`| Requirement | Phase | Status |`):

    - Lines 95-108: HYGIENE-01..14 rows — change `| Pending |` → `| Satisfied |` (14 rows)
    - Lines 109-111: CGFIND-01..03 rows — change `| Pending |` → `| Satisfied |` (3 rows)
    - Lines 124-126: INGESTOR-01..03 rows — change `| Pending |` → `| Satisfied |` (3 rows)

    Do NOT change the Requirement column, Phase column, or column ordering. Do NOT add new
    columns (D-07 forbids evidence/phase-link/restructure). Do NOT touch existing `Complete`
    rows (LIBTOOLS-*, GRAPHCMD-*, SCANNER-*) — per D-05/D-06 they stay as-is.

    Prohibitions (per D-07, D-08): Do NOT edit the "Future Requirements" section.
    Do NOT add a v1.8 deferred-items capture. Do NOT produce retroactive UAT artifacts.
    Do NOT modify any other markdown file.

    **Reconciliation note on count:** CONTEXT.md and the phase-scope summary say "24" REQ-IDs.
    Counted from the lists in D-05: HYGIENE-01..14 (14) + CGFIND-01..03 (3) + INGESTOR-01..03 (3) = **20**.
    Use the explicit REQ-ID lists (D-05) as ground truth; trust the IDs, not the count. The
    acceptance criteria below pin the resulting state by grep counts.
  </action>
  <verify>
    <automated>grep -v '^#' /Users/pat/Personal/agent-research/.planning/REQUIREMENTS.md | grep -c '^- \[ \] \*\*\(HYGIENE\|CGFIND\|INGESTOR\)-'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^- \[x\] \*\*HYGIENE-' .planning/REQUIREMENTS.md` returns `14`
    - `grep -c '^- \[x\] \*\*CGFIND-' .planning/REQUIREMENTS.md` returns `3`
    - `grep -c '^- \[x\] \*\*INGESTOR-' .planning/REQUIREMENTS.md` returns `3`
    - `grep -c '^- \[ \] \*\*\(HYGIENE\|CGFIND\|INGESTOR\)-' .planning/REQUIREMENTS.md` returns `0` (no remaining unchecked items in these three groups)
    - `grep -c '| HYGIENE-.* | Phase 35 | Satisfied |' .planning/REQUIREMENTS.md` returns `14`
    - `grep -c '| CGFIND-.* | Phase 36 | Satisfied |' .planning/REQUIREMENTS.md` returns `3`
    - `grep -c '| INGESTOR-.* | Phase 40 | Satisfied |' .planning/REQUIREMENTS.md` returns `3`
    - `grep -c '| HYGIENE-.* | Phase 35 | Pending |' .planning/REQUIREMENTS.md` returns `0`
    - `grep -c '| CGFIND-.* | Phase 36 | Pending |' .planning/REQUIREMENTS.md` returns `0`
    - `grep -c '| INGESTOR-.* | Phase 40 | Pending |' .planning/REQUIREMENTS.md` returns `0`
    - `grep -c '| LIBTOOLS-.* | Phase 37 | Complete |' .planning/REQUIREMENTS.md` returns `5` (untouched per D-05)
    - `grep -c '| GRAPHCMD-.* | Phase 38 | Complete |' .planning/REQUIREMENTS.md` returns `4` (untouched)
    - `grep -c '| SCANNER-.* | Phase 39 | Complete |' .planning/REQUIREMENTS.md` returns `3` (untouched)
    - `grep -c '## Future Requirements' .planning/REQUIREMENTS.md` returns `1` (section header preserved, no other edits per D-07)
    - `git diff --stat .planning/REQUIREMENTS.md` shows exactly 1 file modified, and `git diff .planning/REQUIREMENTS.md` shows ONLY checkbox `[ ]→[x]` flips and `Pending→Satisfied` substitutions (no other diff hunks)
  </acceptance_criteria>
  <done>Twenty REQ-IDs (HYGIENE-01..14, CGFIND-01..03, INGESTOR-01..03) show `- [x]` in the checklist and `Satisfied` in the traceability table; no other rows touched; no structural changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This phase modifies a test file's gate decorator and a markdown traceability table. No new attack surface — no user input crosses any new boundary, no network calls, no auth, no dependencies. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-01 | (n/a) | n/a | accept | No new attack surface — phase modifies test gate metadata (`pytest.mark.skipif` decorator) and a markdown traceability table only. The added `import os` and `os.environ.get("GRAPH_WIKI_RUN_INTEGRATION")` are read-only env-var reads identical to the canonical pattern already used in `test_bedrock_iam.py` and `conftest.py`. Markdown edits cannot execute code. |
</threat_model>

<verification>
Run the success oracle locally before commit:

```bash
pytest tests/test_integration_gate.py -x
```

Expected: exits 0 (was failing before Task 1). Then spot-check the REQUIREMENTS.md sync:

```bash
# Should match the counts in Task 2 acceptance criteria
grep -c '^- \[x\] \*\*HYGIENE-' .planning/REQUIREMENTS.md  # 14
grep -c '^- \[x\] \*\*CGFIND-' .planning/REQUIREMENTS.md   # 3
grep -c '^- \[x\] \*\*INGESTOR-' .planning/REQUIREMENTS.md # 3
grep -c '| Phase 35 | Satisfied |' .planning/REQUIREMENTS.md  # 14
grep -c '| Phase 36 | Satisfied |' .planning/REQUIREMENTS.md  # 3
grep -c '| Phase 40 | Satisfied |' .planning/REQUIREMENTS.md  # 3
```

Optional sanity check (per CONTEXT.md "Claude's Discretion"): run the broader integration meta-suite to confirm no regressions:

```bash
pytest tests/ -x --ignore=agents
```
</verification>

<success_criteria>
- `pytest tests/test_integration_gate.py` exits 0
- `agents/graph-wiki-agent/tests/integration/test_scan_graph_end_to_end.py` contains the canonical `INTEGRATION_GATE = pytest.mark.skipif(not os.environ.get("GRAPH_WIKI_RUN_INTEGRATION"), ...)` constant and the test function is decorated with `@INTEGRATION_GATE`
- `.planning/REQUIREMENTS.md` shows `[x]` for HYGIENE-01..14, CGFIND-01..03, INGESTOR-01..03 (20 total)
- The same 20 REQ-IDs show Status = `Satisfied` in the traceability table
- LIBTOOLS-*, GRAPHCMD-*, SCANNER-* rows and `Complete` statuses untouched
- No new files created; no other files modified; no structural changes to REQUIREMENTS.md
- File remains valid Python (ast parse exits 0); markdown remains rendered identically except for the targeted substitutions
</success_criteria>

<output>
Create `.planning/phases/41-address-v1-7-tech-debt-integration-gate-traceability/41-01-SUMMARY.md` when done.
</output>
</content>
