# v1.3 Tooling Cleanup — Milestone Archive

**Status:** ✅ Shipped 2026-05-20
**Phases:** 17, 18, 19, 20, 21 (5 phases, 25 plans)
**Timeline:** 2026-05-19 → 2026-05-20 (~2 calendar days of dense execution)
**Git range:** `04e6f8a` → `e0c2908` (65 phase commits + tracking)
**Diff:** 186 files changed, +3,396 / −1,117 lines
**Requirements:** 19/19 satisfied (13 declared at scoping + 6 WMC-* backfilled from Phase 20 at close)
**Audit:** [`v1.3-MILESTONE-AUDIT.md`](../v1.3-MILESTONE-AUDIT.md) — gaps_found at audit time (1 brand-gate, 2 doc-refresh), all closed inline before archive

## Milestone Goal

Burn down the v1.2 carry-forward bug list in `vault-io` (`scan_monorepo`, `update_tokens`, `init_vault` / `detect_containers`) and the `/init` plugin command shadow, and address the Phase 16 code review findings so the trace pipeline + eval harness refactor lands clean. Two phases (20 + 21) were added during execution as scope expansions.

## Key Accomplishments

- **vault-io bug burndown complete** — All 3 v1.2 carry-forward bugs fixed in Phase 17: companion-page diff returns 0 phantom deletions (was 28), Bedrock CountTokens uses the correct `input=` param shape, and `init_vault.py` + `detect_containers.py` use `resolve_wiki_and_repo()`'s repo return value at the v2 workspace layout. 8 wiki pages re-stamped to non-zero tokens (TOK-03 operational closure).
- **Plugin command unshadows native `/init`** — Phase 18 renamed `plugins/graph-wiki/commands/init.md` → `bootstrap.md` via `git mv` (history preserved). Typer CLI subcommand `init` → `bootstrap` and MCP tool `wiki_init` → `wiki_bootstrap` with full Pydantic model rename. Brand-gate (`scripts/check-brand.sh` CHECK 2 + CHECK 3 + `.brand-grep-allow`) extended to enforce no reintroduction. SC#3 (manual UAT: typing `/init` in Claude Code fires native CLAUDE.md workflow) remains user-owned at next plugin install.
- **Workspace manifest model config** — Phase 20 moved all model overrides into `<workspace>/.graph-wiki.yaml` `plugins[].roles[]` with per-role fallback to packaged `models.toml`. Hard-cut deleted `WikiConfig.models_path`, `set_models_path()`, `--config` CLI option, and `GRAPH_WIKI_CONFIG` env var. `~/Personal/deep-agents/graph-wiki/.graph-wiki.yaml` populated with the full 9-role block; live verification confirms each role resolves.
- **Mechanical package rename `code-wiki-agent` → `graph-wiki-agent`** — Phase 21 renamed the agent package across directories, Python modules (`code_wiki_agent` / `code_wiki_mcp` → `graph_wiki_agent` / `graph_wiki_mcp`), console scripts, internal symbols, user-facing strings, trace dir (`.code-wiki/` → `.graph-wiki/`), plugin shell-outs, tests, and planning docs. `scripts/check-brand.sh` extended with the new R-04 rule and `.brand-grep-allow` updated for narrow historical exemptions. 5 staged plans across 5 sequential waves.
- **Phase 16 code review burndown** — Phase 19 triaged all 15 Phase 16 review findings (6 warnings + 9 info, 0 critical). 13 → fixed (per-commit gate green: 395 passed / 23 skipped after every commit); 2 → no-action (IN-02 + IN-05 dispositioned as "no-action — review self-corrected on re-scan" per CONTEXT.md `<specifics>`). `19-REVIEW-BURNDOWN.md` is the canonical disposition table for future code-review passes.
- **Audit-gap closures at close** — Inline fixes for the 3 docs-only gaps the milestone audit surfaced: F1 brand-gate hit (1-line `.brand-grep-allow` entry for 19-02-SUMMARY.md narrative), F2 traceability checkboxes (12 of 13 flipped to Complete), F3 WMC-* backfill (6 Phase 20 requirements promoted into REQUIREMENTS.md). Brand-gate exits 0 at HEAD.

## Known Deferred Items at Close

7 items acknowledged (see STATE.md `## Deferred Items`):

- 🔴 **Phase 18 SC#3 manual UAT** — type `/init` in Claude Code with plugin installed, confirm native CLAUDE.md workflow fires. By-design manual gate (Phase 18 D-07); not auto-verifiable. Owned by user on next plugin install.
- 🟡 **2 quick-task status markers stale** — `260519-k9t-preflight-role` + `260519-lf1-bedrock-audit` are shipped (PLAN + SUMMARY exist) but the audit-open index reports them as `missing`. Cosmetic.
- 🔵 **2 phases with open CONTEXT.md question lines** — Phase 18 (3) + Phase 20 (3). All questions were answered during execution; the question markers were not cleared from the CONTEXT.md text.
- 🟡 **Nyquist compliance** — 0/5 v1.3 phases produced VALIDATION.md, same pattern as v1.1 (0/5) and v1.2 (0/6). Retro decision (retro-validate the 16 phases vs. disable the toggle) carried to v1.4 scoping.

## Phase Details

### Phase 17: vault-io Bug Fixes (5 plans)
**Goal:** All three vault-io behavioral bugs are fixed so scan reports accurate diffs, token counts are stamped correctly, and repo/container resolution works at the v2 workspace layout.
**Requirements:** SCAN-01, SCAN-02, TOK-01, TOK-02, TOK-03, WSRES-01, WSRES-02, WSRES-03
**Verification:** `passed` (5/5 SCs satisfied)
**Plans:**
- 17-01 — SCAN companion-page fold + unit tests (SCAN-01, SCAN-02)
- 17-02 — Bedrock CountTokens API shape fix + mocked + gated integration test (TOK-01, TOK-02)
- 17-03 — Workspace/repo resolution fixes in `init_vault.py` + `detect_containers.py` (WSRES-01, WSRES-02, WSRES-03)
- 17-04 — TOK-03 live re-stamp + `17-VERIFICATION.md` (TOK-03)
- 17-05 — Gap closure: plumb `workspace_path` through `_resolve_pinned_containers` + `_discover_heuristic` (WSRES-02 BLOCKER closure)

### Phase 18: Plugin Command Rename (6 plans)
**Goal:** Claude Code's built-in `/init` is reachable again by renaming the conflicting plugin command to `/graph-wiki:bootstrap` with all references updated.
**Requirements:** CMD-01, CMD-02, CMD-03
**Verification:** `human_needed` (7/8 must-haves auto-verified; SC#3 is by-design manual UAT)
**Plans:**
- Wave 1 (parallel): 18-01 (rename `init.md` → `bootstrap.md`, CMD-01), 18-02 (MCP `wiki_init` → `wiki_bootstrap`, CMD-02 MCP), 18-03 (Typer init → bootstrap, CMD-02 CLI)
- Wave 2: 18-04 (sweep 11 active-source refs, CMD-03 active), 18-05 (sweep 18 historical .planning/ refs, CMD-03 historical)
- Wave 3: 18-06 (extend brand-gate + `.brand-grep-allow` + red-then-green sanity, CMD-03 gate)

### Phase 20: Workspace Manifest Model Config (4 plans)
**Goal:** All wiki model-override configuration lives in `<workspace>/.graph-wiki.yaml` `plugins[].roles[]`; packaged `models.toml` becomes per-role fallback; the orphan `wiki-config.toml` pathway is removed.
**Requirements:** WMC-01, WMC-02, WMC-03, WMC-04, WMC-05a, WMC-05b
**Verification:** `passed` (5/5 SCs PASS)
**Plans:**
- 20-01 — Extend `workspace-io.manifest` with `roles[]` round-trip + `read_roles` accessor + README schema docs (WMC-01, WMC-05a)
- 20-02 — Wire workspace-override into `model-adapter.loader.make_llm` with per-role fallback + tests; delete `set_models_path` (WMC-02)
- 20-03 — Delete `WikiConfig.models_path` / `--config` / `GRAPH_WIKI_CONFIG` plumbing + update `test_config.py` (WMC-03)
- 20-04 — Populate `graph-wiki/.graph-wiki.yaml` with full 9-role block + docs sweep (WMC-04, WMC-05b)

### Phase 21: Rename graph-wiki-agent → graph-wiki-agent (5 plans)
**Goal:** Mechanical rename of the agent package across the full repository — directory names, Python modules, console scripts, internal symbols, user-facing strings, trace dir, plugin shell-outs, tests, and planning docs — landed across staged commits with `scripts/check-brand.sh` extended.
**Requirements:** none in REQUIREMENTS.md (accepted scope-by-PLAN-frontmatter pattern; rename itself is the requirement, tracked in PLAN `must_haves`)
**Verification:** `passed` (9/9 must-haves verified)
**Plans:**
- 21-01 — Worktree setup + `git mv` directory moves
- 21-02 — `pyproject.toml` `name` + console scripts + `uv.lock` regeneration
- 21-03 — Sweep imports + identifier renames + kebab strings + trace-dir inside agents/graph-wiki-agent/
- 21-04 — Plugin shellouts + GRAPH_WIKI_* env-var rename + integration-gate test + cross-package `.graph-wiki/` trace-dir sweep
- 21-05 — Full `.planning/` sweep + repo/plugin docs + spike-findings skill + extend `scripts/check-brand.sh` + `.brand-grep-allow` Phase 21 section + SP-2 final two-stage gate

### Phase 19: Phase 16 Code Review Burndown (5 plans)
**Goal:** Every Phase 16 code review finding has a documented disposition (fixed, dismissed with rationale, or converted to a follow-up todo).
**Requirements:** REVIEW-01, REVIEW-02
**Verification:** `passed` (3/3 SCs PASS)
**Note:** Reordered to end of execution queue so Phase 21 rename ships before triaging review findings on the renamed package surface.
**Plans:**
- Wave 1 (parallel — no file-level conflicts): 19-01 (divergence eval regex fixes WR-01..03), 19-02 (core runtime WR-05/WR-06), 19-03 (test + integration WR-04 + IN-03/04/07), 19-04 (query trace + docs IN-01/06/08/09)
- Wave 2 (depends on wave 1 commit SHAs): 19-05 (author `19-REVIEW-BURNDOWN.md` disposition table; record IN-02 + IN-05 as `no-action — review self-corrected on re-scan`)

## Tech Debt Carried Forward

- **`librarian.py:21` `_SLUG_ONLY_RE` PascalCase limitation** — out-of-scope observation from Phase 19 (logged in 19-01-SUMMARY). Same issue as the synthesizer fix; no scope creep allowed. Carry forward if it becomes load-bearing.
- **Nyquist compliance retro** — 0/16 v1.1/v1.2/v1.3 phases produced VALIDATION.md. Decide in v1.4 scoping: retro-validate the 16 phases vs. disable the toggle.
- **Phase 14 SC#4 plugin smoke transcript** — manual `/graph-wiki:query` transcript not captured at v1.2 close; still deferred at v1.3 close. Capture as regression artifact at v1.4 or v2.0.
- **`next-milestone-planning` thread closure** — carried v1.0 → v1.1 → v1.2 → v1.3. Either close at v1.4 scoping or convert to requirements at v1.5.
- **Open-source release prep** — README badges, contribution guide, PyPI publish dry-run. Deferred to v2.0 GA per PROJECT.md.

---

*Archived: 2026-05-20 via `/gsd-complete-milestone 1.3`.*
*Audit report: [`v1.3-MILESTONE-AUDIT.md`](../v1.3-MILESTONE-AUDIT.md).*
