---
phase: 48-graph-propose-domains
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/model-adapter/src/model_adapter/models.toml
  - packages/model-adapter/models.toml
  - packages/model-adapter/tests/test_loader.py
autonomous: true
requirements: [PROPOSE-06]
must_haves:
  truths:
    - "D-19: `model_adapter.make_llm('domain-proposer')` returns a working `_GuardedChatBedrockConverse` instance backed by the new role block in models.toml"
    - "D-21: `model_adapter.make_llm('domain-proposer', model_override='some-model-id')` overrides the model_id while keeping the role's other config"
    - "Loading the `domain-proposer` role does not change the model_id of any other role"
  artifacts:
    - path: "packages/model-adapter/src/model_adapter/models.toml"
      provides: "domain-proposer role definition"
      contains: "[roles.domain-proposer]"
    - path: "packages/model-adapter/tests/test_loader.py"
      provides: "test_domain_proposer_role test case"
      contains: "def test_domain_proposer_role"
  key_links:
    - from: "agents/graph-wiki-agent/src/graph_wiki_agent/commands/propose_domains.py"
      to: "model_adapter.make_llm"
      via: "make_llm('domain-proposer', model_override=...)"
      pattern: "make_llm\\([\"']domain-proposer[\"']"
---

<decisions_implemented>
- **D-19:** New `domain-proposer` role added to both `models.toml` files; initial config mirrors scanner per the CONTEXT note about v1.8 → v1.9 eval revisit.
- **D-21:** `--model` flag thread is verified end-to-end here at the role-loader layer (`make_llm("domain-proposer", model_override=...)`); the CLI wiring lives in Plan 02 + Plan 03.
</decisions_implemented>

<objective>
Add the `domain-proposer` role to `model-adapter` so Phase 48's LLM fan-out has its own tier-tunable model handle (D-19), with `--model` override support (D-21).

Purpose: PROPOSE-06 requires per-LLM-call cost tracking under a distinct role. Distinct role = independent tuning + clean trace filtering. This is a tiny, leaf-only change with no graph-wiki-agent dependency.
Output: Updated `models.toml` (both src and packaged copy) + a unit test proving the role loads and `model_override` works.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-graph-propose-domains/48-CONTEXT.md
@.planning/phases/48-graph-propose-domains/48-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 48-01-01: Add `domain-proposer` role to models.toml and verify with a unit test</name>
  <read_first>
    - packages/model-adapter/src/model_adapter/models.toml (current role table; copy `[roles.scanner]` shape)
    - packages/model-adapter/models.toml (mirror copy at package root; both must be kept in sync per existing pattern)
    - packages/model-adapter/src/model_adapter/loader.py (`make_llm` signature; confirm `model_override` keyword)
    - packages/model-adapter/tests/test_loader.py (existing role test pattern; mirror for the new role)
    - packages/model-adapter/tests/test_narrator_role.py (companion role test; same pattern)
  </read_first>
  <files>packages/model-adapter/src/model_adapter/models.toml, packages/model-adapter/models.toml, packages/model-adapter/tests/test_loader.py</files>
  <action>
    Add a new `[roles.domain-proposer]` block to BOTH `models.toml` files (src and package-root) per D-19. Initial config mirrors `[roles.scanner]`: `model_id = "us.anthropic.claude-haiku-4-5-20251001-v1:0"`, `region = "us-east-1"`, `max_tokens = 1024`, `max_concurrency = 5`. Add a short comment above the block:
      `# Phase 48 D-19 — LLM proposal for `graph propose-domains`. Separate from`
      `# scanner so cost-per-role is trackable and the model can be tuned`
      `# independently. Initial config mirrors scanner; revisit in v1.9 eval.`

    Append a new test `test_domain_proposer_role` to `tests/test_loader.py` modeled after the existing `test_narrator_role` (or the equivalent role-loading test pattern). The test must:
      1. Call `load_role_config("domain-proposer")` and assert the returned dict has the expected `model_id`, `region`, `max_tokens`, `max_concurrency` keys with the values above.
      2. Call `make_llm("domain-proposer")` and assert the returned object is an instance of `_GuardedChatBedrockConverse` (or whatever class the loader returns — match the existing scanner/narrator tests' assertion shape).
      3. Call `make_llm("domain-proposer", model_override="us.amazon.nova-lite-v1:0")` and assert the returned instance's `model_id` attribute equals `"us.amazon.nova-lite-v1:0"` (proving D-21 override works for this role).

    Use the EXACT test naming and assertion style from `test_narrator_role` — do not invent a new test helper.
  </action>
  <verify>
    <automated>uv run --package model-adapter pytest tests/test_loader.py::test_domain_proposer_role -x</automated>
  </verify>
  <done>
    `domain-proposer` role appears in both `models.toml` files with the spec'd values. `pytest tests/test_loader.py::test_domain_proposer_role` exits 0. No other role tests regress (run `uv run --package model-adapter pytest -x` to confirm).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `--model` CLI flag → `make_llm` | User-controlled string flows into Bedrock model_id; could be a typo or unsupported model. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-SC | Tampering | `models.toml` config | accept | Config file is under version control; changes are reviewed. No package installs in this plan, so the v1.7 supply-chain checkpoint pattern does not apply. |
| T-48-MO | Information disclosure | `--model` override path | accept | Invalid model_id surfaces as an `AccessDeniedException` translated to `BedrockAccessDenied` by `_GuardedChatBedrockConverse` (existing adapter behavior); no PII or secrets leak. |
</threat_model>

<verification>
- `uv run --package model-adapter pytest -x` — all role tests pass, including the new `test_domain_proposer_role`.
- `grep -n "\[roles.domain-proposer\]" packages/model-adapter/src/model_adapter/models.toml packages/model-adapter/models.toml` — both files contain the new block (exit 0, two matches).
</verification>

<success_criteria>
- `[roles.domain-proposer]` block exists in both `models.toml` files with `model_id`, `region`, `max_tokens`, `max_concurrency`.
- `make_llm("domain-proposer")` returns a working LLM handle.
- `make_llm("domain-proposer", model_override="...")` overrides the model_id.
- All pre-existing model-adapter tests still pass.
</success_criteria>

<output>
Create `.planning/phases/48-graph-propose-domains/48-01-SUMMARY.md` when done.
</output>
