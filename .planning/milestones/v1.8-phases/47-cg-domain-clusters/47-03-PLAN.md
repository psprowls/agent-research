---
phase: 47-cg-domain-clusters
plan: 03
type: execute
wave: 3
depends_on: ["01", "02"]
files_modified:
  - packages/graph-io/tests/integration/__init__.py
  - packages/graph-io/tests/integration/test_cluster_cli.py
  - packages/graph-io/pyproject.toml
autonomous: true
requirements: [CLUSTER-04, CLUSTER-05]
user_setup: []

must_haves:
  truths:
    - "D-26: tests/integration/test_cluster_cli.py runs `cg domain-clusters` via subprocess against the agent-research repo's own code.db, asserts exit 0 + meaningful clusters or degenerate_warning, and verifies byte-identical stdout across two invocations"
    - "An integration test runs `cg domain-clusters` against the actual agent-research code.db and exits 0 (CLUSTER-04)"
    - "Running `cg domain-clusters --fmt json` twice in succession against the same DB produces byte-identical stdout (CLUSTER-05)"
    - "When the agent-research graph is not initialized in the test environment, the integration test skips cleanly rather than failing"
    - "`integration` is a registered pytest marker in packages/graph-io/pyproject.toml"
  artifacts:
    - path: "packages/graph-io/tests/integration/__init__.py"
      provides: "Empty package marker enabling integration test discovery"
    - path: "packages/graph-io/tests/integration/test_cluster_cli.py"
      provides: "Integration tests against the actual agent-research graph"
      contains: "@pytest.mark.integration"
      min_lines: 80
    - path: "packages/graph-io/pyproject.toml"
      provides: "Registers the integration pytest marker"
      contains: "integration"
  key_links:
    - from: "packages/graph-io/tests/integration/test_cluster_cli.py"
      to: "packages/graph-io/src/graph_io/cli/q_domain_clusters.py"
      via: "subprocess invocation of cg domain-clusters"
      pattern: "domain-clusters"
    - from: "packages/graph-io/pyproject.toml"
      to: "packages/graph-io/tests/integration/test_cluster_cli.py"
      via: "marker registration enables @pytest.mark.integration without warnings"
      pattern: "integration"
---

<objective>
Add the agent-research-self integration tests for `cg domain-clusters`: verify that the
command runs against the actual repo's `code.db`, produces meaningful output (or a
degenerate warning), and produces byte-identical output on repeated invocations. Register
the `integration` pytest marker so `@pytest.mark.integration` does not generate warnings.

Purpose: CLUSTER-04 mandates an integration test that exercises the command against the
`agent-research` graph itself. CLUSTER-05's byte-identical guarantee is re-verified at the
subprocess level (stronger than the in-process determinism test from Plan 01).

Output:
- `packages/graph-io/tests/integration/__init__.py` — package marker
- `packages/graph-io/tests/integration/test_cluster_cli.py` — integration tests
- `packages/graph-io/pyproject.toml` — `integration` marker registered
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/47-cg-domain-clusters/47-CONTEXT.md
@.planning/phases/47-cg-domain-clusters/47-RESEARCH.md
@.planning/phases/47-cg-domain-clusters/47-01-PLAN.md
@.planning/phases/47-cg-domain-clusters/47-02-PLAN.md
@packages/graph-io/pyproject.toml
@packages/graph-io/tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register `integration` marker in packages/graph-io/pyproject.toml</name>
  <files>packages/graph-io/pyproject.toml</files>
  <read_first>packages/graph-io/pyproject.toml</read_first>
  <action>
Edit `packages/graph-io/pyproject.toml`. Find the existing `[tool.pytest.ini_options]` section (currently contains only `testpaths = ["tests"]`). Add a `markers` field:

```
[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
  "integration: requires the agent-research workspace graph (skipped when unavailable)",
]
```

Do NOT modify `testpaths`. Do NOT touch any other section of the file. Preserve TOML formatting exactly.
  </action>
  <verify>uv run --package graph-io python -c "import tomllib; d = tomllib.loads(open('packages/graph-io/pyproject.toml').read()); assert 'markers' in d['tool']['pytest']['ini_options']"</verify>
  <acceptance_criteria>
    - `packages/graph-io/pyproject.toml` contains `markers = [` inside the `[tool.pytest.ini_options]` block
    - `packages/graph-io/pyproject.toml` contains the literal string `integration:`
    - `tomllib.loads(...)` parses the file successfully (no TOML syntax errors)
    - `testpaths = ["tests"]` is still present
  </acceptance_criteria>
  <done>Marker registered; pytest will no longer emit `PytestUnknownMarkWarning` for `@pytest.mark.integration`.</done>
</task>

<task type="auto">
  <name>Task 2: Create tests/integration/ package and integration test file</name>
  <files>packages/graph-io/tests/integration/__init__.py, packages/graph-io/tests/integration/test_cluster_cli.py</files>
  <read_first>packages/graph-io/tests/conftest.py, packages/graph-io/src/graph_io/cli/q_domain_clusters.py, packages/graph-io/src/graph_io/store.py, packages/graph-io/src/graph_io/exit_codes.py, .planning/phases/47-cg-domain-clusters/47-CONTEXT.md, .planning/phases/47-cg-domain-clusters/47-RESEARCH.md</read_first>
  <action>
1. Create empty file `packages/graph-io/tests/integration/__init__.py` (zero bytes — Python package marker; pytest will discover tests via testpaths regardless, but the __init__.py makes the dir a proper subpackage).

2. Create `packages/graph-io/tests/integration/test_cluster_cli.py` with the following content:

Imports:
```
from __future__ import annotations
import json
import subprocess
import sys
from pathlib import Path
import pytest
```

Helpers at module scope:
```
def _repo_root() -> Path:
    """Walk up from this file to find the agent-research repo root."""
    p = Path(__file__).resolve()
    for ancestor in p.parents:
        if (ancestor / "pyproject.toml").exists() and (ancestor / "packages").exists():
            return ancestor
    pytest.skip("Cannot locate agent-research repo root from test file location")

def _cg_cmd() -> list[str]:
    return [sys.executable, "-m", "graph_io.cli.main"]

def _agent_research_graph_available() -> bool:
    """True if the workspace has a code.db that read_only_connect would accept."""
    from workspace_io.config import resolve
    from workspace_io.paths import graph_dir
    try:
        ws = resolve(_repo_root(), require_manifest=False).workspace
        db = graph_dir(ws) / "code.db"
        return db.exists()
    except Exception:
        return False
```

Tests (all decorated with `@pytest.mark.integration`):

```
@pytest.mark.integration
def test_cg_help_lists_command():
    """CLUSTER-04: `cg --help` lists domain-clusters."""
    result = subprocess.run(
        [*_cg_cmd(), "--help"],
        capture_output=True, text=True,
    )
    assert result.returncode == 0
    assert "domain-clusters" in result.stdout

@pytest.mark.integration
def test_subcommand_help_exit_zero():
    """CLUSTER-04: `cg domain-clusters --help` exits 0 and shows --hub-threshold."""
    result = subprocess.run(
        [*_cg_cmd(), "domain-clusters", "--help"],
        capture_output=True, text=True,
    )
    assert result.returncode == 0
    assert "--hub-threshold" in result.stdout

@pytest.mark.integration
def test_run_against_agent_research_graph():
    """CLUSTER-04: command runs against the actual agent-research graph."""
    if not _agent_research_graph_available():
        pytest.skip(
            "agent-research code.db not initialised; run `cg update` from repo root"
        )
    repo = _repo_root()
    result = subprocess.run(
        [*_cg_cmd(), "--repo", str(repo), "--fmt", "json", "domain-clusters"],
        capture_output=True, text=True,
    )
    assert result.returncode == 0, f"stderr={result.stderr}"
    payload = json.loads(result.stdout)
    # Must contain either at least one cluster OR a degenerate_warning explaining why.
    assert (
        len(payload["clusters"]) >= 1
        or payload["degenerate_warning"] is not None
    ), f"Expected clusters or a warning; got {payload}"
    # JSON shape locked by D-20.
    assert list(payload.keys()) == [
        "hub_threshold",
        "n_packages_total",
        "degenerate_warning",
        "clusters",
        "cross_cutting",
    ]

@pytest.mark.integration
def test_byte_identical_repeated_invocation():
    """CLUSTER-05: two invocations produce byte-identical stdout."""
    if not _agent_research_graph_available():
        pytest.skip(
            "agent-research code.db not initialised; run `cg update` from repo root"
        )
    repo = _repo_root()
    cmd = [*_cg_cmd(), "--repo", str(repo), "--fmt", "json", "domain-clusters"]
    out1 = subprocess.check_output(cmd)
    out2 = subprocess.check_output(cmd)
    assert out1 == out2, "Successive `cg domain-clusters` invocations produced different stdout"
```

Notes:
- The integration tests run against the agent-research repo itself. They walk up the filesystem to find the repo root and use `--repo` to point the CLI there.
- If the graph hasn't been initialized (`code.db` missing), the tests skip with a clear message — they do NOT fail. This makes them safe in CI without prior `cg update`.
- The `_cg_cmd()` helper uses `python -m graph_io.cli.main` to avoid PATH dependencies.
- Help-text tests do NOT need the graph; they always run.
- Determinism test does subprocess-level byte comparison — stronger than Plan 01's in-process JSON comparison because it also covers the CLI rendering layer.
  </action>
  <verify>uv run --package graph-io pytest tests/integration/test_cluster_cli.py -x -q -m integration</verify>
  <acceptance_criteria>
    - `packages/graph-io/tests/integration/__init__.py` exists (0-byte file allowed)
    - `packages/graph-io/tests/integration/test_cluster_cli.py` exists
    - File contains `@pytest.mark.integration` at least 4 times
    - File contains `def _repo_root(`, `def _cg_cmd(`, `def _agent_research_graph_available(`
    - File contains `def test_cg_help_lists_command`, `def test_subcommand_help_exit_zero`, `def test_run_against_agent_research_graph`, `def test_byte_identical_repeated_invocation`
    - File contains `pytest.skip(` (skip-when-not-initialized pattern)
    - File contains `list(payload.keys()) == [` (JSON key-order assertion)
    - File contains `subprocess.check_output` (byte-identical comparison)
    - `uv run --package graph-io pytest tests/integration/test_cluster_cli.py -m integration` exits 0 (tests pass or skip cleanly — neither fail nor error)
  </acceptance_criteria>
  <done>Integration tests live in `tests/integration/`, decorated with `@pytest.mark.integration`, and pass or skip cleanly against the current repo.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite verification — Plan 01 + Plan 02 + Plan 03 tests all pass</name>
  <files></files>
  <read_first>packages/graph-io/tests/test_cluster.py, packages/graph-io/tests/integration/test_cluster_cli.py</read_first>
  <action>
Run the full graph-io test suite to confirm no regressions in any pre-existing test and that all Phase 47 tests pass.

Steps:
1. `uv run --package graph-io pytest -x -q`
2. If anything fails, fix it surgically. Do NOT introduce new behavior — this is a verification task.

Also bootstrap the agent-research graph if it's not initialised, so the integration tests in this plan exercise their real-data path rather than skipping:
3. `uv run cg update` from the repo root (if `_agent_research_graph_available()` would otherwise return False).
4. Re-run `uv run --package graph-io pytest -x -q -m integration` to confirm integration tests run (not just skip).

If `cg update` fails for environmental reasons (no git history, missing manifest, etc.), leave the integration tests in their skip state — the skip path is the documented safe behavior. The verification task accepts either: (a) all integration tests pass, or (b) help-text integration tests pass AND the two graph-dependent tests skip with the documented message.
  </action>
  <verify>uv run --package graph-io pytest -x -q</verify>
  <acceptance_criteria>
    - `uv run --package graph-io pytest -x -q` exits 0
    - Output shows `0 failed` and `0 errors`
    - At least 16 tests collected across `tests/test_cluster.py` and `tests/integration/test_cluster_cli.py`
    - No `PytestUnknownMarkWarning` related to `integration` in pytest output (marker is registered)
  </acceptance_criteria>
  <done>Full graph-io test suite green; Phase 47 ships with passing unit + integration coverage.</done>
</task>

</tasks>

<verification>
- [ ] `uv run --package graph-io pytest -x -q` exits 0
- [ ] `packages/graph-io/tests/integration/test_cluster_cli.py` exists and contains 4 @pytest.mark.integration tests
- [ ] `packages/graph-io/pyproject.toml` registers the `integration` marker
- [ ] `uv run cg domain-clusters --fmt json` (when graph is initialized) returns byte-identical output twice
</verification>

<success_criteria>
- All three tasks completed
- Integration test suite runs against agent-research repo (or skips cleanly when graph absent)
- Full graph-io test suite green
- No new PytestUnknownMarkWarning
- Phase 47 end-to-end: cg domain-clusters is shippable
</success_criteria>

<output>
After completion, create `.planning/phases/47-cg-domain-clusters/47-03-SUMMARY.md`
</output>
