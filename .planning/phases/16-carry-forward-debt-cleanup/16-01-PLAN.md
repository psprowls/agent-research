---
phase: 16-carry-forward-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/subagent-runtime/src/subagent_runtime/trace_io.py
  - packages/subagent-runtime/src/subagent_runtime/pool.py
  - packages/subagent-runtime/tests/test_trace_io.py
  - agents/code-wiki-agent/src/code_wiki_agent/commands/ingest.py
  - agents/code-wiki-agent/src/code_wiki_agent/commands/query.py
  - agents/code-wiki-agent/tests/integration/test_trace_coverage.py
  - packages/eval-harness/src/eval_harness/divergence/code_reader.py
  - packages/eval-harness/src/eval_harness/divergence/synthesizer.py
  - packages/eval-harness/src/eval_harness/divergence/rubrics/code_reader.md
  - packages/eval-harness/src/eval_harness/divergence/rubrics/synthesizer.md
  - packages/eval-harness/src/eval_harness/divergence/__init__.py
  - packages/eval-harness/src/eval_harness/divergence/metric.py
  - packages/eval-harness/src/eval_harness/two_gate.py
  - packages/eval-harness/tests/fixtures/post-rebrand-vault/
  - packages/eval-harness/tests/test_scanner_regression.py
  - eval/cases/code_reader_cases.json
  - packages/eval-harness/tests/test_models_toml_sweep_candidates.py
  - docs/cancellation.md
  - docs/testing.md
  - tests/test_integration_gate.py
  - packages/model-adapter/tests/test_loader.py
  - .planning/phases/16-carry-forward-debt-cleanup/16-VERIFICATION.md
autonomous: true
requirements:
  - TRACE-FU-01
  - SWEEP-FU-02
  - SWEEP-FU-03
  - SWEEP-FU-04
  - MCP-CAN-01
  - MCP-CAN-02
  - MODEL-FU-01
user_setup: []

must_haves:
  truths:
    - "Every production trace JSONL record from commands/{scan,lint,ingest,query} includes input_tokens/output_tokens populated from usage_metadata (None allowed only on Bedrock error responses)"
    - "A real-fan-out integration test gated by CODE_WIKI_RUN_INTEGRATION=1 asserts every non-error JSONL record under .code-wiki/traces/ has non-None token fields"
    - "ROLES_WITH_DIVERGENCE contains all 6 in-scope roles (librarian, ingestor, linter, scanner, code_reader, synthesizer) and Gate 1 runs for all 6"
    - "eval/cases/code_reader_cases.json contains 5–6 cases and every case produces a non-trivial score against the current corpus"
    - "A synthetic post-rebrand fixture vault exists and a CI-runnable scanner regression test runs against it without regression vs. v1.1 baseline"
    - "Either the MCP wire-level cancel spike produces a working integration path decision, or docs/cancellation.md §4–§5 carries an event-driven (not date-driven) re-eval trigger naming langchain-aws#663 release OR aioboto3 GA/1.0"
    - "docs/testing.md exists, documents the canonical CODE_WIKI_RUN_INTEGRATION skip-decorator, inventories every gated test file, and a grep gate fails CI on divergence"
    - "test_load_role_config_synthesizer_limits asserts cfg['model_id'] == 'qwen.qwen3-32b-v1:0' and uv run pytest is green"
    - "16-VERIFICATION.md exists with per-SC sections #1–#5 citing trace JSONL output, sweep scores, cancel spike decision, grep-gate output, and synthesizer model_id assertion"
  artifacts:
    - path: "packages/subagent-runtime/src/subagent_runtime/trace_io.py"
      provides: "Shared JSONL trace-record writer (D-04 extraction)"
      exports: ["write_trace_record"]
    - path: "agents/code-wiki-agent/tests/integration/test_trace_coverage.py"
      provides: "Gated TRACE-FU-01 regression test (D-05)"
    - path: "packages/eval-harness/src/eval_harness/divergence/code_reader.py"
      provides: "code_reader divergence checks (D-06)"
      exports: ["CODE_READER_CHECKS"]
    - path: "packages/eval-harness/src/eval_harness/divergence/synthesizer.py"
      provides: "synthesizer divergence checks (D-06)"
      exports: ["SYNTHESIZER_CHECKS"]
    - path: "packages/eval-harness/src/eval_harness/divergence/rubrics/code_reader.md"
      provides: "code_reader judge rubric"
    - path: "packages/eval-harness/src/eval_harness/divergence/rubrics/synthesizer.md"
      provides: "synthesizer judge rubric"
    - path: "packages/eval-harness/tests/fixtures/post-rebrand-vault/"
      provides: "Synthetic fixture vault for CI scanner regression (D-11a)"
    - path: "packages/eval-harness/tests/test_scanner_regression.py"
      provides: "Scanner regression test against fixture vault (SWEEP-FU-04)"
    - path: "docs/testing.md"
      provides: "Canonical CODE_WIKI_RUN_INTEGRATION gate rule + inventory (D-10)"
    - path: "tests/test_integration_gate.py"
      provides: "Grep gate pytest meta-test (D-10 enforcement)"
    - path: ".planning/phases/16-carry-forward-debt-cleanup/16-VERIFICATION.md"
      provides: "Per-SC evidence (transcripts, scores, spike decision, grep-gate output)"
  key_links:
    - from: "packages/subagent-runtime/src/subagent_runtime/pool.py::_write_trace"
      to: "packages/subagent-runtime/src/subagent_runtime/trace_io.py::write_trace_record"
      via: "thin-delegate refactor"
      pattern: "from subagent_runtime.trace_io import write_trace_record"
    - from: "agents/code-wiki-agent/src/code_wiki_agent/commands/ingest.py"
      to: "packages/subagent-runtime/src/subagent_runtime/trace_io.py::write_trace_record"
      via: "direct helper call after llm.ainvoke"
      pattern: "write_trace_record\\("
    - from: "agents/code-wiki-agent/src/code_wiki_agent/commands/query.py:977"
      to: "usage_metadata token extraction"
      via: "lift None-guard from pool.py:203-209 into query_summary record"
      pattern: "tokens_in.*usage_metadata"
    - from: "packages/eval-harness/src/eval_harness/two_gate.py:36"
      to: "ROLES_WITH_DIVERGENCE frozenset"
      via: "frozenset extension to 6 elements"
      pattern: "code_reader.*synthesizer"
    - from: "packages/eval-harness/src/eval_harness/divergence/__init__.py"
      to: "ROLE_CHECKS + ROLE_RUBRICS dicts"
      via: "register CODE_READER_CHECKS, SYNTHESIZER_CHECKS, and rubric paths"
      pattern: "code_reader.*synthesizer"
---

<objective>
Close the v1.1 carry-forward debt: trace pipeline gap (TRACE-FU-01), sweep matrix coverage (SWEEP-FU-02/03/04), MCP wire-level cancel closure or re-deferral (MCP-CAN-01), CODE_WIKI_RUN_INTEGRATION gate consistency (MCP-CAN-02), and synthesizer model-config test drift (MODEL-FU-01). Per D-14, all work lands in this single bundled atomic plan with 9 per-step commits.

Purpose: v1.2 cannot ship until these debt items either resolve in code/tests OR carry explicitly-documented re-deferral evidence with event-driven re-eval triggers. The Bedrock path (`code-wiki-agent` / `subagent-runtime`) is the entire surface — plugin/Claude-Code path is out per `[[project_plugin_port_model]]`.

Output: New `trace_io` helper, two new divergence rule modules + rubrics, expanded `code_reader_cases.json`, synthetic fixture vault + scanner regression test, refreshed `docs/cancellation.md` (if spike gate fails) plus new `docs/testing.md` + grep gate, extended synthesizer test, `16-VERIFICATION.md` with per-SC evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-carry-forward-debt-cleanup/16-CONTEXT.md
@.planning/phases/16-carry-forward-debt-cleanup/16-PATTERNS.md
@docs/trace-schema.md
@docs/cancellation.md
@packages/subagent-runtime/src/subagent_runtime/pool.py
@packages/eval-harness/src/eval_harness/two_gate.py
@packages/eval-harness/src/eval_harness/divergence/librarian.py
@packages/eval-harness/src/eval_harness/divergence/scanner.py
@packages/eval-harness/src/eval_harness/divergence/__init__.py
@packages/eval-harness/src/eval_harness/divergence/metric.py
@packages/eval-harness/src/eval_harness/divergence/rubrics/librarian.md
@eval/cases/code_reader_cases.json
@agents/code-wiki-agent/tests/conftest.py
@agents/code-wiki-agent/tests/integration/test_query_e2e.py
@packages/model-adapter/tests/test_loader.py
@packages/model-adapter/src/model_adapter/models.toml

<interfaces>
<!-- Key contracts the executor will use. Extracted from the patterns map. -->

write_trace_record contract (NEW — D-04 helper extraction):
```python
def write_trace_record(
    path: Path,
    role: str,
    model_id: str,
    item: Any,
    status: str,           # "success" | "cancelled" | "error"
    latency_ms: int,
    response: Any,         # ChatBedrockConverse response or None
    *,
    error: str | None = None,
) -> None:
    """Writes one JSONL record. Never raises (OSError swallowed)."""
```

Existing call sites that MUST be preserved after refactor (pool.py:137-151):
```python
self._write_trace(trace_file, role, model_id, item, "success", latency_ms, result)
self._write_trace(trace_file, role, model_id, item, "cancelled", latency_ms, None)
self._write_trace(trace_file, role, model_id, item, "error", latency_ms, None, error=str(exc))
```

DivergenceCheck contract (existing — packages/eval-harness/src/eval_harness/divergence/check.py):
```python
@dataclass
class DivergenceCheck:
    id: str                # e.g. "CR-001-no-invention"
    source_anchor: str     # "packages/prompt-sources/agents/<role>.md#<section>"
    severity: Literal["hard", "soft"]
    check: Callable[[AgentOutputProxy, Path], Verdict]
```

ROLES_WITH_DIVERGENCE extension target (two_gate.py:36):
```python
ROLES_WITH_DIVERGENCE: frozenset[str] = frozenset(
    {"librarian", "ingestor", "linter", "scanner", "code_reader", "synthesizer"}
)
```

CODE_WIKI_RUN_INTEGRATION canonical pattern (conftest.py:19-22):
```python
INTEGRATION_GATE = pytest.mark.skipif(
    not os.environ.get("CODE_WIKI_RUN_INTEGRATION"),
    reason="Set CODE_WIKI_RUN_INTEGRATION=1 to run real Bedrock invocations",
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract trace writer to trace_io.py and refactor pool._write_trace to delegate (D-04)</name>
  <files>packages/subagent-runtime/src/subagent_runtime/trace_io.py, packages/subagent-runtime/src/subagent_runtime/pool.py, packages/subagent-runtime/tests/test_trace_io.py</files>
  <read_first>
    - packages/subagent-runtime/src/subagent_runtime/pool.py (full file — esp. lines 27-39 imports, 121-151 call sites, 182-231 _write_trace body)
    - packages/subagent-runtime/tests/ (locate existing pool trace tests to understand the test shape)
    - docs/trace-schema.md (confirm schema_version: 1 field set; helper must preserve verbatim)
    - .planning/phases/16-carry-forward-debt-cleanup/16-PATTERNS.md (§"NEW trace_io.py" + §"MOD pool.py" — exact extraction pattern with full record-construction body)
    - .planning/phases/16-carry-forward-debt-cleanup/16-CONTEXT.md §decisions D-03/D-04 (locked decisions)
  </read_first>
  <action>
    Create `packages/subagent-runtime/src/subagent_runtime/trace_io.py` containing a module-level `write_trace_record(path, role, model_id, item, status, latency_ms, response, *, error=None) -> None` function. Extract the record construction + JSONL append logic VERBATIM from `pool.py:194-231` (per D-04: pure extraction, no behavior change). Preserve all four invariants: (a) `usage_metadata is None` guard on `ChatBedrockConverse` responses (pattern from `pool.py:203-209`), (b) `schema_version: 1` on every record, (c) OSError on write caught + logged WARNING, never raises (pattern from `pool.py:227-231`), (d) `_compute_cost_usd` call preserved — locate it in `pool.py` and either move it alongside `write_trace_record` in `trace_io.py` (preferred) or import it back from `pool.py` (avoid circular import). Module imports: `from __future__ import annotations`, `json`, `logging`, `time`, `Path`, `Any`. `logger = logging.getLogger(__name__)`.

    Refactor `pool.py::SubagentPool._write_trace` to a thin delegate: `from subagent_runtime.trace_io import write_trace_record` and call it. All three call sites in `_run_one` (pool.py:137-151, the success/cancelled/error invocations) stay byte-identical — they continue to call `self._write_trace(...)`.

    Create `packages/subagent-runtime/tests/test_trace_io.py` with three unit tests against the helper directly (no Bedrock, no fan-out): (1) `test_write_trace_record_writes_valid_jsonl_with_tokens` — pass a mock response object with `usage_metadata = {"input_tokens": 100, "output_tokens": 50, "total_tokens": 150}`, parse the appended JSONL line, assert `schema_version == 1`, `tokens_in == 100`, `tokens_out == 50`, `status == "success"`. (2) `test_write_trace_record_handles_none_usage_metadata` — pass a response with `usage_metadata = None` (Bedrock error simulation), assert `tokens_in is None` and `tokens_out is None` but record still written. (3) `test_write_trace_record_swallows_oserror` — pass a path inside a non-existent directory or use monkeypatch on `Path.open` to raise `OSError`, assert function returns normally (no raise).

    Commit as step 1 of D-14's 9-step sequence with message: `refactor(subagent-runtime): extract trace writer to trace_io.py (TRACE-FU-01 D-04)`.
  </action>
  <verify>
    <automated>uv run --package subagent-runtime pytest packages/subagent-runtime/tests/test_trace_io.py -x -v && uv run --package subagent-runtime pytest packages/subagent-runtime/tests/ -x</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/subagent-runtime/src/subagent_runtime/trace_io.py` exists with exported `write_trace_record` function
    - `grep -n "from subagent_runtime.trace_io import write_trace_record" packages/subagent-runtime/src/subagent_runtime/pool.py` returns one or more matches
    - `pool.py::_write_trace` body is a thin delegate (under 5 lines of body code, no record-construction logic remaining)
    - `grep -c "schema_version" packages/subagent-runtime/src/subagent_runtime/trace_io.py` ≥ 1
    - `grep -c "usage_metadata" packages/subagent-runtime/src/subagent_runtime/trace_io.py` ≥ 1 (None-guard preserved)
    - `grep -c "except OSError" packages/subagent-runtime/src/subagent_runtime/trace_io.py` ≥ 1 (write-failure swallow preserved)
    - All 3 new unit tests in `test_trace_io.py` pass
    - Existing `packages/subagent-runtime/tests/` suite continues to pass (no regressions)
    - Git log shows one commit with message starting `refactor(subagent-runtime): extract trace writer`
  </acceptance_criteria>
  <done>trace_io.py helper exists, pool.py delegates to it, unit tests green, existing pool tests still pass, step-1 commit landed.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor ingest.py and query.py to write trace records via the helper (D-03) + add gated regression test (D-05)</name>
  <files>agents/code-wiki-agent/src/code_wiki_agent/commands/ingest.py, agents/code-wiki-agent/src/code_wiki_agent/commands/query.py, agents/code-wiki-agent/tests/integration/test_trace_coverage.py</files>
  <read_first>
    - agents/code-wiki-agent/src/code_wiki_agent/commands/ingest.py (full file — esp. lines 420-460 around the direct `llm.ainvoke` call at line 438)
    - agents/code-wiki-agent/src/code_wiki_agent/commands/query.py (lines 950-1000 around the `query_summary` record at line 977)
    - agents/code-wiki-agent/tests/integration/test_query_e2e.py (canonical gated test shape — module preamble, INTEGRATION_GATE, subprocess pattern)
    - agents/code-wiki-agent/tests/conftest.py:19-22 (canonical INTEGRATION_GATE)
    - packages/subagent-runtime/src/subagent_runtime/trace_io.py (the new helper from Task 1)
    - .planning/phases/16-carry-forward-debt-cleanup/16-PATTERNS.md (§"MOD ingest.py" + §"MOD query.py" + §"NEW test_trace_coverage.py" — concrete shapes including trace-dir derivation pattern)
    - .planning/phases/16-carry-forward-debt-cleanup/16-CONTEXT.md §decisions D-03/D-05
  </read_first>
  <action>
    **Refactor ingest.py:438** — Around the `resp = await llm.ainvoke(...)` call site in `ingest.py` (currently writing no trace at all): wrap in `t0 = time.monotonic()` start and `latency_ms = int((time.monotonic() - t0) * 1000)` stop. Compute `trace_dir = wiki / ".code-wiki" / "traces"` and `trace_dir.mkdir(parents=True, exist_ok=True)` (pattern lifted from `query.py:977-979`). Resolve `resolved_model_id` from `ingestor_cfg["model_id"]` (or `model_override` when set). Call `write_trace_record(trace_file_path, role="ingestor", model_id=resolved_model_id, item=<source_path or stable identifier>, status="success", latency_ms=latency_ms, response=resp)` on the success path. On exception, call the helper with `status="error", response=None, error=str(exc)` (preserve existing exception propagation). Trace file path: use the same per-run JSONL filename convention as `pool.py` (the helper itself doesn't pick paths — caller decides).

    **Backfill query.py:977 query_summary** — The `summary_record` dict (query.py:980-994) is a `kind: "query_summary"` discriminator record (NOT a per-item record routed through `write_trace_record`, which writes per-item records). Per D-03's intent of "every call site produces tokens-populated trace records", choose the shape per scout: prefer **synthesizer-specific** `tokens_in` / `tokens_out` fields on the summary record, populated by lifting the None-guard block verbatim from `pool.py:203-209` and applying it to the synthesizer LLM response object. The synthesizer call is the LLM call that produces the final answer; locate it in `query.py` (search for `ChatBedrockConverse` invocation or the role="synthesizer" call) and capture `tokens_in_synth` + `tokens_out_synth`, then add both fields to `summary_record`. Preserve the existing OSError-swallow at `query.py:995-996`.

    Also locate the **synthesizer per-item call** in `query.py` (the actual `await llm.ainvoke(...)` for the synthesizer): if it does NOT already write a per-item JSONL record, route it through `write_trace_record(...)` with `role="synthesizer"`. If it already writes through `SubagentPool`, no change needed there — only the summary backfill applies.

    **Create gated regression test** `agents/code-wiki-agent/tests/integration/test_trace_coverage.py` modeled on `test_query_e2e.py`. Module preamble: docstring "End-to-end TRACE-FU-01 regression: real fan-out asserts every JSONL record has non-None input_tokens/output_tokens (SC#1 D-05). Gated by CODE_WIKI_RUN_INTEGRATION=1." Imports: `json`, `os`, `subprocess`, `Path`, `pytest`. Define `INTEGRATION_GATE` using the canonical pattern from `conftest.py:19-22`. Test function `test_trace_pipeline_records_token_usage(tmp_path)` runs the sequence: (a) `subprocess.run(["uv", "run", "--package", "code-wiki-agent", "code-wiki-agent", "scan", "--vault", str(tmp_path / "wiki"), ...])`, (b) `ingest`, (c) `query "..."`. After the run, iterate `(tmp_path / "wiki" / ".code-wiki" / "traces").glob("*.jsonl")`; for each JSON line, parse and skip records where `rec.get("status") == "error"` OR `"event" in rec` (batch terminal records) OR `rec.get("kind") == "query_summary"` (separate shape — assertions on summary record are inline: if `kind == "query_summary"`, assert `rec.get("tokens_in") is not None` AND `rec.get("tokens_out") is not None`). For all other (per-item) records, assert `rec["tokens_in"] is not None` and `rec["tokens_out"] is not None` with `rec` included in the failure message. Mark with `@pytest.mark.integration` and `@INTEGRATION_GATE`.

    Pre-seed `tmp_path / "wiki"` either by copying an existing fixture vault (search for `FIXTURE_VAULT` patterns in `test_query_e2e.py`) or by running the wiki bootstrap before scan.

    Commit as step 2: `feat(code-wiki-agent): trace usage_metadata on ingest + query_summary call sites (TRACE-FU-01 D-03/D-05)`.
  </action>
  <verify>
    <automated>uv run pytest agents/code-wiki-agent/tests/ -x --co -q | grep -q test_trace_pipeline_records_token_usage && uv run pytest agents/code-wiki-agent/tests/ -x -q -k "not integration" && CODE_WIKI_RUN_INTEGRATION=1 uv run pytest agents/code-wiki-agent/tests/integration/test_trace_coverage.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `agents/code-wiki-agent/tests/integration/test_trace_coverage.py` exists and is collected by pytest
    - `grep -c "write_trace_record" agents/code-wiki-agent/src/code_wiki_agent/commands/ingest.py` ≥ 1 (success path) and ≥ 2 total (success + error paths)
    - `grep -c "tokens_in" agents/code-wiki-agent/src/code_wiki_agent/commands/query.py | grep -v '^#'` ≥ 1 (summary backfill landed)
    - `grep -n "usage_metadata" agents/code-wiki-agent/src/code_wiki_agent/commands/query.py` returns the None-guard block (lifted from pool.py:203-209)
    - `test_trace_pipeline_records_token_usage` is skipped without `CODE_WIKI_RUN_INTEGRATION=1`, passes with it set (asserts every non-error/non-event JSONL record has non-None tokens)
    - Non-integration test subset for `agents/code-wiki-agent/` remains green
    - Git log shows step-2 commit with message starting `feat(code-wiki-agent): trace usage_metadata`
  </acceptance_criteria>
  <done>ingest.py emits per-call trace records, query.py summary record carries synthesizer token fields, gated regression test green under CODE_WIKI_RUN_INTEGRATION=1, step-2 commit landed.</done>
</task>

<task type="auto">
  <name>Task 3: Author code_reader + synthesizer divergence rule modules and rubrics (D-06)</name>
  <files>packages/eval-harness/src/eval_harness/divergence/code_reader.py, packages/eval-harness/src/eval_harness/divergence/synthesizer.py, packages/eval-harness/src/eval_harness/divergence/rubrics/code_reader.md, packages/eval-harness/src/eval_harness/divergence/rubrics/synthesizer.md, packages/eval-harness/src/eval_harness/divergence/__init__.py, packages/eval-harness/src/eval_harness/divergence/metric.py, packages/eval-harness/src/eval_harness/two_gate.py</files>
  <read_first>
    - packages/eval-harness/src/eval_harness/divergence/librarian.py (full file — module structure, _check_* callables, LIBRARIAN_CHECKS export shape, AgentOutputProxy/DivergenceCheck/Verdict types)
    - packages/eval-harness/src/eval_harness/divergence/scanner.py (full file — alternate hard-only rule shape for cross-reference)
    - packages/eval-harness/src/eval_harness/divergence/check.py (DivergenceCheck / Verdict / AgentOutputProxy contracts)
    - packages/eval-harness/src/eval_harness/divergence/__init__.py (ROLE_CHECKS dict + ROLE_RUBRICS dict registration shape)
    - packages/eval-harness/src/eval_harness/divergence/metric.py (esp. lines 40-45 `_ROLE_JUDGE_ID` mapping — extend with CR-JUDGE + SYN-JUDGE)
    - packages/eval-harness/src/eval_harness/divergence/rubrics/librarian.md (header comments, scoring section, judge-only ID shape)
    - packages/eval-harness/src/eval_harness/divergence/rubrics/scanner.md (alternate rubric shape if present)
    - packages/eval-harness/src/eval_harness/two_gate.py (esp. lines 11-12 docstring + line 36 ROLES_WITH_DIVERGENCE + lines 103-126 Gate 1 branch)
    - packages/eval-harness/tests/test_two_gate_scorer.py:173-227 (current synthesizer-skips-Gate-1 behavior — flip after this task)
    - packages/prompt-sources/agents/code_reader.md (canonical source anchor for CR rule IDs)
    - packages/prompt-sources/agents/synthesizer.md (canonical source anchor for SYN rule IDs)
    - .planning/phases/16-carry-forward-debt-cleanup/16-PATTERNS.md §"NEW code_reader.py & synthesizer.py" + §"NEW Rubric files" + §"MOD two_gate.py:36"
    - .planning/phases/16-carry-forward-debt-cleanup/16-CONTEXT.md §decisions D-06
  </read_first>
  <action>
    **Author `code_reader.py`** at `packages/eval-harness/src/eval_harness/divergence/code_reader.py` modeled on `librarian.py`. Imports: `from __future__ import annotations`, `re`, `Path`, `from eval_harness.divergence.check import AgentOutputProxy, DivergenceCheck, Verdict`. Include the security comment block (T-06-15 — regex/string only, no eval/exec). Author 3 hard + 1 soft programmatic checks targeting `code_reader` behaviors derived from `packages/prompt-sources/agents/code_reader.md`. Each check: `def _check_<name>(output: AgentOutputProxy, vault: Path) -> Verdict` returning `Verdict(passed=..., excerpt=...)` with excerpts ≤200 chars. Export `CODE_READER_CHECKS: list[DivergenceCheck] = [DivergenceCheck(id="CR-001-<slug>", source_anchor="packages/prompt-sources/agents/code_reader.md#<section>", severity="hard", check=_check_<name>), ...]`. Use rule-ID prefix `CR-`. Match Phase-6 rigor: ground each rule in a concrete prompt-source section anchor, not a generic heuristic. Per D-06, if the executor's scout reveals that one of the two roles cannot take a programmatic rubric, surface that as a deviation (do not silently downgrade).

    **Author `synthesizer.py`** at `packages/eval-harness/src/eval_harness/divergence/synthesizer.py` with the same module shape. Author 3 hard + 1 soft programmatic checks targeting `synthesizer` behaviors derived from `packages/prompt-sources/agents/synthesizer.md`. Use rule-ID prefix `SYN-`. Export `SYNTHESIZER_CHECKS`.

    **Author rubric `rubrics/code_reader.md`** at `packages/eval-harness/src/eval_harness/divergence/rubrics/code_reader.md` modeled on `rubrics/librarian.md`. Top: 3 HTML comments (Source: `packages/prompt-sources/agents/code_reader.md`, Anchor: `## Rules + ## Red flags` or equivalent section names found during read, Source-commit: current git SHA of the prompt source). Body: `# Divergence Rubric — Code Reader` heading, intro paragraph naming the canonical spec, `## Scoring Criteria` with 2–3 judge-only rules numbered `CR-005-<slug>`, `CR-006-<slug>` (numbering after the programmatic rule IDs), each rule as bold ID + 1-paragraph description + `(pass/fail)` suffix. `## Scoring` section explaining 0.0–1.0 fraction-of-criteria-passed mapping.

    **Author rubric `rubrics/synthesizer.md`** with the same shape, judge-only rules `SYN-005-<slug>` etc.

    **Register in `divergence/__init__.py`** — import `CODE_READER_CHECKS` and `SYNTHESIZER_CHECKS`; extend `ROLE_CHECKS` dict to include both; extend `ROLE_RUBRICS` dict to include `_RUBRICS_DIR / "code_reader.md"` and `_RUBRICS_DIR / "synthesizer.md"`.

    **Extend `metric.py:40-45 _ROLE_JUDGE_ID`** to add `"code_reader": "CR-JUDGE"` and `"synthesizer": "SYN-JUDGE"`.

    **Extend `two_gate.py:36 ROLES_WITH_DIVERGENCE`** one-line change: add `"code_reader"` and `"synthesizer"` to the frozenset. Update the module docstring at lines 11-12: replace any "D-08 (synthesizer, code_reader): Gate 2 only" comment with "All 6 in-scope roles run both gates (D-06 Phase 16)". After this change, verify callers in `sweep.py` pass the new metrics — search for `divergence_metric_or_none` and ensure the new roles are reachable.

    **Flip the synthesizer-skips-Gate-1 test** at `packages/eval-harness/tests/test_two_gate_scorer.py:173-227`: update the test to assert that synthesizer NOW runs Gate 1 (or delete/rename the test if its sole purpose was to assert the skip behavior — preserve baseline-comparability tests, change-behavior tests update).

    Commit as step 3: `feat(eval-harness): divergence rubrics for code_reader + synthesizer (SWEEP-FU-02 D-06)`.
  </action>
  <verify>
    <automated>uv run --package eval-harness pytest packages/eval-harness/tests/ -x -q && python3 -c "from eval_harness.divergence import ROLE_CHECKS, ROLE_RUBRICS; assert 'code_reader' in ROLE_CHECKS and 'synthesizer' in ROLE_CHECKS; assert 'code_reader' in ROLE_RUBRICS and 'synthesizer' in ROLE_RUBRICS; from eval_harness.two_gate import ROLES_WITH_DIVERGENCE; assert ROLES_WITH_DIVERGENCE == frozenset({'librarian','ingestor','linter','scanner','code_reader','synthesizer'})"</automated>
  </verify>
  <acceptance_criteria>
    - Files `code_reader.py` and `synthesizer.py` exist under `packages/eval-harness/src/eval_harness/divergence/` with `CODE_READER_CHECKS` and `SYNTHESIZER_CHECKS` exports respectively
    - Each module has 3 hard + 1 soft programmatic check (4 total) — `grep -c 'severity="hard"' .../code_reader.py` == 3 and `grep -c 'severity="soft"' .../code_reader.py` == 1; same for synthesizer.py
    - Each `DivergenceCheck.source_anchor` points to a real anchor in `packages/prompt-sources/agents/{code_reader,synthesizer}.md` (file path exists)
    - Rubric files `rubrics/code_reader.md` and `rubrics/synthesizer.md` exist with the 3 HTML-comment header (Source/Anchor/Source-commit) and `## Scoring Criteria` + `## Scoring` sections
    - `python3 -c "from eval_harness.divergence import ROLE_CHECKS; assert set(ROLE_CHECKS) >= {'librarian','ingestor','linter','scanner','code_reader','synthesizer'}"` succeeds
    - `two_gate.py:36` ROLES_WITH_DIVERGENCE equals `frozenset({"librarian","ingestor","linter","scanner","code_reader","synthesizer"})`
    - `metric.py` _ROLE_JUDGE_ID contains entries for `code_reader` and `synthesizer`
    - `uv run --package eval-harness pytest` is green
    - Git log shows step-3 commit
  </acceptance_criteria>
  <done>Two new divergence rule modules + rubrics exist, registered in __init__.py and metric.py, two_gate.py expanded to 6 roles, eval-harness tests green, step-3 commit landed.</done>
</task>

<task type="auto">
  <name>Task 4: Expand code_reader_cases.json to 5–6 cases and retune (D-07)</name>
  <files>eval/cases/code_reader_cases.json, packages/eval-harness/tests/test_models_toml_sweep_candidates.py</files>
  <read_first>
    - eval/cases/code_reader_cases.json (existing 3 cases — preserve verbatim for baseline comparability per D-07)
    - eval/cases/query_cases.json (reference for case JSON shape if code_reader cases need richer structure)
    - packages/eval-harness/tests/test_models_toml_sweep_candidates.py:22-51 (`IN_SCOPE_ROLES` definition + `code_reader_cases.json` count/path constraints — check whether hard-coded count assertion needs bumping)
    - packages/workspace-io/src/workspace_io/ (target candidate: workspace-io for new case)
    - packages/vault-io/src/vault_io/lint_wiki.py (target candidate for case — Phase 14 ported file)
    - packages/vault-io/src/vault_io/wiki_search.py (target candidate for case — Phase 14 ported file)
    - plugins/graph-wiki/ (target candidate: graph-wiki plugin entry points if dir exists post-Phase 14)
    - .planning/phases/16-carry-forward-debt-cleanup/16-PATTERNS.md §"NEW code_reader_cases.json"
    - .planning/phases/16-carry-forward-debt-cleanup/16-CONTEXT.md §decisions D-07
  </read_first>
  <action>
    Preserve the existing 3 cases in `eval/cases/code_reader_cases.json` UNMODIFIED for baseline comparability (D-07 explicit). Append 2–3 new cases continuing `case_id` numbering `code-reader-04`, `code-reader-05`, optionally `code-reader-06`. Each new case follows the existing JSON shape: `{"case_id": "code-reader-NN", "query": "<vault-thin question targeting post-rebrand surface>", "expected_answer": "cannot be answered from vault pages alone; requires reading <specific source file path>", "tags": ["code-reader", "vault-thin"]}`. Target candidates (pick the 2–3 most thematically distinct):

    - `workspace-io` — query: "How does GraphWikiConfig.resolve() walk upward for .graph-wiki.yaml?" expected answer cites `packages/workspace-io/src/workspace_io/config.py`.
    - `graph-wiki` plugin entry — query: "What does the /graph-wiki:query slash command shell out to?" expected answer cites `plugins/graph-wiki/commands/<file>.sh` or equivalent (verify file exists during scout — if Phase 14 plan 3 not yet landed, pick a stable alternative).
    - `vault-io.lint_wiki` — query targeting a specific function in the ported `packages/vault-io/src/vault_io/lint_wiki.py`.
    - `vault-io.wiki_search` — query targeting a specific behavior in the ported `packages/vault-io/src/vault_io/wiki_search.py`.

    Each `expected_answer` MUST use the "cannot be answered from vault pages alone; requires reading <path>" prose pattern that forces the code-fallback path (vault-thin invariant).

    Check `packages/eval-harness/tests/test_models_toml_sweep_candidates.py:22-51` — if any assertion hard-codes `len(code_reader_cases) == 3`, bump to the new count. If the file uses `>= 3` or no count assertion, no change needed.

    Retune: this means running `uv run --package eval-harness pytest -k code_reader -v` or the equivalent sweep dry-run path to confirm every case (existing 3 + new 2–3) produces a non-trivial score. "Non-trivial" = the divergence/score signal is meaningfully > 0 and < 1 (not stuck at floor/ceiling). If a case scores trivially, adjust the query phrasing or expected_answer text to produce real signal — record the retune in 16-VERIFICATION.md SC#2 evidence.

    Commit as step 4: `feat(eval-cases): expand code_reader_cases.json with post-rebrand surface (SWEEP-FU-03 D-07)`.
  </action>
  <verify>
    <automated>python3 -c "import json; cases = json.load(open('eval/cases/code_reader_cases.json')); assert 5 <= len(cases) <= 6, f'expected 5-6 cases, got {len(cases)}'; assert all('code-reader' in c['tags'] and 'vault-thin' in c['tags'] for c in cases); existing_ids = {c['case_id'] for c in cases[:3]}; assert existing_ids == {'code-reader-01','code-reader-02','code-reader-03'}, f'first 3 must be preserved verbatim, got {existing_ids}'" && uv run --package eval-harness pytest packages/eval-harness/tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `eval/cases/code_reader_cases.json` contains 5 or 6 cases (JSON parses to a list of that length)
    - The first 3 cases (case_id `code-reader-01`, `02`, `03`) are byte-identical to the pre-change version (preserve baseline) — verify via `git diff eval/cases/code_reader_cases.json` showing only additions to the array
    - Every new case has `tags` containing `"code-reader"` and `"vault-thin"`
    - Every new case `expected_answer` matches the prose pattern `cannot be answered from vault pages alone; requires reading` (case-insensitive substring check)
    - Every new case `query` targets a post-rebrand-surface candidate (`workspace-io`, `graph-wiki`, `vault-io.lint_wiki`, or `vault-io.wiki_search`) — verify via grep on the queries field
    - `packages/eval-harness/tests/test_models_toml_sweep_candidates.py` passes (count assertion bumped if it existed)
    - Git log shows step-4 commit
  </acceptance_criteria>
  <done>code_reader_cases.json has 5–6 cases with baseline preserved, every new case targets post-rebrand surface, eval-harness tests green, step-4 commit landed.</done>
</task>

<task type="auto">
  <name>Task 5: Build synthetic post-rebrand fixture vault + scanner regression test (D-11a, SWEEP-FU-04 fixture half)</name>
  <files>packages/eval-harness/tests/fixtures/post-rebrand-vault/, packages/eval-harness/tests/test_scanner_regression.py</files>
  <read_first>
    - packages/vault-io/tests/fixtures/round-trip-vault/ (existing fixture vault — structure reference)
    - agents/code-wiki-agent/tests/conftest.py:25-50 (fixture_vault_path pattern — how downstream code references fixtures)
    - agents/code-wiki-agent/tests/integration/test_query_e2e.py:27-34 (FIXTURE_VAULT precomputation pattern)
    - packages/eval-harness/src/eval_harness/sweep.py (sweep runner — understand the scanner regression entry point)
    - packages/eval-harness/src/eval_harness/two_gate.py (after Task 3 — 6-role frozenset is the input contract)
    - .planning/milestones/v1.1-MILESTONE-AUDIT.md (locate the v1.1 scanner baseline scores for the regression comparison; if absent, use a deterministic baseline snapshot)
    - .planning/phases/16-carry-forward-debt-cleanup/16-PATTERNS.md §"NEW Synthetic fixture vault" + §"per-VERIFICATION.md transcript pattern"
    - .planning/phases/16-carry-forward-debt-cleanup/16-CONTEXT.md §decisions D-11
  </read_first>
  <action>
    **Build the fixture vault** under `packages/eval-harness/tests/fixtures/post-rebrand-vault/`. Mirror the structure of `~/Personal/wiki/deep-agents` but smaller — enough for the scanner regression sweep to produce non-trivial output (typically 8–15 package pages). Required pages reflecting current names: `packages/workspace-io.md`, `packages/vault-io.md`, `packages/prompt-sources.md`, `packages/subagent-runtime.md`, `packages/model-adapter.md`, `packages/eval-harness.md`, plus root `index.md` / `CLAUDE.md` analog if the upstream vault has them. Each page: realistic frontmatter (matching the existing vault frontmatter schema — see `packages/vault-io/src/vault_io/layout_io.py` for the canonical schema), short body (~50–200 lines), realistic wikilinks. No `lattice*` symbols anywhere — the fixture is the "post-rebrand vault" by construction.

    **Create scanner regression test** `packages/eval-harness/tests/test_scanner_regression.py`. Module preamble: docstring "Scanner regression sweep against the synthetic post-rebrand fixture vault. CI-runnable (no Bedrock — uses recorded baseline). SWEEP-FU-04 (a) per D-11." Use the `FIXTURE_VAULT` precomputation pattern from `test_query_e2e.py:27-34` (path resolved at import time). Test function: run the scanner role through the existing sweep entry point (locate in `sweep.py` — search for `scanner` invocation and the score-extraction path) against the fixture vault, then compare against a pinned baseline. Two viable shapes — pick during scout: (a) **snapshot pattern** using `syrupy` (already in the dependency stack per CLAUDE.md) — `assert scanner_scores == snapshot`, with first-run capture into `__snapshots__/`. (b) **explicit baseline file** pattern — store baseline scores in `packages/eval-harness/tests/fixtures/post-rebrand-vault/scanner-baseline.json`, assert current scores match within tolerance. Prefer (a) if syrupy is already wired into the eval-harness test suite; (b) otherwise.

    Mark test with `@pytest.mark.integration` only if it actually invokes Bedrock; if it runs purely against recorded/offline scanner logic, do not gate it (CI-runnable per D-11a). If Bedrock invocation IS required (the scanner sweep can't run offline), gate it with `CODE_WIKI_RUN_INTEGRATION` using the canonical decorator AND ensure the snapshot/baseline still functions as a deterministic regression check when the gate is set.

    **Critical: "no regression vs. v1.1 baseline" interpretation.** If the v1.1 baseline scanner scores against the fixture vault don't yet exist (which is likely — the fixture is brand-new in Phase 16), then the first run of this test SEEDS the baseline (snapshot capture), and subsequent runs compare against it. Document this seeding choice in `16-VERIFICATION.md` SC#2 evidence so future readers understand that "regression vs. v1.1 baseline" was operationalized as "first-Phase-16-run baseline against the new fixture, frozen forward."

    Commit as step 5: `test(eval-harness): synthetic post-rebrand fixture vault + scanner regression (SWEEP-FU-04a D-11)`.
  </action>
  <verify>
    <automated>uv run --package eval-harness pytest packages/eval-harness/tests/test_scanner_regression.py -x -v && ls packages/eval-harness/tests/fixtures/post-rebrand-vault/ | head -20 && grep -rE 'lattice|LATTICE' packages/eval-harness/tests/fixtures/post-rebrand-vault/ | (! grep .)</automated>
  </verify>
  <acceptance_criteria>
    - Directory `packages/eval-harness/tests/fixtures/post-rebrand-vault/` exists with at least 6 package page files (one per current `packages/*` member)
    - Every fixture file contains current names (`workspace-io`, `vault-io`, `prompt-sources`, `subagent-runtime`, `model-adapter`, `eval-harness`) — `grep -rl workspace-io packages/eval-harness/tests/fixtures/post-rebrand-vault/` returns ≥ 1
    - `grep -rE 'lattice|LATTICE|lattice_workspace|lattice_wiki_core' packages/eval-harness/tests/fixtures/post-rebrand-vault/` returns ZERO hits (fixture is post-rebrand by construction)
    - `packages/eval-harness/tests/test_scanner_regression.py` exists and is collected by pytest
    - The test passes (either as a snapshot first-run capture or against a pinned baseline file)
    - If the test uses syrupy snapshots, `__snapshots__/` directory exists alongside the test with at least one `.ambr` file committed
    - If the test uses an explicit baseline file, `scanner-baseline.json` (or equivalent) is committed alongside the fixture
    - Git log shows step-5 commit
  </acceptance_criteria>
  <done>Synthetic post-rebrand fixture vault committed, scanner regression test passes against it with seeded/pinned baseline, no lattice* hits in fixture, step-5 commit landed.</done>
</task>

<task type="auto">
  <name>Task 6: MCP cancel spike (D-08) + conditional cancellation.md refresh (D-09)</name>
  <files>docs/cancellation.md, .planning/phases/16-carry-forward-debt-cleanup/16-VERIFICATION.md</files>
  <read_first>
    - docs/cancellation.md (full file — especially §4 Known Limitations and §5 Future Work; lines 155–210 cover the v1.1 deferral context)
    - .planning/research/STACK.md lines 239 and 649 (langchain-aws#663 upstream signal documentation)
    - .planning/milestones/v1.1-MILESTONE-AUDIT.md lines 37, 191 (Phase 8 SC#1 deferral history)
    - packages/subagent-runtime/src/subagent_runtime/pool.py (current cancel handling — search for `CancelledError` and asyncio.Task cancel patterns to understand the in-process cancel surface)
    - .planning/phases/16-carry-forward-debt-cleanup/16-PATTERNS.md §"MOD docs/cancellation.md §4–§5"
    - .planning/phases/16-carry-forward-debt-cleanup/16-CONTEXT.md §decisions D-08 + D-09
  </read_first>
  <action>
    **Time-bound spike (half-day to one-day per D-08):** Assess whether a working integration path exists for real Bedrock wire-level cancel. Concrete steps:

    1. Check `langchain-aws` releases on PyPI: has `#663` been merged AND included in a published release? (`uv pip index versions langchain-aws` or check the GitHub PR status). Record version + merge status.
    2. Check `aioboto3` current release: has it reached a named milestone (GA / 1.0)? Record version + milestone status.
    3. If `aioboto3` is usable but `langchain-aws` has not merged #663: assess whether a thin adapter we'd own + maintain in this repo is feasible (rough sketch: subclass `ChatBedrockConverse`, route through `aioboto3.client('bedrock-runtime').invoke_model_with_response_stream` with a real cancel-aware coroutine). Record adapter feasibility verdict.

    **Gate (D-08):** Pursue real wire-level cancel implementation ONLY IF either path qualifies as a "working integration path." If gate PASSES — the implementation work is a follow-up plan, NOT this task's scope; this task simply records the gate decision and the chosen path. If gate FAILS at all three integration paths — proceed to refresh `docs/cancellation.md`.

    **Gate-fail path — refresh `docs/cancellation.md` §4–§5:**

    - §4 "Known Limitations": refresh the v1.1 wording with the **current** blocker status as of the spike date. Cite langchain-aws version + PR #663 status verbatim from PyPI / GitHub. Cite aioboto3 version + milestone status.
    - §5 "Future Work": replace any date-driven or milestone-tied re-eval triggers with the **event-driven** trigger from D-09 verbatim: "Re-evaluate when `langchain-aws` cuts a release with #663 merged, OR when `aioboto3` reaches a named milestone (GA / 1.0). Pat tracks upstream; whichever lands first re-opens the cancel work." Remove any "v1.2+" calendar phrasing. Preserve the §5 list of other deferred cancel paths (SIGINT, orphan-thread monitoring, per-tool E2E cancel) unchanged unless their wording also drifted.

    **Either gate outcome — capture the spike in `16-VERIFICATION.md`:** Append (or initialize if not yet existing) an `## SC#3: MCP Cancellation Closure (MCP-CAN-01)` section that records: (a) the spike date, (b) versions checked with sources (PyPI link / GitHub PR link), (c) the gate verdict (pursue / re-defer), (d) if re-defer: the diff against `docs/cancellation.md` (commit hash + section line numbers); if pursue: the chosen integration path and a forward-pointer to a follow-up plan or open requirement.

    **No fenced code blocks in this task action.** Spike write-up format: prose + the cancellation.md diff captured in VERIFICATION as a fenced block at execution time.

    Commit as step 6: `docs(cancellation): refresh §4–§5 with event-driven re-eval trigger (MCP-CAN-01 D-08/D-09)` if gate failed, OR `docs(verification): record MCP cancel spike pursue-verdict (MCP-CAN-01 D-08)` if gate passed.
  </action>
  <verify>
    <automated>test -f .planning/phases/16-carry-forward-debt-cleanup/16-VERIFICATION.md && grep -q "SC#3" .planning/phases/16-carry-forward-debt-cleanup/16-VERIFICATION.md && grep -qE "langchain-aws.*#663|langchain-aws.*PR.*663" .planning/phases/16-carry-forward-debt-cleanup/16-VERIFICATION.md && grep -qE "aioboto3.*(GA|1\.0|milestone)" docs/cancellation.md</automated>
  </verify>
  <acceptance_criteria>
    - `16-VERIFICATION.md` contains an `## SC#3` (or `## SC#3: MCP Cancellation Closure`) section
    - The SC#3 section names a spike date, the langchain-aws version + #663 status checked, and the aioboto3 version + milestone status checked (all with source citations: PyPI URL or GitHub PR URL)
    - The SC#3 section names the gate verdict: "pursue" OR "re-defer"
    - IF gate verdict is "re-defer": `docs/cancellation.md` §4 references the current blocker status and §5 contains the verbatim event-driven trigger from D-09 (search for "langchain-aws cuts a release with #663 merged" OR "aioboto3 reaches a named milestone")
    - IF gate verdict is "pursue": `16-VERIFICATION.md` SC#3 names the chosen integration path (released `langchain-aws` with #663 / `aioboto3` + thin adapter) and forward-points to a follow-up plan or open requirement
    - Calendar-date re-eval phrasing has been removed from `docs/cancellation.md` §5 (no "v1.2+ deadline" or "by Q3" style dates)
    - Git log shows step-6 commit
  </acceptance_criteria>
  <done>Spike conducted, gate decision recorded in 16-VERIFICATION.md SC#3, cancellation.md refreshed per D-09 (event-driven trigger) if gate failed, step-6 commit landed.</done>
</task>

<task type="auto">
  <name>Task 7: Author docs/testing.md + integration-gate grep gate (D-10, MCP-CAN-02)</name>
  <files>docs/testing.md, tests/test_integration_gate.py</files>
  <read_first>
    - docs/cancellation.md (sibling shape — 5-section structure to mirror)
    - agents/code-wiki-agent/tests/conftest.py:17-22 (canonical INTEGRATION_GATE — single source of truth for gate text)
    - agents/code-wiki-agent/tests/integration/test_mcp_e2e.py:20-23 (matches canonical)
    - agents/code-wiki-agent/tests/integration/test_mcp_stdio.py:142-143
    - agents/code-wiki-agent/tests/integration/test_query_e2e.py:39-42
    - agents/code-wiki-agent/tests/integration/test_bedrock_iam.py:33-35 (DIVERGENT — inline pytest.skip, not module-level decorator)
    - packages/subagent-runtime/tests/integration/test_pool_bedrock.py:29-30 (cross-package gated test)
    - scripts/check-brand.sh (script-form grep-gate analog — for reference even if we choose pytest form)
    - packages/eval-harness/tests/test_models_toml_sweep_candidates.py (pytest meta-test analog for grep-gate test-form)
    - .planning/phases/16-carry-forward-debt-cleanup/16-PATTERNS.md §"NEW docs/testing.md" + §"NEW Grep gate"
    - .planning/phases/16-carry-forward-debt-cleanup/16-CONTEXT.md §decisions D-10
  </read_first>
  <action>
    **Author `docs/testing.md`** modeled on `docs/cancellation.md`'s 5-section structure:

    - §1 "Integration test gate — spec basis" — one-paragraph scope statement: the `CODE_WIKI_RUN_INTEGRATION=1` opt-in gate controls whether tests invoking real Bedrock run in CI / local. Without the env var, gated tests skip. Reference where the gate originated (Phase 8 host-reliability work).
    - §2 "Internal pattern" — describe the chain: (1) test file imports `pytest`, `os`; (2) defines or imports `INTEGRATION_GATE` decorator; (3) applies `@INTEGRATION_GATE` at function level (NOT inline `pytest.skip(...)` inside the body).
    - §3 "Canonical pattern" — embed the verbatim block from `conftest.py:17-22`:
      The exact lines:
      `INTEGRATION_GATE = pytest.mark.skipif(`
      `    not os.environ.get("CODE_WIKI_RUN_INTEGRATION"),`
      `    reason="Set CODE_WIKI_RUN_INTEGRATION=1 to run real Bedrock invocations",`
      `)`
      Reason text MUST be verbatim — that's the canonical reason string that the grep gate matches.
    - §4 "Inventory of gated test files" — table listing every file path + line range that uses the gate, sourced from PATTERNS.md inventory:
      - `agents/code-wiki-agent/tests/conftest.py:17-22` (canonical home)
      - `agents/code-wiki-agent/tests/integration/test_mcp_e2e.py:20-23`
      - `agents/code-wiki-agent/tests/integration/test_mcp_stdio.py:142-143`
      - `agents/code-wiki-agent/tests/integration/test_query_e2e.py:39-42`
      - `agents/code-wiki-agent/tests/integration/test_bedrock_iam.py:33-35` — note divergence (inline `pytest.skip`)
      - `packages/subagent-runtime/tests/integration/test_pool_bedrock.py:29-30`
      - `agents/code-wiki-agent/tests/integration/test_trace_coverage.py` (NEW from Task 2)
    - §5 "Future / enforcement" — describe the grep gate mechanism (`tests/test_integration_gate.py`) and the policy: any new integration test must either match the canonical pattern OR be allowlisted via comment marker in the test file.

    **Resolve the `test_bedrock_iam.py` divergence** per D-10 (locked: "single rule"). Either: (a) refactor `test_bedrock_iam.py:33-35` to use the canonical module-level `INTEGRATION_GATE` decorator (preferred — brings the file into the canonical shape), OR (b) explicitly allowlist it in `docs/testing.md` §4 AND in the grep gate's allowlist mechanism with a one-line rationale. Pick (a) unless inline skip is semantically required (e.g. the skip depends on a runtime check beyond env var presence — read the file to confirm).

    **Author the grep gate as a pytest meta-test** at `tests/test_integration_gate.py` (repo-level, not package-scoped — so it sweeps every package). Pattern modeled on `test_models_toml_sweep_candidates.py`. Imports: `from pathlib import Path`, `re`. Define a regex matching the canonical pattern: `_CANONICAL_PATTERN = re.compile(r'pytest\.mark\.skipif\(\s*\n?\s*not os\.environ\.get\("CODE_WIKI_RUN_INTEGRATION"\)')`. Test function: walk the repo root, glob `**/tests/integration/test_*.py`, for each file assert either (a) `_CANONICAL_PATTERN.search(file_text)` matches OR (b) the file contains a `# integration-gate-allow` comment marker. On failure, list the divergent files in the assertion message.

    Repo root resolution: `Path(__file__).parent.parent` from `tests/test_integration_gate.py` reaches the repo root.

    Commit as step 7: `docs(testing): canonical CODE_WIKI_RUN_INTEGRATION gate + meta-test (MCP-CAN-02 D-10)`.
  </action>
  <verify>
    <automated>test -f docs/testing.md && grep -q "CODE_WIKI_RUN_INTEGRATION" docs/testing.md && grep -q "Set CODE_WIKI_RUN_INTEGRATION=1 to run real Bedrock invocations" docs/testing.md && test -f tests/test_integration_gate.py && uv run pytest tests/test_integration_gate.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/testing.md` exists with sections §1–§5 (or equivalent headings)
    - `docs/testing.md` embeds the verbatim canonical block: `pytest.mark.skipif(`, `not os.environ.get("CODE_WIKI_RUN_INTEGRATION")`, `reason="Set CODE_WIKI_RUN_INTEGRATION=1 to run real Bedrock invocations"`
    - `docs/testing.md` §4 inventory table lists all 6 existing gated test files + the new `test_trace_coverage.py` from Task 2 (≥ 7 paths)
    - `test_bedrock_iam.py` divergence is resolved: either the file now uses the canonical module-level decorator OR it carries a `# integration-gate-allow` marker referenced in `docs/testing.md` §4
    - File `tests/test_integration_gate.py` exists, is collected by pytest, and passes
    - The grep gate test FAILS (in a dry-run experiment, not committed) when a fake integration test file is created without the canonical pattern and without the allowlist marker — verify mentally during scout; do not actually break the build
    - `uv run pytest tests/test_integration_gate.py` is green
    - Git log shows step-7 commit
  </acceptance_criteria>
  <done>docs/testing.md authored with canonical pattern + inventory, test_bedrock_iam.py divergence resolved, grep-gate meta-test passes, step-7 commit landed.</done>
</task>

<task type="auto">
  <name>Task 8: Extend synthesizer test with model_id assertion (D-13, MODEL-FU-01)</name>
  <files>packages/model-adapter/tests/test_loader.py</files>
  <read_first>
    - packages/model-adapter/tests/test_loader.py (full file — esp. lines 116-122 `test_load_role_config_librarian_values` for the assertion shape, and lines 125-130 `test_load_role_config_synthesizer_limits` for the extension target)
    - packages/model-adapter/src/model_adapter/models.toml (esp. lines 113-130 synthesizer block — confirm `model_id = "qwen.qwen3-32b-v1:0"`)
    - packages/model-adapter/src/model_adapter/loader.py:27-41 (set_models_path override — read-only context)
    - .planning/phases/16-carry-forward-debt-cleanup/16-PATTERNS.md §"MOD test_loader.py:125-130"
    - .planning/phases/16-carry-forward-debt-cleanup/16-CONTEXT.md §decisions D-13
  </read_first>
  <action>
    Extend `test_load_role_config_synthesizer_limits` in `packages/model-adapter/tests/test_loader.py` (lines 125-130) by adding ONE new assertion before the existing `assert cfg["max_tokens"] == 4096` line: `assert cfg["model_id"] == "qwen.qwen3-32b-v1:0"`. The string `"qwen.qwen3-32b-v1:0"` is the bundled default sourced from `packages/model-adapter/src/model_adapter/models.toml:121` (verify the line/value during scout — if the value has changed since context-gathering, use the actual current TOML value and surface that as a deviation).

    Optional but preferred per PATTERNS.md: hoist the literal `"qwen.qwen3-32b-v1:0"` to a module-level constant `QWEN_SYNTHESIZER_ARN = "qwen.qwen3-32b-v1:0"` near the top of `test_loader.py` (matching the `HAIKU_ARN` pattern referenced in PATTERNS.md), and use the constant in the assertion. This makes future drift fixes a one-line constant update.

    Do NOT add per-role parameterized tests (rejected per D-13 option B). Do NOT rename the test (it was already renamed from `_uses_sonnet` to `_limits` in prior v1.1 work). Single targeted assertion is the locked answer.

    Commit as step 8: `test(model-adapter): assert synthesizer model_id == qwen-32b (MODEL-FU-01 D-13)`.
  </action>
  <verify>
    <automated>uv run --package model-adapter pytest packages/model-adapter/tests/test_loader.py::test_load_role_config_synthesizer_limits -x -v && grep -c 'cfg\["model_id"\] == "qwen.qwen3-32b-v1:0"' packages/model-adapter/tests/test_loader.py</automated>
  </verify>
  <acceptance_criteria>
    - `test_load_role_config_synthesizer_limits` in `packages/model-adapter/tests/test_loader.py` contains a new assertion `cfg["model_id"] == "qwen.qwen3-32b-v1:0"` (or via the `QWEN_SYNTHESIZER_ARN` constant)
    - The test name is preserved (`_limits`, NOT renamed back to `_uses_sonnet` or to anything else)
    - The existing two assertions (`max_tokens == 4096`, `max_concurrency == 3`) remain
    - `uv run --package model-adapter pytest` is green
    - No new test functions added (rejected D-13 option B verified by `git diff` showing only insertions inside the existing function body — or constant hoisting at module level)
    - Git log shows step-8 commit
  </acceptance_criteria>
  <done>Synthesizer test asserts current Qwen default model_id, model-adapter test suite green, step-8 commit landed.</done>
</task>

<task type="auto">
  <name>Task 9: Live-vault scanner re-sweep + 16-VERIFICATION.md finalization (D-11b)</name>
  <files>.planning/phases/16-carry-forward-debt-cleanup/16-VERIFICATION.md</files>
  <read_first>
    - ~/Personal/wiki/deep-agents/ (post-Phase-15 vault — the live target for the re-sweep)
    - .planning/phases/15-wiki-self-update/15-VERIFICATION.md (Phase 15 D-08/D-09 transcript pattern — the fenced-markdown shape for live-run evidence)
    - .planning/phases/14-plugin-port-m3b/ (Phase 14 SC#4 transcript pattern as cross-reference)
    - packages/eval-harness/src/eval_harness/sweep.py (sweep entry point — find the CLI invocation pattern for a live scanner run)
    - packages/eval-harness/src/eval_harness/preflight.py + pricing.py (D-12 — preflight dry-run estimator that gates the live run by Pat's judgement, not a hard cap)
    - .planning/phases/16-carry-forward-debt-cleanup/16-CONTEXT.md §decisions D-11 + D-12 + §canonical_refs (per-VERIFICATION.md transcript pattern)
    - .planning/phases/16-carry-forward-debt-cleanup/16-PATTERNS.md §"Per-VERIFICATION.md transcript pattern"
    - All prior tasks' outputs (Tasks 1–8) to populate per-SC evidence sections
  </read_first>
  <action>
    **(a) Live-vault scanner re-sweep against `~/Personal/wiki/deep-agents`** per D-11b. Steps:

    1. Run the preflight dry-run estimator (`uv run --package eval-harness python -m eval_harness.preflight ...` — exact CLI shape determined during scout in `preflight.py` / `pricing.py`) against `~/Personal/wiki/deep-agents` for the scanner role with the current models profile (Qwen3-32B fan-out per `[[project_wiki_setup]]`). Capture the estimated cost output.
    2. Per D-12, this is judgement-driven — review the estimate, decide whether to proceed (no hard cap). If proceeding, run the live scanner sweep (`uv run --package eval-harness python -m eval_harness.sweep --role scanner --vault ~/Personal/wiki/deep-agents ...` — exact CLI from `sweep.py` scout). Capture the per-role score output.
    3. Compare scores against the v1.1 baseline (locate the v1.1 baseline in `.planning/milestones/v1.1-MILESTONE-AUDIT.md` or recorded sweep output — if no v1.1 scanner baseline exists for the live vault, document the absence and treat this run as a milestone-grade evidence capture, not a regression check).

    **(b) Finalize `16-VERIFICATION.md`** as a single document with per-SC sections. If SC#3 was already initialized by Task 6, extend; otherwise create the full file. Required structure:

    - `# Phase 16 Verification` header with date.
    - `## SC#1: TRACE-FU-01 — usage_metadata coverage` — cite the new helper file path (`packages/subagent-runtime/src/subagent_runtime/trace_io.py`), the refactored call sites (ingest.py + query.py), and run output from `CODE_WIKI_RUN_INTEGRATION=1 uv run pytest agents/code-wiki-agent/tests/integration/test_trace_coverage.py` as a fenced markdown block.
    - `## SC#2: SWEEP-FU-02/03/04 — divergence matrix + cases + scanner re-sweep` — cite the two_gate.py expansion, the new rubric file paths, the code_reader_cases.json delta (3 → 5 or 6 cases), the fixture-vault scanner regression test output, AND the live-vault re-sweep transcript from step (a) above as a fenced block. Include any retune notes from Task 4.
    - `## SC#3: MCP-CAN-01 — cancel spike` — already populated by Task 6 (extend if needed).
    - `## SC#4: MCP-CAN-02 — gate consistency` — cite `docs/testing.md` path, the grep-gate test output (`uv run pytest tests/test_integration_gate.py -v` as a fenced block), and confirm the `test_bedrock_iam.py` divergence resolution decision.
    - `## SC#5: MODEL-FU-01 — synthesizer model_id assertion` — cite the `test_loader.py` diff (the one-line `model_id` assertion), and the `uv run --package model-adapter pytest` output as a fenced block.

    Final section: `## Phase 16 Status` summary table listing each REQ-ID (TRACE-FU-01, SWEEP-FU-02, SWEEP-FU-03, SWEEP-FU-04, MCP-CAN-01, MCP-CAN-02, MODEL-FU-01) with `COMPLETE` or `RE-DEFERRED` status and a one-line citation pointing to the SC section.

    Commit as step 9: `docs(verification): finalize 16-VERIFICATION.md with per-SC evidence (Phase 16 closeout)`.
  </action>
  <verify>
    <automated>test -f .planning/phases/16-carry-forward-debt-cleanup/16-VERIFICATION.md && for sc in "SC#1" "SC#2" "SC#3" "SC#4" "SC#5"; do grep -q "$sc" .planning/phases/16-carry-forward-debt-cleanup/16-VERIFICATION.md || { echo "missing $sc"; exit 1; }; done && for req in TRACE-FU-01 SWEEP-FU-02 SWEEP-FU-03 SWEEP-FU-04 MCP-CAN-01 MCP-CAN-02 MODEL-FU-01; do grep -q "$req" .planning/phases/16-carry-forward-debt-cleanup/16-VERIFICATION.md || { echo "missing $req"; exit 1; }; done && uv run pytest -x -q --ignore=agents/code-wiki-agent/tests/integration --ignore=packages/subagent-runtime/tests/integration</automated>
  </verify>
  <acceptance_criteria>
    - `16-VERIFICATION.md` contains all 5 SC sections (`SC#1` through `SC#5`)
    - Every one of the 7 requirement IDs (TRACE-FU-01, SWEEP-FU-02, SWEEP-FU-03, SWEEP-FU-04, MCP-CAN-01, MCP-CAN-02, MODEL-FU-01) appears at least once in the file
    - SC#2 contains the live-vault scanner re-sweep transcript as a fenced markdown block (presence of triple backticks within SC#2)
    - SC#1, SC#2, SC#4, SC#5 each contain at least one fenced markdown block with command output (verify by `grep -A 1 '^## SC#' 16-VERIFICATION.md` and presence of triple-backtick lines after each header)
    - Final `## Phase 16 Status` summary table exists with one row per REQ-ID and a status column
    - `uv run pytest` is green across the non-integration test suite
    - Git log shows step-9 commit AND the full 9-commit sequence is present on the branch (step-1 through step-9)
  </acceptance_criteria>
  <done>16-VERIFICATION.md complete with per-SC evidence and live-vault transcript, all 7 REQ-IDs cited, non-integration test suite green, full 9-commit sequence on branch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ChatBedrockConverse response → trace JSONL | LLM response object's `usage_metadata` dict is treated as data and written to disk |
| Test files → `tests/test_integration_gate.py` grep regex | Test file source text is read and matched against a static regex; no eval/exec |
| AgentOutputProxy text → divergence `_check_*` callables | LLM output text is matched with regex / string ops only (T-06-15 invariant preserved) |
| Live vault `~/Personal/wiki/deep-agents/` → scanner sweep | Read-only access to live vault; no writes during D-11b live re-sweep |
| Spike write-up → `docs/cancellation.md` | Manual edit of documentation; reviewed by Pat before commit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering | trace_io.py JSONL append | accept | OSError-swallow preserved verbatim from pool.py:227-231; no new write surface |
| T-16-02 | Information Disclosure | trace records on disk | accept | Trace records contain token counts + model IDs; no PII; existing schema unchanged |
| T-16-03 | Denial of Service | trace write failure masking task results | mitigate | OSError caught + logged WARNING (helper preserves pool.py invariant); regression test would catch silent mask |
| T-16-04 | Tampering | divergence `_check_*` callables on LLM output | mitigate | Regex + string ops only; no eval/exec of LLM-generated text (T-06-15 invariant carried forward in module headers) |
| T-16-05 | Information Disclosure | live-vault re-sweep against ~/Personal/wiki/deep-agents | accept | Read-only access; no vault rewrites; D-11b is evidence capture only |
| T-16-06 | Elevation of Privilege | CODE_WIKI_RUN_INTEGRATION env var bypass in test pattern divergence | mitigate | New `tests/test_integration_gate.py` meta-test enforces canonical decorator on every integration test file; CI-gated |
| T-16-07 | Repudiation | cancel spike decision not recorded | mitigate | D-08 mandates spike write-up in 16-VERIFICATION.md SC#3; D-09 mandates event-driven re-eval trigger in docs/cancellation.md §5 if gate fails |
| T-16-08 | Tampering | model_id drift in synthesizer config | mitigate | D-13 test assertion locks current Qwen default; future swap fails test loudly |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | Phase 16 introduces ZERO new third-party dependencies — all new modules use stdlib + existing in-repo packages; package-legitimacy gate not triggered |
</threat_model>

<verification>

**Pre-flight checks (before executing Tasks 1–9):**

- `uv sync` succeeds with no new dependency resolution failures (we add none).
- `uv run pytest` baseline is green (or note pre-existing failures so we don't blame them on Phase 16).
- Git working tree is clean.

**Per-task verification (see each task's `<verify>` block).**

**End-of-phase verification (Task 9 produces this):**

- `16-VERIFICATION.md` contains all 5 SC sections.
- `uv run pytest -x -q` is green across the non-integration suite.
- `CODE_WIKI_RUN_INTEGRATION=1 uv run pytest agents/code-wiki-agent/tests/integration/test_trace_coverage.py` is green.
- `uv run pytest tests/test_integration_gate.py` is green.
- `grep -rE 'lattice|LATTICE' packages/eval-harness/tests/fixtures/post-rebrand-vault/` returns zero hits.
- Git log shows 9 commits in step order: refactor(subagent-runtime) → feat(code-wiki-agent) trace → feat(eval-harness) rubrics → feat(eval-cases) → test(eval-harness) fixture → docs(cancellation) OR docs(verification) cancel spike → docs(testing) gate → test(model-adapter) → docs(verification) finalize.

</verification>

<success_criteria>

This plan completes when:

1. **SC#1 (TRACE-FU-01):** `trace_io.py` helper exists, `pool.py` delegates to it, `ingest.py` and `query.py` write tokens-populated records, `test_trace_coverage.py` is green under `CODE_WIKI_RUN_INTEGRATION=1`.
2. **SC#2 (SWEEP-FU-02/03/04):** `ROLES_WITH_DIVERGENCE` covers all 6 in-scope roles, `code_reader_cases.json` has 5–6 cases with baseline preserved, synthetic post-rebrand fixture vault + scanner regression test pass, live-vault re-sweep transcript in `16-VERIFICATION.md` SC#2.
3. **SC#3 (MCP-CAN-01):** Spike conducted; either real cancel pursued with a named integration path, or `docs/cancellation.md` §4–§5 refreshed with the event-driven D-09 trigger. SC#3 in `16-VERIFICATION.md` cites the decision.
4. **SC#4 (MCP-CAN-02):** `docs/testing.md` exists with canonical pattern + inventory; `tests/test_integration_gate.py` grep gate is green; `test_bedrock_iam.py` divergence resolved.
5. **SC#5 (MODEL-FU-01):** `test_load_role_config_synthesizer_limits` asserts `cfg["model_id"] == "qwen.qwen3-32b-v1:0"`; `uv run pytest` is green.

All 9 per-step commits on the branch. All 7 REQ-IDs cited in `16-VERIFICATION.md`.

</success_criteria>

<output>
Create `.planning/phases/16-carry-forward-debt-cleanup/16-01-SUMMARY.md` when done. Summary should cite each of the 9 step commits by hash + message, reference `16-VERIFICATION.md` per-SC evidence, and list any deviations encountered (e.g. cancel-spike gate verdict, retune adjustments to code_reader cases).
</output>
