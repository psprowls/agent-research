---
phase: 51-package-family-removal-divergence-rule-cleanup
plan: 02
type: execute
wave: 2
depends_on: [51-01]
files_modified:
  - packages/wiki-io/src/wiki_io/entity_writer.py
  - packages/wiki-io/src/wiki_io/link_rewriter.py
  - packages/wiki-io/src/wiki_io/lint/dependency.py
  - packages/wiki-io/src/wiki_io/assets/page-templates/entity-package-family.md
  - packages/wiki-io/src/wiki_io/assets/page-templates/package-family.md
  - packages/wiki-io/src/wiki_io/assets/page-templates/dependency.md
  - packages/wiki-io/tests/test_entity_writer.py
  - packages/wiki-io/tests/test_link_rewriter_build_table.py
  - packages/wiki-io/tests/test_assets.py
autonomous: true
requirements: [PKGFAM-03, PKGFAM-05]

must_haves:
  truths:
    - "`ADMITTED_KINDS` in `wiki_io.entity_writer` is the complete and final frozenset (no `_V18` alias, no subtraction-narrow) — exactly 6 kinds: repository, domain, package, plugin, dependency, test_suite."
    - "`entity-package-family.md` and `package-family.md` no longer exist in `wiki_io/assets/page-templates/`."
    - "`link_rewriter.py` has zero `package-family` code paths (D-04 deferral branches deleted; Phase 53 cutover handles any live-vault `[[package-family/...]]` links atomically per RESEARCH.md Pitfall 4)."
    - "`lint/dependency.py` `VALID_KINDS` no longer contains `\"package-family\"` (Option A per RESEARCH.md Pitfall 3); cross-page family-membership lint rules are deleted."
    - "All existing wiki-io tests pass after deletions; `test_admitted_kinds_shape` expects 6 kinds; `test_round_trip` fixture invariants hold (fixture itself is edited in Plan 04)."
  artifacts:
    - path: "packages/wiki-io/src/wiki_io/entity_writer.py"
      provides: "Entity-writer dispatch with `ADMITTED_KINDS` finalized (no V18 alias, no package_family entries in `_URI_PREFIX_BY_KIND`)"
      contains: "ADMITTED_KINDS"
    - path: "packages/wiki-io/src/wiki_io/link_rewriter.py"
      provides: "Link rewriter without `package-family` D-04 deferral branches"
    - path: "packages/wiki-io/src/wiki_io/lint/dependency.py"
      provides: "Dependency lint with no `package-family` kind discriminator"
    - path: "packages/wiki-io/tests/test_assets.py"
      provides: "Template-absence regression test (Wave 0 addition)"
  key_links:
    - from: "packages/wiki-io/src/wiki_io/entity_writer.py"
      to: "ADMITTED_KINDS"
      via: "frozenset constant"
      pattern: "ADMITTED_KINDS.*frozenset"
    - from: "packages/wiki-io/tests/test_entity_writer.py"
      to: "ADMITTED_KINDS"
      via: "import (no _V18)"
      pattern: "from wiki_io.entity_writer import ADMITTED_KINDS"
---

<objective>
Remove all `package_family` and `package-family` scaffolding from `wiki-io`: simplify `ADMITTED_KINDS` to a single complete-and-final frozenset (deleting the `ADMITTED_KINDS_V18` alias per D-04), delete the two template assets, scrub `link_rewriter.py`'s D-04 deferral branches (RESEARCH.md Pitfall 4 strict-deletion path), drop the `package-family` kind from `lint/dependency.py` (RESEARCH.md Pitfall 3 Option A), and update tests accordingly.

Purpose: Closes PKGFAM-03 (template + alias + lint scaffolding removal) and ensures success criterion #2 (the frozenset is "complete and final" with no subtraction-narrow) and success criterion #3 (template file is gone). Depends on Plan 51-01 to keep CI green during deletion — graph-io's `_VALID_KINDS` must already be clean so the wiki-io dispatch table mirrors upstream.

Output:
- `entity_writer.py` with `ADMITTED_KINDS` finalized to 6 kinds, `_URI_PREFIX_BY_KIND` cleaned, V18 alias deleted, all docstring/comment references scrubbed
- `entity-package-family.md` and `package-family.md` template files deleted
- `link_rewriter.py` with all 9 active `package-family` code paths deleted
- `lint/dependency.py` with `package-family` kind discriminator removed (and family-membership cross-page rules)
- Test files updated: V18 references renamed, `package_family` strategies/params removed, new template-absence test added
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/51-package-family-removal-divergence-rule-cleanup/51-CONTEXT.md
@.planning/phases/51-package-family-removal-divergence-rule-cleanup/51-RESEARCH.md
@.planning/phases/51-package-family-removal-divergence-rule-cleanup/51-VALIDATION.md
@.planning/phases/51-package-family-removal-divergence-rule-cleanup/51-01-PLAN.md
@packages/wiki-io/src/wiki_io/entity_writer.py
@packages/wiki-io/src/wiki_io/link_rewriter.py
@packages/wiki-io/src/wiki_io/lint/dependency.py
@packages/wiki-io/tests/test_entity_writer.py
@packages/wiki-io/tests/test_link_rewriter_build_table.py
</context>

<tasks>

<task type="auto">
  <name>Task 51-02-01: Finalize `ADMITTED_KINDS`, delete `_V18` alias, scrub `entity_writer.py`, delete template assets</name>
  <files>packages/wiki-io/src/wiki_io/entity_writer.py, packages/wiki-io/src/wiki_io/assets/page-templates/entity-package-family.md, packages/wiki-io/src/wiki_io/assets/page-templates/package-family.md, packages/wiki-io/src/wiki_io/assets/page-templates/dependency.md, packages/wiki-io/tests/test_entity_writer.py, packages/wiki-io/tests/test_assets.py</files>
  <read_first>
    - packages/wiki-io/src/wiki_io/entity_writer.py (target — read lines 1-200 including `ADMITTED_KINDS`, `_URI_PREFIX_BY_KIND`, and the `_V18` alias at 195-196; also read line 530 for the fallback comment)
    - packages/wiki-io/tests/test_entity_writer.py (target — 9 V18 call sites + `_package_family_uri_strategy` + parametrize cases per RESEARCH.md)
    - packages/wiki-io/src/wiki_io/assets/page-templates/entity-package-family.md (target — delete)
    - packages/wiki-io/src/wiki_io/assets/page-templates/package-family.md (target — delete)
    - packages/wiki-io/src/wiki_io/assets/page-templates/dependency.md (read lines 4 and 9 for comment scrubbing)
    - .planning/phases/51-package-family-removal-divergence-rule-cleanup/51-RESEARCH.md §"Code Examples" — `ADMITTED_KINDS` simplification
    - .planning/phases/51-package-family-removal-divergence-rule-cleanup/51-CONTEXT.md §D-04 (delete V18 alias outright, no deprecation)
  </read_first>
  <action>
    In `packages/wiki-io/src/wiki_io/entity_writer.py`:
    1. Remove `"package_family"` from the `ADMITTED_KINDS` frozenset literal (currently at line ~66 per RESEARCH.md). Final frozenset must be exactly the 6 kinds: `repository`, `domain`, `package`, `plugin`, `dependency`, `test_suite`.
    2. Remove the `"package_family": "package_family"` entry from the `_URI_PREFIX_BY_KIND` dict (line ~82 per RESEARCH.md).
    3. Delete the "Edge-derived (package_family)" comment block (line ~125 per RESEARCH.md).
    4. **Delete the entire `ADMITTED_KINDS_V18` alias line (line 195-196 per RESEARCH.md)** — per D-04, no deprecation grace.
    5. Remove the `"package_family v1.9"` comment annotation at line ~530.
    6. Clean docstring references at lines 9 and 22 (drop `package_family` from URI-prefix list / example mentions).

    Delete the two template files outright:
    - `packages/wiki-io/src/wiki_io/assets/page-templates/entity-package-family.md`
    - `packages/wiki-io/src/wiki_io/assets/page-templates/package-family.md`

    In `packages/wiki-io/src/wiki_io/assets/page-templates/dependency.md`: scrub the `package-family` references at lines 4 and 9 (per RESEARCH.md — comment-only mentions of the now-retired discriminator).

    In `packages/wiki-io/tests/test_entity_writer.py`:
    - Rename ALL 9 `ADMITTED_KINDS_V18` references to `ADMITTED_KINDS` (per RESEARCH.md "Wiki-io removal" section).
    - Delete `_package_family_uri_strategy` (if it is a standalone hypothesis strategy) AND remove it from the `_admitted_uri_strategy` union.
    - Delete the `("package_family:aws", ...)` parametrize case (per RESEARCH.md).
    - Update `test_admitted_kinds_shape` so the expected frozenset has 6 kinds instead of 7 (drop `package_family`).

    Create new test file `packages/wiki-io/tests/test_assets.py` (or append to an existing assets-test if present) with a small belt-and-suspenders test `test_no_package_family_template`:
    - Use `importlib.resources.files("wiki_io.assets.page-templates")` to enumerate template names.
    - Assert that `"entity-package-family.md"` and `"package-family.md"` are NOT present.
  </action>
  <verify>
    <automated>uv run --package wiki-io pytest packages/wiki-io/tests/test_entity_writer.py packages/wiki-io/tests/test_assets.py -x</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "package_family" packages/wiki-io/src/wiki_io/entity_writer.py` returns 0.
    - Source: `grep -n "ADMITTED_KINDS_V18" packages/wiki-io/` returns zero lines.
    - Source: `test -f packages/wiki-io/src/wiki_io/assets/page-templates/entity-package-family.md` returns false (file deleted).
    - Source: `test -f packages/wiki-io/src/wiki_io/assets/page-templates/package-family.md` returns false (file deleted).
    - Behavior: `pytest packages/wiki-io/tests/test_entity_writer.py -x` passes; `test_admitted_kinds_shape` asserts a 6-kind frozenset.
    - Behavior: `pytest packages/wiki-io/tests/test_assets.py::test_no_package_family_template -x` passes.
    - Source: `python -c "from wiki_io.entity_writer import ADMITTED_KINDS_V18"` raises `ImportError`.
  </acceptance_criteria>
  <done>
    `ADMITTED_KINDS` is the 6-kind complete-and-final frozenset; `_V18` alias is gone; template files deleted; entity_writer docstrings clean; test references updated; new template-absence assertion test passes.
  </done>
</task>

<task type="auto">
  <name>Task 51-02-02: Strict-deletion of `link_rewriter.py` D-04 deferral branches</name>
  <files>packages/wiki-io/src/wiki_io/link_rewriter.py, packages/wiki-io/tests/test_link_rewriter_build_table.py</files>
  <read_first>
    - packages/wiki-io/src/wiki_io/link_rewriter.py (target — lines 11, 50, 53, 289, 296, 304, 344-345, 413, 443-444 per RESEARCH.md — these are the 9 active code paths)
    - packages/wiki-io/tests/test_link_rewriter_build_table.py (target — 4 tests to rework per RESEARCH.md)
    - .planning/phases/51-package-family-removal-divergence-rule-cleanup/51-RESEARCH.md §"Common Pitfalls" Pitfall 4 (strict deletion recommended; Phase 53 cutover handles live-vault leftovers)
    - .planning/phases/51-package-family-removal-divergence-rule-cleanup/51-CONTEXT.md §"Claude's Discretion" (delete unless clearly historical comment markers)
  </read_first>
  <action>
    Strict-deletion path per RESEARCH.md Pitfall 4. In `packages/wiki-io/src/wiki_io/link_rewriter.py`, remove ALL 9 `package-family` code paths:

    1. The `OLD_LAYOUT_ROOTS` entry for `"package-family"` (around line 11 / 50 per RESEARCH.md inventory).
    2. Both `_KIND_FOR_PREFIX` entries — the bare `package-family` and the `wiki/`-prefixed form (around lines 53, 289).
    3. The `source2` skip-branch that logs `[[package-family/...]]` as unresolvable (around line 296).
    4. The `source3` log-branch (around line 304).
    5. The deferral conditional at lines 344-345.
    6. The branch at line 413.
    7. The fallback log emission at lines 443-444.

    Each deletion removes an executable branch, not a comment. After deletion, any `[[package-family/...]]` wikilink in a live vault passes through the rewriter unchanged — Phase 53's atomic cutover is responsible for rewriting or removing those links per Pitfall 4.

    In `packages/wiki-io/tests/test_link_rewriter_build_table.py`, rework the 4 tests that exercised the deferral logic:
    - If a test's sole purpose is to validate the deferred-package-family branch (e.g., asserts that `[[package-family/aws]]` is logged as unresolvable), DELETE that test.
    - If a test mixes a package-family case with other valid cases, REMOVE only the package-family parametrize entries / fixture rows.
    - Do not invent new tests; the goal is faithful removal, not coverage expansion.

    Run `pytest packages/wiki-io/tests/test_link_rewriter*.py -x` after editing to confirm everything still parses and passes.
  </action>
  <verify>
    <automated>uv run --package wiki-io pytest packages/wiki-io/tests/test_link_rewriter_build_table.py -x</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "package-family\|package_family" packages/wiki-io/src/wiki_io/link_rewriter.py` returns 0.
    - Source: `grep -nE 'package-family|package_family' packages/wiki-io/tests/test_link_rewriter_build_table.py` returns zero lines.
    - Behavior: `pytest packages/wiki-io/tests/test_link_rewriter_build_table.py -x` passes (4 tests reworked; remaining tests for other kinds still green).
    - Behavior: `pytest packages/wiki-io/tests/ -k link_rewriter -x` passes (sibling link_rewriter tests not regressed).
  </acceptance_criteria>
  <done>
    All 9 `package-family` code paths in `link_rewriter.py` are deleted; the 4 test cases that exercised them are reworked/removed; sibling link_rewriter tests remain green.
  </done>
</task>

<task type="auto">
  <name>Task 51-02-03: Remove `package-family` from `lint/dependency.py` (RESEARCH.md Option A) and run full wiki-io regression</name>
  <files>packages/wiki-io/src/wiki_io/lint/dependency.py</files>
  <read_first>
    - packages/wiki-io/src/wiki_io/lint/dependency.py (target — read lines 17, 29-30, 37, 46-47, 50-58, 78-119 per RESEARCH.md)
    - .planning/phases/51-package-family-removal-divergence-rule-cleanup/51-RESEARCH.md §"Common Pitfalls" Pitfall 3 (Option A: delete the lint feature; explicit reasoning anchor)
    - .planning/phases/51-package-family-removal-divergence-rule-cleanup/51-CONTEXT.md §"Claude's Discretion" (lint references are executable code; default delete)
    - packages/wiki-io/tests/test_lint_dependency.py (read to understand which assertions remain after `kind: package-family` removal)
  </read_first>
  <action>
    Per RESEARCH.md Pitfall 3 Option A (recommended): delete the lint-side `package-family` discriminator. The lint feature targets frontmatter `kind:` values on dependency wiki pages; the `package-family` value is being retired. In `packages/wiki-io/src/wiki_io/lint/dependency.py`:

    1. Remove `"package-family"` from the `VALID_KINDS` set/literal at line ~17.
    2. Delete the `kind == "package-family"` branch (lines ~50-55 per RESEARCH.md).
    3. Drop `family_pages` and `package_family_claims` accumulation logic (lines ~29-30, ~46-47).
    4. Update the `dep-kind-not-in-enum` error message (line ~37) to reflect the new enum membership (no `package-family`).
    5. Delete cross-page family-membership rules (lines ~78-119).

    Do NOT rename the discriminator to something else (Option B is explicitly rejected). Future "dependency-family / dependency clustering" work is deferred per REQUIREMENTS.md "Future Requirements".

    After editing `dependency.py`, run the wiki-io lint regression suite. If `test_lint_dependency.py` has tests specifically targeting `kind: package-family` validation, delete them; tests covering other lint rules (`dep-package-without-ecosystem`, `dep-stub-detail-page`, etc.) MUST still pass.

    Finally, run the full wiki-io test suite to confirm the round-trip fixture invariant has not been broken by the source-side deletions in Tasks 51-02-01 and 51-02-02. NOTE: the fixture itself is edited in Plan 51-04; this task only verifies source-side changes compose cleanly. If `test_round_trip` fails because fixture markdowns still reference `package_family`, that is EXPECTED and will be resolved by Plan 51-04 in Wave 3. Document the failure shape in the SUMMARY but do not block this plan on it.
  </action>
  <verify>
    <automated>uv run --package wiki-io pytest packages/wiki-io/tests/ -x --ignore=packages/wiki-io/tests/test_round_trip.py</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "package-family\|package_family" packages/wiki-io/src/wiki_io/lint/dependency.py` returns 0.
    - Source: `grep -c "family_pages\|package_family_claims" packages/wiki-io/src/wiki_io/lint/dependency.py` returns 0.
    - Behavior: `pytest packages/wiki-io/tests/test_lint_dependency.py -x` passes (tests for other dependency-lint rules unaffected).
    - Behavior: `pytest packages/wiki-io/tests/ -x --ignore=packages/wiki-io/tests/test_round_trip.py` passes (round-trip fixture coupling deferred to Plan 04).
    - Documentation: SUMMARY records whether `test_round_trip` is failing on fixture-coupling (expected, resolved by Plan 04) or for some other reason (must investigate).
  </acceptance_criteria>
  <done>
    `lint/dependency.py` no longer references `package-family`; `dep-kind-not-in-enum` error message reflects the new enum; cross-page family rules deleted; full wiki-io suite passes (excluding `test_round_trip`, whose fixture is resolved in Plan 04).
  </done>
</task>

</tasks>

<verification>
After all 3 tasks complete:

1. `uv run --package wiki-io pytest packages/wiki-io/tests/ -x --ignore=packages/wiki-io/tests/test_round_trip.py` — green (round-trip deferred to Plan 04 fixture edits).
2. `grep -rn "package_family\|package-family\|ADMITTED_KINDS_V18" packages/wiki-io/src/` returns zero hits in `.py` files.
3. `test -f packages/wiki-io/src/wiki_io/assets/page-templates/entity-package-family.md` is false.
4. `test -f packages/wiki-io/src/wiki_io/assets/page-templates/package-family.md` is false.
5. `python -c "from wiki_io.entity_writer import ADMITTED_KINDS; assert len(ADMITTED_KINDS) == 6"` returns 0 exit code.

This plan unblocks Plan 51-04 (which depends on the source-side templates and lint being clean before the fixture is edited).
</verification>

<success_criteria>
- `ADMITTED_KINDS` is a 6-kind complete-and-final frozenset (success criterion #2 of phase).
- `ADMITTED_KINDS_V18` alias is gone (D-04 honored).
- `entity-package-family.md` and `package-family.md` template files are deleted (success criterion #3).
- `link_rewriter.py` has zero `package-family` code paths (RESEARCH.md Pitfall 4 strict-deletion).
- `lint/dependency.py` no longer ships the `package-family` kind discriminator (RESEARCH.md Pitfall 3 Option A).
- All wiki-io tests pass except `test_round_trip` (which is resolved by Plan 04 fixture edits).
- New `test_no_package_family_template` assertion is present and passing.
</success_criteria>

<output>
Create `.planning/phases/51-package-family-removal-divergence-rule-cleanup/51-02-SUMMARY.md` when done.
</output>
