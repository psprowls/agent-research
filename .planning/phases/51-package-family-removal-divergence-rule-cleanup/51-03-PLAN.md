---
phase: 51-package-family-removal-divergence-rule-cleanup
plan: 03
type: execute
wave: 3
depends_on: [51-02]
files_modified:
  - packages/eval-harness/src/eval_harness/divergence/librarian.py
  - packages/eval-harness/baselines/divergence-librarian.json
  - packages/eval-harness/tests/test_divergence_checks.py
  - packages/eval-harness/tests/test_divergence_baseline.py
  - packages/eval-harness/tests/test_two_gate_scorer.py
autonomous: true
requirements: [CLEANUP-01]

must_haves:
  truths:
    - "`_SLUG_ONLY_RE` is gone from `eval_harness/divergence/librarian.py`."
    - "`_check_no_slug_only_wikilinks` is gone from `librarian.py` (SYN-002's parallel function in `synthesizer.py` is RETAINED per RESEARCH.md Pitfall 1)."
    - "`LIB-003-no-slug-only-wikilinks` registry entry is removed from `LIBRARIAN_CHECKS`; LIB-001, LIB-002, LIB-004 are unchanged."
    - "`baselines/divergence-librarian.json` no longer contains the `LIB-003-no-slug-only-wikilinks` block; the baseline is internally consistent (regenerated via `pytest --accept-divergence-baseline` if Bedrock creds available, else hand-edited per D-05 fallback + RESEARCH.md Pitfall 2)."
    - "All divergence-related tests pass: `test_divergence_checks.py`, `test_divergence_baseline.py`, `test_two_gate_scorer.py`."
  artifacts:
    - path: "packages/eval-harness/src/eval_harness/divergence/librarian.py"
      provides: "Librarian divergence registry with LIB-003 removed; LIB-001/002/004 intact"
      contains: "LIBRARIAN_CHECKS"
    - path: "packages/eval-harness/baselines/divergence-librarian.json"
      provides: "Baseline snapshot consistent with the post-deletion registry; `recorded_at` and `agent_commit` updated"
  key_links:
    - from: "packages/eval-harness/src/eval_harness/divergence/librarian.py"
      to: "LIBRARIAN_CHECKS"
      via: "DivergenceCheck list registration"
      pattern: "LIBRARIAN_CHECKS.*list\\[DivergenceCheck\\]"
    - from: "packages/eval-harness/baselines/divergence-librarian.json"
      to: "LIBRARIAN_CHECKS"
      via: "baseline shape mirrors registry; no orphan rule entries"
      pattern: "LIB-003"
---

<objective>
Delete the `_SLUG_ONLY_RE` regex, the `_check_no_slug_only_wikilinks` librarian-role check function, and the `LIB-003-no-slug-only-wikilinks` registry entry from `eval-harness/divergence/librarian.py`; regenerate the divergence baseline JSON; update divergence-related tests.

Purpose: Satisfies CLEANUP-01 and phase success criterion #5. This plan is the eval-harness leg of the deletion; it runs in Wave 3 in parallel with Plan 51-04 (fixture surgery) because they touch disjoint package trees (`eval-harness/` vs `wiki-io/tests/fixtures/`). RESEARCH.md Pitfall 1 is critical: the synthesizer role has its own `_check_no_slug_only_wikilinks` (SYN-002) which uses different logic and MUST remain; the CLEANUP-01 grep gate is scoped to `librarian.py` only.

Output:
- `librarian.py` with `_SLUG_ONLY_RE` (line 53), `_check_no_slug_only_wikilinks` (lines 85-93), and the `LIB-003` `DivergenceCheck` registry entry (lines 117-122) deleted; LIB-001/002/004 untouched
- `baselines/divergence-librarian.json` regenerated (or hand-edited per D-05 fallback) with the `LIB-003-no-slug-only-wikilinks` block removed and `recorded_at`/`agent_commit` updated
- Test fixtures in `test_divergence_checks.py` (lines 97-112), `test_divergence_baseline.py` (line 35 helper), and `test_two_gate_scorer.py` (lines 98, 144) updated to drop LIB-003 references
- Full eval-harness divergence test suite green
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/51-package-family-removal-divergence-rule-cleanup/51-CONTEXT.md
@.planning/phases/51-package-family-removal-divergence-rule-cleanup/51-RESEARCH.md
@.planning/phases/51-package-family-removal-divergence-rule-cleanup/51-VALIDATION.md
@packages/eval-harness/src/eval_harness/divergence/librarian.py
@packages/eval-harness/src/eval_harness/divergence/synthesizer.py
@packages/eval-harness/baselines/divergence-librarian.json
@packages/eval-harness/tests/test_divergence_checks.py
@packages/eval-harness/tests/test_divergence_baseline.py
@packages/eval-harness/tests/test_two_gate_scorer.py
@packages/eval-harness/tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 51-03-01: Delete `_SLUG_ONLY_RE`, `_check_no_slug_only_wikilinks`, and the `LIB-003` registry entry from `librarian.py`</name>
  <files>packages/eval-harness/src/eval_harness/divergence/librarian.py</files>
  <read_first>
    - packages/eval-harness/src/eval_harness/divergence/librarian.py (target — read entire file, especially lines 53, 85-93, 117-122 per RESEARCH.md)
    - packages/eval-harness/src/eval_harness/divergence/synthesizer.py (READ ONLY — confirm SYN-002's `_check_no_slug_only_wikilinks` at line 46 is structurally distinct and is NOT to be touched per RESEARCH.md Pitfall 1)
    - .planning/phases/51-package-family-removal-divergence-rule-cleanup/51-RESEARCH.md §"Common Pitfalls" Pitfall 1 (DO NOT delete synthesizer's parallel function)
    - .planning/phases/51-package-family-removal-divergence-rule-cleanup/51-RESEARCH.md §"Code Examples" — "Divergence rule registry single-row delete"
  </read_first>
  <action>
    In `packages/eval-harness/src/eval_harness/divergence/librarian.py`:

    1. Delete the `_SLUG_ONLY_RE` regex assignment at line ~53 (RESEARCH.md verified).
    2. Delete the entire `_check_no_slug_only_wikilinks` function at lines ~85-93.
    3. From the `LIBRARIAN_CHECKS: list[DivergenceCheck]` registration block at lines ~104-129, delete ONLY the `DivergenceCheck(id="LIB-003-no-slug-only-wikilinks", ...)` entry (lines ~117-122 per RESEARCH.md). LIB-001, LIB-002, LIB-004 entries MUST remain unchanged.

    Do NOT touch `packages/eval-harness/src/eval_harness/divergence/synthesizer.py`. RESEARCH.md Pitfall 1 documents that `synthesizer.py:46` defines a separately-registered `_check_no_slug_only_wikilinks` for the SYN-002 check, which uses different logic (`"/" not in slug` vs `_SLUG_ONLY_RE`) and serves the synthesizer role's gate. Deleting it would break unrelated tests.

    After editing, confirm via grep that `librarian.py` is clean:
    - `grep -c "_SLUG_ONLY_RE\|_check_no_slug_only_wikilinks\|LIB-003" packages/eval-harness/src/eval_harness/divergence/librarian.py` must return 0.
    - `grep -c "_check_no_slug_only_wikilinks" packages/eval-harness/src/eval_harness/divergence/synthesizer.py` must return at least 1 (SYN-002 still present).
  </action>
  <verify>
    <automated>uv run --package eval-harness python -c "from eval_harness.divergence.librarian import LIBRARIAN_CHECKS; ids = [c.id for c in LIBRARIAN_CHECKS]; assert 'LIB-003-no-slug-only-wikilinks' not in ids; assert 'LIB-001-wikilinks-resolve' in ids or any('LIB-001' in i for i in ids); print('librarian registry clean:', ids)"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "_SLUG_ONLY_RE\|_check_no_slug_only_wikilinks\|LIB-003" packages/eval-harness/src/eval_harness/divergence/librarian.py` returns 0.
    - Source: `grep -c "_check_no_slug_only_wikilinks" packages/eval-harness/src/eval_harness/divergence/synthesizer.py` returns >= 1 (SYN-002 retained).
    - Behavior: `python -c "from eval_harness.divergence.librarian import LIBRARIAN_CHECKS; assert all(c.id != 'LIB-003-no-slug-only-wikilinks' for c in LIBRARIAN_CHECKS)"` exits 0.
    - Behavior: `python -c "from eval_harness.divergence.librarian import _SLUG_ONLY_RE"` raises `ImportError`.
    - Behavior: `python -c "from eval_harness.divergence.librarian import _check_no_slug_only_wikilinks"` raises `ImportError`.
  </acceptance_criteria>
  <done>
    LIB-003 is gone from `librarian.py` (regex + function + registry entry); SYN-002's parallel function in `synthesizer.py` is untouched and importable; the librarian registry retains LIB-001, LIB-002, LIB-004.
  </done>
</task>

<task type="auto">
  <name>Task 51-03-02: Update divergence tests + regenerate baseline JSON</name>
  <files>packages/eval-harness/tests/test_divergence_checks.py, packages/eval-harness/tests/test_divergence_baseline.py, packages/eval-harness/tests/test_two_gate_scorer.py, packages/eval-harness/baselines/divergence-librarian.json</files>
  <read_first>
    - packages/eval-harness/tests/test_divergence_checks.py (target — lines 97-112 contain LIB-003 test cases per RESEARCH.md)
    - packages/eval-harness/tests/test_divergence_baseline.py (target — line 35 `_make_results` helper per RESEARCH.md)
    - packages/eval-harness/tests/test_two_gate_scorer.py (target — lines 98 and 144 per RESEARCH.md)
    - packages/eval-harness/baselines/divergence-librarian.json (target — block to delete at lines 29-33 per RESEARCH.md)
    - .planning/phases/51-package-family-removal-divergence-rule-cleanup/51-CONTEXT.md §D-05 (regen via tooling; hand-edit only as fallback)
    - .planning/phases/51-package-family-removal-divergence-rule-cleanup/51-RESEARCH.md §"Common Pitfalls" Pitfall 2 (live-Bedrock-regen requires `GRAPH_WIKI_RUN_EVAL=1` + AWS creds + ~$2 spend; hand-edit fallback is acceptable per D-05 last clause)
    - packages/eval-harness/tests/conftest.py (read for `--accept-divergence-baseline` pytest hook registration)
  </read_first>
  <action>
    **Test edits (do first):**

    1. `packages/eval-harness/tests/test_divergence_checks.py`: delete the LIB-003 test cases at lines ~97-112 (typically named `test_lib003_*` per RESEARCH.md). Leave LIB-001/002/004 test cases alone.

    2. `packages/eval-harness/tests/test_divergence_baseline.py`: in the `_make_results` helper at line ~35, remove the `"LIB-003-no-slug-only-wikilinks"` key from the dict it returns. Any other test functions that hardcode LIB-003 expectations (e.g., asserting a specific row count) must be updated accordingly.

    3. `packages/eval-harness/tests/test_two_gate_scorer.py`: at lines ~98 and ~144, remove the `LIB-003-no-slug-only-wikilinks` entries from the `heavy_failures` and `clean_results` dicts respectively. The numeric assertions that depend on row counts must be updated to reflect one fewer LIB-* row.

    **Baseline regeneration (D-05 path selection):**

    Pick ONE path at execution time:

    - **Path A — Live tooling regen (D-05 preferred, RESEARCH.md Pitfall 2 documents cost):** If `AWS_ACCESS_KEY_ID` (or `AWS_PROFILE`) is set in the executor's environment, run:
      `GRAPH_WIKI_RUN_EVAL=1 uv run --package eval-harness pytest packages/eval-harness/tests/test_divergence.py -k librarian --accept-divergence-baseline`
      This rewrites `packages/eval-harness/baselines/divergence-librarian.json` with `recorded_at` and `agent_commit` stamped automatically. Expected runtime 5-15 minutes; expected Bedrock spend ~$0.50-$2.

    - **Path B — Hand-edit fallback (D-05 final clause + RESEARCH.md Pitfall 2 recommendation for solo dev):** Open `packages/eval-harness/baselines/divergence-librarian.json` and delete the `LIB-003-no-slug-only-wikilinks` block at lines ~29-33 (the exact line range may shift slightly — locate by the key string). Update `recorded_at` to the current UTC ISO timestamp (e.g., `2026-05-28T12:34:56Z`). Update `agent_commit` to the short SHA of the current `HEAD` (`git rev-parse --short HEAD`). Verify the JSON parses with `python -c "import json; json.load(open('packages/eval-harness/baselines/divergence-librarian.json'))"`.

    Document the chosen path in the SUMMARY (`Path A: live regen` or `Path B: hand-edit, AWS creds absent`).

    After both test edits and baseline regen are complete, run the full divergence test suite:
    `uv run --package eval-harness pytest packages/eval-harness/tests/test_divergence_checks.py packages/eval-harness/tests/test_divergence_baseline.py packages/eval-harness/tests/test_two_gate_scorer.py -x`

    All three test modules must pass. If `test_divergence_baseline.py` fails on a `recorded_at` / `agent_commit` mismatch with Path B, re-run the hand-edit with the exact format the tests expect (study the test source first).
  </action>
  <verify>
    <automated>uv run --package eval-harness pytest packages/eval-harness/tests/test_divergence_checks.py packages/eval-harness/tests/test_divergence_baseline.py packages/eval-harness/tests/test_two_gate_scorer.py -x</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "LIB-003" packages/eval-harness/tests/test_divergence_checks.py packages/eval-harness/tests/test_divergence_baseline.py packages/eval-harness/tests/test_two_gate_scorer.py packages/eval-harness/baselines/divergence-librarian.json` returns 0.
    - Source: `python -c "import json; data=json.load(open('packages/eval-harness/baselines/divergence-librarian.json')); print(list(data.get('results', data).keys()) if isinstance(data, dict) else type(data))"` runs without raising and the output does not include `LIB-003-no-slug-only-wikilinks`.
    - Behavior: `pytest packages/eval-harness/tests/test_divergence_checks.py packages/eval-harness/tests/test_divergence_baseline.py packages/eval-harness/tests/test_two_gate_scorer.py -x` passes.
    - Behavior: Full eval-harness suite `pytest packages/eval-harness/tests/ -x` passes (CLEANUP-01 with no LIB-003 collateral damage).
    - Documentation: SUMMARY records which path (A live regen vs B hand-edit) was taken and why.
  </acceptance_criteria>
  <done>
    Divergence tests no longer reference LIB-003; baseline JSON regenerated (or hand-edited) with the LIB-003 block removed; full divergence + eval-harness suite green; path selection documented.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `uv run --package eval-harness pytest packages/eval-harness/tests/ -x` — full eval-harness suite green.
2. Grep gate G2 (success criterion #5):
   - `! grep -rn "_SLUG_ONLY_RE\|LIB-003" packages/eval-harness/ | grep -q .` (zero hits)
   - `! grep -n "_check_no_slug_only_wikilinks" packages/eval-harness/src/eval_harness/divergence/librarian.py | grep -q .` (zero hits in librarian.py specifically)
3. SYN-002 sanity check: `grep -c "_check_no_slug_only_wikilinks" packages/eval-harness/src/eval_harness/divergence/synthesizer.py` returns >= 1 (SYN-002 intact per Pitfall 1).
4. Baseline JSON validity: `python -c "import json; json.load(open('packages/eval-harness/baselines/divergence-librarian.json'))"` succeeds.

This plan plus Plan 51-04 are the only Wave 3 plans; together they close the phase.
</verification>

<success_criteria>
- `_SLUG_ONLY_RE`, `_check_no_slug_only_wikilinks`, and the `LIB-003` registry entry are gone from `librarian.py`.
- `synthesizer.py`'s SYN-002 parallel function is untouched (RESEARCH.md Pitfall 1).
- `baselines/divergence-librarian.json` no longer contains the `LIB-003-no-slug-only-wikilinks` block; the baseline parses as valid JSON.
- All divergence-related tests pass: `test_divergence_checks.py`, `test_divergence_baseline.py`, `test_two_gate_scorer.py`.
- Full eval-harness suite passes.
- Baseline regen path (A live or B hand-edit) is documented in the SUMMARY.
- Phase exit gate G2 (grep) passes.
</success_criteria>

<output>
Create `.planning/phases/51-package-family-removal-divergence-rule-cleanup/51-03-SUMMARY.md` when done.
</output>
