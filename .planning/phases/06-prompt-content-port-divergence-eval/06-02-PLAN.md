---
phase: 06-prompt-content-port-divergence-eval
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/code-wiki-agent/tests/prompts/__init__.py
  - agents/code-wiki-agent/tests/prompts/test_prompt_snapshots.py
  - agents/code-wiki-agent/tests/prompts/test_provenance.py
  - cores/eval-harness/tests/test_divergence_checks.py
  - cores/eval-harness/tests/test_divergence_baseline.py
  - cores/eval-harness/tests/test_divergence.py
  - cores/eval-harness/tests/conftest.py
autonomous: true
requirements: [PORT-01, PORT-06, EVAL-11, EVAL-12, EVAL-13]
tags: [test-scaffolding, wave-0, divergence-eval, prompt-port]

must_haves:
  truths:
    - "Test packages and skeleton test modules exist for every requirement-mapped command in 06-VALIDATION.md"
    - "Skeleton tests are runnable (collect cleanly) and explicitly fail-fast or skip until implementations land in later waves"
    - "`pytest_addoption` registers `--accept-divergence-baseline` so EVAL-13 work in 06-10 has a flag hook"
  artifacts:
    - path: "agents/code-wiki-agent/tests/prompts/__init__.py"
      provides: "Test package marker"
    - path: "agents/code-wiki-agent/tests/prompts/test_prompt_snapshots.py"
      provides: "Skeleton with one pytest.skip-marked test per role import target"
    - path: "agents/code-wiki-agent/tests/prompts/test_provenance.py"
      provides: "Skeleton with provenance regex + pytest.skip when no fragments exist yet"
    - path: "cores/eval-harness/tests/test_divergence_checks.py"
      provides: "Skeleton parametrized over rule IDs, skip-marked until rules exist"
    - path: "cores/eval-harness/tests/test_divergence_baseline.py"
      provides: "Skeleton load/write/check_regression tests, skip-marked"
    - path: "cores/eval-harness/tests/test_divergence.py"
      provides: "EVAL_GATE-marked integration test skeleton"
    - path: "cores/eval-harness/tests/conftest.py"
      modifies: "Adds --accept-divergence-baseline option + accept_baseline fixture"
  key_links:
    - from: "test_divergence.py"
      to: "conftest.py::accept_baseline fixture"
      via: "pytest fixture injection"
      pattern: "def test_divergence_regression\\(.*accept_baseline.*\\)"
---

<objective>
Create empty/skeleton test files for every requirement-to-test mapping in 06-VALIDATION.md so later tasks can wire `<automated>` blocks against real test paths immediately. No production behavior is added — these files define the test contract and skip-mark themselves until implementations land.

Purpose: Honors the Nyquist rule from 06-VALIDATION.md (every task in this phase must point at one of the listed pytest commands). Without these scaffold files, `<automated>` blocks would reference files that don't exist and fail collection. Wave 0 in spirit (no implementation), but runs as Wave 1 because it has no upstream dependencies.

Output: Six test module skeletons plus a `conftest.py` addition for the `--accept-divergence-baseline` flag. Each skeleton imports its target (wrapped in try/except ImportError -> pytest.skip) so `pytest --collect-only` succeeds.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-prompt-content-port-divergence-eval/06-CONTEXT.md
@.planning/phases/06-prompt-content-port-divergence-eval/06-RESEARCH.md
@.planning/phases/06-prompt-content-port-divergence-eval/06-PATTERNS.md
@.planning/phases/06-prompt-content-port-divergence-eval/06-VALIDATION.md
@cores/eval-harness/tests/conftest.py
@cores/eval-harness/tests/test_structural.py
@cores/eval-harness/tests/test_baseline.py
@cores/eval-harness/tests/eval/test_sweep_eval.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create code-wiki-agent prompt test scaffolding</name>
  <files>
    agents/code-wiki-agent/tests/prompts/__init__.py,
    agents/code-wiki-agent/tests/prompts/test_prompt_snapshots.py,
    agents/code-wiki-agent/tests/prompts/test_provenance.py
  </files>
  <read_first>
    - agents/code-wiki-agent/tests/__init__.py (confirm convention)
    - agents/code-wiki-agent/tests/unit/test_commands_scan.py (style analog from PATTERNS.md)
    - .planning/phases/06-prompt-content-port-divergence-eval/06-PATTERNS.md §`tests/prompts/test_prompt_snapshots.py` and §`tests/prompts/test_provenance.py`
    - .planning/phases/06-prompt-content-port-divergence-eval/06-VALIDATION.md (the Wave 0 Requirements section lists the exact test ids needed)
  </read_first>
  <action>
    Create `agents/code-wiki-agent/tests/prompts/__init__.py` (empty file marking the package).

    Create `agents/code-wiki-agent/tests/prompts/test_prompt_snapshots.py` with `from __future__ import annotations` header, module docstring, `import pytest`, `from syrupy.assertion import SnapshotAssertion`. Define seven test functions, one per expected exported constant: `test_librarian_system_snapshot`, `test_ingestor_system_snapshot`, `test_linter_page_quality_system_snapshot`, `test_linter_adr_chain_system_snapshot`, `test_linter_stale_claims_system_snapshot`, `test_scanner_system_snapshot`, `test_synthesizer_system_snapshot`, `test_code_reader_system_snapshot`. Each function imports its target inside the function body wrapped in a try/except ImportError that calls `pytest.skip("prompts module not yet implemented")`. Each calls `assert <CONST> == snapshot`. This makes the file collectible NOW and lets 06-04..06-07 enable snapshots once the prompt modules land.

    Create `agents/code-wiki-agent/tests/prompts/test_provenance.py` with `from __future__ import annotations`, `import re`, `from pathlib import Path`, `import pytest`. Anchor `FRAGMENT_DIR = Path(__file__).resolve().parents[2] / "src" / "code_wiki_agent" / "prompts" / "_fragments"`. Anchor `PROMPT_SOURCES_DIR = Path(__file__).resolve().parents[4] / "cores" / "prompt-sources"`. Define `_PROVENANCE_RE` exactly per PATTERNS.md §test_provenance (matches `# Source: ...\n# Anchor: ...\n# Source-commit: <hex>`). Define two tests:
      - `test_all_fragments_have_provenance_header`: if `FRAGMENT_DIR` does not exist OR contains only `__init__.py`, call `pytest.skip("no fragment files yet (06-03 hasn't landed)")`. Otherwise, iterate every non-`__init__.py` `.py` file under FRAGMENT_DIR and assert `_PROVENANCE_RE.search(text)` matches with a message containing the filename.
      - `test_provenance_source_paths_resolve`: same skip guard; for each fragment, parse out the `Source:` value, assert it starts with `cores/prompt-sources/` and that the path (stripped of the prefix and joined to PROMPT_SOURCES_DIR) exists.

    All three files use `from __future__ import annotations` on line 1.
  </action>
  <verify>
    <automated>uv run pytest agents/code-wiki-agent/tests/prompts/ --collect-only -q 2>&1 | grep -E '(error|collected)' | tail -5 && uv run pytest agents/code-wiki-agent/tests/prompts/test_provenance.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `agents/code-wiki-agent/tests/prompts/__init__.py` exists (may be empty).
    - `test_prompt_snapshots.py` defines exactly 8 test functions (one per role: librarian, ingestor, linter ×3, scanner, synthesizer, code_reader).
    - Each snapshot test imports inside its body and calls `pytest.skip` on ImportError.
    - `test_provenance.py` defines `_PROVENANCE_RE` matching the 3-line header in 06-PATTERNS.md.
    - `test_provenance.py` skips cleanly when `_fragments/` directory is empty or missing.
    - `uv run pytest agents/code-wiki-agent/tests/prompts/ --collect-only` reports >= 10 tests collected without errors.
    - All current tests pass (every one should report SKIPPED because no production code exists yet).
  </acceptance_criteria>
  <done>
    Three scaffold files exist; pytest collects all tests without import errors; tests skip cleanly because prompts module does not exist yet.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create eval-harness divergence test scaffolding + accept_baseline option</name>
  <files>
    cores/eval-harness/tests/conftest.py,
    cores/eval-harness/tests/test_divergence_checks.py,
    cores/eval-harness/tests/test_divergence_baseline.py,
    cores/eval-harness/tests/test_divergence.py
  </files>
  <read_first>
    - cores/eval-harness/tests/conftest.py (existing EVAL_GATE + fixtures)
    - cores/eval-harness/tests/test_structural.py (unit-test analog from PATTERNS.md)
    - cores/eval-harness/tests/test_baseline.py (schema-test analog)
    - cores/eval-harness/tests/eval/test_sweep_eval.py (EVAL_GATE analog)
    - .planning/phases/06-prompt-content-port-divergence-eval/06-PATTERNS.md §`eval-harness/tests/test_divergence_*`
    - .planning/phases/06-prompt-content-port-divergence-eval/06-RESEARCH.md §`pytest Divergence Gate Test`
  </read_first>
  <action>
    Modify `cores/eval-harness/tests/conftest.py` to add the `--accept-divergence-baseline` option exactly per the shape shown in 06-RESEARCH.md §`--accept-divergence-baseline CLI hook`:
      - Add (or extend if `pytest_addoption` already exists) a `pytest_addoption(parser)` hook registering `--accept-divergence-baseline` with `action="store_true"`, `default=False`, helpful message: "Overwrite divergence baselines with current run results".
      - Add an `accept_baseline` fixture returning `request.config.getoption("--accept-divergence-baseline")`.
    Do NOT remove or alter the existing `EVAL_GATE` constant, existing fixtures, or any other content in conftest.py.

    Create `cores/eval-harness/tests/test_divergence_checks.py`: `from __future__ import annotations`, `import pytest`, `from pathlib import Path`. Define a module-level guard: at import time, try `from eval_harness.divergence.check import DivergenceCheck, AgentOutputProxy` and from each of `librarian, ingestor, linter, scanner` try `from eval_harness.divergence.<role> import {ROLE_PREFIX}_CHECKS`. If any ImportError, set `_DIVERGENCE_AVAILABLE = False`. Add a module-level `pytestmark = pytest.mark.skipif(not _DIVERGENCE_AVAILABLE, reason="divergence module not yet implemented")`. Define stub tests `test_lib001_passes_on_resolved_wikilink`, `test_lib001_fails_on_unresolved_wikilink`, `test_ing001_frontmatter_present`, `test_lnt002_findings_nonempty`, `test_scn001_frontmatter_present` that each do `pytest.skip("filled in by 06-08")` for now — they exist to make the test discoverable.

    Create `cores/eval-harness/tests/test_divergence_baseline.py`: same import-guard shape. Define `test_load_baseline_returns_empty_when_missing`, `test_write_baseline_schema`, `test_check_regression_raises_on_hard_increase`, `test_check_regression_does_not_raise_for_soft`, `test_accept_baseline_flag_rewrites_file` — all skip-stubbed for now.

    Create `cores/eval-harness/tests/test_divergence.py`: `from __future__ import annotations`, `import os`, `import pytest`, `from pathlib import Path`. Define `EVAL_GATE = pytest.mark.skipif(not os.environ.get("CODE_WIKI_RUN_EVAL"), reason="Set CODE_WIKI_RUN_EVAL=1 to run divergence eval")` per PATTERNS.md §EVAL_GATE. Define an import-guard `_DIVERGENCE_AVAILABLE`. Define `BASELINES_DIR = Path(__file__).parent.parent / "baselines"`. Add `@EVAL_GATE`-marked stub `test_divergence_regression(role, accept_baseline)` parametrized over `["librarian", "ingestor", "linter", "scanner"]` that skips with `"filled in by 06-11"` for now.

    Every new test file uses `from __future__ import annotations` on line 1.
  </action>
  <verify>
    <automated>uv run pytest cores/eval-harness/tests/test_divergence_checks.py cores/eval-harness/tests/test_divergence_baseline.py cores/eval-harness/tests/test_divergence.py --collect-only -q 2>&1 | tail -10 && uv run pytest cores/eval-harness/tests/test_divergence_checks.py cores/eval-harness/tests/test_divergence_baseline.py cores/eval-harness/tests/test_divergence.py -x -q 2>&1 | tail -5 && uv run pytest --help 2>&1 | grep -- '--accept-divergence-baseline'</automated>
  </verify>
  <acceptance_criteria>
    - `cores/eval-harness/tests/conftest.py` now registers `--accept-divergence-baseline` (visible in `pytest --help` output).
    - `accept_baseline` fixture defined and returns the option value.
    - Three test files exist with skip-stubbed tests for every rule-id and baseline behavior listed in 06-VALIDATION.md.
    - `pytest --collect-only` reports all tests collected (no import errors).
    - All tests skip when run (no module-not-found failures, no production code referenced beyond import-guarded stubs).
    - `EVAL_GATE` in `test_divergence.py` uses `CODE_WIKI_RUN_EVAL` env var (matches the existing harness gate pattern).
  </acceptance_criteria>
  <done>
    All three new test files collect and skip cleanly; `--accept-divergence-baseline` option visible in pytest help; conftest.py is the only modified file outside the new scaffolds.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test files -> conftest.py options | New pytest plugin option introduced; respected by the workspace pyproject |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Tampering | Existing `conftest.py` (EVAL_GATE + other fixtures) | mitigate | Task action explicitly preserves existing content; `pytest_addoption` is extended (or co-defined) without removing existing options |
| T-06-04 | Denial of Service | Test collection failure due to missing imports | mitigate | Every new test file uses an import-guard + module-level pytestmark.skipif so tests skip rather than fail on missing implementations |
</threat_model>

<verification>
- Six new test files collect under `uv run pytest --collect-only -q`
- All tests SKIP (no failures, no collection errors)
- `--accept-divergence-baseline` option registered (visible in `pytest --help`)
- Existing tests still pass (no regressions in conftest.py)
</verification>

<success_criteria>
- Test scaffolding in place for every Wave 0 requirement listed in 06-VALIDATION.md
- All `<automated>` blocks in 06-03..06-11 can point at these test files immediately
- `--accept-divergence-baseline` is wired before 06-10 needs it
</success_criteria>

<output>
Create `.planning/phases/06-prompt-content-port-divergence-eval/06-02-SUMMARY.md` when done.
</output>
