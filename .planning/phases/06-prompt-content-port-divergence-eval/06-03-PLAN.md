---
phase: 06-prompt-content-port-divergence-eval
plan: 03
type: execute
wave: 2
depends_on: [06-01, 06-02]
files_modified:
  - agents/code-wiki-agent/src/code_wiki_agent/prompts/__init__.py
  - agents/code-wiki-agent/src/code_wiki_agent/prompts/_fragments/__init__.py
  - agents/code-wiki-agent/src/code_wiki_agent/prompts/_fragments/iron_rules.py
  - agents/code-wiki-agent/src/code_wiki_agent/prompts/_fragments/page_categories.py
  - agents/code-wiki-agent/src/code_wiki_agent/prompts/_fragments/citation_rules.py
  - agents/code-wiki-agent/src/code_wiki_agent/prompts/_fragments/frontmatter_rules.py
autonomous: true
requirements: [PORT-01, PORT-06]
tags: [prompt-port, fragments, provenance]

must_haves:
  truths:
    - "prompts/__init__.py and prompts/_fragments/__init__.py exist as importable Python packages under code_wiki_agent"
    - "Each of the 4 shared fragment files exports its named constant (IRON_RULES, PAGE_CATEGORIES, CITATION_RULES, FRONTMATTER_RULES)"
    - "Every fragment file carries the locked 3-line provenance header (Source / Anchor / Source-commit)"
    - "Every Source: header path resolves to an existing file under cores/prompt-sources/"
    - "Fragment content is semantically faithful to the source anchor (iron rules verbatim, page categories verbatim, citation rules paraphrased only to remove host-specific references per D-05/D-06)"
  artifacts:
    - path: "agents/code-wiki-agent/src/code_wiki_agent/prompts/__init__.py"
      provides: "Package marker (may export role constants via re-export, optional)"
    - path: "agents/code-wiki-agent/src/code_wiki_agent/prompts/_fragments/__init__.py"
      provides: "Subpackage marker"
    - path: "agents/code-wiki-agent/src/code_wiki_agent/prompts/_fragments/iron_rules.py"
      provides: "IRON_RULES constant — SKILL.md §Iron rules verbatim (7 rules)"
      contains: "IRON_RULES"
    - path: "agents/code-wiki-agent/src/code_wiki_agent/prompts/_fragments/page_categories.py"
      provides: "PAGE_CATEGORIES constant — SKILL.md §Page categories table"
      contains: "PAGE_CATEGORIES"
    - path: "agents/code-wiki-agent/src/code_wiki_agent/prompts/_fragments/citation_rules.py"
      provides: "CITATION_RULES constant — librarian/ingestor citation convention"
      contains: "CITATION_RULES"
    - path: "agents/code-wiki-agent/src/code_wiki_agent/prompts/_fragments/frontmatter_rules.py"
      provides: "FRONTMATTER_RULES constant — required frontmatter fields"
      contains: "FRONTMATTER_RULES"
  key_links:
    - from: "_fragments/*.py"
      to: "cores/prompt-sources/{SKILL.md,agents/*.md}"
      via: "# Source: header in fragment provenance comment"
      pattern: "^# Source: cores/prompt-sources/"
    - from: "test_provenance.py"
      to: "_fragments/*.py"
      via: "regex match on 3-line header"
      pattern: "_PROVENANCE_RE"
---

<objective>
Create the `prompts/` and `prompts/_fragments/` Python packages under `code_wiki_agent`, then write the four shared fragment files (`iron_rules.py`, `page_categories.py`, `citation_rules.py`, `frontmatter_rules.py`) per D-01/D-02/D-03 with locked provenance headers. Content is ported verbatim where canonical (iron rules, page-category table) and lightly adapted only to remove host-specific references (citation rules, frontmatter rules) per D-05/D-06.

Purpose: This is the shared-content foundation that the 4 per-role prompt files in 06-04..06-07 compose against. Splitting fragments now de-duplicates verbatim content that appears across all four lattice-wiki agent files (per RESEARCH §Source File Content Analysis). Provenance headers enable drift detection per PORT-06.

Output: Two `__init__.py` package markers + four fragment files with `# Source: / # Anchor: / # Source-commit:` headers and one SCREAMING_SNAKE_CASE constant each.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-prompt-content-port-divergence-eval/06-CONTEXT.md
@.planning/phases/06-prompt-content-port-divergence-eval/06-RESEARCH.md
@.planning/phases/06-prompt-content-port-divergence-eval/06-PATTERNS.md
@cores/prompt-sources/SKILL.md
@cores/prompt-sources/agents/librarian.md
@cores/prompt-sources/agents/ingestor.md
@cores/prompt-sources/agents/scanner.md
@cores/prompt-sources/SOURCE-COMMIT
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create prompts package + shared fragment files</name>
  <files>
    agents/code-wiki-agent/src/code_wiki_agent/prompts/__init__.py,
    agents/code-wiki-agent/src/code_wiki_agent/prompts/_fragments/__init__.py,
    agents/code-wiki-agent/src/code_wiki_agent/prompts/_fragments/iron_rules.py,
    agents/code-wiki-agent/src/code_wiki_agent/prompts/_fragments/page_categories.py,
    agents/code-wiki-agent/src/code_wiki_agent/prompts/_fragments/citation_rules.py,
    agents/code-wiki-agent/src/code_wiki_agent/prompts/_fragments/frontmatter_rules.py
  </files>
  <read_first>
    - cores/prompt-sources/SKILL.md (§Iron rules, §Page categories — exact line ranges per RESEARCH)
    - cores/prompt-sources/agents/librarian.md (§Rules — citation rule bullets)
    - cores/prompt-sources/agents/ingestor.md (§Workflow step 4 — frontmatter fields, §Rules — cite aggressively)
    - cores/prompt-sources/agents/scanner.md (§Workflow step 3 — stub frontmatter fields)
    - cores/prompt-sources/SOURCE-COMMIT (the 7+ char SHA to put in `# Source-commit:` headers)
    - .planning/phases/06-prompt-content-port-divergence-eval/06-PATTERNS.md §`_fragments/iron_rules.py` and §Provenance header (locked format)
    - .planning/phases/06-prompt-content-port-divergence-eval/06-RESEARCH.md §Source File Content Analysis (what content goes in each fragment) and §Adaptation Map (host-specific references to rewrite)
    - agents/code-wiki-agent/src/code_wiki_agent/commands/scan.py (style reference for `"""\` backslash-after-triple-quote)
    - agents/code-wiki-agent/src/code_wiki_agent/commands/__init__.py (package init style)
  </read_first>
  <action>
    Create `agents/code-wiki-agent/src/code_wiki_agent/prompts/__init__.py`: empty file (or a one-line module docstring). Do NOT eagerly import role files here — that would create import-order coupling that breaks if any role file is missing. Per-role files are imported by their consumers directly.

    Create `agents/code-wiki-agent/src/code_wiki_agent/prompts/_fragments/__init__.py`: empty file (just a package marker; underscore prefix marks it as a private subpackage per Python convention).

    Read the SHA from `cores/prompt-sources/SOURCE-COMMIT` (first line, 7+ hex chars) and use it as the `<SHA>` value for every `# Source-commit:` header in this plan.

    Create `iron_rules.py`. Header (exactly 3 lines, no blank line between header and constant, blank line between header and `IRON_RULES =`):
      Line 1: `# Source: cores/prompt-sources/SKILL.md`
      Line 2: `# Anchor: ## Iron rules (L193-L201)`  (verify exact line range by reading SKILL.md; update if shifted)
      Line 3: `# Source-commit: <SHA from SOURCE-COMMIT>`
    Then a blank line, then `IRON_RULES = """\` opening (backslash escape per PATTERNS.md to avoid leading newline), then the 7 numbered iron rules verbatim from SKILL.md §Iron rules, then `"""`. No `from __future__` import — fragments are pure string-constant modules and have no annotations.

    Create `page_categories.py`. Header:
      Line 1: `# Source: cores/prompt-sources/SKILL.md`
      Line 2: `# Anchor: ## Page categories (table — L143-L156)` (verify exact range)
      Line 3: `# Source-commit: <SHA>`
    `PAGE_CATEGORIES = """\` opening, then the verbatim category table from SKILL.md §Page categories, then `"""`. Adapt `<workspace>/wiki/` references conservatively: keep the wording from canonical source but ensure path references read as `vault root` rather than the `<workspace>/wiki/` placeholder (per RESEARCH §Adaptation Map).

    Create `citation_rules.py`. Header:
      Line 1: `# Source: cores/prompt-sources/agents/librarian.md`
      Line 2: `# Anchor: ## Rules (citation bullets — L73-L77)` (verify exact range)
      Line 3: `# Source-commit: <SHA>`
    `CITATION_RULES = """\` opening. Content combines the citation/wikilink convention bullets from librarian.md §Rules (lines ~73-77) AND the ingestor.md `## Rules` "Cite aggressively" bullet. Adapt: REMOVE references to `/lattice-wiki:ingest` slash commands or `${CLAUDE_PLUGIN_ROOT}` paths (RESEARCH §Adaptation Map). Preserve: the `[[wikilink]]` syntax statement, the `path:line` annotation rule, "every claim cites" rule, the rule that wikilinks must point at existing vault pages.

    Create `frontmatter_rules.py`. Header:
      Line 1: `# Source: cores/prompt-sources/agents/ingestor.md`
      Line 2: `# Anchor: ## Workflow step 4 (required fields — L50-L58)` (verify)
      Line 3: `# Source-commit: <SHA>`
    `FRONTMATTER_RULES = """\` opening. Content lists the required frontmatter fields union across ingestor + scanner. Required fields list: `title`, `category`, `page_type` (ingestor-specific), `target_slug` (ingestor-specific), `summary`, `tags`, plus scanner-specific `package_path`, `language`. State that not all fields apply to every role: scanner stubs use the package subset, ingestor source pages use the page-type subset. Adapt: replace `<workspace>/wiki/` with neutral wording about the vault.

    Token budget: per RESEARCH §Pitfall 1, each fragment should stay under ~200 tokens. If a fragment grows beyond that, trim workflow prose (orchestration concerns belong in command code, not the LLM prompt).

    Critical: do not include any Python-level imports from `cores/prompt-sources/` (RESEARCH Anti-Pattern — the vendored directory is text, not importable Python).
  </action>
  <verify>
    <automated>uv run python -c "from code_wiki_agent.prompts._fragments.iron_rules import IRON_RULES; from code_wiki_agent.prompts._fragments.page_categories import PAGE_CATEGORIES; from code_wiki_agent.prompts._fragments.citation_rules import CITATION_RULES; from code_wiki_agent.prompts._fragments.frontmatter_rules import FRONTMATTER_RULES; assert 'Iron rules' in IRON_RULES, IRON_RULES[:200]; assert 'wikilink' in CITATION_RULES.lower(); assert 'title' in FRONTMATTER_RULES.lower(); print('OK')" && uv run pytest agents/code-wiki-agent/tests/prompts/test_provenance.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - All 6 files exist at the paths above.
    - Each fragment file's first three non-empty lines match `_PROVENANCE_RE` from `test_provenance.py`.
    - Each fragment file's `Source:` value, stripped of the `cores/prompt-sources/` prefix and joined to the workspace `cores/prompt-sources/` dir, resolves to an existing file.
    - `uv run pytest agents/code-wiki-agent/tests/prompts/test_provenance.py -x` passes (PORT-06 gate satisfied).
    - `IRON_RULES`, `PAGE_CATEGORIES`, `CITATION_RULES`, `FRONTMATTER_RULES` are each importable and contain the expected canonical phrases ("Iron rules", category names, "wikilink", "title").
    - No fragment file imports from `cores/prompt-sources/` or from any other Python module (each is a pure constant module).
    - `uv sync` still succeeds (no broken module structure).
  </acceptance_criteria>
  <done>
    All fragment files importable; `test_provenance.py` is now active (no longer skipped) and passes; `IRON_RULES` contains the 7 canonical iron rules; `PAGE_CATEGORIES` contains the category table; `CITATION_RULES` removes slash-command references but preserves the [[wikilink]] / cite-every-claim invariants; `FRONTMATTER_RULES` lists the required-field union for ingestor + scanner.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vendored .md content -> Python string constants | Content copied at port time; no runtime read of `cores/prompt-sources/` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Tampering | Prompt injection via vendored source content | mitigate | Fragments are static Python string constants assigned at import time; not user-controlled; vendored files are read-only versioned content (RESEARCH §Security Domain) |
| T-06-06 | Information Disclosure | Stale provenance after re-vendor (Pitfall 4) | accept | Re-vendor is a manual procedure documented in 06-01 SOURCE-COMMIT marker; SHA drift detection happens via git diff at re-vendor time, not at runtime |
</threat_model>

<verification>
- All 4 fragment files import without errors
- `test_provenance.py` passes (3-line header + Source: path resolves under `cores/prompt-sources/`)
- `IRON_RULES` contains the 7 iron rules verbatim from SKILL.md
- No host-specific references (`${CLAUDE_PLUGIN_ROOT}`, `/lattice-wiki:`) remain in `CITATION_RULES` or `FRONTMATTER_RULES`
</verification>

<success_criteria>
- Shared fragment foundation in place for per-role composition in 06-04..06-07
- PORT-06 provenance test gate passes
- Workspace continues to build (`uv sync` succeeds)
</success_criteria>

<output>
Create `.planning/phases/06-prompt-content-port-divergence-eval/06-03-SUMMARY.md` when done.
</output>
