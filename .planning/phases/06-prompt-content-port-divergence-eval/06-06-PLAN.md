---
phase: 06-prompt-content-port-divergence-eval
plan: 06
type: execute
wave: 3
depends_on: [06-03]
files_modified:
  - agents/code-wiki-agent/src/code_wiki_agent/prompts/linter.py
  - agents/code-wiki-agent/src/code_wiki_agent/commands/lint.py
autonomous: true
requirements: [PORT-04]
tags: [prompt-port, linter, lint-command, three-group-fanout]

must_haves:
  truths:
    - "prompts/linter.py exports exactly three constants: LINTER_PAGE_QUALITY_SYSTEM, LINTER_ADR_CHAIN_SYSTEM, LINTER_STALE_CLAIMS_SYSTEM"
    - "Each of the three composed prompts references IRON_RULES via the shared fragment"
    - "Each group prompt encodes the canonical prioritization rule: code drift > contradictions > broken links > orphans > stale > style"
    - "LINTER_PAGE_QUALITY_SYSTEM covers all canonical semantic check categories from linter.md (9 categories, expanding the current 5-bullet version per RESEARCH)"
    - "Each prompt forbids silent fixes (report-only output)"
    - "commands/lint.py imports the three constants from prompts.linter instead of defining them inline"
  artifacts:
    - path: "agents/code-wiki-agent/src/code_wiki_agent/prompts/linter.py"
      provides: "LINTER_PAGE_QUALITY_SYSTEM, LINTER_ADR_CHAIN_SYSTEM, LINTER_STALE_CLAIMS_SYSTEM"
      contains: "LINTER_PAGE_QUALITY_SYSTEM"
    - path: "agents/code-wiki-agent/src/code_wiki_agent/commands/lint.py"
      provides: "Lint command with import-based prompt sourcing for all 3 group prompts"
  key_links:
    - from: "prompts/linter.py"
      to: "_fragments/iron_rules"
      via: "Python import"
      pattern: "from code_wiki_agent.prompts._fragments.iron_rules import IRON_RULES"
    - from: "commands/lint.py"
      to: "prompts.linter"
      via: "import replacing the 3 inline constants"
      pattern: "from code_wiki_agent.prompts.linter import"
---

<objective>
Port the linter prompt content from `cores/prompt-sources/agents/linter.md` into `prompts/linter.py`, preserving the existing Phase-5 3-group fan-out structure (page_quality, adr_chain, stale_claims). Each group prompt composes from IRON_RULES + local rules and incorporates the canonical 9 semantic check categories from linter.md (expanding the current paraphrased 5-bullet version per RESEARCH).

Purpose: Completes PORT-04. The 3-group split is a Phase-5 architectural decision that the port must preserve — call sites in `commands/lint.py` line ~454 reference the constants by exact name. Content port expands coverage from paraphrased subset to canonical full set.

Output: One new `prompts/linter.py` with 3 exports + refactored `commands/lint.py` importing them. Snapshot tests for all 3 constants recorded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-prompt-content-port-divergence-eval/06-CONTEXT.md
@.planning/phases/06-prompt-content-port-divergence-eval/06-RESEARCH.md
@.planning/phases/06-prompt-content-port-divergence-eval/06-PATTERNS.md
@cores/prompt-sources/agents/linter.md
@agents/code-wiki-agent/src/code_wiki_agent/commands/lint.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create prompts/linter.py with 3 composed group prompts</name>
  <files>
    agents/code-wiki-agent/src/code_wiki_agent/prompts/linter.py
  </files>
  <read_first>
    - cores/prompt-sources/agents/linter.md (full file — 3-pass structure, rules, red flags)
    - agents/code-wiki-agent/src/code_wiki_agent/commands/lint.py (lines 69-106 — existing 3 LINTER_*_SYSTEM constants; line ~454 — call site that consumes them by name)
    - agents/code-wiki-agent/src/code_wiki_agent/prompts/_fragments/iron_rules.py
    - .planning/phases/06-prompt-content-port-divergence-eval/06-PATTERNS.md (section: prompts/linter.py)
    - .planning/phases/06-prompt-content-port-divergence-eval/06-RESEARCH.md (sections: Source File Content Analysis -> Linter-local, Adaptation Map)
  </read_first>
  <behavior>
    - Module exports exactly three constants: `LINTER_PAGE_QUALITY_SYSTEM`, `LINTER_ADR_CHAIN_SYSTEM`, `LINTER_STALE_CLAIMS_SYSTEM`
    - Each constant is built at import time via `"\n\n".join([...])` (per D-02)
    - Each constant contains IRON_RULES content (substring "Iron rules")
    - Each constant contains the prioritization rule: "code drift", "contradictions", "broken links", "orphans", "stale", "style" appearing in that conceptual order in a single line/paragraph (RESEARCH: code drift > contradictions > broken links > orphans > stale > style)
    - Each constant forbids silent writes: contains "report" + "not" + "edit" or "do not write" or similar (LNT-003 check in 06-08 will validate this with substring search)
    - LINTER_PAGE_QUALITY_SYSTEM enumerates at minimum the 9 canonical semantic check categories from linter.md (expanding the current 5-bullet version)
    - LINTER_ADR_CHAIN_SYSTEM specifically addresses ADR-chain health (broken supersedes links, orphan ADRs)
    - LINTER_STALE_CLAIMS_SYSTEM specifically addresses claims that contradict current code (the "code drift" priority category)
    - No constant contains `${CLAUDE_PLUGIN_ROOT}`, `/lattice-wiki:`, `lint_wiki.py`, `graph_analyzer.py` (all per RESEARCH Adaptation Map)
    - 3 snapshot tests in 06-02 scaffold (`test_linter_page_quality_system_snapshot`, `test_linter_adr_chain_system_snapshot`, `test_linter_stale_claims_system_snapshot`) record on first run
  </behavior>
  <action>
    Create `prompts/linter.py`: `from __future__ import annotations` line 1. Imports:
      `from code_wiki_agent.prompts._fragments.iron_rules import IRON_RULES`

    Per RESEARCH, linter does NOT use PAGE_CATEGORIES or CITATION_RULES (linter checks structure, not citation style). Define an optional local constant `LINT_PRIORITY_ORDER` near the top of the file (linter-only, not in shared fragments per PATTERNS.md). Content: a short string stating the prioritization rule "Prioritize findings: code drift > contradictions > broken links > orphans > stale > style." This is reused across all three group prompts.

    Build three `LINTER_*_SYSTEM = "\n\n".join([...])` constants:

    `LINTER_PAGE_QUALITY_SYSTEM`:
      1. Role intro: "You are a code wiki quality linter..." (adapted from existing constant; preserve "Report one finding per line in plain text").
      2. IRON_RULES.
      3. LINT_PRIORITY_ORDER.
      4. Semantic check categories list (role-local, adapted from linter.md Pass 2 / Rules — RESEARCH calls out 9 categories where current prompt has 5). Categories to include: vague/placeholder summaries, missing required frontmatter, inconsistent terminology across pages, missing wikilinks where expected, orphaned pages, dead wikilinks, stale claims contradicting code, missing ADRs for major decisions, missing index entries.
      5. Output format reminder + "Do not include write operations — report only" (LNT-003 invariant).

    `LINTER_ADR_CHAIN_SYSTEM`:
      1. Role intro: "You are a code wiki ADR (Architecture Decision Record) chain linter..."
      2. IRON_RULES.
      3. LINT_PRIORITY_ORDER.
      4. ADR-chain-specific rules (role-local, adapted from linter.md Pass 3 ADR chain): check `supersedes` references resolve, check superseded ADRs are marked superseded, flag ADR chains that loop, flag orphan ADRs.
      5. Output format + report-only invariant.

    `LINTER_STALE_CLAIMS_SYSTEM`:
      1. Role intro: "You are a code wiki stale-claims linter..."
      2. IRON_RULES.
      3. LINT_PRIORITY_ORDER.
      4. Stale-claim-specific rules (role-local, adapted from linter.md Pass 2 stale claims): claims about packages/APIs that no longer exist in code, version references that are out of date, citations to removed source files, factual statements contradicted by current source.
      5. Output format + report-only invariant.

    Adapt away (per RESEARCH Adaptation Map): all `${CLAUDE_PLUGIN_ROOT}/.../{lint_wiki,graph_analyzer}.py` references, `/lattice-wiki:scan`, `/lattice-wiki:ingest` slash commands (replace with "re-run `wiki_scan`" / "re-ingest via `wiki_ingest`" if needed conceptually, or omit).

    Token budget: each of the 3 prompts targets <300 tokens per RESEARCH §Pitfall 1 (3 groups × ~300 = ~900 tokens total at fan-out, vs ~115 for the existing SCANNER_SYSTEM single-prompt calibration).

    After writing, run all 3 linter snapshot tests with `--snapshot-update -x` once to record initial snapshots.
  </action>
  <verify>
    <automated>uv run python -c "from code_wiki_agent.prompts.linter import LINTER_PAGE_QUALITY_SYSTEM, LINTER_ADR_CHAIN_SYSTEM, LINTER_STALE_CLAIMS_SYSTEM; prompts=[LINTER_PAGE_QUALITY_SYSTEM,LINTER_ADR_CHAIN_SYSTEM,LINTER_STALE_CLAIMS_SYSTEM]; [assert_=p for p in prompts if 'Iron rules' in p]; assert all('Iron rules' in p for p in prompts), 'Iron rules fragment missing'; assert all('code drift' in p.lower() for p in prompts), 'priority order missing'; assert 'ADR' in LINTER_ADR_CHAIN_SYSTEM; assert all('CLAUDE_PLUGIN_ROOT' not in p for p in prompts); assert all('/lattice-wiki:' not in p for p in prompts); print('OK lens=', [len(p) for p in prompts])" && uv run pytest agents/code-wiki-agent/tests/prompts/test_prompt_snapshots.py -x -q -k 'linter'</automated>
  </verify>
  <acceptance_criteria>
    - `prompts/linter.py` importable; exports exactly the 3 required constants by the exact names `LINTER_PAGE_QUALITY_SYSTEM`, `LINTER_ADR_CHAIN_SYSTEM`, `LINTER_STALE_CLAIMS_SYSTEM` (call sites depend on the names).
    - Each constant is built via `"\n\n".join([...])` at import time.
    - All three contain the substring `Iron rules` (proves IRON_RULES fragment was included).
    - All three contain `code drift` (proves prioritization rule present).
    - `LINTER_ADR_CHAIN_SYSTEM` contains `ADR`.
    - No constant contains `CLAUDE_PLUGIN_ROOT` or `/lattice-wiki:`.
    - All three corresponding snapshot tests pass after one-time `--snapshot-update`.
    - Each constant <3000 chars (token budget proxy for ~300 tokens × 3 groups).
  </acceptance_criteria>
  <done>
    `prompts/linter.py` exports 3 group prompts; snapshots recorded; IRON_RULES + LINT_PRIORITY_ORDER appear in each; 9 semantic categories expanded in page-quality prompt; adaptation map honored; report-only invariant stated in all 3.
  </done>
</task>

<task type="auto">
  <name>Task 2: Swap inline 3 LINTER_*_SYSTEM constants in commands/lint.py for imports</name>
  <files>
    agents/code-wiki-agent/src/code_wiki_agent/commands/lint.py
  </files>
  <read_first>
    - agents/code-wiki-agent/src/code_wiki_agent/commands/lint.py (full file — note inline definitions at lines ~69-106, call sites at line ~454 that consume the 3 names)
    - agents/code-wiki-agent/src/code_wiki_agent/prompts/linter.py (just created)
    - .planning/phases/06-prompt-content-port-divergence-eval/06-PATTERNS.md (section: commands/lint.py)
  </read_first>
  <action>
    In `commands/lint.py`, delete the three inline `LINTER_*_SYSTEM = """\..."""` blocks (lines ~69-106). Replace with a single grouped import in the imports section:

      `from code_wiki_agent.prompts.linter import (  # noqa: F401`
      `    LINTER_ADR_CHAIN_SYSTEM,`
      `    LINTER_PAGE_QUALITY_SYSTEM,`
      `    LINTER_STALE_CLAIMS_SYSTEM,`
      `)`

    Preserve every other line in `commands/lint.py` — the call site at line ~454 (`("page_quality", LINTER_PAGE_QUALITY_SYSTEM, pages_sample)`, etc.) consumes the constants by name and is unchanged.

    If the module docstring lists these names as public API, update it to note "(re-exported from `code_wiki_agent.prompts.linter`)".

    Run lint-related tests to confirm no regressions.
  </action>
  <verify>
    <automated>uv run pytest agents/code-wiki-agent/tests/ -x -q -k 'lint' 2>&1 | tail -15 && uv run python -c "from code_wiki_agent.commands.lint import LINTER_PAGE_QUALITY_SYSTEM, LINTER_ADR_CHAIN_SYSTEM, LINTER_STALE_CLAIMS_SYSTEM; from code_wiki_agent.prompts.linter import LINTER_PAGE_QUALITY_SYSTEM as PQ, LINTER_ADR_CHAIN_SYSTEM as AC, LINTER_STALE_CLAIMS_SYSTEM as SC; assert LINTER_PAGE_QUALITY_SYSTEM is PQ; assert LINTER_ADR_CHAIN_SYSTEM is AC; assert LINTER_STALE_CLAIMS_SYSTEM is SC; print('OK')" && (! grep -E '^LINTER_(PAGE_QUALITY|ADR_CHAIN|STALE_CLAIMS)_SYSTEM[[:space:]]*=' agents/code-wiki-agent/src/code_wiki_agent/commands/lint.py)</automated>
  </verify>
  <acceptance_criteria>
    - `commands/lint.py` no longer defines the 3 `LINTER_*_SYSTEM` constants inline (grep returns no matches).
    - All 3 are imported from `code_wiki_agent.prompts.linter` and re-exposed from `commands.lint`.
    - Identity check passes: `commands.lint.LINTER_X is prompts.linter.LINTER_X` for all 3.
    - Existing lint-related tests pass.
    - Call site at line ~454 (`("page_quality", LINTER_PAGE_QUALITY_SYSTEM, pages_sample)`, etc.) is unchanged.
  </acceptance_criteria>
  <done>
    Inline constants deleted; grouped import in place; lint tests green; identity check passes for all 3.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Prompt source -> LLM input | Three prompts assembled at import time as immutable strings; consumed by 3 parallel subagent calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-11 | Tampering | Report-only invariant (LNT-003) drift | mitigate | Each composed prompt explicitly states "report only — do not include write operations"; LNT-003 divergence check in 06-08 validates this on agent outputs |
| T-06-12 | Information Disclosure | Group-prompt token budget blow-up at 3x fan-out | mitigate | RESEARCH §Pitfall 1 calibrates each group to <300 tokens; acceptance criteria caps each prompt at <3000 chars |
</threat_model>

<verification>
- 3 linter snapshot tests pass
- `commands/lint.py` no longer defines the 3 constants inline
- Identity check passes for all 3 constants
- Existing lint tests still pass
</verification>

<success_criteria>
- PORT-04 delivered: 3 linter group prompts composed from canonical fragments + adapted role-local content
- 3-group fan-out architecture preserved
- 9-category coverage expansion from current 5-bullet paraphrase
- Report-only invariant stated in all 3 group prompts
- commands/lint.py refactored to import-based prompt sourcing
</success_criteria>

<output>
Create `.planning/phases/06-prompt-content-port-divergence-eval/06-06-SUMMARY.md` when done.
</output>
