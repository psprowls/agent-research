---
phase: 59-decouple-graph-wiki-agent-from-graph-io-cli-migrate-commands
plan: 02
type: execute
wave: 2
depends_on: ["59-01"]
files_modified:
  - agents/graph-wiki-agent/src/graph_wiki_agent/commands/graph.py
  - agents/graph-wiki-agent/src/graph_wiki_agent/graph_tools.py
autonomous: true
requirements: [SC-01, SC-02, SC-04]
must_haves:
  truths:
    - "No module under agents/graph-wiki-agent/ imports graph_io.cli (grep returns nothing)"
    - "commands/graph.py contains no argparse, _build_namespace, _capture_run, or contextlib.redirect_stdout shim"
    - "graph build calls graph_io.update.run and maps its exceptions to the cg exit-code contract"
    - "All 6 describe subcommands + graph query open a read-only conn via a shared helper, call graph_io.queries.*, render via graph_io.render, and preserve the exact exit codes incl. AMBIGUOUS(7) and the --in-package GENERIC quirk"
    - "graph_tools.py imports render from graph_io.render (not graph_io.cli._format)"
    - "Trace schema unchanged: schema_version=1, same event names, same JSONL path, cost fields omitted on proxy commands"
  artifacts:
    - path: "agents/graph-wiki-agent/src/graph_wiki_agent/commands/graph.py"
      provides: "Typed-API graph build/describe/query commands + _open_graph_conn helper"
      contains: "from graph_io import"
    - path: "agents/graph-wiki-agent/src/graph_wiki_agent/graph_tools.py"
      provides: "Librarian tools importing render from public module"
      contains: "from graph_io.render import render"
  key_links:
    - from: "agents/graph-wiki-agent/src/graph_wiki_agent/commands/graph.py"
      to: "graph_io.queries / graph_io.update / graph_io.render / graph_io.store"
      via: "typed function calls"
      pattern: "queries\\.|update\\.run|render\\.|read_only_connect"
    - from: "agents/graph-wiki-agent/src/graph_wiki_agent/commands/graph.py"
      to: "graph_io.exit_codes.AMBIGUOUS"
      via: "entry-point disambiguation"
      pattern: "exit_codes\\.AMBIGUOUS"
---

<objective>
Migrate the agent off `graph_io.cli` entirely. Rewrite `commands/graph.py` to call the typed library (`graph_io.queries.*`, `graph_io.update.run`, `graph_io.store.read_only_connect`) plus the public `graph_io.render` formatter from Plan 01, deleting the `_build_namespace`/`_capture_run`/`argparse` shim (D-04..D-07). Reproduce the cg exit-code contract exactly via a shared connect+map helper, including AMBIGUOUS(7) for ambiguous entry points and the `--in-package` no-match GENERIC quirk. Also swap the one `graph_io.cli._format` import in `graph_tools.py` to `graph_io.render`.

Purpose: SC#1 (no `graph_io.cli` import anywhere in the agent) + SC#2 (remove the Namespace/capture shim). Trace schema preserved exactly (D-07).
Output: rewritten `commands/graph.py`, one-line import fix in `graph_tools.py`. (Tests are rebuilt in Plan 03.)

AUDIT NOTE: RESEARCH/CONTEXT claimed `graph.py` is the ONLY agent module importing `graph_io.cli`. The grep gate proves a second importer: `graph_tools.py:16 from graph_io.cli._format import render`. SC#1 is package-wide, so this plan migrates BOTH. The fix is trivial because Plan 01 already promoted `render` to the public module.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-decouple-graph-wiki-agent-from-graph-io-cli-migrate-commands/59-CONTEXT.md
@.planning/phases/59-decouple-graph-wiki-agent-from-graph-io-cli-migrate-commands/59-RESEARCH.md
@.planning/phases/59-decouple-graph-wiki-agent-from-graph-io-cli-migrate-commands/59-PATTERNS.md
@.planning/phases/59-decouple-graph-wiki-agent-from-graph-io-cli-migrate-commands/59-01-SUMMARY.md

<interfaces>
<!-- Typed API the migrated graph.py must call. Source: 59-RESEARCH.md RQ1-RQ4, 59-PATTERNS.md. -->

graph_io.queries (open sqlite3.Connection first arg):
  describe_package(conn, *, name) -> PackageDescription | None
  describe_path(conn, *, path) -> PathDescription | None
  describe_repository(conn) -> RepoDescription | None
  describe_domain(conn, *, name) -> DomainDescription | None        # packages/subdomains NOT included
  describe_entry_point(conn, *, package_name, entry_name) -> EntryPointDescription | None   # NO name= kwarg
  describe_test_suite(conn, *, suite_name) -> SuiteDescription | None                        # suite_name=, NOT name=
  find(conn, *, name, kind, in_package) -> list[record]

graph_io.update.run(repo_root, *, workspace=None, full=False, lock_timeout_ms=None) -> None  # raises on error
  raises: update.NotInGitRepoError(5), update.UpdateInProgressError(6), store.SchemaMismatchError(4); other -> GENERIC(1)
  does NOT raise GraphNotInitializedError (it creates the DB)

graph_io.store: read_only_connect(db_path), GraphNotInitializedError, SchemaMismatchError
graph_io.exit_codes: SUCCESS=0 GENERIC=1 STALE=2 NOT_INITIALIZED=3 SCHEMA_MISMATCH=4 NOT_IN_GIT_REPO=5 UPDATE_IN_PROGRESS=6 AMBIGUOUS=7
graph_io.render: render(...), format_package/path/repo/domain/entry_point/suite (from Plan 01)
workspace_io.paths.graph_dir(workspace) -> workspace/.graph   (db = graph_dir(ws)/"code.db")

Exit-code contract per command (59-RESEARCH.md RQ3 — reproduce EXACTLY):
  describe <kind>: SUCCESS(0) | not-found GENERIC(1)+stderr | NOT_INITIALIZED(3) | SCHEMA_MISMATCH(4)
  describe entry-point: + AMBIGUOUS(7) when a bare name matches >1 package
  query: SUCCESS(0) incl name/kind zero-result | GENERIC(1) when --in-package given AND zero records | store errors as above
  build: SUCCESS(0) | NOT_IN_GIT_REPO(5) | UPDATE_IN_PROGRESS(6) | SCHEMA_MISMATCH(4) | GENERIC(1)

Preserve verbatim (do NOT touch): _SCHEMA_VERSION=1, _iso_utc_timestamp, _iso_utc_record_timestamp,
_resolve_paths, _trace_path, _write_trace_record (incl. the `if model_id is not None or event.startswith("graph_build")`
condition), the 6 @graph_describe_app.command CLI signatures, graph build's --model stderr note, the
propose-domains registration at the bottom.

Domain extra SQL (keep inline in graph.py; feeds format_domain) — exact queries in 59-PATTERNS.md lines 256-274.
Entry-point disambiguation (copy into graph.py; convert return->raise typer.Exit, sys.stderr->typer.echo err=True) —
exact block in 59-PATTERNS.md lines 414-444.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite commands/graph.py onto the typed API (build + describe + query)</name>
  <files>agents/graph-wiki-agent/src/graph_wiki_agent/commands/graph.py</files>
  <read_first>
    - agents/graph-wiki-agent/src/graph_wiki_agent/commands/graph.py (the full module being rewritten; preserve trace helpers + CLI signatures)
    - agents/graph-wiki-agent/src/graph_wiki_agent/commands/scan.py (lines ~535-558: the read_only_connect + except GraphNotInitializedError pattern to mirror for _open_graph_conn)
    - packages/graph-io/src/graph_io/cli/q_describe_domain.py (the two inline SQL queries to replicate for the domain command)
    - packages/graph-io/src/graph_io/cli/q_describe_entry_point.py (the disambiguation block to replicate, incl. AMBIGUOUS(7))
    - packages/graph-io/src/graph_io/cli/q_find.py (the --in-package GENERIC quirk, lines 67-68)
    - packages/graph-io/src/graph_io/cli/ops_update.py (the exception->exit-code contract for build)
    - .planning/phases/59-decouple-graph-wiki-agent-from-graph-io-cli-migrate-commands/59-PATTERNS.md (lines 319-481: the migration blueprint for graph.py)
  </read_first>
  <action>
    Remove the shim imports (`import argparse`, `import contextlib`, `import io`, the `from graph_io.cli import (...)` block) and the `_build_namespace` + `_capture_run` functions and the `_DESCRIBE_DISPATCH` table. Add `import sqlite3`, `from graph_io import exit_codes, queries, render as _render, update`, `from graph_io.store import GraphNotInitializedError, SchemaMismatchError, read_only_connect`, `from workspace_io.paths import graph_dir`.

    Add a private `_open_graph_conn(workspace: Path) -> sqlite3.Connection` mirroring scan.py: opens `read_only_connect(graph_dir(workspace)/"code.db")`, on `GraphNotInitializedError` does `typer.echo(f"error: {exc}", err=True); raise typer.Exit(code=exit_codes.NOT_INITIALIZED)`, on `SchemaMismatchError` raises `typer.Exit(code=exit_codes.SCHEMA_MISMATCH)`. It does NOT close the conn — callers use try/finally.

    graph build: replace the namespace+capture block with a try/except around `update.run(repo, workspace=workspace_path, full=full)` mapping `update.NotInGitRepoError`->NOT_IN_GIT_REPO, `update.UpdateInProgressError`->UPDATE_IN_PROGRESS, `SchemaMismatchError`->SCHEMA_MISMATCH, `Exception`->GENERIC (each echoes `error: {exc}` to stderr and sets exit_code). Do NOT wrap GraphNotInitializedError (update.run creates the DB). Keep the start/complete trace records, the --model stderr note, and the final `if exit_code != 0: raise typer.Exit(code=exit_code)`. Preserve the "write complete trace regardless of whether work was done" behavior (no-op update still records exit_code=0).

    graph describe: replace `_run_describe`'s dispatch-table body. For package/path/repository/domain/suite: `_resolve_paths`, `conn = _open_graph_conn(ws)`, in a try/finally(conn.close()) call the matching typed query (repository takes no identifier; test_suite uses `suite_name=identifier` NOT name=), then if desc is None echo `error: {kind-noun} not found: {identifier}` to stderr and raise `typer.Exit(exit_codes.GENERIC)`, else `typer.echo(_render.format_<kind>(desc, fmt="human"))`. Preserve the existing per-kind not-found stderr wording from each q_describe_*.py. For domain, run the two inline SQL queries (packages, subdomains) inside the same try block before close and pass them to `_render.format_domain(desc, packages, subdomains, fmt="human")`. For entry_point, copy the disambiguation block verbatim from q_describe_entry_point.py (parse "package:entry" vs bare-name scan), converting `return exit_codes.AMBIGUOUS` to `raise typer.Exit(code=exit_codes.AMBIGUOUS)` and `print(..., file=sys.stderr)` to `typer.echo(..., err=True)`; on single match call `queries.describe_entry_point(conn, package_name=..., entry_name=...)`. Keep the graph_describe trace record (model_id=None, cost omitted) and the kebab `command=` string.

    graph query: keep the Typer-layer no-filter guard (exit 2). Replace the capture block with `conn=_open_graph_conn(ws)`, try/finally call `queries.find(conn, name=name, kind=kind, in_package=in_package)`. Then the D-07 quirk: `if in_package is not None and not records:` write the trace record (exit_code=GENERIC) if tracing then `raise typer.Exit(code=exit_codes.GENERIC)`. Otherwise define the `_notice` stderr callback and `typer.echo(_render.render(records, fmt="human", cap=50, on_truncate=_notice))`, then write the success trace record.

    Use only `graph_io.exit_codes.*` constants — no magic ints. Do NOT import anything from `graph_io.cli`. Do NOT place fenced code in the implementation beyond what the patterns file already specifies; follow 59-PATTERNS.md exactly for output shapes and exit codes.
  </action>
  <verify>
    <automated>grep -rn "graph_io.cli\|argparse\|_build_namespace\|_capture_run\|redirect_stdout" agents/graph-wiki-agent/src/graph_wiki_agent/commands/graph.py | grep -v "^.*#" ; uv run --package graph-wiki-agent python -c "from graph_wiki_agent.commands import graph"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "graph_io\.cli|^import argparse|_build_namespace|_capture_run|redirect_stdout" agents/graph-wiki-agent/src/graph_wiki_agent/commands/graph.py` returns NOTHING (SC#1 + SC#2 for this file)
    - `grep -c "exit_codes.AMBIGUOUS" agents/graph-wiki-agent/src/graph_wiki_agent/commands/graph.py` >= 1 (AMBIGUOUS(7) preserved)
    - module imports cleanly: `uv run --package graph-wiki-agent python -c "from graph_wiki_agent.commands import graph"` exits 0
    - `_SCHEMA_VERSION = 1` still present and unbumped; `_write_trace_record`, `_resolve_paths`, `_trace_path` retained
    - `queries.describe_test_suite(conn, suite_name=...)` and `queries.describe_entry_point(conn, package_name=..., entry_name=...)` are the call forms used (no `name=` kwarg to either)
  </acceptance_criteria>
  <done>graph.py calls only typed graph_io functions + graph_io.render; shim removed; exit-code contract (incl. AMBIGUOUS) and trace schema preserved; module imports.</done>
</task>

<task type="auto">
  <name>Task 2: Swap graph_tools.py off graph_io.cli._format</name>
  <files>agents/graph-wiki-agent/src/graph_wiki_agent/graph_tools.py</files>
  <read_first>
    - agents/graph-wiki-agent/src/graph_wiki_agent/graph_tools.py (line 5 docstring mention + line 16 import to change)
    - .planning/phases/59-decouple-graph-wiki-agent-from-graph-io-cli-migrate-commands/59-01-SUMMARY.md (confirm public module name graph_io.render)
  </read_first>
  <action>
    Change line 16 `from graph_io.cli._format import render` to `from graph_io.render import render`. Update the module docstring line 5 reference from `graph_io.cli._format.render(...)` to `graph_io.render.render(...)` (surgical: only that phrase). No behavior change — `render` is the same callable (Plan 01 made `_format.render` a re-export of it). Do not touch the dispatch table, the 50-row cap, or any tool body.
  </action>
  <verify>
    <automated>grep -rn "graph_io.cli" agents/graph-wiki-agent/src/ || echo "CLEAN"; uv run --package graph-wiki-agent python -c "from graph_wiki_agent.graph_tools import build_graph_tools"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "graph_io.cli" agents/graph-wiki-agent/src/` returns NOTHING / "CLEAN" (SC#1 satisfied package-wide for src)
    - `graph_tools.py` line 16 reads `from graph_io.render import render`
    - `uv run --package graph-wiki-agent python -c "from graph_wiki_agent.graph_tools import build_graph_tools"` exits 0
  </acceptance_criteria>
  <done>graph_tools.py imports render from the public module; no graph_io.cli import remains anywhere in agents/graph-wiki-agent/src/.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argv (--workspace, describe/query identifiers) → graph.py | User-controlled strings reach path resolution and parameterized queries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-03 | Tampering (path) | `--workspace` arg / GRAPH_WIKI_WORKSPACE → graph_dir() | accept | Existing `workspace_io.config.resolve()` normalizes the path before any open(); no change in this phase. Read-only connect only. |
| T-59-04 | Tampering (SQLi) | inline domain/entry-point SQL + queries.find() | mitigate | Domain + entry-point inline SQL use `?` placeholders (copied verbatim from cg, parameterized); queries.find() already parameterized. Reviewer confirms no f-string interpolation of identifiers into SQL. |
| T-59-05 | Information Disclosure | exit codes / error strings in trace + stderr | accept | Exit codes and stderr wording reproduced byte-identical from the existing cg contract (D-05); no new internal detail exposed. Trace omits cost fields (D-07). |
| T-59-SC | Tampering | npm/pip/cargo installs | N/A | No package installs (RESEARCH Package Legitimacy Audit: zero new packages). |
</threat_model>

<verification>
- `grep -rn "graph_io.cli" agents/graph-wiki-agent/src/` → empty (SC#1).
- No argparse/_build_namespace/_capture_run/redirect_stdout in graph.py (SC#2).
- Both modules import cleanly under uv.
- (Behavioral byte-identical + exit-code verification happens in Plan 03's snapshot/exit-code tests.)
</verification>

<success_criteria>
- SC#1: no `graph_io.cli` import under `agents/graph-wiki-agent/src/`.
- SC#2: Namespace/capture shim removed from graph.py.
- D-04/D-05: shared connect+map helper reproduces the exit-code contract incl. AMBIGUOUS(7) and --in-package GENERIC quirk.
- D-06: graph build uses update.run with its own exception map.
- D-07: trace schema unchanged.
</success_criteria>

<output>
Create `.planning/phases/59-decouple-graph-wiki-agent-from-graph-io-cli-migrate-commands/59-02-SUMMARY.md` when done. Record: the `_open_graph_conn` signature/location, how the domain extra SQL and entry-point disambiguation were placed, and confirmation that graph_tools.py was the second (research-missed) cli importer now migrated.
</output>
