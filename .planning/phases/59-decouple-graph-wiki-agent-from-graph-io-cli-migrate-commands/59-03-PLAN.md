---
phase: 59-decouple-graph-wiki-agent-from-graph-io-cli-migrate-commands
plan: 03
type: execute
wave: 3
depends_on: ["59-02"]
files_modified:
  - agents/graph-wiki-agent/tests/conftest.py
  - agents/graph-wiki-agent/tests/unit/test_seeded_graph_workspace_smoke.py
  - agents/graph-wiki-agent/tests/unit/test_commands_graph.py
  - agents/graph-wiki-agent/tests/unit/__snapshots__/test_commands_graph.ambr
autonomous: true
requirements: [SC-03, SC-04]
must_haves:
  truths:
    - "A seeded_graph_workspace session fixture yields the seeded workspace Path (not a conn) without modifying seeded_graph_conn"
    - "Each of the 6 describe subcommands + graph query has a real-DB syrupy snapshot test asserting byte-identical human output and exit_code==0"
    - "Exit-code branches (NOT_INITIALIZED, SCHEMA_MISMATCH, --in-package GENERIC, ambiguous entry-point AMBIGUOUS, no-filter exit 2) are covered (real DB where easy, mock where awkward)"
    - "graph build trace tests use real/monkeypatched update.run (not ops_update) and assert schema_version=1, model_id recording, and cost-field omission on proxy commands"
    - "The full graph-wiki-agent suite passes (SC#4)"
  artifacts:
    - path: "agents/graph-wiki-agent/tests/conftest.py"
      provides: "seeded_graph_workspace session fixture (yields ws Path)"
      contains: "def seeded_graph_workspace"
    - path: "agents/graph-wiki-agent/tests/unit/test_commands_graph.py"
      provides: "Real-DB snapshot + exit-code tests replacing the Namespace-mock tests"
      contains: "seeded_graph_workspace"
  key_links:
    - from: "agents/graph-wiki-agent/tests/unit/test_commands_graph.py"
      to: "GRAPH_WIKI_WORKSPACE env -> _open_graph_conn"
      via: "CliRunner.invoke(env=...)"
      pattern: "GRAPH_WIKI_WORKSPACE"
    - from: "test_commands_graph.py"
      to: "syrupy snapshot"
      via: "assert result.output == snapshot"
      pattern: "== snapshot"
---

<objective>
Rebuild `test_commands_graph.py` per D-08/D-09: delete the tests that mock cg-module dispatch and assert `argparse.Namespace` shape (they test the deleted mechanism), and replace them with real-DB syrupy snapshot tests + real/mock exit-code tests that verify the migrated typed-API commands behave byte-identically (SC#3) and that the full suite is green (SC#4). Add a `seeded_graph_workspace` session fixture that yields the workspace Path so CliRunner can point `GRAPH_WIKI_WORKSPACE` at a real seeded DB.

Purpose: This is the verification gate for the whole phase — it proves the Plan 02 migration preserved behavior. It also fills the VALIDATION.md Wave 0 gaps (new fixture, replacement tests, snapshot files).
Output: new `seeded_graph_workspace` fixture, rewritten test module, generated `.ambr` snapshot file, green full suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-decouple-graph-wiki-agent-from-graph-io-cli-migrate-commands/59-CONTEXT.md
@.planning/phases/59-decouple-graph-wiki-agent-from-graph-io-cli-migrate-commands/59-RESEARCH.md
@.planning/phases/59-decouple-graph-wiki-agent-from-graph-io-cli-migrate-commands/59-PATTERNS.md
@.planning/phases/59-decouple-graph-wiki-agent-from-graph-io-cli-migrate-commands/59-02-SUMMARY.md

<interfaces>
<!-- Test infrastructure. Source: 59-RESEARCH.md RQ5, 59-PATTERNS.md lines 484-571. -->

Existing fixture (DO NOT modify — test_graph_tools.py + test_query_graph_tools_wiring.py depend on it):
  seeded_graph_conn (conftest.py:95) -> yields sqlite3.Connection only

New fixture to add (additive, session-scoped, mirrors seeded_graph_conn but returns ws Path):
  seeded_graph_workspace(tmp_path_factory) -> Path   # ws = resolve_workspace(repo_root).workspace; no teardown
  Full body in 59-PATTERNS.md lines 540-571. Uses module-level _GRAPH_IO_FIXTURE + shutil/subprocess
  already imported in conftest.py.

sample_monorepo contents available for snapshots (59-RESEARCH.md lines 476-483):
  packages: commonlib, jspkg, mypkg, pyutil, webutil
  domains: core (mypkg, pyutil), web (jspkg, webutil), presentation (parent of web)
  entry point: mypkg-run
  test suites: present (Python + JS)

CliRunner snapshot pattern (59-PATTERNS.md lines 503-521):
  result = runner.invoke(app, ["graph","describe","package","mypkg"], env={"GRAPH_WIKI_WORKSPACE": str(ws)})
  assert result.exit_code == 0, result.output
  assert result.output == snapshot

Exit-code mock pattern — exception-raise, NOT int-return (59-PATTERNS.md lines 523-531):
  with patch.object(update, "run", side_effect=update.NotInGitRepoError("...")): ...
  assert result.exit_code == exit_codes.NOT_IN_GIT_REPO

Tests to KEEP verbatim (do not depend on deleted mechanism):
  test_graph_help_lists_exactly_three_subcommands, test_graph_build_help_flags,
  test_graph_describe_help_lists_six_kinds, test_graph_query_no_filters_fails_fast

Tests to REPLACE (mock ops_update / q_describe_* / q_find dispatch + Namespace asserts):
  test_graph_build_dispatches_to_ops_update, test_graph_build_writes_trace,
  test_graph_build_model_recorded_not_invoked, test_graph_describe_dispatch_all_six_kinds,
  test_graph_describe_trace_omits_cost_fields, test_graph_query_dispatch, test_cg_exit_codes_propagate

snapshot file path: agents/graph-wiki-agent/tests/unit/__snapshots__/test_commands_graph.ambr (syrupy auto)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (Wave 0 scaffold): Add seeded_graph_workspace fixture</name>
  <files>agents/graph-wiki-agent/tests/conftest.py, agents/graph-wiki-agent/tests/unit/test_seeded_graph_workspace_smoke.py</files>
  <read_first>
    - agents/graph-wiki-agent/tests/conftest.py (lines 59-128: _resolve_sample_monorepo_fixture, _GRAPH_IO_FIXTURE, seeded_graph_conn — mirror its build steps)
    - .planning/phases/59-decouple-graph-wiki-agent-from-graph-io-cli-migrate-commands/59-PATTERNS.md (lines 535-571: the exact fixture body)
    - packages/graph-io/tests/conftest.py (the seeded_db sibling pattern)
  </read_first>
  <action>
    Add a new `@pytest.fixture(scope="session") def seeded_graph_workspace(tmp_path_factory)` to conftest.py using the body in 59-PATTERNS.md lines 540-571: copytree `_GRAPH_IO_FIXTURE` into a fresh tmp `repo`, `git init/config/add/commit`, `update.run(repo_root, full=True)`, resolve `ws = resolve_workspace(repo_root, require_manifest=False).workspace`, and `return ws` (no yield, no teardown). Reuse the module-level `_GRAPH_IO_FIXTURE`, `shutil`, `subprocess` already present. Skip with the same message if the fixture path is absent. Do NOT modify `seeded_graph_conn` (it must keep yielding a conn — test_graph_tools.py depends on that shape).

    Also add a one-test smoke module `agents/graph-wiki-agent/tests/unit/test_seeded_graph_workspace_smoke.py` that requests the `seeded_graph_workspace` fixture and asserts the returned path exists and that `graph_dir(ws)/"code.db"` is a file (i.e. it actually instantiates and exercises the fixture body, not just its presence). This guarantees a broken fixture body fails in Task 1 rather than confusingly surfacing inside the Task 2 snapshot run. (Task 2 may later fold this assertion into the main test module and delete the smoke file, or leave it — keeping it is fine.)
  </action>
  <verify>
    <automated>cd agents/graph-wiki-agent && uv run --package graph-wiki-agent pytest tests/unit/test_graph_tools.py tests/unit/test_seeded_graph_workspace_smoke.py -x -q && grep -c "def seeded_graph_workspace" tests/conftest.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def seeded_graph_workspace" agents/graph-wiki-agent/tests/conftest.py` == 1
    - `grep -c "def seeded_graph_conn" agents/graph-wiki-agent/tests/conftest.py` == 1 (unchanged, still present)
    - `uv run --package graph-wiki-agent pytest tests/unit/test_graph_tools.py -x` still green (seeded_graph_conn untouched)
    - `tests/unit/test_seeded_graph_workspace_smoke.py` exists and passes — it requests `seeded_graph_workspace` and asserts the seeded `code.db` exists (the fixture body is actually exercised here, not just grepped)
  </acceptance_criteria>
  <done>seeded_graph_workspace session fixture added; seeded_graph_conn untouched; existing graph_tools tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite test_commands_graph.py with real-DB snapshots + exit-code tests, generate snapshots</name>
  <files>agents/graph-wiki-agent/tests/unit/test_commands_graph.py, agents/graph-wiki-agent/tests/unit/__snapshots__/test_commands_graph.ambr</files>
  <read_first>
    - agents/graph-wiki-agent/tests/unit/test_commands_graph.py (the full current file — keep 4 shape tests, replace the 7 mock tests)
    - agents/graph-wiki-agent/tests/unit/test_trace_viewer.py (CliRunner + SnapshotAssertion usage in this package)
    - .planning/phases/59-decouple-graph-wiki-agent-from-graph-io-cli-migrate-commands/59-PATTERNS.md (lines 484-531: snapshot + exit-code test patterns)
    - .planning/phases/59-decouple-graph-wiki-agent-from-graph-io-cli-migrate-commands/59-RESEARCH.md (lines 485-497: which branches use snapshot vs mock)
  </read_first>
  <action>
    Keep the 4 CLI-shape tests verbatim (help/no-filter). Delete the 7 mock-based tests and the `tmp_workspace`-only mock setup where it only served them. Add:
    (a) Six `describe` snapshot tests using `seeded_graph_workspace` + `CliRunner.invoke(app, ["graph","describe",<kind>,<id?>], env={"GRAPH_WIKI_WORKSPACE": str(ws)})`, asserting `result.exit_code == 0` and `result.output == snapshot`. Use real fixture names: package `mypkg`, domain `core`, entry-point `mypkg-run`, repository (no id), a real path from the fixture, a real test-suite name (discover via a `graph query --kind test_suite` invocation or the fixture listing). One snapshot per kind.
    (b) A `graph query` snapshot test (e.g. `--kind package`) asserting exit 0 + `result.output == snapshot`.
    (c) Exit-code tests: `--in-package nonexistent` → exit `exit_codes.GENERIC` (real DB, easy); ambiguous entry-point → AMBIGUOUS(7) via mocking `conn.execute`/`queries` to return >1 package row (mock — hard to provoke from sample_monorepo); NOT_INITIALIZED and SCHEMA_MISMATCH via `patch` on `read_only_connect` raising `GraphNotInitializedError`/`SchemaMismatchError`; keep the existing no-filter → exit 2 test.
    (d) Build/trace tests rewritten against `graph_io.update` (NOT graph_module.ops_update which no longer exists): monkeypatch `update.run` (success no-op) and assert the trace file has `graph_build_start`/`graph_build_complete`, `schema_version==1`, `exit_code==0`; a `--model my-model-id` test asserting the stderr "not invoked in v1.7" note + `model_id` recorded; a build exit-code test patching `update.run` with `side_effect=update.NotInGitRepoError(...)` asserting exit `NOT_IN_GIT_REPO`.
    (e) A describe-trace cost-omission test (real DB `describe package mypkg --trace`) asserting `model_id`/`tokens_in`/`tokens_out`/`cost_usd` absent and required fields present (D-03/D-07).
    Then generate the snapshot baseline: run pytest with `--snapshot-update` once, then a clean run to confirm match. Remove any now-unused imports (MagicMock if no longer used). Do NOT reference `graph_module.ops_update`, `q_describe_*`, or `q_find` — those imports are gone after Plan 02.
  </action>
  <verify>
    <automated>cd agents/graph-wiki-agent && uv run --package graph-wiki-agent pytest tests/unit/test_commands_graph.py --snapshot-update -q && uv run --package graph-wiki-agent pytest tests/unit/test_commands_graph.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "ops_update|q_describe_|q_find|_build_namespace|argparse.Namespace" agents/graph-wiki-agent/tests/unit/test_commands_graph.py` returns NOTHING (old mechanism gone)
    - `grep -c "== snapshot" agents/graph-wiki-agent/tests/unit/test_commands_graph.py` >= 7 (6 describe + query)
    - The 4 CLI-shape tests still present and passing
    - exit-code tests cover GENERIC(--in-package no-match), AMBIGUOUS(7), NOT_INITIALIZED, SCHEMA_MISMATCH, exit-2 no-filter, NOT_IN_GIT_REPO(build)
    - `tests/unit/__snapshots__/test_commands_graph.ambr` exists and `uv run --package graph-wiki-agent pytest tests/unit/test_commands_graph.py -x` is green on a clean (non-update) run
  </acceptance_criteria>
  <done>test_commands_graph.py rewritten: 4 shape tests kept, 7 mock tests replaced with real-DB snapshots + exception-based exit-code tests; snapshot baseline committed; module green.</done>
</task>

<task type="auto">
  <name>Task 3 (phase gate): Run full graph-wiki-agent + graph-io regression suites</name>
  <files>agents/graph-wiki-agent/tests/unit/test_commands_graph.py</files>
  <read_first>
    - .planning/phases/59-decouple-graph-wiki-agent-from-graph-io-cli-migrate-commands/59-RESEARCH.md (lines 700-706: phase-gate sampling rate / commands)
  </read_first>
  <action>
    Run the full `graph-wiki-agent` suite and the graph-io cg-formatter regression suite as the phase gate (SC#4). If any test fails, fix the offending test or production code (within the scope of this phase's changes) until green. Do NOT use snapshot-update to paper over a real behavioral regression — if a describe/query snapshot differs from the pre-migration byte output, that is an SC#3 failure to investigate in graph.py/render.py, not a snapshot to rebase. Note: the known pre-existing failures in `packages/wiki-io/tests/test_overview_template_wikilinks.py` (STATE.md) are out of scope and not part of this gate.
  </action>
  <verify>
    <automated>uv run --package graph-wiki-agent pytest && uv run --package graph-io pytest tests/test_cli_format.py tests/test_cli_describe.py tests/test_cli_describe_entry_point.py tests/test_cli_exit_codes.py tests/test_cli_anti_regression.py -q && (grep -nE "_build_namespace|_capture_run|argparse" agents/graph-wiki-agent/src/graph_wiki_agent/commands/graph.py || echo CLEAN)</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --package graph-wiki-agent pytest` exits 0 (SC#4 full suite green)
    - graph-io cg regression suite (format/describe/entry-point/exit-codes/anti-regression) exits 0 (cg byte-identical guard still green after both plans)
    - Final package-wide grep `grep -rn "graph_io.cli" agents/graph-wiki-agent/` (excluding __pycache__/.ambr) returns NOTHING (SC#1 end-to-end)
    - SC#2 re-check: `grep -nE "_build_namespace|_capture_run|argparse" agents/graph-wiki-agent/src/graph_wiki_agent/commands/graph.py || echo CLEAN` prints `CLEAN` (the argparse.Namespace / stdout-capture shim is gone)
  </acceptance_criteria>
  <done>Both suites green; SC#1 grep clean package-wide; phase verification-ready.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture git/subprocess → temp dir | Test-only; seeds a throwaway repo under tmp_path_factory |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-06 | Tampering | subprocess git calls in fixture | accept | Test-only, fixed argv (no user input), operates on tmp_path_factory dir; mirrors existing seeded_graph_conn fixture already in the tree. |
| T-59-07 | Information Disclosure | snapshot .ambr contents | accept | Snapshots capture sample_monorepo fixture output only — no secrets, no real-vault data. |
| T-59-SC | Tampering | npm/pip/cargo installs | N/A | No package installs (syrupy/pytest already in dev deps). |
</threat_model>

<verification>
- seeded_graph_workspace fixture present; seeded_graph_conn unchanged; test_graph_tools.py green.
- 6 describe + query snapshots assert byte-identical output; exit-code branches covered.
- Full graph-wiki-agent suite green (SC#4); cg regression suite green; package-wide graph_io.cli grep empty (SC#1).
</verification>

<success_criteria>
- D-08: mock/Namespace tests replaced with real-DB snapshot + exit-code tests.
- D-09: seeded_graph_workspace fixture reused via GRAPH_WIKI_WORKSPACE; seeded_graph_conn untouched.
- SC#3: byte-identical behavior verified by snapshots + exit-code assertions.
- SC#4: `uv run --package graph-wiki-agent pytest` passes.
</success_criteria>

<output>
Create `.planning/phases/59-decouple-graph-wiki-agent-from-graph-io-cli-migrate-commands/59-03-SUMMARY.md` when done. Record: which describe branches used snapshot vs mock and why, the real fixture identifiers used (path, test-suite name), and the final full-suite + grep results.
</output>
