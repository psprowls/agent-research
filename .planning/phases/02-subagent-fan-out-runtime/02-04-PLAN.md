---
phase: 02-subagent-fan-out-runtime
plan: 04
type: execute
wave: 4
depends_on: [01, 02, 03]
files_modified:
  - cores/subagent-runtime/src/subagent_runtime/pool.py
  - cores/subagent-runtime/tests/test_pool.py
  - agents/code-wiki-agent/src/code_wiki_agent/cli.py
  - agents/code-wiki-agent/tests/unit/test_trace_viewer.py
  - cores/model-adapter/src/model_adapter/__init__.py
autonomous: true
gap_closure: true
requirements: [SUB-04, OBS-01, OBS-02, BED-02]
tags: [gap-closure, recursion-limit, trace-uniqueness, cli-robustness]

must_haves:
  truths:
    - "A subagent that requires 30 sequential tool calls completes without GraphRecursionError; every subagent invocation site passes an explicit recursion_limit in config that REACHES the task callable (not just constructed and dropped)"
    - "Two run_all() calls within the same wall-clock second produce two distinct trace files (no filename collision, no merged records)"
    - "Rendering a JSONL trace file containing a malformed line (truncated write, binary noise) emits a stderr warning naming the offending line number and continues to the next line; exit code remains 0 if at least one valid record is rendered"
    - "from model_adapter import load_role_config succeeds (public API export)"
  artifacts:
    - path: "cores/subagent-runtime/src/subagent_runtime/pool.py"
      provides: "SubagentPool with config delivered to task callable and unique trace filenames"
      contains: "uuid"
    - path: "cores/subagent-runtime/tests/test_pool.py"
      provides: "Updated test_12 asserting config reaches the task; test_11 with the asyncio.sleep(1.1) workaround removed"
      contains: "received_configs"
    - path: "agents/code-wiki-agent/src/code_wiki_agent/cli.py"
      provides: "trace command with json.JSONDecodeError handling around json.loads"
      contains: "JSONDecodeError"
    - path: "agents/code-wiki-agent/tests/unit/test_trace_viewer.py"
      provides: "New unit test test_trace_command_skips_malformed_lines covering the malformed-JSONL path"
      contains: "malformed"
    - path: "cores/model-adapter/src/model_adapter/__init__.py"
      provides: "Public API export of load_role_config"
      contains: "load_role_config"
  key_links:
    - from: "cores/subagent-runtime/src/subagent_runtime/pool.py:_run_one"
      to: "task callable"
      via: "RunnableConfig passed as second positional argument when task signature accepts it (inspect.signature dispatch)"
      pattern: "task\\(item, _config\\)"
    - from: "cores/subagent-runtime/src/subagent_runtime/pool.py:run_all"
      to: ".code-wiki/traces/<unix_ts>_<hex>.jsonl"
      via: "uuid.uuid4().hex[:8] suffix appended to int(time.time())"
      pattern: "uuid\\.uuid4"
    - from: "agents/code-wiki-agent/src/code_wiki_agent/cli.py:trace"
      to: "stderr warning"
      via: "try/except json.JSONDecodeError around json.loads, typer.echo(..., err=True) and continue"
      pattern: "json\\.JSONDecodeError"
---

<objective>
Close the three BLOCKER gaps identified in 02-VERIFICATION.md:

1. **CR-01 (SUB-04):** `RunnableConfig(recursion_limit=rlimit)` is constructed in `pool.py:_run_one` but never delivered to the task callable. Make the config reach the task so Phase 3+ subagents wrapping compiled LangGraph graphs do not silently hit the default 25-step recursion cap.
2. **CR-02 (OBS-01):** `f"{int(time.time())}.jsonl"` trace filename has 1-second resolution. Add a UUID8 suffix so simultaneous `run_all()` calls produce distinct files. Remove the 1.1s sleep workaround from `test_separate_trace_files_per_run_all`.
3. **CR-03 (OBS-02):** `json.loads(line)` in `cli.py:trace` has no error handler. Wrap in try/except, emit stderr warning, continue. Add a unit test for the malformed-JSONL path.
4. **WARNING (BED-02):** `load_role_config` is used externally via the non-public `model_adapter.loader` import path. Export it from `model_adapter/__init__.py`.

After this plan, ROADMAP Phase 2 success criterion #2 (SUB-04, "every subagent invocation site passes an explicit recursion_limit in config") is provably met at the unit-test level, the OBS-01 "two run_all calls -> two files" guarantee holds in production (not just with a sleep workaround), and the OBS-02 trace viewer is robust to corrupted JSONL.

Purpose: Restores phase-completion integrity. The 3 blockers were flagged in 02-REVIEW.md (commit ea85bb7) but no fix landed before verification ran.

Output: Surgical edits to 5 files. No new files. No new modules. Each edit traces directly to a named gap.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-subagent-fan-out-runtime/02-VERIFICATION.md
@.planning/phases/02-subagent-fan-out-runtime/02-REVIEW.md
@.planning/phases/02-subagent-fan-out-runtime/02-CONTEXT.md
@.planning/phases/02-subagent-fan-out-runtime/02-RESEARCH.md
@.planning/phases/02-subagent-fan-out-runtime/02-02-SUMMARY.md
@.planning/phases/02-subagent-fan-out-runtime/02-03-SUMMARY.md

@cores/subagent-runtime/src/subagent_runtime/pool.py
@cores/subagent-runtime/tests/test_pool.py
@agents/code-wiki-agent/src/code_wiki_agent/cli.py
@agents/code-wiki-agent/tests/unit/test_trace_viewer.py
@cores/model-adapter/src/model_adapter/__init__.py
@cores/model-adapter/src/model_adapter/loader.py

<interfaces>
<!-- Current state of the production code as of 02-VERIFICATION.md (commit 3161b5e). -->

From cores/subagent-runtime/src/subagent_runtime/pool.py (lines 87-148, current state — has the bugs):
- async run_all(items, task, role, *, model_id, max_concurrency, recursion_limit=None) -> FanOutResult
- Line 90: task: Callable[[Any], Awaitable[Any]]  (single-arg signature)
- Line 114: trace_file = self._trace_dir / f"{int(time.time())}.jsonl"  (BUG CR-02)
- Lines 122-123: _config = RunnableConfig(recursion_limit=rlimit); result = await task(item)  (BUG CR-01: _config dropped)
- imports include: asyncio, json, logging, time, RunnableConfig — needs to add `import uuid` and `import inspect`

From cores/subagent-runtime/tests/test_pool.py (Test 11 line 349, Test 12 lines 370-415):
- Test 11 (test_separate_trace_files_per_run_all): contains `await asyncio.sleep(1.1)` at line 349 — REMOVE after filename is unique
- Test 12 (test_recursion_limit_propagated_to_runnableconfig): monkeypatches RunnableConfig; asserts call_count and call_args.kwargs.recursion_limit — must ALSO assert the config object reached the task

From agents/code-wiki-agent/src/code_wiki_agent/cli.py (lines 86-116, current trace command):
- Line 98: `record = json.loads(line)` — bare call, no try/except (BUG CR-03)
- Loop variable: `raw_line` from file.read_text().splitlines(); stripped to `line`
- All output via typer.echo (err=True for stderr)

From cores/model-adapter/src/model_adapter/__init__.py (lines 10-13):
- Currently exports: BedrockAccessDenied, make_llm
- Does NOT export: load_role_config (BUG WARNING — must add)
- loader.py already exports load_role_config at module level

JSONL trace record schema (unchanged, Plan 02):
- role, model_id, prompt_hash (null), item_id, status, latency_ms, tokens_in, tokens_out, cost_usd (null), timestamp, error (only on error records)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix pool.py — deliver RunnableConfig to task + unique trace filenames</name>
  <files>
    cores/subagent-runtime/src/subagent_runtime/pool.py,
    cores/subagent-runtime/tests/test_pool.py
  </files>
  <read_first>
    cores/subagent-runtime/src/subagent_runtime/pool.py,
    cores/subagent-runtime/tests/test_pool.py,
    cores/subagent-runtime/tests/conftest.py,
    .planning/phases/02-subagent-fan-out-runtime/02-VERIFICATION.md,
    .planning/phases/02-subagent-fan-out-runtime/02-REVIEW.md,
    .planning/phases/02-subagent-fan-out-runtime/02-AI-SPEC.md
  </read_first>
  <behavior>
    - **CR-01 fix verification (test update):** `test_recursion_limit_propagated_to_runnableconfig` (test_12) must, in addition to asserting that `RunnableConfig` was called with the right `recursion_limit`, also assert that the constructed config object was DELIVERED to the task callable. Concrete check: the task closure records the received config in a list; after `run_all`, assert the recorded config equals the object that `RunnableConfig` returned. For Part A (recursion_limit=42) assert each task call received a config dict with `recursion_limit == 42`; for Part B (default=99) assert the single task call received `recursion_limit == 99`. The MagicMock used for `RunnableConfig` already has `side_effect=lambda **kwargs: kwargs` so it returns a plain dict — exploit that to make the assertion concrete.

    - **CR-02 fix verification (test update):** `test_separate_trace_files_per_run_all` (test_11) must pass WITHOUT `await asyncio.sleep(1.1)`. Remove the sleep entirely. The two `run_all` calls happen back-to-back in the same wall-clock second; the test still asserts `len(trace_files) == 2` and `trace_files[0] != trace_files[1]`. This is the regression gate that proves filename uniqueness does not rely on time resolution.

    - **All 12 existing tests in test_pool.py must continue to pass.** No other test bodies change.
  </behavior>
  <action>
    Two surgical edits to `cores/subagent-runtime/src/subagent_runtime/pool.py` and two corresponding test updates in `cores/subagent-runtime/tests/test_pool.py`.

    ### Edit A — pool.py: deliver `_config` to the task callable (CR-01)

    1. Update the `task` parameter type annotation on `run_all` (line 90) from `Callable[[Any], Awaitable[Any]]` to `Callable[..., Awaitable[Any]]` so existing single-arg test callables AND new two-arg callables both type-check. Rationale: keeps Plan 02 unit tests and Plan 03 integration tests (single-arg) working while permitting callers to declare `(item, config)`.

    2. Add `import inspect` to the imports section at the top of pool.py (alphabetical placement: after `import asyncio` and before `import json`).

    3. In `_run_one` (currently lines 116-134), change the task invocation from `result = await task(item)` to deliver the config via signature introspection. Replace this single line:

       `result = await task(item)`

       With this 6-line block (placed immediately after the existing `_config = RunnableConfig(recursion_limit=rlimit)` line, replacing the bare `await task(item)` call):

       Pseudocode for the executor:
       - Compute `sig = inspect.signature(task)` once per item dispatch.
       - If `len(sig.parameters) >= 2`: `result = await task(item, _config)` — config is delivered (Phase 3+ subagents wrapping LangGraph graphs declare `(item, config)`).
       - Else: `result = await task(item)` — single-arg fallback preserves all existing test fixtures and Plan 03 integration test closures.

       Document this choice with a 3-line code comment immediately above the `inspect.signature(task)` block: cite ROADMAP SC#2 and note that the introspection is the SUB-04 delivery mechanism. Example comment:

       `# SUB-04 / ROADMAP SC#2: deliver the RunnableConfig (carrying recursion_limit)`
       `# to any task that declares (item, config). Single-arg tasks remain supported`
       `# for backward compatibility with current unit tests and Plan 03 closures.`

       Why this approach (vs. always passing config, or `task.with_config(...).ainvoke(...)`):
       - Existing test fixtures (`make_task`, integration tests, Plan 03 closures) all use single-arg `async def task(item):`. Forcing every caller to a two-arg signature would break all 12 unit tests and all 3 integration tests with no upside in this phase.
       - `task.with_config(_config).ainvoke(item)` requires the task to BE a `Runnable`. Our tasks are plain async functions, not Runnables — this would raise `AttributeError` for every existing caller.
       - The introspection approach is fully backward-compatible AND delivers the config when the caller opts in. Phase 3+ subagent dispatchers that wrap LangGraph-compiled graphs will declare `(item, config)` and the config will reach them.

    4. **Do NOT** remove the `_config` construction even though most current tests do not use it — the construction is what `test_12` monkeypatches. Keep `_config = RunnableConfig(recursion_limit=rlimit)` on its own line.

    ### Edit B — pool.py: unique trace filename (CR-02)

    1. Add `import uuid` to the imports section (alphabetical placement: after `import time`, before the langchain import).

    2. Change line 114 from:
       `trace_file = self._trace_dir / f"{int(time.time())}.jsonl"`
       to:
       `trace_file = self._trace_dir / f"{int(time.time())}_{uuid.uuid4().hex[:8]}.jsonl"`

       Format choice rationale (document in the existing block comment above line 114 — extend the existing comment, don't add a new one): unix timestamp prefix preserves chronological ordering when listing trace files; 8-hex-char UUID suffix gives ~4 billion combinations per second of collision resistance, sufficient for any realistic fan-out workload. Full UUID would be longer with no practical benefit.

    ### Edit C — test_pool.py: update test_12 to assert config delivery

    Modify `test_recursion_limit_propagated_to_runnableconfig` (currently lines 370-415):

    - Replace the inline `simple_task` definition with a version that records received configs. Introduce a list `received_configs_a` (and a parallel `received_configs_b` for Part B) and define `simple_task_a` / `simple_task_b` as `async def simple_task_x(*args):` so the function accepts both single-arg and two-arg invocations. Inside the body: if `len(args) >= 2`, append `args[1]` to the corresponding list; then return the existing MagicMock with usage_metadata.

    - After Part A's `run_all`, ADD assertions (in addition to keeping the existing call_count/call_args assertions):
      - `assert len(received_configs_a) == 2` with message naming actual length
      - For each entry: assert it is a `dict` (because the mocked `RunnableConfig` has `side_effect=lambda **kwargs: kwargs`)
      - For each entry: assert `cfg.get("recursion_limit") == 42`

    - After Part B's `run_all`, ADD assertions:
      - `assert len(received_configs_b) == 1`
      - `assert received_configs_b[0].get("recursion_limit") == 99`

    - Keep all existing assertions (`mock_rc_a.call_count == 2`, `c.kwargs.get("recursion_limit") == 42`, etc.) — these prove construction; the new assertions prove delivery.

    ### Edit D — test_pool.py: remove sleep workaround from test_11

    In `test_separate_trace_files_per_run_all` (currently lines 332-362):
    - Remove the `import time` line inside the test body if present (the test does not otherwise use `time` after the sleep is gone)
    - Remove these two lines (current lines 348-349):
      - `# Sleep slightly more than 1 second to guarantee distinct unix timestamps`
      - `await asyncio.sleep(1.1)`
    - Replace them with the comment: `# UUID suffix on trace filename guarantees uniqueness even within the same wall-clock second (CR-02 fix)`
    - All other test logic unchanged — the assertions (`len(trace_files) == 2` and `trace_files[0] != trace_files[1]`) remain.

    ### Constraints

    - **Surgical:** Touch only the lines listed above. Do NOT reformat, do NOT rename, do NOT reorder unrelated imports, do NOT alter other tests.
    - **Backward compatibility:** All 12 existing unit tests must still pass — the inspect-based dispatch makes single-arg tasks work unchanged.
    - All new output uses standard Python error logging or stays inside the pool (no print/echo).
  </action>
  <verify>
    <automated>cd /Users/pat/Personal/deep-agents &amp;&amp; uv run --package subagent-runtime pytest cores/subagent-runtime/tests/test_pool.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^import uuid" cores/subagent-runtime/src/subagent_runtime/pool.py` returns 1
    - `grep -c "^import inspect" cores/subagent-runtime/src/subagent_runtime/pool.py` returns 1
    - `grep -E "uuid\.uuid4\(\)\.hex\[:8\]" cores/subagent-runtime/src/subagent_runtime/pool.py` matches at least one line (the trace_file path construction)
    - `grep -E "inspect\.signature\(task\)" cores/subagent-runtime/src/subagent_runtime/pool.py` matches at least one line (the dispatch helper)
    - `grep -E "await task\(item, _config\)" cores/subagent-runtime/src/subagent_runtime/pool.py` matches at least one line (the two-arg branch)
    - `grep -c "asyncio.sleep(1.1)" cores/subagent-runtime/tests/test_pool.py` returns 0 (workaround removed)
    - `grep -c "received_configs" cores/subagent-runtime/tests/test_pool.py` returns at least 4 (delivery assertions added: 2 list vars + 2 task fns minimum)
    - `uv run --package subagent-runtime pytest cores/subagent-runtime/tests/test_pool.py -x -q` exits 0 with 12 passed
    - `uv run --package subagent-runtime pytest cores/subagent-runtime/tests/test_pool.py::test_separate_trace_files_per_run_all -x -q` completes in under 1 second (no sleep workaround)
    - `uv run --package subagent-runtime pytest cores/subagent-runtime/tests/test_pool.py::test_recursion_limit_propagated_to_runnableconfig -x -q` exits 0 (delivery assertions pass)
    - `git diff cores/subagent-runtime/src/subagent_runtime/pool.py` shows additive changes only to the imports block, the `_run_one` task dispatch site, and the trace_file path; the run_all signature change is the annotation widening from `Callable[[Any], Awaitable[Any]]` to `Callable[..., Awaitable[Any]]`
  </acceptance_criteria>
  <done>
    `_config` (the `RunnableConfig` with the resolved recursion_limit) is delivered to any task callable that declares `(item, config)`, satisfying ROADMAP SC#2 / SUB-04. Trace filenames carry a UUID8 suffix so simultaneous `run_all()` calls produce distinct files, satisfying OBS-01 T-P2-5 without the 1.1s sleep workaround. All 12 pool unit tests pass; the regression gates (test_11 without sleep, test_12 with delivery assertion) hold.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix cli.py — json.JSONDecodeError handling in trace viewer + malformed-line unit test</name>
  <files>
    agents/code-wiki-agent/src/code_wiki_agent/cli.py,
    agents/code-wiki-agent/tests/unit/test_trace_viewer.py
  </files>
  <read_first>
    agents/code-wiki-agent/src/code_wiki_agent/cli.py,
    agents/code-wiki-agent/tests/unit/test_trace_viewer.py,
    .planning/phases/02-subagent-fan-out-runtime/02-VERIFICATION.md,
    .planning/phases/02-subagent-fan-out-runtime/02-REVIEW.md
  </read_first>
  <behavior>
    - **CR-03 fix:** A trace file containing one malformed JSONL line (e.g. `not a valid {json`) between two valid lines must render the two valid lines, emit a single stderr warning naming the offending line number, and exit 0 (not crash with a traceback).
    - **Edge case:** A trace file containing ONLY malformed lines must exit 0 with stderr warnings for each line and an empty Summary block (total_records=0, total_tokens=0). The "operator can render a trace file" guarantee from OBS-02 holds even on a fully corrupted file.
    - **New unit test (`test_trace_command_skips_malformed_lines`):** Write a 3-line JSONL file: valid record, malformed line, valid record. Invoke `code-wiki-agent trace <file>`. Assert exit code 0. Assert stdout contains BOTH valid item_ids (the malformed line did not abort rendering). Assert stderr contains a warning mentioning the malformed line (e.g. "line 2" or "malformed").
    - All 4 existing trace-viewer unit tests must continue to pass unchanged.
  </behavior>
  <action>
    Two surgical edits.

    ### Edit A — cli.py: wrap json.loads in try/except json.JSONDecodeError

    In `agents/code-wiki-agent/src/code_wiki_agent/cli.py`, modify the `trace` function body (currently lines 86-116). Specifically the loop that begins at line 94.

    Current loop (lines 93-100 of cli.py):
    - line 93: `records: list[dict] = []`
    - line 94: `for raw_line in file.read_text().splitlines():`
    - line 95: `    line = raw_line.strip()`
    - line 96: `    if not line:`
    - line 97: `        continue`
    - line 98: `    record = json.loads(line)`
    - line 99: `    records.append(record)`
    - line 100: `    typer.echo(_render_trace_record(record))`

    Required final loop:
    - `records: list[dict] = []`
    - `for line_number, raw_line in enumerate(file.read_text().splitlines(), start=1):`
    -     `line = raw_line.strip()`
    -     `if not line:`
    -         `continue`
    -     `try:`
    -         `record = json.loads(line)`
    -     `except json.JSONDecodeError as exc:`
    -         `typer.echo(f"warning: skipping malformed JSONL line {line_number}: {exc.msg}", err=True)`
    -         `continue`
    -     `records.append(record)`
    -     `typer.echo(_render_trace_record(record))`

    Key points:
    - `enumerate(..., start=1)` so the warning names a human-friendly 1-based line number that matches what an operator sees in their editor.
    - Use `exc.msg` (the human-readable decode error message) not `str(exc)` (which appends the line/col offsets within the bad line itself — already implied by our outer line number).
    - `continue` skips just the bad line; the Summary block still runs after the loop with whatever valid records were parsed.
    - The existing `if not file.exists():` check (lines 89-91) is unchanged — it produces a different stderr message and exit code 1.
    - Do NOT change exit behavior: exit code stays 0 if the file was readable, even if some lines were malformed. This is per VERIFICATION.md guidance "emit a warning to stderr (typer.echo(err=True)) and continue to the next line".

    Touch no other lines in cli.py. The `version` command, the `_render_trace_record` / `_aggregate_trace` helpers, and the Summary block code are unchanged.

    ### Edit B — test_trace_viewer.py: add malformed-line test

    Append a new test function to `agents/code-wiki-agent/tests/unit/test_trace_viewer.py` AFTER `test_render_trace_record_pure_function` (i.e. as test #5; do not alter the first 4 tests).

    New test `test_trace_command_skips_malformed_lines(tmp_path: Path) -> None`:

    Body:
    - Build `trace_file = tmp_path / "trace_with_bad_lines.jsonl"`.
    - Define two valid records as Python dicts using the same schema as `_write_trace_fixture` (role="scanner", model_id="us.anthropic.claude-haiku-4-5-20251001-v1:0", prompt_hash=None, cost_usd=None, plus item_id="page-good-a" / "page-good-b", distinct latency_ms (100 / 200) and tokens, timestamps "2026-05-13T10:00:00Z" / "2026-05-13T10:00:01Z").
    - Write three lines: `json.dumps(valid_a) + "\n"`, then the literal string `"not a valid {json line\n"` (the malformed middle line), then `json.dumps(valid_b) + "\n"`.
    - Invoke `result = _run_trace_cmd([str(trace_file)])` (existing helper).
    - Assert `result.returncode == 0` with a message naming both stdout and stderr on failure.
    - Assert `"page-good-a" in result.stdout` and `"page-good-b" in result.stdout` — proves both valid records rendered.
    - Compute `stderr_lower = result.stderr.lower()`. Assert that EITHER `"malformed"` OR `"line 2"` appears in `stderr_lower` — proves the stderr warning fires for the malformed middle line.

    The new test reuses the existing module-level `_run_trace_cmd` helper and `_PROJECT_ROOT` constant — no new imports needed beyond what the file already imports (`json`, `subprocess`, `pytest`, `Path`).

    ### Constraints

    - **Surgical:** Only the trace function's loop and the new test function. Do not modify _render_trace_record, _aggregate_trace, the Summary block, the version command, or any of the first 4 tests.
    - **No print() — keep using typer.echo(err=True)** for stderr output. Matches the existing missing-file branch and the "stderr-only logging rule" called out in 02-03-PLAN.
  </action>
  <verify>
    <automated>cd /Users/pat/Personal/deep-agents &amp;&amp; uv run --package code-wiki-agent pytest agents/code-wiki-agent/tests/unit/test_trace_viewer.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "json.JSONDecodeError" agents/code-wiki-agent/src/code_wiki_agent/cli.py` returns 1
    - `grep -c "enumerate(file.read_text().splitlines(), start=1)" agents/code-wiki-agent/src/code_wiki_agent/cli.py` returns 1
    - `grep -c "skipping malformed JSONL line" agents/code-wiki-agent/src/code_wiki_agent/cli.py` returns 1
    - `grep -c "test_trace_command_skips_malformed_lines" agents/code-wiki-agent/tests/unit/test_trace_viewer.py` returns 1
    - `uv run --package code-wiki-agent pytest agents/code-wiki-agent/tests/unit/test_trace_viewer.py -x -q` exits 0 with 5 passed (4 existing + 1 new)
    - `uv run --package code-wiki-agent pytest agents/code-wiki-agent/tests/unit/test_trace_viewer.py::test_trace_command_skips_malformed_lines -x -q` exits 0
    - `grep -v '^#' agents/code-wiki-agent/src/code_wiki_agent/cli.py | grep -c "print("` returns 0 (no bare prints; still typer.echo only)
    - `git diff agents/code-wiki-agent/src/code_wiki_agent/cli.py` shows changes only inside the for-loop body of the `trace` function (no edits to version, helpers, or Summary block)
  </acceptance_criteria>
  <done>
    The `trace` CLI command skips malformed JSONL lines with a stderr warning and continues rendering, restoring the OBS-02 "operator can render a trace file" guarantee on partial/corrupted trace files. The new unit test covers the malformed-JSONL path. All 5 trace viewer unit tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Export load_role_config in model_adapter public API</name>
  <files>
    cores/model-adapter/src/model_adapter/__init__.py
  </files>
  <read_first>
    cores/model-adapter/src/model_adapter/__init__.py,
    cores/model-adapter/src/model_adapter/loader.py,
    .planning/phases/02-subagent-fan-out-runtime/02-VERIFICATION.md
  </read_first>
  <behavior>
    - `from model_adapter import load_role_config` succeeds and returns the same callable as `from model_adapter.loader import load_role_config`.
    - `from model_adapter import make_llm, BedrockAccessDenied` continues to work (existing exports preserved).
    - The docstring's "Public API" section lists `load_role_config(role)` alongside `make_llm(role)`.
  </behavior>
  <action>
    Surgical edit to `cores/model-adapter/src/model_adapter/__init__.py` (currently 13 lines).

    Required final state of the file (replace in entirety):

    - Line 1-6 docstring (preserve `"""model_adapter — Bedrock model loader for code-wiki-agent.` opener; extend the Public API listing):
      - `make_llm(role)           → ChatBedrockConverse with actionable IAM errors`
      - `load_role_config(role)   → dict with model_id, region, max_tokens, max_concurrency`
      - `BedrockAccessDenied      → raised when Bedrock rejects with AccessDenied`
    - `from __future__ import annotations` (unchanged)
    - `from model_adapter.exceptions import BedrockAccessDenied` (unchanged)
    - `from model_adapter.loader import load_role_config, make_llm` (changed: add `load_role_config` alphabetically before `make_llm`)
    - `__all__ = ["BedrockAccessDenied", "load_role_config", "make_llm"]` (changed: insert `"load_role_config"` alphabetically)

    Concrete changes:
    1. Docstring: add a `load_role_config(role)` line between `make_llm` and `BedrockAccessDenied`, with arrow describing the return shape (per loader.py's actual return: {model_id, region, max_tokens, max_concurrency}).
    2. Import line: change `from model_adapter.loader import make_llm` to `from model_adapter.loader import load_role_config, make_llm` (alphabetical).
    3. `__all__`: insert `"load_role_config"` between `"BedrockAccessDenied"` and `"make_llm"` (alphabetical).

    No other edits. Do not touch `model_adapter/loader.py` or `model_adapter/exceptions.py`.
  </action>
  <verify>
    <automated>cd /Users/pat/Personal/deep-agents &amp;&amp; uv run --package model-adapter python -c "from model_adapter import load_role_config, make_llm, BedrockAccessDenied; cfg = load_role_config('librarian'); assert cfg['max_tokens']==2048; print('ok')" &amp;&amp; uv run --package model-adapter pytest cores/model-adapter/tests/test_loader.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "load_role_config" cores/model-adapter/src/model_adapter/__init__.py` returns at least 3 (docstring line + import line + __all__ entry)
    - `grep -E "^__all__ = \[\"BedrockAccessDenied\", \"load_role_config\", \"make_llm\"\]$" cores/model-adapter/src/model_adapter/__init__.py` matches exactly
    - `grep -E "^from model_adapter\.loader import load_role_config, make_llm$" cores/model-adapter/src/model_adapter/__init__.py` matches exactly
    - `uv run --package model-adapter python -c "from model_adapter import load_role_config; cfg = load_role_config('librarian'); assert cfg['max_tokens']==2048 and cfg['max_concurrency']==5"` exits 0
    - `uv run --package model-adapter python -c "from model_adapter import make_llm, BedrockAccessDenied; print('ok')"` exits 0 (no regression on existing exports)
    - `uv run --package model-adapter pytest cores/model-adapter/tests/test_loader.py -x -q` exits 0 (20 existing tests still green)
  </acceptance_criteria>
  <done>
    `load_role_config` is part of the model_adapter public API. Callers can use `from model_adapter import load_role_config` instead of the previously-required `from model_adapter.loader import load_role_config`. The BED-02 ModelRegistry contract surface is now consistent: every consumer-facing helper is reachable from the package root.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller-supplied `task` callable → SubagentPool._run_one | Plan 02 already documented that callers must not leak secrets via item repr or exception messages. CR-01 fix introduces a NEW boundary: the RunnableConfig dict now flows from the pool into caller-supplied code. A malicious task could log or persist the recursion_limit; this is acceptable because the value is a plain integer with no secret content. |
| Trace file → `cli.py:trace` json.loads | The trace file is operator-controlled (output of our own pool). CR-03 mitigates the existing crash-on-malformed-line failure mode by emitting a stderr warning and continuing — the file is not externally sourced, so we accept JSONDecodeError as a recoverable parse error rather than a security event. |
| Filesystem trace_dir → uuid suffix | uuid.uuid4() draws from `os.urandom` on every supported platform (CPython on Linux, macOS, Windows). No predictability concern. UUID8 collision probability per-second-window: birthday-paradox math puts the 50% collision floor at ~77,000 simultaneous calls per second — far beyond any realistic fan-out workload. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | Information Disclosure | RunnableConfig contents leaked via caller task closure | accept | The only field passed today is `recursion_limit` (int). No credentials, no PII. Phase 3+ may extend the config; revisit this boundary then. |
| T-02-04-02 | Denial of Service | Malformed JSONL file with billions of lines crashes trace viewer via memory exhaustion | accept | `file.read_text().splitlines()` was already the loading strategy in Plan 03 (T-02-03-03 in 02-03-PLAN's threat register). Phase 4 may switch to line-by-line iteration; CR-03 fix does not regress this. |
| T-02-04-03 | Tampering | Operator deliberately writes malformed JSONL hoping to crash subsequent renders | mitigate | The new try/except json.JSONDecodeError block ensures any single bad line emits a stderr warning and is skipped — the render proceeds. Exit code is 0 if the file was readable. |
| T-02-04-04 | Spoofing | Two run_all() calls from different processes share trace_dir; UUID collision causes record merging | accept | UUID8 suffix gives ~4B combinations per second; collision probability at realistic fan-out scale (≤100 concurrent processes) is negligible. Full UUID would be longer with no practical benefit. |
| T-02-04-05 | Elevation of Privilege | Two-arg task signature lets a malicious task introspect or mutate RunnableConfig | accept | Tasks are caller-supplied trusted code; the pool is library code, not a sandbox boundary. RunnableConfig is a plain dict — mutation by the task does not affect the pool's subsequent dispatches because each item gets a freshly-constructed `_config`. |

**Residual risk:** low — all gap fixes are surgical reactions to identified BLOCKERS in 02-VERIFICATION.md; no new attack surface introduced.
</threat_model>

<verification>
After all three tasks complete:

1. `uv run --package subagent-runtime pytest cores/subagent-runtime/tests/test_pool.py -x -q` exits 0 with 12 passed; total runtime < 1 second (proves sleep workaround removed AND no other test regressions).
2. `uv run --package code-wiki-agent pytest agents/code-wiki-agent/tests/unit/test_trace_viewer.py -x -q` exits 0 with 5 passed (4 original + new malformed-line test).
3. `uv run --package model-adapter pytest cores/model-adapter/tests/test_loader.py -x -q` exits 0 with 20 passed (existing tests still green after __init__.py edit).
4. `uv run python -c "from model_adapter import load_role_config; print(load_role_config('scanner')['max_tokens'])"` prints `500` (proves public API export works end-to-end).
5. Re-running the verification batch from 02-VERIFICATION.md "Behavioral Spot-Checks" section — all PASS rows continue to pass; the three FAILED rows (SC2 recursion delivery, T-P2-5 distinct trace files without sleep, T-P3-1 malformed JSONL handling) now PASS.
6. `grep -c "asyncio.sleep(1.1)" cores/subagent-runtime/tests/test_pool.py` returns 0 (workaround eliminated).
7. `grep -c "uuid.uuid4().hex[:8]" cores/subagent-runtime/src/subagent_runtime/pool.py` returns at least 1 (filename uniqueness mechanism in place).
8. `grep -c "await task(item, _config)" cores/subagent-runtime/src/subagent_runtime/pool.py` returns at least 1 (config delivery to two-arg tasks in place).
9. `grep -c "except json.JSONDecodeError" agents/code-wiki-agent/src/code_wiki_agent/cli.py` returns 1 (error handling in trace CLI in place).
</verification>

<success_criteria>
- All three BLOCKER gaps from 02-VERIFICATION.md are closed:
  - CR-01: RunnableConfig delivered to task callable via signature introspection; test asserts delivery (not just construction)
  - CR-02: Trace filenames carry a UUID8 suffix; test passes without asyncio.sleep workaround
  - CR-03: trace CLI tolerates malformed JSONL lines with stderr warning and exit 0
- WARNING closed: `load_role_config` exported from `model_adapter` package root
- All previously-passing tests still pass (12 pool tests + 4 trace viewer tests + 20 loader tests = 36 minimum green)
- One new test added (`test_trace_command_skips_malformed_lines`) covering the CR-03 path
- ROADMAP Phase 2 success criteria #2 (SUB-04 recursion limit propagation), unit-level OBS-01 (distinct trace files per run_all), and OBS-02 (operator can render trace file even if partially corrupted) are provably met
- No new files created. All edits are surgical modifications to 5 existing files.
- `git diff` shows additive-only changes outside the explicitly-named edit sites (no incidental reformatting)
</success_criteria>

<output>
After completion, create `.planning/phases/02-subagent-fan-out-runtime/02-04-SUMMARY.md` documenting:
- Each of the 3 BLOCKER gaps (CR-01, CR-02, CR-03) with: file:line of the fix, the test that now proves the fix, and a one-line "before vs after" diff summary
- The chosen CR-01 dispatch strategy (`inspect.signature(task)` two-arg/one-arg branch) with a note about Phase 3+ adoption (subagents wrapping LangGraph graphs should declare `(item, config)`)
- The WARNING fix (load_role_config export) and confirmation that no callers needed to change (loader.py path still works)
- Re-verification status: which 02-VERIFICATION.md gaps are now closed, and the command sequence to re-run /gsd-verify-work after this plan
- Phase 2 close-out next step: `/gsd-verify-work 02` re-run, then manual Bedrock integration test gate from 02-03-PLAN remains the only human-gated criterion
</output>
