---
phase: 28-schema-v2-uri-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/graph-io/src/graph_io/schema.py
  - packages/graph-io/tests/test_schema.py
autonomous: true
requirements: [SCHEMA-01, SCHEMA-04]
must_haves:
  truths:
    - "schema.SCHEMA_VERSION equals 2"
    - "nodes CREATE TABLE includes a nullable uri TEXT column (D-09: column is nullable; D-10: uri is a dedicated column, not stored in attrs_json)"
    - "idx_nodes_uri index is created by apply_schema and listed in sqlite_master"
    - "schema.apply_schema is idempotent at v2 (second call does not duplicate metadata rows or fail)"
    - "Sentinel test test_schema_version_is_two exists (renamed from _is_one) and is green (D-12 sentinel #1)"
    - "Sentinel test test_nodes_table_has_uri_column exists and is green (D-12 sentinel #2)"
  artifacts:
    - path: "packages/graph-io/src/graph_io/schema.py"
      provides: "Schema v2 DDL with uri column and uri index"
      contains: "SCHEMA_VERSION = 2"
    - path: "packages/graph-io/tests/test_schema.py"
      provides: "Sentinel tests for v2 schema and uri column"
      contains: "def test_schema_version_is_two"
  key_links:
    - from: "schema._DDL_STATEMENTS"
      to: "nodes table"
      via: "CREATE TABLE includes uri TEXT (nullable)"
      pattern: "uri\\s+TEXT"
    - from: "schema._DDL_STATEMENTS"
      to: "idx_nodes_uri"
      via: "CREATE INDEX IF NOT EXISTS"
      pattern: "idx_nodes_uri"
---

<objective>
Bump `graph-io` to schema v2 by adding a nullable `uri TEXT` column to the `nodes` CREATE TABLE statement and a new `idx_nodes_uri` index. Update the version constant and the existing sentinel test. Add a new sentinel test asserting the `uri` column is present on the `nodes` table.

Purpose: This is the wedge that downstream plans (03, 05) drive into. Every emitter built in Phase 29-31 needs the column to exist before it can write to it; every CLI command that opens a non-`--full` connection needs the version-mismatch path to be reachable so it can exit code 4. Both depend on this plan landing first.

Output: Updated `schema.py` (3-line constant bump + 1-line column addition + 1-line index statement), updated/added sentinel tests in `test_schema.py`. No new modules, no new exports.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-schema-v2-uri-foundation/28-CONTEXT.md

<interfaces>
<!-- Current schema.py module surface — extracted to avoid scavenger-hunt. -->

From packages/graph-io/src/graph_io/schema.py:
- Module-level constant: `SCHEMA_VERSION = 1` (line 12) — bump to 2
- Module-level tuple: `_DDL_STATEMENTS = ( ... )` (lines 14-44) — append `uri TEXT` to nodes CREATE TABLE, add CREATE INDEX statement for `idx_nodes_uri`
- Function: `apply_schema(conn: sqlite3.Connection) -> None` (lines 47-59) — no signature change; relies on `_DDL_STATEMENTS` and `SCHEMA_VERSION`

From packages/graph-io/tests/test_schema.py:
- Fixture: `conn` is provided by an upstream conftest (not the tests/conftest.py shown — likely workspace-level pytest fixture; verify locally)
- Helper: `_table_names(conn)`, `_index_names(conn)` (lines 12-23) — reuse for new sentinel
- Existing test: `test_apply_schema_creates_indexes` asserts the full index set — MUST be updated to include `idx_nodes_uri`
- Existing test: `test_schema_version_is_one` (line 57) — rename + change assertion to == 2
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bump SCHEMA_VERSION and add uri column + index to DDL</name>
  <files>packages/graph-io/src/graph_io/schema.py</files>
  <read_first>
    - packages/graph-io/src/graph_io/schema.py (current DDL — preserve all existing statements verbatim except the nodes CREATE TABLE and the version constant)
    - .planning/phases/28-schema-v2-uri-foundation/28-CONTEXT.md (D-09, D-10 — column is nullable; v1.6 deliberately defers UNIQUE constraint)
    - .planning/research/PITFALLS.md (pitfalls 1, 4 — bumping SCHEMA_VERSION without DDL update is the failure mode this plan prevents)
  </read_first>
  <behavior>
    - SCHEMA_VERSION is the integer literal 2
    - nodes table created by apply_schema has columns: id, kind, name, path, line, attrs_json, uri (uri is TEXT, nullable, no default, no UNIQUE constraint)
    - apply_schema creates an index named idx_nodes_uri ON nodes(uri)
    - apply_schema remains idempotent — calling it twice on the same connection neither raises nor produces a second schema_version metadata row
    - All existing indexes (idx_nodes_kind_name, idx_nodes_path, idx_edges_dst_kind, idx_edges_src_kind) and tables (nodes, edges, metadata) still exist after apply_schema
  </behavior>
  <action>
    Change SCHEMA_VERSION from 1 to 2. In _DDL_STATEMENTS, modify the CREATE TABLE nodes statement to add a trailing `uri TEXT` column (after attrs_json) — the column is nullable (no NOT NULL, no DEFAULT, no UNIQUE per D-10 lock and v1.7 deferral). Add a new statement to the tuple: `"CREATE INDEX IF NOT EXISTS idx_nodes_uri ON nodes(uri)"`. Index name matches the existing `idx_nodes_kind_name` / `idx_nodes_path` convention (Claude's Discretion per CONTEXT.md). Do NOT alter the edges table, the metadata table, or the existing indexes. Do NOT add migration code — schema.py docstring already states "Bumping SCHEMA_VERSION forces a full rebuild via `cg update --full`", and the unlink+rebuild path is owned by Plan 05.
  </action>
  <verify>
    <automated>uv run --package graph-io pytest packages/graph-io/tests/test_schema.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "SCHEMA_VERSION = 2" packages/graph-io/src/graph_io/schema.py` returns at least one match
    - `grep -E "uri\s+TEXT" packages/graph-io/src/graph_io/schema.py` returns a match inside the nodes CREATE TABLE block
    - `grep -n "idx_nodes_uri" packages/graph-io/src/graph_io/schema.py` returns a match in a CREATE INDEX statement
    - `grep -v '^#' packages/graph-io/src/graph_io/schema.py | grep -c "UNIQUE"` returns 0 (uri must NOT be UNIQUE per D-lock)
    - `uv run --package graph-io pytest packages/graph-io/tests/test_schema.py::test_apply_schema_is_idempotent -x` exits 0
  </acceptance_criteria>
  <done>
    schema.py has SCHEMA_VERSION = 2, the nodes CREATE TABLE statement includes a nullable `uri TEXT` column, and `_DDL_STATEMENTS` includes a CREATE INDEX IF NOT EXISTS for `idx_nodes_uri`. No other DDL is altered. Existing idempotency test still passes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update + add sentinel tests in test_schema.py</name>
  <files>packages/graph-io/tests/test_schema.py</files>
  <read_first>
    - packages/graph-io/tests/test_schema.py (existing helpers `_table_names` and `_index_names`)
    - packages/graph-io/src/graph_io/schema.py (post-Task-1 state — sentinel must match what Task 1 produced)
    - .planning/phases/28-schema-v2-uri-foundation/28-CONTEXT.md (D-12 names: `test_schema_version_is_two`, `test_nodes_table_has_uri_column`)
  </read_first>
  <behavior>
    - `test_schema_version_is_two` (no fixture needed) asserts `schema.SCHEMA_VERSION == 2`
    - `test_nodes_table_has_uri_column` calls `schema.apply_schema(conn)`, then queries `PRAGMA table_info('nodes')` and asserts a row exists with `name='uri'` and `type='TEXT'` (case-insensitive match on type)
    - `test_apply_schema_creates_indexes` is updated to include `idx_nodes_uri` in the expected index set
    - The old `test_schema_version_is_one` no longer exists (rename, not co-existence)
  </behavior>
  <action>
    Rename the existing `test_schema_version_is_one` function to `test_schema_version_is_two` and change the assertion from `== 1` to `== 2`. Add a new function `test_nodes_table_has_uri_column(conn)` that calls `schema.apply_schema(conn)` then runs `conn.execute("PRAGMA table_info('nodes')").fetchall()` and asserts a row exists with column name `"uri"` and a TEXT-family type (compare case-insensitively against `"TEXT"`). Update `test_apply_schema_creates_indexes` to include `"idx_nodes_uri"` in the expected set literal. Do not change the other existing tests.
  </action>
  <verify>
    <automated>uv run --package graph-io pytest packages/graph-io/tests/test_schema.py::test_schema_version_is_two packages/graph-io/tests/test_schema.py::test_nodes_table_has_uri_column packages/graph-io/tests/test_schema.py::test_apply_schema_creates_indexes -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def test_schema_version_is_two" packages/graph-io/tests/test_schema.py` returns a match
    - `grep -n "def test_schema_version_is_one" packages/graph-io/tests/test_schema.py` returns NO match
    - `grep -n "def test_nodes_table_has_uri_column" packages/graph-io/tests/test_schema.py` returns a match
    - `grep -n "idx_nodes_uri" packages/graph-io/tests/test_schema.py` returns a match inside the expected-index set
    - `uv run --package graph-io pytest packages/graph-io/tests/test_schema.py -x` exits 0 (entire file green)
  </acceptance_criteria>
  <done>
    The renamed sentinel and the new uri-column sentinel both pass. The index sentinel test now expects `idx_nodes_uri`. No existing tests in this file regress.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer→DDL | Schema constants/DDL are author-controlled; no external input crosses this boundary in Plan 01. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01-01 | Tampering | schema._DDL_STATEMENTS (developer error: bump version without adding column) | mitigate | Sentinel `test_nodes_table_has_uri_column` fails at CI time if the column is missing; pitfall 1 in PITFALLS.md is the exact failure mode being guarded. |
| T-28-01-02 | Integrity | UNIQUE constraint accidentally added to uri column | mitigate | Acceptance criterion greps for `UNIQUE` and asserts zero matches; v1.7 explicitly defers UNIQUE per CONTEXT.md deferred list. |
| T-28-01-SC | Tampering | npm/pip/cargo installs | accept | Plan installs zero packages; no Package Legitimacy Gate required. |
</threat_model>

<verification>
- `uv run --package graph-io pytest packages/graph-io/tests/test_schema.py -x` exits 0
- All three sentinel tests (`test_schema_version_is_two`, `test_nodes_table_has_uri_column`, plus the updated `test_apply_schema_creates_indexes`) pass
- Other graph-io tests that depend on schema.py compilation still import the module without error: `uv run --package graph-io python -c "from graph_io import schema; assert schema.SCHEMA_VERSION == 2"` exits 0
</verification>

<success_criteria>
- schema.py exports SCHEMA_VERSION = 2
- nodes CREATE TABLE has nullable `uri TEXT` column (no UNIQUE)
- `idx_nodes_uri` index appears in `_DDL_STATEMENTS`
- Two new/renamed sentinel tests are green
- No regression in existing schema tests
</success_criteria>

<output>
Create `.planning/phases/28-schema-v2-uri-foundation/28-01-SUMMARY.md` when done
</output>
