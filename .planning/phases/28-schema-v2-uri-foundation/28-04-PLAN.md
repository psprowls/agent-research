---
phase: 28-schema-v2-uri-foundation
plan: 04
type: execute
wave: 2
depends_on: ["28-01"]
files_modified:
  - packages/graph-io/src/graph_io/cli/ops_update.py
  - packages/graph-io/tests/test_cli_exit_codes.py
autonomous: true
requirements: [SCHEMA-02]
must_haves:
  truths:
    - "`cg update` (incremental, no --full) against a schema-v1 DB exits with code 4 (SCHEMA_MISMATCH), prints a stderr message that includes the literal substring `cg update --full`, and emits NO Python traceback (D-02: cli wraps every non-`--full` path with `except store.SchemaMismatchError`, prints friendly stderr, calls sys.exit(SCHEMA_MISMATCH=4); no traceback)"
    - "`cg find foo` against a schema-v1 DB still exits 4 (existing wiring in q_find.py — regression guard via test)"
    - "`cg update --full` against a schema-v1 DB does NOT exit 4 from this handler (the --full path is owned by Plan 05's unlink+rebuild flow; it must reach Plan 05's code without being short-circuited by the SchemaMismatchError handler)"
    - "The handler ordering is: `NotInGitRepoError` → `UpdateInProgressError` → `SchemaMismatchError` → generic `Exception`. SchemaMismatchError MUST be caught BEFORE the generic `except Exception` so it gets exit code 4, not 1."
  artifacts:
    - path: "packages/graph-io/src/graph_io/cli/ops_update.py"
      provides: "SchemaMismatchError handler routing to exit_codes.SCHEMA_MISMATCH"
      contains: "store.SchemaMismatchError"
  key_links:
    - from: "ops_update.run()"
      to: "exit_codes.SCHEMA_MISMATCH"
      via: "except store.SchemaMismatchError → print stderr → return SCHEMA_MISMATCH"
      pattern: "store\\.SchemaMismatchError"
---

<objective>
Add a `SchemaMismatchError` handler to `packages/graph-io/src/graph_io/cli/ops_update.py` so that incremental `cg update` (without `--full`) against a schema-v1 database exits with code 4 and prints a user-friendly message directing the user to run `cg update --full`. All other `cg` subcommands that open a read connection (find, status, callers, callees, describe-*, etc.) ALREADY wire `SchemaMismatchError` to exit 4 (verified during planning — see q_find.py:26). The only gap is `cg update`, which currently catches only `NotInGitRepoError` and `UpdateInProgressError` and routes `SchemaMismatchError` through the generic `Exception` handler → exit 1.

Add a regression test in `test_cli_exit_codes.py` that creates a v1 DB and asserts both `cg find` (existing behavior) and `cg update` (newly-wired behavior) exit 4 with the expected stderr message.

Purpose: Satisfies SCHEMA-02 / Success Criterion #2. The handler must land BEFORE Plan 05 because Plan 05's v1→v2 unlink+rebuild path only runs under `--full`. Users who type `cg update` (without `--full`) on a v1 DB need a clean error, not a traceback.

Output: 5-line edit to ops_update.py (one new `except` block) + 1 new test function in test_cli_exit_codes.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-schema-v2-uri-foundation/28-CONTEXT.md
@.planning/phases/28-schema-v2-uri-foundation/28-01-SUMMARY.md

<interfaces>
<!-- Current ops_update.py — exact code to modify -->

From packages/graph-io/src/graph_io/cli/ops_update.py (the entire 28-line file):

```
"""cg update [--full] — refresh the code graph from git."""

from __future__ import annotations

import argparse
import sys

from graph_io import exit_codes, update


def add_arguments(parser: argparse.ArgumentParser) -> None:
    parser.add_argument("--full", action="store_true", help="full rebuild from scratch")


def run(args: argparse.Namespace) -> int:
    try:
        update.run(args.repo, workspace=args.workspace, full=args.full)
    except update.NotInGitRepoError as exc:
        print(f"error: {exc}", file=sys.stderr)
        return exit_codes.NOT_IN_GIT_REPO
    except update.UpdateInProgressError as exc:
        print(f"error: {exc}", file=sys.stderr)
        return exit_codes.UPDATE_IN_PROGRESS
    except Exception as exc:                  # ← currently catches SchemaMismatchError here → exit 1
        print(f"error: {exc}", file=sys.stderr)
        return exit_codes.GENERIC
    return exit_codes.SUCCESS
```

Reference: other subcommands have already-correct wiring — see `packages/graph-io/src/graph_io/cli/q_find.py:26-28` as the canonical pattern:
```
except store.SchemaMismatchError as exc:
    print(f"error: {exc}", file=sys.stderr)
    return exit_codes.SCHEMA_MISMATCH
```

Also note: `update.run` itself doesn't directly raise `SchemaMismatchError` — `store.connect(db_path, create=True)` is called inside `update.run` (update.py:161). When `create=True` is passed, `store.connect` calls `schema.apply_schema(conn)` and DOES NOT raise SchemaMismatchError (the version check only runs on `create=False` or `read_only_connect`). HOWEVER, when Plan 05's v1→v2 detection logic is in place, it WILL surface SchemaMismatchError from a probe connection. For incremental updates (Plan 05 territory: probe BEFORE create-true reopen), the error path needs to be wired NOW. So this plan adds the handler defensively; Plan 05 will raise from update.run via store.SchemaMismatchError (the existing exception class).

Test file context: `packages/graph-io/tests/test_cli_exit_codes.py` already exists with patterns for invoking `main(argv)` and asserting exit codes + stderr. Use it as the style reference.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add SchemaMismatchError handler to ops_update.py</name>
  <files>packages/graph-io/src/graph_io/cli/ops_update.py</files>
  <read_first>
    - packages/graph-io/src/graph_io/cli/ops_update.py (full file)
    - packages/graph-io/src/graph_io/cli/q_find.py (canonical handler pattern)
    - packages/graph-io/src/graph_io/store.py (SchemaMismatchError class — already exists, line 17-25)
    - .planning/phases/28-schema-v2-uri-foundation/28-CONTEXT.md (D-02: error message must include `cg update --full` directive; Claude's Discretion on exact wording)
  </read_first>
  <behavior>
    - `cg update` (incremental) against a v1 DB exits with `exit_codes.SCHEMA_MISMATCH` (= 4), NOT `exit_codes.GENERIC` (= 1)
    - The stderr output contains the literal substring `cg update --full` (the existing `SchemaMismatchError.__str__` already includes this — store.py line 24-25 ends with "run `cg update --full` to rebuild" — so the existing `print(f"error: {exc}", file=sys.stderr)` pattern from q_find.py is sufficient)
    - The stderr output does NOT contain `Traceback` (no unhandled exception)
    - `cg update --full` is NOT short-circuited — it must reach `update.run(..., full=True)` and proceed (Plan 05's unlink+rebuild path runs there). The new handler only triggers when `update.run` itself raises SchemaMismatchError, which (post-Plan-05) only happens on the non-`--full` path.
    - Existing handlers for `NotInGitRepoError` and `UpdateInProgressError` are unchanged and still take precedence
  </behavior>
  <action>
    Add `store` to the import line: change `from graph_io import exit_codes, update` to `from graph_io import exit_codes, store, update`. Insert a new `except` block AFTER the `UpdateInProgressError` block and BEFORE the generic `except Exception` block — preserving the existing handler order. The new block matches the q_find.py pattern verbatim:
    ```
    except store.SchemaMismatchError as exc:
        print(f"error: {exc}", file=sys.stderr)
        return exit_codes.SCHEMA_MISMATCH
    ```
    Do NOT alter the existing message format string, do NOT colorize, do NOT add `--full` boilerplate (the SchemaMismatchError.__str__ already includes the directive). Do NOT touch any other CLI module (per CONTEXT.md scope — handlers in q_*.py and ops_status.py / ops_dump.py / ops_sync_wiki.py are already correct).
  </action>
  <verify>
    <automated>uv run --package graph-io pytest packages/graph-io/tests/test_cli_exit_codes.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "except store\.SchemaMismatchError" packages/graph-io/src/graph_io/cli/ops_update.py` returns a match
    - `grep -E "from graph_io import .*store" packages/graph-io/src/graph_io/cli/ops_update.py` returns a match
    - `grep -E "exit_codes\.SCHEMA_MISMATCH" packages/graph-io/src/graph_io/cli/ops_update.py` returns a match
    - The handler appears BEFORE `except Exception`: `awk '/except store\.SchemaMismatchError/,/except Exception/' packages/graph-io/src/graph_io/cli/ops_update.py | head -1` contains `SchemaMismatchError`
    - `uv run --package graph-io python -c "from graph_io.cli import ops_update; import inspect; src = inspect.getsource(ops_update.run); assert 'SchemaMismatchError' in src and src.index('SchemaMismatchError') < src.index('except Exception')"` exits 0
  </acceptance_criteria>
  <done>
    `ops_update.py` imports `store` and includes an `except store.SchemaMismatchError` handler positioned before the generic `except Exception`, returning `exit_codes.SCHEMA_MISMATCH`. Existing handlers (`NotInGitRepoError`, `UpdateInProgressError`) are unchanged.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add regression tests covering cg update + cg find against a v1 DB</name>
  <files>packages/graph-io/tests/test_cli_exit_codes.py</files>
  <read_first>
    - packages/graph-io/tests/test_cli_exit_codes.py (existing patterns — confirm how `main(argv)` is invoked, how stderr is captured, how a workspace fixture is constructed)
    - packages/graph-io/tests/conftest.py and any shared fixture file (look for tmp_workspace or similar)
    - packages/graph-io/src/graph_io/store.py (SchemaMismatchError message format)
    - packages/graph-io/src/graph_io/exit_codes.py (SCHEMA_MISMATCH = 4)
  </read_first>
  <behavior>
    - `test_cg_update_on_v1_db_exits_schema_mismatch`: constructs a synthetic schema-v1 `code.db` by opening sqlite directly and inserting `INSERT INTO metadata(key,value) VALUES ('schema_version','1')` (plus a stub schema, OR by monkey-patching `schema.SCHEMA_VERSION` to 1 before calling `store.connect(create=True)` and then restoring). Then invokes `cg update` (no --full) via `main(['--repo', str(repo_root), 'update'])` and asserts:
      - Return code == 4 (`exit_codes.SCHEMA_MISMATCH`)
      - Captured stderr contains the substring `cg update --full`
      - Captured stderr does NOT contain the substring `Traceback`
    - `test_cg_find_on_v1_db_exits_schema_mismatch`: same setup, invokes `cg find foo` and asserts the same three properties (regression guard for the existing q_find.py wiring — must not regress as a side effect of this plan)
    - `test_cg_update_full_on_v1_db_does_not_exit_4_from_ops_update_handler`: runs `cg update --full` on the same v1 DB. This test is the handler-precedence guard — its purpose is to prove the SchemaMismatchError handler did NOT swallow the --full path. Wave 2 (this plan) lands BEFORE Wave 3 (Plan 05), so the --full rebuild path does NOT yet exist at the time this test is first written — it will currently exit non-zero (likely 1) via some other failure (e.g., schema.apply_schema overwriting a v1 metadata row, or a downstream error). The test MUST be decorated unconditionally with `@pytest.mark.xfail(strict=False, reason='Plan 05 wires the --full path that converts v1 → v2; until then this test confirms the handler is correctly bypassed for --full')`. Plan 05 Task 4 removes this marker after the rebuild path lands. The IMPORTANT assertion is `result != 4` — proving the SchemaMismatchError handler did not swallow the --full path.
  </behavior>
  <action>
    Add three test functions to `packages/graph-io/tests/test_cli_exit_codes.py`. Use the existing test patterns in that file as the style reference (likely uses `capsys` or a `tmp_workspace` fixture and invokes `from graph_io.cli.main import main`).
    
    For creating the v1 DB fixture, the cleanest approach is:
    1. Use `monkeypatch.setattr(schema, 'SCHEMA_VERSION', 1)` BEFORE calling `store.connect(db_path, create=True)` to seed a v1 metadata row
    2. Close the connection
    3. Restore `SCHEMA_VERSION` (monkeypatch auto-cleans on test exit)
    4. Invoke the CLI command
    
    If `test_cli_exit_codes.py` already has a fixture for creating a v1 DB, REUSE it. Read the file first; do not duplicate fixtures.
    
    For the `cg update --full` test, decorate it UNCONDITIONALLY with `@pytest.mark.xfail(strict=False, reason='Plan 05 wires the --full path that converts v1 → v2; until then this test confirms the handler is correctly bypassed for --full')`. Wave ordering is deterministic (Plan 04 = Wave 2 runs before Plan 05 = Wave 3), so the marker is always required at the moment this plan ships — do NOT make the decoration conditional on "if Plan 05 hasn't shipped". The test body still asserts `result != 4` so the handler-precedence regression is caught regardless of xfail status. Plan 05 Task 4 removes this exact marker after wiring the rebuild path.
  </action>
  <verify>
    <automated>uv run --package graph-io pytest packages/graph-io/tests/test_cli_exit_codes.py::test_cg_update_on_v1_db_exits_schema_mismatch packages/graph-io/tests/test_cli_exit_codes.py::test_cg_find_on_v1_db_exits_schema_mismatch -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def test_cg_update_on_v1_db_exits_schema_mismatch" packages/graph-io/tests/test_cli_exit_codes.py` returns a match
    - `grep -n "def test_cg_find_on_v1_db_exits_schema_mismatch" packages/graph-io/tests/test_cli_exit_codes.py` returns a match
    - `grep -n "def test_cg_update_full_on_v1_db_does_not_exit_4_from_ops_update_handler" packages/graph-io/tests/test_cli_exit_codes.py` returns a match
    - `grep -B2 "def test_cg_update_full_on_v1_db_does_not_exit_4_from_ops_update_handler" packages/graph-io/tests/test_cli_exit_codes.py | grep -c "xfail(strict=False"` returns at least 1 (the unconditional xfail marker is present)
    - `grep -n "cg update --full" packages/graph-io/tests/test_cli_exit_codes.py` returns a match (the substring is checked in stderr)
    - `uv run --package graph-io pytest packages/graph-io/tests/test_cli_exit_codes.py::test_cg_update_on_v1_db_exits_schema_mismatch packages/graph-io/tests/test_cli_exit_codes.py::test_cg_find_on_v1_db_exits_schema_mismatch -x` exits 0
    - `uv run --package graph-io pytest packages/graph-io/tests/test_cli_exit_codes.py -x` exits 0 (full file — including xfail-marked third test which is allowed to xfail strict=False)
  </acceptance_criteria>
  <done>
    Three regression tests exist covering the SCHEMA-02 behavior for `cg update`, the pre-existing `cg find` wiring, and the handler-precedence guard for `cg update --full`. The first two are green; the third is unconditionally xfail-marked (Plan 05 Task 4 will remove the marker). Stderr substring `cg update --full` and absence of `Traceback` are both asserted.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user shell→CLI argv | `cg update` argv is parsed by argparse; no untrusted input crosses into the new handler beyond what `update.run` already accepts. |
| store→CLI exception | `SchemaMismatchError` is raised from `_check_schema_version` in store.py; the handler is a pure exception translator (no logic in the catch block beyond formatting). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-04-01 | Information disclosure | Stack trace leaked to stderr exposes filesystem paths from `code.db` location | mitigate | The new handler prints `f"error: {exc}"` (NOT `traceback.print_exc()`) — matches q_find.py pattern. Acceptance criterion asserts `Traceback` substring is absent from stderr. |
| T-28-04-02 | Tampering | Handler ordering bug — `except Exception` placed before `SchemaMismatchError` swallows the specific exception | mitigate | Acceptance criterion uses `inspect.getsource` to assert SchemaMismatchError appears textually BEFORE `except Exception`. |
| T-28-04-03 | Denial-of-Service | User mass-runs `cg update` on a v1 DB expecting failure → no infinite loop, just exit 4 | accept | Exit-and-die; no retry logic introduced by this plan. |
| T-28-04-SC | Tampering | npm/pip/cargo installs | accept | Plan installs zero packages. |
</threat_model>

<verification>
- `uv run --package graph-io pytest packages/graph-io/tests/test_cli_exit_codes.py -x` exits 0
- Manual smoke (if developer wishes): build a v1 DB and run `cg update` → exit 4, stderr mentions `cg update --full`, no traceback
- Existing tests in test_cli_exit_codes.py do not regress (the new handler is additive)
</verification>

<success_criteria>
- ops_update.py imports `store` and has an `except store.SchemaMismatchError` handler returning `exit_codes.SCHEMA_MISMATCH`
- Handler ordering is correct (SchemaMismatchError before generic Exception)
- `cg update` on a v1 DB exits 4 with a friendly stderr message
- `cg find` on a v1 DB still exits 4 (regression preserved)
- `cg update --full` on a v1 DB does NOT exit 4 via this handler (Plan 05 owns the rebuild path)
</success_criteria>

<output>
Create `.planning/phases/28-schema-v2-uri-foundation/28-04-SUMMARY.md` when done
</output>
</output>
