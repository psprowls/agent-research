---
phase: 05-remaining-commands
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - cores/vault-io/src/vault_io/lint/container.py
  - cores/vault-io/src/vault_io/lint/dependency.py
  - cores/vault-io/src/vault_io/lint/domain.py
  - cores/vault-io/src/vault_io/lint/file_map.py
  - cores/vault-io/src/vault_io/lint/package_sync.py
  - cores/vault-io/src/vault_io/lint/source_sync.py
  - cores/vault-io/src/vault_io/lint/workflow_hints.py
  - cores/vault-io/tests/test_lint_modules.py
autonomous: true
requirements: [CMD-05]

must_haves:
  truths:
    - "All 7 lint mechanical rule modules can be imported from vault_io.lint.{container,dependency,domain,file_map,package_sync,source_sync,workflow_hints}"
    - "Each module exposes a `check()` function and a `GROUP` constant matching its lattice-wiki-core counterpart"
    - "Each module's check() returns a list of human-readable issue strings when run against a fixture vault"
    - "_is_placeholder_target() from vault_io.lint.common is the wikilink placeholder filter used by any module that walks wikilinks"
  artifacts:
    - path: "cores/vault-io/src/vault_io/lint/container.py"
      provides: "check(repo, wiki) for container drift; GROUP='container'"
    - path: "cores/vault-io/src/vault_io/lint/dependency.py"
      provides: "check(...) for dependency layer drift; GROUP='dependency'"
    - path: "cores/vault-io/src/vault_io/lint/domain.py"
      provides: "check(...) for domain placement; GROUP='domain'"
    - path: "cores/vault-io/src/vault_io/lint/file_map.py"
      provides: "check(...) for file_map drift; uses _git_ls_files from vault_io.scan_monorepo"
    - path: "cores/vault-io/src/vault_io/lint/package_sync.py"
      provides: "check(...) for package_sync drift"
    - path: "cores/vault-io/src/vault_io/lint/source_sync.py"
      provides: "check(...) for source_sync drift"
    - path: "cores/vault-io/src/vault_io/lint/workflow_hints.py"
      provides: "check(...) for workflow_hints"
  key_links:
    - from: "cores/vault-io/src/vault_io/lint/file_map.py"
      to: "cores/vault-io/src/vault_io/scan_monorepo.py"
      via: "import _git_ls_files"
      pattern: "from vault_io.scan_monorepo import.*_git_ls_files"
    - from: "cores/vault-io/src/vault_io/lint/package_sync.py"
      to: "cores/vault-io/src/vault_io/git_state.py"
      via: "import from vault_io.git_state"
      pattern: "from vault_io.git_state"
    - from: "cores/vault-io/src/vault_io/lint/dependency.py"
      to: "cores/vault-io/src/vault_io/lint/common.py"
      via: "import shared parsing helpers"
      pattern: "from vault_io.lint.common"
---

## Phase Goal

**As a** Pat (preparing to ship the `lint` command), **I want to** port the 7 mechanical lint rule modules from lattice-wiki-core into vault_io with all imports rewritten to vault_io equivalents, **so that** the `lint` command in plan-05-06 can simply import them and produce parity findings against real vaults.

<objective>
Port the 7 mechanical lint rule modules from `lattice-wiki-core/lint/` into `cores/vault-io/src/vault_io/lint/`, performing only mechanical import swaps (`lattice_wiki_core.X` → `vault_io.X`). No logic changes. Replace the Wave 0 stub `test_lint_modules.py` with real assertions against the existing fixture vaults.

This plan is intentionally a single foundation deliverable: every byte of business logic comes from the lattice-wiki-core sources; only import paths change. The 7 modules total ~485 lines of source code (counted; see RESEARCH §Group B). Because the changes are mechanical, this plan can run parallel to plan-05-01 and plan-05-03 in Wave 1 — no overlapping files.

Per MVP_MODE: this plan does not directly add user-visible capability — it is foundation for plan-05-06's `lint` vertical slice. Justification: the 7 modules cannot be merged into plan-05-06 without exceeding the 2-3-task / ~50% context budget (lint orchestration alone is ~250 inline lines plus 3-group LLM fan-out). Splitting at the module-port boundary is the natural seam.

Output:
- 7 new files in `cores/vault-io/src/vault_io/lint/`
- `tests/test_lint_modules.py` with at least 7 parametrized test cases (one per module) asserting `check()` returns a list against `edge-case-vault` or `round-trip-vault` fixtures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-remaining-commands/05-RESEARCH.md
@.planning/phases/05-remaining-commands/05-PATTERNS.md

<interfaces>
<!-- Existing vault_io symbols the ported modules will import. Verified against source. -->

From `cores/vault-io/src/vault_io/lint/common.py` (ALREADY PORTED — these symbols exist):
- `WIKILINK_RE`, `LOG_ENTRY_RE`, `FILE_MAP_SECTION_RE` (regex constants)
- `parse_frontmatter(text) -> dict`
- `strip_code(text) -> str`
- `strip_frontmatter(text) -> str`
- `_is_placeholder_target(target: str) -> bool` (VAULT-06 wikilink placeholder filter)

From `cores/vault-io/src/vault_io/scan_monorepo.py`:
- `_git_ls_files(root: Path) -> list[str]` (private but acceptable per RESEARCH Risk 6)
- `discover_workspaces`, `compute_diff`, `build_file_map`, `pick_representative`, etc. (already exported)

From `cores/vault-io/src/vault_io/git_state.py`:
- `is_clean_main`, `head_commit`, `changed_files_since`

From `cores/vault-io/src/vault_io/layout_io.py`:
- `read_layout`, `write_layout`, `ensure_subpage`

Port sources (read these in full — do NOT paraphrase, port verbatim with import swaps):
- `/Users/pat/Personal/lattice/packages/lattice-wiki-core/src/lattice_wiki_core/lint/container.py` (62 lines)
- `/Users/pat/Personal/lattice/packages/lattice-wiki-core/src/lattice_wiki_core/lint/dependency.py` (155 lines)
- `/Users/pat/Personal/lattice/packages/lattice-wiki-core/src/lattice_wiki_core/lint/domain.py` (43 lines)
- `/Users/pat/Personal/lattice/packages/lattice-wiki-core/src/lattice_wiki_core/lint/file_map.py` (66 lines)
- `/Users/pat/Personal/lattice/packages/lattice-wiki-core/src/lattice_wiki_core/lint/package_sync.py` (52 lines)
- `/Users/pat/Personal/lattice/packages/lattice-wiki-core/src/lattice_wiki_core/lint/source_sync.py` (49 lines)
- `/Users/pat/Personal/lattice/packages/lattice-wiki-core/src/lattice_wiki_core/lint/workflow_hints.py` (58 lines)
</interfaces>

Existing fixtures available (verified):
- `cores/vault-io/tests/fixtures/round-trip-vault/`
- `cores/vault-io/tests/fixtures/single-package-vault/`
- `cores/vault-io/tests/fixtures/edge-case-vault/`
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port 7 lint mechanical modules with import swaps</name>
  <files>
    cores/vault-io/src/vault_io/lint/container.py,
    cores/vault-io/src/vault_io/lint/dependency.py,
    cores/vault-io/src/vault_io/lint/domain.py,
    cores/vault-io/src/vault_io/lint/file_map.py,
    cores/vault-io/src/vault_io/lint/package_sync.py,
    cores/vault-io/src/vault_io/lint/source_sync.py,
    cores/vault-io/src/vault_io/lint/workflow_hints.py
  </files>
  <read_first>
    /Users/pat/Personal/lattice/packages/lattice-wiki-core/src/lattice_wiki_core/lint/container.py,
    /Users/pat/Personal/lattice/packages/lattice-wiki-core/src/lattice_wiki_core/lint/dependency.py,
    /Users/pat/Personal/lattice/packages/lattice-wiki-core/src/lattice_wiki_core/lint/domain.py,
    /Users/pat/Personal/lattice/packages/lattice-wiki-core/src/lattice_wiki_core/lint/file_map.py,
    /Users/pat/Personal/lattice/packages/lattice-wiki-core/src/lattice_wiki_core/lint/package_sync.py,
    /Users/pat/Personal/lattice/packages/lattice-wiki-core/src/lattice_wiki_core/lint/source_sync.py,
    /Users/pat/Personal/lattice/packages/lattice-wiki-core/src/lattice_wiki_core/lint/workflow_hints.py,
    cores/vault-io/src/vault_io/lint/common.py,
    cores/vault-io/src/vault_io/scan_monorepo.py
  </read_first>
  <behavior>
    - Each new file is a byte-for-byte port of its lattice-wiki-core counterpart EXCEPT every `from lattice_wiki_core.X` import is rewritten to `from vault_io.X`.
    - Each module preserves its `GROUP` module-level constant.
    - Each module preserves its `check(...)` function signature (do not rename, do not refactor parameters).
    - `file_map.py` imports `_git_ls_files` from `vault_io.scan_monorepo` (private function reused intentionally per RESEARCH Risk 6).
    - `package_sync.py` and `source_sync.py` import from `vault_io.git_state` (not `lattice_wiki_core.git_state`).
    - `domain.py` has no vault_io imports — port verbatim with no changes.
    - All 7 files can be imported without ImportError after the port.
  </behavior>
  <action>
    For each of the 7 port-source paths listed in `<read_first>`, read the file in full and write its `vault_io` counterpart at the corresponding path under `cores/vault-io/src/vault_io/lint/`. Perform ONLY these substitutions globally inside each file:
    - `from lattice_wiki_core.layout_io` → `from vault_io.layout_io`
    - `from lattice_wiki_core.scan_monorepo` → `from vault_io.scan_monorepo`
    - `from lattice_wiki_core.git_state` → `from vault_io.git_state`
    - `from lattice_wiki_core.lint.common` → `from vault_io.lint.common`
    - `import lattice_wiki_core` → `import vault_io`
    Do NOT rename functions, classes, constants, or change docstrings. Do NOT add new dependencies. Do NOT introduce abstractions. Preserve the `GROUP = "<name>"` module-level constants exactly. Verify by importing each module in a Python REPL via `python -c "from vault_io.lint import container, dependency, domain, file_map, package_sync, source_sync, workflow_hints"` (which is asserted in the test in Task 2).
  </action>
  <verify>
    <automated>uv run python -c "from vault_io.lint import container, dependency, domain, file_map, package_sync, source_sync, workflow_hints; print(all(hasattr(m, 'check') for m in [container, dependency, domain, file_map, package_sync, source_sync, workflow_hints]))"</automated>
  </verify>
  <acceptance_criteria>
    - All 7 files exist under `cores/vault-io/src/vault_io/lint/`
    - `grep -l "lattice_wiki_core" cores/vault-io/src/vault_io/lint/*.py | wc -l` returns 0 (no lattice imports survived)
    - `grep -c "^GROUP = " cores/vault-io/src/vault_io/lint/{container,dependency,domain,file_map,package_sync,source_sync,workflow_hints}.py` shows GROUP defined in each (filter `-v '^#'` if comment lines interfere)
    - `grep -c "^def check\b" cores/vault-io/src/vault_io/lint/{container,dependency,domain,file_map,package_sync,source_sync,workflow_hints}.py` shows `check` defined in each
    - `uv run python -c "from vault_io.lint import container, dependency, domain, file_map, package_sync, source_sync, workflow_hints"` exits 0
    - Diff line counts between each port pair are dominated by import-line changes only (verify by running `diff /Users/pat/Personal/lattice/packages/lattice-wiki-core/src/lattice_wiki_core/lint/container.py cores/vault-io/src/vault_io/lint/container.py | grep -v "^[<>] from " | grep -v "^[<>] import " | grep -v "^---" | grep -v "^[0-9]"` returns no meaningful logic diff)
  </acceptance_criteria>
  <done>7 modules exist; all importable; no lattice_wiki_core imports remain; `GROUP` constants and `check()` signatures preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: test_lint_modules.py — assertions against fixture vaults for all 7 modules</name>
  <files>cores/vault-io/tests/test_lint_modules.py</files>
  <read_first>
    cores/vault-io/tests/test_wikilink_predicate.py,
    cores/vault-io/tests/conftest.py,
    cores/vault-io/src/vault_io/lint/container.py,
    cores/vault-io/src/vault_io/lint/dependency.py,
    cores/vault-io/src/vault_io/lint/domain.py,
    cores/vault-io/src/vault_io/lint/file_map.py,
    cores/vault-io/src/vault_io/lint/package_sync.py,
    cores/vault-io/src/vault_io/lint/source_sync.py,
    cores/vault-io/src/vault_io/lint/workflow_hints.py
  </read_first>
  <behavior>
    - Replace the Wave 0 skip-stub in `test_lint_modules.py` with real test cases.
    - Each of 7 modules has at least one test: it can be imported, `GROUP` constant is the expected string, `check(...)` returns a `list` when invoked against the appropriate fixture vault path (signature varies — read the ported module to determine arg shape).
    - One smoke test asserts every module's `GROUP` value is one of the 7 expected strings and they are all distinct.
    - At least one test invokes a `check()` against `edge-case-vault` and asserts the result is `isinstance(result, list)`. We do NOT assert on the content of findings — Task 2 only verifies the modules are wired correctly. Finding-count parity is a plan-05-06 concern.
    - Tests use `pathlib.Path(__file__).parent / "fixtures" / "edge-case-vault"` for fixture locating.
  </behavior>
  <action>
    Overwrite `cores/vault-io/tests/test_lint_modules.py` (currently a Wave 0 skip-stub from plan-05-01). Add 8 tests: `test_all_modules_importable`, `test_GROUP_constants_unique_and_expected`, and one `test_<module>_check_returns_list` for each of the 7 modules. Use the fixture `cores/vault-io/tests/fixtures/edge-case-vault/` (which exists, per RESEARCH §Existing Code Inventory). For each module, read its `check()` signature (signatures differ — `container.check(repo, wiki)` vs. `dependency.check(wiki)`, etc.; consult the ported module source AFTER plan-05-02 Task 1 has run). Where a module's `check()` needs a `repo` path, point it at the fixture's parent dir or `tmp_path` as appropriate (use the lattice-wiki-core `lint_wiki.py:scan()` invocation site as the source of truth for invocation order). Do NOT mock anything — these are pure-Python pure-function modules. Expected `GROUP` values: `container`, `dependency`, `domain`, `file_map`, `package_sync`, `source_sync`, `workflow_hints`.
  </action>
  <verify>
    <automated>uv run --package vault-io pytest cores/vault-io/tests/test_lint_modules.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `cores/vault-io/tests/test_lint_modules.py` contains no `pytest.skip` or `pytestmark = pytest.mark.skip` lines
    - `grep -c "^def test_" cores/vault-io/tests/test_lint_modules.py` returns >= 9 (smoke + 7 per-module + import test)
    - `uv run --package vault-io pytest cores/vault-io/tests/test_lint_modules.py -x` exits 0
    - All 7 module-specific test cases pass (no skips, no xfails)
    - The 7 `GROUP` constants are exactly the set `{container, dependency, domain, file_map, package_sync, source_sync, workflow_hints}`
  </acceptance_criteria>
  <done>test_lint_modules.py asserts importability, GROUP uniqueness, and check() return type for all 7 modules; all assertions green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem → lint module | `check()` reads vault and repo files; same trust as caller |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Tampering | Lint modules reading arbitrary vault paths | accept | Read-only file access; same user context; no remote attack surface |
| T-05-02-02 | Information Disclosure | Issue strings may include path fragments | accept | Output destined for the user's own terminal/log; same user context |
</threat_model>

<verification>
- All 7 modules ported with import-only diffs against the lattice-wiki-core source.
- `uv run --package vault-io pytest cores/vault-io/tests/test_lint_modules.py -x` green.
- `uv run --package vault-io pytest cores/vault-io/tests -x` still green (no regressions to existing wikilink_predicate or other tests).
- No `lattice_wiki_core` imports remain in `vault_io/lint/`.
</verification>

<success_criteria>
1. `from vault_io.lint import container, dependency, domain, file_map, package_sync, source_sync, workflow_hints` succeeds.
2. Each module exposes `GROUP` and `check(...)` matching its lattice-wiki-core counterpart.
3. `test_lint_modules.py` passes with no skips for any of the 7 modules.
4. plan-05-06 can write `from vault_io.lint.container import check as check_container_drift` (etc.) without further preparation.
</success_criteria>

<output>
After completion, create `.planning/phases/05-remaining-commands/05-02-SUMMARY.md` recording:
- 7 module file paths (and line counts)
- The exact `check()` signature of each module (so plan-05-06 doesn't have to re-derive)
- Whether `_git_ls_files` is exported as `_git_ls_files` or required renaming in vault_io.scan_monorepo
</output>
