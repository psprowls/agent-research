---
phase: 05-remaining-commands
plan: 06
type: execute
wave: 4
depends_on: [05-02, 05-05]
files_modified:
  - agents/code-wiki-agent/src/code_wiki_agent/commands/lint.py
  - agents/code-wiki-agent/src/code_wiki_agent/cli.py
  - agents/code-wiki-agent/src/code_wiki_mcp/server.py
  - agents/code-wiki-agent/tests/unit/test_commands_lint.py
  - agents/code-wiki-agent/tests/commands/test_lint_parity.py
autonomous: true
requirements: [CMD-05, MCP-01, MCP-03]

must_haves:
  truths:
    - "User can run `code-wiki-agent lint` and receive a full mechanical-pass report (orphans, broken wikilinks, stale, missing frontmatter, code-drift across 7 modules)"
    - "User can run `code-wiki-agent lint --json` and receive a structured findings dict (D-10 — no write-back to vault)"
    - "Lint runs 3 parallel semantic linter subagents: page_quality, adr_chain, stale_claims (D-09)"
    - "Mechanical pass calls all 7 ported lint modules (container, dependency, domain, file_map, package_sync, source_sync, workflow_hints) AND the inline ~250-line scan() logic from lint_wiki.py (orphans/broken/stale/missing-fm/duplicate-titles/log-gap)"
    - "Broken-link detection honors _is_placeholder_target() — [[wiki/...]] and [[work/<slug>]] produce zero findings (success criterion 3)"
    - "`--stale-days` defaults to 90, `--log-gap-days` defaults to 14 (per lint_wiki.py:main)"
    - "MCP `wiki_lint` emits progress notifications after mechanical pass and after semantic fan-out (MCP-03)"
  artifacts:
    - path: "agents/code-wiki-agent/src/code_wiki_agent/commands/lint.py"
      provides: "run_lint() + LintResult dataclass + 3 LINTER_*_SYSTEM prompts + mechanical orchestration (inline scan() port) + semantic fan-out via SubagentPool"
      min_lines: 300
    - path: "agents/code-wiki-agent/tests/commands/test_lint_parity.py"
      provides: "Parity test asserting mechanical findings against edge-case-vault fixture match a recorded shape"
  key_links:
    - from: "agents/code-wiki-agent/src/code_wiki_agent/commands/lint.py"
      to: "cores/vault-io/src/vault_io/lint/{container,dependency,domain,file_map,package_sync,source_sync,workflow_hints}.py"
      via: "import check as check_<group> from each module"
      pattern: "from vault_io.lint.container import check"
    - from: "agents/code-wiki-agent/src/code_wiki_agent/commands/lint.py"
      to: "cores/vault-io/src/vault_io/lint/common.py"
      via: "import _is_placeholder_target, WIKILINK_RE, LOG_ENTRY_RE, parse_frontmatter, strip_code, strip_frontmatter"
      pattern: "from vault_io.lint.common import"
    - from: "agents/code-wiki-agent/src/code_wiki_agent/commands/lint.py"
      to: "cores/subagent-runtime/src/subagent_runtime/pool.py"
      via: "SubagentPool.run_all(items=3-group-tuples, task=run_linter_group, role='linter', ...)"
      pattern: "SubagentPool.*run_all"
---

## Phase Goal

**As a** Pat, **I want to** run `code-wiki-agent lint` (CLI) or `wiki_lint` (MCP) against my vault and receive a unified report covering mechanical drift (orphans, broken wikilinks honoring the `[[wiki/...]]`/`[[work/<slug>]]` placeholder filter, stale pages, missing frontmatter, 7 specialized drift checks) PLUS semantic findings from 3 parallel linter subagents (page quality, ADR chain integrity, stale claims), **so that** the vault stays healthy and parity with lattice-wiki's lint output is achieved.

<objective>
Deliver the `lint` command end-to-end vertical slice — the LAST command in Phase 5 and the most complex. Per RESEARCH §`lint` Command (Complexity: VERY HIGH). This plan ports the inline ~250-line `scan()` function from `lattice-wiki-core/lint_wiki.py:scan()` (lines 77-331) into `commands/lint.py`, wires the 7 mechanical modules from plan-05-02, runs 3 semantic linter subagents in parallel via SubagentPool, and ships CLI + MCP surfaces with parity tests against `edge-case-vault`.

Per MVP_MODE: after this plan, a real user can lint their vault end-to-end — the final capability needed for full lattice-wiki parity. This plan closes phase success criterion 3 (broken-link placeholder filter) and criterion 5 (parity tests).

Output:
- `commands/lint.py` (new, ~300-400 lines — the largest single file in this phase)
- `cli.py` (modified — add `lint` subcommand)
- `server.py` (modified — add `wiki_lint` tool)
- `tests/unit/test_commands_lint.py` (replaces Wave 0 stub)
- `tests/commands/test_lint_parity.py` (replaces Wave 0 stub)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-remaining-commands/05-RESEARCH.md
@.planning/phases/05-remaining-commands/05-PATTERNS.md
@.planning/phases/05-remaining-commands/05-CONTEXT.md
@.planning/phases/05-02-SUMMARY.md
@.planning/phases/05-04-SUMMARY.md
@.planning/phases/05-05-SUMMARY.md

<interfaces>
<!-- All present after plan-05-02. -->

From `vault_io.lint.common` (already exists pre-Phase 5):
- `WIKILINK_RE`, `LOG_ENTRY_RE`, `FILE_MAP_SECTION_RE` (compiled regexes)
- `_is_placeholder_target(target: str) -> bool` (VAULT-06 — filters `[[wiki/...]]`, `[[work/<slug>]]` patterns)
- `parse_frontmatter(text: str) -> dict`
- `strip_code(text: str) -> str`
- `strip_frontmatter(text: str) -> str`

From `vault_io.lint.{container,dependency,domain,file_map,package_sync,source_sync,workflow_hints}` (plan-05-02 ported these):
- Each exposes `check(...)` and `GROUP` constant. Exact `check()` signature per module is in plan-05-02 SUMMARY (read it before implementing).

From `vault_io.scan_monorepo`:
- `discover_workspaces(repo, pinned_containers=None)`
- `unscope` helper (used by lint_wiki.py — verify it is exported)

From `vault_io.layout_io`:
- `read_layout(wiki)`

From `cores/subagent-runtime`:
- `SubagentPool.run_all(...)` — same API as plan-05-04 uses

From `cores/model-adapter`:
- `make_llm("linter")`, `load_role_config("linter")`; `linter` role already in `models.toml` with `max_tokens = 3000` (per ROADMAP Phase 2 success criterion 3)

Port source (READ IN FULL — this is the largest direct port in the phase):
- `/Users/pat/Personal/lattice/packages/lattice-wiki-core/src/lattice_wiki_core/lint_wiki.py` (507 lines)
  - `scan()` function (lines 77-331): mechanical orchestration including page walk, link-graph construction, orphan/broken/stale/missing-fm/duplicate-title/log-gap detection
  - `main()` function (lines ~340-end): contains the `--stale-days=90` and `--log-gap-days=14` defaults

Existing fixture: `cores/vault-io/tests/fixtures/edge-case-vault/` has known broken links, truncated frontmatter, orphans — use for parity assertions.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: commands/lint.py — mechanical pass (inline scan() port + 7 module wiring) + semantic 3-group fan-out</name>
  <files>
    agents/code-wiki-agent/src/code_wiki_agent/commands/lint.py,
    agents/code-wiki-agent/tests/unit/test_commands_lint.py
  </files>
  <read_first>
    /Users/pat/Personal/lattice/packages/lattice-wiki-core/src/lattice_wiki_core/lint_wiki.py,
    cores/vault-io/src/vault_io/lint/common.py,
    cores/vault-io/src/vault_io/lint/container.py,
    cores/vault-io/src/vault_io/lint/dependency.py,
    cores/vault-io/src/vault_io/lint/domain.py,
    cores/vault-io/src/vault_io/lint/file_map.py,
    cores/vault-io/src/vault_io/lint/package_sync.py,
    cores/vault-io/src/vault_io/lint/source_sync.py,
    cores/vault-io/src/vault_io/lint/workflow_hints.py,
    agents/code-wiki-agent/src/code_wiki_agent/commands/scan.py,
    agents/code-wiki-agent/src/code_wiki_agent/commands/query.py,
    cores/subagent-runtime/src/subagent_runtime/pool.py,
    .planning/phases/05-remaining-commands/05-RESEARCH.md,
    .planning/phases/05-remaining-commands/05-PATTERNS.md,
    cores/vault-io/tests/fixtures/edge-case-vault
  </read_first>
  <behavior>
    - `LintResult` dataclass fields (final, matches `scan()` return dict shape from `lint_wiki.py`):
      - `wiki: str`, `total_pages: int`
      - `orphans: list[str]`
      - `broken_links: list[tuple[str, str]]`  (source_page, target)
      - `stale: list[tuple[str, str]]`  (page, updated_date)
      - `missing_frontmatter: list[str]`
      - `duplicate_titles: dict[str, list[str]]`
      - `log_gap: dict | None`
      - `code_drift: dict`
      - `container_drift: list[str]`
      - `source_sync_drift: list[str]`
      - `file_map_drift: list[str]`
      - `package_sync_drift: list[str]`
      - `domain_placement: list[str]`
      - `workflow_hints: list[str]`
      - `semantic_findings: dict[str, list[str]]`  (keys: `page_quality`, `adr_chain`, `stale_claims`)
      - `errors: list[str]`  (per-group semantic errors)
    - `async def run_lint(vault_path: Path | None = None, stale_days: int = 90, log_gap_days: int = 14) -> LintResult` orchestrates:
      1. Resolve `wiki, repo`; `layout = read_layout(wiki)`
      2. MECHANICAL inline pass — port `lint_wiki.py:scan()` lines 77-331 verbatim with import swaps (lattice_wiki_core.* → vault_io.*). This includes the page walk, frontmatter parse, link graph, orphan detection, broken-link detection (using `_is_placeholder_target` filter), stale-page detection (using `stale_cutoff = today - timedelta(days=stale_days)`), missing-frontmatter check, duplicate-title detection, log-gap detection (using `LOG_ENTRY_RE`).
      3. MECHANICAL module pass — call each of the 7 ported `check(...)` functions and assign results to the corresponding LintResult fields. Argument shapes per plan-05-02 SUMMARY.
      4. SEMANTIC pass — build the 3 semantic groups: `("page_quality", LINTER_PAGE_QUALITY_SYSTEM, pages_sample)`, `("adr_chain", LINTER_ADR_CHAIN_SYSTEM, adr_pages)`, `("stale_claims", LINTER_STALE_CLAIMS_SYSTEM, pages_with_source_path)`. `pages_sample` is a sampled subset (e.g., first 20 non-work pages); `adr_pages` is all pages under `adrs/`; `pages_with_source_path` is pages whose frontmatter contains `source_path` or `package_path`.
      5. `pool = SubagentPool(trace_dir=wiki / ".code-wiki" / "traces"); cfg = load_role_config("linter"); fan_result = await pool.run_all(items=semantic_groups, task=run_linter_group, role="linter", model_id=cfg["model_id"], max_concurrency=cfg["max_concurrency"])`.
      6. `run_linter_group((name, system, pages_input))`: send `[SystemMessage(system), HumanMessage(build_linter_input(pages_input))]` to `make_llm("linter").ainvoke(...)`; parse response into a list of finding strings (one per non-empty line); return list.
      7. Map successes back to `semantic_findings[name] = findings`; errors go to `result.errors`.
      8. Return `LintResult(...)`. NO write-back to vault (D-10).
    - Define 3 prompt constants: `LINTER_PAGE_QUALITY_SYSTEM`, `LINTER_ADR_CHAIN_SYSTEM`, `LINTER_STALE_CLAIMS_SYSTEM` — each instructs the model to return one finding per line in plain text (no JSON). Concise (200-400 token system prompts).
    - `LINTED_TOPS` constant — read from `lint_wiki.py` (typically `{"packages", "apps", "domains", "concepts", "adrs", "work"}`); preserve verbatim.
    - Tests in `test_commands_lint.py` (overwrite Wave 0 stub):
      - `test_lint_result_dataclass_shape` — assert all 18 fields exist.
      - `test_run_lint_mechanical_finds_orphans_in_fixture` — run against `edge-case-vault`; assert `result.orphans` is a list and contains at least one expected orphan path.
      - `test_run_lint_broken_links_skip_placeholder_targets` — set up a fixture with `[[wiki/foo]]` and `[[work/bar]]` and `[[real-broken]]`; assert only `real-broken` appears in `broken_links` (success criterion 3 of the phase).
      - `test_run_lint_stale_days_threshold_default_90` — assert `stale_days=90` default is used.
      - `test_run_lint_log_gap_days_threshold_default_14` — assert `log_gap_days=14` default is used.
      - `test_run_lint_calls_all_7_module_check_functions` — monkeypatch each of the 7 `check` symbols; assert all 7 were called.
      - `test_run_lint_semantic_fanout_3_groups` — monkeypatch `SubagentPool.run_all`; assert it was called once with `items` of length 3 and `role="linter"`.
      - `test_run_lint_semantic_errors_surface_in_result_errors` — patch run_all to return 2 successes + 1 PerItemError; assert `result.errors` has length 1.
      - `test_run_lint_no_write_back_to_vault` — assert vault directory unchanged after run (snapshot dir contents before/after).
  </behavior>
  <action>
    Create `agents/code-wiki-agent/src/code_wiki_agent/commands/lint.py`. Import the 7 ported check functions with aliases per PATTERNS.md (`from vault_io.lint.container import check as check_container_drift`, etc.). Import `WIKILINK_RE`, `LOG_ENTRY_RE`, `_is_placeholder_target`, `parse_frontmatter`, `strip_code`, `strip_frontmatter` from `vault_io.lint.common`. Import `discover_workspaces` (and `unscope` if used) from `vault_io.scan_monorepo`. Import `read_layout` from `vault_io.layout_io`. Import `SubagentPool`, `FanOutResult` from `subagent_runtime.pool`. Import `make_llm`, `load_role_config` from `model_adapter.loader`. Import `HumanMessage`, `SystemMessage` from `langchain_core.messages`.

    Define `LINTED_TOPS` set (copy from lint_wiki.py). Define `LintResult` dataclass with the 18 fields above.

    Port the inline `scan()` function body (lines 77-331 of lint_wiki.py) into a helper named `_mechanical_pass(wiki, repo, stale_days, log_gap_days, layout) -> dict` that returns a dict of all mechanical fields. Substitute `lattice_wiki_core.*` imports for `vault_io.*` equivalents; otherwise port logic VERBATIM — including the `LINTED_TOPS` check, the work-archived skip, the link-target derivation, the orphan/broken/stale/missing-fm/dup-title/log-gap detection paths. Use `_is_placeholder_target` exactly where lint_wiki.py uses it (filtering `__BROKEN__:` candidates). Treat any helper function defined inside lint_wiki.py:scan() as inline code — port it inline OR extract as a private helper in commands/lint.py.

    Implement `_semantic_pass(wiki, pages, pool, cfg) -> tuple[dict, list[str]]` that runs the 3-group fan-out and returns `(semantic_findings_dict, errors_list)`.

    Implement `async def run_lint(...)` that orchestrates: resolve → read_layout → mechanical pass → 7 module checks → semantic pass → return LintResult.

    Define the 3 system prompts as module-level constants. Each prompt should be 150-400 tokens, output one finding per line, plain text.

    Overwrite `tests/unit/test_commands_lint.py` with the 9 behavior tests. Use `edge-case-vault` for mechanical assertions; monkeypatch `SubagentPool.run_all` and `make_llm("linter")` for semantic tests so no Bedrock calls happen. For `test_run_lint_no_write_back_to_vault`, compute a recursive checksum of the vault dir before/after.

    The full implementation will be ~300-400 lines; this is the largest single file in Phase 5 and is the legitimate reason this plan stands alone.
  </action>
  <verify>
    <automated>uv run --package code-wiki-agent pytest agents/code-wiki-agent/tests/unit/test_commands_lint.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `agents/code-wiki-agent/src/code_wiki_agent/commands/lint.py` exists; contains `class LintResult`, `async def run_lint(`, `LINTER_PAGE_QUALITY_SYSTEM`, `LINTER_ADR_CHAIN_SYSTEM`, `LINTER_STALE_CLAIMS_SYSTEM`
    - `grep -c "from vault_io.lint.\(container\|dependency\|domain\|file_map\|package_sync\|source_sync\|workflow_hints\) import check as" agents/code-wiki-agent/src/code_wiki_agent/commands/lint.py` returns 7
    - `grep -c "_is_placeholder_target" agents/code-wiki-agent/src/code_wiki_agent/commands/lint.py` returns >= 1
    - `grep -c 'role="linter"' agents/code-wiki-agent/src/code_wiki_agent/commands/lint.py` returns >= 1
    - `grep -c "stale_days.*90\|stale_days: int = 90" agents/code-wiki-agent/src/code_wiki_agent/commands/lint.py` returns >= 1
    - `grep -c "log_gap_days.*14\|log_gap_days: int = 14" agents/code-wiki-agent/src/code_wiki_agent/commands/lint.py` returns >= 1
    - `wc -l agents/code-wiki-agent/src/code_wiki_agent/commands/lint.py` reports >= 300 lines (sanity check on port completeness)
    - `agents/code-wiki-agent/tests/unit/test_commands_lint.py` has no `pytest.skip` and `grep -c "^def test_\|^async def test_" agents/code-wiki-agent/tests/unit/test_commands_lint.py` returns >= 9
    - `uv run --package code-wiki-agent pytest agents/code-wiki-agent/tests/unit/test_commands_lint.py -x` exits 0
    - `test_run_lint_broken_links_skip_placeholder_targets` passes — this is the gate for phase success criterion 3
  </acceptance_criteria>
  <done>commands/lint.py with mechanical + semantic passes shipped; all 9 unit tests pass; broken-link placeholder filter verified; lattice-wiki parity in finding shape.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CLI `lint` + MCP `wiki_lint` + parity test against edge-case-vault</name>
  <files>
    agents/code-wiki-agent/src/code_wiki_agent/cli.py,
    agents/code-wiki-agent/src/code_wiki_mcp/server.py,
    agents/code-wiki-agent/tests/commands/test_lint_parity.py,
    agents/code-wiki-agent/tests/unit/test_mcp_new_tools.py
  </files>
  <read_first>
    agents/code-wiki-agent/src/code_wiki_agent/cli.py,
    agents/code-wiki-agent/src/code_wiki_mcp/server.py,
    agents/code-wiki-agent/src/code_wiki_agent/commands/lint.py,
    agents/code-wiki-agent/tests/commands/test_scan_parity.py,
    cores/vault-io/tests/fixtures/edge-case-vault
  </read_first>
  <behavior>
    - CLI: `@app.command() def lint(vault: str = "", stale_days: int = 90, log_gap_days: int = 14, json_output: bool = False)`. Calls `asyncio.run(run_lint(...))`. JSON output emits `json.dumps(dataclasses.asdict(result), indent=2)`. Human output renders a multi-section report: section headers per finding category, counts per section, indented finding lines. Partial-failure exit code 3 if `result.errors` is non-empty (CLI-06).
    - MCP: `WikiLintInput(vault_path: str = "", stale_days: int = 90, log_gap_days: int = 14)`. `WikiLintOutput` mirrors `LintResult` (all 18 fields; serialise tuples as lists). `@mcp.tool(name="wiki_lint", description="Run mechanical + semantic lint pass over the wiki.")` async function emits progress notifications (MCP-03): `progress=0,total=3,message="Starting lint"` at entry; `progress=1,total=3,message="Mechanical pass complete: {N} pages"` between mechanical and semantic — to satisfy this without splitting `run_lint`, the MCP wrapper may emit a single midpoint that fires unconditionally before the call AND after; alternatively, expose a progress callback on `run_lint`. Simpler approach for v1: emit `progress=0,total=2,message="Starting lint"` and `progress=2,total=2,message=f"Lint complete: {N} mechanical + {M} semantic findings"`. This satisfies MCP-03.
    - Parity test (`test_lint_parity.py`): use `edge-case-vault` fixture; mock `SubagentPool.run_all` to return empty semantic findings (no Bedrock calls); call `run_lint(vault_path=fixture_path)`; assert (a) `LintResult` JSON-serialises via `dataclasses.asdict` + `json.dumps` without error, (b) `result.broken_links` contains at least one entry (edge-case-vault has known broken links per RESEARCH §Test fixtures), (c) `result.missing_frontmatter` contains at least one entry (edge-case-vault has truncated frontmatter), (d) no entries in `broken_links` match the patterns `[[wiki/...]]` or `[[work/...]]` (placeholder filter assertion — phase success criterion 3 verified at parity layer too).
    - Extend `tests/unit/test_mcp_new_tools.py`: add `test_wiki_lint_tool_registered`, `test_wiki_lint_input_schema`, `test_wiki_lint_emits_progress`. After this task, the `test_mcp_new_tools.py` skip from plan-05-01 is fully replaced — assert no skips remain.
  </behavior>
  <action>
    In `cli.py`, AFTER the existing `ingest` sub-app block (added in plan-05-05) add `@app.command() def lint(...)`. Body: try `result = asyncio.run(run_lint(vault_path, stale_days=stale_days, log_gap_days=log_gap_days))` except RuntimeError → exit 1. Human-readable output: print each finding category as a section: `## Orphans (N)`, `## Broken links (N)`, etc., with bullet items below each.

    In `server.py`, AFTER `wiki_ingest` block (added in plan-05-05) and BEFORE `def main()`, add `WikiLintInput`/`WikiLintOutput` + `@mcp.tool(name="wiki_lint", description="Run mechanical + semantic lint pass over the wiki.")` async function. Wrap in try/except per PATTERNS §Error Handling (MCP). Emit 2 progress notifications.

    Overwrite Wave 0 stub `tests/commands/test_lint_parity.py` with the parity test. Mock `SubagentPool.run_all` to return `FanOutResult(successes=[(g, []) for g in groups], errors=[])` so no Bedrock is touched. Assert finding-shape invariants (NOT exact counts — finding counts depend on fixture state and may drift).

    Extend `tests/unit/test_mcp_new_tools.py` with the 3 wiki_lint tests. After this addition, also run `grep -c "pytestmark = pytest.mark.skip" agents/code-wiki-agent/tests/unit/test_mcp_new_tools.py` and confirm it returns 0 (the module-level skip from plan-05-01 should have been removed in plan-05-04 Task 2; if it lingers, remove it here).
  </action>
  <verify>
    <automated>uv run --package code-wiki-agent pytest agents/code-wiki-agent/tests/unit/test_mcp_new_tools.py agents/code-wiki-agent/tests/commands/test_lint_parity.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^def lint\|def cmd_lint' agents/code-wiki-agent/src/code_wiki_agent/cli.py` returns >= 1
    - `grep -c "wiki_lint\b" agents/code-wiki-agent/src/code_wiki_mcp/server.py` returns >= 2
    - `grep -c "stale_days\|log_gap_days" agents/code-wiki-agent/src/code_wiki_mcp/server.py` returns >= 2
    - `agents/code-wiki-agent/tests/commands/test_lint_parity.py` has no `pytest.skip` and contains real assertions including the placeholder-filter check
    - `grep -c "pytestmark = pytest.mark.skip" agents/code-wiki-agent/tests/unit/test_mcp_new_tools.py` returns 0
    - `uv run code-wiki-agent lint --help` shows `--vault`, `--stale-days`, `--log-gap-days`, `--json`
    - `uv run --package code-wiki-agent pytest agents/code-wiki-agent/tests/unit/test_mcp_new_tools.py agents/code-wiki-agent/tests/commands/test_lint_parity.py -x` exits 0
    - End-to-end: `uv run --package code-wiki-agent pytest agents/code-wiki-agent/tests/unit agents/code-wiki-agent/tests/commands -x` exits 0 (full phase test suite green; Wave 6 gate per VALIDATION.md)
  </acceptance_criteria>
  <done>CLI `lint` and MCP `wiki_lint` shipped; parity test green; placeholder filter verified at parity layer; full phase test suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vault filesystem → lint reads | Reads all pages under wiki; same user trust |
| Linter LLM response → result string | LLM output stored only in `LintResult.semantic_findings` — never written to vault (D-10) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06-01 | DoS | Vault contains 10k pages → mechanical pass OOMs | accept | Same scale lattice-wiki handles today; if it becomes a problem, paginate the page walk in a follow-up |
| T-05-06-02 | Tampering | Linter LLM returns malicious finding text | accept | Findings are returned to user in a report only — no eval, no write-back; user reads and judges |
| T-05-06-03 | Information Disclosure | Linter prompts include vault page content | accept | Same user → Bedrock data plane; user explicitly invoked lint |
| T-05-06-04 | Tampering | Inline scan() port introduces a subtle behavior diff vs. lattice-wiki | mitigate | Port verbatim with import swaps only; parity test asserts known finding shape on edge-case-vault |
</threat_model>

<verification>
- Both tasks complete; tests green.
- Full phase test suite: `uv run pytest cores/vault-io/tests agents/code-wiki-agent/tests -x` exits 0.
- `_StdoutGuard` import order preserved in server.py (no imports between lines 15-80).
- ROADMAP Phase 5 plans list can be updated to mark all 6 plans as `[x]` after this lands.
</verification>

<success_criteria>
1. `code-wiki-agent lint --json` against `edge-case-vault` emits a JSON object with all 18 `LintResult` fields.
2. `result.broken_links` contains zero entries matching `[[wiki/...]]` or `[[work/<slug>]]` patterns even when the vault contains such links — phase success criterion 3 verified.
3. Default `--stale-days=90` and `--log-gap-days=14` are honored (verified via test).
4. MCP `wiki_lint` is registered with typed Pydantic schemas (MCP-01) and emits at least 2 progress notifications (MCP-03).
5. NO write-back to vault (D-10) — vault checksum unchanged after `run_lint` (verified via test).
6. Full Phase 5 test suite green (Wave 6 gate per VALIDATION.md): `uv run pytest cores/vault-io/tests agents/code-wiki-agent/tests -x` exits 0.
</success_criteria>

<output>
After completion, create `.planning/phases/05-remaining-commands/05-06-SUMMARY.md` recording:
- Final `LintResult` field set
- The 3 `LINTER_*_SYSTEM` prompt texts
- Whether the inline scan() port produced any port-time corrections (line numbers + reasons)
- Confirmation that the placeholder filter test passes (phase success criterion 3)
- Pointer to the full-suite green test command for the phase verifier
</output>
