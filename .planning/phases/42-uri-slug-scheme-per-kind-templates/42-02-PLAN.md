---
phase: 42-uri-slug-scheme-per-kind-templates
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/graph-io/src/graph_io/uri.py
  - packages/graph-io/tests/test_uri.py
  - packages/wiki-io/src/wiki_io/assets/page-templates/entity-repository.md
  - packages/wiki-io/src/wiki_io/assets/page-templates/entity-domain.md
  - packages/wiki-io/src/wiki_io/assets/page-templates/entity-package.md
  - packages/wiki-io/src/wiki_io/assets/page-templates/entity-package-family.md
  - packages/wiki-io/src/wiki_io/assets/page-templates/entity-plugin.md
  - packages/wiki-io/src/wiki_io/assets/page-templates/entity-dependency.md
  - packages/wiki-io/src/wiki_io/assets/page-templates/entity-test-suite.md
autonomous: true
requirements:
  - URI-03

must_haves:
  truths:
    - "Three new URI builder functions exist in `graph_io.uri`: `package_family_uri(name)`, `plugin_uri(name)`, `dependency_uri(ecosystem, name)`"
    - "Each new builder returns a correctly-shaped string matching the v1.8 URI convention (e.g. `package_family:aws`, `plugin:graph-wiki`, `dependency:pypi/boto3`)"
    - "Seven entity-* templates exist under `packages/wiki-io/src/wiki_io/assets/page-templates/`, one per admitted kind"
    - "Every template declares `kind:` frontmatter with the underscore-form value matching ADMITTED_KINDS (D-02, D-18)"
    - "Every template has a `## Narrative` H2 section per D-16 — no HTML-comment sentinels"
    - "Workspace tests run green: `uv run --package graph-io pytest tests/test_uri.py -x` exits 0"
  artifacts:
    - path: packages/graph-io/src/graph_io/uri.py
      provides: "3 new URI builder functions appended to existing 7 builders"
      contains: "package_family_uri"
    - path: packages/graph-io/tests/test_uri.py
      provides: "Unit tests for 3 new URI builders (asserts exact return string for each)"
      min_lines: 30
    - path: packages/wiki-io/src/wiki_io/assets/page-templates/entity-package.md
      provides: "Per-kind template for `kind: package` entities (D-17 body shape: H1 + ## Narrative + canonical sections)"
      contains: "kind: package"
  key_links:
    - from: "packages/graph-io/src/graph_io/uri.py"
      to: "(none — new builders are standalone, no dependencies on other modules)"
      via: "—"
      pattern: "—"
    - from: "packages/wiki-io/src/wiki_io/assets/page-templates/entity-*.md"
      to: "ADMITTED_KINDS in entity_writer.py (cross-validation in Plan 03)"
      via: "frontmatter kind: value"
      pattern: "^kind: (repository|domain|package|package_family|plugin|dependency|test_suite)$"
---

<objective>
Provision the URI builders for the 3 new v1.8 kinds and the 7 entity-page templates. This is the mechanical authoring half of Phase 42 — code additions follow existing patterns line-for-line.

Purpose: Phase 43's `write_entities()` needs both pieces — URI builders to compose URIs from graph rows, and templates to instantiate new entity pages from. Locking the URI shape and template body in Phase 42 means Phase 43 only writes glue, not contracts.

Output: 3 new URI builder functions, 1 test file (extended or created), and 7 markdown templates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/42-uri-slug-scheme-per-kind-templates/42-CONTEXT.md
@.planning/phases/42-uri-slug-scheme-per-kind-templates/42-RESEARCH.md
@packages/graph-io/src/graph_io/uri.py
@packages/wiki-io/src/wiki_io/assets/page-templates/package-family.md
@packages/wiki-io/src/wiki_io/assets/page-templates/dependency.md
@CLAUDE.md

<interfaces>
<!-- Existing URI builder signatures in graph_io/uri.py (read-only reference). -->
<!-- New builders match this style: one-line signature, single f-string body, snake_case kind prefix. -->

```python
# Existing — DO NOT modify
def repo_uri(ctx: RepoContext) -> str:
    return f"repo:{ctx.org}/{ctx.repo}"

def pkg_uri(ctx: RepoContext, name: str) -> str:
    return f"pkg:{ctx.org}/{ctx.repo}/{name}"

def test_suite_uri(ctx: RepoContext, suite_name: str) -> str:
    return f"test_suite:{ctx.org}/{ctx.repo}/{suite_name}"
```

```python
# New — three additions appended at the end of uri.py (no RepoContext; concept-level not repo-scoped)
def package_family_uri(name: str) -> str:
    return f"package_family:{name}"

def plugin_uri(name: str) -> str:
    return f"plugin:{name}"

def dependency_uri(ecosystem: str, name: str) -> str:
    return f"dependency:{ecosystem}/{name}"
```

Template path discrepancy reconciliation: REQUIREMENTS.md URI-03 mentions `wiki_io/templates/entities/` but CONTEXT.md D-17 + the existing project convention places templates at `packages/wiki-io/src/wiki_io/assets/page-templates/`. This plan honors CONTEXT.md (locked decisions are source of truth per GSD workflow). Plan 03 propagates the corrected path back to REQUIREMENTS.md.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add 3 new URI builders to graph_io/uri.py with tests</name>
  <files>packages/graph-io/src/graph_io/uri.py, packages/graph-io/tests/test_uri.py</files>
  <read_first>
    - packages/graph-io/src/graph_io/uri.py (full file — match style and append at end, after `test_suite_uri`)
    - packages/graph-io/tests/ (run `ls packages/graph-io/tests/` to confirm whether `test_uri.py` exists; if missing, create it; if present, extend it)
    - .planning/phases/42-uri-slug-scheme-per-kind-templates/42-CONTEXT.md (D-04 — minimal signatures, no `RepoContext` for the 3 new builders)
    - .planning/phases/42-uri-slug-scheme-per-kind-templates/42-RESEARCH.md (`Pattern 3` URI builder signature parity)
  </read_first>
  <behavior>
    - `test_package_family_uri`: `assert package_family_uri("aws") == "package_family:aws"`
    - `test_plugin_uri`: `assert plugin_uri("graph-wiki") == "plugin:graph-wiki"`
    - `test_dependency_uri`: `assert dependency_uri("pypi", "boto3") == "dependency:pypi/boto3"`
    - `test_dependency_uri_npm`: `assert dependency_uri("npm", "react") == "dependency:npm/react"` — multi-ecosystem coverage
    - Tests for existing builders (`repo_uri`, `pkg_uri`, `domain_uri`, `test_suite_uri`, `entry_point_uri`, `subpkg_uri`, `file_uri`) are added IF AND ONLY IF `test_uri.py` did not exist before this task. If it already existed with its own coverage, do not duplicate.
  </behavior>
  <action>Append the 3 new builder functions to `packages/graph-io/src/graph_io/uri.py` AFTER the existing `domain_uri` function (line ~40) and BEFORE the `_SSH_REMOTE_RE` regex (line ~43). Body of each is a single f-string per D-04. Keep `from __future__ import annotations` already at module top.

Then in `packages/graph-io/tests/test_uri.py`:
- If file exists: append the 4 tests from `<behavior>` to it.
- If file missing: create it with `from __future__ import annotations`, `from graph_io.uri import package_family_uri, plugin_uri, dependency_uri, RepoContext, repo_uri, pkg_uri, domain_uri, test_suite_uri, entry_point_uri, subpkg_uri, file_uri`, then BOTH the 4 new-builder tests AND minimal smoke tests for the 7 existing builders (one assertion each — keeps tests file complete).

Do NOT modify `RepoContext`, `parse_remote_url`, or any existing builder. Do NOT add type imports beyond what's already in the module.</action>
  <verify>
    <automated>uv run --package graph-io pytest tests/test_uri.py -x</automated>
  </verify>
  <done>
    - `uri.py` contains 3 new builders with exactly the signatures in `<interfaces>`
    - `test_uri.py` exists with at least the 4 new-builder tests
    - All tests pass; no regressions in existing graph-io tests (`uv run --package graph-io pytest -x` exits 0)
    - Existing functions in `uri.py` byte-identical to before (verify with `git diff packages/graph-io/src/graph_io/uri.py` showing only additions)
  </done>
</task>

<task type="auto">
  <name>Task 2: Author 7 entity-*.md templates under wiki-io/assets/page-templates/</name>
  <files>packages/wiki-io/src/wiki_io/assets/page-templates/entity-repository.md, packages/wiki-io/src/wiki_io/assets/page-templates/entity-domain.md, packages/wiki-io/src/wiki_io/assets/page-templates/entity-package.md, packages/wiki-io/src/wiki_io/assets/page-templates/entity-package-family.md, packages/wiki-io/src/wiki_io/assets/page-templates/entity-plugin.md, packages/wiki-io/src/wiki_io/assets/page-templates/entity-dependency.md, packages/wiki-io/src/wiki_io/assets/page-templates/entity-test-suite.md</files>
  <read_first>
    - packages/wiki-io/src/wiki_io/assets/page-templates/package-family.md (existing single-file template — body-shape reference)
    - packages/wiki-io/src/wiki_io/assets/page-templates/dependency.md (existing single-file template — frontmatter-shape reference)
    - .planning/phases/42-uri-slug-scheme-per-kind-templates/42-CONTEXT.md (D-16, D-17, D-18 — narrative H2, per-kind canonical sections, underscore `kind:` values)
    - .planning/phases/42-uri-slug-scheme-per-kind-templates/42-RESEARCH.md (`Pattern 2` template body shape; `Pitfall 3` hyphen-vs-underscore drift)
  </read_first>
  <action>Create 7 new template files, one per admitted kind. Each template MUST follow this exact shape (D-17):

1. **YAML frontmatter** opening with `---`, declaring AT MINIMUM:
   - `title: <placeholder name>` (e.g. `<Repository Name>`, `<Domain Name>`)
   - `uri: <kind-uri-placeholder>` (e.g. `<repo-uri>`, `<package-family-uri>`)
   - `kind: <underscore-form-value>` — MUST use underscore-form per D-02/D-18 (`kind: package_family`, NOT `kind: package-family`)
   - `graph_name: <graph-name>` placeholder
   - `last_scan_at: <YYYY-MM-DD>` placeholder
   - All scanner-owned keys relevant to this kind, populated with empty placeholders (e.g. for `entity-package.md`: `domains: []`, `depends_on: []`, `test_suites: []`, `entry_points: []`, `language: ""`, `version: ""`)
   - `updated: <YYYY-MM-DD>` placeholder
   - Close with `---`
2. **H1 title** matching frontmatter title (e.g. `# <Repository Name>`)
3. **`## Narrative`** H2 followed by exactly the placeholder line: `_(scanner will populate on next scan)_`
4. **Per-kind canonical H2 sections**, each followed by a one-line placeholder bullet (humans fill in). Sections per kind (D-17):

| Template | Canonical H2 sections after `## Narrative` |
|----------|--------------------------------------------|
| `entity-repository.md` | `## Overview`, `## Layout` |
| `entity-domain.md` | `## Why this domain exists`, `## Contained packages`, `## Decisions` |
| `entity-package.md` | `## Key patterns`, `## Gotchas`, `## Related` |
| `entity-package-family.md` | `## What this family covers`, `## Gotchas` |
| `entity-plugin.md` | `## What it is`, `## How we use it` |
| `entity-dependency.md` | `## Why we depend on this`, `## Gotchas / workarounds` |
| `entity-test-suite.md` | `## Coverage notes`, `## Gotchas` |

D-16 hard rule: the narrative-region marker is the literal text `## Narrative` — NOT an HTML comment, NOT `<!-- narrative -->`, NOT any sentinel form. Phase 45's LLM scanner targets the region under this exact H2.

Match the frontmatter-padding style of existing templates (`package-family.md`, `dependency.md`): comments after frontmatter values are OK; trailing whitespace is OK; close `---` at end of frontmatter.

Filenames use hyphens (`entity-package-family.md`); `kind:` values use underscores (`kind: package_family`) — see RESEARCH.md `Pitfall 3` for why these are different layers.</action>
  <verify>
    <automated>python3 -c "
import frontmatter, pathlib
admitted = {'repository','domain','package','package_family','plugin','dependency','test_suite'}
template_dir = pathlib.Path('packages/wiki-io/src/wiki_io/assets/page-templates')
templates = sorted(template_dir.glob('entity-*.md'))
assert len(templates) == 7, f'expected 7 entity templates, got {len(templates)}: {[t.name for t in templates]}'
seen = set()
for t in templates:
    fm = frontmatter.load(t)
    kind = fm.get('kind')
    assert kind in admitted, f'{t.name}: kind {kind!r} not in ADMITTED_KINDS'
    body = fm.content
    assert '## Narrative' in body, f'{t.name}: missing ## Narrative H2'
    seen.add(kind)
assert seen == admitted, f'missing kinds: {admitted - seen}'
print('OK 7 templates, all kinds covered, all have ## Narrative')
"</automated>
  </verify>
  <done>
    - All 7 files exist at the named paths under `packages/wiki-io/src/wiki_io/assets/page-templates/`
    - Each file parses as valid frontmatter (no YAML syntax errors)
    - Each file's `kind:` value is in `{repository, domain, package, package_family, plugin, dependency, test_suite}` — underscore-form
    - Each file contains an exact `## Narrative` line followed by `_(scanner will populate on next scan)_`
    - The 7 templates cover all 7 admitted kinds (one-to-one mapping; no duplicates, no missing kinds)
    - Per-kind canonical H2 sections present per the table in `<action>`
    - Inline `python3` verifier returns `OK 7 templates, all kinds covered, all have ## Narrative`
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internal-only | URI builders are pure string concatenation; templates are static markdown assets shipped in the wheel. No external untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-03 | Tampering | URI-builder output injecting `..` or `/` into slug | accept | The 3 new builders accept `name` and `ecosystem` strings; these come from `graph-io` scan code paths, not user input. `encode_slug` in `entity_writer.py` defangs path separators (`/` → `__`). Risk: zero practical exposure. |
| T-42-04 | Tampering | Template `kind:` drift between filename and frontmatter | mitigate | `<verify>` block on Task 2 asserts every template's `kind:` value is in `ADMITTED_KINDS`. Plan 03's cross-cutting test re-asserts after Plan 01 lands. |
</threat_model>

<verification>
- 3 new URI builders importable from `graph_io.uri`
- 7 entity templates exist and each declares a valid `kind:` value
- Every template has `## Narrative` H2 per D-16
- `uv run --package graph-io pytest tests/test_uri.py -x` exits 0
- Inline frontmatter validator returns OK
- No regressions: `uv run --package graph-io pytest -x` exits 0
</verification>

<success_criteria>
- 3 URI builders work for the 7 worked-example URIs in CONTEXT.md D-01 (build them via the builders, encode them, decode them — round trip)
- 7 templates cover the 7 admitted kinds exactly (bijection)
- Templates ready for Phase 43's `write_entities()` to instantiate
- No file overlap with Plan 01 (Wave-1 parallelism preserved)
</success_criteria>

<output>
Create `.planning/phases/42-uri-slug-scheme-per-kind-templates/42-02-SUMMARY.md` when done. Summary captures: whether `test_uri.py` existed before this plan or was created fresh, the 7 final template filenames, any template-body decisions made under "Claude's Discretion" (D-17 says "Phase 42 implementer may tune them during template authoring").
</output>
