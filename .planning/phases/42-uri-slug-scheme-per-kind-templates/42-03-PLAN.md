---
phase: 42-uri-slug-scheme-per-kind-templates
plan: 03
type: execute
wave: 2
depends_on:
  - 42-01
  - 42-02
files_modified:
  - packages/wiki-io/src/wiki_io/init_vault.py
  - packages/wiki-io/tests/test_init_vault.py
  - packages/wiki-io/tests/test_entity_templates.py
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - URI-04
  - URI-03

must_haves:
  truths:
    - "`FIXED_VAULT_DIRS` in `init_vault.py` contains the string `\"entities\"` after this plan"
    - "Running `init_wiki(...)` on a fresh tmp directory produces a `wiki/entities/` directory containing a `_index.md` file with the sentinel-comment content"
    - "A new cross-cutting test in `tests/test_entity_templates.py` asserts every entity template's `kind:` value is a member of `ADMITTED_KINDS` (closes the loop between Plan 01's constant and Plan 02's templates)"
    - "REQUIREMENTS.md URI-03 text references the actual template path (`packages/wiki-io/src/wiki_io/assets/page-templates/`) and the actual narrative-region marker (`## Narrative` H2)"
    - "ROADMAP.md Phase 42 success criterion #3 references the same corrected path and marker"
    - "All workspace tests run green: `uv run pytest` exits 0"
  artifacts:
    - path: packages/wiki-io/src/wiki_io/init_vault.py
      provides: "FIXED_VAULT_DIRS contains 'entities'; init_wiki writes wiki/entities/_index.md sentinel"
      contains: "entities"
    - path: packages/wiki-io/tests/test_entity_templates.py
      provides: "Cross-cutting validator: every entity template's kind is in ADMITTED_KINDS, every template has ## Narrative H2"
      min_lines: 30
  key_links:
    - from: "packages/wiki-io/tests/test_entity_templates.py"
      to: "packages/wiki-io/src/wiki_io/entity_writer.py::ADMITTED_KINDS (Plan 01) AND packages/wiki-io/src/wiki_io/assets/page-templates/entity-*.md (Plan 02)"
      via: "Python import + filesystem glob"
      pattern: "from wiki_io.entity_writer import ADMITTED_KINDS"
    - from: "packages/wiki-io/src/wiki_io/init_vault.py"
      to: "wiki_path / 'entities' / '_index.md'"
      via: "file write"
      pattern: "entities.*_index.md"
---

<objective>
Wire the new entity lane into vault bootstrap (`init_vault.py`), add a cross-cutting test that closes the loop between Plan 01's `ADMITTED_KINDS` constant and Plan 02's 7 templates, and reconcile the documentation drift in REQUIREMENTS.md / ROADMAP.md (template path + narrative marker).

Purpose: Finalize Phase 42's foundation by (a) ensuring fresh vault bootstraps create the `wiki/entities/` directory, (b) catching any future kind/template drift in one cross-cutting test, and (c) eliminating two textual conflicts between REQUIREMENTS.md/ROADMAP and CONTEXT.md (the locked decisions).

Output: 1 source edit (`init_vault.py`), 1 test edit (`test_init_vault.py` — new test cases), 1 new test file (`test_entity_templates.py`), 2 docs edits (REQUIREMENTS.md, ROADMAP.md).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/42-uri-slug-scheme-per-kind-templates/42-CONTEXT.md
@.planning/phases/42-uri-slug-scheme-per-kind-templates/42-RESEARCH.md
@.planning/phases/42-uri-slug-scheme-per-kind-templates/42-VALIDATION.md
@.planning/phases/42-uri-slug-scheme-per-kind-templates/42-01-PLAN.md
@.planning/phases/42-uri-slug-scheme-per-kind-templates/42-02-PLAN.md
@packages/wiki-io/src/wiki_io/init_vault.py
@packages/wiki-io/tests/test_init_vault.py
@packages/wiki-io/src/wiki_io/entity_writer.py
@CLAUDE.md

<interfaces>
<!-- After Plan 01 + Plan 02 land, the following are available — this plan depends on them. -->

```python
# From Plan 01 (wiki-io/entity_writer.py)
ADMITTED_KINDS: frozenset[str]    # 7 underscore-form kinds
SCANNER_OWNED_KEYS: frozenset[str]
def encode_slug(uri: str) -> str: ...
def decode_slug(slug: str) -> str: ...
```

Plan 02 created 7 files under `packages/wiki-io/src/wiki_io/assets/page-templates/entity-*.md`. Each declares `kind:` matching `ADMITTED_KINDS` and contains `## Narrative` H2.

The existing `init_vault.py::init_wiki` function flow:
1. Validates target dir is empty (or `--force`)
2. Creates `raw/` and `work/` workspace siblings
3. Calls `_workspace_init` (registers plugin, writes .graph-wiki.yaml)
4. Resolves pinned containers
5. Loops over `structural_dirs + FIXED_VAULT_DIRS` calling `mkdir(parents=True, exist_ok=True)`
6. Writes section index stubs (`SECTION_INDEX_STUBS`)
7. Renders tool templates
8. Copies page-templates
9. Writes .gitignore

This plan inserts ONE line into `FIXED_VAULT_DIRS` and ONE block writing `wiki/entities/_index.md` after the directory-creation loop (step 5).

Path reconciliation: REQUIREMENTS.md URI-03 currently says `packages/wiki-io/src/wiki_io/templates/entities/` (4 mentions); CONTEXT.md D-17 + existing project convention places templates at `packages/wiki-io/src/wiki_io/assets/page-templates/`. ROADMAP.md success criterion #3 also says `wiki_io/templates/entities/`. Both docs need correction. Similarly, both docs reference a "prose sentinel comment reserving the narrative region" but D-16 locks to `## Narrative` H2. Both edits are mechanical text reconciliation, not scope changes.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 'entities' to FIXED_VAULT_DIRS + write _index.md sentinel</name>
  <files>packages/wiki-io/src/wiki_io/init_vault.py</files>
  <read_first>
    - packages/wiki-io/src/wiki_io/init_vault.py (full file — locate `FIXED_VAULT_DIRS` and the `init_wiki` function's directory-creation loop)
    - .planning/phases/42-uri-slug-scheme-per-kind-templates/42-CONTEXT.md (D-14, D-15)
    - .planning/phases/42-uri-slug-scheme-per-kind-templates/42-RESEARCH.md (`Code Example 2` — exact edit pattern for the _index.md sentinel write)
  </read_first>
  <action>Make two surgical edits to `packages/wiki-io/src/wiki_io/init_vault.py`:

1. **Add `"entities"` to `FIXED_VAULT_DIRS`** (currently lines 46-53). Insert it BEFORE `"sources"` (alphabetical-ish order keeps file diff readable; the existing list isn't strictly alphabetical so honoring the existing grouping is fine — `concepts`, `architecture`, `adrs`, `entities`, `sources`, `dependencies`, `.templates` reads naturally).

2. **Write `wiki/entities/_index.md` sentinel** AFTER the directory-creation loop (currently lines 194-195 inside `init_wiki`):
   ```python
   # NEW BLOCK — inserted after the FIXED_VAULT_DIRS mkdir loop
   entities_index = wiki_path / "entities" / "_index.md"
   if not entities_index.exists():
       entities_index.write_text(
           "<!-- generated by graph-wiki-agent scan; "
           "see ../index.md for the canonical listing -->\n",
           encoding="utf-8",
       )
       installed_files.append(str(entities_index.relative_to(wiki_path)))
   ```

D-15: the sentinel content is a single comment line. Do NOT add any structural markdown content beneath it; the real entity index lives at `wiki/index.md` (Phase 44 generator).

Do NOT modify any other part of the file. Do NOT remove existing entries from `FIXED_VAULT_DIRS`. Do NOT add `"entities"` to `SECTION_INDEX_STUBS` — entities use a custom `_index.md` sentinel, not the section-stub treatment.</action>
  <verify>
    <automated>uv run --package wiki-io python -c "from wiki_io.init_vault import FIXED_VAULT_DIRS; assert 'entities' in FIXED_VAULT_DIRS, FIXED_VAULT_DIRS; print('OK')"</automated>
  </verify>
  <done>
    - `FIXED_VAULT_DIRS` contains the literal string `"entities"`
    - `init_wiki` body contains a new block writing `wiki/entities/_index.md` with the sentinel comment text (verified by reading the diff)
    - `git diff packages/wiki-io/src/wiki_io/init_vault.py` shows additions only (no deletions or unrelated modifications)
    - Inline `python -c` verifier returns `OK`
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test cases for URI-04 (vault bootstraps with entities/)</name>
  <files>packages/wiki-io/tests/test_init_vault.py</files>
  <read_first>
    - packages/wiki-io/src/wiki_io/init_vault.py (post-Task-1 — confirm the edits landed before writing tests against them)
    - packages/wiki-io/tests/test_init_vault.py (existing test style + `_build_v2_repo` fixture pattern)
    - packages/wiki-io/tests/conftest.py (fixtures `tmp_repo`, `vault_path`, `write_file` helper)
    - .planning/phases/42-uri-slug-scheme-per-kind-templates/42-CONTEXT.md (D-14, D-15)
    - .planning/phases/42-uri-slug-scheme-per-kind-templates/42-RESEARCH.md (Open Question Q3 — two-test approach: trivial constant assertion + actual write-side test)
  </read_first>
  <behavior>
    - `test_entities_in_fixed_vault_dirs`: trivial constant-shape test. `assert "entities" in FIXED_VAULT_DIRS`. Catches regression if anyone removes the entry.
    - `test_entities_dir_bootstrapped_after_init_wiki`: end-to-end test that exercises `init_wiki` against a synthetic repo (using the existing `_build_v2_repo` pattern as a model) and asserts (a) `wiki/entities/` directory exists, (b) `wiki/entities/_index.md` exists, (c) its content is the sentinel comment line (matches `<!--.*generated by graph-wiki-agent scan.*-->`), (d) the relative path is in the returned `installed_files` list.
  </behavior>
  <action>Append two test functions to `packages/wiki-io/tests/test_init_vault.py`:

1. `test_entities_in_fixed_vault_dirs`:
   ```python
   def test_entities_in_fixed_vault_dirs() -> None:
       from wiki_io.init_vault import FIXED_VAULT_DIRS
       assert "entities" in FIXED_VAULT_DIRS
   ```

2. `test_entities_dir_bootstrapped_after_init_wiki(tmp_path)`:
   - Use the existing `_build_v2_repo(tmp_path)` helper as a model — build a synthetic v2-layout repo with `graph-wiki/wiki/` workspace.
   - Call `init_wiki(wiki=workspace/'wiki', repo=repo, topic='test-entities', tool='claude-code', force=True, non_interactive=True)`.
   - If `init_wiki` calls into `_workspace_init` and that has hard side effects (git init, .graph-wiki.yaml write) — and the existing `_build_v2_repo` already pre-creates `.graph-wiki.yaml` so this should work. If a side effect blocks the test, fall back to testing only `FIXED_VAULT_DIRS` membership in test 1 and exercise the `_index.md` write via a unit test that monkeypatches or directly calls only the directory-creation + sentinel block.
   - Assert `(workspace / 'wiki' / 'entities').is_dir()`
   - Assert `(workspace / 'wiki' / 'entities' / '_index.md').is_file()`
   - Assert the file content matches `<!--\s*generated by graph-wiki-agent scan` (regex; the trailing `; see ../index.md for the canonical listing -->\n` is the rest of the line per D-15).

If the end-to-end approach fails because `_workspace_init` has hard requirements not satisfiable in tmp_path, downgrade test 2 to a unit-style test that monkeypatches `_workspace_init` to a no-op via `pytest.MonkeyPatch` and proceeds. Document the chosen approach in the test docstring.</action>
  <verify>
    <automated>uv run --package wiki-io pytest tests/test_init_vault.py -x</automated>
  </verify>
  <done>
    - Both new test cases pass
    - No regressions in existing `test_init_vault.py` cases
    - Test docstrings reference URI-04 + D-14/D-15
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Cross-cutting entity-template validator test</name>
  <files>packages/wiki-io/tests/test_entity_templates.py</files>
  <read_first>
    - packages/wiki-io/src/wiki_io/entity_writer.py (Plan 01 — confirms `ADMITTED_KINDS` available)
    - packages/wiki-io/src/wiki_io/assets/page-templates/ (run `ls` — confirms Plan 02's 7 entity-*.md templates landed)
    - packages/wiki-io/tests/test_entity_writer.py (Plan 01 — match test style; do not duplicate)
    - .planning/phases/42-uri-slug-scheme-per-kind-templates/42-CONTEXT.md (D-16, D-18; Pitfall 5)
  </read_first>
  <behavior>
    - `test_seven_entity_templates_exist`: asserts exactly 7 files match `assets/page-templates/entity-*.md`.
    - `test_each_template_kind_in_admitted_kinds`: parses each template's frontmatter with `python-frontmatter`, asserts `template["kind"] in ADMITTED_KINDS` per D-18.
    - `test_each_template_has_narrative_h2`: asserts the literal substring `\n## Narrative\n` appears in each template's body content per D-16.
    - `test_templates_cover_all_admitted_kinds`: collects the 7 templates' `kind` values into a set, asserts it equals `ADMITTED_KINDS` (bijection check).
  </behavior>
  <action>Create `packages/wiki-io/tests/test_entity_templates.py` as a NEW file (not in `test_entity_writer.py` — see Plan 02 file-ownership boundary). Top of file:

```python
"""Cross-cutting validation: entity templates align with ADMITTED_KINDS (URI-03 / D-18 / Pitfall 5)."""

from __future__ import annotations

from pathlib import Path

import frontmatter
import pytest

from wiki_io.entity_writer import ADMITTED_KINDS

TEMPLATE_DIR = (
    Path(__file__).resolve().parent.parent
    / "src" / "wiki_io" / "assets" / "page-templates"
)
ENTITY_TEMPLATES = sorted(TEMPLATE_DIR.glob("entity-*.md"))
```

Then implement the 4 tests from `<behavior>`. Use `pytest.mark.parametrize("template_path", ENTITY_TEMPLATES, ids=[p.name for p in ENTITY_TEMPLATES])` for per-template tests so failures point at the specific template.

This file lives in `tests/` (a different directory from Plan 02's template files) — no Wave-1 file overlap. The dependency on Plan 01's `ADMITTED_KINDS` and Plan 02's templates is what places this plan in Wave 2.</action>
  <verify>
    <automated>uv run --package wiki-io pytest tests/test_entity_templates.py -x</automated>
  </verify>
  <done>
    - File `packages/wiki-io/tests/test_entity_templates.py` exists
    - All 4 tests pass (parametrized tests produce 7 sub-tests each for the per-template assertions — 1 + 7 + 7 + 1 = 16 sub-tests)
    - Failure messages identify the specific template by filename if a kind drifts
  </done>
</task>

<task type="auto">
  <name>Task 4: Reconcile REQUIREMENTS.md and ROADMAP.md template-path + narrative-marker text</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (locate URI-03 — single line under `### URI — URI Slug Scheme & Per-Kind Templates`)
    - .planning/ROADMAP.md (locate Phase 42 success criterion #3 — under `### Phase 42:`)
    - .planning/phases/42-uri-slug-scheme-per-kind-templates/42-CONTEXT.md (D-16, D-17 — the locked decisions)
    - .planning/phases/42-uri-slug-scheme-per-kind-templates/42-RESEARCH.md (`Open Question Q1` + `Open Question Q2` — reconciliation plan)
  </read_first>
  <action>Two surgical text edits:

1. **REQUIREMENTS.md URI-03**: Replace `packages/wiki-io/src/wiki_io/templates/entities/` with `packages/wiki-io/src/wiki_io/assets/page-templates/`. Replace `reserves prose region with sentinel comment for LLM narrative` with `reserves prose region with a \`## Narrative\` H2 section for LLM narrative`. Keep the rest of URI-03 unchanged (the 7 filenames, the `kind:` frontmatter requirement).

2. **ROADMAP.md Phase 42 success criterion #3**: Replace `wiki_io/templates/entities/` with `wiki_io/assets/page-templates/`. Replace `prose sentinel comment reserving the narrative region` with `a \`## Narrative\` H2 section reserving the narrative region`.

These are mechanical text reconciliations between REQUIREMENTS.md/ROADMAP and CONTEXT.md (the locked-decisions source of truth). They do not change scope — they correct documentation drift surfaced during this phase's planning.

Do NOT modify any other line in REQUIREMENTS.md or ROADMAP.md. Specifically: do not touch URI-01, URI-02, URI-04, URI-05, URI-06, the v1.8 milestone heading, or success criteria #1, #2, #4, #5 of Phase 42.</action>
  <verify>
    <automated>grep -F "packages/wiki-io/src/wiki_io/assets/page-templates/" .planning/REQUIREMENTS.md && grep -F "Narrative" .planning/REQUIREMENTS.md && grep -F "wiki_io/assets/page-templates/" .planning/ROADMAP.md && grep -F "Narrative" .planning/ROADMAP.md && ! grep -F "wiki_io/templates/entities/" .planning/REQUIREMENTS.md && ! grep -F "wiki_io/templates/entities/" .planning/ROADMAP.md</automated>
  </verify>
  <done>
    - REQUIREMENTS.md URI-03 line references the corrected path and `## Narrative` H2
    - ROADMAP.md Phase 42 success criterion #3 references the corrected path and `## Narrative` H2
    - The strings `wiki_io/templates/entities/` and `prose sentinel comment` no longer appear in either file
    - No other lines modified (`git diff` shows only 2 line-level changes per file)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internal-only | All edits in this plan are to project source/docs. No external network, no untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-05 | Tampering | `init_vault.py` sentinel write could overwrite an existing `_index.md` if force=True | accept | Task 1 guards with `if not entities_index.exists()`. `force=True` on `init_wiki` already validates target dir is empty before reaching this code path. Risk: zero in practice. |
| T-42-06 | Tampering | REQUIREMENTS.md / ROADMAP.md edits could accidentally modify other requirements | mitigate | Task 4 acceptance includes `git diff` review — additions/changes confined to URI-03 + Phase 42 success criterion #3. |
</threat_model>

<verification>
- `FIXED_VAULT_DIRS` contains `"entities"`
- Running `init_wiki` produces `wiki/entities/_index.md` with sentinel content
- Cross-cutting template test asserts the 7 templates' `kind:` values equal `ADMITTED_KINDS` exactly
- REQUIREMENTS.md URI-03 and ROADMAP.md success criterion #3 reference the actual project paths (`assets/page-templates/`) and the actual narrative marker (`## Narrative` H2)
- Full workspace test suite green: `uv run pytest` exits 0
</verification>

<success_criteria>
- All 5 phase success criteria from ROADMAP.md are met simultaneously after this plan lands:
  1. Property test of ≥1,000 URIs passes (Plan 01)
  2. Round-trip stable (Plan 01)
  3. 7 templates exist with `kind:` + `## Narrative` (Plan 02 + Plan 03 validator)
  4. Fresh vault contains `wiki/entities/_index.md` (this plan, Tasks 1+2)
  5. Frozenset constants present and disjoint from human keys (Plan 01)
- Documentation drift eliminated: REQUIREMENTS.md / ROADMAP / CONTEXT.md all agree on template path and narrative marker
</success_criteria>

<output>
Create `.planning/phases/42-uri-slug-scheme-per-kind-templates/42-03-SUMMARY.md` when done. Summary captures: the chosen approach for test 2 (end-to-end vs. monkeypatched), exact diff of REQUIREMENTS.md and ROADMAP.md (line count), confirmation that the full workspace test suite passed, and a one-line phase-completion attestation: "Phase 42 contracts locked; ready for Phase 43 entity-writer implementation."
</output>
