---
phase: 04-eval-harness
plan: 04
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - cores/eval-harness/src/eval_harness/baseline.py
  - cores/eval-harness/tests/test_baseline.py
  - eval/README.md
autonomous: true
requirements:
  - EVAL-03
  - EVAL-08

must_haves:
  truths:
    - "_build_cmd() produces correct claude -p subprocess command with required flags"
    - "Baseline JSON snapshot includes case_id, model_arn, prompt_hash, vault_content_hash, timestamp_utc, seed"
    - "BaselineRecorder.record() saves snapshot to eval/baselines/<case_id>.json"
    - "Subprocess is never launched with shell=True"
    - "Prompt is the final positional argument in the command list"
    - "eval/README.md documents how to record a baseline"
  artifacts:
    - path: "cores/eval-harness/src/eval_harness/baseline.py"
      provides: "RunResult, _build_cmd, run_headless, BaselineRecorder"
      exports: ["RunResult", "_build_cmd", "run_headless", "BaselineRecorder", "EVAL_SYSTEM_PROMPT_QA"]
    - path: "cores/eval-harness/tests/test_baseline.py"
      provides: "unit tests for _build_cmd, RunResult, baseline JSON schema"
    - path: "eval/README.md"
      provides: "Recording a Baseline section with prerequisites, command, and output structure"
  key_links:
    - from: "BaselineRecorder.record()"
      to: "eval/baselines/<case_id>.json"
      via: "json.dumps with schema fields"
      pattern: "vault_content_hash"
    - from: "_build_cmd()"
      to: "subprocess (never shell=True)"
      via: "list form cmd with prompt as final positional arg"
      pattern: "shell=True"
---

<objective>
Port the headless baseline recorder from lattice-evals into eval-harness as independent code — delivering the capability to snapshot the current lattice-wiki query output for each eval case as a reproducible oracle. Also create eval/README.md documenting how to record a baseline.

Purpose: The baseline is the reference point for regression detection (EVAL-09). Without it, regression_check() has nothing to compare against. This plan wires the one-time recording flow documented as "make record-baseline".

Output: baseline.py (RunResult, _build_cmd, run_headless, BaselineRecorder), test_baseline.py (unit tests for all deterministic parts, no actual claude subprocess launched in tests), eval/README.md (baseline recording instructions).
</objective>

<execution_context>
@/Users/pat/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-eval-harness/04-CONTEXT.md
@.planning/phases/04-eval-harness/04-RESEARCH.md
@.planning/phases/04-eval-harness/04-PATTERNS.md

# Prior plan SUMMARY
@.planning/phases/04-eval-harness/04-eval-harness-01-SUMMARY.md

# Port source (read carefully — drop lattice-wiki-specific sections)
@/Users/pat/Personal/lattice/packages/lattice-evals/src/lattice_evals/runner_headless.py

# Existing eval artifacts
@eval/cases/query_cases.json
</context>

<interfaces>
<!-- Key contracts from port source and prior plans. -->

From lattice-evals/runner_headless.py (port source):
```python
EVAL_SYSTEM_PROMPT_QA = (
    "EVAL MODE (Q&A): This session runs inside an automated headless evaluation of a "
    "question-and-answer task. Answer the user's question directly using only read-only "
    "tools (Read, Glob, Grep, Bash for read-only commands). Do NOT call Edit or Write, "
    "and do NOT use Bash to modify files, install packages, run builds, or run tests — "
    "the prompt is asking for an answer, not an implementation. Do NOT pause to ask "
    "clarifying questions or present designs for approval. End your final reply with "
    "<DONE> on its own line once the answer is complete."
)

@dataclass
class RunResult:
    final_status: str     # "success" | "budget_exceeded" | "error"
    budget_exceeded: bool
    wall_seconds: float
    turns: int

def _build_cmd(
    *,
    prompt: str,
    worktree_path: Path,
    system_prompt: str,
    plugin_dirs: list[Path] | None,
    model_override: str | None,
) -> list[str]: ...  # returns ["claude", "-p", "--output-format", "stream-json", ..., prompt]
```

Baseline JSON schema (EVAL-08 reproducibility fields, including seed per SC-5):
```json
{
  "case_id": "str",
  "query": "str",
  "answer": "str",
  "model_arn": "str",
  "prompt_hash": "sha256 of (case_id + query + system_prompt)",
  "vault_content_hash": "sha256 of sorted md5s of all .md files in vault",
  "timestamp_utc": "ISO 8601 string",
  "lattice_wiki_version": "str (from subprocess or 'unknown')",
  "seed": null
}
```

Note: seed=None in the baseline because the claude CLI (lattice-wiki) has no seed parameter. The field is included so that snapshot comparison code can handle it uniformly.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Baseline recorder — port + unit tests + eval/README.md</name>
  <files>
    cores/eval-harness/src/eval_harness/baseline.py
    cores/eval-harness/tests/test_baseline.py
    eval/README.md
  </files>
  <read_first>
    /Users/pat/Personal/lattice/packages/lattice-evals/src/lattice_evals/runner_headless.py
    cores/eval-harness/src/eval_harness/isolation.py
    eval/cases/query_cases.json
  </read_first>
  <behavior>
    - test_baseline.py::test_build_cmd_flags: _build_cmd(prompt="What is X?", worktree_path=tmp_path, system_prompt="...", plugin_dirs=None, model_override=None) returns list starting with ["claude", "-p", "--output-format", "stream-json"] and ending with "What is X?"
    - test_baseline.py::test_build_cmd_plugin_dir: when plugin_dirs=[Path("/some/plugin")], result contains "--plugin-dir" and "/some/plugin"
    - test_baseline.py::test_build_cmd_model_override: when model_override="sonnet-4-6", result contains ["--model", "sonnet-4-6"]
    - test_baseline.py::test_build_cmd_no_shell: _build_cmd returns a list (not a string); never shell=True
    - test_baseline.py::test_build_cmd_prompt_is_last: result[-1] == "What is X?" (prompt is final positional arg)
    - test_baseline.py::test_baseline_schema: BaselineRecorder._make_snapshot(case, run_result, answer, vault_path) returns dict with all required keys: case_id, query, answer, model_arn, prompt_hash, vault_content_hash, timestamp_utc, seed
    - test_baseline.py::test_baseline_seed_is_none: snapshot["seed"] is None (claude CLI has no seed parameter; field present for schema uniformity)
    - test_baseline.py::test_prompt_hash_deterministic: same (case_id, query, system_prompt) always produces same prompt_hash
    - test_baseline.py::test_vault_content_hash: _vault_content_hash(fixture_vault_path) returns a non-empty hex string; calling twice returns same value
  </behavior>
  <action>
    cores/eval-harness/src/eval_harness/baseline.py:

    Port from lattice-evals/runner_headless.py following PATTERNS.md guidance. Drop these sections (lattice-wiki-specific, not needed):
      - auto_user / multi-turn stdin management
      - _select_reply() user simulator
      - _validate_credentials() OAuth check
      - _add_worktree() / _remove_wiki() / _build_cfg_dir()
      - simulator_* fields from RunResult

    Keep and port verbatim (adjusting imports only):
      - EVAL_SYSTEM_PROMPT_QA constant (copy exact text from PATTERNS.md lines 83-92)
      - RunResult dataclass: final_status (str), budget_exceeded (bool), wall_seconds (float), turns (int)
      - _build_cmd() function: exact signature per PATTERNS.md. Security (T-4-03): cmd is ALWAYS a list, NEVER a string joined for shell. prompt is ALWAYS cmd.append(prompt) as the final element, NEVER f-string interpolated into the command.
      - _spawn(): subprocess.Popen with stdin=subprocess.DEVNULL, stdout=subprocess.PIPE, stderr=subprocess.PIPE — never shell=True
      - run_headless() one-shot variant: read stdout events as JSON lines, break on ev["type"] == "result", accumulate assistant content from ev["type"] == "assistant" events. Return RunResult. Add max_wall_seconds=300 as a timeout.

    Add new components (not in lattice-evals, specific to eval-harness):

    _vault_content_hash(vault_path: Path) -> str: collect all .md files recursively, sort by path string, compute sha256 of their concatenated content hashes (or sorted md5s of each file — simple and deterministic). Return hex digest.

    _prompt_hash(case_id: str, query: str, system_prompt: str) -> str: sha256(f"{case_id}|{query}|{system_prompt}".encode()).hexdigest().

    BaselineRecorder class:
      __init__(self, cases_path: Path, vault_path: Path, baselines_dir: Path, *, plugin_dirs: list[Path] | None = None, model_arn: str = "us.anthropic.claude-sonnet-4-6")
      _make_snapshot(self, case: dict, run_result: RunResult, answer: str) -> dict: builds the JSON schema dict with all 8 fields per the EVAL-08 schema above. Include seed=None explicitly (the claude CLI has no seed parameter; field present for snapshot schema uniformity with SweepResult).
      record(self, case: dict) -> Path: run async with EvalWorktree(self.vault_path), call run_headless(), extract answer, call _make_snapshot(), write to self.baselines_dir / f"{safe_case_id}.json" (safe_case_id = re.sub(r"[^a-zA-Z0-9._-]", "_", case["case_id"])), return path
      record_all(self) -> list[Path]: load cases from cases_path (json.load + validate), call record() for each, return list of written paths

    run_headless() uses asyncio internally or accepts an existing event loop — keep it sync-compatible (the lattice-evals version is sync; keep it sync here too, since BaselineRecorder.record() is the async entry point via EvalWorktree).

    Module header: from __future__ import annotations. Imports: dataclasses, hashlib, json, subprocess, time, datetime, pathlib, typing, re. Import EvalWorktree from eval_harness.isolation. No import of lattice_evals — this is independent code.

    Security (T-4-03 enforcement): In _build_cmd(), add an assert isinstance(cmd, list) at the end of the function (defensive guard that makes the invariant explicit). This is the only subprocess construction point in the harness.

    eval/README.md: Create (or update if it exists) with a "Recording a Baseline" section containing:
      Prerequisites: claude CLI installed and on PATH (the lattice-wiki plugin runs inside it); the lattice-wiki plugin path configured (e.g., LATTICE_WIKI_PLUGIN_DIR env var or --plugin-dir flag).
      The exact command to run a full baseline recording: CODE_WIKI_RUN_EVAL=1 uv run --package eval-harness python -m eval_harness.baseline --cases eval/cases/query_cases.json --vault <path-to-vault> --out eval/baselines/
      Expected output structure in eval/baselines/: one JSON file per case_id (e.g., eval/baselines/package-lookup.json), each containing the 8 schema fields (case_id, query, answer, model_arn, prompt_hash, vault_content_hash, timestamp_utc, seed).
      A note that seed is always null in baselines (the claude CLI does not expose a seed parameter).

    test_baseline.py: All tests are pure unit — no subprocess spawned, no Bedrock calls. Test _build_cmd() by calling it directly and inspecting the returned list. Test _make_snapshot() by calling BaselineRecorder._make_snapshot() with a synthetic RunResult and asserting all 8 keys are present including seed=None. Test _vault_content_hash() using fixture_vault_path (just verifies it returns a non-empty hex string and is deterministic). Test _prompt_hash() determinism by calling twice with same args.
  </action>
  <verify>
    <automated>uv run --package eval-harness pytest cores/eval-harness/tests/test_baseline.py -x -q && uv run --package eval-harness pytest cores/eval-harness/tests/ -m "not eval" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_baseline.py exits 0 with all 9 tests green (including test_baseline_seed_is_none)
    - baseline.py exports RunResult, _build_cmd, run_headless, BaselineRecorder, EVAL_SYSTEM_PROMPT_QA
    - "grep 'shell=True' cores/eval-harness/src/eval_harness/baseline.py" returns 0 matches
    - "grep 'shell=False' cores/eval-harness/src/eval_harness/baseline.py || grep 'Popen' cores/eval-harness/src/eval_harness/baseline.py" confirms subprocess usage is list-based
    - _build_cmd returns a list and its last element is the prompt string (verified by test_build_cmd_prompt_is_last)
    - BaselineRecorder._make_snapshot returns dict with 8 keys: case_id, query, answer, model_arn, prompt_hash, vault_content_hash, timestamp_utc, seed
    - snapshot["seed"] is None (verified by test_baseline_seed_is_none)
    - eval/README.md exists and contains a "Recording a Baseline" section
    - Full eval-harness unit suite: "uv run --package eval-harness pytest cores/eval-harness/tests/ -m 'not eval' -x -q" exits 0
  </acceptance_criteria>
  <done>baseline.py ported from lattice-evals with lattice-wiki-specific code dropped; all 9 unit tests pass; shell=True is absent; prompt is always the final positional argument; seed=None is included in snapshot schema; eval/README.md documents baseline recording.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prompt string → subprocess argv | User-supplied query string reaches the subprocess command list |
| plugin_dirs → subprocess argv | User-provided plugin directory paths reach the subprocess |
| baselines_dir / case_id → filesystem write | case_id used as filename component when writing snapshot |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-03 | Tampering | _build_cmd() subprocess construction | mitigate | cmd is always a list; subprocess.Popen(cmd, shell=False); prompt appended as a list element (cmd.append(prompt)) never via string interpolation; assert isinstance(cmd, list) guard at end of _build_cmd |
| T-4-01 | Tampering | BaselineRecorder cases JSON loading | mitigate | json.load() + validate case_id is str and query is str before use as filename/prompt; reject cases missing required keys |
| T-4-05 (new) | Tampering | case_id as filename component | mitigate | case_id sanitized before use in path: re.sub(r"[^a-zA-Z0-9._-]", "_", case_id) before constructing baselines_dir / f"{safe_case_id}.json" |
</threat_model>

<verification>
After task completes:

1. Unit tests: `uv run --package eval-harness pytest cores/eval-harness/tests/test_baseline.py -v` shows all 9 PASSED
2. No shell=True: `grep -c "shell=True" cores/eval-harness/src/eval_harness/baseline.py` returns 0
3. Prompt is last: `uv run --package eval-harness pytest cores/eval-harness/tests/test_baseline.py::test_build_cmd_prompt_is_last -v` shows PASSED
4. Seed present: `uv run --package eval-harness pytest cores/eval-harness/tests/test_baseline.py::test_baseline_seed_is_none -v` shows PASSED
5. Full suite: `uv run --package eval-harness pytest cores/eval-harness/tests/ -m "not eval" -x -q` exits 0
6. Exports: `uv run --package eval-harness python -c "from eval_harness.baseline import RunResult, _build_cmd, BaselineRecorder, EVAL_SYSTEM_PROMPT_QA; print('ok')"` exits 0
7. README exists: `grep -c "Recording a Baseline" eval/README.md` returns >= 1
</verification>

<success_criteria>
- baseline.py is a clean port of lattice-evals runner_headless.py with OAuth/git/simulator code dropped
- _build_cmd() always returns a list with prompt as the final element; never uses shell=True
- BaselineRecorder._make_snapshot() produces a dict with all 8 EVAL-08 reproducibility fields including seed=None
- case_id and vault content hash are both stable across multiple calls with the same inputs
- All 9 unit tests pass without subprocess spawning or Bedrock calls
- eval/README.md documents prerequisites, exact recording command, expected output structure, and seed=null note
- EVAL-03 (headless subprocess recorder) and EVAL-08 (reproducibility fields including seed) are fully covered
</success_criteria>

<output>
After completion, create `.planning/phases/04-eval-harness/04-eval-harness-04-SUMMARY.md`
</output>
