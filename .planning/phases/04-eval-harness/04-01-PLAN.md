---
phase: 04-eval-harness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cores/eval-harness/pyproject.toml
  - cores/eval-harness/src/eval_harness/__init__.py
  - cores/eval-harness/src/eval_harness/pricing.py
  - cores/eval-harness/src/eval_harness/structural.py
  - cores/eval-harness/tests/conftest.py
  - cores/eval-harness/tests/test_pricing.py
  - cores/eval-harness/tests/test_structural.py
  - cores/model-adapter/src/model_adapter/models.toml
  - agents/code-wiki-agent/src/code_wiki_agent/commands/query.py
  - cores/subagent-runtime/src/subagent_runtime/pool.py
  - eval/cases/query_cases.json
autonomous: true
requirements:
  - EVAL-01
  - EVAL-02
  - EVAL-06
  - EVAL-07

must_haves:
  truths:
    - "eval-harness package is importable as a workspace member"
    - "Pricing module returns correct USD costs for all 5 Bedrock model IDs"
    - "Structural checker correctly identifies citation presence and page drill count"
    - "run_query() accepts librarian_model_override parameter"
    - "pool.py writes real cost_usd values using pricing module"
    - "models.toml judge_b is Nova Pro, not Sonnet"
    - "eval/cases/query_cases.json contains 3+ test cases"
    - "All unit tests pass without Bedrock calls"
  artifacts:
    - path: "cores/eval-harness/pyproject.toml"
      provides: "workspace member declaration + deepeval/pytest-evals deps"
      contains: "name = \"eval-harness\""
    - path: "cores/eval-harness/src/eval_harness/pricing.py"
      provides: "PRICES dict + cost_for_usage()"
      exports: ["PRICES", "UnknownModelError", "cost_for_usage"]
    - path: "cores/eval-harness/src/eval_harness/structural.py"
      provides: "check_structural() deterministic EVAL-06 checks"
      exports: ["check_structural"]
    - path: "eval/cases/query_cases.json"
      provides: "3+ eval test cases from fixture vault"
      contains: "query"
  key_links:
    - from: "pool.py _write_trace()"
      to: "eval_harness.pricing.cost_for_usage"
      via: "lazy import inside _write_trace"
      pattern: "from eval_harness.pricing import cost_for_usage"
    - from: "run_query()"
      to: "librarian_model_override param"
      via: "ChatBedrockConverse direct construction"
      pattern: "librarian_model_override"
---

<objective>
Scaffold the eval-harness workspace package and complete the infrastructure modifications that make the package usable end-to-end for deterministic checks.

Purpose: Every subsequent plan depends on these foundations — the package must be importable, pricing must be correct, structural checks must work, and run_query must accept the model override needed by the sweep runner.

Output: importable eval-harness package, pricing + structural modules, updated models.toml, query.py with override param, pool.py with real cost accounting, eval cases JSON, full unit test coverage for all deterministic code.
</objective>

<execution_context>
@/Users/pat/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

# Phase context
@.planning/phases/04-eval-harness/04-CONTEXT.md
@.planning/phases/04-eval-harness/04-RESEARCH.md
@.planning/phases/04-eval-harness/04-PATTERNS.md

# Files being modified — read before touching
@cores/model-adapter/src/model_adapter/models.toml
@agents/code-wiki-agent/src/code_wiki_agent/commands/query.py
@cores/subagent-runtime/src/subagent_runtime/pool.py
@cores/subagent-runtime/pyproject.toml
@agents/code-wiki-agent/tests/conftest.py

# Port source (read, do not depend on)
@/Users/pat/Personal/lattice/packages/lattice-evals/src/lattice_evals/pricing.py
</context>

<interfaces>
<!-- Key contracts the executor needs. -->

From agents/code-wiki-agent/src/code_wiki_agent/commands/query.py:
```python
@dataclass
class QueryResult:
    answer: str
    citations: list[str]
    pages_drilled: int
    search_scores: dict

async def run_query(
    query: str,
    vault_path: Path | None = None,
    top_k: int = 5,
) -> QueryResult: ...
# librarian_llm = make_llm("librarian") lives at line ~529 after lib_cfg = load_role_config("librarian")
```

From cores/subagent-runtime/src/subagent_runtime/pool.py:
```python
def _write_trace(self, path, role, model_id, item, status, latency_ms, response, *, error=None):
    # tokens_in/tokens_out already extracted from response.usage_metadata
    # line ~200: "cost_usd": None,  # Phase 4 adds cost accounting
```

From lattice-evals/pricing.py (port source):
```python
class UnknownModelError(KeyError): pass

PRICES: dict[str, dict[str, float]] = {
    "claude-sonnet-4-6": {"input": 3.0, "output": 15.0, "cache_read": 0.30, "cache_write": 3.75},
    ...
}

def cost_for_usage(model: str, usage: dict[str, int]) -> float:
    # usage keys: input, output, cache_read, cache_write (all int token counts)
    ...
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1a: Scaffold eval-harness package — pyproject, __init__, conftest, eval cases</name>
  <files>
    cores/eval-harness/pyproject.toml
    cores/eval-harness/src/eval_harness/__init__.py
    cores/eval-harness/tests/conftest.py
    eval/cases/query_cases.json
  </files>
  <read_first>
    cores/subagent-runtime/pyproject.toml
    agents/code-wiki-agent/tests/conftest.py
    cores/vault-io/tests/fixtures/round-trip-vault/ (list contents to know valid pages for eval cases)
  </read_first>
  <action>
    Create the directory tree: cores/eval-harness/src/eval_harness/, cores/eval-harness/tests/, eval/cases/.

    cores/eval-harness/pyproject.toml: mirror subagent-runtime/pyproject.toml. Name = "eval-harness". Dependencies include deepeval>=4.0.0, pytest-evals>=0.3.4, python-frontmatter>=1.1.0, code-wiki-agent (workspace dep), subagent-runtime (workspace dep), model-adapter (workspace dep). Build system = uv_build. pytest.ini_options: testpaths=["tests"], addopts="--import-mode=importlib", asyncio_mode="auto", markers include both "integration: requires real Bedrock" and "eval: requires CODE_WIKI_RUN_EVAL=1". uv.sources entries for all three workspace deps.

    cores/eval-harness/src/eval_harness/__init__.py: module docstring only, no imports (per D-05 it is independent code, minimal surface).

    cores/eval-harness/tests/conftest.py: Mirror agents/code-wiki-agent/tests/conftest.py exactly. Rename INTEGRATION_GATE to EVAL_GATE; use CODE_WIKI_RUN_EVAL env var. fixture_vault_path computes path as Path(__file__).parent.parent.parent.parent / "cores" / "vault-io" / "tests" / "fixtures" / "round-trip-vault".

    eval/cases/query_cases.json: Create JSON array of 4 objects, each with keys: case_id (str), query (str), expected_answer (str), tags (list[str]). Cases based on actual pages in the fixture vault:
      1. Package lookup: "What does lattice-wiki-core do?" — tags: ["package-lookup"]
      2. Concept query: "How does the wiki index work?" — tags: ["concept"]
      3. Cross-reference: "Which packages use lattice-source-parser?" — tags: ["cross-reference"]
      4. Edge case: "What is the log.md format?" — tags: ["log", "format"]
    expected_answer values should be plausible short phrases (not verbatim wiki content), e.g., "lattice-wiki-core provides the core wiki maintenance logic".
  </action>
  <verify>
    <automated>uv run --package eval-harness python -c "import eval_harness" && uv run --package eval-harness python -c "import json, pathlib; cases = json.load(open(pathlib.Path.cwd() / 'eval' / 'cases' / 'query_cases.json')); assert len(cases) >= 3"</automated>
  </verify>
  <done>eval-harness is importable; query_cases.json exists with 4 valid cases each containing case_id, query, expected_answer, and tags.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1b: Core modules — pricing.py, structural.py, and their unit tests</name>
  <files>
    cores/eval-harness/src/eval_harness/pricing.py
    cores/eval-harness/src/eval_harness/structural.py
    cores/eval-harness/tests/test_pricing.py
    cores/eval-harness/tests/test_structural.py
  </files>
  <read_first>
    /Users/pat/Personal/lattice/packages/lattice-evals/src/lattice_evals/pricing.py
    agents/code-wiki-agent/src/code_wiki_agent/commands/query.py
    cores/vault-io/tests/fixtures/round-trip-vault/ (list contents to confirm pages for structural tests)
    cores/eval-harness/tests/conftest.py (just created — read for fixture_vault_path definition)
  </read_first>
  <behavior>
    - test_pricing.py::test_nova_pro_cost: cost_for_usage("us.amazon.nova-pro-v1:0", {"input": 1_000_000, "output": 1_000_000}) == 4.0 (0.80 + 3.20)
    - test_pricing.py::test_nova_lite_cost: cost_for_usage("us.amazon.nova-lite-v1:0", {"input": 1_000_000, "output": 1_000_000}) == 1.5 (0.30 + 1.20)
    - test_pricing.py::test_qwen3_cost: cost_for_usage("qwen.qwen3-32b-v1:0", {"input": 1_000_000, "output": 1_000_000}) == 2.0 (0.40 + 1.60)
    - test_pricing.py::test_unknown_model_raises: cost_for_usage("unknown-model", ...) raises UnknownModelError
    - test_structural.py::test_fixture_vault_has_pages: len(list(fixture_vault_path.rglob("*.md"))) > 3
    - test_structural.py::test_known_good: check_structural(QueryResult(answer="See [[packages/lattice-wiki-core]].", citations=["packages/lattice-wiki-core"], pages_drilled=3, search_scores={}), fixture_vault_path)["citations_resolve"] is True
    - test_structural.py::test_no_citation: check_structural(QueryResult(answer="no links", citations=[], pages_drilled=1, search_scores={}), fixture_vault_path)["has_citation"] is False
    - test_structural.py::test_unresolved_citation: check_structural(QueryResult(answer="[[nonexistent/page]]", citations=["nonexistent/page"], pages_drilled=1, search_scores={}), fixture_vault_path)["citations_resolve"] is False
    - test_structural.py::test_all_keys_present: check_structural(...) returns dict with all 7 keys: has_citation, citations_resolve, unresolved_citations, pages_drilled_positive, has_code_path, frontmatter_valid, json_schema_valid
  </behavior>
  <action>
    cores/eval-harness/src/eval_harness/pricing.py: Port from lattice-evals/pricing.py verbatim (module docstring + from __future__ import annotations + UnknownModelError + PRICES + cost_for_usage). Extend PRICES with these Bedrock entries (no cache_read/cache_write keys — Bedrock non-Claude models do not support prompt caching):
      "us.anthropic.claude-haiku-4-5-20251001-v1:0": {"input": 1.0, "output": 5.0}
      "us.anthropic.claude-sonnet-4-6": {"input": 3.0, "output": 15.0}
      "us.amazon.nova-pro-v1:0": {"input": 0.80, "output": 3.20}
      "us.amazon.nova-lite-v1:0": {"input": 0.30, "output": 1.20}
      "qwen.qwen3-32b-v1:0": {"input": 0.40, "output": 1.60}
    Keep the three Claude entries from lattice-evals (opus-4-7, sonnet-4-6, haiku-4-5) with cache keys intact.
    Fallback model for Qwen3 failures: "us.amazon.nova-micro-v1:0" — add to PRICES: {"input": 0.035, "output": 0.14}.

    cores/eval-harness/src/eval_harness/structural.py: Implement check_structural(result: QueryResult, vault_path: Path) -> dict. Import QueryResult from code_wiki_agent.commands.query. Re-implement citation resolution inline (do not import private _extract_wikilinks from query.py — use re.findall directly). Return dict with ALL 7 keys:

      has_citation (bool): result.citations is non-empty.
      citations_resolve (bool): all entries in result.citations resolve to an .md file (vault_path / f"{link}.md" exists, or vault_path.glob(f"**/{base}.md") fallback). True if citations is empty (vacuously true).
      unresolved_citations (list[str]): citations that do not resolve.
      pages_drilled_positive (bool): result.pages_drilled > 0.
      has_code_path (bool): result.answer contains at least one path-like string — match with re.search(r'(src/|tests/|cores/|agents/|[a-zA-Z0-9_/.-]+\.py|[a-zA-Z0-9_/.-]+\.ts)', result.answer) is not None.
      frontmatter_valid (bool): for each page slug in result.citations, check vault_path / f"{slug}.md" (or glob fallback). If the file exists, load it with frontmatter.load() and verify post.metadata.get("title") is not None and not empty. If ALL existing citation pages have valid frontmatter (or citations is empty), return True; otherwise False.
      json_schema_valid (bool): isinstance(result, QueryResult) and isinstance(result.answer, str) and result.answer is not None and isinstance(result.citations, list) and isinstance(result.pages_drilled, (int, list)) and isinstance(result.search_scores, (dict, list)).

    Security (T-4-01): In structural.py's check_structural(), validate that the result parameter is a QueryResult instance (isinstance check) before any field access. Do not eval() any field.

    cores/eval-harness/tests/test_pricing.py: Write tests per <behavior> block. Import cost_for_usage, UnknownModelError from eval_harness.pricing. No mocks needed.

    cores/eval-harness/tests/test_structural.py: Write tests per <behavior> block. Import check_structural from eval_harness.structural. Import QueryResult from code_wiki_agent.commands.query. Use fixture_vault_path fixture from conftest. Verify the citation target "packages/lattice-wiki-core" actually exists in the fixture vault before writing the test (check the vault rglob output). Add test_all_keys_present that asserts all 7 expected keys are present in the return dict.
  </action>
  <verify>
    <automated>uv run --package eval-harness pytest cores/eval-harness/tests/test_pricing.py cores/eval-harness/tests/test_structural.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - "uv run --package eval-harness pytest cores/eval-harness/tests/test_pricing.py -q" exits 0, all tests green
    - "uv run --package eval-harness pytest cores/eval-harness/tests/test_structural.py -q" exits 0, all tests green
    - eval_harness.pricing.PRICES has keys for all 5 Bedrock model IDs listed in the action (including qwen.qwen3-32b-v1:0 and us.amazon.nova-micro-v1:0)
    - eval_harness.structural.check_structural returns dict with all 7 keys: has_citation, citations_resolve, unresolved_citations, pages_drilled_positive, has_code_path, frontmatter_valid, json_schema_valid
    - test_structural.py asserts all 7 keys are present in the return dict (test_all_keys_present)
  </acceptance_criteria>
  <done>pricing module covers all 5 Bedrock models plus nova-micro fallback; structural checker returns all 7 EVAL-06 required keys and passes all unit tests.</done>
</task>

<task type="auto">
  <name>Task 2: Modify query.py, pool.py, and models.toml</name>
  <files>
    agents/code-wiki-agent/src/code_wiki_agent/commands/query.py
    cores/subagent-runtime/src/subagent_runtime/pool.py
    cores/model-adapter/src/model_adapter/models.toml
  </files>
  <read_first>
    agents/code-wiki-agent/src/code_wiki_agent/commands/query.py
    cores/subagent-runtime/src/subagent_runtime/pool.py
    cores/model-adapter/src/model_adapter/models.toml
    cores/eval-harness/src/eval_harness/pricing.py (just created — read for import path)
  </read_first>
  <action>
    query.py — add librarian_model_override parameter (per RESEARCH.md Pitfall 4, D-05):
      Change run_query signature from (query, vault_path, top_k) to (query, vault_path, top_k, librarian_model_override: str | None = None).
      Update the docstring Args section to document librarian_model_override.
      At the block "lib_cfg = load_role_config('librarian') / librarian_llm = make_llm('librarian')", add a conditional: if librarian_model_override is not None, construct librarian_llm directly as ChatBedrockConverse(model_id=librarian_model_override, region_name=lib_cfg["region"], max_tokens=lib_cfg["max_tokens"]). Import ChatBedrockConverse from langchain_aws at the top of the file (check if already imported; it may not be). Else branch calls make_llm("librarian") as before. This is a surgical 8-line change — do not touch anything else in query.py.

    pool.py — complete cost accounting in _write_trace() (per PATTERNS.md Modifications section):
      At line ~200 where "cost_usd": None currently sits, replace None with computed value. After tokens_in and tokens_out are extracted from usage_metadata, add: if tokens_in is not None and tokens_out is not None, use a try/except block with a lazy import: "from eval_harness.pricing import cost_for_usage, UnknownModelError" inside the try, then compute cost_usd = cost_for_usage(model_id, {"input": tokens_in, "output": tokens_out}). Except (UnknownModelError, ImportError, KeyError): cost_usd = None. Assign cost_usd into the record dict. Do not add eval_harness to subagent-runtime's pyproject.toml dependencies — the lazy import pattern intentionally avoids a hard dep from subagent-runtime on eval-harness (the comment in pool.py already notes this).

    models.toml — update judge_b model_id (per D-07):
      Change [roles.judge_b] model_id from "us.anthropic.claude-sonnet-4-6" to "us.amazon.nova-pro-v1:0". No other fields change. This is a single-line edit.

    Security (T-4-02): In pool.py, the model_id used in the trace record is already supplied by the caller (not derived from user input at this point in the flow), so no sanitization needed here. T-4-02 sanitization belongs in sweep.py (Plan 02) where model_id from sweep config is used as a filename component.
  </action>
  <verify>
    <automated>uv run --package code-wiki-agent python -c "from code_wiki_agent.commands.query import run_query; import inspect; sig = inspect.signature(run_query); assert 'librarian_model_override' in sig.parameters" && grep -c "librarian_model_override" agents/code-wiki-agent/src/code_wiki_agent/commands/query.py && grep "model_id" cores/model-adapter/src/model_adapter/models.toml | grep "judge_b" | grep -c "nova-pro" && uv run --package subagent-runtime pytest cores/subagent-runtime/tests/ -x -q -m "not integration"</automated>
  </verify>
  <acceptance_criteria>
    - "from code_wiki_agent.commands.query import run_query; import inspect; 'librarian_model_override' in inspect.signature(run_query).parameters" is True
    - grep for "librarian_model_override" in query.py returns >= 2 matches (signature + usage)
    - cores/model-adapter/src/model_adapter/models.toml [roles.judge_b] model_id = "us.amazon.nova-pro-v1:0"
    - pool.py contains "from eval_harness.pricing import cost_for_usage" (inside try block)
    - uv run --package subagent-runtime pytest cores/subagent-runtime/tests/ -m "not integration" exits 0 (no regressions in subagent-runtime tests)
    - uv run --package code-wiki-agent pytest agents/code-wiki-agent/tests/ -m "not integration" exits 0 (no regressions in query tests)
  </acceptance_criteria>
  <done>All three existing files modified correctly, existing tests still green, librarian_model_override accepted by run_query.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eval/cases/*.json → structural.py | JSON files are committed artifacts but treated as untrusted input during parsing (could be malformed) |
| model_id string → trace JSONL filename | Model ID strings from sweep config used as filename components in later plans |
| prompt string → baseline recorder subprocess | User-supplied query string passed to claude -p subprocess in Plan 04 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-01 | Tampering | structural.py / eval cases loading | mitigate | In check_structural(), validate result is a QueryResult instance before processing citations. JSON schema validation (query: str, expected_answer: str) applied at load time in sweep.py (Plan 02). |
| T-4-02 | Tampering | pool.py trace JSONL / sweep.py filename | mitigate (partial — full mitigation in Plan 02) | pool.py cost_usd computation uses model_id supplied by the caller; no filename use in Plan 01. Filename sanitization re.sub(r"[^a-zA-Z0-9._:-]", "_", model_id) applied in sweep.py. |
| T-4-03 | Tampering | baseline recorder subprocess | accept (Plan 01 has no subprocess) | Subprocess injection via prompt addressed in Plan 04 via cmd list form, never shell=True. |
</threat_model>

<verification>
After all three tasks complete:

1. Package importable: `uv run --package eval-harness python -c "import eval_harness"` exits 0
2. All eval-harness unit tests: `uv run --package eval-harness pytest cores/eval-harness/tests/ -m "not eval" -x -q` exits 0
3. No regressions in subagent-runtime: `uv run --package subagent-runtime pytest cores/subagent-runtime/tests/ -m "not integration" -x -q` exits 0
4. No regressions in code-wiki-agent: `uv run --package code-wiki-agent pytest agents/code-wiki-agent/tests/ -m "not integration" -x -q` exits 0
5. models.toml judge_b: `grep -A4 "\[roles.judge_b\]" cores/model-adapter/src/model_adapter/models.toml | grep -c "nova-pro"` returns 1
6. run_query signature: `uv run --package code-wiki-agent python -c "import inspect; from code_wiki_agent.commands.query import run_query; print(inspect.signature(run_query))"` includes librarian_model_override
</verification>

<success_criteria>
- eval-harness workspace member importable; `uv sync` includes it in lockfile
- pricing.py covers all 5 Bedrock model IDs with verified USD/M token rates, plus nova-micro fallback
- structural.py check_structural() returns all 7 EVAL-06 required keys; passes on known-good QueryResult with valid vault fixture
- run_query() accepts librarian_model_override: str | None = None; passes it to ChatBedrockConverse when non-None
- pool.py _write_trace() computes cost_usd from eval_harness.pricing when tokens available
- models.toml [roles.judge_b] model_id = "us.amazon.nova-pro-v1:0"
- eval/cases/query_cases.json has 4 cases covering package-lookup, concept, cross-reference, format queries
- All existing package tests remain green
</success_criteria>

<output>
After completion, create `.planning/phases/04-eval-harness/04-eval-harness-01-SUMMARY.md`
</output>
