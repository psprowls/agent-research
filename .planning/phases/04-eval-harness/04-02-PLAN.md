---
phase: 04-eval-harness
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - cores/eval-harness/src/eval_harness/isolation.py
  - cores/eval-harness/src/eval_harness/sweep.py
  - cores/eval-harness/tests/test_isolation.py
  - cores/eval-harness/tests/test_sweep.py
autonomous: true
requirements:
  - EVAL-04
  - EVAL-06
  - EVAL-08

must_haves:
  truths:
    - "EvalWorktree copies fixture vault to tmpdir on enter and cleans up on exit"
    - "Sweep runner iterates N model IDs and collects N SweepResult entries"
    - "One failing model does not abort the sweep; others succeed"
    - "model_id is sanitized before use as filename component"
    - "Each SweepResult includes wall_seconds and structural checks"
  artifacts:
    - path: "cores/eval-harness/src/eval_harness/isolation.py"
      provides: "EvalWorktree async context manager (shutil.copytree-based)"
      exports: ["EvalWorktree"]
    - path: "cores/eval-harness/src/eval_harness/sweep.py"
      provides: "SweepResult dataclass + run_sweep()"
      exports: ["SweepResult", "run_sweep"]
    - path: "cores/eval-harness/tests/test_isolation.py"
      provides: "EvalWorktree unit tests"
    - path: "cores/eval-harness/tests/test_sweep.py"
      provides: "run_sweep unit tests with mocked run_query"
  key_links:
    - from: "sweep.py run_sweep()"
      to: "run_query(librarian_model_override=model_id)"
      via: "EvalWorktree async context manager"
      pattern: "librarian_model_override=model_id"
    - from: "sweep.py SweepResult"
      to: "check_structural(result, vault_path)"
      via: "structural.check_structural() call"
      pattern: "check_structural"
---

<objective>
Build the isolation layer (EvalWorktree) and model sweep runner that together deliver a working end-to-end sweep: given 3 candidate model IDs, produce 3 SweepResult records with structural checks, token counts, and cost estimates.

Purpose: This is the first vertical slice where a real (or mocked) run_query is called under isolation — the harness produces data that judges and the cost-frontier report will consume in Plan 03.

Output: EvalWorktree (tmpdir isolation), SweepResult dataclass, run_sweep() function, full unit test coverage with mocked run_query.
</objective>

<execution_context>
@/Users/pat/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-eval-harness/04-CONTEXT.md
@.planning/phases/04-eval-harness/04-RESEARCH.md
@.planning/phases/04-eval-harness/04-PATTERNS.md

# Prior plan SUMMARY needed (sweep depends on pricing/structural from plan 01)
@.planning/phases/04-eval-harness/04-eval-harness-01-SUMMARY.md

# Port source (do not depend on)
@/Users/pat/Personal/lattice/packages/lattice-evals/src/lattice_evals/isolation.py

# Files just created in plan 01 — read for import paths and signatures
@cores/eval-harness/src/eval_harness/pricing.py
@cores/eval-harness/src/eval_harness/structural.py
</context>

<interfaces>
<!-- Key contracts from plan 01 outputs and existing code. -->

From cores/eval-harness/src/eval_harness/pricing.py:
```python
def cost_for_usage(model: str, usage: dict[str, int]) -> float: ...
class UnknownModelError(KeyError): ...
```

From cores/eval-harness/src/eval_harness/structural.py:
```python
def check_structural(result: QueryResult, vault_path: Path) -> dict:
    # returns: {"has_citation": bool, "citations_resolve": bool,
    #           "unresolved_citations": list[str], "pages_drilled_positive": bool}
```

From agents/code-wiki-agent/src/code_wiki_agent/commands/query.py (modified in plan 01):
```python
async def run_query(
    query: str,
    vault_path: Path | None = None,
    top_k: int = 5,
    librarian_model_override: str | None = None,
) -> QueryResult: ...
```

From fixture vault:
```
cores/vault-io/tests/fixtures/round-trip-vault/.code-wiki/bm25/   (exists)
cores/vault-io/tests/fixtures/round-trip-vault/.code-wiki/search.db (exists)
cores/vault-io/tests/fixtures/round-trip-vault/.code-wiki/traces/  (exists)
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: EvalWorktree isolation layer</name>
  <files>
    cores/eval-harness/src/eval_harness/isolation.py
    cores/eval-harness/tests/test_isolation.py
  </files>
  <read_first>
    /Users/pat/Personal/lattice/packages/lattice-evals/src/lattice_evals/isolation.py
    cores/eval-harness/src/eval_harness/__init__.py
  </read_first>
  <behavior>
    - test_isolation.py::test_evalworktree_creates_copy: async with EvalWorktree(fixture_vault_path) as wt: assert wt.path.exists() and (wt.path / "index.md").exists()
    - test_isolation.py::test_evalworktree_includes_code_wiki: async with EvalWorktree(fixture_vault_path) as wt: assert (wt.path / ".code-wiki" / "bm25").exists()
    - test_isolation.py::test_evalworktree_cleans_up: hold tmp_dir ref, assert it does NOT exist after __aexit__
    - test_isolation.py::test_evalworktree_isolation: two concurrent EvalWorktrees have distinct paths
  </behavior>
  <action>
    cores/eval-harness/src/eval_harness/isolation.py: Implement EvalWorktree as the simplified IsolationContext described in RESEARCH.md Pattern 3 (do NOT port the full lattice-evals IsolationContext — it has OAuth, git worktree, and plugin registry logic that must be dropped).

    Class EvalWorktree:
      __init__(self, source_vault: Path) -> None: store self._source, init self.path = None, self._tmp = None
      async __aenter__(self) -> EvalWorktree: use tempfile.mkdtemp(prefix="eval-wt-") for self._tmp, self.path = Path(self._tmp) / "vault", shutil.copytree(self._source, self.path, dirs_exist_ok=False), return self
      async __aexit__(self, *exc) -> None: if self._tmp and Path(self._tmp).exists(): shutil.rmtree(Path(self._tmp), ignore_errors=True)

    The copy includes .code-wiki/ (BM25 + SQLite indexes travel with the vault — no rebuild needed). The traces/ subdir receives new JSONL during sweep runs without cross-run conflicts.

    Module header: from __future__ import annotations, imports: shutil, tempfile, from pathlib import Path.

    test_isolation.py: Use fixture_vault_path from conftest. All tests are async (asyncio_mode=auto). The "cleans_up" test must capture self._tmp before __aexit__ and verify it's gone after the context manager exits. The "isolation" test opens two EvalWorktrees concurrently with asyncio.gather() or simply sequential open-and-close.
  </action>
  <verify>
    <automated>uv run --package eval-harness pytest cores/eval-harness/tests/test_isolation.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_isolation.py exits 0 with all 4 tests green
    - isolation.py contains class EvalWorktree with __aenter__ and __aexit__
    - isolation.py does NOT import subprocess, oauth, or git
    - EvalWorktree copies the .code-wiki/ directory (verify via test_evalworktree_includes_code_wiki)
    - Tmpdir is removed after context exit (verify via test_evalworktree_cleans_up)
  </acceptance_criteria>
  <done>EvalWorktree creates isolated vault copy, includes .code-wiki index, cleans up on exit, all 4 tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Sweep runner — SweepResult + run_sweep()</name>
  <files>
    cores/eval-harness/src/eval_harness/sweep.py
    cores/eval-harness/tests/test_sweep.py
  </files>
  <read_first>
    cores/eval-harness/src/eval_harness/isolation.py
    cores/eval-harness/src/eval_harness/pricing.py
    cores/eval-harness/src/eval_harness/structural.py
    cores/subagent-runtime/src/subagent_runtime/pool.py
    eval/cases/query_cases.json
  </read_first>
  <behavior>
    - test_sweep.py::test_sweep_collects_results: run_sweep with 2 model_ids and 1 case using mocked run_query returns list of 2 SweepResult
    - test_sweep.py::test_sweep_partial_failure: run_sweep with 2 model_ids where one raises returns list of 1 success + 1 SweepResult with status="error"
    - test_sweep.py::test_sweep_includes_structural: each SweepResult.structural has key "has_citation"
    - test_sweep.py::test_sweep_sanitizes_model_id: SweepResult for model "us.amazon.nova-pro-v1:0" has safe_model_id "us.amazon.nova-pro-v1_0" (colon replaced with underscore per re.sub rule)
    - test_sweep.py::test_sweep_loads_cases: run_sweep reads cases from eval/cases/query_cases.json, validates each has "query" and "expected_answer" keys (T-4-01)
    - test_sweep.py::test_sweep_records_wall_seconds: SweepResult.wall_seconds > 0.0
  </behavior>
  <action>
    cores/eval-harness/src/eval_harness/sweep.py:

    SweepResult dataclass (from PATTERNS.md): fields: model_id (str), safe_model_id (str), query (str), answer (str), citations (list[str]), pages_drilled (int), tokens_in (int | None), tokens_out (int | None), cost_usd (float | None), wall_seconds (float), structural (dict), status (str) = "ok", judge_scores (dict | None) = None.

    Token/cost extraction: After run_query() returns, read trace JSONL from wt.path / ".code-wiki" / "traces" — parse the most recent JSONL file (sorted by mtime), sum tokens_in and tokens_out across all records for this run. Then compute cost_usd via cost_for_usage(model_id, {"input": tokens_in, "output": tokens_out}), catching UnknownModelError silently (cost_usd = None).

    run_sweep(cases_path: Path, vault_path: Path, model_ids: list[str]) -> list[SweepResult]:
      - Load cases from cases_path using json.load(). Validate each entry has "query" (str) and "expected_answer" (str) keys; skip entries missing these (T-4-01 schema enforcement).
      - For each model_id, compute safe_model_id = re.sub(r"[^a-zA-Z0-9._:-]", "_", model_id) — wait, per T-4-02: use re.sub(r"[^a-zA-Z0-9._-]", "_", model_id) to sanitize colons too, producing "us.amazon.nova-pro-v1_0". Use safe_model_id only for filenames; model_id is used for run_query calls.
      - For each (model_id, case) pair, run in an asyncio.gather(return_exceptions=True) loop per pool.py partial-failure pattern. Each coro: async with EvalWorktree(vault_path) as wt, then await run_query(case["query"], vault_path=wt.path, top_k=5, librarian_model_override=model_id), record result as SweepResult. If exception, produce SweepResult with status="error" and answer="".
      - call check_structural(query_result, wt.path) inside each coro to populate structural field.
      - run_sweep is an async function; the module exports it. Tests call it with asyncio.run or rely on asyncio_mode=auto.

    Import structure: from __future__ import annotations, dataclasses, asyncio, time, json, re, pathlib, from eval_harness.isolation import EvalWorktree, from eval_harness.structural import check_structural, from eval_harness.pricing import cost_for_usage, UnknownModelError, from code_wiki_agent.commands.query import run_query, QueryResult.

    test_sweep.py: Mock run_query using unittest.mock.AsyncMock (not pytest-mock — stdlib is sufficient per CLAUDE.md). Inject mock via monkeypatch (pytest's built-in monkeypatch fixture). Each test creates a minimal cases_path using tmp_path with a JSON file. The "partial failure" test makes one model_id's run_query raise RuntimeError. The "sanitizes_model_id" test uses model_id "us.amazon.nova-pro-v1:0" and asserts safe_model_id is "us.amazon.nova-pro-v1_0".
  </action>
  <verify>
    <automated>uv run --package eval-harness pytest cores/eval-harness/tests/test_sweep.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_sweep.py exits 0 with all 6 tests green
    - sweep.py contains dataclass SweepResult with all required fields including safe_model_id and status
    - sweep.py contains async def run_sweep(cases_path, vault_path, model_ids) -> list[SweepResult]
    - sweep.py uses re.sub to sanitize model_id (grep: "re.sub" in sweep.py returns >= 1 match)
    - sweep.py uses asyncio.gather(return_exceptions=True) for parallel execution
    - sweep.py validates JSON case schema (both "query" and "expected_answer" keys checked)
    - "uv run --package eval-harness pytest cores/eval-harness/tests/ -m 'not eval' -x -q" exits 0 (full unit suite still green)
  </acceptance_criteria>
  <done>run_sweep() iterates model IDs with partial-failure tolerance, produces SweepResult per model, sanitizes model_id for filenames, validates case schema, all 6 tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eval/cases/*.json → sweep.py | JSON case files validated for schema before use |
| model_id string → safe_model_id filename component | Model ID sanitized before use in filenames |
| fixture vault → EvalWorktree tmpdir | shutil.copytree is safe; source is a committed directory |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-01 | Tampering | sweep.py JSON case loading | mitigate | json.load() + validate both "query" and "expected_answer" keys are present and are strings; skip entries missing these keys with a logged warning |
| T-4-02 | Tampering | sweep.py safe_model_id filename | mitigate | re.sub(r"[^a-zA-Z0-9._-]", "_", model_id) applied to produce safe_model_id before any filename construction; model_id itself (unsanitized) only used for API calls |
| T-4-03 | Tampering | baseline recorder subprocess | accept (not in this plan) | Subprocess isolation addressed in Plan 04 |
</threat_model>

<verification>
After both tasks complete:

1. Full eval-harness unit suite: `uv run --package eval-harness pytest cores/eval-harness/tests/ -m "not eval" -x -q` exits 0
2. EvalWorktree copies .code-wiki: `uv run --package eval-harness pytest cores/eval-harness/tests/test_isolation.py -v` shows all 4 tests PASSED
3. Sweep partial failure: `uv run --package eval-harness pytest cores/eval-harness/tests/test_sweep.py::test_sweep_partial_failure -v` shows PASSED
4. No regressions: `uv run --package code-wiki-agent pytest agents/code-wiki-agent/tests/ -m "not integration" -x -q` exits 0
</verification>

<success_criteria>
- EvalWorktree is an async context manager; creates tmpdir copy of fixture vault including .code-wiki/; cleans up on exit
- run_sweep() accepts cases_path, vault_path, model_ids; iterates model IDs with asyncio.gather(return_exceptions=True); returns list[SweepResult]
- SweepResult includes safe_model_id (model_id with unsafe chars replaced by _), status ("ok" or "error"), structural checks, token counts, cost_usd
- JSON schema validation: cases missing "query" or "expected_answer" keys are skipped with a warning
- All 10 unit tests in test_isolation.py + test_sweep.py pass
- Full eval-harness unit suite passes in < 30 seconds without Bedrock calls
</success_criteria>

<output>
After completion, create `.planning/phases/04-eval-harness/04-eval-harness-02-SUMMARY.md`
</output>
