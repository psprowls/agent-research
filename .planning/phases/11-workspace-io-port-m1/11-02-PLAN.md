---
phase: 11-workspace-io-port-m1
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - packages/workspace-io/src/workspace_io/__init__.py
  - packages/workspace-io/src/workspace_io/config.py
  - packages/workspace-io/src/workspace_io/manifest.py
  - packages/workspace-io/src/workspace_io/init.py
  - packages/workspace-io/src/workspace_io/paths.py
  - packages/workspace-io/src/workspace_io/render.py
  - packages/workspace-io/src/workspace_io/versions.py
  - packages/workspace-io/src/workspace_io/_local_config.py
  - packages/workspace-io/src/workspace_io/assets/CLAUDE.md.template
autonomous: true
requirements: [WS-02, WS-03, WS-04, WS-05, WS-06, WS-07]
must_haves:
  truths:
    - "workspace_io.config.resolve() walks up cwd for .graph-wiki.yaml and honors GRAPH_WIKI_WORKSPACE env"
    - "workspace_io.config.resolve() raises RuntimeError naming code-wiki-agent init when no manifest is found (strict; D-03)"
    - "workspace_io.manifest.read() returns v2 dict and raises RuntimeError on v1 format (D-14)"
    - "workspace_io.init() is idempotent — re-running with same version does not rewrite manifest body"
    - "schema.py is NOT ported (D-06)"
    - "Asset template ships inside the wheel at workspace_io/assets/CLAUDE.md.template"
  artifacts:
    - path: "packages/workspace-io/src/workspace_io/config.py"
      provides: "GraphWikiConfig dataclass, resolve(cwd), _find_repo_root, _resolve_workspace"
      contains: "class GraphWikiConfig"
    - path: "packages/workspace-io/src/workspace_io/manifest.py"
      provides: "read(path), write(path, data) for .graph-wiki.yaml v2"
      contains: "def read"
    - path: "packages/workspace-io/src/workspace_io/init.py"
      provides: "init(repo_root, *, plugin, version, workspace=None) idempotent bootstrap"
      contains: "def init"
    - path: "packages/workspace-io/src/workspace_io/paths.py"
      provides: "manifest_path, wiki_dir, raw_dir, work_dir, knowledge_dir, graph_dir"
      contains: "def wiki_dir"
    - path: "packages/workspace-io/src/workspace_io/render.py"
      provides: "render_workspace_claude_md(workspace), AUTO_START/AUTO_END markers"
      contains: "AUTO_START"
    - path: "packages/workspace-io/src/workspace_io/versions.py"
      provides: "PendingUpdate, pending_updates, warn_if_stale"
      contains: "class PendingUpdate"
    - path: "packages/workspace-io/src/workspace_io/_local_config.py"
      provides: "read(path) for .graph-wiki.local.yaml — bespoke line parser"
      contains: "def read"
    - path: "packages/workspace-io/src/workspace_io/assets/CLAUDE.md.template"
      provides: "Workspace CLAUDE.md template with workspace-io:auto markers"
      contains: "workspace-io:auto:plugins"
  key_links:
    - from: "workspace_io.config.resolve"
      to: "workspace_io._local_config.read"
      via: "import + call in _resolve_workspace"
      pattern: "from workspace_io import _local_config"
    - from: "workspace_io.init"
      to: "workspace_io.manifest.read/write"
      via: "manifest module import"
      pattern: "from workspace_io import manifest"
    - from: "workspace_io.render._TEMPLATE_PATH"
      to: "src/workspace_io/assets/CLAUDE.md.template"
      via: "Path(__file__).resolve().parent / \"assets\" / \"CLAUDE.md.template\""
      pattern: "Path\\(__file__\\)\\.resolve\\(\\)\\.parent"
    - from: "render.py AUTO_START/AUTO_END constants"
      to: "assets/CLAUDE.md.template marker strings"
      via: "must match exactly or every re-render appends a fresh block (Pitfall #3)"
      pattern: "workspace-io:auto:plugins:(start|end)"
---

<objective>
Port the 8 source modules from `lattice-workspace` to `workspace-io` with the rebrand and behavioral divergences locked in CONTEXT.md (D-01..D-16). Drop `schema.py` (D-06). Apply the strict-manifest divergence in `config.resolve()` (D-03) and the v1-raises divergence in `manifest.read()` (D-14). Wire up the package `__init__.py` to re-export the public surface.

Purpose: This plan delivers the entire `workspace_io.*` surface that vault-io (Plan 04), the CLI (Plan 05), and tests (Plan 03) consume. The ports are mostly mechanical — the analog files do the heavy lifting via `<read_first>`.

Output: A complete `workspace_io` package with 8 source modules + 1 asset template, all importable via `uv run --package workspace-io python -c "from workspace_io import GraphWikiConfig, init, resolve, PendingUpdate, pending_updates, warn_if_stale"`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-workspace-io-port-m1/11-CONTEXT.md
@.planning/phases/11-workspace-io-port-m1/11-RESEARCH.md
@.planning/phases/11-workspace-io-port-m1/11-PATTERNS.md

<interfaces>
<!-- Public surface that __init__.py must re-export (matches lattice's __init__.py shape with rebrand) -->

from workspace_io.config import GraphWikiConfig, resolve
from workspace_io.init import init
from workspace_io.versions import PendingUpdate, pending_updates, warn_if_stale

__all__ = [
    "GraphWikiConfig",
    "PendingUpdate",
    "init",
    "pending_updates",
    "resolve",
    "warn_if_stale",
]

<!-- Core dataclass shape (per RESEARCH.md §Code Examples + PATTERNS.md §config.py) -->

@dataclass(frozen=True)
class GraphWikiConfig:
    workspace: Path
    repo_root: Path

def resolve(cwd: Path | None = None) -> GraphWikiConfig:
    ...

<!-- manifest module signatures (PATTERNS.md §manifest.py) -->

def read(path: Path) -> dict: ...   # raises RuntimeError on v1
def write(path: Path, data: dict) -> None: ...

<!-- init module signature (PATTERNS.md §init.py) -->

def init(
    repo_root: Path,
    *,
    plugin: str,
    version: str,
    workspace: Path | None = None,
) -> None: ...

<!-- Rebrand symbol map (RESEARCH.md §Source Layout) -->

| lattice symbol/string | workspace_io symbol/string |
|-----------------------|---------------------------|
| LatticeConfig | GraphWikiConfig |
| LATTICE_WORKSPACE (env) | GRAPH_WIKI_WORKSPACE |
| LOCAL_CONFIG_FILENAME = ".lattice.local.yaml" | LOCAL_CONFIG_FILENAME = ".graph-wiki.local.yaml" |
| LATTICE_DIRECTORY_KEY = "lattice-directory" | LATTICE_DIRECTORY_KEY = "graph-wiki-directory" |
| DEFAULT_WORKSPACE_NAME = "lattice" | DEFAULT_WORKSPACE_NAME = "graph-wiki" |
| ".lattice.yaml" (filename) | ".graph-wiki.yaml" |
| lattice_workspace.* (imports) | workspace_io.* |
| "<!-- lattice-workspace:auto:plugins:start -->" | "<!-- workspace-io:auto:plugins:start -->" |
| _PLUGIN_POINTERS entries (lattice-wiki, lattice-graph, etc.) | { "code-wiki-agent": "see wiki/CLAUDE.md" } |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port pure modules (config, manifest, paths, _local_config, versions)</name>
  <files>packages/workspace-io/src/workspace_io/config.py, packages/workspace-io/src/workspace_io/manifest.py, packages/workspace-io/src/workspace_io/paths.py, packages/workspace-io/src/workspace_io/_local_config.py, packages/workspace-io/src/workspace_io/versions.py</files>
  <read_first>
    - /Users/pat/Personal/lattice/packages/lattice-workspace/src/lattice_workspace/config.py (port source, 78 lines)
    - /Users/pat/Personal/lattice/packages/lattice-workspace/src/lattice_workspace/manifest.py (port source, 65 lines — drop `_coerce()` per D-14)
    - /Users/pat/Personal/lattice/packages/lattice-workspace/src/lattice_workspace/paths.py (port source, 30 lines — verbatim per D-09)
    - /Users/pat/Personal/lattice/packages/lattice-workspace/src/lattice_workspace/_local_config.py (port source, 49 lines — verbatim, do NOT refactor to use yaml.safe_load per Pitfall #2)
    - /Users/pat/Personal/lattice/packages/lattice-workspace/src/lattice_workspace/versions.py (port source, 62 lines)
    - /Users/pat/Personal/deep-agents/.planning/phases/11-workspace-io-port-m1/11-PATTERNS.md (§config.py, §manifest.py, §paths.py, §_local_config.py, §versions.py for exact ported shapes)
    - /Users/pat/Personal/deep-agents/.planning/phases/11-workspace-io-port-m1/11-RESEARCH.md (§Common Pitfalls #2, §`GraphWikiConfig` post-port shape — strict check)
    - /Users/pat/Personal/deep-agents/.planning/phases/11-workspace-io-port-m1/11-CONTEXT.md (D-03, D-09, D-10, D-13, D-14, D-15, D-16)
  </read_first>
  <action>
    Port these 5 modules from `lattice_workspace/` to `workspace_io/` applying the rebrand symbol map from `<interfaces>` and the divergences below. Use `from __future__ import annotations` at the top of every module (per shared pattern in PATTERNS.md §Shared Patterns).

    `config.py`: Port the full 78-line source. Apply rebrand symbol map (LatticeConfig→GraphWikiConfig, LATTICE_WORKSPACE→GRAPH_WIKI_WORKSPACE env name, LOCAL_CONFIG_FILENAME→`.graph-wiki.local.yaml`, LATTICE_DIRECTORY_KEY→`graph-wiki-directory`, DEFAULT_WORKSPACE_NAME→`graph-wiki`). Add the strict-manifest check at the END of the cwd-discovery branch (NOT the env-override branch) per D-03 and PATTERNS.md §config.py: after computing `workspace = _resolve_workspace(repo_root)`, check `(workspace / ".graph-wiki.yaml").exists()`; if not, `raise RuntimeError(f"No .graph-wiki.yaml found in {workspace}. Run: code-wiki-agent init <path>")`. CRITICAL: env-override branch must NOT enforce the strict check — `GRAPH_WIKI_WORKSPACE` must work even before a manifest is written so tests can use it (see RESEARCH.md §Code Examples annotation). Port `_main()` and `__name__ == "__main__"` guard verbatim. Keep `_find_repo_root` (D-15) — public-by-convention since vault-io imports it in Plan 04.

    `manifest.py`: Port the read/write functions. Drop `_coerce()` entirely (D-14). New `read()` per PATTERNS.md §manifest.py: if file missing return `{}`; load YAML with `or {}` guard; if `raw.get("version", 1) < 2` raise RuntimeError naming `version: 2` requirement; normalize `initialized_at` to str (PyYAML coerces bare dates to `datetime.date`). Port `write()` verbatim — only the comment about `.lattice.yaml` filename changes to `.graph-wiki.yaml`. Preserve `yaml.safe_dump(payload, sort_keys=False, default_flow_style=False)` exactly.

    `paths.py`: Port verbatim (D-09). Change the single string in `manifest_path`: `.lattice.yaml` → `.graph-wiki.yaml`. All 6 helpers (`manifest_path`, `wiki_dir`, `raw_dir`, `work_dir`, `knowledge_dir`, `graph_dir`) retained.

    `_local_config.py`: Port verbatim (D-09 / Pitfall #2 — bespoke line parser, NOT `yaml.safe_load`). Update only the docstring/comment from `.lattice.local.yaml` → `.graph-wiki.local.yaml`. Do NOT refactor; the parser deliberately handles edge cases PyYAML would mishandle.

    `versions.py`: Port verbatim. Only change is import paths `lattice_workspace.*` → `workspace_io.*`. All 3 exports (`PendingUpdate`, `warn_if_stale`, `pending_updates`) preserved.

    After all 5 files are written, confirm with a smoke test (Task 3 below handles full verification): `uv run --package workspace-io python -c "from workspace_io.config import GraphWikiConfig, resolve; from workspace_io import manifest, paths, versions, _local_config; print('imports ok')"`.

    Do NOT touch `__init__.py` re-exports yet — Task 3 handles that after `init.py` exists.
  </action>
  <acceptance_criteria>
    - File `packages/workspace-io/src/workspace_io/config.py` exists; `grep -c "class GraphWikiConfig" packages/workspace-io/src/workspace_io/config.py` returns `1`
    - File `packages/workspace-io/src/workspace_io/config.py` contains the strict-manifest check: `grep -c "code-wiki-agent init" packages/workspace-io/src/workspace_io/config.py` returns at least `1`
    - File `packages/workspace-io/src/workspace_io/config.py` does NOT mention `LatticeConfig` or `LATTICE_WORKSPACE`: `grep -cE 'LatticeConfig|LATTICE_WORKSPACE' packages/workspace-io/src/workspace_io/config.py` returns `0`
    - File `packages/workspace-io/src/workspace_io/manifest.py` exists; does NOT contain `_coerce`: `grep -c '_coerce' packages/workspace-io/src/workspace_io/manifest.py` returns `0`
    - File `packages/workspace-io/src/workspace_io/manifest.py` raises on v1: `grep -cE 'version.*< 2|version.*\\< 2' packages/workspace-io/src/workspace_io/manifest.py` returns at least `1`
    - File `packages/workspace-io/src/workspace_io/paths.py` returns `.graph-wiki.yaml`: `grep -c '.graph-wiki.yaml' packages/workspace-io/src/workspace_io/paths.py` returns at least `1` (and does NOT contain `.lattice.yaml`: `grep -c '\\.lattice\\.yaml' packages/workspace-io/src/workspace_io/paths.py` returns `0`)
    - File `packages/workspace-io/src/workspace_io/_local_config.py` does NOT use `yaml.safe_load`: `grep -c 'yaml.safe_load\\|import yaml' packages/workspace-io/src/workspace_io/_local_config.py` returns `0` (bespoke parser per Pitfall #2)
    - File `packages/workspace-io/src/workspace_io/versions.py` exports `PendingUpdate`, `warn_if_stale`, `pending_updates`: `grep -cE 'class PendingUpdate|def warn_if_stale|def pending_updates' packages/workspace-io/src/workspace_io/versions.py` returns at least `3`
    - Smoke import test passes: `uv run --package workspace-io python -c "from workspace_io.config import GraphWikiConfig, resolve; from workspace_io import manifest, paths, versions, _local_config"` exits 0
  </acceptance_criteria>
  <verify>
    <automated>uv run --package workspace-io python -c "from workspace_io.config import GraphWikiConfig, resolve; from workspace_io import manifest, paths, versions, _local_config" && ! grep -qE 'LatticeConfig|LATTICE_WORKSPACE' packages/workspace-io/src/workspace_io/config.py && ! grep -q '_coerce' packages/workspace-io/src/workspace_io/manifest.py</automated>
  </verify>
  <done>
    All 5 pure modules ported with rebrand applied and divergences (D-03, D-14) in place. Modules import cleanly. No `init.py`, `render.py`, or asset yet — Task 2 covers those.
  </done>
</task>

<task type="auto">
  <name>Task 2: Port init/render/asset and wire __init__.py re-exports</name>
  <files>packages/workspace-io/src/workspace_io/init.py, packages/workspace-io/src/workspace_io/render.py, packages/workspace-io/src/workspace_io/assets/CLAUDE.md.template, packages/workspace-io/src/workspace_io/__init__.py</files>
  <read_first>
    - /Users/pat/Personal/lattice/packages/lattice-workspace/src/lattice_workspace/init.py (port source, 104 lines — drop `write_schema` call per D-06)
    - /Users/pat/Personal/lattice/packages/lattice-workspace/src/lattice_workspace/render.py (port source, 118 lines — rename markers, rebrand `_PLUGIN_POINTERS`)
    - /Users/pat/Personal/lattice/packages/lattice-workspace/src/lattice_workspace/assets/CLAUDE.md.template (port source, ~30 lines — rebrand prose + marker strings)
    - /Users/pat/Personal/lattice/packages/lattice-workspace/src/lattice_workspace/__init__.py (port source, 13 lines — public re-export surface)
    - /Users/pat/Personal/deep-agents/.planning/phases/11-workspace-io-port-m1/11-PATTERNS.md (§init.py, §render.py, §assets/CLAUDE.md.template, §__init__.py for exact ported shapes + rebrand tables)
    - /Users/pat/Personal/deep-agents/.planning/phases/11-workspace-io-port-m1/11-RESEARCH.md (§Common Pitfalls #3 — marker string match; §Open Questions #1 — `_PLUGIN_POINTERS` should be `{"code-wiki-agent": "see wiki/CLAUDE.md"}`)
    - /Users/pat/Personal/deep-agents/.planning/phases/11-workspace-io-port-m1/11-CONTEXT.md (D-04, D-06, D-07, D-08, D-11, D-12, D-16; Claude's Discretion — `init` idempotent, marker strings)
  </read_first>
  <action>
    Port `init.py`, `render.py`, the asset template, and finalize `__init__.py` re-exports.

    `init.py`: Port from lattice source applying the changes in PATTERNS.md §init.py:
    - Drop `from lattice_workspace.schema import write_schema` import (D-06)
    - Drop the `write_schema(_paths.work_dir(workspace))` call in the `init()` body (D-06)
    - Rename `_GITIGNORE_ENTRY = ".lattice.local.yaml"` → `".graph-wiki.local.yaml"` (D-16)
    - Default workspace path: replace hardcoded `repo_root / "lattice"` with `repo_root / "graph-wiki"` (matches DEFAULT_WORKSPACE_NAME — see RESEARCH.md Open Question #1)
    - Fix imports: `from lattice_workspace import manifest` → `from workspace_io import manifest`; same for `paths` and `render`
    - Preserve idempotency (re-running with same version skips write — current source behavior, keep it)
    - Preserve all helpers verbatim (`_is_inside_git_repo`, `_git_init`, `_ensure_gitignore_entry`)
    - Plugin entry write per D-11/D-12: same shape as lattice v2 — `{name, installed_version, applied_version}` with both versions set to the caller-provided `version` kwarg

    `render.py`: Port from lattice source:
    - Rename `AUTO_START = "<!-- lattice-workspace:auto:plugins:start -->"` → `"<!-- workspace-io:auto:plugins:start -->"` (Pitfall #3 — must match the template marker strings exactly)
    - Rename `AUTO_END` similarly
    - Replace `_PLUGIN_POINTERS` dict contents with a single entry: `{"code-wiki-agent": "see wiki/CLAUDE.md"}` (RESEARCH.md Open Question #1 — Claude's Discretion answer). Drop all old `lattice-*` entries.
    - Fix imports: `from lattice_workspace import manifest` → `from workspace_io import manifest`; `from lattice_workspace.paths import manifest_path` → `from workspace_io.paths import manifest_path`
    - `_TEMPLATE_PATH = Path(__file__).resolve().parent / "assets" / "CLAUDE.md.template"` — preserve verbatim (works in editable + wheel installs)
    - Port all helpers verbatim (`_render_plugin_list`, `_render_full_template`, `_refresh_auto_block`, `render_workspace_claude_md`)

    `assets/CLAUDE.md.template`: Port from lattice source applying the prose rebrand table in PATTERNS.md §assets/CLAUDE.md.template:
    - `# Lattice Workspace` → `# Graph-Wiki Workspace`
    - `.lattice.yaml` → `.graph-wiki.yaml`
    - `.lattice.local.yaml` → `.graph-wiki.local.yaml`
    - `lattice-directory:` → `graph-wiki-directory:`
    - `lattice-wiki` references → `code-wiki-agent`
    - `<!-- lattice-workspace:auto:plugins:start -->` → `<!-- workspace-io:auto:plugins:start -->` (MUST match `render.py` AUTO_START exactly — Pitfall #3)
    - `<!-- lattice-workspace:auto:plugins:end -->` → `<!-- workspace-io:auto:plugins:end -->` (MUST match `render.py` AUTO_END)
    - Preserve `{{PLUGIN_LIST}}` placeholder token unchanged
    - Preserve overall structure (per D-08, minimum-viable rebrand; template body polish deferred)

    `__init__.py`: Replace the docstring-only file from Plan 01 with the full re-export surface from PATTERNS.md §__init__.py:

    ```
    """workspace-io: graph-wiki workspace bootstrap, manifest IO, and config resolution."""
    from workspace_io.config import GraphWikiConfig, resolve
    from workspace_io.init import init
    from workspace_io.versions import PendingUpdate, pending_updates, warn_if_stale

    __all__ = ["GraphWikiConfig", "PendingUpdate", "init", "pending_updates", "resolve", "warn_if_stale"]
    ```

    (Use the actual Python syntax in the file; the box above is illustrative — do NOT include code fences in the file content.)
  </action>
  <acceptance_criteria>
    - File `packages/workspace-io/src/workspace_io/init.py` exists; does NOT import `schema` or call `write_schema`: `grep -cE 'write_schema|from.*schema' packages/workspace-io/src/workspace_io/init.py` returns `0`
    - File `packages/workspace-io/src/workspace_io/init.py` uses `graph-wiki` as default workspace: `grep -c '"graph-wiki"' packages/workspace-io/src/workspace_io/init.py` returns at least `1`; does NOT contain `"lattice"` as a literal directory: `grep -cE '"lattice"' packages/workspace-io/src/workspace_io/init.py` returns `0`
    - File `packages/workspace-io/src/workspace_io/init.py` _GITIGNORE_ENTRY references `.graph-wiki.local.yaml`: `grep -c '.graph-wiki.local.yaml' packages/workspace-io/src/workspace_io/init.py` returns at least `1`
    - File `packages/workspace-io/src/workspace_io/render.py` AUTO markers use `workspace-io:auto`: `grep -c 'workspace-io:auto:plugins' packages/workspace-io/src/workspace_io/render.py` returns at least `2` (start + end)
    - File `packages/workspace-io/src/workspace_io/render.py` does NOT contain `lattice` in markers or _PLUGIN_POINTERS: `grep -cE 'lattice-workspace:auto|"lattice-wiki"|"lattice-graph"|"lattice-curator"' packages/workspace-io/src/workspace_io/render.py` returns `0`
    - File `packages/workspace-io/src/workspace_io/render.py` _PLUGIN_POINTERS contains `"code-wiki-agent"`: `grep -c '"code-wiki-agent"' packages/workspace-io/src/workspace_io/render.py` returns at least `1`
    - File `packages/workspace-io/src/workspace_io/assets/CLAUDE.md.template` exists; marker strings match render.py: `grep -c 'workspace-io:auto:plugins:start' packages/workspace-io/src/workspace_io/assets/CLAUDE.md.template` returns at least `1`; no `lattice` markers: `grep -c 'lattice-workspace:auto' packages/workspace-io/src/workspace_io/assets/CLAUDE.md.template` returns `0`
    - File `packages/workspace-io/src/workspace_io/__init__.py` re-exports public surface: `uv run --package workspace-io python -c "from workspace_io import GraphWikiConfig, init, resolve, PendingUpdate, pending_updates, warn_if_stale; print('ok')"` exits 0 with `ok` printed
    - Asset path is discoverable from the loaded module: `uv run --package workspace-io python -c "from workspace_io import render; assert render._TEMPLATE_PATH.exists(), render._TEMPLATE_PATH"` exits 0
  </acceptance_criteria>
  <verify>
    <automated>uv run --package workspace-io python -c "from workspace_io import GraphWikiConfig, init, resolve, PendingUpdate, pending_updates, warn_if_stale, render; assert render._TEMPLATE_PATH.exists(), render._TEMPLATE_PATH; print('ok')" && ! grep -qE 'write_schema|lattice-workspace:auto' packages/workspace-io/src/workspace_io/init.py packages/workspace-io/src/workspace_io/render.py packages/workspace-io/src/workspace_io/assets/CLAUDE.md.template</automated>
  </verify>
  <done>
    `init.py`, `render.py`, asset template, and `__init__.py` ported with all rebrand changes applied and divergences (D-06 schema drop) in place. Full public re-export surface importable. Marker strings consistent across `render.py` and the template. Asset packaged inside the wheel.
  </done>
</task>

<task type="auto">
  <name>Task 3: Sanity-check ported modules against decisions and pitfalls</name>
  <files>packages/workspace-io/src/workspace_io/</files>
  <read_first>
    - /Users/pat/Personal/deep-agents/.planning/phases/11-workspace-io-port-m1/11-CONTEXT.md (Decisions section — every D-XX has a verification)
    - /Users/pat/Personal/deep-agents/.planning/phases/11-workspace-io-port-m1/11-RESEARCH.md (§Common Pitfalls — 7 pitfalls; §Code Examples — strict resolve shape)
    - /Users/pat/Personal/deep-agents/packages/workspace-io/src/workspace_io/config.py (verify rebrand)
    - /Users/pat/Personal/deep-agents/packages/workspace-io/src/workspace_io/manifest.py (verify v1-raises behavior)
    - /Users/pat/Personal/deep-agents/packages/workspace-io/src/workspace_io/init.py (verify schema dropped)
  </read_first>
  <action>
    Run a battery of in-process sanity checks against the ported modules, exercising each decision (D-XX) and pitfall WITHOUT a pytest suite (Plan 03 writes the test suite). The checks below catch porting mistakes early so Plan 03's test suite is debugging real test-logic issues, not stale ports.

    Execute these shell commands in sequence and capture results:

    1. `uv run --package workspace-io python -c "from workspace_io.config import resolve; import os; os.environ.pop('GRAPH_WIKI_WORKSPACE', None); 
try:
    resolve('/tmp')
except RuntimeError as e:
    assert 'code-wiki-agent init' in str(e), str(e)
    print('D-03 strict ok')
else:
    raise AssertionError('expected RuntimeError')"` — confirms D-03 strict-manifest check fires and error names `code-wiki-agent init`.

    2. `uv run --package workspace-io python -c "import tempfile, os; from pathlib import Path; from workspace_io.manifest import read; 
with tempfile.NamedTemporaryFile('w', suffix='.yaml', delete=False) as f:
    f.write('version: 1\\nplugins: []\\n')
    p = Path(f.name)
try:
    read(p)
except RuntimeError as e:
    print('D-14 v1-raises ok:', str(e)[:60])
finally:
    p.unlink()"` — confirms D-14 v1-format raises.

    3. `uv run --package workspace-io python -c "from workspace_io import paths; assert paths.manifest_path('/tmp').name == '.graph-wiki.yaml', paths.manifest_path('/tmp'); assert paths.wiki_dir('/tmp').name == 'wiki'; print('D-04/D-09 paths ok')"` — confirms D-04 wiki_dir convention and D-09 paths-verbatim.

    4. `uv run --package workspace-io python -c "from workspace_io import init, render; assert callable(init); assert render.AUTO_START == '<!-- workspace-io:auto:plugins:start -->', render.AUTO_START; assert render.AUTO_END == '<!-- workspace-io:auto:plugins:end -->'; assert 'code-wiki-agent' in render._PLUGIN_POINTERS; print('render ok')"` — confirms marker strings match Pitfall #3 fix and _PLUGIN_POINTERS rebranded.

    5. `uv run --package workspace-io python -c "from workspace_io.assets import *" 2>&1 | head -5 || true; uv run --package workspace-io python -c "from pathlib import Path; from workspace_io import render; t = render._TEMPLATE_PATH; assert t.exists(), t; body = t.read_text(); assert 'workspace-io:auto:plugins:start' in body; assert 'lattice-workspace:auto' not in body; print('asset template ok')"` — confirms asset packaged and marker strings consistent.

    6. `uv run --package workspace-io python -c "import tempfile, datetime; from pathlib import Path; from workspace_io.init import init; from workspace_io.manifest import read; 
with tempfile.TemporaryDirectory() as d:
    repo = Path(d)
    init(repo, plugin='code-wiki-agent', version='0.1.0')
    init(repo, plugin='code-wiki-agent', version='0.1.0')  # idempotent
    m = read(repo / 'graph-wiki' / '.graph-wiki.yaml')
    assert m['version'] == 2
    assert any(p['name'] == 'code-wiki-agent' and p['installed_version'] == '0.1.0' and p['applied_version'] == '0.1.0' for p in m['plugins']), m
    assert (repo / '.gitignore').read_text().strip().endswith('.graph-wiki.local.yaml')
    print('init idempotent + D-11/D-12 manifest shape ok')"` — confirms init bootstraps correctly, idempotency holds, D-11 schema applied, D-12 versions equal, gitignore entry written.

    7. Final: confirm no `LATTICE_WORKSPACE` env-var reference survives anywhere in `workspace_io`: `grep -rE 'LATTICE_WORKSPACE|LatticeConfig|lattice_workspace\\.' packages/workspace-io/src/workspace_io/ | grep -v __pycache__` must return zero lines.

    If any check fails, fix the corresponding ported module before declaring the task done. Do NOT skip a failing check.
  </action>
  <acceptance_criteria>
    - Sanity check 1 (D-03 strict): exits 0 with "D-03 strict ok"
    - Sanity check 2 (D-14 v1-raises): exits 0 with "D-14 v1-raises ok:"
    - Sanity check 3 (paths): exits 0 with "D-04/D-09 paths ok"
    - Sanity check 4 (render markers): exits 0 with "render ok"
    - Sanity check 5 (asset template): exits 0 with "asset template ok"
    - Sanity check 6 (init idempotent + manifest shape): exits 0 with "init idempotent + D-11/D-12 manifest shape ok"
    - Final grep: `grep -rE 'LATTICE_WORKSPACE|LatticeConfig|lattice_workspace\\.' packages/workspace-io/src/workspace_io/ | grep -v __pycache__` returns zero lines
  </acceptance_criteria>
  <verify>
    <automated>uv run --package workspace-io python -c "import tempfile; from pathlib import Path; from workspace_io.init import init; from workspace_io.manifest import read; d = tempfile.mkdtemp(); repo = Path(d); init(repo, plugin='code-wiki-agent', version='0.1.0'); init(repo, plugin='code-wiki-agent', version='0.1.0'); m = read(repo / 'graph-wiki' / '.graph-wiki.yaml'); assert m['version'] == 2 and any(p['name'] == 'code-wiki-agent' for p in m['plugins']); print('ok')" && ! grep -rE 'LATTICE_WORKSPACE|LatticeConfig|lattice_workspace\.' packages/workspace-io/src/workspace_io/ --include="*.py"</automated>
  </verify>
  <done>
    All 6 sanity checks pass; no `LATTICE_WORKSPACE`/`LatticeConfig`/`lattice_workspace.` references survive in `workspace_io` source. Ports are correct against every locked decision; Plan 03 can confidently port the test suite knowing the source behaviors match expectations.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem ↔ workspace_io.config | `GRAPH_WIKI_WORKSPACE` env var supplies an untrusted path |
| filesystem ↔ workspace_io.manifest | `.graph-wiki.yaml` content parsed via yaml.safe_load |
| local_config file ↔ workspace_io._local_config | `.graph-wiki.local.yaml` contains user-supplied paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02 | Tampering | `GRAPH_WIKI_WORKSPACE` env value | mitigate | `Path(env).expanduser().resolve()` — preserved verbatim from lattice source (`config.py` line ~52) |
| T-11-03 | Tampering | `.graph-wiki.yaml` parsing | mitigate | `yaml.safe_load` exclusively (never `yaml.load`); v1-format raises RuntimeError instead of silent coercion (D-14) |
| T-11-04 | Tampering | `manifest.write()` writing outside workspace | mitigate | `path.parent.mkdir(parents=True, exist_ok=True)` only creates dirs under the explicitly-passed `path`; no traversal because caller controls path |
| T-11-05 | Information Disclosure | Asset template contents | accept | Template ships inside the wheel; contains no secrets; identical risk profile to lattice source |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan; modules use only stdlib + already-resolved `pyyaml` |
</threat_model>

<verification>
- All 6 in-process sanity checks pass (D-03, D-04, D-06, D-09, D-11, D-12, D-14, Pitfall #3)
- `uv run --package workspace-io python -c "from workspace_io import GraphWikiConfig, init, resolve, PendingUpdate, pending_updates, warn_if_stale"` exits 0
- `grep -rE 'LATTICE_WORKSPACE|LatticeConfig|lattice_workspace\\.' packages/workspace-io/src/workspace_io/ --include="*.py"` returns zero lines
- `assets/CLAUDE.md.template` exists and is loadable via `render._TEMPLATE_PATH`
</verification>

<success_criteria>
- WS-02, WS-03, WS-04, WS-05, WS-06 (verified work-only, dropped), WS-07 satisfied
- Phase 11 SC #1 partially advanced (source ready; tests come in Plan 03)
- Decisions D-01, D-03, D-04, D-06, D-08, D-09, D-10, D-11, D-12, D-13, D-14, D-15, D-16 implemented in source
</success_criteria>

<output>
Create `.planning/phases/11-workspace-io-port-m1/11-02-SUMMARY.md` when done.
</output>
