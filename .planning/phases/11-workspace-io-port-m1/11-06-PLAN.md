---
phase: 11-workspace-io-port-m1
plan: 06
type: execute
wave: 4
depends_on: [11-03, 11-04, 11-05]
files_modified:
  - .planning/PROJECT.md
autonomous: true
requirements: [WS-10]
must_haves:
  truths:
    - "PROJECT.md Key Decisions records that .graph-wiki.yaml and wiki-config.toml are different surfaces (no migration script)"
    - "D-05: existing ~/Personal/wiki/deep-agents/ is throwaway — Pat deletes and re-inits via code-wiki-agent init; no migration script ships."
    - "Full workspace test suite is green end-to-end (workspace-io + vault-io)"
    - "Zero CODE_WIKI_REAL_VAULT_PATH references remain anywhere under packages/ or agents/"
    - "uv sync resolves the full workspace including workspace-io"
    - "import workspace_io and import vault_io._workspace both succeed"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Key Decisions entry resolving WS-10"
      contains: "wiki-config.toml"
  key_links:
    - from: ".planning/PROJECT.md Key Decisions"
      to: "RESEARCH.md verdict (wiki-config.toml vs .graph-wiki.yaml different surfaces)"
      via: "decision body cites the runtime config vs workspace manifest distinction"
      pattern: "wiki-config.toml"
---

<objective>
Close WS-10 by recording the `wiki-config.toml` vs `.graph-wiki.yaml` decision in PROJECT.md Key Decisions (verdict: different surfaces, no migration script needed), then perform end-to-end verification that the phase is complete: full test suite green, env-var rename sweep clean, two-phase init works locally.

Purpose: This plan delivers the documentation half of the phase (WS-10) and locks in the integration gate before `/gsd:verify-work` runs. Plans 01-05 ship the code; this plan certifies the phase against Phase 11's 5 success criteria.

Output: PROJECT.md updated with the locked WS-10 decision; full workspace test suite green; ready for verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-workspace-io-port-m1/11-CONTEXT.md
@.planning/phases/11-workspace-io-port-m1/11-RESEARCH.md
@.planning/PROJECT.md

<interfaces>
WS-10 verdict to record (from CONTEXT.md Canonical Refs + RESEARCH.md):

- `wiki-config.toml` (at repo root) = runtime CLI config — fields `{models_path, vault_path}`, read by `WikiConfig` dataclass in `code_wiki_agent.config`. Used by `code-wiki-agent --config <path>` to locate models and the default vault.
- `.graph-wiki.yaml` (per workspace) = workspace manifest — fields `{version, initialized_at, plugins[{name, installed_version, applied_version}]}`, read/written by `workspace_io.manifest`. Tracks which plugins have initialized the workspace.
- Verdict: completely different surfaces. No migration script needed or appropriate.

PROJECT.md structure (verified via Read):

- File: /Users/pat/Personal/deep-agents/.planning/PROJECT.md
- Section heading: `## Key Decisions` at line 162
- New decision entries should follow the existing bullet style in that section
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record WS-10 decision in PROJECT.md Key Decisions</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - /Users/pat/Personal/deep-agents/.planning/PROJECT.md (full file — verify Key Decisions section structure, find insertion point near line 162)
    - /Users/pat/Personal/deep-agents/.planning/phases/11-workspace-io-port-m1/11-CONTEXT.md (Canonical Refs section — wiki-config.toml verdict; D-05 throwaway vault)
    - /Users/pat/Personal/deep-agents/.planning/phases/11-workspace-io-port-m1/11-RESEARCH.md (wiki-config.toml vs .graph-wiki.yaml Different Surfaces (WS-10 Answer) section)
    - /Users/pat/Personal/deep-agents/wiki-config.toml (real file — 2 lines; confirm contents match RESEARCH.md verdict)
  </read_first>
  <action>
    Append a new entry to `.planning/PROJECT.md` under the `## Key Decisions` section recording the WS-10 verdict.

    Read PROJECT.md first to find the exact insertion point — locate the `## Key Decisions` heading (around line 162) and add a new bullet at the end of the existing decision list (or in the most recent dated subsection if decisions are grouped by date). Match the existing bullet style; do NOT introduce new formatting conventions.

    The new entry MUST include all three elements:
    1. Date stamp: `2026-05-17` (today, per environment context)
    2. WS-10 reference
    3. The verdict with concrete contents of both files

    Suggested body (adapt to match the existing PROJECT.md bullet style):

    `2026-05-17 (Phase 11 / WS-10): wiki-config.toml and .graph-wiki.yaml are different surfaces — no migration script. wiki-config.toml is the runtime CLI config (fields: models_path, vault_path) read by WikiConfig dataclass; .graph-wiki.yaml is the per-workspace manifest (fields: version, initialized_at, plugins[]) read/written by workspace_io.manifest. The two coexist: wiki-config.toml lives at the repo root and points the CLI at models + a default vault; .graph-wiki.yaml lives inside each workspace and tracks which plugins initialized it. Pat is deleting the existing ~/Personal/wiki/deep-agents/ throwaway vault (D-05) and re-initing at a supported location via "code-wiki-agent init" rather than migrating.`

    Do NOT modify any other lines in PROJECT.md. Do NOT reorder existing decisions. Do NOT add the entry under a different heading.

    Verify by grepping after the edit: `grep -A 5 'wiki-config.toml' .planning/PROJECT.md` should show the new entry under or near the Key Decisions section.
  </action>
  <acceptance_criteria>
    - `.planning/PROJECT.md` mentions `wiki-config.toml` at least twice (once in the new decision and possibly elsewhere): `grep -c 'wiki-config.toml' .planning/PROJECT.md` returns at least `1`
    - `.planning/PROJECT.md` mentions `.graph-wiki.yaml` in the Key Decisions context: `grep -c '\.graph-wiki\.yaml' .planning/PROJECT.md` returns at least `1`
    - The new decision references WS-10 and the "different surfaces" verdict: `grep -cE 'WS-10|different surfaces' .planning/PROJECT.md` returns at least `1`
    - The decision is in the Key Decisions section: search confirms the new bullet appears after the `## Key Decisions` heading and before the next `##` heading
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'wiki-config.toml' .planning/PROJECT.md && grep -q '.graph-wiki.yaml' .planning/PROJECT.md && grep -qE 'WS-10|different surfaces' .planning/PROJECT.md</automated>
  </verify>
  <done>
    WS-10 closed: PROJECT.md Key Decisions records that wiki-config.toml and .graph-wiki.yaml are distinct surfaces, with both file contents named and the migration-script-not-needed rationale documented.
  </done>
</task>

<task type="auto">
  <name>Task 2: End-to-end phase verification — full suite green, env-var sweep clean, two-phase init smoke test</name>
  <files>(read-only verification)</files>
  <read_first>
    - /Users/pat/Personal/deep-agents/.planning/ROADMAP.md (Phase 11 Success Criteria — 5 items)
    - /Users/pat/Personal/deep-agents/.planning/REQUIREMENTS.md (WS-01 through WS-10 — confirm all covered by Plans 01-06)
  </read_first>
  <action>
    Run the full integration verification gate for Phase 11. This task does not modify code — it asserts the phase is complete against ROADMAP §Phase 11 Success Criteria. Execute each check and capture results.

    SC #1 — uv sync + workspace-io tests:
    - `uv sync` exits 0
    - `uv run --package workspace-io pytest` exits 0 (no failures; expected ~67 tests from Plan 03)

    SC #2 — env-var rename verification:
    - `grep -rE 'CODE_WIKI_REAL_VAULT_PATH' packages/ agents/ --include="*.py" --include="*.toml" --include="*.md"` returns zero lines (excluding planning/ docs which may reference the rename history)
    - `grep -rE 'GRAPH_WIKI_WORKSPACE' packages/vault-io/src/ agents/code-wiki-agent/src/ --include="*.py"` returns at least 15 lines (env var is now the canonical reference)

    SC #3 — MCP boundary contract preserved:
    - vault-io test suite covers it: `uv run --package vault-io pytest -x` exits 0
    - Specifically `uv run --package vault-io pytest packages/vault-io/tests/test_ports_importable.py -v` shows the explicit-path short-circuit test passes

    SC #4 — PROJECT.md WS-10 entry exists (verified by Task 1 already; re-confirm here):
    - `grep -c 'wiki-config.toml' .planning/PROJECT.md` returns at least `1`
    - The entry is dated 2026-05-17 or later (recent)

    SC #5 — ported tests use `.graph-wiki.yaml` filename:
    - `grep -rc '.graph-wiki.yaml' packages/workspace-io/tests/ --include="*.py"` shows at least 10 hits (every test file that constructs a manifest path)
    - `grep -rc '.lattice.yaml' packages/workspace-io/tests/ --include="*.py"` returns 0 hits
    - `grep -rc 'lattice_workspace' packages/workspace-io/ --include="*.py"` returns 0 hits

    Two-phase init smoke test (D-07 verification — exercises Plan 05 wiring against Plan 02 source):
    Run an end-to-end init in a tmp dir and verify both phases complete:

    `TMPDIR=$(mktemp -d) && cd "$TMPDIR" && unset GRAPH_WIKI_WORKSPACE && uv run --package code-wiki-agent python -c "
import asyncio, sys
sys.path.insert(0, '$TMPDIR')
from pathlib import Path
from workspace_io import init as ws_init
import importlib.metadata
ws_init(Path('$TMPDIR'), plugin='code-wiki-agent', version=importlib.metadata.version('code-wiki-agent'))
manifest = Path('$TMPDIR/graph-wiki/.graph-wiki.yaml')
assert manifest.exists(), f'manifest not created: {manifest}'
print('two-phase init phase 1 ok:', manifest)
" && rm -rf "$TMPDIR"`

    (The full `code-wiki-agent init <path>` CLI invocation is a checkpoint:human-verify candidate but is omitted here per phase scope — the workspace_io.init half is the in-scope deliverable; init_wiki integration is exercised by existing vault-io tests.)

    If ANY check fails, stop and report which SC is unmet. Do NOT mark the task done.

    If all checks pass, the phase is complete. Summarize results in this task's completion notes for the SUMMARY.md output.
  </action>
  <acceptance_criteria>
    - SC #1: `uv sync && uv run --package workspace-io pytest` exits 0
    - SC #2 (env-var sweep clean): `grep -rE 'CODE_WIKI_REAL_VAULT_PATH' packages/ agents/ --include="*.py" --include="*.toml"` returns zero lines
    - SC #3: `uv run --package vault-io pytest` exits 0; explicit-path short-circuit test (test_resolve_wiki_and_repo_strict_raises_without_manifest + test_resolve_wiki_and_repo_honors_env_var) included in the pass count
    - SC #4: `.planning/PROJECT.md` contains a recent entry mentioning `wiki-config.toml` and `.graph-wiki.yaml`
    - SC #5: `grep -rc '\.lattice\.yaml' packages/workspace-io/tests/ --include="*.py"` returns `0`; `grep -rc 'lattice_workspace' packages/workspace-io/ --include="*.py"` returns `0`
    - Two-phase init smoke: workspace_io.init invoked against a tmp dir creates `<tmp>/graph-wiki/.graph-wiki.yaml`
  </acceptance_criteria>
  <verify>
    <automated>uv sync && uv run --package workspace-io pytest && uv run --package vault-io pytest && ! grep -rE 'CODE_WIKI_REAL_VAULT_PATH' packages/ agents/ --include="*.py" --include="*.toml" && ! grep -rE 'lattice_workspace|\.lattice\.yaml' packages/workspace-io/ --include="*.py"</automated>
  </verify>
  <done>
    All 5 Phase 11 success criteria are satisfied. Full workspace + vault-io test suites green. Zero CODE_WIKI_REAL_VAULT_PATH references in code. Zero lattice_workspace references in workspace-io. PROJECT.md WS-10 decision in place. Two-phase init smoke test passes. Phase 11 ready for `/gsd:verify-work`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — documentation-only edit + read-only verification) | This plan adds prose to PROJECT.md and runs grep/pytest |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-11 | Information Disclosure | PROJECT.md decision body | accept | Decision text references file names that are already public; no secrets included |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
- All 5 Phase 11 Success Criteria from ROADMAP §Phase 11 are satisfied
- All 10 WS-XX requirements are addressed by Plans 01-06:
  - WS-01 (workspace member): Plan 01
  - WS-02 (config.py port + resolve walk-up): Plan 02
  - WS-03 (manifest.py port for .graph-wiki.yaml): Plan 02
  - WS-04 (init.py port + CLI wiring): Plans 02 (port) + 05 (wire into commands/init.py)
  - WS-05 (paths/render/versions/_local_config/asset port): Plan 02
  - WS-06 (schema.py drop decision verified): Plan 02 (D-06 implemented)
  - WS-07 (env var + symbol rename): Plans 02 (workspace_io source) + 04 (vault-io) + 05 (agent)
  - WS-08 (vault-io delegation): Plan 04
  - WS-09 (ported tests pass): Plan 03
  - WS-10 (wiki-config.toml decision in PROJECT.md): Plan 06
</verification>

<success_criteria>
- WS-10 satisfied
- All 5 Phase 11 ROADMAP success criteria satisfied
- Phase 11 ready for `/gsd:verify-work` and milestone close-out
</success_criteria>

<output>
Create `.planning/phases/11-workspace-io-port-m1/11-06-SUMMARY.md` when done. Include in the summary: the 5 SC verification results, the WS-10 decision text inserted into PROJECT.md, and any remaining caveats for `/gsd:verify-work` to inspect.
</output>
