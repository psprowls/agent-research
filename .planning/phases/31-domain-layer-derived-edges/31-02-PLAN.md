---
phase: 31-domain-layer-derived-edges
plan: 02
type: execute
wave: 1
depends_on: [31-01]
files_modified:
  - packages/graph-io/src/graph_io/import_scan.py
  - packages/graph-io/src/graph_io/test_suites.py
  - packages/graph-io/tests/test_import_scan.py
autonomous: true
requirements:
  - DERIVED-01
  - DERIVED-02
tags:
  - graph-io
  - import-scan
  - shared-helper
  - phase-30-back-port

must_haves:
  truths:
    - "A new module packages/graph-io/src/graph_io/import_scan.py exists with the public function scan_package_imports(conn, repo_root, pkg_name, pkg_rel, *, include_test_files=False) -> set[tuple[str, str | None]] returning the distinct (pkg_name, pkg_rel) tuples of first-party Packages imported by files in the given Package (D-10)"
    - "import_scan.py contains both _PYTHON_IMPORT_RE = re.compile(r'^\\s*(?:from|import)\\s+([\\w\\.]+)', re.MULTILINE) and _JS_IMPORT_RE = re.compile(r\"\"\"(?:from|import|require)\\s*\\(?\\s*['\\\"]([^'\\\"]+)['\\\"]\"\"\") as module-private regexes (lifted verbatim from Phase 30's test_suites.py)"
    - "import_scan.py contains module-private helpers _build_pkg_index(pkg_rows_typed) -> list[tuple[str, str, str | None]] (deepest-prefix-first sort) and _build_importable_maps(pkg_rows_typed) -> tuple[dict[str, tuple[str, str | None]], dict[str, tuple[str, str | None]]] returning (py_importable_to_pkg, js_name_to_pkg) — same shapes Phase 30 builds inline"
    - "scan_package_imports queries File rows from the DB scoped to the given Package via 'SELECT path FROM nodes WHERE kind=\"file\" AND path LIKE ?' with the pkg_rel prefix + '/%', filtering further on attrs_json's is_test field when include_test_files=False"
    - "Test file filter: scan_package_imports parses each File row's attrs_json (which contains is_test set by Phase 29 structural_nodes.emit) and SKIPS files where is_test=true if include_test_files=False (D-11)"
    - "For each non-skipped file, scan_package_imports reads the file from disk (via repo_root / rel_path), catches OSError and continues, runs the appropriate regex by extension (.py uses _PYTHON_IMPORT_RE; .js/.jsx/.ts/.tsx/.mjs/.cjs use _JS_IMPORT_RE), and dispatches to _match_python_import / _match_js_import per match"
    - "_match_python_import takes a module string (the m.group(1) of _PYTHON_IMPORT_RE), splits on '.' and takes the first segment, then looks it up in py_importable_to_pkg; returns (pkg_name, pkg_rel) or None"
    - "_match_js_import takes a specifier string, an importing-file Path, repo_root, js_name_to_pkg, and pkg_index; for relative specifiers ('.' or '/' prefix) it tries common extensions (.ts, .js, .tsx, .jsx, /index.ts, /index.js) against (importing.parent / spec).resolve() and runs _owning_package on the first existing candidate; for bare specs it splits on '/' and uses the first segment (or first two for '@scope/name')"
    - "The function returns a set[tuple[str, str | None]] so callers can union across files; duplicates are deduplicated by the set semantics"
    - "Phase 30's test_suites.py is back-ported: _emit_tests_edges no longer contains _PYTHON_IMPORT_RE / _JS_IMPORT_RE / py_importable_to_pkg / js_name_to_pkg construction inline; instead it builds a per-suite set by calling scan_package_imports for each suite's File rows AND emitting the K=5 Repository edge when len(matched_pkgs) >= _REPOSITORY_EDGE_THRESHOLD"
    - "Implementation note for the back-port: because TestSuite -> File membership is by re-parenting under a TestSuite node, the existing _emit_tests_edges already iterates per-suite roots and per-file inside each suite. The back-port replaces the inline regex scan loop with a call to a new internal helper scan_files_imports(repo_root, file_paths, py_map, js_map, pkg_index) that takes a precomputed file list — OR refactor _emit_tests_edges to call scan_package_imports per Package and intersect with the suite's file membership. Choose the per-file variant to preserve Phase 30's exact semantics; introduce scan_files_imports as a public function alongside scan_package_imports."
    - "The actual public surface of import_scan.py is two functions: scan_package_imports(conn, repo_root, pkg_name, pkg_rel, *, include_test_files=False) for the Phase 31 derived-edges path, AND scan_files_imports(repo_root, file_rel_paths, pkg_rows_typed) for the Phase 30 test_suites path. Both share the same regex constants and helper builders. This avoids reshaping the per-file vs per-package contract."
    - "Phase 30 tests (packages/graph-io/tests/test_test_suites.py — to exist after Phase 30 execute) continue to pass after the back-port; the public behavior of test_suites.emit is unchanged (same edge sets emitted for the same input fixtures)"
    - "packages/graph-io/tests/test_import_scan.py exists with at least 8 distinct test functions covering: (a) Python imports resolved via py_importable_to_pkg; (b) JS bare-spec imports; (c) JS relative imports resolved through _owning_package; (d) JS scoped-package imports (@scope/name); (e) test files excluded when include_test_files=False; (f) test files included when include_test_files=True; (g) unreadable files (OSError) skipped silently; (h) stdlib / third-party imports ignored (not in maps)"
  artifacts:
    - path: "packages/graph-io/src/graph_io/import_scan.py"
      provides: "scan_package_imports + scan_files_imports — shared import-graph scanner for Phase 30 (TestSuite -> Package) and Phase 31 (references / depends_on derivation)"
      contains: "def scan_package_imports"
      exports:
        - scan_package_imports
        - scan_files_imports
    - path: "packages/graph-io/src/graph_io/test_suites.py"
      provides: "Back-ported test_suites.emit — internal scan loop now calls scan_files_imports"
      contains: "from graph_io.import_scan import"
    - path: "packages/graph-io/tests/test_import_scan.py"
      provides: "Unit coverage for the shared import scanner"
      contains: "test_scan_package_imports_python"
  key_links:
    - from: "scan_package_imports"
      to: "Phase 30 _owning_package + _resolve_import_root"
      via: "imports from graph_io.structural_nodes (hoisted in Phase 30 plan 30-01)"
      pattern: "from graph_io.structural_nodes import _owning_package, _resolve_import_root"
    - from: "test_suites._emit_tests_edges"
      to: "scan_files_imports"
      via: "function call — replaces inline regex scan"
      pattern: "scan_files_imports(repo_root="
---

<objective>
Extract the import-scan logic Phase 30 places inline inside
`_emit_tests_edges` into a shared `graph_io.import_scan` module so Phase 31's
`derived_edges.compute` can call the same scanner for production-code import
analysis. Back-port Phase 30's `test_suites.py` to use the new helper. Ship
unit tests for the helper itself.

This plan exists because CONTEXT.md D-10 forbids two parallel scanners — the
plan-checker's multi-source coverage audit would flag a duplicated regex
constant pair as a coverage gap. Phase 30 is in `planned` state at the time
of this plan; the back-port edits the not-yet-executed module so there is no
shipped code to migrate.

Output:
1. `import_scan.py` with `scan_package_imports` and `scan_files_imports`.
2. `test_suites.py` calls `scan_files_imports` instead of inline regex.
3. `test_import_scan.py` covers Python / JS / test-file-filter behavior.
4. Phase 30's `test_test_suites.py` continues to pass with identical edge sets.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-domain-layer-derived-edges/31-CONTEXT.md
@.planning/phases/31-domain-layer-derived-edges/31-RESEARCH.md
@.planning/phases/30-entry-points-test-suites/30-CONTEXT.md
@.planning/phases/30-entry-points-test-suites/30-03-PLAN.md
@packages/graph-io/src/graph_io/structural_nodes.py
@packages/graph-io/src/graph_io/upsert.py
@packages/graph-io/src/graph_io/uri.py

<interfaces>
<!-- Shapes the new module consumes. -->

From packages/graph-io/src/graph_io/structural_nodes.py (Phase 29 + Phase 30 plan 30-01):
  _owning_package(rel_path: str, pkg_index: list[tuple[str, str, str | None]]) -> tuple[str, str | None] | None
  _resolve_import_root(pkg_dir: Path, importable: str) -> Path | None

From packages/graph-io/src/graph_io/upsert.py:
  _upsert_edge / upsert_records  (called only by test_suites.py, not by import_scan.py)

From packages/graph-io/src/graph_io/uri.py:
  RepoContext                              # used as type hint in scan_package_imports for symmetry; not strictly required

Existing schema (packages/graph-io/src/graph_io/schema.py):
  nodes(id, kind, name, path, line, attrs_json, uri)
  edges(src, dst, kind, attrs_json)

Phase 30 plan 30-03 currently produces this inline (to be extracted):
  _PYTHON_IMPORT_RE = re.compile(r"^\s*(?:from|import)\s+([\w\.]+)", re.MULTILINE)
  _JS_IMPORT_RE = re.compile(r"""(?:from|import|require)\s*\(?\s*['"]([^'"]+)['"]""")
  pkg_index = [(prefix, name, rel)] sorted by len(prefix) DESC
  py_importable_to_pkg = {pkg.name.replace('-','_'): (name, rel)}
  js_name_to_pkg = {pkg.name: (name, rel)}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create import_scan.py module skeleton with regex constants and helper builders</name>
  <read_first>
    - packages/graph-io/src/graph_io/structural_nodes.py (lines around the module-level _owning_package and _resolve_import_root hoisted by Phase 30 plan 30-01 — confirm both are module-level and importable)
    - packages/graph-io/src/graph_io/uri.py (RepoContext shape — used for type hint symmetry)
    - .planning/phases/30-entry-points-test-suites/30-03-PLAN.md lines 632-737 (Phase 30's inline scan implementation — lift verbatim into the new module)
  </read_first>
  <files>packages/graph-io/src/graph_io/import_scan.py</files>
  <behavior>
    - A new file packages/graph-io/src/graph_io/import_scan.py exists with module docstring `"""Shared import-graph scanner. Used by test_suites.emit and derived_edges.compute (Phase 30 + Phase 31)."""`
    - Module-private regex constants _PYTHON_IMPORT_RE and _JS_IMPORT_RE match the patterns in Phase 30 plan 30-03 byte-for-byte
    - Module-private helpers _build_pkg_index(pkg_rows_typed) and _build_importable_maps(pkg_rows_typed) build the deepest-prefix-first index and the (py_importable_to_pkg, js_name_to_pkg) tuple respectively
    - PKG_ROW_TYPE = tuple[str, str | None, str | None] is exported as a type alias for the (pkg_name, pkg_rel, pkg_attrs_json) tuple shape so callers don't need to redeclare it
    - The module imports _owning_package and _resolve_import_root from graph_io.structural_nodes
    - The module imports json (for parsing pkg_attrs_json and File.attrs_json) and re (for the regex constants); it imports Path from pathlib
    - The module does NOT import sqlite3 — connection handling is the caller's responsibility (the helpers operate on pkg_rows_typed and file_rel_paths, not on the connection directly)
    - However scan_package_imports (added in Task 2) DOES take conn as its first arg
    - Module imports cleanly: `uv run --package graph-io python -c "from graph_io import import_scan; print('ok')"` exits 0
  </behavior>
  <action>
    Create packages/graph-io/src/graph_io/import_scan.py with the following
    skeleton (Task 2 fills in scan_package_imports / scan_files_imports):

      """Shared import-graph scanner.

      Used by test_suites.emit (Phase 30, TestSuite -> Package edges) and
      derived_edges.compute (Phase 31, references / depends_on derivation).
      The two callers differ only in whether test files are included; the
      regex passes and the package-prefix index are identical.
      """

      from __future__ import annotations

      import json
      import re
      from pathlib import Path

      from graph_io.structural_nodes import _owning_package, _resolve_import_root  # noqa: F401

      PkgRow = tuple[str, str | None, str | None]
      """(pkg_name, pkg_rel, pkg_attrs_json) — the tuple shape callers pass in."""

      _PYTHON_IMPORT_RE = re.compile(r"^\s*(?:from|import)\s+([\w\.]+)", re.MULTILINE)
      _JS_IMPORT_RE = re.compile(
          r"""(?:from|import|require)\s*\(?\s*['"]([^'"]+)['"]"""
      )

      _JS_EXTENSIONS = (".ts", ".js", ".tsx", ".jsx", ".mjs", ".cjs")
      _JS_INDEX_SUFFIXES = ("index.ts", "index.js", "index.tsx", "index.jsx")
      _SCAN_EXTENSIONS_JS = frozenset(_JS_EXTENSIONS)


      def _build_pkg_index(
          pkg_rows: list[PkgRow],
      ) -> list[tuple[str, str, str | None]]:
          """Return [(pkg_prefix, pkg_name, pkg_rel)] sorted deepest-prefix-first.

          pkg_prefix is pkg_rel if not None else "" (root-rooted packages).
          """
          rows: list[tuple[str, str, str | None]] = []
          for pkg_name, pkg_rel, _attrs in pkg_rows:
              prefix = pkg_rel if pkg_rel is not None else ""
              rows.append((prefix, pkg_name, pkg_rel))
          rows.sort(key=lambda row: len(row[0]), reverse=True)
          return rows


      def _build_importable_maps(
          pkg_rows: list[PkgRow],
      ) -> tuple[
          dict[str, tuple[str, str | None]],
          dict[str, tuple[str, str | None]],
      ]:
          """Return (py_importable_to_pkg, js_name_to_pkg).

          Python: pkg.name.replace('-', '_') -> (name, rel)
          JS/TS:  pkg.name -> (name, rel)  (package.json name including @scope)
          """
          py_map: dict[str, tuple[str, str | None]] = {}
          js_map: dict[str, tuple[str, str | None]] = {}
          for pkg_name, pkg_rel, pkg_attrs_json in pkg_rows:
              attrs = json.loads(pkg_attrs_json) if pkg_attrs_json else {}
              lang = attrs.get("language")
              if lang == "python":
                  importable = pkg_name.replace("-", "_")
                  py_map[importable] = (pkg_name, pkg_rel)
              elif lang in {"javascript", "typescript"}:
                  js_map[pkg_name] = (pkg_name, pkg_rel)
          return py_map, js_map


      def _match_python_import(
          module_str: str,
          py_map: dict[str, tuple[str, str | None]],
      ) -> tuple[str, str | None] | None:
          """Return (pkg_name, pkg_rel) for the first dotted segment, or None."""
          top = module_str.split(".", 1)[0]
          return py_map.get(top)


      def _match_js_import(
          spec: str,
          importing_file: Path,
          repo_root: Path,
          js_map: dict[str, tuple[str, str | None]],
          pkg_index: list[tuple[str, str, str | None]],
      ) -> tuple[str, str | None] | None:
          """Resolve a JS/TS import specifier to (pkg_name, pkg_rel) or None."""
          if spec.startswith(".") or spec.startswith("/"):
              try:
                  resolved = (importing_file.parent / spec).resolve()
              except OSError:
                  return None
              candidates: list[Path] = [resolved]
              for ext in _JS_EXTENSIONS:
                  candidates.append(resolved.with_suffix(ext))
              for idx in _JS_INDEX_SUFFIXES:
                  candidates.append(resolved / idx)
              for cand in candidates:
                  if cand.exists():
                      try:
                          rel = cand.relative_to(repo_root).as_posix()
                      except ValueError:
                          continue
                      return _owning_package(rel, pkg_index)
              return None
          # Bare specifier — split @scope/name (two segments) or name (one)
          if spec.startswith("@"):
              key = "/".join(spec.split("/", 2)[:2])
          else:
              key = spec.split("/", 1)[0]
          return js_map.get(key)


      # Task 2 adds: scan_package_imports / scan_files_imports

    Run the module-imports-cleanly check to confirm.
  </action>
  <verify>
    <automated>cd /Users/pat/Personal/agent-research && uv run --package graph-io python -c "from graph_io import import_scan; assert callable(import_scan._build_pkg_index); assert callable(import_scan._build_importable_maps); assert import_scan._PYTHON_IMPORT_RE.pattern == r'^\\s*(?:from|import)\\s+([\\w\\.]+)'; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/graph-io/src/graph_io/import_scan.py` exists
    - `grep -n '^_PYTHON_IMPORT_RE' packages/graph-io/src/graph_io/import_scan.py` returns 1 line
    - `grep -n '^_JS_IMPORT_RE' packages/graph-io/src/graph_io/import_scan.py` returns 1 line
    - `grep -n '^def _build_pkg_index\|^def _build_importable_maps\|^def _match_python_import\|^def _match_js_import' packages/graph-io/src/graph_io/import_scan.py` returns exactly 4 lines
    - `grep -n 'from graph_io.structural_nodes import _owning_package, _resolve_import_root' packages/graph-io/src/graph_io/import_scan.py` returns 1 line
    - Module imports cleanly: the verify command exits 0
  </acceptance_criteria>
  <done>
    Skeleton in place with regex constants, helper builders, and the
    _match_* dispatchers; ready for the public scan_* functions in Task 2.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement scan_package_imports and scan_files_imports public functions</name>
  <read_first>
    - packages/graph-io/src/graph_io/import_scan.py (from Task 1)
    - .planning/phases/31-domain-layer-derived-edges/31-CONTEXT.md (D-10, D-11 — public contract)
    - packages/graph-io/src/graph_io/upsert.py (NodeKey shape — informs the path-prefix query in scan_package_imports)
    - .planning/phases/30-entry-points-test-suites/30-03-PLAN.md lines 654-715 (per-file scan loop — lift logic into scan_files_imports)
  </read_first>
  <files>packages/graph-io/src/graph_io/import_scan.py</files>
  <behavior>
    - scan_package_imports(conn, repo_root, pkg_name, pkg_rel, *, include_test_files=False) returns a set[tuple[str, str | None]] — the (pkg_name, pkg_rel) of every first-party Package imported by files within the given Package
    - The function SELECTs File node rows scoped to the Package via the path LIKE prefix; for each row it reads attrs_json's `is_test` field and skips when include_test_files=False AND is_test=True
    - Internally it loads ALL Package rows once (for the py_map / js_map / pkg_index), then iterates the filtered File rows and calls scan_files_imports under the hood
    - scan_files_imports(repo_root, file_rel_paths, pkg_rows) returns a set[tuple[str, str | None]] — the (pkg_name, pkg_rel) of every first-party Package imported by any file in the given list
    - scan_files_imports builds the maps + index once from pkg_rows, then iterates file_rel_paths, reads each file with read_text(errors='ignore'), catches OSError and continues
    - For each file: dispatch by extension. .py → iterate _PYTHON_IMPORT_RE matches; .js/.jsx/.ts/.tsx/.mjs/.cjs → iterate _JS_IMPORT_RE matches; otherwise skip
    - All resolved (pkg_name, pkg_rel) tuples are added to the result set; the set's union semantics dedupe naturally
    - scan_package_imports does NOT mutate the connection (no INSERT/UPDATE/DELETE); only SELECT
    - File path resolution: the rel_path stored on File.path is repo-rooted and posix-formatted (Phase 28 convention); full disk path is repo_root / rel_path
  </behavior>
  <action>
    Append the following functions to packages/graph-io/src/graph_io/import_scan.py
    (below the helpers added in Task 1):

      import sqlite3


      def scan_files_imports(
          repo_root: Path,
          file_rel_paths: list[str],
          pkg_rows: list[PkgRow],
      ) -> set[tuple[str, str | None]]:
          """Scan a list of files for first-party Package imports.

          Returns the deduplicated set of (pkg_name, pkg_rel) tuples for every
          first-party Package the files import. Unreadable files (OSError) are
          silently skipped. Stdlib / third-party imports that don't appear in
          the py/js maps are ignored.
          """
          py_map, js_map = _build_importable_maps(pkg_rows)
          pkg_index = _build_pkg_index(pkg_rows)
          matched: set[tuple[str, str | None]] = set()
          for rel in file_rel_paths:
              fpath = repo_root / rel
              try:
                  content = fpath.read_text(encoding="utf-8", errors="ignore")
              except OSError:
                  continue
              ext = Path(rel).suffix
              if ext == ".py":
                  for m in _PYTHON_IMPORT_RE.finditer(content):
                      hit = _match_python_import(m.group(1), py_map)
                      if hit is not None:
                          matched.add(hit)
              elif ext in _SCAN_EXTENSIONS_JS:
                  for m in _JS_IMPORT_RE.finditer(content):
                      hit = _match_js_import(
                          m.group(1), fpath, repo_root, js_map, pkg_index,
                      )
                      if hit is not None:
                          matched.add(hit)
          return matched


      def scan_package_imports(
          conn: sqlite3.Connection,
          repo_root: Path,
          pkg_name: str,
          pkg_rel: str | None,
          *,
          include_test_files: bool = False,
      ) -> set[tuple[str, str | None]]:
          """Return distinct first-party Packages imported by files in pkg.

          pkg_rel is the Package's repo-relative path prefix (None for root).
          include_test_files=False excludes files with attrs_json.is_test=true
          (Phase 31 D-11). include_test_files=True returns all imports —
          needed by Phase 30 test_suites.emit when scanning test-file imports.
          """
          # Collect all Package rows once for the maps/index
          pkg_rows: list[PkgRow] = [
              (row[0], row[1], row[2])
              for row in conn.execute(
                  "SELECT name, path, attrs_json FROM nodes WHERE kind='package'"
              ).fetchall()
          ]

          # Query File rows scoped to the Package
          if pkg_rel:
              like = pkg_rel + "/%"
              rows = conn.execute(
                  "SELECT path, attrs_json FROM nodes WHERE kind='file' AND path LIKE ?",
                  (like,),
              ).fetchall()
          else:
              rows = conn.execute(
                  "SELECT path, attrs_json FROM nodes WHERE kind='file'"
              ).fetchall()

          file_paths: list[str] = []
          for path, attrs_json in rows:
              if not include_test_files:
                  attrs = json.loads(attrs_json) if attrs_json else {}
                  if attrs.get("is_test"):
                      continue
              file_paths.append(path)

          return scan_files_imports(repo_root, file_paths, pkg_rows)

    Both functions are public (no leading underscore). Document the contract
    in their docstrings; do not duplicate it in module-level comments.
  </action>
  <verify>
    <automated>cd /Users/pat/Personal/agent-research && uv run --package graph-io python -c "from graph_io.import_scan import scan_package_imports, scan_files_imports; assert callable(scan_package_imports); assert callable(scan_files_imports); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '^def scan_files_imports' packages/graph-io/src/graph_io/import_scan.py` returns 1 line
    - `grep -n '^def scan_package_imports' packages/graph-io/src/graph_io/import_scan.py` returns 1 line
    - Both functions are reachable via `from graph_io.import_scan import scan_package_imports, scan_files_imports`
    - `grep -F 'include_test_files: bool = False' packages/graph-io/src/graph_io/import_scan.py` exits 0 (D-11 default)
    - `grep -F 'attrs.get(\"is_test\")' packages/graph-io/src/graph_io/import_scan.py` exits 0 (test-file filter)
    - `grep -F "kind='file'" packages/graph-io/src/graph_io/import_scan.py` exits 0 (File-scoped SELECT)
    - Module imports cleanly: the verify command exits 0
  </acceptance_criteria>
  <done>
    `scan_package_imports` and `scan_files_imports` are the public surface
    Phase 31's derived_edges.compute and Phase 30's back-ported test_suites
    consume.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add unit tests in test_import_scan.py covering py / js / test-filter behavior</name>
  <read_first>
    - packages/graph-io/src/graph_io/import_scan.py (full file after Tasks 1-2)
    - packages/graph-io/tests/conftest.py (existing test scaffolding)
    - packages/graph-io/tests/test_structural_nodes.py (analog test patterns — packages.refresh + structural_nodes.emit + pytest tmp_path setup)
    - packages/graph-io/tests/test_packages.py (analog for setting up Package rows in a fresh DB)
  </read_first>
  <files>packages/graph-io/tests/test_import_scan.py</files>
  <behavior>
    - tests/test_import_scan.py exists with at least 8 distinct test functions per must_haves truths
    - Each test follows the pattern: tmp_path → write packages on disk → store.connect + schema.apply_schema → packages.refresh → structural_nodes.emit (to populate File rows with attrs.is_test) → call scan_package_imports or scan_files_imports → assertEqual on returned set
    - For (a) Python imports: fixture has package_a with src/package_a/foo.py importing package_b; package_b is a sibling package. Assert scan returns {(package_b_name, package_b_rel)}.
    - For (b) JS bare-spec: fixture has @scope/foo and bar packages; a file in @scope/foo imports 'bar'. Assert resolution.
    - For (c) JS relative import: a file in pkg_a imports '../pkg_b/index.js'. Assert resolution via _owning_package.
    - For (d) Scoped package: a file in pkg_a imports '@scope/foo'; @scope/foo is a Package with that name. Assert resolution.
    - For (e) Test file excluded: a test file in pkg_a/tests/test_foo.py imports pkg_b; with include_test_files=False, scan returns empty (or missing pkg_b from the result).
    - For (f) Test file included: same fixture as (e), with include_test_files=True; scan returns {pkg_b}.
    - For (g) Unreadable file: a File row pointing at a path that doesn't exist on disk; scan does not raise, returns the result computed from the other files.
    - For (h) Stdlib import: a file imports `json` and `typing`; scan returns empty (neither name is in py_map).
  </behavior>
  <action>
    Create packages/graph-io/tests/test_import_scan.py. Skeleton (adapt to
    actual conftest fixtures):

      """Unit tests for graph_io.import_scan (Phase 31 D-10)."""

      from __future__ import annotations

      import sqlite3
      from pathlib import Path

      import pytest

      from graph_io import packages, schema, store, structural_nodes
      from graph_io.import_scan import scan_files_imports, scan_package_imports
      from graph_io.uri import RepoContext

      CTX = RepoContext(org="testorg", repo="testrepo")


      def _setup(tmp_path: Path) -> sqlite3.Connection:
          conn = store.connect(tmp_path / "code.db", create=True)
          return conn


      def _emit_pipeline(conn: sqlite3.Connection, repo_root: Path) -> None:
          with store.transaction(conn):
              packages.refresh(conn, repo_root=repo_root, ctx=CTX)
              structural_nodes.emit(
                  conn, repo_root=repo_root, ctx=CTX, skip_dirs=frozenset(),
              )


      def _write_pkg_py(root: Path, name: str, files: dict[str, str]) -> None:
          pkg_dir = root / "packages" / name
          src_dir = pkg_dir / "src" / name.replace("-", "_")
          src_dir.mkdir(parents=True, exist_ok=True)
          (src_dir / "__init__.py").write_text("")
          (pkg_dir / "pyproject.toml").write_text(
              f'[project]\nname = "{name}"\nversion = "0.0.0"\n'
              f'requires-python = ">=3.11"\n'
              f'[build-system]\nrequires = ["uv_build"]\nbuild-backend = "uv_build"\n'
          )
          for rel, content in files.items():
              (pkg_dir / rel).parent.mkdir(parents=True, exist_ok=True)
              (pkg_dir / rel).write_text(content)


      def _write_pkg_js(root: Path, name: str, files: dict[str, str]) -> None:
          # @scope/foo -> packages/@scope/foo for fixture simplicity
          pkg_dir = root / "packages" / name.replace("/", "__")
          pkg_dir.mkdir(parents=True, exist_ok=True)
          (pkg_dir / "package.json").write_text(
              '{"name": "' + name + '", "version": "0.0.0"}'
          )
          for rel, content in files.items():
              (pkg_dir / rel).parent.mkdir(parents=True, exist_ok=True)
              (pkg_dir / rel).write_text(content)


      def test_scan_package_imports_python(tmp_path: Path) -> None:
          _write_pkg_py(tmp_path, "pkg-a", {
              "src/pkg_a/foo.py": "from pkg_b import bar\n",
          })
          _write_pkg_py(tmp_path, "pkg-b", {
              "src/pkg_b/bar.py": "x = 1\n",
          })
          conn = _setup(tmp_path)
          _emit_pipeline(conn, tmp_path)
          result = scan_package_imports(
              conn, tmp_path, "pkg-a", "packages/pkg-a", include_test_files=False,
          )
          assert ("pkg-b", "packages/pkg-b") in result

      # ... seven more tests following the same shape:
      # test_scan_js_bare_spec_resolved
      # test_scan_js_relative_import_resolved
      # test_scan_js_scoped_package
      # test_scan_excludes_test_files_by_default
      # test_scan_includes_test_files_when_flag_set
      # test_scan_unreadable_file_silently_skipped
      # test_scan_stdlib_imports_ignored

    Each test must use the EXACT _emit_pipeline shape to ensure
    structural_nodes.emit has populated File.attrs.is_test correctly. The
    test_files filter test (e/f) writes a file at
    packages/pkg-a/tests/test_foo.py so it gets is_test=true via the
    tests/-ancestor rule from Phase 29.

    For (g) unreadable file: use a sentinel path that's listed in the File
    nodes but absent from disk — easiest is to call scan_files_imports
    directly with a hand-built list including a non-existent path; assert
    scan returns the set from the readable files only.

    For (h) stdlib: write a file with `import json` and `import typing`
    only; assert the result set is empty (no packages in the test fixture
    are named json/typing).
  </action>
  <verify>
    <automated>cd /Users/pat/Personal/agent-research && uv run --package graph-io pytest packages/graph-io/tests/test_import_scan.py -q</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/graph-io/tests/test_import_scan.py` exists
    - `grep -cE '^def test_' packages/graph-io/tests/test_import_scan.py` returns >= 8 (eight distinct test functions)
    - `grep -F 'scan_package_imports' packages/graph-io/tests/test_import_scan.py` exits 0
    - `grep -F 'scan_files_imports' packages/graph-io/tests/test_import_scan.py` exits 0
    - `grep -F 'include_test_files=True' packages/graph-io/tests/test_import_scan.py` exits 0 (test (f))
    - `grep -F 'include_test_files=False' packages/graph-io/tests/test_import_scan.py` exits 0 (test (e))
    - `uv run --package graph-io pytest packages/graph-io/tests/test_import_scan.py -q` exits 0 with at least 8 passed tests
  </acceptance_criteria>
  <done>
    The shared scanner has direct unit coverage independent of Phase 30's
    test_suites integration tests. Future regressions to either consumer can
    be triaged against this suite.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Back-port test_suites.py to use scan_files_imports</name>
  <read_first>
    - packages/graph-io/src/graph_io/test_suites.py (full file — Phase 30 deliverable; expected shape per .planning/phases/30-entry-points-test-suites/30-03-PLAN.md lines 595-737)
    - packages/graph-io/src/graph_io/import_scan.py (full file — public surface from Tasks 1-2)
    - packages/graph-io/tests/test_test_suites.py (Phase 30 deliverable — expected shape per 30-03-PLAN.md task 4; tests must continue to pass after back-port)
  </read_first>
  <files>packages/graph-io/src/graph_io/test_suites.py</files>
  <behavior>
    - test_suites.py no longer contains module-level _PYTHON_IMPORT_RE or _JS_IMPORT_RE constants — both come from import_scan
    - test_suites.py imports scan_files_imports from graph_io.import_scan at the top of the module
    - The _emit_tests_edges function builds the pkg_rows list (shape: list[tuple[str, str | None, str | None]]) and the repo-root path, then for each TestSuite root: collects the suite's File rel_paths into a list, calls scan_files_imports(repo_root, file_rel_paths, pkg_rows), and emits one tests edge per resolved (pkg_name, pkg_rel) in the result set
    - The K=5 Repository edge threshold logic (D-12 in Phase 30) is preserved: if len(matched_pkgs) >= _REPOSITORY_EDGE_THRESHOLD emit one TestSuite -> Repository edge
    - _REPOSITORY_EDGE_THRESHOLD remains a module-level constant in test_suites.py (Phase 30 D-12 keeps the threshold local to the consumer)
    - _build_pkg_index remains in test_suites.py ONLY IF it has Phase 30-specific behavior beyond import_scan's _build_pkg_index; in practice, Phase 30 uses the same shape, so test_suites.py removes its private _build_pkg_index and consumes import_scan's helper indirectly through scan_files_imports
    - All Phase 30 tests in packages/graph-io/tests/test_test_suites.py continue to pass after the back-port (same edge sets emitted for the same input fixtures)
    - Public behavior of test_suites.emit is unchanged: same parameters, same return type, same emitted edges
  </behavior>
  <action>
    Edit packages/graph-io/src/graph_io/test_suites.py:

    1. Remove the module-level constants:
         _PYTHON_IMPORT_RE = re.compile(...)
         _JS_IMPORT_RE = re.compile(...)

    2. Remove any private helper that becomes redundant:
         _build_pkg_index   (use import_scan's via scan_files_imports)
         _build_importable_maps  (same)

    3. Keep _REPOSITORY_EDGE_THRESHOLD = 5 as a module constant — it's the
       Phase 30 D-12 K threshold, semantically owned by test_suites.

    4. Add import at the top of the module:
         from graph_io.import_scan import scan_files_imports

    5. Rewrite the body of _emit_tests_edges. The new shape:

         def _emit_tests_edges(
             conn, repo_root, ctx, pkg_rows_typed, roots, root_files, repo_key,
         ) -> None:
             # Cast pkg_rows_typed to import_scan's PkgRow shape (identical)
             pkg_rows = list(pkg_rows_typed)
             edges_out: list[GraphEdge] = []
             for r in roots:
                 if r.owner_kind == "repository":
                     suite_key_name = r.rel_path
                 else:
                     suite_key_name = Path(r.rel_path).name
                 suite_key = ("test_suite", suite_key_name, r.rel_path)

                 # All test file paths in this suite
                 file_rel_paths = list(root_files[r.rel_path])

                 matched_pkgs = scan_files_imports(
                     repo_root, file_rel_paths, pkg_rows,
                 )

                 for pkg_name, pkg_rel in matched_pkgs:
                     edges_out.append(GraphEdge(
                         src=suite_key,
                         dst=("package", pkg_name, pkg_rel),
                         kind="tests",
                         attrs={},
                     ))

                 if len(matched_pkgs) >= _REPOSITORY_EDGE_THRESHOLD:
                     edges_out.append(GraphEdge(
                         src=suite_key,
                         dst=repo_key,
                         kind="tests",
                         attrs={},
                     ))

             if edges_out:
                 upsert.upsert_records(conn, GraphRecords(nodes=[], edges=edges_out))

    Remove the `import re` line at the top of test_suites.py if no other
    usage remains. Keep all other imports (json, sqlite3, etc.) only if
    they're still referenced elsewhere in the module.

    Do NOT alter the public emit() function signature, the TestSuite-node
    emission logic, or the file re-parenting logic. Only the internals of
    _emit_tests_edges change.
  </action>
  <verify>
    <automated>cd /Users/pat/Personal/agent-research && uv run --package graph-io pytest packages/graph-io/tests/test_test_suites.py packages/graph-io/tests/test_import_scan.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'from graph_io.import_scan import scan_files_imports' packages/graph-io/src/graph_io/test_suites.py` returns 1 line
    - `grep -nE '^_PYTHON_IMPORT_RE|^_JS_IMPORT_RE' packages/graph-io/src/graph_io/test_suites.py` returns 0 lines (constants moved to import_scan)
    - `grep -n '_REPOSITORY_EDGE_THRESHOLD = 5' packages/graph-io/src/graph_io/test_suites.py` returns 1 line (still owned by test_suites)
    - `grep -F 'scan_files_imports(repo_root' packages/graph-io/src/graph_io/test_suites.py` exits 0
    - `uv run --package graph-io pytest packages/graph-io/tests/test_test_suites.py -q` exits 0 (Phase 30 tests pass — back-port preserves public behavior)
    - `uv run --package graph-io pytest packages/graph-io/tests/test_import_scan.py -q` exits 0 (Task 3 tests still pass — back-port doesn't break the helper)
  </acceptance_criteria>
  <done>
    Phase 30's test_suites.py consumes the shared scanner. There is exactly
    one regex-based import scanner in the codebase (in import_scan.py).
    Phase 31's derived_edges.compute (Plan 31-04) reads from the same surface.
  </done>
</task>

</tasks>

<verification>
The full graph-io test suite must remain green after all four tasks complete:

  cd /Users/pat/Personal/agent-research && uv run --package graph-io pytest packages/graph-io/tests/ -q

Expected: 0 failures, 0 errors. Net test count change vs the pre-plan
baseline: + (count of new tests in test_import_scan.py, >= 8).
</verification>

<success_criteria>
- [ ] `packages/graph-io/src/graph_io/import_scan.py` exists with `scan_package_imports` and `scan_files_imports` (D-10)
- [ ] `_PYTHON_IMPORT_RE` and `_JS_IMPORT_RE` live in `import_scan.py` only (no duplication)
- [ ] `test_suites.py` (Phase 30 deliverable) imports `scan_files_imports` and no longer holds the regex constants
- [ ] `packages/graph-io/tests/test_import_scan.py` has >= 8 tests, all green
- [ ] `packages/graph-io/tests/test_test_suites.py` (Phase 30 tests) remains green after the back-port
- [ ] Full graph-io test suite exits 0
</success_criteria>
