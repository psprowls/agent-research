---
phase: 31-domain-layer-derived-edges
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - packages/graph-io/pyproject.toml
  - packages/graph-io/src/graph_io/uri.py
  - packages/graph-io/tests/test_uri.py
autonomous: true
requirements:
  - DOMAIN-01
  - DOMAIN-03
tags:
  - graph-io
  - wave-0
  - roadmap-amendment
  - pyyaml-dep
  - uri-shape

must_haves:
  truths:
    - "ROADMAP.md Phase 31 Success Criterion #2 wording is amended per D-15: it reads 'skip ONLY the cycle-participating containment edges (keeping the acyclic remainder)' instead of 'skip all domain_contains_domain edges'. The amendment is a single-line edit to line 168 of .planning/ROADMAP.md."
    - "packages/graph-io/pyproject.toml adds 'pyyaml>=6.0' to its [project].dependencies list so domains.emit can use yaml.safe_load (D-06). No other dependency changes."
    - "packages/graph-io/src/graph_io/uri.py amends domain_uri from def domain_uri(name: str) -> str to def domain_uri(ctx: RepoContext, name: str) -> str returning f'domain:{ctx.org}/{ctx.repo}/{name}' (D-05). All other helpers in uri.py are unchanged."
    - "packages/graph-io/tests/test_uri.py replaces test_domain_uri_has_no_ctx with test_domain_uri_with_ctx asserting domain_uri(RepoContext('acme', 'repo'), 'billing') == 'domain:acme/repo/billing'. No other test changes."
    - "After this plan, pyyaml is importable from inside the graph-io workspace package (uv sync re-resolves the lock when CI runs against the new pyproject.toml)."
    - "Zero callers of domain_uri exist outside uri.py and test_uri.py — verified by grep before the edit; the signature break is safe."
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Amended SC#2 wording for Phase 31 (D-15)"
      contains: "skip ONLY the cycle-participating containment edges"
    - path: "packages/graph-io/pyproject.toml"
      provides: "pyyaml dependency declaration"
      contains: "pyyaml>=6.0"
    - path: "packages/graph-io/src/graph_io/uri.py"
      provides: "domain_uri(ctx, name) with ctx-aware URI shape"
      contains: "def domain_uri(ctx: RepoContext, name: str)"
      exports:
        - domain_uri
    - path: "packages/graph-io/tests/test_uri.py"
      provides: "Updated test for ctx-aware domain_uri"
      contains: "test_domain_uri_with_ctx"
  key_links:
    - from: "domain_uri"
      to: "RepoContext"
      via: "function parameter; consistent with pkg_uri / file_uri shape"
      pattern: "ctx: RepoContext"
---

<objective>
Land three independent single-file edits required before Phase 31's Wave 1
plans can start: (1) amend `.planning/ROADMAP.md` SC#2 wording per D-15 so
downstream verification reads the corrected text; (2) add `pyyaml>=6.0` to
`packages/graph-io/pyproject.toml` dependencies so the Wave 1 `domains.emit`
plan can use `yaml.safe_load`; (3) amend `uri.py::domain_uri` to take
`(ctx, name)` and produce `domain:<org>/<repo>/<name>` per D-05, plus update
the single existing test in `test_uri.py` that asserts the old single-arg
shape.

No new modules are created. No new tests are added beyond the in-place
amendment to `test_domain_uri_has_no_ctx`. This plan is intentionally tiny —
each edit is a precondition for a different Wave 1 plan and bundling them
into one Wave 0 plan keeps the dependency graph shallow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-domain-layer-derived-edges/31-CONTEXT.md
@.planning/phases/31-domain-layer-derived-edges/31-RESEARCH.md
@packages/graph-io/pyproject.toml
@packages/graph-io/src/graph_io/uri.py
@packages/graph-io/tests/test_uri.py

<interfaces>
<!-- Existing surfaces being amended (no new surfaces in this plan). -->

From packages/graph-io/src/graph_io/uri.py (Phase 28):
  RepoContext                              # frozen dataclass(org, repo)
  repo_uri(ctx) -> str
  pkg_uri(ctx, name) -> str
  subpkg_uri(ctx, pkg_name, dotted_path) -> str
  file_uri(ctx, rel_path) -> str
  entry_point_uri(ctx, pkg_name, ep_name) -> str
  test_suite_uri(ctx, suite_name) -> str
  domain_uri(name) -> str                  # CURRENT — to be amended

From packages/graph-io/tests/test_uri.py:
  test_domain_uri_has_no_ctx               # asserts domain_uri("billing") == "domain:billing" — to be replaced
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Amend ROADMAP.md SC#2 wording (D-15)</name>
  <read_first>
    - .planning/ROADMAP.md (line 168 — the SC#2 entry for Phase 31)
    - .planning/phases/31-domain-layer-derived-edges/31-CONTEXT.md (D-15 — exact required wording)
  </read_first>
  <files>.planning/ROADMAP.md</files>
  <behavior>
    - Line 168 of .planning/ROADMAP.md (the SC#2 entry inside the Phase 31 block) reads exactly: "  2. A `domains.yaml` with a cycle (`payments → billing → payments`) causes `domains.emit` to print a warning identifying the cycle and skip ONLY the cycle-participating containment edges (keeping the acyclic remainder) without crashing — `cg update` exits 0"
    - No other line in ROADMAP.md is modified.
    - The replacement is a single-line edit; line numbers in the file are preserved (no inserted or removed lines).
  </behavior>
  <action>
    In .planning/ROADMAP.md, find the line that begins with "  2. A
    `domains.yaml` with a cycle" inside the Phase 31 block (currently line 168).
    Replace the entire line's content from "skip all `domain_contains_domain`
    edges without crashing" with "skip ONLY the cycle-participating containment
    edges (keeping the acyclic remainder) without crashing".

    Exact target line after the edit:

      2. A `domains.yaml` with a cycle (`payments → billing → payments`) causes `domains.emit` to print a warning identifying the cycle and skip ONLY the cycle-participating containment edges (keeping the acyclic remainder) without crashing — `cg update` exits 0

    Do NOT touch SC#1, SC#3, SC#4, SC#5, or any neighbouring phase block.
    Do NOT renumber, reorder, or rewrap.
  </action>
  <verify>
    <automated>cd /Users/pat/Personal/agent-research && grep -F 'skip ONLY the cycle-participating containment edges (keeping the acyclic remainder)' .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'skip ONLY the cycle-participating containment edges (keeping the acyclic remainder)' .planning/ROADMAP.md` exits 0 and prints exactly one matching line
    - `grep -F 'skip all `domain_contains_domain` edges without crashing' .planning/ROADMAP.md` exits 1 (the old wording is gone)
    - `wc -l .planning/ROADMAP.md` returns the same line count as before the edit (single-line in-place replacement, no insertion or deletion)
    - The Phase 31 block header line `### Phase 31: Domain Layer + Derived Edges` is still present
  </acceptance_criteria>
  <done>
    SC#2 reads per D-15. Downstream verification of Phase 31 reads the
    corrected wording.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add pyyaml dependency to graph-io pyproject.toml (D-06)</name>
  <read_first>
    - packages/graph-io/pyproject.toml (current [project].dependencies list)
    - packages/workspace-io/pyproject.toml (confirms pyyaml>=6.0 is already in the workspace dependency set — the same version pin should be reused)
  </read_first>
  <files>packages/graph-io/pyproject.toml</files>
  <behavior>
    - packages/graph-io/pyproject.toml [project].dependencies contains "pyyaml>=6.0" as a new entry alongside the existing "source-parser" and "workspace-io" entries.
    - The dependencies list ordering: keep the existing order, add "pyyaml>=6.0" as the LAST entry (alphabetical order is not required; matching existing style is sufficient).
    - No [tool.uv.sources] entry is needed — pyyaml is a published package, not a workspace member.
    - No other section of pyproject.toml is modified.
  </behavior>
  <action>
    In packages/graph-io/pyproject.toml, locate the dependencies list:

      dependencies = [
        "source-parser",
        "workspace-io",
      ]

    Replace it with:

      dependencies = [
        "source-parser",
        "workspace-io",
        "pyyaml>=6.0",
      ]

    Do NOT touch any other line, including [tool.uv.sources],
    [tool.pytest.ini_options], [build-system], or the [project] header.
  </action>
  <verify>
    <automated>cd /Users/pat/Personal/agent-research && grep -F 'pyyaml>=6.0' packages/graph-io/pyproject.toml && uv sync --package graph-io 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'pyyaml>=6.0' packages/graph-io/pyproject.toml` exits 0
    - `grep -c '^"' packages/graph-io/pyproject.toml | head -1` shows the dependencies list grew by exactly one entry (three deps total: source-parser, workspace-io, pyyaml)
    - `uv sync --package graph-io` exits 0 (pyyaml is fetchable from the index)
    - `uv run --package graph-io python -c "import yaml; print(yaml.__version__)"` exits 0 and prints a version >= 6.0
  </acceptance_criteria>
  <done>
    PyYAML is a declared graph-io dependency and importable from the workspace
    venv. Wave 1 plan 31-03 can use `yaml.safe_load`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Amend domain_uri signature and update its single test</name>
  <read_first>
    - packages/graph-io/src/graph_io/uri.py (current domain_uri definition, lines 41-42)
    - packages/graph-io/tests/test_uri.py (current test_domain_uri_has_no_ctx at lines 64-66)
    - .planning/phases/31-domain-layer-derived-edges/31-CONTEXT.md (D-05 — required URI shape)
    - .planning/phases/31-domain-layer-derived-edges/31-RESEARCH.md (§3 — grep verification that no callers exist)
  </read_first>
  <files>packages/graph-io/src/graph_io/uri.py
packages/graph-io/tests/test_uri.py</files>
  <behavior>
    - `domain_uri` in `packages/graph-io/src/graph_io/uri.py` takes two positional arguments: `ctx: RepoContext` and `name: str`. It returns `f"domain:{ctx.org}/{ctx.repo}/{name}"`.
    - The amended function signature exactly matches the shape used by every other URI helper in the file (`pkg_uri`, `file_uri`, `entry_point_uri`, etc. — all take `ctx` as the first positional arg).
    - `packages/graph-io/tests/test_uri.py` REPLACES `test_domain_uri_has_no_ctx` with a new test named `test_domain_uri_with_ctx` that asserts `domain_uri(RepoContext('acme', 'repo'), 'billing') == 'domain:acme/repo/billing'`.
    - No other test in `test_uri.py` is modified. The import line `from graph_io.uri import (... domain_uri ...)` already includes `domain_uri` and remains unchanged.
    - `uv run --package graph-io pytest packages/graph-io/tests/test_uri.py -q` exits 0 with all tests green.
  </behavior>
  <action>
    Edit 1 — packages/graph-io/src/graph_io/uri.py:

    Locate the current definition:

      def domain_uri(name: str) -> str:
          return f"domain:{name}"

    Replace with:

      def domain_uri(ctx: RepoContext, name: str) -> str:
          return f"domain:{ctx.org}/{ctx.repo}/{name}"

    Edit 2 — packages/graph-io/tests/test_uri.py:

    Locate the current test:

      def test_domain_uri_has_no_ctx() -> None:
          # Domain identity is repo-agnostic in v1.6.
          assert domain_uri("billing") == "domain:billing"

    Replace with:

      def test_domain_uri_with_ctx() -> None:
          # Domain identity is repo-scoped per Phase 31 D-05.
          ctx = RepoContext(org="acme", repo="repo")
          assert domain_uri(ctx, "billing") == "domain:acme/repo/billing"

    Do NOT add new tests in this plan — exhaustive coverage of edge cases
    (escape chars, empty name, etc.) is not required at this step; the ctx-aware
    shape is exercised by Plan 31-03's `test_domains.py` tests through the
    `domains.emit` integration. This task only fixes the existing assertion.
  </action>
  <verify>
    <automated>cd /Users/pat/Personal/agent-research && uv run --package graph-io pytest packages/graph-io/tests/test_uri.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE '^def domain_uri\(ctx: RepoContext, name: str\) -> str:' packages/graph-io/src/graph_io/uri.py` returns exactly one matching line
    - `grep -F 'def domain_uri(name: str)' packages/graph-io/src/graph_io/uri.py` exits 1 (old signature gone)
    - `grep -F 'f"domain:{ctx.org}/{ctx.repo}/{name}"' packages/graph-io/src/graph_io/uri.py` exits 0
    - `grep -F 'def test_domain_uri_with_ctx' packages/graph-io/tests/test_uri.py` exits 0
    - `grep -F 'def test_domain_uri_has_no_ctx' packages/graph-io/tests/test_uri.py` exits 1 (old test removed)
    - `grep -F "assert domain_uri(ctx, 'billing') == 'domain:acme/repo/billing'" packages/graph-io/tests/test_uri.py` exits 0
    - `uv run --package graph-io pytest packages/graph-io/tests/test_uri.py -q` exits 0
    - `grep -rn 'domain_uri(' packages/graph-io/src/ | grep -v 'uri.py'` exits 1 (still zero callers outside uri.py — confirms signature break is safe)
  </acceptance_criteria>
  <done>
    `domain_uri` matches the (ctx, name) shape and the matching test exercises
    `domain:acme/repo/billing`. Wave 1 plan 31-03 (domains.emit) can use the
    helper without further URI scaffolding.
  </done>
</task>

</tasks>

<verification>
After all three tasks pass their acceptance gates, the full graph-io test
suite must remain green:

  cd /Users/pat/Personal/agent-research && uv run --package graph-io pytest packages/graph-io/tests/ -q

Expected: 0 failures, 0 errors. No new tests are added by this plan beyond
the single-line replacement in `test_uri.py`, so the failure count is
identical to the pre-plan baseline minus the removed `test_domain_uri_has_no_ctx`
plus the added `test_domain_uri_with_ctx` (net: 0 change in count).
</verification>

<success_criteria>
- [ ] ROADMAP.md SC#2 reads the D-15 wording (grep assertion passes)
- [ ] pyyaml>=6.0 is in `packages/graph-io/pyproject.toml` dependencies and `import yaml` succeeds inside the graph-io venv
- [ ] `domain_uri(ctx, name)` returns `domain:<org>/<repo>/<name>` and `test_uri.py` asserts the new shape
- [ ] `pytest packages/graph-io/tests/test_uri.py -q` exits 0
- [ ] `pytest packages/graph-io/tests/ -q` exits 0 (no Phase 29/30 regressions)
</success_criteria>
