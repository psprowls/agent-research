---
phase: 31-domain-layer-derived-edges
plan: 03
type: execute
wave: 1
depends_on: [31-01]
files_modified:
  - packages/graph-io/src/graph_io/domains.py
  - packages/graph-io/tests/test_domains.py
  - packages/graph-io/tests/fixtures/sample_monorepo/domains.yaml
autonomous: true
requirements:
  - DOMAIN-01
  - DOMAIN-02
  - DOMAIN-03
  - DOMAIN-04
  - DOMAIN-05
tags:
  - graph-io
  - domains
  - emitter
  - yaml-parsing
  - cycle-detection

must_haves:
  truths:
    - "A new module packages/graph-io/src/graph_io/domains.py exists with one public function emit(conn, *, repo_root, ctx, skip_dirs) modeled after structural_nodes.emit (D-01..D-06 + D-15)"
    - "emit reads <repo_root>/domains.yaml via yaml.safe_load; if the file is missing, emit returns immediately without writing any Domain nodes or edges (DOMAIN-04, D-03)"
    - "emit catches yaml.YAMLError; on parse failure it raises a graph-io DomainYamlError(exit_code=4) with message 'domains.yaml: YAML parse error: <details>' (D-06)"
    - "For each top-level key (domain name) in the parsed dict, emit produces one Domain node with kind='domain', name=<domain-name>, path=None, uri=domain_uri(ctx, name), attrs containing description and owner when present (D-01, D-05)"
    - "Unknown top-level keys per-domain (anything other than packages/parent/description/owner) are logged at WARNING level with the format 'domains.yaml: domain <name> has unknown key <key>' and ignored without failing the parse (D-01)"
    - "For each package name in <domain>.packages, emit produces one belongs_to_domain edge (Package -> Domain). The edge's src is the existing Package node (looked up by Package.name); the dst is the Domain node just emitted (DOMAIN-02, D-02)"
    - "Multi-domain membership is achieved naturally by listing the same package name under multiple top-level entries; each listing produces one edge — duplicates are deduplicated by the upsert primary key (src, dst, kind) (D-02)"
    - "Unknown package names (names in <domain>.packages that don't match any Package.name in the DB) are logged at WARNING level with format 'domains.yaml: package <name> (in domain <dom>) is not a known package. Known packages: <sorted comma-separated list>' and the offending entry is silently skipped (no edge emitted); the Domain node itself still emits (D-04, SC#4)"
    - "For each domain entry with a parent: <other-domain> field, emit queues a (child_domain, parent_domain) edge candidate for the cycle-detection pass (D-15)"
    - "Cycle detection: a module-private function _detect_cycles(parent_map: dict[str, str]) -> list[set[str]] runs Tarjan's SCC against the parent_map graph and returns each SCC of size > 1 as a set; AND it also identifies self-loops (domain whose parent is itself) and returns each self-loop as a singleton set in a SEPARATE returned list (or stamps a flag) (D-15)"
    - "For each SCC of size >= 1 that participates in a cycle (size > 1 or self-loop), emit prints exactly one warning of the format 'domains.yaml: cycle detected involving domains: <comma-separated sorted SCC members>. Skipping <N> domain_contains_domain edge(s); the acyclic remainder is preserved.' (D-15)"
    - "For every (child, parent) containment edge candidate, emit checks whether BOTH endpoints are in the SAME SCC of size > 1, OR child == parent (self-loop); if so the edge is SKIPPED. All other containment edges emit a domain_contains_domain edge (Domain[parent] -> Domain[child]) (D-15, DOMAIN-03)"
    - "Orphan parent (parent points to a domain name that doesn't exist in the parsed dict) is logged at WARNING level and the containment edge is skipped; the Domain node itself still emits"
    - "Validation: a domain entry where <domain>.packages is missing (KeyError) or not a list (TypeError) is logged at WARNING level and the domain is skipped entirely (no Domain node, no edges); a non-mapping top-level value (e.g. a string under a domain key) is similarly logged and skipped"
    - "All node and edge writes go through upsert.upsert_records (or upsert._upsert_node / _upsert_edge directly); domains.emit does NOT execute raw INSERTs against the nodes/edges tables"
    - "domains.emit is wrapped in `with conn:` (single transaction) — partial failures roll back; the function is callable from inside an outer transaction without nesting issues (sqlite3 silently no-ops nested with blocks on the same connection)"
    - "A new fixture file tests/fixtures/sample_monorepo/domains.yaml is added with a small valid configuration mapping mypkg to a 'core' domain and jspkg to a 'web' domain with 'presentation' as parent (web -> presentation containment edge)"
    - "Unit tests in tests/test_domains.py cover: (a) missing domains.yaml → zero Domain nodes; (b) valid yaml → Domain node + belongs_to_domain edges; (c) multi-domain membership (same package in two domains); (d) cycle of length 2 → warning + acyclic remainder preserved; (e) cycle of length 3 → only intra-SCC edges skipped; (f) self-loop → warning + edge skipped + node emitted; (g) orphan parent → warning + edge skipped; (h) unknown package → warning with full known-package list; (i) unknown top-level key → warning + key ignored + Domain emitted; (j) missing required 'packages:' field → warning + domain skipped; (k) non-list 'packages:' → warning + domain skipped; (l) yaml parse error → DomainYamlError raised with exit code 4; (m) test_no_convention_inference_from_test_dir (SC#5 — repo with tests/billing/ but no domains.yaml → no Domain(billing) node)"
  artifacts:
    - path: "packages/graph-io/src/graph_io/domains.py"
      provides: "emit(conn, *, repo_root, ctx, skip_dirs) — Domain nodes + belongs_to_domain + domain_contains_domain edges from domains.yaml"
      contains: "def emit"
      exports:
        - emit
        - DomainYamlError
    - path: "packages/graph-io/tests/test_domains.py"
      provides: "Per-shape unit tests for domains.emit"
      contains: "test_emit_domain_nodes"
    - path: "packages/graph-io/tests/fixtures/sample_monorepo/domains.yaml"
      provides: "Valid domains.yaml fixture for end-to-end testing"
      contains: "core:"
  key_links:
    - from: "domains.emit"
      to: "Domain nodes"
      via: "yaml.safe_load + per-domain iteration + upsert._upsert_node"
      pattern: 'kind="domain"'
    - from: "domains.emit"
      to: "belongs_to_domain edges"
      via: "per-package iteration with Package.name lookup against the DB"
      pattern: 'kind="belongs_to_domain"'
    - from: "domains.emit"
      to: "_detect_cycles (Tarjan SCC)"
      via: "module-private helper called once per emit() pass"
      pattern: "_detect_cycles(parent_map)"
---

<objective>
Create `packages/graph-io/src/graph_io/domains.py` implementing the
`domains.emit` emitter per CONTEXT.md D-01..D-06 and D-15. The emitter
reads `<repo_root>/domains.yaml`, produces `Domain` nodes, `belongs_to_domain`
edges (Package → Domain), and `domain_contains_domain` edges (Domain →
Domain) — applying Tarjan-based cycle detection (D-15) that skips ONLY the
cycle-participating containment edges and preserves the acyclic remainder.

Also add a valid `tests/fixtures/sample_monorepo/domains.yaml` so the
fixture-driven tests in Plan 31-04 can exercise the full update.run pipeline
with a real Domain layer, and ship the comprehensive unit-test suite per the
must_haves list.

Out of scope: `derived_edges.compute` (Plan 31-04), `update.run` wiring
(Plan 31-04), shared `import_scan.py` (Plan 31-02). This plan only emits
Domain layer nodes and edges; it does not compute references or depends_on.

Output:
1. `domains.py` with `emit` + `DomainYamlError` + `_detect_cycles` private helper.
2. `tests/fixtures/sample_monorepo/domains.yaml` (valid case).
3. `tests/test_domains.py` covering 13 cases.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-domain-layer-derived-edges/31-CONTEXT.md
@.planning/phases/31-domain-layer-derived-edges/31-RESEARCH.md
@.planning/phases/29-structural-nodes-containment-tree/29-CONTEXT.md
@packages/graph-io/src/graph_io/structural_nodes.py
@packages/graph-io/src/graph_io/upsert.py
@packages/graph-io/src/graph_io/uri.py
@packages/graph-io/src/graph_io/packages.py
@packages/graph-io/src/graph_io/exit_codes.py

<interfaces>
<!-- Existing surfaces consumed by domains.py. -->

From packages/graph-io/src/graph_io/upsert.py:
  upsert_records(conn, GraphRecords)
  _upsert_node(conn, GraphNode) -> int
  _upsert_edge(conn, GraphEdge) -> None

From packages/graph-io/src/graph_io/uri.py (after Plan 31-01):
  domain_uri(ctx: RepoContext, name: str) -> str  # 'domain:<org>/<repo>/<name>'
  RepoContext

From packages/graph-io/src/graph_io/exit_codes.py:
  # File defines exit-code constants; add DOMAIN_YAML_PARSE_ERROR = 4 if not present
  # (Phase 28 may have already used 4 for SchemaMismatchError; D-06 deliberately reuses it)

From source_parser.projections.graph:
  GraphNode(kind, name, path, line, attrs) -> dataclass
  GraphEdge(src, dst, kind, attrs) -> dataclass
  GraphRecords(nodes, edges)

Domain.uri shape (Phase 31 D-05, locked by Plan 31-01):
  domain:<org>/<repo>/<domain_name>

Edge kinds (new in Phase 31):
  belongs_to_domain         # Package -> Domain
  domain_contains_domain    # Domain (parent) -> Domain (child)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold domains.py with parsing, Domain-node emission, and belongs_to_domain edges</name>
  <read_first>
    - .planning/phases/31-domain-layer-derived-edges/31-CONTEXT.md (D-01..D-06 — schema, parsing, package validation)
    - .planning/phases/31-domain-layer-derived-edges/31-RESEARCH.md (§5 — yaml parsing strategy and per-domain validation)
    - packages/graph-io/src/graph_io/structural_nodes.py (analog emit() shape — module structure, with conn: pattern, GraphNode/GraphEdge usage)
    - packages/graph-io/src/graph_io/uri.py (domain_uri after Plan 31-01)
    - packages/graph-io/src/graph_io/upsert.py (upsert primitives)
  </read_first>
  <files>packages/graph-io/src/graph_io/domains.py</files>
  <behavior>
    - File packages/graph-io/src/graph_io/domains.py exists with module docstring `"""Domain emitter: domains.yaml -> Domain nodes + belongs_to_domain + domain_contains_domain edges (Phase 31)."""`
    - Module imports: yaml (PyYAML), json, logging, pathlib.Path, sqlite3; source_parser.projections.graph for GraphNode/GraphEdge/GraphRecords; graph_io.upsert, graph_io.uri.domain_uri, graph_io.uri.RepoContext
    - Module declares `_KNOWN_KEYS = frozenset({"packages", "parent", "description", "owner"})` and `_DOMAIN_KIND = "domain"` and `_BELONGS_TO_DOMAIN_KIND = "belongs_to_domain"` and `_DOMAIN_CONTAINS_DOMAIN_KIND = "domain_contains_domain"`
    - Public class `DomainYamlError(Exception)` is defined with an `exit_code: int = 4` class attribute (D-06)
    - Module-private `_load_domains_yaml(repo_root)` function returns the parsed dict or None when missing; raises DomainYamlError on yaml.YAMLError or non-mapping top-level
    - Module-private `_log_or_warn(msg)` helper uses `logging.getLogger("graph_io.domains").warning(msg)` consistently
    - Public `emit(conn, *, repo_root, ctx, skip_dirs)` is defined (Task 2 fills in the body; this task ships the scaffold + the Domain-node and belongs_to_domain edge emission paths)
    - At minimum after this task: emit reads the yaml, queries Package.name set, iterates domains, emits Domain nodes, emits belongs_to_domain edges, validates packages with warnings, ignores unknown keys. Cycle detection and domain_contains_domain edges are TASK 2.
    - Module imports cleanly: `uv run --package graph-io python -c "from graph_io import domains; print('ok')"` exits 0
  </behavior>
  <action>
    Create packages/graph-io/src/graph_io/domains.py:

      """Domain emitter.

      Reads `<repo_root>/domains.yaml` and emits:
        - Domain nodes (one per top-level key)
        - belongs_to_domain edges (Package -> Domain)
        - domain_contains_domain edges (Domain -> Domain, tree)

      Cycle detection (Phase 31 D-15) skips ONLY cycle-participating
      containment edges; the acyclic remainder is preserved.
      """

      from __future__ import annotations

      import json
      import logging
      import sqlite3
      from pathlib import Path

      import yaml
      from source_parser.projections.graph import GraphEdge, GraphNode, GraphRecords

      from graph_io import upsert
      from graph_io.uri import RepoContext, domain_uri

      _LOG = logging.getLogger("graph_io.domains")

      _KNOWN_KEYS = frozenset({"packages", "parent", "description", "owner"})
      _DOMAIN_KIND = "domain"
      _BELONGS_TO_DOMAIN_KIND = "belongs_to_domain"
      _DOMAIN_CONTAINS_DOMAIN_KIND = "domain_contains_domain"


      class DomainYamlError(Exception):
          """Raised on unrecoverable domains.yaml parse error.

          The CLI surface maps this exception to exit code 4 (D-06,
          consistent with the Phase 28 schema-mismatch exit-code reuse).
          """
          exit_code: int = 4


      def _load_domains_yaml(repo_root: Path) -> dict | None:
          """Return parsed domains.yaml as a dict, or None if the file is missing.

          Raises DomainYamlError on parse failure or non-mapping top-level.
          """
          path = repo_root / "domains.yaml"
          if not path.exists():
              return None
          try:
              with path.open("r", encoding="utf-8") as f:
                  data = yaml.safe_load(f) or {}
          except yaml.YAMLError as exc:
              raise DomainYamlError(
                  f"domains.yaml: YAML parse error: {exc}"
              ) from exc
          if not isinstance(data, dict):
              raise DomainYamlError(
                  f"domains.yaml: top-level must be a mapping, got "
                  f"{type(data).__name__}"
              )
          return data


      def _known_packages(conn: sqlite3.Connection) -> list[tuple[str, str | None]]:
          """Return sorted [(pkg_name, pkg_rel)] from the Package nodes."""
          rows = conn.execute(
              "SELECT name, path FROM nodes WHERE kind='package' ORDER BY name"
          ).fetchall()
          return [(name, path) for name, path in rows]


      def emit(
          conn: sqlite3.Connection,
          *,
          repo_root: Path,
          ctx: RepoContext,
          skip_dirs: frozenset[str],
      ) -> None:
          """Emit Domain nodes + belongs_to_domain + domain_contains_domain edges.

          Idempotent: re-running over the same domains.yaml produces the same
          edge set (upsert dedupes on (src, dst, kind)).

          Missing domains.yaml is NOT an error (DOMAIN-04 / D-03).
          """
          data = _load_domains_yaml(repo_root)
          if data is None:
              return

          known_pkgs = _known_packages(conn)
          known_names = {name for name, _rel in known_pkgs}
          known_name_to_rel = {name: rel for name, rel in known_pkgs}
          known_sorted_csv = ", ".join(sorted(known_names))

          nodes_out: list[GraphNode] = []
          edges_out: list[GraphEdge] = []
          parent_map: dict[str, str] = {}    # child -> parent
          all_domain_names: set[str] = set()

          for dom_name, dom_attrs in data.items():
              if not isinstance(dom_attrs, dict):
                  _LOG.warning(
                      "domains.yaml: domain '%s' must be a mapping, got %s — skipping",
                      dom_name, type(dom_attrs).__name__,
                  )
                  continue
              pkgs = dom_attrs.get("packages")
              if pkgs is None:
                  _LOG.warning(
                      "domains.yaml: domain '%s' missing required 'packages:' field — skipping",
                      dom_name,
                  )
                  continue
              if not isinstance(pkgs, list):
                  _LOG.warning(
                      "domains.yaml: domain '%s' has non-list 'packages:' field — skipping",
                      dom_name,
                  )
                  continue

              for key in sorted(set(dom_attrs.keys()) - _KNOWN_KEYS):
                  _LOG.warning(
                      "domains.yaml: domain '%s' has unknown key '%s' — ignored",
                      dom_name, key,
                  )

              all_domain_names.add(dom_name)

              # Domain node
              dom_attrs_for_node: dict = {"uri": domain_uri(ctx, dom_name)}
              if "description" in dom_attrs:
                  dom_attrs_for_node["description"] = dom_attrs["description"]
              if "owner" in dom_attrs:
                  dom_attrs_for_node["owner"] = dom_attrs["owner"]
              nodes_out.append(GraphNode(
                  kind=_DOMAIN_KIND,
                  name=dom_name,
                  path=None,
                  line=None,
                  attrs=dom_attrs_for_node,
              ))

              # belongs_to_domain edges
              for pkg_name in pkgs:
                  if not isinstance(pkg_name, str):
                      _LOG.warning(
                          "domains.yaml: domain '%s' has non-string entry in packages: %r — skipping",
                          dom_name, pkg_name,
                      )
                      continue
                  if pkg_name not in known_names:
                      _LOG.warning(
                          "domains.yaml: package '%s' (in domain '%s') is not a known package. Known packages: %s",
                          pkg_name, dom_name, known_sorted_csv,
                      )
                      continue
                  pkg_rel = known_name_to_rel.get(pkg_name)
                  edges_out.append(GraphEdge(
                      src=("package", pkg_name, pkg_rel),
                      dst=(_DOMAIN_KIND, dom_name, None),
                      kind=_BELONGS_TO_DOMAIN_KIND,
                      attrs={},
                  ))

              # Queue parent for cycle-detection pass
              parent = dom_attrs.get("parent")
              if parent is not None:
                  if not isinstance(parent, str):
                      _LOG.warning(
                          "domains.yaml: domain '%s' has non-string parent: %r — skipping containment edge",
                          dom_name, parent,
                      )
                  else:
                      parent_map[dom_name] = parent

          # Containment edges (Task 2 fills this in — cycle detection + skip)
          _emit_containment_edges(parent_map, all_domain_names, edges_out)

          # Single-transaction write
          with conn:
              upsert.upsert_records(
                  conn, GraphRecords(nodes=nodes_out, edges=edges_out),
              )


      def _emit_containment_edges(
          parent_map: dict[str, str],
          all_domain_names: set[str],
          edges_out: list[GraphEdge],
      ) -> None:
          """STUB — Task 2 implements Tarjan SCC + cycle-aware edge skipping."""
          # Placeholder: emit every containment edge without checking cycles.
          # Task 2 replaces this body with the full D-15 implementation.
          for child, parent in parent_map.items():
              if parent not in all_domain_names:
                  _LOG.warning(
                      "domains.yaml: domain '%s' has parent '%s' which is not a declared domain — skipping containment edge",
                      child, parent,
                  )
                  continue
              edges_out.append(GraphEdge(
                  src=(_DOMAIN_KIND, parent, None),
                  dst=(_DOMAIN_KIND, child, None),
                  kind=_DOMAIN_CONTAINS_DOMAIN_KIND,
                  attrs={},
              ))

    Confirm module imports cleanly.
  </action>
  <verify>
    <automated>cd /Users/pat/Personal/agent-research && uv run --package graph-io python -c "from graph_io import domains; assert callable(domains.emit); assert issubclass(domains.DomainYamlError, Exception); assert domains.DomainYamlError.exit_code == 4; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/graph-io/src/graph_io/domains.py` exists
    - `grep -n '^def emit' packages/graph-io/src/graph_io/domains.py` returns 1 line
    - `grep -n '^class DomainYamlError' packages/graph-io/src/graph_io/domains.py` returns 1 line
    - `grep -F 'exit_code: int = 4' packages/graph-io/src/graph_io/domains.py` exits 0
    - `grep -F 'yaml.safe_load' packages/graph-io/src/graph_io/domains.py` exits 0 (no yaml.load)
    - `grep -F 'yaml.load(' packages/graph-io/src/graph_io/domains.py` exits 1 (security: never use yaml.load)
    - `grep -F '_KNOWN_KEYS = frozenset' packages/graph-io/src/graph_io/domains.py` exits 0
    - `grep -F 'belongs_to_domain' packages/graph-io/src/graph_io/domains.py` exits 0
    - `grep -F 'domain_contains_domain' packages/graph-io/src/graph_io/domains.py` exits 0
    - Module imports cleanly: the verify command exits 0
  </acceptance_criteria>
  <done>
    Skeleton in place. Domain nodes and belongs_to_domain edges emit. Cycle
    detection is still STUB — Task 2 lands the Tarjan implementation.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement _detect_cycles (Tarjan SCC) and replace the containment-edge stub</name>
  <read_first>
    - packages/graph-io/src/graph_io/domains.py (current state after Task 1, especially _emit_containment_edges stub)
    - .planning/phases/31-domain-layer-derived-edges/31-CONTEXT.md (D-15 — cycle detection contract; specifics §15 SCC algorithm)
    - .planning/phases/31-domain-layer-derived-edges/31-RESEARCH.md (§4 — reference Tarjan implementation skeleton)
  </read_first>
  <files>packages/graph-io/src/graph_io/domains.py</files>
  <behavior>
    - A new module-private function _detect_cycles(parent_map: dict[str, str]) -> tuple[list[set[str]], set[str]] returns (sccs_size_gt_1, self_loops). sccs_size_gt_1 is a list of SCCs each containing > 1 node; self_loops is the set of domain names where parent_map[name] == name
    - The function builds the directed graph child -> parent from parent_map, runs Tarjan's SCC (recursive, single DFS pass), and returns SCCs of size > 1
    - Self-loops (domain whose parent is itself) are detected separately via direct comparison and returned in the self_loops set
    - _emit_containment_edges is rewritten to:
        1. Call _detect_cycles(parent_map)
        2. For each SCC of size > 1: emit one warning of the form 'domains.yaml: cycle detected involving domains: <sorted-csv>. Skipping <N> domain_contains_domain edge(s); the acyclic remainder is preserved.' where N is the count of intra-SCC edges
        3. For each self-loop: emit one warning 'domains.yaml: domain <name> declares itself as parent — skipping containment edge.'
        4. For each (child, parent) in parent_map: skip the edge if BOTH endpoints are in the SAME SCC of size > 1, OR child == parent (self-loop); otherwise emit the domain_contains_domain edge (parent -> child)
        5. Orphan parents (parent not in all_domain_names) continue to produce the existing 'parent not a declared domain' warning + edge skip
    - For the warning's intra-SCC edge count: count the number of (child, parent) pairs in parent_map where both child and parent are in the same SCC; print this as N in the warning
    - Idempotency: running emit twice produces the same warnings and the same emitted edges (upsert dedupes on (src, dst, kind))
    - Output is deterministic — SCCs printed in deterministic order: sort by min member name, members within each SCC printed in sorted order
  </behavior>
  <action>
    In packages/graph-io/src/graph_io/domains.py, replace the STUB body of
    _emit_containment_edges and add _detect_cycles above it:

      def _detect_cycles(
          parent_map: dict[str, str],
      ) -> tuple[list[set[str]], set[str]]:
          """Return (sccs_size_gt_1, self_loops) for the (child -> parent) graph.

          self_loops is the set of domain names where parent_map[name] == name.
          sccs_size_gt_1 is a list of SCCs each containing > 1 distinct nodes.
          Singleton SCCs (the acyclic case) are NOT returned.
          """
          self_loops: set[str] = {
              n for n, p in parent_map.items() if n == p
          }
          # Build adjacency: child -> [parent] (excluding self-loops to keep
          # Tarjan focused on multi-node cycles; self-loops handled separately)
          nodes: set[str] = set()
          adj: dict[str, list[str]] = {}
          for child, parent in parent_map.items():
              if child == parent:
                  continue
              nodes.add(child)
              nodes.add(parent)
              adj.setdefault(child, []).append(parent)
              adj.setdefault(parent, [])

          index: dict[str, int] = {}
          lowlink: dict[str, int] = {}
          on_stack: set[str] = set()
          stack: list[str] = []
          counter = [0]
          sccs: list[set[str]] = []

          def strongconnect(v: str) -> None:
              index[v] = counter[0]
              lowlink[v] = counter[0]
              counter[0] += 1
              stack.append(v)
              on_stack.add(v)
              for w in adj.get(v, []):
                  if w not in index:
                      strongconnect(w)
                      lowlink[v] = min(lowlink[v], lowlink[w])
                  elif w in on_stack:
                      lowlink[v] = min(lowlink[v], index[w])
              if lowlink[v] == index[v]:
                  component: set[str] = set()
                  while True:
                      w = stack.pop()
                      on_stack.remove(w)
                      component.add(w)
                      if w == v:
                          break
                  if len(component) > 1:
                      sccs.append(component)

          for n in sorted(nodes):   # deterministic iteration
              if n not in index:
                  strongconnect(n)

          # Sort SCCs by min member name for deterministic output
          sccs.sort(key=lambda s: min(s))
          return sccs, self_loops


      def _emit_containment_edges(
          parent_map: dict[str, str],
          all_domain_names: set[str],
          edges_out: list[GraphEdge],
      ) -> None:
          """Emit domain_contains_domain edges, skipping cycle-participating
          edges and self-loops (D-15)."""
          sccs, self_loops = _detect_cycles(parent_map)

          # Build SCC membership map for O(1) "are both endpoints in same SCC" check
          scc_of: dict[str, int] = {}
          for i, scc in enumerate(sccs):
              for member in scc:
                  scc_of[member] = i

          # Emit one warning per SCC of size > 1
          for scc in sccs:
              intra_count = sum(
                  1 for child, parent in parent_map.items()
                  if child in scc and parent in scc and child != parent
              )
              members_csv = ", ".join(sorted(scc))
              _LOG.warning(
                  "domains.yaml: cycle detected involving domains: %s. "
                  "Skipping %d domain_contains_domain edge(s); the acyclic remainder is preserved.",
                  members_csv, intra_count,
              )

          # Emit one warning per self-loop
          for name in sorted(self_loops):
              _LOG.warning(
                  "domains.yaml: domain '%s' declares itself as parent — skipping containment edge.",
                  name,
              )

          # Emit containment edges, skipping cycle-participating ones
          for child, parent in parent_map.items():
              if child == parent:
                  continue   # self-loop: warned above, edge skipped
              if child in scc_of and parent in scc_of and scc_of[child] == scc_of[parent]:
                  continue   # intra-SCC edge: warned above, edge skipped
              if parent not in all_domain_names:
                  _LOG.warning(
                      "domains.yaml: domain '%s' has parent '%s' which is not a declared domain — skipping containment edge",
                      child, parent,
                  )
                  continue
              edges_out.append(GraphEdge(
                  src=(_DOMAIN_KIND, parent, None),
                  dst=(_DOMAIN_KIND, child, None),
                  kind=_DOMAIN_CONTAINS_DOMAIN_KIND,
                  attrs={},
              ))

    Delete the placeholder loop in the previous _emit_containment_edges body.
    Keep the function signature identical (parent_map, all_domain_names,
    edges_out) so emit's caller doesn't change.
  </action>
  <verify>
    <automated>cd /Users/pat/Personal/agent-research && uv run --package graph-io python -c "from graph_io.domains import _detect_cycles; sccs, sl = _detect_cycles({'a': 'b', 'b': 'a'}); assert sccs == [{'a', 'b'}], sccs; assert sl == set(), sl; sccs, sl = _detect_cycles({'x': 'x'}); assert sccs == [], sccs; assert sl == {'x'}, sl; sccs, sl = _detect_cycles({'a': 'b', 'b': 'c', 'c': 'a'}); assert sccs == [{'a', 'b', 'c'}], sccs; sccs, sl = _detect_cycles({'a': 'b', 'b': 'c'}); assert sccs == [], sccs; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '^def _detect_cycles' packages/graph-io/src/graph_io/domains.py` returns 1 line
    - `grep -F 'def strongconnect' packages/graph-io/src/graph_io/domains.py` exits 0 (Tarjan inner func)
    - `grep -F 'lowlink[v]' packages/graph-io/src/graph_io/domains.py` exits 0 (Tarjan algorithm marker)
    - `grep -F 'self_loops' packages/graph-io/src/graph_io/domains.py` exits 0
    - `grep -F 'cycle detected involving domains:' packages/graph-io/src/graph_io/domains.py` exits 0 (warning text)
    - `grep -F 'declares itself as parent' packages/graph-io/src/graph_io/domains.py` exits 0 (self-loop warning)
    - The verify command's inline assertions all pass — Tarjan correctly identifies length-2, length-3 cycles, self-loops, and the acyclic case
  </acceptance_criteria>
  <done>
    Cycle detection lives in domains.py; cycle-participating containment
    edges are skipped per D-15; the acyclic remainder is preserved. The
    function is deterministic and idempotent.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add tests/fixtures/sample_monorepo/domains.yaml (valid case)</name>
  <read_first>
    - packages/graph-io/tests/fixtures/sample_monorepo/ (contents — should exist after Phase 29 + Phase 30 fixtures; if not present yet because Phase 30 hasn't executed, the fixture root is created by Phase 29 plan 29-04)
    - .planning/phases/31-domain-layer-derived-edges/31-CONTEXT.md (specifics — fixture extension)
    - .planning/phases/31-domain-layer-derived-edges/31-RESEARCH.md (§7 — fixture strategy)
  </read_first>
  <files>packages/graph-io/tests/fixtures/sample_monorepo/domains.yaml</files>
  <behavior>
    - File `packages/graph-io/tests/fixtures/sample_monorepo/domains.yaml` exists with valid YAML mapping mypkg to a 'core' domain and jspkg to a 'web' domain, with 'web' having 'presentation' as parent and 'presentation' being a top-level domain with no packages
    - The yaml is parseable by yaml.safe_load and produces the expected dict structure
    - No cycle in the fixture — cycle cases are constructed dynamically in tests via tmp_path overrides (per RESEARCH §7)
  </behavior>
  <action>
    Create packages/graph-io/tests/fixtures/sample_monorepo/domains.yaml with:

      core:
        packages: [mypkg]
        description: "Core utilities domain"
      web:
        packages: [jspkg]
        parent: presentation
      presentation:
        packages: []
        description: "Top-level UI layer"

    No other fixture files are touched.
  </action>
  <verify>
    <automated>cd /Users/pat/Personal/agent-research && uv run --package graph-io python -c "import yaml, pathlib; data = yaml.safe_load(pathlib.Path('packages/graph-io/tests/fixtures/sample_monorepo/domains.yaml').read_text()); assert 'core' in data and 'web' in data and 'presentation' in data; assert data['core']['packages'] == ['mypkg']; assert data['web']['parent'] == 'presentation'; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/graph-io/tests/fixtures/sample_monorepo/domains.yaml` exists
    - `wc -l packages/graph-io/tests/fixtures/sample_monorepo/domains.yaml | awk '{print $1}'` returns >= 7
    - The verify command's parse + assertions all pass
  </acceptance_criteria>
  <done>
    The valid-case fixture is in place for Plan 31-04's end-to-end test.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Add comprehensive unit tests in tests/test_domains.py</name>
  <read_first>
    - packages/graph-io/src/graph_io/domains.py (full file after Tasks 1-2)
    - packages/graph-io/tests/test_structural_nodes.py (analog test patterns — tmp_path + packages.refresh + assertion shape)
    - packages/graph-io/tests/conftest.py (shared fixtures)
    - .planning/phases/31-domain-layer-derived-edges/31-CONTEXT.md (D-04 warning format prototype; D-15 cycle warning prototype)
  </read_first>
  <files>packages/graph-io/tests/test_domains.py</files>
  <behavior>
    - File packages/graph-io/tests/test_domains.py exists with at least 13 distinct test functions per the must_haves truth (m)
    - Each test follows the pattern: tmp_path → write package skeletons + domains.yaml → store.connect + schema → packages.refresh → domains.emit → SQL assertions on nodes and edges tables (or pytest.raises for DomainYamlError)
    - Where warnings are asserted: use the `caplog` pytest fixture with `caplog.set_level(logging.WARNING, logger="graph_io.domains")` to capture log records; assert on `caplog.text` containing the expected substring
    - Tests do NOT depend on the on-disk sample_monorepo fixture (they build their own minimal packages inline in tmp_path) — keeps tests focused and avoids coupling to Phase 29/30 fixture changes
    - test_no_convention_inference_from_test_dir (test (m)): build a tmp_path with packages/foo/ AND tests/billing/ subdirs but NO domains.yaml; call emit; assert SELECT COUNT(*) FROM nodes WHERE kind='domain' AND name='billing' returns 0 (SC#5)
  </behavior>
  <action>
    Create packages/graph-io/tests/test_domains.py. Skeleton:

      """Unit tests for graph_io.domains.emit (Phase 31 DOMAIN-01..05)."""

      from __future__ import annotations

      import json
      import logging
      import sqlite3
      from pathlib import Path

      import pytest

      from graph_io import domains, packages, schema, store
      from graph_io.domains import DomainYamlError
      from graph_io.uri import RepoContext

      CTX = RepoContext(org="testorg", repo="testrepo")


      def _setup(tmp_path: Path) -> sqlite3.Connection:
          conn = store.connect(tmp_path / "code.db", create=True)
          return conn


      def _write_pkg(root: Path, name: str, lang: str = "python") -> None:
          pkg_dir = root / "packages" / name
          if lang == "python":
              src_dir = pkg_dir / "src" / name.replace("-", "_")
              src_dir.mkdir(parents=True, exist_ok=True)
              (src_dir / "__init__.py").write_text("")
              (pkg_dir / "pyproject.toml").write_text(
                  f'[project]\nname = "{name}"\nversion = "0.0.0"\n'
                  f'requires-python = ">=3.11"\n'
                  f'[build-system]\nrequires = ["uv_build"]\nbuild-backend = "uv_build"\n'
              )
          else:
              pkg_dir.mkdir(parents=True, exist_ok=True)
              (pkg_dir / "package.json").write_text(
                  '{"name": "' + name + '", "version": "0.0.0"}'
              )


      def _refresh_packages(conn, repo_root: Path) -> None:
          with store.transaction(conn):
              packages.refresh(conn, repo_root=repo_root, ctx=CTX)


      def _count_domains(conn) -> int:
          return conn.execute(
              "SELECT COUNT(*) FROM nodes WHERE kind='domain'"
          ).fetchone()[0]


      def _count_edges(conn, kind: str) -> int:
          return conn.execute(
              "SELECT COUNT(*) FROM edges WHERE kind=?", (kind,),
          ).fetchone()[0]


      # === Tests ===

      def test_missing_yaml_zero_domain(tmp_path: Path) -> None:
          _write_pkg(tmp_path, "mypkg")
          conn = _setup(tmp_path)
          _refresh_packages(conn, tmp_path)
          domains.emit(conn, repo_root=tmp_path, ctx=CTX, skip_dirs=frozenset())
          assert _count_domains(conn) == 0


      def test_emit_domain_nodes(tmp_path: Path) -> None:
          _write_pkg(tmp_path, "mypkg")
          (tmp_path / "domains.yaml").write_text(
              "core:\n  packages: [mypkg]\n  description: 'Core domain'\n"
          )
          conn = _setup(tmp_path)
          _refresh_packages(conn, tmp_path)
          domains.emit(conn, repo_root=tmp_path, ctx=CTX, skip_dirs=frozenset())
          assert _count_domains(conn) == 1
          assert _count_edges(conn, "belongs_to_domain") == 1
          row = conn.execute(
              "SELECT uri, attrs_json FROM nodes WHERE kind='domain' AND name='core'"
          ).fetchone()
          assert row[0] == "domain:testorg/testrepo/core"
          attrs = json.loads(row[1])
          assert attrs.get("description") == "Core domain"


      def test_multi_domain_membership(tmp_path: Path) -> None:
          _write_pkg(tmp_path, "mypkg")
          (tmp_path / "domains.yaml").write_text(
              "a:\n  packages: [mypkg]\n"
              "b:\n  packages: [mypkg]\n"
          )
          conn = _setup(tmp_path)
          _refresh_packages(conn, tmp_path)
          domains.emit(conn, repo_root=tmp_path, ctx=CTX, skip_dirs=frozenset())
          assert _count_edges(conn, "belongs_to_domain") == 2


      def test_cycle_skip_only_intra_scc(tmp_path: Path, caplog) -> None:
          _write_pkg(tmp_path, "mypkg")
          # Two-domain cycle + a third domain outside the cycle
          (tmp_path / "domains.yaml").write_text(
              "payments:\n  packages: []\n  parent: billing\n"
              "billing:\n  packages: [mypkg]\n  parent: payments\n"
              "outside:\n  packages: []\n  parent: payments\n"
          )
          conn = _setup(tmp_path)
          _refresh_packages(conn, tmp_path)
          with caplog.at_level(logging.WARNING, logger="graph_io.domains"):
              domains.emit(conn, repo_root=tmp_path, ctx=CTX, skip_dirs=frozenset())
          assert "cycle detected involving domains: billing, payments" in caplog.text
          # outside->payments is NOT in the SCC; it should still be emitted
          edges = conn.execute(
              "SELECT n1.name AS parent, n2.name AS child FROM edges e "
              "JOIN nodes n1 ON e.src=n1.id JOIN nodes n2 ON e.dst=n2.id "
              "WHERE e.kind='domain_contains_domain'"
          ).fetchall()
          edge_pairs = {(p, c) for p, c in edges}
          assert ("payments", "outside") in edge_pairs
          # Intra-cycle edges are skipped
          assert ("payments", "billing") not in edge_pairs
          assert ("billing", "payments") not in edge_pairs


      def test_cycle_length_3_intra_scc_only_skipped(tmp_path: Path, caplog) -> None:
          _write_pkg(tmp_path, "mypkg")
          (tmp_path / "domains.yaml").write_text(
              "a:\n  packages: [mypkg]\n  parent: b\n"
              "b:\n  packages: []\n  parent: c\n"
              "c:\n  packages: []\n  parent: a\n"
          )
          conn = _setup(tmp_path)
          _refresh_packages(conn, tmp_path)
          with caplog.at_level(logging.WARNING, logger="graph_io.domains"):
              domains.emit(conn, repo_root=tmp_path, ctx=CTX, skip_dirs=frozenset())
          assert "cycle detected involving domains: a, b, c" in caplog.text
          assert _count_edges(conn, "domain_contains_domain") == 0
          assert _count_domains(conn) == 3  # all 3 nodes still emit


      def test_self_loop_skip(tmp_path: Path, caplog) -> None:
          _write_pkg(tmp_path, "mypkg")
          (tmp_path / "domains.yaml").write_text(
              "a:\n  packages: [mypkg]\n  parent: a\n"
          )
          conn = _setup(tmp_path)
          _refresh_packages(conn, tmp_path)
          with caplog.at_level(logging.WARNING, logger="graph_io.domains"):
              domains.emit(conn, repo_root=tmp_path, ctx=CTX, skip_dirs=frozenset())
          assert "declares itself as parent" in caplog.text
          assert _count_edges(conn, "domain_contains_domain") == 0
          assert _count_domains(conn) == 1


      def test_orphan_parent_skip(tmp_path: Path, caplog) -> None:
          _write_pkg(tmp_path, "mypkg")
          (tmp_path / "domains.yaml").write_text(
              "a:\n  packages: [mypkg]\n  parent: nonexistent\n"
          )
          conn = _setup(tmp_path)
          _refresh_packages(conn, tmp_path)
          with caplog.at_level(logging.WARNING, logger="graph_io.domains"):
              domains.emit(conn, repo_root=tmp_path, ctx=CTX, skip_dirs=frozenset())
          assert "is not a declared domain" in caplog.text
          assert _count_edges(conn, "domain_contains_domain") == 0
          assert _count_domains(conn) == 1


      def test_unknown_package_warns_with_known_list(tmp_path: Path, caplog) -> None:
          _write_pkg(tmp_path, "mypkg")
          _write_pkg(tmp_path, "otherpkg")
          (tmp_path / "domains.yaml").write_text(
              "a:\n  packages: [bogus]\n"
          )
          conn = _setup(tmp_path)
          _refresh_packages(conn, tmp_path)
          with caplog.at_level(logging.WARNING, logger="graph_io.domains"):
              domains.emit(conn, repo_root=tmp_path, ctx=CTX, skip_dirs=frozenset())
          assert "package 'bogus' (in domain 'a')" in caplog.text
          # Full sorted known-package list must appear (SC#4)
          assert "mypkg" in caplog.text
          assert "otherpkg" in caplog.text
          # Domain node still emits, but no belongs_to_domain edge
          assert _count_domains(conn) == 1
          assert _count_edges(conn, "belongs_to_domain") == 0


      def test_unknown_top_level_key_logged_and_ignored(tmp_path: Path, caplog) -> None:
          _write_pkg(tmp_path, "mypkg")
          (tmp_path / "domains.yaml").write_text(
              "a:\n  packages: [mypkg]\n  weird_extra: yes\n"
          )
          conn = _setup(tmp_path)
          _refresh_packages(conn, tmp_path)
          with caplog.at_level(logging.WARNING, logger="graph_io.domains"):
              domains.emit(conn, repo_root=tmp_path, ctx=CTX, skip_dirs=frozenset())
          assert "has unknown key 'weird_extra'" in caplog.text
          assert _count_domains(conn) == 1  # Domain still emits


      def test_missing_packages_field_skips_domain(tmp_path: Path, caplog) -> None:
          _write_pkg(tmp_path, "mypkg")
          (tmp_path / "domains.yaml").write_text(
              "a:\n  description: 'no packages'\n"
          )
          conn = _setup(tmp_path)
          _refresh_packages(conn, tmp_path)
          with caplog.at_level(logging.WARNING, logger="graph_io.domains"):
              domains.emit(conn, repo_root=tmp_path, ctx=CTX, skip_dirs=frozenset())
          assert "missing required 'packages:' field" in caplog.text
          assert _count_domains(conn) == 0


      def test_non_list_packages_skips_domain(tmp_path: Path, caplog) -> None:
          _write_pkg(tmp_path, "mypkg")
          (tmp_path / "domains.yaml").write_text(
              "a:\n  packages: 'mypkg'\n"
          )
          conn = _setup(tmp_path)
          _refresh_packages(conn, tmp_path)
          with caplog.at_level(logging.WARNING, logger="graph_io.domains"):
              domains.emit(conn, repo_root=tmp_path, ctx=CTX, skip_dirs=frozenset())
          assert "non-list 'packages:' field" in caplog.text
          assert _count_domains(conn) == 0


      def test_yaml_parse_error_raises_domain_yaml_error(tmp_path: Path) -> None:
          _write_pkg(tmp_path, "mypkg")
          (tmp_path / "domains.yaml").write_text(
              "a: { invalid: yaml: payload\n"
          )
          conn = _setup(tmp_path)
          _refresh_packages(conn, tmp_path)
          with pytest.raises(DomainYamlError) as exc_info:
              domains.emit(conn, repo_root=tmp_path, ctx=CTX, skip_dirs=frozenset())
          assert exc_info.value.exit_code == 4
          assert "YAML parse error" in str(exc_info.value)


      def test_no_convention_inference_from_test_dir(tmp_path: Path) -> None:
          _write_pkg(tmp_path, "mypkg")
          # Create a tests/billing/ directory — this should NOT produce a Domain('billing')
          (tmp_path / "tests" / "billing").mkdir(parents=True, exist_ok=True)
          (tmp_path / "tests" / "billing" / "__init__.py").write_text("")
          # No domains.yaml — explicit-config-only (DOMAIN-05)
          conn = _setup(tmp_path)
          _refresh_packages(conn, tmp_path)
          domains.emit(conn, repo_root=tmp_path, ctx=CTX, skip_dirs=frozenset())
          # SC#5: no Domain node named 'billing' should exist
          row = conn.execute(
              "SELECT id FROM nodes WHERE kind='domain' AND name='billing'"
          ).fetchone()
          assert row is None
          assert _count_domains(conn) == 0


      def test_idempotent_emit(tmp_path: Path) -> None:
          _write_pkg(tmp_path, "mypkg")
          (tmp_path / "domains.yaml").write_text(
              "core:\n  packages: [mypkg]\n"
          )
          conn = _setup(tmp_path)
          _refresh_packages(conn, tmp_path)
          domains.emit(conn, repo_root=tmp_path, ctx=CTX, skip_dirs=frozenset())
          edges_first = conn.execute(
              "SELECT src, dst, kind FROM edges ORDER BY src, dst, kind"
          ).fetchall()
          domains.emit(conn, repo_root=tmp_path, ctx=CTX, skip_dirs=frozenset())
          edges_second = conn.execute(
              "SELECT src, dst, kind FROM edges ORDER BY src, dst, kind"
          ).fetchall()
          assert edges_first == edges_second
          assert _count_domains(conn) == 1   # not duplicated

    Confirm pytest discovers all 13 test functions and they pass.
  </action>
  <verify>
    <automated>cd /Users/pat/Personal/agent-research && uv run --package graph-io pytest packages/graph-io/tests/test_domains.py -q</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/graph-io/tests/test_domains.py` exists
    - `grep -cE '^def test_' packages/graph-io/tests/test_domains.py` returns >= 13 (thirteen distinct test functions)
    - `grep -F 'def test_missing_yaml_zero_domain' packages/graph-io/tests/test_domains.py` exits 0
    - `grep -F 'def test_emit_domain_nodes' packages/graph-io/tests/test_domains.py` exits 0
    - `grep -F 'def test_multi_domain_membership' packages/graph-io/tests/test_domains.py` exits 0
    - `grep -F 'def test_cycle_skip_only_intra_scc' packages/graph-io/tests/test_domains.py` exits 0
    - `grep -F 'def test_self_loop_skip' packages/graph-io/tests/test_domains.py` exits 0
    - `grep -F 'def test_no_convention_inference_from_test_dir' packages/graph-io/tests/test_domains.py` exits 0
    - `grep -F 'def test_unknown_package_warns_with_known_list' packages/graph-io/tests/test_domains.py` exits 0
    - `grep -F 'def test_idempotent_emit' packages/graph-io/tests/test_domains.py` exits 0
    - `grep -F 'def test_yaml_parse_error_raises_domain_yaml_error' packages/graph-io/tests/test_domains.py` exits 0
    - `uv run --package graph-io pytest packages/graph-io/tests/test_domains.py -q` exits 0 with all 13+ tests passing
  </acceptance_criteria>
  <done>
    domains.emit has direct, fixture-independent coverage for every DOMAIN-01..05
    requirement and the D-15 cycle-recovery contract. Plan 31-04's end-to-end
    test can rely on these behaviors being green.
  </done>
</task>

</tasks>

<verification>
The full graph-io test suite must remain green after all four tasks complete:

  cd /Users/pat/Personal/agent-research && uv run --package graph-io pytest packages/graph-io/tests/ -q

Net test count change: + at least 13 (the new test_domains.py tests). Phase 29
and Phase 30 tests must remain green (no regressions).
</verification>

<success_criteria>
- [ ] `packages/graph-io/src/graph_io/domains.py` exists with `emit`, `DomainYamlError`, `_detect_cycles` (DOMAIN-01..05)
- [ ] `_detect_cycles` correctly identifies length-2, length-3 SCCs and self-loops
- [ ] `_emit_containment_edges` skips ONLY cycle-participating edges (D-15)
- [ ] `tests/fixtures/sample_monorepo/domains.yaml` exists with valid config
- [ ] `tests/test_domains.py` has >= 13 tests covering all CONTEXT.md D-01..D-15 paths and SC#5
- [ ] Full graph-io test suite remains green
</success_criteria>
