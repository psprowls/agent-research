---
phase: 31-domain-layer-derived-edges
plan: 04
type: execute
wave: 2
depends_on: [31-02, 31-03]
files_modified:
  - packages/graph-io/src/graph_io/derived_edges.py
  - packages/graph-io/src/graph_io/update.py
  - packages/graph-io/tests/test_derived_edges.py
autonomous: true
requirements:
  - DERIVED-01
  - DERIVED-02
  - DERIVED-03
  - DERIVED-04
tags:
  - graph-io
  - derived-edges
  - references
  - depends-on
  - update-orchestration
  - testsuite-domain

must_haves:
  truths:
    - "A new module packages/graph-io/src/graph_io/derived_edges.py exists with one public function compute(conn, *, repo_root, ctx) that wraps three pass functions (references, depends_on, TestSuite→Domain) in a single transaction (D-17, DERIVED-03)"
    - "compute starts with three DELETE statements (single transaction): DELETE FROM edges WHERE kind IN ('references', 'depends_on'); DELETE FROM edges WHERE kind='tests' AND dst IN (SELECT id FROM nodes WHERE kind='domain'); these run BEFORE any INSERT — trivially idempotent (D-17). The targeted DELETE for tests-with-Domain-dst clears the prior pass's TestSuite→Domain edges WITHOUT touching TestSuite→Package or TestSuite→Repository edges, which remain Phase 30's responsibility (D-14)"
    - "After the DELETEs, compute calls _compute_references_and_depends_on(conn, repo_root, ctx) which performs a SINGLE shared traversal of (domain → packages → imports) building two bucket maps (ref_buckets and dep_buckets) and emitting edges from both in one pass (D-17 — avoids double-scanning imports)"
    - "Then compute calls _compute_testsuite_domain(conn, ctx) which iterates TestSuite nodes and emits TestSuite -> Domain 'tests' edges per D-12 / D-13"
    - "_compute_references_and_depends_on uses scan_package_imports(conn, repo_root, pkg_name, pkg_rel, include_test_files=False) from graph_io.import_scan (D-10 + D-11) — test files are EXCLUDED from references and depends_on derivation"
    - "references edge (kind='references') is Domain D -> Package P; emitted when at least one Package directly in D imports P AND P is NOT directly in D; attrs_json carries usage_count = number of distinct Packages directly in D that import P (D-07, D-08, D-18)"
    - "depends_on edge (kind='depends_on') is Domain A -> Domain B (A != B); emitted when at least one Package in A directly imports at least one Package in B; attrs_json carries usage_count = number of distinct (importing_pkg_in_A, imported_pkg_in_B) pairs; self-loops (A == B) are NEVER emitted (D-09, D-18)"
    - "_compute_testsuite_domain emits TestSuite -> Domain 'tests' edge ONLY when: (a) the TestSuite has >= 2 distinct TestSuite -> Package edges; (b) every Package the TestSuite touches has a belongs_to_domain edge to the SAME single Domain D; (c) no Package the TestSuite touches has any belongs_to_domain edge to a Domain other than D (D-12)"
    - "Multi-domain TestSuites (Packages span multiple Domains) emit NO TestSuite -> Domain edge — the multi-Package edges from Phase 30 already capture cross-cutting (D-13)"
    - "Single-Package TestSuites (only 1 TestSuite -> Package edge) emit NO TestSuite -> Domain edge — they're unit suites (D-12)"
    - "Transitive subdomain membership is NOT bubbled up at compute time — only DIRECT belongs_to_domain edges participate (DERIVED-04); queries that need transitive results walk domain_contains_domain at read time"
    - "packages/graph-io/src/graph_io/update.py is amended: inside the store.transaction(conn) block, AFTER test_suites.emit and BEFORE resolve.sweep, add: from graph_io import domains; domains.emit(conn, repo_root=repo_root, ctx=ctx, skip_dirs=skip_dirs). AFTER _enforce_strict_tree_invariant (which Phase 30 adds), add: from graph_io import derived_edges; derived_edges.compute(conn, repo_root=repo_root, ctx=ctx). The call order is locked by D-16."
    - "If Phase 30's update.py edits have not yet shipped at the time this plan runs, the planner MUST insert the domains.emit and derived_edges.compute calls in the correct positions relative to the post-Phase-30 expected layout — even if test_suites.emit and entry_points.emit and _enforce_strict_tree_invariant don't yet appear in the file, the inserted lines should reference them by the symbol names Phase 30 commits"
    - "packages/graph-io/tests/test_derived_edges.py covers: (a) references count > 0 after cross-domain imports (SC#3); (b) depends_on count > 0 after cross-domain imports (SC#3); (c) idempotency — second cg update produces identical edge sets (SC#3, D-17); (d) no transitive bubbling: a package in a subdomain D' that imports across does NOT produce a references edge from D's parent domain (DERIVED-04); (e) TestSuite -> Domain emitted when all suite packages are in one Domain (D-12); (f) TestSuite -> Domain NOT emitted when suite spans multiple Domains (D-13); (g) TestSuite -> Domain NOT emitted for single-package suites (D-12); (h) end-to-end: full update.run on sample_monorepo fixture produces expected Domain + edge counts (orchestration test)"
    - "The end-to-end test (h) calls update.run against the sample_monorepo fixture (with the domains.yaml from Plan 31-03) and asserts the final database contains: 3 Domain nodes (core, web, presentation); belongs_to_domain edges (mypkg -> core, jspkg -> web); domain_contains_domain edge (presentation -> web); and that running update.run a SECOND time produces an unchanged edges table (idempotency at the orchestration level — SC#3)"
  artifacts:
    - path: "packages/graph-io/src/graph_io/derived_edges.py"
      provides: "compute(conn, *, repo_root, ctx) — recomputes references / depends_on / TestSuite→Domain edges in one transaction"
      contains: "def compute"
      exports:
        - compute
    - path: "packages/graph-io/src/graph_io/update.py"
      provides: "Amended run() with domains.emit + derived_edges.compute in the locked call order (D-16)"
      contains: "domains.emit("
    - path: "packages/graph-io/tests/test_derived_edges.py"
      provides: "Unit + end-to-end coverage for derived edges"
      contains: "test_references_emitted"
  key_links:
    - from: "derived_edges.compute"
      to: "import_scan.scan_package_imports"
      via: "function call per Package; include_test_files=False (D-11)"
      pattern: "scan_package_imports(conn"
    - from: "update.run"
      to: "domains.emit"
      via: "function call between test_suites.emit and resolve.sweep (D-16)"
      pattern: "domains.emit(conn"
    - from: "update.run"
      to: "derived_edges.compute"
      via: "function call after _enforce_strict_tree_invariant (D-16)"
      pattern: "derived_edges.compute(conn"
---

<objective>
Create `packages/graph-io/src/graph_io/derived_edges.py` implementing the
single-transaction delete-then-recompute pass for `references`, `depends_on`,
and `TestSuite → Domain` `tests` edges per CONTEXT.md D-07, D-08, D-09, D-12,
D-13, D-14, D-17, D-18.

Amend `packages/graph-io/src/graph_io/update.py` to insert `domains.emit`
between `test_suites.emit` and `resolve.sweep`, and `derived_edges.compute`
after `_enforce_strict_tree_invariant`, locking the call order from D-16.

Ship `tests/test_derived_edges.py` covering all DERIVED-01..04 requirements
plus the end-to-end orchestration test against the `sample_monorepo` fixture
(which Plan 31-03 extended with `domains.yaml`).

Out of scope: any change to `domains.py` (Plan 31-03), `import_scan.py`
(Plan 31-02), or `uri.py` / `pyproject.toml` (Plan 31-01).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-domain-layer-derived-edges/31-CONTEXT.md
@.planning/phases/31-domain-layer-derived-edges/31-RESEARCH.md
@.planning/phases/30-entry-points-test-suites/30-CONTEXT.md
@packages/graph-io/src/graph_io/update.py
@packages/graph-io/src/graph_io/upsert.py
@packages/graph-io/src/graph_io/import_scan.py
@packages/graph-io/src/graph_io/domains.py

<interfaces>
<!-- Existing surfaces consumed by derived_edges.compute. -->

From packages/graph-io/src/graph_io/import_scan.py (Plan 31-02):
  scan_package_imports(conn, repo_root, pkg_name, pkg_rel, *, include_test_files=False) -> set[tuple[str, str | None]]

From packages/graph-io/src/graph_io/domains.py (Plan 31-03):
  emit(conn, *, repo_root, ctx, skip_dirs) -> None
  # Domain nodes have kind='domain', name=<domain-name>, path=NULL
  # belongs_to_domain edges: src=Package, dst=Domain, kind='belongs_to_domain'

From packages/graph-io/src/graph_io/upsert.py:
  upsert_records(conn, GraphRecords)

From source_parser.projections.graph:
  GraphNode, GraphEdge, GraphRecords

Schema (packages/graph-io/src/graph_io/schema.py):
  nodes(id, kind, name, path, line, attrs_json, uri)
  edges(src, dst, kind, attrs_json)
  # belongs_to_domain edges from Plan 31-03 connect Package nodes to Domain nodes
  # tests edges from Phase 30 connect TestSuite nodes to Package nodes
  # NEW: references (Domain -> Package), depends_on (Domain -> Domain), tests (TestSuite -> Domain)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold derived_edges.py with compute() outer transaction and DELETE statements</name>
  <read_first>
    - packages/graph-io/src/graph_io/import_scan.py (Plan 31-02 — public surface for scan_package_imports)
    - packages/graph-io/src/graph_io/domains.py (Plan 31-03 — Domain node and belongs_to_domain edge shape)
    - .planning/phases/31-domain-layer-derived-edges/31-CONTEXT.md (D-17 — single-transaction delete-then-recompute)
    - .planning/phases/31-domain-layer-derived-edges/31-RESEARCH.md (§6 — schema column name is `dst`, NOT `child_id` as in CONTEXT pseudocode)
    - packages/graph-io/src/graph_io/upsert.py (upsert primitives)
  </read_first>
  <files>packages/graph-io/src/graph_io/derived_edges.py</files>
  <behavior>
    - File packages/graph-io/src/graph_io/derived_edges.py exists with module docstring `"""Derived-edge computation: references / depends_on / TestSuite→Domain (Phase 31 D-07..D-18)."""`
    - Module imports: json, sqlite3, pathlib.Path; source_parser.projections.graph for GraphEdge, GraphRecords; graph_io.upsert; graph_io.import_scan.scan_package_imports; graph_io.uri.RepoContext
    - Module-level kind constants:
      _REFERENCES_KIND = "references"
      _DEPENDS_ON_KIND = "depends_on"
      _TESTS_KIND = "tests"
      _DOMAIN_KIND = "domain"
      _PACKAGE_KIND = "package"
      _TEST_SUITE_KIND = "test_suite"
      _BELONGS_TO_DOMAIN_KIND = "belongs_to_domain"
    - Public function compute(conn, *, repo_root, ctx) opens a single `with conn:` block, runs the three DELETE statements, then calls _compute_references_and_depends_on and _compute_testsuite_domain in order
    - At minimum after this task: the DELETE statements run; the two compute helpers are STUBS that do nothing. Tasks 2-3 fill them in.
    - Module imports cleanly: `uv run --package graph-io python -c "from graph_io import derived_edges; assert callable(derived_edges.compute); print('ok')"` exits 0
  </behavior>
  <action>
    Create packages/graph-io/src/graph_io/derived_edges.py:

      """Derived-edge computation.

      Runs AFTER resolve.sweep and AFTER _enforce_strict_tree_invariant
      (Phase 30 D-19). In a single transaction (D-17):

        1. DELETE all 'references' / 'depends_on' edges
        2. DELETE 'tests' edges whose dst is a Domain
        3. Recompute references and depends_on in one shared traversal
        4. Recompute TestSuite -> Domain 'tests' edges

      Trivially idempotent — second cg update produces identical edge set.
      Test files are EXCLUDED from references / depends_on derivation
      (Phase 31 D-11); their imports flow through Phase 30's TestSuite->Package
      edges and (here) TestSuite->Domain edges.
      """

      from __future__ import annotations

      import json
      import sqlite3
      from collections import defaultdict
      from pathlib import Path

      from source_parser.projections.graph import GraphEdge, GraphRecords

      from graph_io import upsert
      from graph_io.import_scan import scan_package_imports
      from graph_io.uri import RepoContext

      _REFERENCES_KIND = "references"
      _DEPENDS_ON_KIND = "depends_on"
      _TESTS_KIND = "tests"
      _DOMAIN_KIND = "domain"
      _PACKAGE_KIND = "package"
      _TEST_SUITE_KIND = "test_suite"
      _BELONGS_TO_DOMAIN_KIND = "belongs_to_domain"


      def compute(
          conn: sqlite3.Connection,
          *,
          repo_root: Path,
          ctx: RepoContext,
      ) -> None:
          """Recompute references / depends_on / TestSuite→Domain edges.

          Single transaction — delete-then-recompute per D-17. Trivially
          idempotent: a second invocation produces the same edge set.
          """
          with conn:
              # Step 1: clear prior pass's derived edges
              conn.execute(
                  "DELETE FROM edges WHERE kind IN (?, ?)",
                  (_REFERENCES_KIND, _DEPENDS_ON_KIND),
              )
              conn.execute(
                  "DELETE FROM edges WHERE kind=? AND dst IN "
                  "(SELECT id FROM nodes WHERE kind=?)",
                  (_TESTS_KIND, _DOMAIN_KIND),
              )

              # Step 2: recompute
              _compute_references_and_depends_on(conn, repo_root, ctx)
              _compute_testsuite_domain(conn, ctx)


      def _compute_references_and_depends_on(
          conn: sqlite3.Connection,
          repo_root: Path,
          ctx: RepoContext,
      ) -> None:
          """STUB — Task 2 implements the shared-traversal compute."""
          return


      def _compute_testsuite_domain(
          conn: sqlite3.Connection,
          ctx: RepoContext,
      ) -> None:
          """STUB — Task 3 implements TestSuite → Domain edge emission."""
          return

    Confirm module imports cleanly.
  </action>
  <verify>
    <automated>cd /Users/pat/Personal/agent-research && uv run --package graph-io python -c "from graph_io import derived_edges; assert callable(derived_edges.compute); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/graph-io/src/graph_io/derived_edges.py` exists
    - `grep -n '^def compute' packages/graph-io/src/graph_io/derived_edges.py` returns 1 line
    - `grep -F 'DELETE FROM edges WHERE kind IN' packages/graph-io/src/graph_io/derived_edges.py` exits 0
    - `grep -F "DELETE FROM edges WHERE kind=? AND dst IN" packages/graph-io/src/graph_io/derived_edges.py` exits 0 (uses dst, not child_id)
    - `grep -F 'child_id' packages/graph-io/src/graph_io/derived_edges.py` exits 1 (no schema-name drift)
    - `grep -F 'from graph_io.import_scan import scan_package_imports' packages/graph-io/src/graph_io/derived_edges.py` exits 0
    - `grep -n '^def _compute_references_and_depends_on\|^def _compute_testsuite_domain' packages/graph-io/src/graph_io/derived_edges.py` returns 2 lines
    - Module imports cleanly: the verify command exits 0
  </acceptance_criteria>
  <done>
    Skeleton in place. The single-transaction DELETE pass runs. Tasks 2-3 fill
    in the two compute helpers.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement _compute_references_and_depends_on (shared traversal)</name>
  <read_first>
    - packages/graph-io/src/graph_io/derived_edges.py (current state after Task 1)
    - .planning/phases/31-domain-layer-derived-edges/31-CONTEXT.md (D-07, D-08, D-09, D-18; specifics §17 pseudocode for the shared traversal)
    - .planning/phases/31-domain-layer-derived-edges/31-RESEARCH.md (§6 — single-transaction strategy)
    - packages/graph-io/src/graph_io/import_scan.py (Plan 31-02 — scan_package_imports public surface)
    - packages/graph-io/src/graph_io/upsert.py (_upsert_edge primitives)
  </read_first>
  <files>packages/graph-io/src/graph_io/derived_edges.py</files>
  <behavior>
    - _compute_references_and_depends_on builds three maps from a single SQL pass over the (Domain, Package, belongs_to_domain) graph: domain_pkgs (Domain.name -> set of Package keys directly in it), pkg_imports (Package key -> set of imported Package keys, via scan_package_imports), pkg_domains (Package key -> set of Domain names containing it)
    - Package keys are (pkg_name, pkg_rel) tuples — matching scan_package_imports's return type
    - For each Domain D and each Package src directly in D, iterate src's imports. For each (src, tgt) import pair: if D is NOT in tgt's domain set, add src to ref_buckets[(D, tgt_pkg_key)]. For every Domain B in tgt's domain set: if B != D, add (src, tgt) to dep_buckets[(D, B)].
    - Emit one references edge per (D, P) bucket key: src=(_DOMAIN_KIND, D, None), dst=(_PACKAGE_KIND, tgt_pkg_name, tgt_pkg_rel), kind=_REFERENCES_KIND, attrs={'usage_count': len(srcs_set)} (D-18)
    - Emit one depends_on edge per (A, B) bucket key: src=(_DOMAIN_KIND, A, None), dst=(_DOMAIN_KIND, B, None), kind=_DEPENDS_ON_KIND, attrs={'usage_count': len(pairs_set)} (D-18)
    - Self-loops (A == B) MUST NEVER appear in dep_buckets — the iteration explicitly skips them (D-09)
    - Use upsert.upsert_records(conn, GraphRecords(nodes=[], edges=edges_out)) to batch-write all edges (preserves the single-transaction invariant; no nested transaction)
    - scan_package_imports is called with include_test_files=False (D-11) — production code only
  </behavior>
  <action>
    Replace the STUB body of _compute_references_and_depends_on in
    packages/graph-io/src/graph_io/derived_edges.py with:

      def _compute_references_and_depends_on(
          conn: sqlite3.Connection,
          repo_root: Path,
          ctx: RepoContext,
      ) -> None:
          # Load all Domain names
          domain_rows = conn.execute(
              "SELECT name FROM nodes WHERE kind=?", (_DOMAIN_KIND,),
          ).fetchall()
          domain_names = [row[0] for row in domain_rows]
          if not domain_names:
              return   # zero-domain mode — nothing to derive

          # Load all Package rows (name, path) and build a quick lookup
          pkg_rows = conn.execute(
              "SELECT name, path FROM nodes WHERE kind=?", (_PACKAGE_KIND,),
          ).fetchall()
          all_pkg_keys: list[tuple[str, str | None]] = [
              (name, path) for name, path in pkg_rows
          ]

          # domain_pkgs: Domain.name -> set of Package keys directly in it
          domain_pkgs: dict[str, set[tuple[str, str | None]]] = defaultdict(set)
          # pkg_domains: Package key -> set of Domain names directly containing it
          pkg_domains: dict[tuple[str, str | None], set[str]] = defaultdict(set)

          membership_rows = conn.execute(
              "SELECT pkg.name, pkg.path, dom.name FROM edges e "
              "JOIN nodes pkg ON e.src=pkg.id "
              "JOIN nodes dom ON e.dst=dom.id "
              "WHERE e.kind=? AND pkg.kind=? AND dom.kind=?",
              (_BELONGS_TO_DOMAIN_KIND, _PACKAGE_KIND, _DOMAIN_KIND),
          ).fetchall()
          for pkg_name, pkg_path, dom_name in membership_rows:
              pkg_key = (pkg_name, pkg_path)
              domain_pkgs[dom_name].add(pkg_key)
              pkg_domains[pkg_key].add(dom_name)

          # pkg_imports: Package key -> set of imported Package keys
          # Scan EVERY Package once (production-code only), not just domain members
          # — because depends_on / references compute against the full import graph
          # rooted at domain-member Packages, but reads from the full Package set
          pkg_imports: dict[tuple[str, str | None], set[tuple[str, str | None]]] = {}
          for pkg_name, pkg_path in all_pkg_keys:
              # Only scan Packages that are domain members; non-domain packages
              # cannot be the SOURCE of a references / depends_on edge.
              # However a non-domain package CAN be the TARGET of a references edge.
              # So we only scan domain-member packages here.
              if (pkg_name, pkg_path) not in pkg_domains:
                  continue
              pkg_imports[(pkg_name, pkg_path)] = scan_package_imports(
                  conn, repo_root, pkg_name, pkg_path, include_test_files=False,
              )

          # Single traversal
          ref_buckets: dict[
              tuple[str, tuple[str, str | None]], set[tuple[str, str | None]],
          ] = defaultdict(set)   # (D, tgt_pkg_key) -> {src_pkg_keys}
          dep_buckets: dict[
              tuple[str, str], set[tuple[tuple[str, str | None], tuple[str, str | None]]],
          ] = defaultdict(set)   # (A, B) -> {(src_pkg_key, tgt_pkg_key)}

          for d_name, src_pkgs in domain_pkgs.items():
              for src_key in src_pkgs:
                  for tgt_key in pkg_imports.get(src_key, ()):
                      tgt_domains = pkg_domains.get(tgt_key, set())
                      # references criterion (D-08): D does NOT directly contain tgt
                      if d_name not in tgt_domains:
                          ref_buckets[(d_name, tgt_key)].add(src_key)
                      # depends_on criterion (D-09): for each Domain B containing tgt, B != d_name
                      for b_name in tgt_domains:
                          if b_name != d_name:
                              dep_buckets[(d_name, b_name)].add((src_key, tgt_key))

          # Materialize edges
          edges_out: list[GraphEdge] = []
          for (d_name, tgt_key), src_set in ref_buckets.items():
              tgt_name, tgt_path = tgt_key
              edges_out.append(GraphEdge(
                  src=(_DOMAIN_KIND, d_name, None),
                  dst=(_PACKAGE_KIND, tgt_name, tgt_path),
                  kind=_REFERENCES_KIND,
                  attrs={"usage_count": len(src_set)},
              ))
          for (a_name, b_name), pair_set in dep_buckets.items():
              edges_out.append(GraphEdge(
                  src=(_DOMAIN_KIND, a_name, None),
                  dst=(_DOMAIN_KIND, b_name, None),
                  kind=_DEPENDS_ON_KIND,
                  attrs={"usage_count": len(pair_set)},
              ))

          if edges_out:
              upsert.upsert_records(
                  conn, GraphRecords(nodes=[], edges=edges_out),
              )

    Do NOT change the public compute() signature or the DELETE statements.
  </action>
  <verify>
    <automated>cd /Users/pat/Personal/agent-research && uv run --package graph-io python -c "from graph_io import derived_edges; assert 'scan_package_imports' in dir(__import__('graph_io.derived_edges', fromlist=['scan_package_imports'])); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'ref_buckets' packages/graph-io/src/graph_io/derived_edges.py` exits 0
    - `grep -F 'dep_buckets' packages/graph-io/src/graph_io/derived_edges.py` exits 0
    - `grep -F '"usage_count":' packages/graph-io/src/graph_io/derived_edges.py` exits 0
    - `grep -F 'include_test_files=False' packages/graph-io/src/graph_io/derived_edges.py` exits 0
    - `grep -F "if b_name != d_name" packages/graph-io/src/graph_io/derived_edges.py` exits 0 (no self-loop in depends_on)
    - `grep -F 'if d_name not in tgt_domains' packages/graph-io/src/graph_io/derived_edges.py` exits 0 (D-08 cross-domain only)
    - Module imports cleanly
  </acceptance_criteria>
  <done>
    references and depends_on are computed in one shared traversal. usage_count
    attrs are populated correctly per D-18.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement _compute_testsuite_domain</name>
  <read_first>
    - packages/graph-io/src/graph_io/derived_edges.py (current state after Task 2)
    - .planning/phases/31-domain-layer-derived-edges/31-CONTEXT.md (D-12, D-13, D-14)
    - .planning/phases/31-domain-layer-derived-edges/31-CONTEXT.md specifics §16 pseudocode
    - .planning/phases/30-entry-points-test-suites/30-CONTEXT.md (D-09 — TestSuite -> Package edge shape; D-13 — defers TestSuite -> Domain to Phase 31)
  </read_first>
  <files>packages/graph-io/src/graph_io/derived_edges.py</files>
  <behavior>
    - _compute_testsuite_domain iterates every TestSuite node in the DB. For each one, it collects the set of Package nodes that TestSuite references via 'tests' edges (Phase 30 D-09)
    - If the Package set has < 2 distinct Packages → skip (single-package suite per D-12)
    - For each Package in the set, look up its belongs_to_domain edges; collect the set of Domain names
    - If ANY Package has zero Domain memberships → skip (cannot determine suite's domain)
    - If the intersection of all Packages' Domain sets has exactly one element AND every Package's Domain set equals that intersection → emit one tests edge TestSuite -> Domain (D-12)
    - Otherwise (intersection size 0 or > 1, or some Package belongs to additional Domains beyond the intersection) → skip (D-13 — multi-domain spans get no Domain edge)
    - Emit edges via upsert.upsert_records(conn, GraphRecords(nodes=[], edges=edges_out))
    - tests-edge attrs are empty (mirrors Phase 30 D-09 TestSuite->Package edges; D-18)
  </behavior>
  <action>
    Replace the STUB body of _compute_testsuite_domain in
    packages/graph-io/src/graph_io/derived_edges.py with:

      def _compute_testsuite_domain(
          conn: sqlite3.Connection,
          ctx: RepoContext,
      ) -> None:
          # Build (suite_id, suite_name, suite_path) -> set of Package keys
          suite_pkg_rows = conn.execute(
              "SELECT ts.id, ts.name, ts.path, pkg.name, pkg.path FROM edges e "
              "JOIN nodes ts ON e.src=ts.id "
              "JOIN nodes pkg ON e.dst=pkg.id "
              "WHERE e.kind=? AND ts.kind=? AND pkg.kind=?",
              (_TESTS_KIND, _TEST_SUITE_KIND, _PACKAGE_KIND),
          ).fetchall()
          suite_pkgs: dict[
              tuple[int, str, str | None],
              set[tuple[str, str | None]],
          ] = defaultdict(set)
          for ts_id, ts_name, ts_path, pkg_name, pkg_path in suite_pkg_rows:
              suite_pkgs[(ts_id, ts_name, ts_path)].add((pkg_name, pkg_path))

          # Build Package key -> set of Domain names
          pkg_domains: dict[tuple[str, str | None], set[str]] = defaultdict(set)
          membership_rows = conn.execute(
              "SELECT pkg.name, pkg.path, dom.name FROM edges e "
              "JOIN nodes pkg ON e.src=pkg.id "
              "JOIN nodes dom ON e.dst=dom.id "
              "WHERE e.kind=? AND pkg.kind=? AND dom.kind=?",
              (_BELONGS_TO_DOMAIN_KIND, _PACKAGE_KIND, _DOMAIN_KIND),
          ).fetchall()
          for pkg_name, pkg_path, dom_name in membership_rows:
              pkg_domains[(pkg_name, pkg_path)].add(dom_name)

          edges_out: list[GraphEdge] = []
          for (ts_id, ts_name, ts_path), pkg_keys in suite_pkgs.items():
              if len(pkg_keys) < 2:
                  continue   # D-12: single-package suites get no Domain edge

              domain_sets = [pkg_domains.get(p, set()) for p in pkg_keys]
              if not all(domain_sets):
                  continue   # any package with zero Domains -> skip

              intersection = set.intersection(*domain_sets)
              if len(intersection) != 1:
                  continue   # D-13: multi-domain spans get no Domain edge
              # All packages must belong to EXACTLY the intersection set
              if any(ds != intersection for ds in domain_sets):
                  continue   # some package has additional Domain memberships beyond the intersection

              (target_domain,) = intersection
              edges_out.append(GraphEdge(
                  src=(_TEST_SUITE_KIND, ts_name, ts_path),
                  dst=(_DOMAIN_KIND, target_domain, None),
                  kind=_TESTS_KIND,
                  attrs={},
              ))

          if edges_out:
              upsert.upsert_records(
                  conn, GraphRecords(nodes=[], edges=edges_out),
              )

    Do NOT change compute() or _compute_references_and_depends_on signatures.
  </action>
  <verify>
    <automated>cd /Users/pat/Personal/agent-research && uv run --package graph-io python -c "from graph_io import derived_edges; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'suite_pkgs' packages/graph-io/src/graph_io/derived_edges.py` exits 0
    - `grep -F 'len(pkg_keys) < 2' packages/graph-io/src/graph_io/derived_edges.py` exits 0 (D-12 single-package skip)
    - `grep -F 'set.intersection(*domain_sets)' packages/graph-io/src/graph_io/derived_edges.py` exits 0
    - `grep -F 'len(intersection) != 1' packages/graph-io/src/graph_io/derived_edges.py` exits 0 (D-13 multi-domain skip)
    - `grep -F 'if any(ds != intersection for ds in domain_sets)' packages/graph-io/src/graph_io/derived_edges.py` exits 0 (full-intersection check)
    - Module imports cleanly
  </acceptance_criteria>
  <done>
    TestSuite -> Domain edges are emitted per D-12 and skipped per D-13.
    Single-package suites and multi-domain spans correctly emit no edge.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Wire domains.emit and derived_edges.compute into update.run (D-16)</name>
  <read_first>
    - packages/graph-io/src/graph_io/update.py (full file — current state including Phase 29 wiring + Phase 30's expected edits per .planning/phases/30-entry-points-test-suites/30-03-PLAN.md)
    - .planning/phases/31-domain-layer-derived-edges/31-CONTEXT.md (D-16 — locked call order)
    - .planning/phases/31-domain-layer-derived-edges/31-RESEARCH.md (§1 — post-Phase-30 expected layout)
  </read_first>
  <files>packages/graph-io/src/graph_io/update.py</files>
  <behavior>
    - update.py imports `domains` and `derived_edges` at the top of the module (alongside existing `_ignore, packages, resolve, schema, store, upsert`)
    - Inside `run()`'s `with store.transaction(conn):` block, AFTER the test_suites.emit call (which Phase 30 inserts) and BEFORE resolve.sweep, the function calls: `domains.emit(conn, repo_root=repo_root, ctx=ctx, skip_dirs=skip_dirs)`
    - AFTER `_enforce_strict_tree_invariant(conn)` (which Phase 30 inserts) and BEFORE the `_set_metadata` calls, the function calls: `derived_edges.compute(conn, repo_root=repo_root, ctx=ctx)`
    - If at the time this plan runs Phase 30's update.py edits have NOT been committed yet (test_suites.emit / entry_points.emit / _enforce_strict_tree_invariant don't yet appear), the executor MUST insert the Phase 31 calls at the locations that WILL exist after Phase 30 lands — i.e., domains.emit goes immediately before resolve.sweep, and derived_edges.compute goes immediately after the structural_nodes.emit / resolve.sweep block. The executor refers to the planned Phase 30 layout in .planning/phases/30-entry-points-test-suites/30-03-PLAN.md
    - Idempotent re-runs: existing Phase 28/29 logic plus the new Phase 31 emit + compute calls produce stable rows on a second cg update (asserted by Task 5)
    - No other functions in update.py are modified (no signature changes, no logic refactors beyond the inserted call sites)
  </behavior>
  <action>
    Edit packages/graph-io/src/graph_io/update.py.

    1. Add `domains` and `derived_edges` to the top-level import line (around
       line 19):

         from graph_io import _ignore, derived_edges, domains, packages, resolve, schema, store, upsert

       (Keep alphabetical order if that's the style already in use.)

    2. Locate the run() function's `with store.transaction(conn):` block
       (around lines 234-258 in the current file, but Phase 30 will have
       added test_suites.emit / entry_points.emit / _enforce_strict_tree_invariant).

       The post-Phase-30 expected layout is:

         with store.transaction(conn):
             _process_files(conn, repo_root, changed, skip_dirs)
             packages.refresh(conn, repo_root=repo_root, ctx=ctx)
             if full:
                 # full-rebuild DELETE pass
                 ...
             from graph_io import entry_points, structural_nodes, test_suites  # noqa: PLC0415
             structural_nodes.emit(conn, repo_root=repo_root, ctx=ctx, skip_dirs=skip_dirs)
             entry_points.emit(conn, repo_root=repo_root, ctx=ctx, skip_dirs=skip_dirs)
             test_suites.emit(conn, repo_root=repo_root, ctx=ctx, skip_dirs=skip_dirs)
             # <-- INSERT domains.emit HERE
             resolve.sweep(conn)
             _enforce_strict_tree_invariant(conn)
             # <-- INSERT derived_edges.compute HERE
             _set_metadata(conn, "last_indexed_commit", head)
             _set_metadata(conn, "last_indexed_at", _dt.datetime.now(_dt.UTC).isoformat())

       Insert exactly these two lines at the marked positions:

         domains.emit(conn, repo_root=repo_root, ctx=ctx, skip_dirs=skip_dirs)

         derived_edges.compute(conn, repo_root=repo_root, ctx=ctx)

       The blank lines and indentation MUST match the surrounding lines
       (8-space indent inside the with block — the same as the other emit()
       calls).

       If Phase 30's edits are not yet present (test_suites.emit / etc. are
       absent from the file), insert domains.emit right before the resolve.sweep
       call, and insert derived_edges.compute right after resolve.sweep + any
       _enforce_strict_tree_invariant present in the file (or directly after
       resolve.sweep if Phase 30 hasn't landed yet). The exact relative ordering
       per D-16 must be preserved REGARDLESS of Phase 30's state — Phase 30 +
       Phase 31 may execute in either order at the file-system level, but the
       FINAL call order in update.py must match the D-16 spec.

    3. Do NOT change any other line in update.py — no helper refactor, no
       signature changes, no docstring updates.
  </action>
  <verify>
    <automated>cd /Users/pat/Personal/agent-research && grep -F 'domains.emit(conn, repo_root=repo_root, ctx=ctx, skip_dirs=skip_dirs)' packages/graph-io/src/graph_io/update.py && grep -F 'derived_edges.compute(conn, repo_root=repo_root, ctx=ctx)' packages/graph-io/src/graph_io/update.py && uv run --package graph-io python -c "from graph_io import update; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nF 'from graph_io import' packages/graph-io/src/graph_io/update.py | head -1` mentions both `domains` and `derived_edges` (or they are imported elsewhere at module top)
    - `grep -F 'domains.emit(conn, repo_root=repo_root, ctx=ctx, skip_dirs=skip_dirs)' packages/graph-io/src/graph_io/update.py` exits 0
    - `grep -F 'derived_edges.compute(conn, repo_root=repo_root, ctx=ctx)' packages/graph-io/src/graph_io/update.py` exits 0
    - The line containing `domains.emit(` appears BEFORE the line containing `resolve.sweep(conn)` (verifiable with `awk '/domains.emit/{d=NR} /resolve.sweep/{r=NR} END{exit !(d && r && d < r)}' packages/graph-io/src/graph_io/update.py`)
    - The line containing `derived_edges.compute(` appears AFTER `resolve.sweep(conn)` in the file (same awk pattern with derived_edges and resolve.sweep)
    - update.py imports cleanly
  </acceptance_criteria>
  <done>
    `update.run` now invokes domains.emit and derived_edges.compute in the
    locked D-16 order. `cg update` end-to-end produces the full Domain layer
    plus derived edges.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Add comprehensive unit + end-to-end tests in tests/test_derived_edges.py</name>
  <read_first>
    - packages/graph-io/src/graph_io/derived_edges.py (full file after Tasks 1-3)
    - packages/graph-io/src/graph_io/update.py (after Task 4)
    - packages/graph-io/tests/test_structural_nodes.py (analog test patterns)
    - packages/graph-io/tests/test_domains.py (Plan 31-03 — analog for domain-setup fixtures)
    - packages/graph-io/tests/fixtures/sample_monorepo/domains.yaml (Plan 31-03 — end-to-end fixture)
    - packages/graph-io/tests/test_update_full.py (analog for end-to-end update.run tests)
  </read_first>
  <files>packages/graph-io/tests/test_derived_edges.py</files>
  <behavior>
    - File packages/graph-io/tests/test_derived_edges.py exists with at least 8 distinct test functions per must_haves truth (n)
    - Each test follows the pattern: tmp_path → write package skeletons + domains.yaml + cross-domain imports → store.connect + schema → packages.refresh → structural_nodes.emit (Phase 29) → (if Phase 30 has shipped: test_suites.emit) → domains.emit → derived_edges.compute → SQL assertions
    - test_references_emitted: build pkg_a in domain D and pkg_b in domain E; pkg_a/src/pkg_a/foo.py imports pkg_b; assert SELECT COUNT(*) FROM edges WHERE kind='references' AND src=(D node) AND dst=(pkg_b node) returns 1; assert usage_count in attrs_json == 1
    - test_depends_on_emitted: same fixture; assert SELECT COUNT(*) FROM edges WHERE kind='depends_on' AND src=(D node) AND dst=(E node) returns 1; assert usage_count == 1
    - test_idempotency: run derived_edges.compute twice on the same DB; assert the (src, dst, kind) tuples are identical between the two passes (D-17, SC#3)
    - test_no_transitive_storage: build domains parent and child (parent contains child via domain_contains_domain); a package in child imports across to another domain; assert references edge originates from CHILD, not parent (DERIVED-04 — no bubble-up at compute time)
    - test_testsuite_domain_emitted: build a TestSuite that has tests edges to pkg_a and pkg_b (both in domain D); assert one TestSuite -> Domain D tests edge exists
    - test_testsuite_no_domain_on_multi_domain_span: TestSuite touches pkg_a (in D) and pkg_b (in E); assert NO TestSuite -> Domain edge exists
    - test_testsuite_no_domain_for_single_package_suite: TestSuite has only 1 tests edge (to pkg_a in D); assert NO TestSuite -> Domain edge exists (D-12)
    - test_update_run_end_to_end: copy sample_monorepo fixture into tmp_path, git init + commit, call update.run(repo_root); assert: 3 Domain nodes (core, web, presentation); belongs_to_domain edge for mypkg -> core and jspkg -> web; domain_contains_domain edge for presentation -> web; run update.run a SECOND time and assert edges table unchanged (SC#3 orchestration idempotency)
    - If Phase 30's test_suites.emit is not yet present at test-run time (i.e., Phase 30 hasn't executed in this codebase yet), the TestSuite-related tests (test_testsuite_*) MUST gracefully handle that — either by mocking the TestSuite nodes + tests edges directly in the DB, OR by skipping with pytest.skip("Phase 30 test_suites.emit not yet integrated") if the test_suites module is unavailable. Recommended: build TestSuite nodes + tests edges directly in test setup so the tests don't depend on Phase 30 execution ordering.
  </behavior>
  <action>
    Create packages/graph-io/tests/test_derived_edges.py. Skeleton:

      """Unit + end-to-end tests for graph_io.derived_edges (Phase 31 DERIVED-01..04)."""

      from __future__ import annotations

      import json
      import shutil
      import sqlite3
      import subprocess
      from pathlib import Path

      import pytest

      from graph_io import (
          derived_edges,
          domains,
          packages,
          schema,
          store,
          structural_nodes,
          update,
      )
      from graph_io.uri import RepoContext

      CTX = RepoContext(org="testorg", repo="testrepo")

      FIXTURE_ROOT = Path(__file__).parent / "fixtures" / "sample_monorepo"


      def _setup(tmp_path: Path) -> sqlite3.Connection:
          conn = store.connect(tmp_path / "code.db", create=True)
          return conn


      def _write_pkg_py(root: Path, name: str, files: dict[str, str]) -> None:
          pkg_dir = root / "packages" / name
          src_dir = pkg_dir / "src" / name.replace("-", "_")
          src_dir.mkdir(parents=True, exist_ok=True)
          (src_dir / "__init__.py").write_text("")
          (pkg_dir / "pyproject.toml").write_text(
              f'[project]\nname = "{name}"\nversion = "0.0.0"\n'
              f'requires-python = ">=3.11"\n'
              f'[build-system]\nrequires = ["uv_build"]\nbuild-backend = "uv_build"\n'
          )
          for rel, content in files.items():
              (pkg_dir / rel).parent.mkdir(parents=True, exist_ok=True)
              (pkg_dir / rel).write_text(content)


      def _emit_phase_29(conn: sqlite3.Connection, root: Path) -> None:
          with store.transaction(conn):
              packages.refresh(conn, repo_root=root, ctx=CTX)
              structural_nodes.emit(
                  conn, repo_root=root, ctx=CTX, skip_dirs=frozenset(),
              )


      def _two_domain_setup(tmp_path: Path) -> sqlite3.Connection:
          _write_pkg_py(tmp_path, "pkg-a", {
              "src/pkg_a/foo.py": "from pkg_b import bar\n",
          })
          _write_pkg_py(tmp_path, "pkg-b", {
              "src/pkg_b/__init__.py": "",
              "src/pkg_b/bar.py": "x = 1\n",
          })
          (tmp_path / "domains.yaml").write_text(
              "D:\n  packages: [pkg-a]\n"
              "E:\n  packages: [pkg-b]\n"
          )
          conn = _setup(tmp_path)
          _emit_phase_29(conn, tmp_path)
          domains.emit(conn, repo_root=tmp_path, ctx=CTX, skip_dirs=frozenset())
          derived_edges.compute(conn, repo_root=tmp_path, ctx=CTX)
          return conn


      def test_references_emitted(tmp_path: Path) -> None:
          conn = _two_domain_setup(tmp_path)
          row = conn.execute(
              "SELECT e.attrs_json FROM edges e "
              "JOIN nodes src ON e.src=src.id "
              "JOIN nodes dst ON e.dst=dst.id "
              "WHERE e.kind='references' AND src.name='D' AND dst.name='pkg-b'"
          ).fetchone()
          assert row is not None
          attrs = json.loads(row[0]) if row[0] else {}
          assert attrs.get("usage_count") == 1


      def test_depends_on_emitted(tmp_path: Path) -> None:
          conn = _two_domain_setup(tmp_path)
          row = conn.execute(
              "SELECT e.attrs_json FROM edges e "
              "JOIN nodes src ON e.src=src.id "
              "JOIN nodes dst ON e.dst=dst.id "
              "WHERE e.kind='depends_on' AND src.name='D' AND dst.name='E'"
          ).fetchone()
          assert row is not None
          attrs = json.loads(row[0]) if row[0] else {}
          assert attrs.get("usage_count") == 1


      def test_no_self_loops_in_depends_on(tmp_path: Path) -> None:
          # pkg-a in domain D imports pkg-a-helper (also in D)
          _write_pkg_py(tmp_path, "pkg-a", {
              "src/pkg_a/foo.py": "from pkg_a_helper import x\n",
          })
          _write_pkg_py(tmp_path, "pkg-a-helper", {
              "src/pkg_a_helper/__init__.py": "x = 1\n",
          })
          (tmp_path / "domains.yaml").write_text(
              "D:\n  packages: [pkg-a, pkg-a-helper]\n"
          )
          conn = _setup(tmp_path)
          _emit_phase_29(conn, tmp_path)
          domains.emit(conn, repo_root=tmp_path, ctx=CTX, skip_dirs=frozenset())
          derived_edges.compute(conn, repo_root=tmp_path, ctx=CTX)
          # D -> D depends_on must NOT exist (D-09)
          count = conn.execute(
              "SELECT COUNT(*) FROM edges e JOIN nodes s ON e.src=s.id "
              "JOIN nodes d ON e.dst=d.id "
              "WHERE e.kind='depends_on' AND s.name='D' AND d.name='D'"
          ).fetchone()[0]
          assert count == 0
          # Intra-domain references also must NOT exist (D-08)
          count = conn.execute(
              "SELECT COUNT(*) FROM edges e JOIN nodes s ON e.src=s.id "
              "JOIN nodes d ON e.dst=d.id "
              "WHERE e.kind='references' AND s.name='D' AND d.name='pkg-a-helper'"
          ).fetchone()[0]
          assert count == 0


      def test_idempotency(tmp_path: Path) -> None:
          conn = _two_domain_setup(tmp_path)
          edges_first = sorted(conn.execute(
              "SELECT src, dst, kind FROM edges WHERE kind IN ('references', 'depends_on')"
          ).fetchall())
          derived_edges.compute(conn, repo_root=tmp_path, ctx=CTX)
          edges_second = sorted(conn.execute(
              "SELECT src, dst, kind FROM edges WHERE kind IN ('references', 'depends_on')"
          ).fetchall())
          assert edges_first == edges_second


      def test_no_transitive_storage(tmp_path: Path) -> None:
          # parent contains child; child has pkg-a importing pkg-x in OUTSIDE
          _write_pkg_py(tmp_path, "pkg-a", {
              "src/pkg_a/foo.py": "from pkg_x import y\n",
          })
          _write_pkg_py(tmp_path, "pkg-x", {
              "src/pkg_x/__init__.py": "y = 1\n",
          })
          (tmp_path / "domains.yaml").write_text(
              "parent:\n  packages: []\n"
              "child:\n  packages: [pkg-a]\n  parent: parent\n"
              "outside:\n  packages: [pkg-x]\n"
          )
          conn = _setup(tmp_path)
          _emit_phase_29(conn, tmp_path)
          domains.emit(conn, repo_root=tmp_path, ctx=CTX, skip_dirs=frozenset())
          derived_edges.compute(conn, repo_root=tmp_path, ctx=CTX)
          # references edge exists from child -> pkg-x
          row = conn.execute(
              "SELECT 1 FROM edges e JOIN nodes s ON e.src=s.id "
              "JOIN nodes d ON e.dst=d.id "
              "WHERE e.kind='references' AND s.name='child' AND d.name='pkg-x'"
          ).fetchone()
          assert row is not None
          # references edge MUST NOT exist from parent -> pkg-x (no transitive bubble-up at compute time, DERIVED-04)
          row = conn.execute(
              "SELECT 1 FROM edges e JOIN nodes s ON e.src=s.id "
              "JOIN nodes d ON e.dst=d.id "
              "WHERE e.kind='references' AND s.name='parent' AND d.name='pkg-x'"
          ).fetchone()
          assert row is None


      def _seed_testsuite(
          conn: sqlite3.Connection,
          suite_name: str,
          suite_path: str | None,
          pkg_keys: list[tuple[str, str | None]],
      ) -> None:
          """Insert a TestSuite node + tests edges directly (bypassing Phase 30
          for testing isolation)."""
          conn.execute(
              "INSERT OR IGNORE INTO nodes(kind, name, path) VALUES (?, ?, ?)",
              ("test_suite", suite_name, suite_path),
          )
          for pkg_name, pkg_path in pkg_keys:
              suite_id = conn.execute(
                  "SELECT id FROM nodes WHERE kind='test_suite' AND name=?",
                  (suite_name,),
              ).fetchone()[0]
              if pkg_path is None:
                  pkg_id = conn.execute(
                      "SELECT id FROM nodes WHERE kind='package' AND name=? AND path IS NULL",
                      (pkg_name,),
                  ).fetchone()[0]
              else:
                  pkg_id = conn.execute(
                      "SELECT id FROM nodes WHERE kind='package' AND name=? AND path=?",
                      (pkg_name, pkg_path),
                  ).fetchone()[0]
              conn.execute(
                  "INSERT OR IGNORE INTO edges(src, dst, kind, attrs_json) "
                  "VALUES (?, ?, 'tests', NULL)",
                  (suite_id, pkg_id),
              )


      def test_testsuite_domain_emitted(tmp_path: Path) -> None:
          _write_pkg_py(tmp_path, "pkg-a", {})
          _write_pkg_py(tmp_path, "pkg-b", {})
          (tmp_path / "domains.yaml").write_text(
              "D:\n  packages: [pkg-a, pkg-b]\n"
          )
          conn = _setup(tmp_path)
          _emit_phase_29(conn, tmp_path)
          domains.emit(conn, repo_root=tmp_path, ctx=CTX, skip_dirs=frozenset())
          _seed_testsuite(conn, "integration", "tests/integration",
                          [("pkg-a", "packages/pkg-a"),
                           ("pkg-b", "packages/pkg-b")])
          derived_edges.compute(conn, repo_root=tmp_path, ctx=CTX)
          row = conn.execute(
              "SELECT 1 FROM edges e JOIN nodes s ON e.src=s.id "
              "JOIN nodes d ON e.dst=d.id "
              "WHERE e.kind='tests' AND s.name='integration' AND d.kind='domain' AND d.name='D'"
          ).fetchone()
          assert row is not None


      def test_testsuite_no_domain_on_multi_domain_span(tmp_path: Path) -> None:
          _write_pkg_py(tmp_path, "pkg-a", {})
          _write_pkg_py(tmp_path, "pkg-b", {})
          (tmp_path / "domains.yaml").write_text(
              "D:\n  packages: [pkg-a]\n"
              "E:\n  packages: [pkg-b]\n"
          )
          conn = _setup(tmp_path)
          _emit_phase_29(conn, tmp_path)
          domains.emit(conn, repo_root=tmp_path, ctx=CTX, skip_dirs=frozenset())
          _seed_testsuite(conn, "integration", "tests/integration",
                          [("pkg-a", "packages/pkg-a"),
                           ("pkg-b", "packages/pkg-b")])
          derived_edges.compute(conn, repo_root=tmp_path, ctx=CTX)
          row = conn.execute(
              "SELECT 1 FROM edges e JOIN nodes s ON e.src=s.id "
              "JOIN nodes d ON e.dst=d.id "
              "WHERE e.kind='tests' AND s.name='integration' AND d.kind='domain'"
          ).fetchone()
          assert row is None


      def test_testsuite_no_domain_for_single_package_suite(tmp_path: Path) -> None:
          _write_pkg_py(tmp_path, "pkg-a", {})
          (tmp_path / "domains.yaml").write_text(
              "D:\n  packages: [pkg-a]\n"
          )
          conn = _setup(tmp_path)
          _emit_phase_29(conn, tmp_path)
          domains.emit(conn, repo_root=tmp_path, ctx=CTX, skip_dirs=frozenset())
          _seed_testsuite(conn, "unit", "tests/unit",
                          [("pkg-a", "packages/pkg-a")])
          derived_edges.compute(conn, repo_root=tmp_path, ctx=CTX)
          row = conn.execute(
              "SELECT 1 FROM edges e JOIN nodes s ON e.src=s.id "
              "JOIN nodes d ON e.dst=d.id "
              "WHERE e.kind='tests' AND s.name='unit' AND d.kind='domain'"
          ).fetchone()
          assert row is None


      def test_update_run_end_to_end(tmp_path: Path) -> None:
          # Copy sample_monorepo into tmp_path, git init + commit, run update.run
          shutil.copytree(FIXTURE_ROOT, tmp_path / "repo")
          repo = tmp_path / "repo"
          subprocess.run(["git", "init", "-q"], cwd=repo, check=True)
          subprocess.run(["git", "add", "-A"], cwd=repo, check=True)
          subprocess.run(
              ["git", "-c", "user.email=t@t", "-c", "user.name=t",
               "commit", "-q", "-m", "init"],
              cwd=repo, check=True,
          )
          update.run(repo, full=True)
          db_path = (repo / ".workspace" / "graph" / "code.db") if (repo / ".workspace").exists() else None
          # Workspace resolution may write elsewhere; locate the DB via the
          # workspace_io public API used by update.run.
          from workspace_io.config import resolve as resolve_ws
          ws = resolve_ws(repo, require_manifest=False).workspace
          from workspace_io.paths import graph_dir
          db_path = graph_dir(ws) / "code.db"
          conn = sqlite3.connect(db_path)
          try:
              # 3 Domain nodes
              count = conn.execute(
                  "SELECT COUNT(*) FROM nodes WHERE kind='domain'"
              ).fetchone()[0]
              assert count == 3
              # belongs_to_domain edges
              count = conn.execute(
                  "SELECT COUNT(*) FROM edges WHERE kind='belongs_to_domain'"
              ).fetchone()[0]
              assert count >= 2   # mypkg -> core, jspkg -> web
              # domain_contains_domain edge presentation -> web
              row = conn.execute(
                  "SELECT 1 FROM edges e JOIN nodes s ON e.src=s.id "
                  "JOIN nodes d ON e.dst=d.id "
                  "WHERE e.kind='domain_contains_domain' AND s.name='presentation' AND d.name='web'"
              ).fetchone()
              assert row is not None
          finally:
              conn.close()
          # SC#3: second update.run produces unchanged edges
          conn = sqlite3.connect(db_path)
          edges_first = sorted(conn.execute(
              "SELECT src, dst, kind FROM edges"
          ).fetchall())
          conn.close()
          update.run(repo, full=False)
          conn = sqlite3.connect(db_path)
          edges_second = sorted(conn.execute(
              "SELECT src, dst, kind FROM edges"
          ).fetchall())
          conn.close()
          assert edges_first == edges_second

    Note for the end-to-end test: the sample_monorepo fixture must contain
    `mypkg` (Python) and `jspkg` (JS) — both from Phase 29 plan 29-04. Phase 31
    plan 31-03 added `domains.yaml`. The exact path of the DB depends on
    workspace_io resolution; use the helper to locate it as shown.

    If the sample_monorepo fixture is incomplete at the time this test runs
    (Phase 29-04 not yet executed), mark the end-to-end test with
    pytest.mark.skipif against the fixture's pyproject.toml existence — but
    by the time Phase 31 executes, Phase 29 must be complete (per STATE.md).
  </action>
  <verify>
    <automated>cd /Users/pat/Personal/agent-research && uv run --package graph-io pytest packages/graph-io/tests/test_derived_edges.py -q</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/graph-io/tests/test_derived_edges.py` exists
    - `grep -cE '^def test_' packages/graph-io/tests/test_derived_edges.py` returns >= 8
    - `grep -F 'def test_references_emitted' packages/graph-io/tests/test_derived_edges.py` exits 0
    - `grep -F 'def test_depends_on_emitted' packages/graph-io/tests/test_derived_edges.py` exits 0
    - `grep -F 'def test_idempotency' packages/graph-io/tests/test_derived_edges.py` exits 0
    - `grep -F 'def test_no_transitive_storage' packages/graph-io/tests/test_derived_edges.py` exits 0
    - `grep -F 'def test_testsuite_domain_emitted' packages/graph-io/tests/test_derived_edges.py` exits 0
    - `grep -F 'def test_testsuite_no_domain_on_multi_domain_span' packages/graph-io/tests/test_derived_edges.py` exits 0
    - `grep -F 'def test_testsuite_no_domain_for_single_package_suite' packages/graph-io/tests/test_derived_edges.py` exits 0
    - `grep -F 'def test_update_run_end_to_end' packages/graph-io/tests/test_derived_edges.py` exits 0
    - `uv run --package graph-io pytest packages/graph-io/tests/test_derived_edges.py -q` exits 0 with all tests passing
    - `uv run --package graph-io pytest packages/graph-io/tests/ -q` exits 0 (full suite green; no Phase 28/29/30 regressions)
  </acceptance_criteria>
  <done>
    derived_edges.compute has direct unit coverage for every DERIVED-01..04
    requirement. The end-to-end test exercises the full update.run pipeline
    on the sample_monorepo fixture and asserts SC#3 idempotency at the
    orchestration level.
  </done>
</task>

</tasks>

<verification>
The full graph-io test suite must remain green after all five tasks complete:

  cd /Users/pat/Personal/agent-research && uv run --package graph-io pytest packages/graph-io/tests/ -q

Net new tests: at least 8 in test_derived_edges.py. Phase 28/29/30 and
Phase 31 plans 31-01..31-03 tests must all remain green.
</verification>

<success_criteria>
- [ ] `packages/graph-io/src/graph_io/derived_edges.py` exists with `compute`, `_compute_references_and_depends_on`, `_compute_testsuite_domain` (D-07..D-18)
- [ ] `update.py` calls `domains.emit` and `derived_edges.compute` in the locked D-16 order
- [ ] `compute()` is a single transaction with delete-then-recompute pattern (D-17)
- [ ] Self-loops are never emitted in `depends_on` (D-09)
- [ ] Intra-domain references are never emitted (D-08)
- [ ] No transitive storage — only direct memberships participate (DERIVED-04)
- [ ] `tests/test_derived_edges.py` has >= 8 tests including the full end-to-end run on `sample_monorepo`
- [ ] Full graph-io test suite remains green
- [ ] SC#3 idempotency holds at the orchestration level (`cg update` twice → unchanged edges)
</success_criteria>
