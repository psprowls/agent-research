---
phase: 07-cost-frontier-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cores/eval-harness/tests/test_role_sweep.py
  - cores/eval-harness/tests/test_two_gate_scorer.py
  - cores/eval-harness/tests/test_report_role_doc.py
  - cores/eval-harness/tests/test_preflight_estimator.py
  - cores/eval-harness/tests/test_recommendation_block.py
  - cores/eval-harness/tests/eval/test_sweep_dry_run.py
autonomous: true
requirements: [SWEEP-01, SWEEP-02, SWEEP-03, SWEEP-04]
tags: [eval-harness, test-scaffolding, wave-0]

must_haves:
  truths:
    - "Test files exist for run_role_sweep dispatch, two-gate scoring, render_role_doc, pre-flight estimator, recommendation block, dry-run integration"
    - "All new test files are marked-skip or xfail until their implementation lands in later waves"
    - "Existing flat test layout (cores/eval-harness/tests/test_*.py) is honored — no new subdirectories beyond the existing tests/eval/ directory"
  artifacts:
    - path: "cores/eval-harness/tests/test_role_sweep.py"
      provides: "Scaffolded unit tests for run_role_sweep (SWEEP-01)"
      contains: "def test_single_role_swap_librarian"
    - path: "cores/eval-harness/tests/test_two_gate_scorer.py"
      provides: "Scaffolded unit tests for D-07 two-gate scoring"
      contains: "def test_two_gate_librarian"
    - path: "cores/eval-harness/tests/test_report_role_doc.py"
      provides: "Scaffolded unit tests for render_role_doc (SWEEP-03)"
      contains: "def test_render_role_doc_contains_required_sections"
    - path: "cores/eval-harness/tests/test_preflight_estimator.py"
      provides: "Scaffolded unit tests for pre-flight cost estimator (D-13)"
      contains: "def test_estimate_24_cell_sweep_within_cap"
    - path: "cores/eval-harness/tests/test_recommendation_block.py"
      provides: "Scaffolded unit tests for models.toml recommendation block emitter (SWEEP-04)"
      contains: "def test_recommendation_block_includes_previous_default"
    - path: "cores/eval-harness/tests/eval/test_sweep_dry_run.py"
      provides: "Scaffolded integration test for --dry-run end-to-end with mock LLM (SWEEP-03)"
      contains: "def test_dry_run_writes_all_role_docs"
  key_links:
    - from: "cores/eval-harness/tests/eval/test_sweep_dry_run.py"
      to: "cores/eval-harness/tests/conftest.py"
      via: "EVAL_GATE skipif marker import"
      pattern: "from conftest import EVAL_GATE"
    - from: "all new test_*.py files"
      to: "pytest.skip / pytest.mark.xfail"
      via: "module-level skip until implementation arrives"
      pattern: "pytest.skip\\(.*not implemented"
---

<objective>
Create Wave 0 test scaffolds for every behavior Phase 7 introduces. Each file declares the imports, fixtures, and test signatures (with module-level `pytest.skip("Pending Plan 07-NN")`) so downstream plans have an exact target to flip green.

Purpose: Nyquist gate compliance — every later task's `<verify>` line will point at one of these tests; without scaffolds the verify commands would be MISSING.

Output: Six test files, all importable, all currently skipped, all wired to the existing `cores/eval-harness/tests/conftest.py` and `eval_helpers.py` patterns.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-cost-frontier-sweep/07-CONTEXT.md
@.planning/phases/07-cost-frontier-sweep/07-RESEARCH.md
@.planning/phases/07-cost-frontier-sweep/07-PATTERNS.md
@.planning/phases/07-cost-frontier-sweep/07-VALIDATION.md
@cores/eval-harness/tests/conftest.py
@cores/eval-harness/tests/eval_helpers.py
@cores/eval-harness/tests/test_sweep.py
@cores/eval-harness/tests/test_divergence_metric.py
@cores/eval-harness/tests/test_report.py
@cores/eval-harness/tests/test_pricing.py
@cores/eval-harness/tests/eval/test_sweep_eval.py

<interfaces>
<!-- Existing test layout uses flat tests/test_*.py + a tests/eval/ subdirectory for live-Bedrock tests. New scaffolds follow this convention -->
<!-- conftest.py provides EVAL_GATE = pytest.mark.skipif(...) — import via `from conftest import EVAL_GATE` -->
<!-- test_sweep.py demonstrates the AsyncMock/patch style for sweep tests -->
<!-- test_divergence_metric.py demonstrates DivergenceMetric construction with AgentOutputProxy -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold unit-test files for sweep runner, two-gate, report, estimator, recommendation block</name>
  <read_first>
    - cores/eval-harness/tests/test_sweep.py (analog for test_role_sweep.py — copy AsyncMock + patch style)
    - cores/eval-harness/tests/test_divergence_metric.py (analog for test_two_gate_scorer.py — DivergenceMetric + AgentOutputProxy construction)
    - cores/eval-harness/tests/test_report.py (analog for test_report_role_doc.py)
    - cores/eval-harness/tests/test_pricing.py (analog for test_preflight_estimator.py)
    - cores/eval-harness/tests/conftest.py (EVAL_GATE definition + fixture_vault_path fixture)
    - cores/eval-harness/tests/eval_helpers.py (shared eval helpers)
    - .planning/phases/07-cost-frontier-sweep/07-PATTERNS.md sections "test_role_sweep.py (new)", "test_two_gate_scorer.py (new)", "test_report_role_doc.py (new)", "test_preflight_estimator.py (new)"
    - .planning/phases/07-cost-frontier-sweep/07-RESEARCH.md §"Phase Requirements -> Test Map"
  </read_first>
  <behavior>
    - test_role_sweep.py exports: test_role_sweep_calls_dispatch_map, test_single_role_swap_librarian (asserts captured kwargs role_model_overrides={"librarian": candidate}), test_single_role_swap_scanner (asserts run_scan called with model_override=candidate), test_sweep_candidates_read_from_models_toml. Module-level pytest.skip("Pending Plan 07-05") guards the file.
    - test_two_gate_scorer.py exports: test_two_gate_librarian_pass, test_two_gate_librarian_divergence_fail (gate 1 fails), test_two_gate_librarian_quality_fail (gate 2 fails), test_synthesizer_uses_end_to_end_only (no gate 1 call). Module-level pytest.skip("Pending Plan 07-05") guards the file.
    - test_report_role_doc.py exports: test_render_role_doc_contains_required_sections (assert "Pareto frontier", "Previous default", role name, raw scores table headings), test_pareto_frontier_filters_dominated_points. Module-level pytest.skip("Pending Plan 07-06") guards the file.
    - test_preflight_estimator.py exports: test_estimate_24_cell_sweep_within_cap (estimate < 25.0), test_estimator_returns_zero_for_empty_candidates, test_estimator_skips_unknown_model_ids. Module-level pytest.skip("Pending Plan 07-04") guards the file.
    - test_recommendation_block.py exports: test_recommendation_block_includes_previous_default, test_recommendation_block_lists_pareto_members_only, test_recommendation_block_uses_run_date. Module-level pytest.skip("Pending Plan 07-06") guards the file.
  </behavior>
  <action>
    Create the five unit-test files under cores/eval-harness/tests/. Each file MUST: (1) include a module docstring summarizing what it covers and the requirement IDs (SWEEP-01..04, D-07, D-11, D-13); (2) declare `pytestmark = pytest.mark.skip(reason="Pending Plan 07-NN")` at module scope with the exact plan ID it depends on; (3) include `from __future__ import annotations` and copy import style from the analog file named in read_first; (4) define each test signature listed in <behavior> with an empty body that raises NotImplementedError or contains a single `assert False, "TODO Plan 07-NN"`; (5) define a `_make_sweep_result(...)` or `_make_query_result(...)` helper consistent with test_sweep.py lines 24-46. Do NOT implement any test logic — implementation arrives with the production code in later plans. NEVER place fenced code blocks inside this action; refer to the read_first source files for exact patterns.
  </action>
  <verify>
    <automated>uv run --package eval-harness pytest cores/eval-harness/tests/test_role_sweep.py cores/eval-harness/tests/test_two_gate_scorer.py cores/eval-harness/tests/test_report_role_doc.py cores/eval-harness/tests/test_preflight_estimator.py cores/eval-harness/tests/test_recommendation_block.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - All five files import cleanly (pytest --collect-only does not raise ImportError)
    - Each file shows N tests collected and N skipped at module level
    - grep -c "pytestmark = pytest.mark.skip" cores/eval-harness/tests/test_role_sweep.py returns 1
    - grep -c "pytestmark = pytest.mark.skip" cores/eval-harness/tests/test_two_gate_scorer.py returns 1
    - grep -c "pytestmark = pytest.mark.skip" cores/eval-harness/tests/test_report_role_doc.py returns 1
    - grep -c "pytestmark = pytest.mark.skip" cores/eval-harness/tests/test_preflight_estimator.py returns 1
    - grep -c "pytestmark = pytest.mark.skip" cores/eval-harness/tests/test_recommendation_block.py returns 1
    - grep -c "def test_single_role_swap_librarian" cores/eval-harness/tests/test_role_sweep.py returns 1
    - grep -c "def test_two_gate_librarian_divergence_fail" cores/eval-harness/tests/test_two_gate_scorer.py returns 1
  </acceptance_criteria>
  <done>Five unit-test files exist, import cleanly, are uniformly module-level-skipped with pointers to the plans that will unlock them, and define every test signature the later plans must turn green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Scaffold dry-run integration test under tests/eval/</name>
  <read_first>
    - cores/eval-harness/tests/eval/test_sweep_eval.py (analog — path resolution pattern, two-phase pytest-evals structure, EVAL_GATE usage)
    - cores/eval-harness/tests/conftest.py
    - cores/eval-harness/tests/eval_helpers.py
    - .planning/phases/07-cost-frontier-sweep/07-PATTERNS.md §"test_sweep_dry_run.py (new)"
  </read_first>
  <behavior>
    - test_sweep_dry_run.py exports: test_dry_run_writes_all_role_docs (asserts .planning/sweep/{role}.md exists for each of the 6 roles + INDEX.md, asserts "Pareto frontier" appears in each role doc, total spend is $0), test_dry_run_pre_flight_estimator_prints_estimate (captures stdout), test_dry_run_skips_bed01_when_no_aws (mocked path).
    - Tests use the mock_llm monkeypatch fixture pattern from conftest.py (no real Bedrock calls).
    - Module-level pytest.skip("Pending Plan 07-06") guards the file initially.
    - `pytestmark = [pytest.mark.eval]` is also declared so the test only runs under `--run-eval` once the skip is lifted (mirrors test_sweep_eval.py).
  </behavior>
  <action>
    Create cores/eval-harness/tests/eval/test_sweep_dry_run.py mirroring the path-resolution and module-level pytest.skip pattern in test_sweep_eval.py lines 38-109. Compute _WORKSPACE_ROOT with the correct parent count for tests/eval/ depth, declare CASES_PATH and FIXTURE_VAULT module-level constants, declare `pytestmark = [pytest.mark.eval, pytest.mark.skip(reason="Pending Plan 07-06")]`, and define the three test signatures from <behavior> with empty bodies that raise NotImplementedError. Do NOT implement logic. Do NOT include fenced code blocks here; refer to the read_first analog file for exact shapes.
  </action>
  <verify>
    <automated>uv run --package eval-harness pytest cores/eval-harness/tests/eval/test_sweep_dry_run.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - File imports without ImportError (--collect-only succeeds)
    - grep -c "def test_dry_run_writes_all_role_docs" cores/eval-harness/tests/eval/test_sweep_dry_run.py returns 1
    - grep -c "pytestmark = " cores/eval-harness/tests/eval/test_sweep_dry_run.py returns 1
    - grep -c "pytest.mark.eval" cores/eval-harness/tests/eval/test_sweep_dry_run.py returns at least 1
    - grep -v '^#' cores/eval-harness/tests/eval/test_sweep_dry_run.py | grep -c "Pending Plan 07-06" returns at least 1
  </acceptance_criteria>
  <done>Dry-run integration test scaffolded with the eval mark + module skip; ready for Plan 07-06 to unlock.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test scaffolds → live eval-harness modules | New tests import production modules that may not yet exist; resolved by module-level skip until target plan lands |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | new test files | accept | scaffolds contain no executable test logic; module-level skip prevents accidental green/red signal |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan; n/a |
</threat_model>

<verification>
After both tasks complete:
- `uv run --package eval-harness pytest cores/eval-harness/tests/ --collect-only -q` reports all six new test files collected
- `uv run --package eval-harness pytest -q -m "not eval"` runs the quick suite unaffected (all new files module-skip)
</verification>

<success_criteria>
Six test files exist; all currently skipped; each will be turned green by an identified later plan (07-04, 07-05, 07-06). No production code touched.
</success_criteria>

<output>
Create `.planning/phases/07-cost-frontier-sweep/07-01-SUMMARY.md` when done with: files created, collect-only counts, the plan-to-test mapping table.
</output>
