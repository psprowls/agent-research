---
phase: 07-cost-frontier-sweep
plan: 07
type: execute
wave: 6
depends_on: ["07-05", "07-06"]
files_modified:
  - cores/eval-harness/src/eval_harness/sweep.py
  - cores/eval-harness/tests/eval/test_sweep_eval.py
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/sweep/librarian.md
  - .planning/sweep/synthesizer.md
  - .planning/sweep/code_reader.md
  - .planning/sweep/scanner.md
  - .planning/sweep/linter.md
  - .planning/sweep/ingestor.md
  - .planning/sweep/INDEX.md
  - .planning/sweep/STORY.md
  - cores/model-adapter/src/model_adapter/models.toml
autonomous: false
requirements: [SWEEP-01, SWEEP-02, SWEEP-03, SWEEP-04, SWEEP-05]
tags: [live-bedrock, matrix-run, docs-correction, human-swap, cost-story]

must_haves:
  truths:
    - "REQUIREMENTS.md SWEEP-01 wording corrected from '7 roles' to 'all 6 agent roles in models.toml' (D-02)"
    - "ROADMAP.md Phase 7 success criterion 1 corrected from '7 roles' to 'all 6 agent roles in models.toml' (D-02)"
    - "run_full_matrix() driver function exists, composes preflight_check + run_role_sweep per role per candidate + score_two_gate + write per-role docs + INDEX.md (SWEEP-01, SWEEP-03)"
    - "Live-Bedrock eval test exercises the full 24-cell matrix (6 in-scope roles × 4 candidates per role, D-04) end-to-end"
    - "Live-Bedrock eval test asserts BED-01 pre-flight succeeds and writes all 6 per-role docs with non-empty raw scores tables (SWEEP-02, SWEEP-03)"
    - "Per-role docs at .planning/sweep/{role}.md (6 files) + .planning/sweep/INDEX.md are committed with raw scores from the live run (SWEEP-03)"
    - "Recommendation comment blocks pasted into cores/model-adapter/src/model_adapter/models.toml under each in-scope role with '# Previous default: <model_id>' provenance line (SWEEP-04)"
    - "If frontier picks differ from current defaults, model_id values updated by the user via the blocking human checkpoint; otherwise existing defaults retained with the recommendation block as evidence (SWEEP-04)"
    - ".planning/sweep/STORY.md exists and tells the v1.0 cost story: pre-port baseline cost vs post-port frontier cost vs total spend, with per-role highlights (SWEEP-05)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "SWEEP-01 wording corrected to 6 agent roles"
      contains: "all 6 agent roles in models.toml"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 7 success criterion 1 corrected"
      contains: "all 6 agent roles in models.toml"
    - path: "cores/eval-harness/src/eval_harness/sweep.py"
      provides: "run_full_matrix(role_candidates, vault_path, cases_paths, repeats, output_dir) driver"
      contains: "async def run_full_matrix"
    - path: ".planning/sweep/INDEX.md"
      provides: "Links to 6 role docs + STORY.md + run metadata"
      contains: "STORY.md"
    - path: ".planning/sweep/STORY.md"
      provides: "Cost-story narrative summarizing the v1.0 cost promise validation (SWEEP-05)"
      contains: "Cost Story"
  key_links:
    - from: "cores/model-adapter/src/model_adapter/models.toml [roles.*]"
      to: "Sweep recommendation comment block + Previous default: <id>"
      via: "manual paste from per-role doc"
      pattern: "# Sweep candidates \\(run"
    - from: ".planning/sweep/INDEX.md"
      to: "STORY.md + 6 role docs"
      via: "markdown links"
      pattern: "STORY\\.md|librarian\\.md|ingestor\\.md|linter\\.md|scanner\\.md|synthesizer\\.md|code_reader\\.md"
---

<objective>
Execute the live 24-cell sweep, write per-role frontier docs, paste recommendation blocks into models.toml, optionally update default model_ids via a blocking human checkpoint, and author the cost-story doc. Also correct the "7 roles" misnomer in REQUIREMENTS.md and ROADMAP.md (D-02).

Purpose: This is where SWEEP-01..05 actually land. Plans 07-01..06 built the machinery; this plan turns the crank, captures the human-in-the-loop swap, and produces the artifact set the milestone is graded against.

Output: Updated REQUIREMENTS.md + ROADMAP.md, run_full_matrix() driver in sweep.py, 6 committed per-role docs + INDEX.md + STORY.md, models.toml with pasted recommendation blocks (and possibly updated model_ids per the human pick).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-cost-frontier-sweep/07-CONTEXT.md
@.planning/phases/07-cost-frontier-sweep/07-RESEARCH.md
@cores/eval-harness/src/eval_harness/sweep.py
@cores/eval-harness/src/eval_harness/preflight.py
@cores/eval-harness/src/eval_harness/two_gate.py
@cores/eval-harness/src/eval_harness/report.py
@cores/eval-harness/tests/eval/test_sweep_eval.py
@cores/model-adapter/src/model_adapter/models.toml
@eval/cases/query_cases.json
@eval/cases/code_reader_cases.json

<interfaces>
<!-- D-02 correction targets: -->
<!--   REQUIREMENTS.md line 37 (SWEEP-01): "all 7 roles" -> "all 6 agent roles in models.toml (librarian, code_reader, scanner, linter, ingestor, synthesizer)" -->
<!--   ROADMAP.md line 75 (Phase 7 success criterion 1): "all 7 roles" -> "all 6 agent roles in models.toml" -->
<!-- run_full_matrix driver shape: -->
<!--   async def run_full_matrix(role_candidates, vault_path, query_cases_path, code_reader_cases_path, ingestor_source_path, repeats, output_dir, *, dry_run, threshold_quality, threshold_other, skip_bed01, auto_confirm) -->
<!--   - calls preflight_check(role_candidates, n_cases, repeats, skip_bed01, auto_confirm) -->
<!--   - for each role in role_candidates: for each candidate: call run_role_sweep -> collect SweepResults -->
<!--   - call score_two_gate per candidate using the right baseline + threshold -->
<!--   - render per-role doc, render recommendation block, render index -->
<!--   - print recommendation blocks to stdout (the human pastes into models.toml at the checkpoint) -->
<!-- Discretion (set here): threshold_quality=0.95, threshold_other=0.90 (from research §Open Question 1 + CONTEXT §"Claude's Discretion") -->
<!-- code_reader special case (research §Open Question 4): use eval/cases/code_reader_cases.json; accept fallback-fires-disclaimer outcome (D-08 end-to-end-only scoring tolerates panel_mean=None, marking quality "n/a" but still reporting cost) -->
<!-- ingestor sweep needs a fixture source file path — pick a stable file under cores/vault-io/tests/fixtures/ or eval/fixtures/ source; planner picks at task time -->
<!-- live test file extends cores/eval-harness/tests/eval/test_sweep_eval.py with a new @pytest.mark.eval test_full_matrix_live (gated by CODE_WIKI_RUN_EVAL=1) -->
<!-- The matrix-run cost envelope (research §Tension 2): ~$6.19 total at 4 cases × 3 repeats × 24 cells with judges enabled; HARD_CAP_USD=$25 leaves 4x headroom -->
<!-- BED-01 verification folds in via preflight_check (Plan 07-04) — SWEEP-02 is satisfied as a side effect of running the matrix -->
<!-- Manual swap policy (D-10): the executor MUST NOT auto-write model_id changes. The human checkpoint presents the frontier and asks the user to apply the swap. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct '7 roles' wording in REQUIREMENTS.md and ROADMAP.md (D-02)</name>
  <read_first>
    - .planning/REQUIREMENTS.md (SWEEP-01 line)
    - .planning/ROADMAP.md (Phase 7 success criterion 1)
    - .planning/phases/07-cost-frontier-sweep/07-CONTEXT.md §D-02
    - .planning/phases/07-cost-frontier-sweep/07-RESEARCH.md §Tension 1
  </read_first>
  <action>
    Update .planning/REQUIREMENTS.md SWEEP-01 bullet by replacing "all 7 roles in `models.toml`" with "all 6 agent roles in `models.toml` (`librarian`, `code_reader`, `scanner`, `linter`, `ingestor`, `synthesizer`)". Add a trailing parenthetical note: "(corrected 2026-MM-DD per Phase 7 D-02: the v1.1 roadmap said 7 but models.toml has 6 in-scope agent roles plus 2 judges that are out of scope for this sweep)". Update .planning/ROADMAP.md Phase 7 success criterion 1 by replacing "all 7 roles on live Bedrock" with "all 6 agent roles in models.toml on live Bedrock". ALSO update the Phase 7 **Goal** line in ROADMAP.md (the bullet beginning "**Goal**:" under the `### Phase 7: Cost-Frontier Sweep` heading) by replacing "all 7 Bedrock roles" with "all 6 in-scope agent roles in models.toml" so the Goal text agrees with the corrected success criterion. Do NOT change any other line. Preserve the rest of the documents byte-for-byte.
  </action>
  <verify>
    <automated>grep -c "all 6 agent roles in" .planning/REQUIREMENTS.md && grep -c "all 6 agent roles in" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "all 6 agent roles in" .planning/REQUIREMENTS.md returns at least 1
    - grep -c "all 6 agent roles in" .planning/ROADMAP.md returns at least 1
    - grep -v '^#' .planning/REQUIREMENTS.md | grep -c "all 7 roles" returns 0
    - grep -v '^#' .planning/ROADMAP.md | grep -c "all 7 roles" returns 0
    - grep -v '^#' .planning/ROADMAP.md | grep -c "all 7 Bedrock roles" returns 0
    - grep -v '^#' .planning/ROADMAP.md | grep -c "all 7" returns 0
    - grep -c "D-02" .planning/REQUIREMENTS.md returns at least 1
  </acceptance_criteria>
  <done>D-02 correction landed; "7 roles" wording is gone from both source documents; provenance pointer to D-02 included.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add run_full_matrix driver + live-Bedrock test that writes the 6 per-role docs</name>
  <read_first>
    - cores/eval-harness/src/eval_harness/sweep.py (existing run_sweep + run_role_sweep)
    - cores/eval-harness/src/eval_harness/preflight.py
    - cores/eval-harness/src/eval_harness/two_gate.py
    - cores/eval-harness/src/eval_harness/report.py (render_role_doc, render_recommendation_block, render_index_md)
    - cores/eval-harness/tests/eval/test_sweep_eval.py (analog — module-level eval mark, EVAL_GATE usage, path resolution)
    - .planning/phases/07-cost-frontier-sweep/07-PATTERNS.md §"sweep.py (extend — add run_role_sweep)" (driver shape)
  </read_first>
  <behavior>
    - `async def run_full_matrix(role_candidates, vault_path, query_cases_path, code_reader_cases_path, ingestor_source_path, repeats, output_dir, *, dry_run=False, threshold_quality=0.95, threshold_other=0.90, skip_bed01=False, auto_confirm=False) -> dict[str, list[SweepResult]]`:
      - Counts total cases (query_cases for librarian/synthesizer/scanner/linter/ingestor; code_reader_cases for code_reader). Calls preflight_check.
      - For each role in role_candidates: for each candidate: calls run_role_sweep(role, candidate, cases_path_for_role, vault_path, repeats=repeats).
      - For each role: computes the current-default panel_mean from the candidate equal to that role's model_id; for every candidate: calls score_two_gate(role, ..., panel_mean=cand_mean, default_panel_mean=default_mean, threshold=threshold_for_role).
      - Selects threshold per role tier (quality vs other) via the discretion defaults.
      - Renders per-role docs via render_role_doc, writes to output_dir/{role}.md.
      - Renders INDEX.md with links to all role docs and STORY.md placeholder.
      - Prints each role's recommendation block to stdout (the human will paste these into models.toml at the checkpoint).
      - Returns a dict mapping role -> list[SweepResult] for downstream use.
    - Two-phase pytest-evals harness contract: add test_full_matrix_live as `@pytest.mark.eval` with EVAL_GATE skipif; loads role_candidates from models.toml; calls run_full_matrix into a tmp output dir under the workspace .planning/sweep/ (the dir written to is a tmp_path in the test; the production driver writes to .planning/sweep/ directly when invoked via the gsd executor in the next task).
    - test_full_matrix_live asserts: preflight_check did NOT raise; each role's doc exists in tmp output dir; each role's doc contains "Pareto frontier" and "Run Metadata"; INDEX.md links all 6 role docs; total Bedrock cost (from SweepResult.cost_usd sums) is positive and below HARD_CAP_USD.
  </behavior>
  <action>
    Note: the `<verify>` step for this task incurs ~$6-7 of real AWS Bedrock spend (the test calls run_full_matrix with dry_run=False against the live 24-cell matrix). Confirm AWS credentials are active and the pre-flight estimator has been exercised in dry-run before executing this verify command. The matrix cost is gated by HARD_CAP_USD=$25 and the preflight_check spend-confirmation prompt (auto_confirm=True in the test so it does not block on input — the cap remains in force).

    Add async def run_full_matrix to cores/eval-harness/src/eval_harness/sweep.py below run_role_sweep. Compose the existing primitives — do NOT reimplement subprocess logic or repeat the EvalWorktree dance (delegate to run_role_sweep). For the per-role threshold, use a small inline tier map: librarian, synthesizer => threshold_quality; scanner, code_reader, linter, ingestor => threshold_other. Then extend cores/eval-harness/tests/eval/test_sweep_eval.py: append `async def test_full_matrix_live(tmp_path)` with `pytestmark = [pytest.mark.eval]` at module level (already present in this file per the existing eval gate). Use EVAL_GATE skipif. Inside the test, call run_full_matrix with dry_run=False, skip_bed01=False, auto_confirm=True so the matrix exercises BED-01 + spend gate but does not block on input(). Cases come from existing eval/cases/query_cases.json + eval/cases/code_reader_cases.json. For ingestor_source_path, pick a stable file (planner picks one — suggested cores/vault-io/tests/fixtures/round-trip-vault/README.md or any markdown file already in the fixture vault). Do NOT include fenced code in this action; rely on read_first analogs.
  </action>
  <verify>
    <automated>CODE_WIKI_RUN_EVAL=1 CODE_WIKI_RUN_JUDGES=1 uv run --package eval-harness pytest cores/eval-harness/tests/eval/test_sweep_eval.py::test_full_matrix_live --run-eval -x</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "async def run_full_matrix" cores/eval-harness/src/eval_harness/sweep.py returns 1
    - grep -c "def test_full_matrix_live" cores/eval-harness/tests/eval/test_sweep_eval.py returns 1
    - test_full_matrix_live exits 0 under CODE_WIKI_RUN_EVAL=1 and CODE_WIKI_RUN_JUDGES=1
    - tmp_path/.../librarian.md, synthesizer.md, code_reader.md, scanner.md, linter.md, ingestor.md, INDEX.md all exist after test
    - Each role doc contains the substring "Pareto frontier" (test assertion)
    - Total cost from the test run is below HARD_CAP_USD (asserted in the test)
    - BED-01 confirmation line "[BED-01] Bedrock connectivity confirmed." appears in captured stdout (capsys assertion)
    - grep -c "code_reader_cases_path" cores/eval-harness/src/eval_harness/sweep.py returns at least 1 (confirms run_full_matrix accepts and routes the code_reader-specific cases file per D-09)
    - run_full_matrix routes the `code_reader` role through `code_reader_cases_path` (NOT `query_cases_path`); test asserts every SweepResult for role="code_reader" has `case_id` in {"code-reader-01","code-reader-02","code-reader-03"} (the IDs defined in eval/cases/code_reader_cases.json)
    - test_full_matrix_live imports `score_two_gate` from `eval_harness.two_gate` and asserts the call signature contract is honored inside `run_full_matrix` (test patches score_two_gate with a wrapper that captures kwargs; asserts kwargs include role, panel_mean, default_panel_mean, threshold)
    - grep -c "from eval_harness.two_gate import score_two_gate" cores/eval-harness/src/eval_harness/sweep.py returns at least 1
  </acceptance_criteria>
  <done>run_full_matrix is implemented and proven against live Bedrock; the eval test writes all 6 per-role docs to a tmp dir and asserts presence. SWEEP-01, SWEEP-02, SWEEP-03 mechanics all proven in passing.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Run the live matrix into .planning/sweep/ and review the frontier</name>
  <what-built>
    The full 24-cell sweep has been driven by run_full_matrix() in a passing test against tmp dirs. This checkpoint produces the COMMITTED .planning/sweep/ artifacts by re-running the same driver — this time writing into the real .planning/sweep/ directory.
  </what-built>
  <how-to-verify>
    1. Confirm AWS credentials are active in the current shell: `aws sts get-caller-identity` should return the expected account.
    2. From the repo root, execute the production sweep (this consumes real Bedrock spend; estimated ~$6.19 total):

       `CODE_WIKI_RUN_EVAL=1 CODE_WIKI_RUN_JUDGES=1 uv run --package eval-harness python -c "import asyncio; from pathlib import Path; from eval_harness.sweep import run_full_matrix; from model_adapter.loader import load_role_config; roles=['librarian','synthesizer','code_reader','scanner','linter','ingestor']; rc={r: load_role_config(r)['sweep_candidates'] for r in roles}; asyncio.run(run_full_matrix(rc, vault_path=Path('cores/vault-io/tests/fixtures/round-trip-vault'), query_cases_path=Path('eval/cases/query_cases.json'), code_reader_cases_path=Path('eval/cases/code_reader_cases.json'), ingestor_source_path=Path('cores/vault-io/tests/fixtures/round-trip-vault/README.md'), repeats=3, output_dir=Path('.planning/sweep'), auto_confirm=True))"`

       (Or run the helper script when authored.)

    3. The pre-flight estimator will print "Estimated cost: $X.XX". Confirm this is below $25.
    4. The [BED-01] line should appear: "[BED-01] Bedrock connectivity confirmed."
    5. After the run, verify .planning/sweep/ contains: librarian.md, synthesizer.md, code_reader.md, scanner.md, linter.md, ingestor.md, INDEX.md. Each role doc should have raw scores + Pareto frontier + Recommendation sections.
    6. Open each role doc and read the recommendation block (the "## Recommendation" section). Compare the frontier picks against the current default model_id for that role.
    7. For each role where the frontier suggests a NEW default (cheaper at acceptable quality):
       - Copy the entire `# Sweep candidates (run YYYY-MM-DD): ...` comment block from the role doc.
       - Paste it under the corresponding `[roles.<name>]` block in `cores/model-adapter/src/model_adapter/models.toml`.
       - If you want to apply the swap, change `model_id = "<old>"` to `model_id = "<new>"` and ensure the comment line `# Previous default: <old>` is present (per D-11). If you choose to keep the current default, paste the recommendation block anyway — SWEEP-04 is about evidence + provenance, not a forced swap.
    8. Run `git diff cores/model-adapter/src/model_adapter/models.toml` and verify the comment blocks landed cleanly.
    9. Run `uv run --package code-wiki-agent pytest agents/code-wiki-agent/tests/ -k "command_overrides or query or scan or lint or ingest" -x` to confirm no regression from any default swap.
  </how-to-verify>
  <resume-signal>
    Type "approved" once .planning/sweep/ contains the 6 role docs + INDEX.md AND models.toml has recommendation blocks under each in-scope role (and optional model_id updates). If the matrix run failed, describe the failure and we'll diagnose before proceeding to STORY.md.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 4: Author .planning/sweep/STORY.md (SWEEP-05)</name>
  <read_first>
    - .planning/PROJECT.md (Core Value — the cost story v1.0 promised)
    - .planning/sweep/INDEX.md (post-matrix, from Task 3)
    - .planning/sweep/librarian.md (post-matrix, post-checkpoint — read all 6 role docs)
    - .planning/sweep/synthesizer.md
    - .planning/sweep/code_reader.md
    - .planning/sweep/scanner.md
    - .planning/sweep/linter.md
    - .planning/sweep/ingestor.md
    - cores/model-adapter/src/model_adapter/models.toml (post-checkpoint — read the pasted recommendation blocks and any model_id updates)
    - .planning/phases/07-cost-frontier-sweep/07-CONTEXT.md §D-12 (cost-story doc placement)
  </read_first>
  <behavior>
    - .planning/sweep/STORY.md has these sections:
      - `# Cost Story — Phase 7 Sweep (run YYYY-MM-DD)` heading.
      - `## What v1.0 Promised`: a one-paragraph recap of PROJECT.md's Core Value (Bedrock cost savings vs lattice-wiki on Claude Code).
      - `## What v1.1 Measured`: a per-role table showing pre-sweep default model + cost, frontier pick + cost, % savings if swapped.
      - `## Highlights`: 2-4 bullets pulling the most consequential findings (e.g., "Switching scanner from haiku to nova-micro saves 60x cost with no quality regression"; "Librarian remains best at haiku — sonnet not justified by judge panel mean").
      - `## Total Spend This Run`: the actual matrix cost from the live run, with judges enabled.
      - `## Decisions`: which defaults were swapped (with old → new model_id) and which were retained, each with a one-line rationale.
      - `## Caveats`: code_reader fixture caveat (Open Question 4 — fallback fires but quality is N/A; cost data still meaningful), any qwen3-32b access issues observed, judge panel notes.
      - `## Next Steps`: pointer to per-role docs for raw numbers, pointer to Phase 8 if relevant.
  </behavior>
  <action>
    Create .planning/sweep/STORY.md. Draft each section per the bullet list in <behavior>. Pull actual numbers from the per-role docs and from the recommendation blocks in models.toml — do NOT invent figures. Where a number is unavailable (e.g., code_reader quality), write "n/a" and reference the caveat. Keep total length to roughly 200-400 lines (this is a human-facing summary, not exhaustive raw data). Do NOT include fenced code blocks in this action; the doc itself will contain markdown tables. Add a Markdown link to INDEX.md at the top and to each role doc inside the per-role table.
  </action>
  <verify>
    <automated>grep -c "^# Cost Story" .planning/sweep/STORY.md && grep -c "What v1.0 Promised" .planning/sweep/STORY.md && grep -c "What v1.1 Measured" .planning/sweep/STORY.md && grep -c "Total Spend This Run" .planning/sweep/STORY.md</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "^# Cost Story" .planning/sweep/STORY.md returns 1
    - grep -c "What v1.0 Promised" .planning/sweep/STORY.md returns at least 1
    - grep -c "What v1.1 Measured" .planning/sweep/STORY.md returns at least 1
    - grep -c "Total Spend This Run" .planning/sweep/STORY.md returns at least 1
    - grep -c "Caveats" .planning/sweep/STORY.md returns at least 1
    - grep -c "Decisions" .planning/sweep/STORY.md returns at least 1
    - Each of the 6 role docs is referenced (markdown link or filename) somewhere in STORY.md: grep -c "librarian.md\\|synthesizer.md\\|code_reader.md\\|scanner.md\\|linter.md\\|ingestor.md" .planning/sweep/STORY.md returns at least 6
    - File length is between 50 and 1000 lines: `wc -l .planning/sweep/STORY.md` reports a sensible value
  </acceptance_criteria>
  <done>STORY.md exists and tells the cost story v1.0 promised to validate; pulls real numbers from the role docs + models.toml; documents the code_reader fixture caveat and any qwen3-32b access notes; links to all per-role docs.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Final review — STORY.md narrative + sweep artifacts committed</name>
  <what-built>
    Task 4 produced .planning/sweep/STORY.md. The full sweep deliverable is now: 6 per-role docs + INDEX.md + STORY.md + updated models.toml with recommendation blocks + corrected REQUIREMENTS.md/ROADMAP.md wording.
  </what-built>
  <how-to-verify>
    1. Read .planning/sweep/STORY.md end-to-end. Verify it tells the cost story coherently:
       - Pre-sweep state (what v1.0 promised) is recapped accurately.
       - Per-role measurements match the numbers in the per-role docs.
       - Decisions section matches the model_id changes (if any) in cores/model-adapter/src/model_adapter/models.toml.
       - Total spend matches the BED-01 / matrix run output (within rounding).
    2. Confirm `.planning/sweep/INDEX.md` links to STORY.md.
    3. Confirm `git status` shows all sweep artifacts unstaged or staged (planner does not commit; user commits or invokes /gsd commit after verification).
    4. Run the full quick suite one more time: `uv run --package eval-harness pytest -q -m "not eval"` exits 0.
    5. Confirm REQUIREMENTS.md / ROADMAP.md no longer say "7 roles".
  </how-to-verify>
  <resume-signal>
    Type "approved" to mark the phase artifacts ready for commit, or "revise: <reason>" to send STORY.md back for edits.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live Bedrock calls | run_full_matrix performs real Bedrock invocations across 24 cells |
| human checkpoint → models.toml edits | Human is the only authorized writer of model_id changes (D-10) |
| STORY.md narrative | Hand-authored; risk of fabricated numbers if executor invents figures |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-14 | Denial of Service (spend exhaustion) | live matrix run | mitigate | preflight_check + HARD_CAP_USD=$25 (Plan 07-04) enforced before any Bedrock call |
| T-07-15 | Tampering | models.toml model_id changes | mitigate | Manual swap policy (D-10); blocking human checkpoint required before any model_id mutation; auto-write of model_id forbidden |
| T-07-16 | Information Disclosure | trace JSONL captures user query content | accept | personal-project scope; documented in STORY.md Caveats; no PII in fixture vault |
| T-07-17 | Integrity | STORY.md content | mitigate | Task 4 read_first mandates all 6 role docs + models.toml as inputs; acceptance criteria require links to all per-role docs (numbers must trace) |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan; n/a |
</threat_model>

<verification>
- `grep -c "all 6 agent roles in" .planning/REQUIREMENTS.md` returns >= 1
- `grep -c "all 6 agent roles in" .planning/ROADMAP.md` returns >= 1
- `ls .planning/sweep/{librarian,synthesizer,code_reader,scanner,linter,ingestor}.md .planning/sweep/INDEX.md .planning/sweep/STORY.md` exits 0
- `grep -c "# Sweep candidates (run" cores/model-adapter/src/model_adapter/models.toml` returns >= 6 (one block per in-scope role)
- `grep -c "# Previous default:" cores/model-adapter/src/model_adapter/models.toml` returns >= 6
- `CODE_WIKI_RUN_EVAL=1 CODE_WIKI_RUN_JUDGES=1 uv run --package eval-harness pytest cores/eval-harness/tests/eval/test_sweep_eval.py::test_full_matrix_live --run-eval -x` exits 0
- `uv run --package eval-harness pytest -q -m "not eval"` exits 0 (no regressions)
- Both human checkpoints (Task 3, Task 5) reach "approved"
</verification>

<success_criteria>
SWEEP-01..05 all satisfied: matrix runs across 6 roles with single-role-swap; BED-01 confirmed in passing; per-role frontier docs committed; models.toml carries recommendation blocks with provenance; STORY.md tells the cost story. D-02 wording correction landed.
</success_criteria>

<output>
Create `.planning/phases/07-cost-frontier-sweep/07-07-SUMMARY.md` with: full matrix run cost vs estimate, per-role swap decisions table (old → new), links to all .planning/sweep/ artifacts, and the human-checkpoint resume signals captured for traceability.
</output>
