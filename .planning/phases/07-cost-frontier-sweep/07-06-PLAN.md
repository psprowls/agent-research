---
phase: 07-cost-frontier-sweep
plan: 06
type: execute
wave: 5
depends_on: ["07-04", "07-05"]
files_modified:
  - cores/eval-harness/src/eval_harness/report.py
  - cores/eval-harness/tests/test_report_role_doc.py
  - cores/eval-harness/tests/test_recommendation_block.py
  - cores/eval-harness/tests/eval/test_sweep_dry_run.py
  - .planning/sweep/.gitkeep
autonomous: true
requirements: [SWEEP-03, SWEEP-04]
tags: [reporting, pareto-frontier, recommendation-block, dry-run]

must_haves:
  truths:
    - "pareto_frontier(table) returns the non-dominated subset by (quality_score, cost_usd) — research §report.py extend (D-10)"
    - "render_role_doc(role, tier, candidates, sweep_results, divergence_results, run_date, commit_sha, total_cost_usd, two_gate_outcomes) returns a markdown string containing role+tier+candidates, raw scores table, Pareto frontier callout, recommendation block, and run metadata (D-12 skeleton)"
    - "render_recommendation_block(role, run_date, frontier, current_default) emits the TOML comment block shape locked in D-11 with the '# Previous default:' provenance line (SWEEP-04)"
    - "render_index_md(role_doc_paths, run_date, total_cost_usd) emits .planning/sweep/INDEX.md tying the 6 role docs together"
    - ".planning/sweep/ directory exists in the repo (.gitkeep committed) so downstream writers do not need to create it"
    - "Dry-run integration test asserts all 6 role docs + INDEX.md are written with 'Pareto frontier' content and $0 Bedrock spend"
  artifacts:
    - path: "cores/eval-harness/src/eval_harness/report.py"
      provides: "pareto_frontier, render_role_doc, render_recommendation_block, render_index_md"
      contains: "def pareto_frontier"
    - path: ".planning/sweep/.gitkeep"
      provides: "Directory exists for per-role docs + INDEX.md + STORY.md"
      contains: ""
    - path: "cores/eval-harness/tests/eval/test_sweep_dry_run.py"
      provides: "Passing integration test driving run_role_sweep with mocked LLM through full doc emission"
      contains: "test_dry_run_writes_all_role_docs"
  key_links:
    - from: "render_role_doc"
      to: "pareto_frontier + cost_frontier_table"
      via: "internal composition"
      pattern: "pareto_frontier"
    - from: "render_recommendation_block"
      to: "models.toml TOML comment shape"
      via: "stdout / file emission for manual paste (D-10, D-11)"
      pattern: "# Sweep candidates \\(run"
---

<objective>
Add the reporting layer the sweep produces: Pareto frontier filter, per-role markdown doc renderer, models.toml recommendation comment block, and INDEX.md. Stand up `.planning/sweep/` as a real directory. Flip the dry-run integration test green by composing run_role_sweep with the renderers under mocked Bedrock.

Purpose: SWEEP-03 requires a committed cost-frontier table per role. SWEEP-04 requires the recommendation block shape (D-11) emitted for the human swap. D-12 specifies per-role files + INDEX.md.

Output: Four new exports in report.py, `.planning/sweep/.gitkeep`, three test files moved green (test_report_role_doc.py, test_recommendation_block.py, test_sweep_dry_run.py).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-cost-frontier-sweep/07-CONTEXT.md
@.planning/phases/07-cost-frontier-sweep/07-RESEARCH.md
@.planning/phases/07-cost-frontier-sweep/07-PATTERNS.md
@cores/eval-harness/src/eval_harness/report.py
@cores/eval-harness/src/eval_harness/pricing.py
@cores/eval-harness/src/eval_harness/sweep.py
@cores/eval-harness/src/eval_harness/two_gate.py
@cores/eval-harness/tests/conftest.py

<interfaces>
<!-- Existing report.py: cost_frontier_table(results) -> dict[model_id, {quality_score, cost_usd, n_runs}]; print_frontier(table); regression_check(...) -->
<!-- D-10: Pareto frontier published per role; user picks default manually. No auto-write. -->
<!-- D-11 recommendation block shape (locked example): -->
<!--   # Sweep candidates (run 2026-MM-DD): pareto-frontier members -->
<!--   #   - <model_id>     (cost=$X.XX, quality=0.XX) -->
<!--   #   - <model_id>     (cost=$X.XX, quality=0.XX) -->
<!--   # Previous default: <current_default_model_id> -->
<!-- D-12 per-role doc skeleton: role name + tier + candidates, raw scores table (model, quality_mean, quality_std, cost_per_run, n_cases, divergence_failures vs baseline), Pareto frontier callout, recommendation, run metadata (date, commit SHA, total cost) -->
<!-- pareto_frontier algorithm (07-PATTERNS.md): a point is dominated if another has >= quality AND <= cost AND strictly better on at least one dimension; cost_usd=None entries are never dominated -->
<!-- INDEX.md: links to all 6 role docs + run date + total cost + pointer to STORY.md -->
<!-- Dry-run integration test mocks make_llm via the conftest.py mock_llm fixture; sweep loops complete with $0 Bedrock spend; writes go to a tmp_path-based ".planning/sweep/" -->
<!-- Conservative tier-to-role from D-03 used for tier name lookup inside render_role_doc -->
<!-- Tier-to-role and threshold defaults are imported from eval_harness.preflight (already defines _ROLE_TIER) — re-export or import in report.py to keep one source of truth -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add pareto_frontier, render_role_doc, render_recommendation_block, render_index_md to report.py</name>
  <read_first>
    - cores/eval-harness/src/eval_harness/report.py (existing cost_frontier_table, print_frontier, regression_check, SweepResult quality-score extraction at lines 41-46)
    - cores/eval-harness/src/eval_harness/sweep.py (SweepResult dataclass fields)
    - cores/eval-harness/src/eval_harness/two_gate.py (TwoGateOutcome — needed for render_role_doc input)
    - .planning/phases/07-cost-frontier-sweep/07-PATTERNS.md §"cores/eval-harness/src/eval_harness/report.py (extend — add render_role_doc, pareto_frontier)" including the exact pareto_frontier algorithm and render_recommendation_block shape
    - .planning/phases/07-cost-frontier-sweep/07-CONTEXT.md §"Specific Ideas" (recommendation block locked example and per-role doc skeleton)
  </read_first>
  <behavior>
    - `pareto_frontier(table: dict[str, dict]) -> dict[str, dict]`: returns the non-dominated subset by (quality_score, cost_usd) per the algorithm in 07-PATTERNS.md. cost_usd=None entries are never dominated.
    - `render_role_doc(role, tier, candidates, sweep_results, divergence_results, run_date, commit_sha, total_cost_usd, two_gate_outcomes) -> str`:
      - Heading 1: f"# Sweep: {role} ({tier} tier)"
      - "## Candidates" section listing each of the 4 candidate model_ids.
      - "## Raw Scores" section: markdown table with columns model_id, quality_mean, quality_std (or n/a), cost_per_run_usd, n_cases, divergence_failures (vs baseline; "n/a" for D-08 roles), gate1, gate2, qualified.
      - "## Pareto Frontier" section: list non-dominated points and a one-line callout indicating which model_id is the cheapest on the frontier.
      - "## Recommendation" section: contains the full TOML recommendation-block text (rendered by render_recommendation_block, embedded as a ```toml fenced code block inside the markdown doc — fenced code IS allowed in rendered output, just not in this PLAN's <action> blocks).
      - "## Run Metadata" section: run_date, commit_sha, total_cost_usd, repeats, n_cases.
    - `render_recommendation_block(role, run_date, frontier, current_default) -> str`: produces the exact D-11 comment-block format (each line starts with `#`). Width-pads model_ids to 50 chars per the pattern map example.
    - `render_index_md(role_doc_paths: list[Path] | list[str], run_date: str, total_cost_usd: float, story_path: str | None = None) -> str`: produces a markdown index linking each role doc; if story_path is provided, includes a "See also: cost story" line.
    - All four functions are pure (no filesystem writes) — they return strings. Filesystem writing is the caller's responsibility (Plan 07-07 driver).
  </behavior>
  <action>
    Edit cores/eval-harness/src/eval_harness/report.py to add the four new public functions at the bottom of the file. Implement pareto_frontier per the 07-PATTERNS.md algorithm (O(n^2) non-dominated filter). render_role_doc composes pareto_frontier + cost_frontier_table + render_recommendation_block. render_recommendation_block builds a list[str] of lines and joins with "\n". Use f-strings with `:.4f` for cost and `:.2f` for quality. For tier lookup inside render_role_doc, accept `tier` as a parameter (caller resolves from preflight._ROLE_TIER). Do NOT inline implementations in this action; the read_first patterns map gives the exact shapes.
  </action>
  <verify>
    <automated>uv run --package eval-harness pytest cores/eval-harness/tests/test_report_role_doc.py cores/eval-harness/tests/test_recommendation_block.py -x</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "def pareto_frontier" cores/eval-harness/src/eval_harness/report.py returns 1
    - grep -c "def render_role_doc" cores/eval-harness/src/eval_harness/report.py returns 1
    - grep -c "def render_recommendation_block" cores/eval-harness/src/eval_harness/report.py returns 1
    - grep -c "def render_index_md" cores/eval-harness/src/eval_harness/report.py returns 1
    - grep -v '^#' cores/eval-harness/tests/test_report_role_doc.py | grep -c "pytestmark = pytest.mark.skip" returns 0
    - grep -v '^#' cores/eval-harness/tests/test_recommendation_block.py | grep -c "pytestmark = pytest.mark.skip" returns 0
    - All test_report_role_doc.py tests pass (test_render_role_doc_contains_required_sections, test_pareto_frontier_filters_dominated_points)
    - All test_recommendation_block.py tests pass (test_recommendation_block_includes_previous_default, test_recommendation_block_lists_pareto_members_only, test_recommendation_block_uses_run_date)
  </acceptance_criteria>
  <done>Four new pure-function renderers exposed from report.py; all six per-role-doc / recommendation-block unit tests are green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create .planning/sweep/ directory; turn dry-run integration test green</name>
  <read_first>
    - .planning/phases/07-cost-frontier-sweep/07-CONTEXT.md §D-12 (per-role docs location)
    - cores/eval-harness/tests/eval/test_sweep_dry_run.py (scaffold from Plan 07-01)
    - cores/eval-harness/tests/eval/test_sweep_eval.py (analog — module-level path resolution, EVAL_GATE pattern, mock_llm usage)
    - cores/eval-harness/tests/conftest.py (mock_llm fixture details)
    - cores/eval-harness/src/eval_harness/sweep.py (run_role_sweep + ROLE_COMMAND_MAP from Plan 07-05)
    - cores/eval-harness/src/eval_harness/report.py (after Task 1)
    - .planning/phases/07-cost-frontier-sweep/07-PATTERNS.md §"test_sweep_dry_run.py (new)"
  </read_first>
  <behavior>
    - Create .planning/sweep/.gitkeep as an empty file (filesystem placeholder so the directory is committed). Add a one-line `.planning/sweep/README.md` (optional — planner discretion) noting "Per-role sweep results land here; see INDEX.md after running the matrix."
    - Lift module-level pytest.skip in test_sweep_dry_run.py; keep `pytestmark = [pytest.mark.eval]` so the file still requires `--run-eval`.
    - test_dry_run_writes_all_role_docs: monkey-patches make_llm AND every command's role LLM construction so no real Bedrock call happens (the test uses the existing conftest mock_llm fixture). Then orchestrates a small dry-run loop: for each role in ROLES, for each candidate in sweep_candidates (loaded from models.toml), call run_role_sweep with repeats=1 and n_cases=1, collect results, call render_role_doc → write to tmp_path / ".planning/sweep/" / f"{role}.md", and render_index_md. Asserts all 6 role docs exist, each contains "Pareto frontier", and INDEX.md links all 6. Asserts total cost (computed from cost_for_usage) is positive but spend is $0 (mocked LLM never charges).
    - test_dry_run_pre_flight_estimator_prints_estimate: captures stdout via capsys; calls preflight_check with auto_confirm=True and skip_bed01=True; asserts "Estimated cost" appears in captured output.
    - test_dry_run_skips_bed01_when_no_aws: monkey-patches make_llm.invoke to raise; calls preflight_check with auto_confirm=True and skip_bed01=True; asserts no SystemExit (BED-01 was skipped).
  </behavior>
  <action>
    Run `touch .planning/sweep/.gitkeep` via the Write tool (create an empty file). Optionally create .planning/sweep/README.md with a one-paragraph description (planner discretion — keep brief if added). Then open cores/eval-harness/tests/eval/test_sweep_dry_run.py: remove the module-level `pytest.mark.skip(reason="Pending Plan 07-06")` from the pytestmark list (keep `pytest.mark.eval`). Implement the three test functions in <behavior>. For mocking the agent commands, use `monkeypatch.setattr` on the run_query / run_scan / run_lint / run_ingest_source symbols imported into sweep.py — replace them with AsyncMocks returning fixture objects. Use tmp_path for the output directory. Do NOT include fenced code in this action; refer to test_sweep_eval.py for the module path resolution pattern and to test_sweep.py for the AsyncMock-returns shape.
  </action>
  <verify>
    <automated>CODE_WIKI_RUN_EVAL=1 uv run --package eval-harness pytest cores/eval-harness/tests/eval/test_sweep_dry_run.py --run-eval -x</automated>
  </verify>
  <acceptance_criteria>
    - ls .planning/sweep/.gitkeep exits 0
    - grep -v '^#' cores/eval-harness/tests/eval/test_sweep_dry_run.py | grep -c "Pending Plan 07-06" returns 0
    - grep -c "pytest.mark.eval" cores/eval-harness/tests/eval/test_sweep_dry_run.py returns at least 1
    - All three test functions in test_sweep_dry_run.py pass under `--run-eval`
    - No real Bedrock call happens: `grep -c "ChatBedrockConverse" /tmp/sweep_dry_run_output.log` (if logged) is irrelevant; the test asserts total Bedrock cost = 0 because mocks return fake results
    - INDEX.md is asserted to contain links to all 6 role doc paths (test assertion)
  </acceptance_criteria>
  <done>.planning/sweep/ is committed; dry-run integration test composes run_role_sweep + render_role_doc + render_index_md end-to-end with mocked Bedrock; all 6 role docs + INDEX.md are written into tmp_path and asserted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderers → markdown content | Pure functions producing strings; no untrusted output paths constructed here |
| dry-run test → filesystem | Writes confined to tmp_path; .planning/sweep/.gitkeep is the only committed write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-12 | Tampering | model_id rendering in markdown | mitigate | _sanitize_model_id applied where any model_id becomes a filename component; comment-block text is human-readable only |
| T-07-13 | Information Disclosure | trace JSONL → render_role_doc | mitigate | render_role_doc receives summary SweepResult fields only (tokens, cost, scores); no raw query text re-emitted into per-role doc by default |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan; n/a |
</threat_model>

<verification>
- `uv run --package eval-harness pytest cores/eval-harness/tests/test_report_role_doc.py cores/eval-harness/tests/test_recommendation_block.py -x` exits 0
- `CODE_WIKI_RUN_EVAL=1 uv run --package eval-harness pytest cores/eval-harness/tests/eval/test_sweep_dry_run.py --run-eval -x` exits 0
- `ls .planning/sweep/` shows .gitkeep
- `uv run --package eval-harness pytest -q -m "not eval"` exits 0 (no regressions in quick suite)
</verification>

<success_criteria>
Renderers are pure-function and unit-tested; .planning/sweep/ exists; dry-run integration test composes the whole pipeline under mocked Bedrock with zero spend and writes all expected artifacts. SWEEP-03 and SWEEP-04 are mechanically reachable now.
</success_criteria>

<output>
Create `.planning/phases/07-cost-frontier-sweep/07-06-SUMMARY.md` with: rendered example of render_recommendation_block for the librarian role, the dry-run test output, and the .planning/sweep/ tree state.
</output>
