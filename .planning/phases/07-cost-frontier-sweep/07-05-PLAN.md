---
phase: 07-cost-frontier-sweep
plan: 05
type: execute
wave: 4
depends_on: ["07-02", "07-03", "07-04"]
files_modified:
  - cores/eval-harness/src/eval_harness/sweep.py
  - cores/eval-harness/src/eval_harness/two_gate.py
  - cores/eval-harness/tests/test_role_sweep.py
  - cores/eval-harness/tests/test_two_gate_scorer.py
autonomous: true
requirements: [SWEEP-01]
tags: [sweep-runner, single-role-swap, two-gate-scoring]

must_haves:
  truths:
    - "run_role_sweep(role, candidate_model_id, cases_path, vault_path, repeats=3) returns a list[SweepResult] for one (role, candidate) cell using single-role-swap (D-06)"
    - "ROLE_COMMAND_MAP routes librarian/synthesizer/code_reader through run_query with role_model_overrides; scanner/linter/ingestor through run_scan/run_lint/run_ingest_source with model_override (research §SWEEP-01)"
    - "Two-gate scoring (D-07) is implemented for librarian, ingestor, linter, scanner: gate 1 wraps DivergenceMetric.run_programmatic + check_regression; gate 2 compares panel_score mean against current-default baseline × threshold"
    - "End-to-end-only scoring (D-08) is implemented for synthesizer and code_reader: only gate 2 runs"
    - "Concurrency is throttled via asyncio.Semaphore(8) at the outer cell loop to mitigate Bedrock rate limits (Pitfall 4)"
    - "Partial-failure isolation preserved: one cell exception does not abort the matrix (existing run_sweep pattern)"
  artifacts:
    - path: "cores/eval-harness/src/eval_harness/two_gate.py"
      provides: "score_two_gate(role, results, baseline, panel_mean, default_mean, threshold) -> TwoGateOutcome; ROLES_WITH_DIVERGENCE constant"
      contains: "ROLES_WITH_DIVERGENCE"
    - path: "cores/eval-harness/src/eval_harness/sweep.py"
      provides: "run_role_sweep, ROLE_COMMAND_MAP, _sweep_query_role, _sweep_scan_role, _sweep_lint_role, _sweep_ingest_role"
      contains: "ROLE_COMMAND_MAP"
    - path: "cores/eval-harness/tests/test_role_sweep.py"
      provides: "Passing unit tests for dispatch map + single-role-swap + sweep_candidates read"
      contains: "test_single_role_swap_librarian"
    - path: "cores/eval-harness/tests/test_two_gate_scorer.py"
      provides: "Passing unit tests for D-07 gate 1+2 logic and D-08 end-to-end-only"
      contains: "test_two_gate_librarian"
  key_links:
    - from: "run_role_sweep"
      to: "ROLE_COMMAND_MAP[role]"
      via: "dispatch on role name"
      pattern: "ROLE_COMMAND_MAP"
    - from: "score_two_gate"
      to: "eval_harness.divergence.metric.check_regression"
      via: "gate 1 baseline regression check"
      pattern: "check_regression"
    - from: "score_two_gate"
      to: "eval_harness.judge.panel_score"
      via: "gate 2 quality mean comparison"
      pattern: "panel_score"
---

<objective>
Add the role-rotation sweep runner (`run_role_sweep`) and the two-gate scorer. After this plan, a single async call drives one (role, candidate) cell through the right command function, with all other roles holding their models.toml defaults (D-06), and the cell's qualifying status is computed via D-07 / D-08 scoring.

Purpose: This is the structural heart of the phase. Plans 07-02..04 prepared the surfaces; this plan composes them into the sweep engine the next plans render and execute.

Output: A new `two_gate.py` module, `run_role_sweep` + dispatch helpers inside `sweep.py`, and two test files moved from skipped scaffolds to fully green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-cost-frontier-sweep/07-CONTEXT.md
@.planning/phases/07-cost-frontier-sweep/07-RESEARCH.md
@.planning/phases/07-cost-frontier-sweep/07-PATTERNS.md
@cores/eval-harness/src/eval_harness/sweep.py
@cores/eval-harness/src/eval_harness/judge.py
@cores/eval-harness/src/eval_harness/structural.py
@cores/eval-harness/src/eval_harness/pricing.py
@cores/eval-harness/src/eval_harness/divergence/__init__.py
@cores/eval-harness/src/eval_harness/divergence/metric.py
@cores/eval-harness/src/eval_harness/divergence/check.py
@cores/eval-harness/src/eval_harness/isolation.py
@agents/code-wiki-agent/src/code_wiki_agent/commands/query.py
@agents/code-wiki-agent/src/code_wiki_agent/commands/scan.py
@agents/code-wiki-agent/src/code_wiki_agent/commands/lint.py
@agents/code-wiki-agent/src/code_wiki_agent/commands/ingest.py

<interfaces>
<!-- Existing run_sweep structure (sweep.py): EvalWorktree per run, _extract_tokens_from_traces, cost_for_usage, check_structural, asyncio.gather(return_exceptions=True), _sanitize_model_id for filenames — REUSE all of these -->
<!-- SweepResult dataclass already exists; reuse fields (model_id, case_id, repeat_idx, tokens_in, tokens_out, cost_usd, structural, judge_scores, etc.) -->
<!-- DivergenceMetric (divergence/metric.py): __init__(role, checks, rubric_path, vault); run_programmatic(outputs) -> per-rule list; run_judge for hard mode -->
<!-- check_regression(role, current_results, baseline) raises AssertionError on regression -->
<!-- load_baseline(role, baselines_dir) loads cores/eval-harness/baselines/divergence-{role}.json -->
<!-- ROLE_CHECKS, ROLE_RUBRICS in divergence/__init__.py — covers librarian, ingestor, linter, scanner -->
<!-- AgentOutputProxy(answer=..., page_type=...) wraps any output for the divergence check -->
<!-- panel_score(query, answer, expected) -> dict[str, float] including "mean" -->
<!-- Pattern: 07-PATTERNS.md §"sweep.py (extend)" lays out the dispatch map and the role-tier semaphore + gather idiom -->
<!-- D-07 two-gate logic: ROLES_WITH_DIVERGENCE = {"librarian", "ingestor", "linter", "scanner"}; for these, gate 1 fails if check_regression raises; gate 2 fails if panel_mean < default_mean * threshold -->
<!-- D-08 end-to-end-only: synthesizer and code_reader use only gate 2 -->
<!-- Discretion (CONTEXT §"Claude's Discretion"): threshold defaults to 0.95 for quality tier (librarian/synthesizer) and 0.90 for mid/cheap-fast — exposed as a parameter for Plan 07-07 to set per role -->
<!-- Concurrency: outer asyncio.Semaphore(8) per Pitfall 4 -->
<!-- Discretion: --role <name> flag is worth keeping (research §Open Question 2). Realized here as the `role` parameter on run_role_sweep — already single-role. Multi-role driver is in Plan 07-07. -->
<!-- code_reader fixture caveat (research §Open Question 4): for the Phase 7 sweep, accept the "fallback fires but returns CODE_FALLBACK_DISCLAIMER" outcome — the cost data is still meaningful even if quality is N/A. The two-gate scorer treats panel_mean=None as "skip gate 2; quality=N/A in SweepResult". This decision is logged in CONTEXT.md as Claude's Discretion. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement two_gate.py — score_two_gate, ROLES_WITH_DIVERGENCE, TwoGateOutcome</name>
  <read_first>
    - cores/eval-harness/src/eval_harness/divergence/metric.py (load_baseline, check_regression, DivergenceMetric)
    - cores/eval-harness/src/eval_harness/divergence/__init__.py (ROLE_CHECKS, ROLE_RUBRICS)
    - cores/eval-harness/src/eval_harness/divergence/check.py (AgentOutputProxy)
    - cores/eval-harness/baselines/divergence-librarian.json (example baseline shape)
    - .planning/phases/07-cost-frontier-sweep/07-CONTEXT.md §D-07, §D-08
    - .planning/phases/07-cost-frontier-sweep/07-PATTERNS.md §"Divergence Baseline Load + Regression Check (two-gate scorer)"
  </read_first>
  <behavior>
    - Constant `ROLES_WITH_DIVERGENCE = {"librarian", "ingestor", "linter", "scanner"}` (D-07).
    - Dataclass `TwoGateOutcome` with fields: `qualified: bool`, `gate1_passed: bool | None` (None for D-08 roles), `gate2_passed: bool | None` (None when panel_mean is None), `divergence_failures: dict[str, int] | None`, `panel_mean: float | None`, `threshold_used: float`, `notes: str`.
    - `score_two_gate(role, divergence_metric_or_none, agent_outputs_by_case, baselines_dir, panel_mean, default_panel_mean, threshold) -> TwoGateOutcome`:
      - If role in ROLES_WITH_DIVERGENCE: builds AgentOutputProxy entries from agent_outputs_by_case; calls divergence_metric.run_programmatic(outputs); calls check_regression(role, current_results, load_baseline(role, baselines_dir)). gate1_passed = True if no AssertionError, False otherwise. Capture the per-rule failure counts in divergence_failures.
      - If role NOT in ROLES_WITH_DIVERGENCE (synthesizer, code_reader): gate1_passed = None.
      - Gate 2: if panel_mean is None OR default_panel_mean is None: gate2_passed = None. Else gate2_passed = (panel_mean >= default_panel_mean * threshold).
      - qualified = (gate1_passed in {True, None}) AND (gate2_passed in {True, None}). If both gates None (no signal at all) the candidate is reported but qualified = False with note "no quality signal".
      - notes captures the human-readable reason for fail/pass.
    - threshold default of 0.95 for quality-tier roles is set by the CALLER (sweep runner), not hard-coded in score_two_gate.
  </behavior>
  <action>
    Create cores/eval-harness/src/eval_harness/two_gate.py. Import from `eval_harness.divergence.metric` and `eval_harness.divergence` per <interfaces>. Define ROLES_WITH_DIVERGENCE as a frozenset. Define TwoGateOutcome as a dataclass with `@dataclass(frozen=True)`. Implement score_two_gate per the branching logic in <behavior>. Catch AssertionError specifically from check_regression to set gate1_passed=False rather than propagating. Use logging.getLogger(__name__) and log at DEBUG for each gate decision; INFO at the qualified outcome line. Do NOT inline code in this action; follow the metric.py + check.py read_first analogs.

    Then open cores/eval-harness/tests/test_two_gate_scorer.py (the scaffold from Plan 07-01) and (a) REMOVE the module-level `pytestmark = pytest.mark.skip(...)` line, (b) IMPLEMENT the four test function bodies per the scaffolds: test_two_gate_librarian_pass, test_two_gate_librarian_divergence_fail, test_two_gate_librarian_quality_fail, test_synthesizer_uses_end_to_end_only. Use AsyncMock/patch to mock DivergenceMetric, check_regression, and panel_score so no live Bedrock call happens. Refer to test_divergence_metric.py for DivergenceMetric construction and AgentOutputProxy usage. The unit tests MUST run green at the end of this task — the scaffold's `assert False, "TODO Plan 07-05"` placeholders MUST be replaced with real assertions on TwoGateOutcome fields.
  </action>
  <verify>
    <automated>uv run --package eval-harness pytest cores/eval-harness/tests/test_two_gate_scorer.py -x</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "ROLES_WITH_DIVERGENCE" cores/eval-harness/src/eval_harness/two_gate.py returns at least 1
    - grep -c "class TwoGateOutcome" cores/eval-harness/src/eval_harness/two_gate.py returns 1
    - grep -c "def score_two_gate" cores/eval-harness/src/eval_harness/two_gate.py returns 1
    - grep -c "from eval_harness.divergence" cores/eval-harness/src/eval_harness/two_gate.py returns at least 1
    - grep -v '^#' cores/eval-harness/tests/test_two_gate_scorer.py | grep -c "pytestmark = pytest.mark.skip" returns 0
    - All test_two_gate_scorer.py tests pass (test_two_gate_librarian_pass, test_two_gate_librarian_divergence_fail, test_two_gate_librarian_quality_fail, test_synthesizer_uses_end_to_end_only).
  </acceptance_criteria>
  <done>two_gate.py exposes ROLES_WITH_DIVERGENCE, TwoGateOutcome, and score_two_gate; all four unit tests are green; divergence + judge integration is exercised via mocks (no live Bedrock).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement run_role_sweep + dispatch map in sweep.py</name>
  <read_first>
    - cores/eval-harness/src/eval_harness/sweep.py (existing run_sweep — copy the _run_one pattern, the _sanitize_model_id helper, _extract_tokens_from_traces, EvalWorktree usage)
    - cores/eval-harness/src/eval_harness/isolation.py (EvalWorktree contract)
    - cores/eval-harness/src/eval_harness/two_gate.py (from Task 1)
    - cores/eval-harness/src/eval_harness/preflight.py (from Plan 07-04)
    - agents/code-wiki-agent/src/code_wiki_agent/commands/query.py (run_query with role_model_overrides — from Plan 07-02)
    - agents/code-wiki-agent/src/code_wiki_agent/commands/scan.py (run_scan with model_override)
    - agents/code-wiki-agent/src/code_wiki_agent/commands/lint.py (run_lint with model_override)
    - agents/code-wiki-agent/src/code_wiki_agent/commands/ingest.py (run_ingest_source with model_override)
    - .planning/phases/07-cost-frontier-sweep/07-PATTERNS.md §"sweep.py (extend — add run_role_sweep)"
  </read_first>
  <behavior>
    - New imports in sweep.py: `from code_wiki_agent.commands.scan import run_scan`, `from code_wiki_agent.commands.lint import run_lint`, `from code_wiki_agent.commands.ingest import run_ingest_source`, `from model_adapter.loader import load_role_config`, `from eval_harness.two_gate import score_two_gate, ROLES_WITH_DIVERGENCE, TwoGateOutcome`.
    - `ROLE_COMMAND_MAP: dict[str, callable]` — module-level constant mapping role name to the appropriate inner runner: librarian/synthesizer/code_reader → _sweep_query_role; scanner → _sweep_scan_role; linter → _sweep_lint_role; ingestor → _sweep_ingest_role.
    - `_sweep_query_role(role, candidate_model_id, case, vault_path)` → returns (QueryResult, trace_dir).  Calls `await run_query(query=case["query"], vault_path=vault_path, role_model_overrides={role: candidate_model_id})`.
    - `_sweep_scan_role`, `_sweep_lint_role`, `_sweep_ingest_role` → analogous, calling the right command function with `model_override=candidate_model_id`. For ingestor, case provides a `source_path` field (or sweep runner caller picks one — for now use a fixture source file path passed in via vault_path's sibling structure; document in module docstring).
    - `async def run_role_sweep(role, candidate_model_id, cases_path, vault_path, repeats=3, semaphore=None) -> list[SweepResult]`:
      - Builds the (case, repeat_idx) coro list.
      - Wraps each cell in a semaphore-acquire (default semaphore = asyncio.Semaphore(8) per Pitfall 4).
      - Uses EvalWorktree per cell for vault isolation.
      - Dispatches via ROLE_COMMAND_MAP[role].
      - Extracts tokens from trace JSONL via existing `_extract_tokens_from_traces`; computes cost via `cost_for_usage`.
      - Returns a list of SweepResult, one per (case, repeat_idx). On per-cell exception, logs and skips (partial-failure isolation).
    - `await asyncio.gather(*coros, return_exceptions=True)` mirrors the existing run_sweep pattern.
    - The two-gate scoring is NOT called inside run_role_sweep — it's called by the OUTER multi-cell driver in Plan 07-07. run_role_sweep only produces raw SweepResults per cell.
  </behavior>
  <action>
    Edit cores/eval-harness/src/eval_harness/sweep.py to add the new imports, the ROLE_COMMAND_MAP module constant, the four private `_sweep_*_role` helpers, and the public `run_role_sweep` async function. Match the existing run_sweep style line-for-line: use the same `_sanitize_model_id` for trace filenames, the same `EvalWorktree` lifecycle, the same `_extract_tokens_from_traces` call, the same `asyncio.gather(return_exceptions=True)` partial-failure pattern. Do NOT delete or modify the existing run_sweep function — it remains for backward compatibility. Place run_role_sweep below run_sweep in the same file. Do NOT include fenced code in this action; rely on read_first analogs.

    Then open cores/eval-harness/tests/test_role_sweep.py (the scaffold from Plan 07-01) and (a) REMOVE the module-level `pytestmark = pytest.mark.skip(...)` line, (b) IMPLEMENT the four test function bodies per the scaffolds: test_role_sweep_calls_dispatch_map, test_single_role_swap_librarian, test_single_role_swap_scanner, test_sweep_candidates_read_from_models_toml. Use AsyncMock/patch on run_query / run_scan / run_lint / run_ingest_source so no live Bedrock call happens. Refer to test_sweep.py for the existing AsyncMock-returns shape. The unit tests MUST run green at the end of this task — the scaffold placeholders MUST be replaced with real assertions on captured kwargs and SweepResult contents.
  </action>
  <verify>
    <automated>uv run --package eval-harness pytest cores/eval-harness/tests/test_role_sweep.py -x</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "async def run_role_sweep" cores/eval-harness/src/eval_harness/sweep.py returns 1
    - grep -c "ROLE_COMMAND_MAP" cores/eval-harness/src/eval_harness/sweep.py returns at least 2
    - grep -c "_sweep_query_role" cores/eval-harness/src/eval_harness/sweep.py returns at least 2
    - grep -c "_sweep_scan_role" cores/eval-harness/src/eval_harness/sweep.py returns at least 2
    - grep -c "_sweep_lint_role" cores/eval-harness/src/eval_harness/sweep.py returns at least 2
    - grep -c "_sweep_ingest_role" cores/eval-harness/src/eval_harness/sweep.py returns at least 2
    - grep -c "asyncio.Semaphore" cores/eval-harness/src/eval_harness/sweep.py returns at least 2 (existing + new)
    - grep -c "from eval_harness.two_gate" cores/eval-harness/src/eval_harness/sweep.py returns at least 1
    - grep -v '^#' cores/eval-harness/tests/test_role_sweep.py | grep -c "pytestmark = pytest.mark.skip" returns 0
    - All test_role_sweep.py tests pass (test_role_sweep_calls_dispatch_map, test_single_role_swap_librarian, test_single_role_swap_scanner, test_sweep_candidates_read_from_models_toml); mocks ensure no live Bedrock call.
    - Existing test_sweep.py tests still pass: `uv run --package eval-harness pytest cores/eval-harness/tests/test_sweep.py -x` exits 0
  </acceptance_criteria>
  <done>run_role_sweep drives any one (role, candidate) cell through the right command function with single-role-swap; existing run_sweep is preserved; semaphore-based concurrency throttle is in place; all unit tests are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sweep runner → agent command functions | Untrusted candidate model_id is bounded to TOML-defined sweep_candidates |
| trace JSONL writes | EvalWorktree provides per-cell tmpdir; no cross-cell leakage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-09 | Denial of Service (rate limit) | parallel Bedrock calls | mitigate | asyncio.Semaphore(8) outer throttle; per-role max_concurrency from models.toml retained |
| T-07-10 | Tampering | trace filename construction from model_id | mitigate | _sanitize_model_id applied to every per-role filename (existing pattern) |
| T-07-11 | Information Disclosure | trace JSONL captures user query content | accept | personal-project scope per CONTEXT scope-reduction note; documented in STORY.md emission downstream |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan; n/a |
</threat_model>

<verification>
- `uv run --package eval-harness pytest cores/eval-harness/tests/test_role_sweep.py cores/eval-harness/tests/test_two_gate_scorer.py -x` exits 0
- `uv run --package eval-harness pytest cores/eval-harness/tests/test_sweep.py -x` exits 0 (existing tests preserved)
- `uv run --package eval-harness pytest -q -m "not eval"` exits 0 overall
</verification>

<success_criteria>
run_role_sweep is the sweep engine for one (role, candidate) cell; ROLE_COMMAND_MAP correctly dispatches to the four command functions; two_gate.score_two_gate is callable and exercises both gates per D-07 / D-08; partial-failure isolation preserved; outer concurrency cap honors Pitfall 4.
</success_criteria>

<output>
Create `.planning/phases/07-cost-frontier-sweep/07-05-SUMMARY.md` with: signatures added, dispatch table, two-gate fail matrix, unit test outcomes.
</output>
