---
phase: 07-cost-frontier-sweep
plan: 04
type: execute
wave: 3
depends_on: ["07-01", "07-03"]
files_modified:
  - cores/eval-harness/src/eval_harness/sweep.py
  - cores/eval-harness/src/eval_harness/preflight.py
  - cores/eval-harness/tests/test_preflight_estimator.py
autonomous: true
requirements: [SWEEP-02]
tags: [pre-flight, cost-estimator, bed-01, dry-run]

must_haves:
  truths:
    - "estimate_sweep_cost(role_candidates, n_cases, repeats) returns a float that aggregates per-role-tier conservative tokens against eval_harness.pricing (D-13)"
    - "Hard cap is $25 — pre-flight aborts with SystemExit when estimate exceeds the cap (research §Tension 2 recommendation)"
    - "preflight_bed01() invokes make_llm('haiku').invoke('ping') and raises SystemExit with the BedrockAccessDenied detail when access fails (SWEEP-02)"
    - "A --dry-run-friendly path is available: callers can request the estimate and the BED-01 ping independently (the dry-run integration test in Plan 07-06 monkey-patches both)"
  artifacts:
    - path: "cores/eval-harness/src/eval_harness/preflight.py"
      provides: "estimate_sweep_cost, HARD_CAP_USD, preflight_bed01, preflight_check"
      contains: "HARD_CAP_USD = 25.0"
    - path: "cores/eval-harness/src/eval_harness/sweep.py"
      provides: "Imports + re-exports preflight helpers for sweep callers"
      contains: "from eval_harness.preflight import"
    - path: "cores/eval-harness/tests/test_preflight_estimator.py"
      provides: "Passing unit tests for estimator math, cap enforcement, unknown-model skip, BED-01 ping happy/sad paths"
      contains: "def test_estimate_24_cell_sweep_within_cap"
  key_links:
    - from: "eval_harness.preflight.estimate_sweep_cost"
      to: "eval_harness.pricing.cost_for_usage"
      via: "per-tier token constants × case count × repeats × pricing"
      pattern: "cost_for_usage"
    - from: "eval_harness.preflight.preflight_bed01"
      to: "model_adapter.loader.make_llm"
      via: "make_llm('haiku').invoke('ping')"
      pattern: 'make_llm\\("haiku"\\)\\.invoke'
---

<objective>
Add the pre-flight cost estimator, hard cap, and BED-01 live-Bedrock ping. These run before the matrix in Plan 07-05 / 07-07 to (a) refuse runs whose conservative estimate exceeds the cap and (b) confirm Bedrock connectivity before the spend begins.

Purpose: D-13 mandates a pre-flight estimator and explicit `--dry-run` plumbing. SWEEP-02 mandates BED-01 verified during the sweep. This plan delivers both behind clear, testable function boundaries that the sweep runner consumes.

Output: A new `preflight.py` module with three callables (`estimate_sweep_cost`, `preflight_bed01`, `preflight_check`), the constant `HARD_CAP_USD = 25.0`, an import line in sweep.py for convenience, and seven passing unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-cost-frontier-sweep/07-CONTEXT.md
@.planning/phases/07-cost-frontier-sweep/07-RESEARCH.md
@.planning/phases/07-cost-frontier-sweep/07-PATTERNS.md
@cores/eval-harness/src/eval_harness/pricing.py
@cores/eval-harness/src/eval_harness/sweep.py
@cores/model-adapter/src/model_adapter/loader.py
@cores/model-adapter/src/model_adapter/exceptions.py

<interfaces>
<!-- D-13 conservative per-tier token constants (research §Tension 6): -->
<!--   cheap-fast (scanner, code_reader): 3000 in / 500 out -->
<!--   mid (linter, ingestor): 5000 in / 1000 out -->
<!--   quality (librarian, synthesizer): 8000 in / 2000 out -->
<!-- Tier-to-role map (D-03): scanner+code_reader=cheap-fast; linter+ingestor=mid; librarian+synthesizer=quality -->
<!-- Hard cap: research §Tension 2 recommends $25 (8x headroom over estimated $6.19 total) -->
<!-- cost_for_usage(model_id, {"input": int, "output": int}) -> float — defined in pricing.py; raises UnknownModelError -->
<!-- model_adapter.loader.make_llm("haiku") returns a ChatBedrockConverse; .invoke("ping") performs a real Bedrock call -->
<!-- model_adapter.exceptions.BedrockAccessDenied raised on credential/IAM failure -->
<!-- Pattern source: 07-PATTERNS.md §"Pre-flight estimator pattern" and §"BED-01 pre-flight pattern" -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement preflight.py module with estimator, cap, and BED-01 ping</name>
  <read_first>
    - cores/eval-harness/src/eval_harness/pricing.py (cost_for_usage, PRICES, UnknownModelError)
    - cores/model-adapter/src/model_adapter/loader.py (make_llm signature)
    - cores/model-adapter/src/model_adapter/exceptions.py (BedrockAccessDenied)
    - .planning/phases/07-cost-frontier-sweep/07-PATTERNS.md §"Pre-flight estimator pattern", §"BED-01 pre-flight pattern"
    - .planning/phases/07-cost-frontier-sweep/07-RESEARCH.md §Tension 6, §Tension 2
  </read_first>
  <behavior>
    - Module-level constants: `HARD_CAP_USD = 25.0`; `_TIER_TOKENS = {"cheap-fast": (3000, 500), "mid": (5000, 1000), "quality": (8000, 2000)}`; `_ROLE_TIER = {"scanner": "cheap-fast", "code_reader": "cheap-fast", "linter": "mid", "ingestor": "mid", "librarian": "quality", "synthesizer": "quality"}`.
    - `estimate_sweep_cost(role_candidates: dict[str, list[str]], n_cases: int, repeats: int) -> float`: iterates roles → candidates → applies cost_for_usage(model_id, {"input": tokens_in*n_cases*repeats, "output": tokens_out*n_cases*repeats}); UnknownModelError → skipped (not added to total); returns aggregate float.
    - `preflight_bed01() -> None`: invokes `make_llm("haiku").invoke("ping")`; on BedrockAccessDenied (or any exception), raises SystemExit with prefix "BED-01 FAILED:" plus the original message; on success prints "[BED-01] Bedrock connectivity confirmed." to stdout.
    - `preflight_check(role_candidates, n_cases, repeats, *, skip_bed01: bool = False, auto_confirm: bool = False) -> float`: orchestrates the above. Computes estimate; if estimate > HARD_CAP_USD raises SystemExit with message containing "exceeds hard cap"; if not skip_bed01 calls preflight_bed01(); if not auto_confirm prompts via `input("Estimated cost: $X.XX, proceed? [y/N] ")` and SystemExits when answer is not in {"y","Y","yes"}. Returns the estimate float.
    - All functions exposed at module top-level (no leading underscore on public API).
  </behavior>
  <action>
    Create cores/eval-harness/src/eval_harness/preflight.py. Place the constants exactly as listed in <behavior>. Implement the three callables per the signatures and behavior in <behavior>. Use `from eval_harness.pricing import cost_for_usage, UnknownModelError` and `from model_adapter.loader import make_llm`. Catch BedrockAccessDenied via `from model_adapter.exceptions import BedrockAccessDenied` and re-raise as SystemExit with the formatted prefix; also catch generic Exception with the same prefix as a defensive net. Use `print(...)` for stdout (no logging.info — must be visible to the user). `preflight_check` `input()` prompt MUST format the dollar amount as `${estimate:.2f}` so the unit test can monkey-patch builtins.input deterministically. Do NOT inline fenced code; rely on 07-PATTERNS.md §"Pre-flight estimator pattern" for the cost loop shape.
  </action>
  <verify>
    <automated>uv run --package eval-harness pytest cores/eval-harness/tests/test_preflight_estimator.py -k "not bed01_live" -x</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "HARD_CAP_USD = 25.0" cores/eval-harness/src/eval_harness/preflight.py returns 1
    - grep -c "def estimate_sweep_cost" cores/eval-harness/src/eval_harness/preflight.py returns 1
    - grep -c "def preflight_bed01" cores/eval-harness/src/eval_harness/preflight.py returns 1
    - grep -c "def preflight_check" cores/eval-harness/src/eval_harness/preflight.py returns 1
    - grep -c '"cheap-fast": (3000, 500)' cores/eval-harness/src/eval_harness/preflight.py returns 1
    - grep -c '"quality": (8000, 2000)' cores/eval-harness/src/eval_harness/preflight.py returns 1
    - grep -c "BedrockAccessDenied" cores/eval-harness/src/eval_harness/preflight.py returns at least 1
    - `python -c "from eval_harness.preflight import estimate_sweep_cost, HARD_CAP_USD, preflight_bed01, preflight_check"` exits 0
  </acceptance_criteria>
  <done>preflight.py exposes the four public names; estimator math uses the conservative per-tier token constants; BED-01 ping has both happy and SystemExit-formatted error paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire preflight into sweep.py + flesh out test_preflight_estimator.py</name>
  <read_first>
    - cores/eval-harness/src/eval_harness/sweep.py (current imports + module surface)
    - cores/eval-harness/src/eval_harness/preflight.py (from Task 1)
    - cores/eval-harness/tests/test_preflight_estimator.py (skipped scaffold from Plan 07-01)
    - .planning/phases/07-cost-frontier-sweep/07-PATTERNS.md §"test_preflight_estimator.py (new)"
  </read_first>
  <behavior>
    - sweep.py adds `from eval_harness.preflight import estimate_sweep_cost, preflight_bed01, preflight_check, HARD_CAP_USD` near its existing imports. No functional change in sweep.py yet (Plan 07-05 consumes these); the import line makes the API discoverable from `eval_harness.sweep`.
    - test_preflight_estimator.py lifts its module-level pytest.skip. Tests:
      - test_estimate_24_cell_sweep_within_cap: builds the full D-03 role_candidates dict with 4 candidates per role × 6 roles; passes n_cases=4, repeats=3; asserts estimate is positive AND below HARD_CAP_USD.
      - test_estimate_returns_zero_for_empty_candidates: empty dict → 0.0.
      - test_estimator_skips_unknown_model_ids: dict with one bogus model_id ("bogus-model") in librarian candidates; assert no exception, total is finite, the bogus contribution is 0.
      - test_estimator_scales_with_repeats: estimate(repeats=3) > estimate(repeats=1) for the same role_candidates; ratio approximately 3.0 (use pytest.approx with rel=0.05).
      - test_preflight_check_aborts_above_cap: monkey-patches estimate to return 100.0 (e.g. via patching pricing.cost_for_usage) OR constructs role_candidates designed to exceed cap; asserts SystemExit raised with message containing "exceeds hard cap"; preflight_bed01 NOT called.
      - test_preflight_check_prompts_for_confirmation: monkey-patches builtins.input to return "n"; asserts SystemExit; with input returning "y" no exception is raised and the estimate is returned.
      - test_preflight_bed01_systemexit_on_access_denied: monkey-patches make_llm to return a fake whose .invoke raises BedrockAccessDenied("arn"); assert SystemExit with prefix "BED-01 FAILED:".
      - test_preflight_bed01_prints_confirmation_on_success: monkey-patches make_llm to return a fake whose .invoke returns a benign object; capsys captures "[BED-01] Bedrock connectivity confirmed."
  </behavior>
  <action>
    Add the import line to cores/eval-harness/src/eval_harness/sweep.py — place it with the other eval_harness module imports near the top of the file. Then open cores/eval-harness/tests/test_preflight_estimator.py and (a) remove the module-level `pytestmark = pytest.mark.skip(...)`, (b) implement the eight test functions in <behavior>. For mocking make_llm, use `monkeypatch.setattr("eval_harness.preflight.make_llm", fake_factory)` (a fake factory returning an object with an `.invoke` method). For BedrockAccessDenied: import from model_adapter.exceptions. Use `capsys` for stdout captures and `monkeypatch.setattr("builtins.input", lambda *_: "n")` for the prompt test. Do NOT include fenced code in this action; the test_preflight_estimator.py scaffolds from Plan 07-01 already lay out signatures.
  </action>
  <verify>
    <automated>uv run --package eval-harness pytest cores/eval-harness/tests/test_preflight_estimator.py -x</automated>
  </verify>
  <acceptance_criteria>
    - All 8 tests pass; pytest exits 0
    - grep -c "from eval_harness.preflight import" cores/eval-harness/src/eval_harness/sweep.py returns at least 1
    - grep -c "estimate_sweep_cost" cores/eval-harness/src/eval_harness/sweep.py returns at least 1
    - grep -v '^#' cores/eval-harness/tests/test_preflight_estimator.py | grep -c "pytestmark = pytest.mark.skip" returns 0
    - grep -c "def test_estimate_24_cell_sweep_within_cap" cores/eval-harness/tests/test_preflight_estimator.py returns 1
    - grep -c "def test_preflight_check_aborts_above_cap" cores/eval-harness/tests/test_preflight_estimator.py returns 1
    - grep -c "def test_preflight_bed01_systemexit_on_access_denied" cores/eval-harness/tests/test_preflight_estimator.py returns 1
    - grep -c "def test_preflight_check_prompts_for_confirmation" cores/eval-harness/tests/test_preflight_estimator.py returns 1
  </acceptance_criteria>
  <done>Pre-flight estimator + BED-01 ping are usable from `eval_harness.sweep` and `eval_harness.preflight`; all eight unit tests are green; no live Bedrock calls during tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pre-flight ping → live AWS Bedrock | Real Bedrock call hits live IAM; failures must surface clearly |
| estimator → pricing table | Untrusted input is bounded to role_candidates from committed TOML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06 | Denial of Service (spend exhaustion) | sweep matrix run | mitigate | HARD_CAP_USD = $25; preflight_check aborts with SystemExit before any Bedrock call; CONTEXT D-13 |
| T-07-07 | Information Disclosure | BED-01 error message | accept | BedrockAccessDenied includes the ARN; printed to stderr only on failure; same surface as today's make_llm |
| T-07-08 | Tampering | role_candidates dict | accept | values come from committed models.toml only; tests confirm unknown models skip silently |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan; n/a |
</threat_model>

<verification>
- `uv run --package eval-harness pytest cores/eval-harness/tests/test_preflight_estimator.py -x` exits 0
- `python -c "from eval_harness.sweep import estimate_sweep_cost, preflight_check, HARD_CAP_USD; print(HARD_CAP_USD)"` prints `25.0`
- `uv run --package eval-harness pytest -q -m "not eval"` exits 0 (no regressions)
</verification>

<success_criteria>
Pre-flight estimator math matches D-13 conservative constants; HARD_CAP_USD is $25 (matches research-recommended value); BED-01 ping has clean happy + sad paths; preflight_check is the single entry point downstream plans call before the matrix.
</success_criteria>

<output>
Create `.planning/phases/07-cost-frontier-sweep/07-04-SUMMARY.md` with: full estimate for the 24-cell matrix using the implemented function, the cap-enforcement test outcome, and the BED-01 mock test outcome.
</output>
