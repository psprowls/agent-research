---
phase: 07-cost-frontier-sweep
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - cores/model-adapter/src/model_adapter/models.toml
  - cores/model-adapter/models.toml
  - eval/cases/code_reader_cases.json
  - eval/cases/query_cases.json
  - cores/eval-harness/tests/test_models_toml_sweep_candidates.py
autonomous: true
requirements: [SWEEP-01]
tags: [config, fixtures, models-toml, vault-thin]

must_haves:
  truths:
    - "sweep_candidates arrays exist on the six in-scope role blocks in cores/model-adapter/src/model_adapter/models.toml (D-05)"
    - "Candidate lists match D-03 tier-to-role mapping exactly (cheap-fast / mid / quality)"
    - "Vault-thin code_reader fixture cases exist and are tagged so non-code_reader sweeps skip them (D-09)"
    - "load_role_config still succeeds for every role; existing make_llm calls remain unaffected"
    - "Top-level cores/model-adapter/models.toml stub carries a comment identifying it as stale (Pitfall 5)"
  artifacts:
    - path: "cores/model-adapter/src/model_adapter/models.toml"
      provides: "sweep_candidates arrays on librarian, synthesizer, code_reader, scanner, linter, ingestor"
      contains: "sweep_candidates"
    - path: "eval/cases/code_reader_cases.json"
      provides: "3 vault-thin queries (D-09) to force _run_code_fallback fire path"
      contains: "code-reader-01"
    - path: "cores/eval-harness/tests/test_models_toml_sweep_candidates.py"
      provides: "Unit tests verifying loader tolerance and per-role candidate sets match D-03 spec"
      contains: "def test_sweep_candidates_present_for_all_six_roles"
  key_links:
    - from: "sweep_candidates entries in models.toml"
      to: "eval_harness.pricing.PRICES dict"
      via: "every listed model_id MUST be priced"
      pattern: "us\\.anthropic\\.|us\\.amazon\\.|qwen\\."
    - from: "eval/cases/code_reader_cases.json"
      to: "agents/code-wiki-agent/src/code_wiki_agent/commands/query.py _run_code_fallback"
      via: "vault-thin queries → empty useful_excerpts → fallback fires"
      pattern: "code-reader|vault-thin"
---

<objective>
Add the `sweep_candidates` config + the vault-thin fixture cases the sweep needs to drive 24 cells. Two parallel-safe surfaces: the TOML config (sweep runner input) and the JSON fixture corpus (sweep cases that exercise the code_reader fallback).

Purpose: Without `sweep_candidates` in models.toml, Plan 07-05's `run_role_sweep()` has nothing to iterate. Without vault-thin cases, code_reader cells return N/A (Pitfall 2).

Output: One TOML config update (six role blocks gain candidate arrays per D-03), one new JSON fixture file for D-09 vault-thin cases, one comment on the stale stub TOML, and a focused unit test proving the TOML still loads and exposes the new key.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-cost-frontier-sweep/07-CONTEXT.md
@.planning/phases/07-cost-frontier-sweep/07-RESEARCH.md
@.planning/phases/07-cost-frontier-sweep/07-PATTERNS.md
@cores/model-adapter/src/model_adapter/models.toml
@cores/model-adapter/models.toml
@cores/model-adapter/src/model_adapter/loader.py
@cores/eval-harness/src/eval_harness/pricing.py
@eval/cases/query_cases.json

<interfaces>
<!-- D-03 tier-to-role candidate map (locked): -->
<!--   Cheap-fast (scanner, code_reader): us.anthropic.claude-haiku-4-5-20251001-v1:0, us.amazon.nova-micro-v1:0, us.amazon.nova-lite-v1:0, qwen.qwen3-32b-v1:0 -->
<!--   Mid (linter, ingestor): us.anthropic.claude-haiku-4-5-20251001-v1:0, us.amazon.nova-pro-v1:0, us.amazon.nova-lite-v1:0, qwen.qwen3-32b-v1:0 -->
<!--   Quality (librarian, synthesizer): us.anthropic.claude-sonnet-4-6, us.anthropic.claude-haiku-4-5-20251001-v1:0, us.amazon.nova-pro-v1:0, qwen.qwen3-32b-v1:0 -->
<!-- D-05: sweep_candidates = [...] is a flat array under [roles.{name}] (sibling of model_id) — research §Tension 8 confirmed loader tolerance -->
<!-- D-09: 3 vault-thin queries (research §Tension 5 lists three exact queries verified against the round-trip-vault) -->
<!-- Pitfall 5: cores/model-adapter/models.toml (top-level) is stale; loader reads from src/model_adapter/models.toml only -->
<!-- Pitfall 2: code_reader fallback only fires when useful_excerpts is empty — vault-thin queries achieve this -->
<!-- All listed candidate model_ids must already exist in eval_harness.pricing.PRICES (verified in research) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sweep_candidates arrays to the six in-scope role blocks in models.toml</name>
  <read_first>
    - cores/model-adapter/src/model_adapter/models.toml (current 10-entry structure)
    - .planning/phases/07-cost-frontier-sweep/07-CONTEXT.md §"Decisions" D-03, D-05
    - .planning/phases/07-cost-frontier-sweep/07-PATTERNS.md §"cores/model-adapter/src/model_adapter/models.toml (extend)"
    - cores/eval-harness/src/eval_harness/pricing.py (verify all 4 candidates per role appear in PRICES dict)
  </read_first>
  <action>
    Add a `sweep_candidates = [...]` array as a sibling of `model_id` inside each of the six in-scope role blocks in cores/model-adapter/src/model_adapter/models.toml: librarian, synthesizer (quality tier), linter, ingestor (mid tier), scanner, code_reader (cheap-fast tier). Use exactly the four model_ids listed in the <interfaces> block per role. Place each array immediately after `max_concurrency` to match the layout shown in 07-PATTERNS.md §"models.toml (extend)". Do NOT modify `judge_a` or `judge_b` (D-01 excludes judges). Do NOT modify the `haiku` or `sonnet` alias blocks. Do NOT change any existing `model_id`, `region`, `max_tokens`, or `max_concurrency` value. Preserve file ordering; insert in place.
  </action>
  <verify>
    <automated>uv run --package eval-harness pytest cores/eval-harness/tests/test_models_toml_sweep_candidates.py -x</automated>
  </verify>
  <acceptance_criteria>
    - grep -v '^#' cores/model-adapter/src/model_adapter/models.toml | grep -c "sweep_candidates" returns 6
    - grep -A6 "\\[roles.librarian\\]" cores/model-adapter/src/model_adapter/models.toml | grep -c "us.anthropic.claude-sonnet-4-6" returns at least 1
    - grep -A6 "\\[roles.scanner\\]" cores/model-adapter/src/model_adapter/models.toml | grep -c "us.amazon.nova-micro-v1:0" returns at least 1
    - grep -A6 "\\[roles.synthesizer\\]" cores/model-adapter/src/model_adapter/models.toml | grep -c "us.amazon.nova-pro-v1:0" returns at least 1
    - grep -A6 "\\[roles.judge_a\\]" cores/model-adapter/src/model_adapter/models.toml | grep -c "sweep_candidates" returns 0
    - `python -c "from model_adapter.loader import load_role_config; r=load_role_config('librarian'); assert len(r['sweep_candidates'])==4"` exits 0
    - `python -c "from model_adapter.loader import make_llm; make_llm('librarian')"` exits 0 (loader still works; ChatBedrockConverse not invoked, but role config parses)
  </acceptance_criteria>
  <done>All six in-scope role blocks have correct 4-element sweep_candidates arrays matching D-03 tier maps; judges and aliases unchanged; loader tolerates the new key.</done>
</task>

<task type="auto">
  <name>Task 2: Mark stale top-level models.toml; add code_reader vault-thin fixtures</name>
  <read_first>
    - cores/model-adapter/models.toml (top-level stub — research §Tension 7 + Pitfall 5)
    - eval/cases/query_cases.json (existing 4-case schema)
    - .planning/phases/07-cost-frontier-sweep/07-RESEARCH.md §Tension 5 (vault-thin candidate queries verified against round-trip-vault)
    - .planning/phases/07-cost-frontier-sweep/07-PATTERNS.md §"eval/cases/query_cases.json (extend)"
  </read_first>
  <behavior>
    - cores/model-adapter/models.toml gains a single top-of-file comment block: a `# STUB:` notice identifying the file as inert and pointing readers to `cores/model-adapter/src/model_adapter/models.toml` as the source of truth. No data changes.
    - eval/cases/code_reader_cases.json is created containing exactly 3 vault-thin cases with case_ids: `code-reader-01`, `code-reader-02`, `code-reader-03`, queries verbatim from research §Tension 5 (the three vault-thin queries verified against the round-trip-vault). Each entry includes `tags` containing both `"code-reader"` and `"vault-thin"`.
    - eval/cases/query_cases.json is UNCHANGED. Vault-thin cases live in their own file (planner choice from D-09 alternatives) — keeps non-code_reader sweep loops simple (no per-case role filtering needed).
  </behavior>
  <action>
    Prepend a stub-notice comment block (using `#` lines, since this is TOML) at the very top of cores/model-adapter/models.toml stating exactly: `# STUB: this file is not loaded in production. Edit cores/model-adapter/src/model_adapter/models.toml.`. Do not change any data lines. Then create eval/cases/code_reader_cases.json containing a single top-level JSON array with three case objects. Each case object has keys `case_id`, `query`, `expected_answer`, `tags`. Use the three queries from research §Tension 5 verbatim. The expected_answer strings should describe the source-only nature of the answer (planner can phrase per the patterns map example). All three tag arrays must contain both `"code-reader"` and `"vault-thin"` strings.
  </action>
  <verify>
    <automated>uv run --package eval-harness pytest cores/eval-harness/tests/test_models_toml_sweep_candidates.py -k "stub or code_reader_cases" -x</automated>
  </verify>
  <acceptance_criteria>
    - head -3 cores/model-adapter/models.toml shows the STUB notice on line 1
    - grep -c "STUB:" cores/model-adapter/models.toml returns 1
    - grep -c "code-reader-01" eval/cases/code_reader_cases.json returns 1
    - grep -c "code-reader-02" eval/cases/code_reader_cases.json returns 1
    - grep -c "code-reader-03" eval/cases/code_reader_cases.json returns 1
    - `python -c "import json; cases=json.load(open('eval/cases/code_reader_cases.json')); assert len(cases)==3 and all('vault-thin' in c['tags'] for c in cases)"` exits 0
    - eval/cases/query_cases.json is unchanged: `git diff -- eval/cases/query_cases.json` is empty
  </acceptance_criteria>
  <done>Top-level stub is labelled; vault-thin fixtures exist in a dedicated file; existing query_cases.json is untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Unit test asserting models.toml loader tolerance + tier-to-role candidate spec</name>
  <read_first>
    - cores/eval-harness/tests/test_models_toml_sweep_candidates.py (from Plan 07-01 — currently a skipped scaffold)
    - cores/model-adapter/src/model_adapter/loader.py
    - cores/eval-harness/src/eval_harness/pricing.py
    - .planning/phases/07-cost-frontier-sweep/07-CONTEXT.md §D-03
  </read_first>
  <behavior>
    - Lift the module-level pytest.skip in test_models_toml_sweep_candidates.py.
    - test_sweep_candidates_present_for_all_six_roles: loops over the 6 in-scope roles and asserts load_role_config(role).get("sweep_candidates") is a list of length 4.
    - test_tier_to_role_candidate_map: asserts librarian and synthesizer contain "us.anthropic.claude-sonnet-4-6"; linter and ingestor contain "us.amazon.nova-pro-v1:0"; scanner and code_reader contain "us.amazon.nova-micro-v1:0".
    - test_no_sweep_candidates_for_judges: asserts judge_a and judge_b have no sweep_candidates key (or the key is missing/empty).
    - test_all_candidates_have_pricing: loops every (role, candidate) and asserts cost_for_usage(candidate, {"input":1,"output":1}) does not raise UnknownModelError.
    - test_make_llm_still_works_for_all_roles: invokes make_llm("librarian"), ..., make_llm("ingestor") (no .invoke call — just construction) and asserts none raise.
    - test_code_reader_cases_json_loads: opens eval/cases/code_reader_cases.json, asserts 3 cases, all tagged "vault-thin".
  </behavior>
  <action>
    Open cores/eval-harness/tests/test_models_toml_sweep_candidates.py (created in Plan 07-01 as a skipped scaffold OR — if not part of 07-01 — author it now under the same conventions as 07-01's test_role_sweep.py). Remove the module-level `pytestmark = pytest.mark.skip(...)` line and implement the seven test functions in <behavior>. Imports: `from model_adapter.loader import load_role_config, make_llm` and `from eval_harness.pricing import cost_for_usage, UnknownModelError`. Constants: define IN_SCOPE_ROLES tuple and the three tier maps from D-03 as module-level constants for readability. Use straightforward asserts; no AsyncMock needed. Do NOT include fenced code blocks in this action.
  </action>
  <verify>
    <automated>uv run --package eval-harness pytest cores/eval-harness/tests/test_models_toml_sweep_candidates.py -x</automated>
  </verify>
  <acceptance_criteria>
    - All 6 tests pass; pytest exits 0
    - grep -v '^#' cores/eval-harness/tests/test_models_toml_sweep_candidates.py | grep -c "pytestmark = pytest.mark.skip" returns 0
    - grep -c "def test_sweep_candidates_present_for_all_six_roles" cores/eval-harness/tests/test_models_toml_sweep_candidates.py returns 1
    - grep -c "def test_all_candidates_have_pricing" cores/eval-harness/tests/test_models_toml_sweep_candidates.py returns 1
    - grep -c "def test_no_sweep_candidates_for_judges" cores/eval-harness/tests/test_models_toml_sweep_candidates.py returns 1
    - grep -c "def test_code_reader_cases_json_loads" cores/eval-harness/tests/test_models_toml_sweep_candidates.py returns 1
  </acceptance_criteria>
  <done>Loader tolerance + tier-to-role candidate spec are pinned by tests; judges remain excluded; all candidate models are priced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TOML config → loader / sweep runner | sweep_candidates values are committed config; same trust as production model_id |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Tampering | sweep_candidates TOML values | accept | Values live in a committed file; threat model identical to today's model_id |
| T-07-05 | Information Disclosure | code_reader_cases.json content | accept | Vault-thin queries reference internal class names; no user data, no secrets |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan; n/a |
</threat_model>

<verification>
- `uv run --package eval-harness pytest cores/eval-harness/tests/test_models_toml_sweep_candidates.py -x` exits 0
- `uv run --package eval-harness pytest -q -m "not eval"` reports no new failures
- `python -c "import tomllib; tomllib.load(open('cores/model-adapter/src/model_adapter/models.toml','rb'))"` exits 0
</verification>

<success_criteria>
Six role blocks have correct candidate arrays; loader and existing make_llm calls are unaffected; vault-thin fixtures exist in a dedicated JSON file; top-level stub TOML is labelled. D-03, D-05, D-09 are all satisfied; Pitfall 5 mitigated.
</success_criteria>

<output>
Create `.planning/phases/07-cost-frontier-sweep/07-03-SUMMARY.md` with: the per-role candidate map as a table, the three vault-thin queries, and the loader-tolerance test outcome.
</output>
