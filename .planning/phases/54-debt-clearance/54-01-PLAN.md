---
phase: 54-debt-clearance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/graph-wiki-agent/tests/integration/test_propose_domains_e2e.py
  - agents/graph-wiki-agent/tests/integration/test_propose_domains_isolation.py
  - agents/graph-wiki-agent/tests/integration/test_scan_entity_integration.py
  - packages/graph-io/tests/integration/test_cluster_cli.py
  - packages/graph-io/tests/integration/test_e2e_apps.py
  - packages/graph-io/tests/integration/test_e2e_builtins.py
  - packages/wiki-io/tests/integration/test_link_rewriter_integration.py
  - .planning/PROJECT.md
autonomous: true
requirements: [DEBT-01, DEBT-02]

must_haves:
  truths:
    - "DEBT-01 / D-01 + D-03: all 7 flagged integration test files named in D-03 (test_propose_domains_e2e.py, test_propose_domains_isolation.py, test_scan_entity_integration.py, test_cluster_cli.py, test_e2e_apps.py, test_e2e_builtins.py, test_link_rewriter_integration.py) carry the `# integration-gate-allow` marker (NOT the `GRAPH_WIKI_RUN_INTEGRATION` skipif) — they are LLM-stubbed or local-only and are meant to run in CI; applying the skipif would silently drop them from CI coverage"
    - "DEBT-01 / D-02: each marker carries a short inline justification (e.g. `# integration-gate-allow — LLM stubbed, runs in CI` or `# integration-gate-allow — local sqlite/fs only`) so the exemption is self-documenting"
    - "DEBT-01 / D-04: `uv run pytest tests/test_integration_gate.py` exits 0; the gate test (`tests/test_integration_gate.py`) and its canonical regex are NOT modified"
    - "DEBT-01: the 5 contrast-set network-gated files are NOT touched (test_bedrock_iam.py, test_mcp_e2e.py, test_query_e2e.py, test_count_tokens_live.py, test_pool_bedrock.py)"
    - "DEBT-01: the 7 files still collect under pytest after the comment edits (comments do not change collection)"
    - "DEBT-02 / D-05: PROJECT.md 'What This Is' (~L5), 'Core Value' (~L9, ~L11), and 'Constraints' (~L351, ~L354) are corrected to name `subagent-runtime` + `langchain-aws` + `langchain-core` and the `graph-wiki` naming, mirroring CLAUDE.md's '## Project' + '### Constraints' wording (D-08)"
    - "DEBT-02 / D-06: historical sections (Context ~328–345, Key Decisions ~358–385, Evolution, prior-milestone narrative incl. L252/L263/L317/L321/L323, and the L27 v1.10-debt bullet) are preserved verbatim — they accurately record decisions from when `deepagents` was the chosen framework / source was named `lattice-wiki`"
    - "DEBT-02 / D-07: the string 'DeepAgents CLI' (the external MCP host product, ~L345/L353/L365) is preserved — only the `deepagents` Python-framework references and the `lattice-wiki` naming in the three in-scope sections are corrected; no blanket string-replace"
    - "DEBT-02 / D-09: `grep -n 'deepagents\\|lattice-wiki' .planning/PROJECT.md` returns no matches inside the 'What This Is', 'Core Value', and 'Constraints' sections (historical sections may still legitimately contain them)"
  artifacts:
    - path: agents/graph-wiki-agent/tests/integration/test_propose_domains_e2e.py
      provides: "module-level `# integration-gate-allow — <why>` marker"
      contains: "# integration-gate-allow"
    - path: packages/graph-io/tests/integration/test_e2e_builtins.py
      provides: "module-level `# integration-gate-allow — <why>` marker"
      contains: "# integration-gate-allow"
    - path: packages/wiki-io/tests/integration/test_link_rewriter_integration.py
      provides: "module-level `# integration-gate-allow — <why>` marker"
      contains: "# integration-gate-allow"
    - path: .planning/PROJECT.md
      provides: "corrected 'What This Is', 'Core Value', and 'Constraints' sections naming subagent-runtime / langchain-aws / langchain-core / graph-wiki"
      contains: "subagent-runtime"
  key_links: []
---

<objective>
Clear two independent pieces of debt so the test suite gate is green and PROJECT.md's current-tense prose matches the shipped stack. No new capability and no runtime code paths are touched — DEBT-01 adds comment markers to 7 test files; DEBT-02 corrects three planning-doc sections.

Purpose: `tests/test_integration_gate.py::test_integration_test_files_use_canonical_gate` currently fails on 7 files, which fails CI on every PR. And PROJECT.md's "What This Is" / Core Value / Constraints still describe the rejected `deepagents` framework and the old `lattice-wiki` naming, contradicting the actual `subagent-runtime` + `langchain-aws` + `langchain-core` stack documented in CLAUDE.md.

Output: 7 test files each carrying a self-documenting `# integration-gate-allow` marker; a corrected PROJECT.md (three sections); a green gate test; and a scoped grep confirming no stale strings remain in the corrected sections while historical sections are preserved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/54-debt-clearance/54-CONTEXT.md
@.planning/phases/54-debt-clearance/54-RESEARCH.md
@.planning/phases/54-debt-clearance/54-VALIDATION.md
@tests/test_integration_gate.py
@CLAUDE.md

<interfaces>
<!-- The gate (tests/test_integration_gate.py) is satisfied by EITHER of: -->
<!--   1. canonical regex: pytest.mark.skipif( (not )?os.environ.get("GRAPH_WIKI_RUN_INTEGRATION") ... ) -->
<!--   2. literal substring: # integration-gate-allow  (any trailing free-form text allowed) -->
<!-- D-01 locks the decision to use mechanism (2) for all 7 files: they are LLM-stubbed -->
<!-- or fully local (sqlite + tmp_path + subprocess), meant to run in CI by default. -->

<!-- CLAUDE.md canonical wording to mirror (D-08): -->
<!-- "## Project": A Python monorepo (managed with `uv`) of LangChain-primitives-based AI -->
<!--   tooling running on AWS Bedrock, with a hand-rolled subagent runtime (`SubagentPool`) -->
<!--   instead of a heavier orchestration framework. The first package, `graph-wiki-agent`, -->
<!--   is a reimplementation of the `graph-wiki` Claude Code plugin (in this repo) — packaged -->
<!--   as both an MCP server (consumed by the DeepAgents CLI) and a headless CLI ... -->
<!-- "### Constraints": Python 3.11+, `uv` workspace, `langchain-aws` + `langchain-core` + -->
<!--   in-house `subagent-runtime` (asyncio.Semaphore-based fan-out). `deepagents`/`langgraph` -->
<!--   were evaluated and intentionally not adopted ... Format compatibility: must read existing -->
<!--   `graph-wiki` vaults without modification ... -->

<!-- The 5 CONTRAST-SET files (genuine network skipif) — DO NOT TOUCH: -->
<!--   agents/graph-wiki-agent/tests/integration/test_bedrock_iam.py -->
<!--   agents/graph-wiki-agent/tests/integration/test_mcp_e2e.py -->
<!--   agents/graph-wiki-agent/tests/integration/test_query_e2e.py -->
<!--   packages/wiki-io/tests/integration/test_count_tokens_live.py -->
<!--   packages/subagent-runtime/tests/integration/test_pool_bedrock.py -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a self-documenting `# integration-gate-allow` marker to all 7 flagged integration test files</name>
  <files>agents/graph-wiki-agent/tests/integration/test_propose_domains_e2e.py, agents/graph-wiki-agent/tests/integration/test_propose_domains_isolation.py, agents/graph-wiki-agent/tests/integration/test_scan_entity_integration.py, packages/graph-io/tests/integration/test_cluster_cli.py, packages/graph-io/tests/integration/test_e2e_apps.py, packages/graph-io/tests/integration/test_e2e_builtins.py, packages/wiki-io/tests/integration/test_link_rewriter_integration.py</files>
  <read_first>
    - tests/test_integration_gate.py (confirm the gate greps for the literal `# integration-gate-allow` substring anywhere in the file; do NOT modify it)
    - .planning/phases/54-debt-clearance/54-CONTEXT.md (D-01, D-02, D-03 — the locked decision to use the marker + the per-file nature notes)
    - .planning/phases/54-debt-clearance/54-RESEARCH.md (DEBT-01 section — marker form + contrast-set DO-NOT-TOUCH list)
    - each of the 7 target files (to place the marker right after the module docstring / leading comment block, before the first import)
  </read_first>
  <behavior>
    - Each of the 7 files contains a module-level comment line beginning `# integration-gate-allow` with a short inline justification, placed near the top of the file (after the module docstring's closing `"""` and before the first import is the preferred spot).
    - The justification reflects each file's actual nature (use these per-file reasons, drawn from D-03):
      - test_propose_domains_e2e.py → `# integration-gate-allow — LLM stubbed, runs in CI`
      - test_propose_domains_isolation.py → `# integration-gate-allow — local cg update, no LLM`
      - test_scan_entity_integration.py → `# integration-gate-allow — narrator LLM stubbed + cg update monkeypatched`
      - test_cluster_cli.py → `# integration-gate-allow — local only; runtime pytest.skip when code.db missing`
      - test_e2e_apps.py → `# integration-gate-allow — subprocess + local, no LLM`
      - test_e2e_builtins.py → `# integration-gate-allow — subprocess + local, no LLM`
      - test_link_rewriter_integration.py → `# integration-gate-allow — pure local fixture vault`
    - No skipif is added; no test body, import, or logic is changed; only a comment line is inserted.
    - The 5 contrast-set network files are NOT modified.
  </behavior>
  <action>For each of the 7 files listed in `<files>`, insert one comment line beginning `# integration-gate-allow` followed by the per-file justification specified in `<behavior>`. Place it as a standalone line immediately after the module docstring's closing `"""` (every file opens with a docstring) and before the first `import`/`from` statement. For `test_e2e_builtins.py` the docstring is single-line (`"""End-to-end Builtin emission + CLI inspection (Phase 49)."""`); place the marker on the line after it. Do NOT add the canonical `GRAPH_WIKI_RUN_INTEGRATION` skipif — D-01 requires the marker because all 7 are LLM-stubbed or local-only and must keep running in CI. Do NOT modify `tests/test_integration_gate.py`. Do NOT touch any of the 5 contrast-set files named in the interfaces block.</action>
  <verify>
    <automated>uv run pytest tests/test_integration_gate.py</automated>
  </verify>
  <done>
    - All 7 files contain a line matching `# integration-gate-allow` (verify: `grep -rl "# integration-gate-allow" agents/graph-wiki-agent/tests/integration packages/graph-io/tests/integration packages/wiki-io/tests/integration` lists exactly the 7 target files)
    - Each marker has a non-empty inline justification after the prefix
    - `uv run pytest tests/test_integration_gate.py` exits 0
    - `tests/test_integration_gate.py` is unchanged (verify: `git diff --stat tests/test_integration_gate.py` shows no changes)
    - None of the 5 contrast-set files appear in `git status`
  </done>
</task>

<task type="auto">
  <name>Task 2: Confirm the 7 annotated files still collect under pytest</name>
  <files>(no files modified — verification only)</files>
  <read_first>
    - the 7 target files (sanity-check the marker landed before the first import and did not break a docstring)
  </read_first>
  <behavior>
    - All 7 annotated files are still importable/collectable by pytest — the inserted comment did not break a docstring or shift indentation.
  </behavior>
  <action>Run pytest in collection-only mode against the three integration directories that contain the edited files and confirm collection succeeds with no collection errors. This is a guard against a marker accidentally being placed inside a docstring or before required `from __future__ import annotations` lines.</action>
  <verify>
    <automated>uv run pytest --collect-only -q agents/graph-wiki-agent/tests/integration packages/graph-io/tests/integration packages/wiki-io/tests/integration</automated>
  </verify>
  <done>
    - Collection exits 0 with zero collection errors for the three directories
    - The 7 edited files appear in the collected set (no ImportError / SyntaxError)
  </done>
</task>

<task type="auto">
  <name>Task 3: Correct the stale stack description in PROJECT.md "What This Is", "Core Value", and "Constraints"</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md (read the whole file to distinguish in-scope current-tense prose from out-of-scope historical sections)
    - CLAUDE.md (the "## Project" + "### Constraints" + §2 Stack-departure note — canonical corrected wording to mirror, D-08)
    - .planning/phases/54-debt-clearance/54-CONTEXT.md (D-05 through D-09 — exact scope fences)
    - .planning/phases/54-debt-clearance/54-RESEARCH.md (DEBT-02 section — line-level in-scope vs out-of-scope map, DeepAgents-CLI distinction)
  </read_first>
  <behavior>
    - "What This Is" (~L5): the opening sentence no longer says `LangChain/deepagents-based`; it describes LangChain-primitives-based tooling on AWS Bedrock with the in-house `subagent-runtime` / `SubagentPool`. The plugin is described as a reimplementation of the `graph-wiki` Claude Code plugin (not `upstream lattice-wiki`). "consumed by the DeepAgents CLI" is preserved (host product, D-07).
    - "Core Value" (~L9 and ~L11): `upstream lattice-wiki plugin` → `graph-wiki` framing; `today's upstream lattice-wiki librarian` → `the current graph-wiki librarian` (or equivalent), mirroring CLAUDE.md's Core Value line.
    - "Constraints" (~L351): `langchain` + `langchain-aws` + `deepagents` → `langchain-aws` + `langchain-core` + in-house `subagent-runtime` (asyncio.Semaphore-based fan-out); the rationale text no longer claims `deepagents` primitives are leveraged. **Resolution of D-08↔D-09 conflict:** CLAUDE.md's own Constraints bullet mentions "`deepagents`/`langgraph` were evaluated and intentionally not adopted", but D-09's verify forbids the literal `deepagents` token anywhere in the corrected sections. D-09 (the mechanical gate) wins; mirror the *meaning* of CLAUDE.md §2 without the forbidden tokens — phrase the rejection as "a heavier orchestration framework was evaluated and intentionally not adopted; see CLAUDE.md §2 stack-departure note". Do NOT write the literal strings `deepagents` or `langgraph` in this bullet.
    - "Constraints" (~L354): `must read existing lattice-wiki vaults` → `must read existing graph-wiki vaults`.
    - All historical/decision-log sections (Context, Key Decisions, Evolution, prior-milestone narrative, and the L27 v1.10-debt bullet) are byte-for-byte unchanged (D-06).
    - The string "DeepAgents CLI" remains wherever it refers to the MCP host product (D-07).
  </behavior>
  <action>Edit `.planning/PROJECT.md` in three places only, using targeted string edits (never a file-wide `sed`):

1. "What This Is" (line ~5): rewrite the opening clause `A Python monorepo (managed with `uv`) of LangChain/deepagents-based AI tooling.` and the `reimplementation of the upstream `lattice-wiki` Claude Code plugin (being ported in this repo as `graph-wiki`)` clause to mirror CLAUDE.md's "## Project" wording — i.e. `LangChain-primitives-based AI tooling running on AWS Bedrock, with a hand-rolled subagent runtime (`SubagentPool`) instead of a heavier orchestration framework` and `reimplementation of the `graph-wiki` Claude Code plugin (in this repo)`. Keep the `consumed by the DeepAgents CLI` phrase intact.

2. "Core Value" (lines ~9 and ~11): replace `upstream lattice-wiki plugin's` with `graph-wiki plugin's` (or `the graph-wiki plugin's`) and `today's upstream lattice-wiki librarian` with `the current graph-wiki librarian`.

3. "Constraints" (lines ~351 and ~354): replace the Tech-stack bullet's `langchain` + `langchain-aws` + `deepagents` — chosen ... to leverage deepagents' subagent primitives without rebuilding them` with `langchain-aws` + `langchain-core` + in-house `subagent-runtime` (asyncio.Semaphore-based fan-out) — chosen to match Pat's stack; a heavier orchestration framework was evaluated and intentionally not adopted, see CLAUDE.md §2 stack-departure note`. **Do NOT write the literal tokens `deepagents` or `langgraph` in this bullet** — D-09's grep gate forbids them in the corrected sections (this resolves the D-08↔D-09 conflict in favor of the mechanical gate; the meaning still mirrors CLAUDE.md §2). Then replace `existing lattice-wiki vaults` with `existing graph-wiki vaults` in the Format-compatibility bullet.

Do NOT alter the Context, Key Decisions, Evolution, or any prior-milestone narrative sections (D-06). Do NOT remove or rename "DeepAgents CLI" host-product references (D-07). Do NOT touch the L27 debt-milestone bullet that quotes the stale state as the thing being fixed.</action>
  <verify>
    <automated>bash -c 'set -e; awk "/^## / { insec = (\$0==\"## What This Is\" || \$0==\"## Core Value\" || \$0==\"## Constraints\") } insec { print }" .planning/PROJECT.md > /tmp/scope54.txt; if grep -nE "deepagents|lattice-wiki" /tmp/scope54.txt; then echo "STALE STRINGS REMAIN IN SCOPED SECTIONS" >&2; exit 1; fi; grep -q "subagent-runtime" /tmp/scope54.txt && grep -q "langchain-core" /tmp/scope54.txt && grep -q "graph-wiki" /tmp/scope54.txt && echo "SCOPED SECTIONS CORRECTED"'</automated>
  </verify>
  <done>
    - The "What This Is", "Core Value", and "Constraints" sections contain no `deepagents` or `lattice-wiki` strings (case-sensitive; "DeepAgents CLI" is allowed and preserved)
    - Those sections name `subagent-runtime`, `langchain-aws`, `langchain-core`, and `graph-wiki`
    - Historical sections are unchanged (verify: `git diff .planning/PROJECT.md` touches only lines in the three in-scope sections; Context/Key Decisions/Evolution hunks are absent)
    - "DeepAgents CLI" still present (verify: `grep -c "DeepAgents CLI" .planning/PROJECT.md` ≥ 1)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This phase touches only test-file comments and a planning-document's prose. No runtime code, no inputs, no network, no data flow, and no execution surface are introduced or modified.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-01-T1 | Tampering | Over-broad PROJECT.md edit falsifying historical record | mitigate | D-06/D-07 scope fences + the scoped verify (only the three current-tense sections are edited; historical sections and "DeepAgents CLI" host references preserved); `git diff` review in `<done>` confirms no historical hunks. |
| T-54-01-T2 | Denial of Service | Marker placed inside a docstring breaks pytest collection of an integration file | mitigate | Task 2 runs `pytest --collect-only` across the three edited directories as an explicit guard. |
| T-54-01-T3 | Tampering | Marker wrongly added to a genuine network-gated contrast-set file, forcing it into default CI | mitigate | Explicit DO-NOT-TOUCH contrast-set list in the interfaces block + `<done>` check that none of the 5 appear in `git status`. |
</threat_model>

<verification>
- `uv run pytest tests/test_integration_gate.py` exits 0 (DEBT-01)
- `grep -rl "# integration-gate-allow" agents/graph-wiki-agent/tests/integration packages/graph-io/tests/integration packages/wiki-io/tests/integration` lists exactly the 7 D-03 files
- `git diff --stat tests/test_integration_gate.py` shows no changes (gate test untouched, D-04)
- `uv run pytest --collect-only -q agents/graph-wiki-agent/tests/integration packages/graph-io/tests/integration packages/wiki-io/tests/integration` exits 0
- Scoped grep over the "What This Is" / "Core Value" / "Constraints" sections of PROJECT.md returns no `deepagents` / `lattice-wiki` matches and DOES contain `subagent-runtime` + `langchain-core` + `graph-wiki` (DEBT-02, D-09)
- `grep -c "DeepAgents CLI" .planning/PROJECT.md` ≥ 1 (host product preserved, D-07)
- `git diff .planning/PROJECT.md` shows no hunks inside Context / Key Decisions / Evolution sections (D-06)
- None of the 5 contrast-set files appear in `git status`
</verification>

<success_criteria>
- The integration gate test passes (DEBT-01 / ROADMAP SC #1)
- All 7 flagged files carry a self-documenting `# integration-gate-allow` marker; the gate test and the 5 contrast-set files are untouched
- The 7 files still collect under pytest
- PROJECT.md "What This Is", "Core Value", and "Constraints" describe the actual stack (`subagent-runtime` + `langchain-aws` + `langchain-core`, `graph-wiki` naming) with no stale `deepagents`/`lattice-wiki` references in those sections (DEBT-02 / ROADMAP SC #2)
- Historical sections and the "DeepAgents CLI" host-product references are preserved verbatim
</success_criteria>

<output>
Create `.planning/phases/54-debt-clearance/54-01-SUMMARY.md` when done. Summary captures:
- The 7 files marked and the per-file justification used
- Confirmation the gate test exits 0 and was itself unmodified
- The exact PROJECT.md lines changed (old → new) for the three in-scope sections
- Confirmation that historical sections and "DeepAgents CLI" references were preserved (with the `git diff` line-range evidence)
</output>
