---
phase: 22-workspace-api-internal-rename
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/vault-io/src/vault_io/_workspace.py
  - packages/workspace-io/src/workspace_io/config.py
  - packages/workspace-io/src/workspace_io/init.py
  - agents/graph-wiki-agent/src/graph_wiki_agent/commands/init.py
  - agents/graph-wiki-agent/src/graph_wiki_agent/commands/scan.py
  - agents/graph-wiki-agent/src/graph_wiki_agent/commands/lint.py
  - agents/graph-wiki-agent/src/graph_wiki_agent/commands/ingest.py
  - agents/graph-wiki-agent/src/graph_wiki_agent/commands/query.py
  - agents/graph-wiki-agent/src/graph_wiki_agent/commands/log.py
  - agents/graph-wiki-agent/src/graph_wiki_agent/cli.py
  - agents/graph-wiki-agent/src/graph_wiki_mcp/server.py
  - agents/graph-wiki-agent/tests/unit/test_commands_ingest.py
  - agents/graph-wiki-agent/tests/test_command_overrides.py
  - agents/graph-wiki-agent/tests/unit/test_commands_scan.py
  - agents/graph-wiki-agent/tests/integration/test_mcp_cancel.py
  - agents/graph-wiki-agent/tests/unit/test_commands_lint.py
  - agents/graph-wiki-agent/tests/unit/test_commands_log.py
  - agents/graph-wiki-agent/tests/unit/test_query_result.py
  - agents/graph-wiki-agent/tests/test_ingest_trace_unit.py
  - agents/graph-wiki-agent/tests/unit/test_query_code_fallback.py
  - agents/graph-wiki-agent/tests/unit/test_commands_bootstrap.py
  - agents/graph-wiki-agent/tests/test_query_trace_unit.py
  - agents/graph-wiki-agent/tests/unit/test_query_summary_schema_version.py
  - agents/graph-wiki-agent/tests/commands/test_scan_parity.py
  - packages/workspace-io/tests/test_config.py
  - packages/eval-harness/tests/test_sweep.py
autonomous: true
requirements:
  - WSAPI-01
  - WSAPI-02
  - WSAPI-03
  - WSAPI-04
  - WSAPI-05
  - WSAPI-06

must_haves:
  truths:
    - "uv run pytest is green workspace-wide after all renames (ROADMAP SC#1, D-04 single workspace-wide gate)"
    - "resolve_wiki_and_repo(workspace_path=Path('/some/workspace')) returns (wiki_dir(workspace_path), repo_root) without any string concatenation in vault-io (ROADMAP SC#2, D-09 wiki_dir derivation always)"
    - "workspace_io.config.resolve_workspace is importable as a public symbol and is called by workspace_io.init.init() instead of hardcoding repo_root / 'graph-wiki' (ROADMAP SC#3)"
    - "A .graph-wiki.local.yaml containing graph-wiki-directory: /custom/path is silently ignored; one containing workspace-directory: /custom/path is honored (ROADMAP SC#4, D-08 hard-cut yaml key)"
    - "grep -r 'vault_path' agents/graph-wiki-agent/src packages/workspace-io/src returns 0 hits (ROADMAP SC#5)"
    - "No test file passes vault_path= as a kwarg to any run_* command or to resolve_wiki_and_repo"
    - "The f-string concatenation Path(f'{workspace_path}' + '/wiki').resolve() does not appear anywhere in packages/vault-io/src (D-02)"
    - "resolve_wiki_and_repo walks up from Path.cwd() (NOT workspace_path) when repo_path is not specified (D-05)"
    - "resolve_wiki_and_repo signature is exactly (workspace_path: Path | None = None, repo_path: Path | None = None) -> tuple[Path, Path | None] (D-06)"
    - "No back-compat shim and no deprecation period for the vault_path kwarg anywhere in this phase (D-07)"
    - "vault-io package directory and vault_io module path remain unchanged in this phase (D-10)"
    - "MCP server Pydantic Field names remain vault_path (Phase 23 owns that); only internal Python kwarg calls flip"
    - "Typer flag literals '--vault' remain in cli.py (Phase 23 owns that); only the local-variable + kwarg names flip"
    - "Plan structure honors D-01 (adopt the 5 unstaged WIP files on main as foundation), D-03 (single big-bang plan + commit), and D-11 (Phase 25 owns packages-dir-misclassification — explicitly not folded here)"
  artifacts:
    - path: "packages/vault-io/src/vault_io/_workspace.py"
      provides: "resolve_wiki_and_repo with workspace_path / repo_path signature, wiki_dir() derivation, CWD-based repo discovery"
      contains: "def resolve_wiki_and_repo(\n    workspace_path: Path | None = None,\n    repo_path: Path | None = None,\n) -> tuple[Path, Path | None]"
    - path: "packages/workspace-io/src/workspace_io/config.py"
      provides: "WORKSPACE_DIRECTORY_KEY constant + public resolve_workspace function"
      contains: "WORKSPACE_DIRECTORY_KEY = \"workspace-directory\""
    - path: "packages/workspace-io/src/workspace_io/init.py"
      provides: "init() that routes default workspace through resolve_workspace(repo_root)"
      contains: "resolve_workspace(repo_root=repo_root)"
    - path: "agents/graph-wiki-agent/src/graph_wiki_agent/commands/init.py"
      provides: "run_init with workspace_path + repo_path kwargs"
      contains: "workspace_path: Path | None = None"
    - path: "agents/graph-wiki-agent/src/graph_wiki_agent/commands/scan.py"
      provides: "run_scan with workspace_path kwarg (repo_path passthrough preserved)"
      contains: "workspace_path: Path | None = None"
    - path: "agents/graph-wiki-agent/src/graph_wiki_agent/commands/lint.py"
      provides: "run_lint with workspace_path kwarg"
      contains: "workspace_path: Path | None = None"
    - path: "agents/graph-wiki-agent/src/graph_wiki_agent/commands/ingest.py"
      provides: "run_ingest_source + run_ingest_work_item with workspace_path kwarg"
      contains: "workspace_path: Path | None = None"
    - path: "agents/graph-wiki-agent/src/graph_wiki_agent/commands/query.py"
      provides: "run_query with workspace_path kwarg"
      contains: "workspace_path: Path | None = None"
    - path: "agents/graph-wiki-agent/src/graph_wiki_agent/commands/log.py"
      provides: "run_log with workspace_path kwarg"
      contains: "workspace_path: Path | None = None"
  key_links:
    - from: "agents/graph-wiki-agent/src/graph_wiki_agent/commands/*.py"
      to: "packages/vault-io/src/vault_io/_workspace.py::resolve_wiki_and_repo"
      via: "resolve_wiki_and_repo(workspace_path) call"
      pattern: "resolve_wiki_and_repo\\(workspace_path"
    - from: "packages/workspace-io/src/workspace_io/init.py"
      to: "packages/workspace-io/src/workspace_io/config.py::resolve_workspace"
      via: "import + call with repo_root kwarg"
      pattern: "resolve_workspace\\(repo_root"
    - from: "agents/graph-wiki-agent/src/graph_wiki_agent/cli.py"
      to: "agents/graph-wiki-agent/src/graph_wiki_agent/commands/*.py::run_*"
      via: "Python kwarg workspace_path=workspace_path (flag literal --vault unchanged)"
      pattern: "workspace_path=workspace_path"
    - from: "agents/graph-wiki-agent/src/graph_wiki_mcp/server.py"
      to: "agents/graph-wiki-agent/src/graph_wiki_agent/commands/*.py::run_*"
      via: "Python kwarg workspace_path=... (Pydantic field input.vault_path unchanged)"
      pattern: "workspace_path="
---

<objective>
Hard-rename the internal Python API surface from `vault_path` to `workspace_path` in one big-bang atomic commit (per D-03). This covers all 6 WSAPI requirements: the `resolve_wiki_and_repo` signature in vault-io, all 6 `run_*` command signatures, every in-tree caller (CLI + MCP server internals), the ~70-mock-point test sweep, the `.graph-wiki.local.yaml` key hard-cut, and the `resolve_workspace` promotion.

Purpose: Eliminate naming drift between "vault" (legacy term) and "workspace" (canonical term per v1.4 milestone). After this phase, the internal Python contract is consistent end-to-end and Phase 23 (external surfaces) can land on a stable foundation.

Output: One commit, workspace-wide `uv run pytest` green, zero `vault_path` hits in `agents/graph-wiki-agent/src` + `packages/workspace-io/src`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-workspace-api-internal-rename/22-CONTEXT.md
@.planning/phases/22-workspace-api-internal-rename/22-PATTERNS.md

<interfaces>
<!-- Key types and contracts extracted from codebase. Executor uses these directly — no exploration required. -->

From packages/vault-io/src/vault_io/_workspace.py (TARGET signature after WSAPI-01 + D-02 + D-05 + D-06):

```python
def resolve_wiki_and_repo(
    workspace_path: Path | None = None,
    repo_path: Path | None = None,
) -> tuple[Path, Path | None]:
    """Return (wiki_path, repo_root).

    Priority:
    1. ``workspace_path`` argument if provided — short-circuit. When ``repo_path``
       is not supplied, walk up from ``Path.cwd()`` to find the repo root.
    2. ``GRAPH_WIKI_WORKSPACE`` env var (via ``workspace_io.config.resolve``).
    3. ``.graph-wiki.yaml`` walk-up from cwd (via ``workspace_io.config.resolve``).
    4. Raises ``RuntimeError`` — names ``graph-wiki-agent bootstrap <path>`` as fix.
    """
    if workspace_path is not None:
        return _ws_paths.wiki_dir(workspace_path), repo_path or _find_repo_root(Path.cwd())
    cfg = _ws_config.resolve()
    return _ws_paths.wiki_dir(cfg.workspace), cfg.repo_root
```

From packages/workspace-io/src/workspace_io/paths.py (already correct — DO NOT modify):

```python
def wiki_dir(workspace_path: Path) -> Path: ...
```

From packages/workspace-io/src/workspace_io/config.py (TARGET symbols after WSAPI-05):

```python
WORKSPACE_DIRECTORY_KEY = "workspace-directory"

def resolve_workspace(repo_root: Path, ...) -> Path: ...  # promoted from _resolve_workspace
```

From `agents/graph-wiki-agent/src/graph_wiki_agent/commands/init.py` (TEMPLATE for run_* renames; WIP already matches D-06):

```python
async def run_init(
    *,
    topic: str,
    tool: str,
    force: bool,
    workspace_path: Path | None = None,
    repo_path: Path | None = None,
) -> InitResult:
    ...
    repo_root = repo_path if repo_path is not None else Path.cwd()
    _ws_init(repo_root, workspace=workspace_path, ...)
    wiki, repo = resolve_wiki_and_repo(workspace_path, repo_root)
```

Mechanical run_* transform pattern (applied uniformly to scan/lint/ingest/query/log):

- Replace parameter `vault_path: Path | None = None` with `workspace_path: Path | None = None`
- Replace call `resolve_wiki_and_repo(vault_path)` with `resolve_wiki_and_repo(workspace_path)`
- Sweep docstring occurrences of `vault_path` term (executor discretion on docstring style)
- DO NOT add `repo_path: Path | None = None` to scan/lint/ingest/query/log — that is init.py-only per PATTERNS.md Pattern A note. (scan.py already has its own pre-existing repo_path parameter — leave untouched.)

MCP server (server.py) per-call-site transform — Pydantic Field name UNCHANGED:

```python
# Pydantic field — STAYS (Phase 23):
class WikiScanInput(BaseModel):
    vault_path: str = Field("", description="...")  # UNCHANGED

# Handler local var reading the field — STAYS:
vault = Path(input.vault_path) if input.vault_path else None  # UNCHANGED

# Internal Python kwarg call — FLIPS:
await run_scan(workspace_path=vault, ...)  # was vault_path=vault
```

CLI (cli.py) per-subcommand transform — Typer flag literal `"--vault"` UNCHANGED:

```python
# Typer option binding — flag literal "--vault" STAYS (Phase 23); only the Python parameter name flips:
workspace: str = typer.Option("", "--vault", help="...")  # "--vault" UNCHANGED

# Local var name — flips for readability:
workspace_path = Path(workspace) if workspace else None  # was vault_path

# Internal Python kwarg call — FLIPS:
result = asyncio.run(run_scan(workspace_path=workspace_path, ...))  # was vault_path=...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adopt + fix vault-io/_workspace.py per D-02 + D-05 + D-06</name>
  <files>packages/vault-io/src/vault_io/_workspace.py</files>
  <read_first>
    - packages/vault-io/src/vault_io/_workspace.py (the WIP file on `main`; 80% renamed, needs D-02 + D-05 fixes)
    - packages/workspace-io/src/workspace_io/paths.py (the `wiki_dir()` function to use)
    - packages/workspace-io/src/workspace_io/config.py::_find_repo_root (the helper called for CWD walk-up)
    - .planning/phases/22-workspace-api-internal-rename/22-CONTEXT.md (D-02, D-05, D-06)
    - .planning/phases/22-workspace-api-internal-rename/22-PATTERNS.md §"packages/vault-io/src/vault_io/_workspace.py — central rename"
  </read_first>
  <action>
    Bring `_workspace.py` to the target shape per D-02 + D-05 + D-06. Concretely: (1) Replace the f-string hack `Path(f"{workspace_path}" + "/wiki").resolve()` on the WIP `if workspace_path is not None:` branch with `_ws_paths.wiki_dir(workspace_path)` (the `_ws_paths` module is already imported). (2) Replace `_find_repo_root(workspace_path)` in that same branch with `_find_repo_root(Path.cwd())` per D-05 (callers that only know workspace fall back to CWD-based repo discovery, NOT workspace-based). (3) Sweep the three remaining `vault_path` term occurrences in the docstring (the WIP still mentions `vault_path` on 3 lines per PATTERNS.md). (4) Confirm signature is exactly `resolve_wiki_and_repo(workspace_path: Path | None = None, repo_path: Path | None = None) -> tuple[Path, Path | None]` per D-06. Do NOT add new imports — `Path`, `_ws_paths`, `_find_repo_root` are already imported in the WIP. Do NOT change the env-var / config fallback branch.
  </action>
  <verify>
    <automated>cd /Users/pat/Personal/deep-agents/.claude/worktrees/phase-22-discuss && grep -n "vault_path\|f\"{workspace_path}\"" packages/vault-io/src/vault_io/_workspace.py | grep -v '^[0-9]*:#' ; test $(grep -c "vault_path" packages/vault-io/src/vault_io/_workspace.py) -eq 0 && test $(grep -c "wiki_dir(workspace_path)" packages/vault-io/src/vault_io/_workspace.py) -ge 1 && test $(grep -c "_find_repo_root(Path.cwd())" packages/vault-io/src/vault_io/_workspace.py) -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "vault_path" packages/vault-io/src/vault_io/_workspace.py` returns 0
    - `grep -c "f\"{workspace_path}\"" packages/vault-io/src/vault_io/_workspace.py` returns 0
    - File contains the exact string `_ws_paths.wiki_dir(workspace_path)` at least once
    - File contains the exact string `_find_repo_root(Path.cwd())` exactly once
    - Signature line reads `def resolve_wiki_and_repo(` followed by `workspace_path: Path | None = None,` and `repo_path: Path | None = None,` and returns `tuple[Path, Path | None]`
    - No new imports added (file imports are unchanged from WIP)
  </acceptance_criteria>
  <done>resolve_wiki_and_repo uses wiki_dir() (no string concat), walks up from cwd for repo discovery when repo_path omitted, and contains zero `vault_path` references.</done>
</task>

<task type="auto">
  <name>Task 2: Adopt workspace-io/config.py (WSAPI-05) + workspace-io/init.py (WSAPI-06)</name>
  <files>packages/workspace-io/src/workspace_io/config.py, packages/workspace-io/src/workspace_io/init.py</files>
  <read_first>
    - packages/workspace-io/src/workspace_io/config.py (WIP on `main`; constant renamed + `_resolve_workspace` promoted)
    - packages/workspace-io/src/workspace_io/init.py (WIP on `main`; routes default workspace through `resolve_workspace`)
    - .planning/phases/22-workspace-api-internal-rename/22-CONTEXT.md (D-08, WSAPI-05, WSAPI-06)
    - .planning/phases/22-workspace-api-internal-rename/22-PATTERNS.md §"workspace_io/config.py" and §"workspace_io/init.py"
  </read_first>
  <action>
    For `config.py`: accept the WIP changes as-is — the constant rename `LATTICE_DIRECTORY_KEY` → `WORKSPACE_DIRECTORY_KEY` (value `"workspace-directory"`), the public `resolve_workspace` promotion, and the `bootstrap` term update in the error message. Additionally sweep the stray `graph-wiki-directory` mention in the module-level docstring (line ~4 per PATTERNS.md). Hard-cut: no fallback shim that reads the old `graph-wiki-directory` key — files with the old key silently fall back to the default workspace (D-08). For `init.py`: accept the WIP `from workspace_io.config import resolve_workspace` import, the `workspace = resolve_workspace(repo_root=repo_root)` line, AND delete the stray commented-out line `# workspace = repo_root / "graph-wiki"` left by the WIP (PATTERNS.md deviation #4). Grep-confirm no other module imports `LATTICE_DIRECTORY_KEY` or `_resolve_workspace`; if any consumers exist outside these two files, update them to import `WORKSPACE_DIRECTORY_KEY` / `resolve_workspace` respectively.
  </action>
  <verify>
    <automated>cd /Users/pat/Personal/deep-agents/.claude/worktrees/phase-22-discuss && test $(grep -c "LATTICE_DIRECTORY_KEY" packages/workspace-io/src/workspace_io/config.py) -eq 0 && test $(grep -c "WORKSPACE_DIRECTORY_KEY" packages/workspace-io/src/workspace_io/config.py) -ge 1 && test $(grep -c "^def resolve_workspace\|^def _resolve_workspace" packages/workspace-io/src/workspace_io/config.py) -eq 1 && grep -q "def resolve_workspace" packages/workspace-io/src/workspace_io/config.py && test $(grep -c "graph-wiki-directory" packages/workspace-io/src/workspace_io/config.py) -eq 0 && grep -q "resolve_workspace(repo_root=repo_root)" packages/workspace-io/src/workspace_io/init.py && test $(grep -c '# workspace = repo_root / "graph-wiki"' packages/workspace-io/src/workspace_io/init.py) -eq 0 && test $(grep -rn "LATTICE_DIRECTORY_KEY\|_resolve_workspace" packages/ agents/ | grep -v '\.pyc' | wc -l) -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `config.py`: zero hits for `LATTICE_DIRECTORY_KEY`; at least one hit for `WORKSPACE_DIRECTORY_KEY`
    - `config.py`: exactly one public top-level `def resolve_workspace(` (no leading underscore); zero `_resolve_workspace` definitions
    - `config.py`: zero `graph-wiki-directory` occurrences (constant value is `"workspace-directory"` and docstring is swept)
    - `init.py`: contains exact string `resolve_workspace(repo_root=repo_root)`
    - `init.py`: zero hits for the stray commented line `# workspace = repo_root / "graph-wiki"`
    - Workspace-wide: zero remaining references to `LATTICE_DIRECTORY_KEY` or `_resolve_workspace` in `packages/` and `agents/` source trees (excluding compiled `.pyc` files)
  </acceptance_criteria>
  <done>config.py exports WORKSPACE_DIRECTORY_KEY + public resolve_workspace; init.py routes default workspace through resolve_workspace(repo_root); no stragglers.</done>
</task>

<task type="auto">
  <name>Task 3: Rename vault_path→workspace_path in 5 sibling run_* commands (scan, lint, ingest, query, log)</name>
  <files>agents/graph-wiki-agent/src/graph_wiki_agent/commands/scan.py, agents/graph-wiki-agent/src/graph_wiki_agent/commands/lint.py, agents/graph-wiki-agent/src/graph_wiki_agent/commands/ingest.py, agents/graph-wiki-agent/src/graph_wiki_agent/commands/query.py, agents/graph-wiki-agent/src/graph_wiki_agent/commands/log.py</files>
  <read_first>
    - agents/graph-wiki-agent/src/graph_wiki_agent/commands/init.py (the WIP template — `run_init` already has the target signature shape)
    - agents/graph-wiki-agent/src/graph_wiki_agent/commands/scan.py (line ~227 signature; line ~269 resolver call; existing repo_path param at line ~230 is PRESERVED)
    - agents/graph-wiki-agent/src/graph_wiki_agent/commands/lint.py (line ~499 signature; line ~525 resolver call)
    - agents/graph-wiki-agent/src/graph_wiki_agent/commands/ingest.py (TWO functions: `run_ingest_source` ~line 369, `run_ingest_work_item` ~line 538)
    - agents/graph-wiki-agent/src/graph_wiki_agent/commands/query.py (line ~802 signature for `run_query`)
    - agents/graph-wiki-agent/src/graph_wiki_agent/commands/log.py (line ~36 signature; line ~59 resolver call)
    - .planning/phases/22-workspace-api-internal-rename/22-PATTERNS.md §"Pattern A: `run_*` kwarg-only rename"
    - .planning/phases/22-workspace-api-internal-rename/22-CONTEXT.md (WSAPI-02, D-07)
  </read_first>
  <action>
    Apply Pattern A uniformly to all 5 sibling commands. For each `run_*` function (6 total across these 5 files — ingest.py has two): (1) rename the kwarg-only parameter `vault_path: Path | None = None` to `workspace_path: Path | None = None` in the signature. (2) rename the resolver call `resolve_wiki_and_repo(vault_path)` to `resolve_wiki_and_repo(workspace_path)`. (3) sweep the term `vault_path` from docstrings (executor discretion on rewording — the term must be purged but the description can be paraphrased). Do NOT add `repo_path: Path | None = None` to any of these 5 files — that param is init.py-only per PATTERNS.md. Do NOT touch private helpers in query.py (`_discover_pages`, `_cosine_search_sqlite`, `_resolve_repo_root` — these are internal helpers per PATTERNS.md "Out-of-scope siblings" and stay with `vault_path` param names; PATTERNS.md explicitly excludes them from this phase's boundary). Do NOT modify `scan.py`'s pre-existing `repo_path: Path | None = None` parameter — it is unrelated to this rename. For ingest.py, apply the transform to BOTH `run_ingest_source` and `run_ingest_work_item`.
  </action>
  <verify>
    <automated>cd /Users/pat/Personal/deep-agents/.claude/worktrees/phase-22-discuss && for f in scan.py lint.py ingest.py query.py log.py; do echo "== $f =="; grep -n "vault_path" "agents/graph-wiki-agent/src/graph_wiki_agent/commands/$f" || echo "clean (boundary)"; done ; test $(grep -E "^(async )?def run_(scan|lint|ingest_source|ingest_work_item|query|log)\b" agents/graph-wiki-agent/src/graph_wiki_agent/commands/{scan,lint,ingest,query,log}.py -A 12 | grep -c "workspace_path: Path | None = None") -eq 6 && test $(grep -E "^(async )?def run_(scan|lint|ingest_source|ingest_work_item|query|log)\b" agents/graph-wiki-agent/src/graph_wiki_agent/commands/{scan,lint,ingest,query,log}.py -A 12 | grep -c "vault_path: Path | None = None") -eq 0 && test $(grep -E "resolve_wiki_and_repo\(workspace_path\)" agents/graph-wiki-agent/src/graph_wiki_agent/commands/{scan,lint,ingest,query,log}.py | wc -l) -ge 6</automated>
  </verify>
  <acceptance_criteria>
    - Every `run_*` function signature across scan.py / lint.py / ingest.py (×2) / query.py / log.py contains `workspace_path: Path | None = None` (6 occurrences total across the 5 files)
    - Zero of those signatures contain `vault_path: Path | None = None`
    - Every `run_*` function body contains exact string `resolve_wiki_and_repo(workspace_path)` instead of `resolve_wiki_and_repo(vault_path)` (≥6 occurrences across the 5 files)
    - `scan.py`'s pre-existing `repo_path: Path | None = None` parameter is unchanged
    - Private helpers in query.py (`_discover_pages`, `_cosine_search_sqlite`, `_resolve_repo_root`) retain their `vault_path` parameter names (out-of-scope per PATTERNS.md)
    - The remaining `vault_path` references in the 5 files (if any) are ONLY in those query.py private helpers
  </acceptance_criteria>
  <done>All 5 sibling run_* command boundaries use workspace_path; init.py already matches; the rename pattern is uniform across all 6 run_* functions.</done>
</task>

<task type="auto">
  <name>Task 4: Rename CLI Typer entry kwarg calls + MCP server internal kwarg calls (WSAPI-03)</name>
  <files>agents/graph-wiki-agent/src/graph_wiki_agent/cli.py, agents/graph-wiki-agent/src/graph_wiki_mcp/server.py</files>
  <read_first>
    - agents/graph-wiki-agent/src/graph_wiki_agent/cli.py (WIP `bootstrap` subcommand ~lines 439-453 — the template; 5 other subcommands at lines ~389, ~424, ~471, ~507, ~532, ~568 still need the rename)
    - agents/graph-wiki-agent/src/graph_wiki_mcp/server.py (6 handler functions; call sites at lines ~125/154/196/243/309/375 per PATTERNS.md)
    - .planning/phases/22-workspace-api-internal-rename/22-PATTERNS.md §"cli.py — Typer entry kwarg rename only" and §"server.py — Pydantic field STAYS, kwarg flips"
    - .planning/phases/22-workspace-api-internal-rename/22-CONTEXT.md (D-07, integration points caveat)
  </read_first>
  <action>
    **cli.py:** Replicate the WIP `bootstrap` pattern to the 5 remaining subcommands (`query`, `log`, `scan`, `ingest_source`, `ingest_work_item`, `lint`). For each: (1) rename the Typer parameter name `vault` is already done in WIP for bootstrap; verify and apply to siblings — if a sibling still has Python parameter `vault: str = typer.Option("", "--vault", ...)`, rename to `workspace: str = typer.Option("", "--vault", ...)` keeping the flag literal `"--vault"` EXACTLY unchanged (Phase 23 owns the flag literal). (2) rename the local variable `vault_path = Path(vault) if vault else None` to `workspace_path = Path(workspace) if workspace else None`. (3) rename every `run_X(vault_path=vault_path, ...)` call to `run_X(workspace_path=workspace_path, ...)`. (4) for positional call sites in `query` (line ~389) and `ingest_source` (line ~507) per PATTERNS.md, rename the local var only; the positional argument stays positional. Help-text wording may keep mentioning "vault"/"--vault" (Phase 23 owns external wording). **server.py:** For each of the 6 MCP handler functions, leave the Pydantic Field name `vault_path: str = Field(...)` UNCHANGED (Phase 23 owns this). Leave the local var read `vault = Path(input.vault_path) if input.vault_path else None` UNCHANGED (it reads the Pydantic field). Flip ONLY the internal Python kwarg in the `await run_X(...)` call from `vault_path=vault` to `workspace_path=vault` (the local var name `vault` is executor discretion — keep as-is to minimize churn).
  </action>
  <verify>
    <automated>cd /Users/pat/Personal/deep-agents/.claude/worktrees/phase-22-discuss && test $(grep -c "vault_path=" agents/graph-wiki-agent/src/graph_wiki_agent/cli.py) -eq 0 && test $(grep -c "vault_path=" agents/graph-wiki-agent/src/graph_wiki_mcp/server.py) -eq 0 && test $(grep -c '"--vault"' agents/graph-wiki-agent/src/graph_wiki_agent/cli.py) -ge 6 && test $(grep -c "vault_path: str = Field" agents/graph-wiki-agent/src/graph_wiki_mcp/server.py) -ge 6 && test $(grep -c "workspace_path=" agents/graph-wiki-agent/src/graph_wiki_agent/cli.py) -ge 5 && test $(grep -c "workspace_path=" agents/graph-wiki-agent/src/graph_wiki_mcp/server.py) -ge 6</automated>
  </verify>
  <acceptance_criteria>
    - `cli.py`: zero occurrences of `vault_path=` as a Python kwarg
    - `cli.py`: ≥6 occurrences of the Typer flag literal `"--vault"` (Phase 23 owns the flag rename — must stay)
    - `cli.py`: ≥5 occurrences of `workspace_path=` as a Python kwarg in `run_*` calls
    - `server.py`: zero occurrences of `vault_path=` as a Python kwarg (the kwarg in `await run_X(...)` calls)
    - `server.py`: ≥6 occurrences of `vault_path: str = Field(` (Pydantic Field declarations — Phase 23 owns this rename, must stay)
    - `server.py`: ≥6 occurrences of `workspace_path=` as a Python kwarg
    - The Pydantic field access expression `input.vault_path` may still appear (reading the unchanged field)
  </acceptance_criteria>
  <done>CLI and MCP server pass workspace_path= to run_* commands; external surfaces (Typer flag literals, Pydantic field names) untouched and ready for Phase 23.</done>
</task>

<task type="auto">
  <name>Task 5: Test-mock sweep — rename vault_path= kwargs in ~14 test files (WSAPI-04)</name>
  <files>agents/graph-wiki-agent/tests/unit/test_commands_ingest.py, agents/graph-wiki-agent/tests/test_command_overrides.py, agents/graph-wiki-agent/tests/unit/test_commands_scan.py, agents/graph-wiki-agent/tests/integration/test_mcp_cancel.py, agents/graph-wiki-agent/tests/unit/test_commands_lint.py, agents/graph-wiki-agent/tests/unit/test_commands_log.py, agents/graph-wiki-agent/tests/unit/test_query_result.py, agents/graph-wiki-agent/tests/test_ingest_trace_unit.py, agents/graph-wiki-agent/tests/unit/test_query_code_fallback.py, agents/graph-wiki-agent/tests/unit/test_commands_bootstrap.py, agents/graph-wiki-agent/tests/test_query_trace_unit.py, agents/graph-wiki-agent/tests/unit/test_query_summary_schema_version.py, agents/graph-wiki-agent/tests/commands/test_scan_parity.py, packages/eval-harness/tests/test_sweep.py</files>
  <read_first>
    - .planning/phases/22-workspace-api-internal-rename/22-PATTERNS.md §"Test files (mock-point + kwarg sweep)" and §"Pattern B: Test mock-point rename"
    - .planning/phases/22-workspace-api-internal-rename/22-CONTEXT.md (D-04 workspace-wide pytest gate, eval-harness ripple note)
    - agents/graph-wiki-agent/tests/unit/test_query_summary_schema_version.py (the file with a `lambda vault_path=None: ...` mock side-effect that also needs renaming per PATTERNS.md line 84 note)
    - Each test file listed in files_modified before editing it
  </read_first>
  <action>
    Sweep every `vault_path=` kwarg in calls to `run_init`, `run_scan`, `run_lint`, `run_ingest_source`, `run_ingest_work_item`, `run_query`, `run_log` across all 14 listed test files. The rename is mechanical: `run_X(..., vault_path=<expr>, ...)` becomes `run_X(..., workspace_path=<expr>, ...)`. Specifics: (1) DO NOT change `patch("graph_wiki_agent.commands.X.resolve_wiki_and_repo", ...)` target strings — the function name is unchanged per PATTERNS.md Pattern B. (2) DO rename `lambda vault_path=None: (...)` mock side-effect signatures to `lambda workspace_path=None: (...)` — at least one such occurrence in test_query_summary_schema_version.py per PATTERNS.md. (3) For positional calls (some tests may pass the path positionally), leave positional usage alone — only kwarg-form calls flip. (4) DO NOT rename pytest fixture names like `vault_path` or `fixture_vault_path` in `conftest.py` (out of scope per PATTERNS.md "Out-of-scope siblings"). (5) For `packages/eval-harness/tests/test_sweep.py`, rename the single `run_query(... vault_path=wt.path)` kwarg to `workspace_path=wt.path` — but leave `run_sweep(..., fixture_vault_path, ...)` alone (Phase 24 owns eval-harness internals). (6) For `packages/vault-io/tests/test_ports_importable.py`, do NOT modify — its `def test_resolve_wiki_and_repo_*` are test function names (identifiers), not mock points (PATTERNS.md "NOT IN SCOPE"). The sweep target is ~30 kwarg call sites; the ~50 `patch(...)` target strings are stable and do not change.
  </action>
  <verify>
    <automated>cd /Users/pat/Personal/deep-agents/.claude/worktrees/phase-22-discuss && test $(grep -rn "vault_path=" agents/graph-wiki-agent/tests/ packages/eval-harness/tests/test_sweep.py 2>/dev/null | grep -v "fixture_vault_path\|fixture(vault_path" | wc -l) -eq 0 && test $(grep -rn "lambda vault_path=" agents/graph-wiki-agent/tests/ | wc -l) -eq 0 && test $(grep -c "patch(\"graph_wiki_agent.commands" agents/graph-wiki-agent/tests/unit/test_commands_ingest.py) -ge 1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "vault_path=" agents/graph-wiki-agent/tests/` returns 0 hits, excluding any line that uses fixture-name expressions like `fixture_vault_path` (the fixture name itself is out of scope)
    - `grep -rn "vault_path=" packages/eval-harness/tests/test_sweep.py` returns 0 hits for `run_query` call sites (the `fixture_vault_path` fixture reference may remain, since Phase 24 owns it)
    - Zero occurrences of `lambda vault_path=` anywhere in the test tree (the side-effect lambda signatures are renamed)
    - `patch(...)` target strings of form `"graph_wiki_agent.commands.<cmd>.resolve_wiki_and_repo"` are UNCHANGED in count and string content (mock points stable per Pattern B)
    - `packages/vault-io/tests/test_ports_importable.py` is unmodified (test function names containing `resolve_wiki_and_repo` are identifiers, not in scope)
  </acceptance_criteria>
  <done>All command kwarg call sites in tests use workspace_path=; mock-point patch() strings unchanged; lambda mock signatures renamed; eval-harness test_sweep.py single kwarg fixed for D-04 gate.</done>
</task>

<task type="auto">
  <name>Task 6: YAML literal sweep — packages/workspace-io/tests/test_config.py (D-08)</name>
  <files>packages/workspace-io/tests/test_config.py</files>
  <read_first>
    - packages/workspace-io/tests/test_config.py (lines ~49, ~58, ~70 — the 3 YAML literals to rename per PATTERNS.md)
    - packages/workspace-io/tests/test_local_config.py (DO NOT modify — PATTERNS.md flags this as out of scope; 13 YAML literals here are opaque parser test data, not key semantics)
    - .planning/phases/22-workspace-api-internal-rename/22-CONTEXT.md (D-08 hard-cut)
    - .planning/phases/22-workspace-api-internal-rename/22-PATTERNS.md §"Pattern C: YAML literal rename"
  </read_first>
  <action>
    In `packages/workspace-io/tests/test_config.py`, rename the YAML key literal in 3 string locations (approximately lines 49, 58, 70 per PATTERNS.md — confirm by reading file). The transform is: `"graph-wiki-directory: {elsewhere}\n"` → `"workspace-directory: {elsewhere}\n"` (and the same for any other f-string that emits this YAML key). These tests exercise the `.graph-wiki.local.yaml` override behavior; under D-08 hard-cut, the override only works under the new key. DO NOT modify `packages/workspace-io/tests/test_local_config.py` — its 13 occurrences of `graph-wiki-directory` are opaque YAML-reader test data and stay (PATTERNS.md out-of-scope row).
  </action>
  <verify>
    <automated>cd /Users/pat/Personal/deep-agents/.claude/worktrees/phase-22-discuss && test $(grep -c "graph-wiki-directory" packages/workspace-io/tests/test_config.py) -eq 0 && test $(grep -c "workspace-directory" packages/workspace-io/tests/test_config.py) -ge 3 && test $(grep -c "graph-wiki-directory" packages/workspace-io/tests/test_local_config.py) -ge 1</automated>
  </verify>
  <acceptance_criteria>
    - `packages/workspace-io/tests/test_config.py`: zero occurrences of `graph-wiki-directory`
    - `packages/workspace-io/tests/test_config.py`: ≥3 occurrences of `workspace-directory` (the renamed YAML key literals)
    - `packages/workspace-io/tests/test_local_config.py`: still contains its original ≥1 occurrences of `graph-wiki-directory` (unchanged — out of scope)
  </acceptance_criteria>
  <done>test_config.py uses the new workspace-directory key for override-behavior tests; test_local_config.py opaque-parser data left intact.</done>
</task>

<task type="auto">
  <name>Task 7: Workspace-wide pytest gate + final grep audit (D-04 + ROADMAP SC#1 + SC#5)</name>
  <files></files>
  <read_first>
    - .planning/ROADMAP.md §"Phase 22" success criteria (SC#1 and SC#5)
    - .planning/phases/22-workspace-api-internal-rename/22-CONTEXT.md (D-04 single quality gate)
  </read_first>
  <action>
    Run the workspace-wide gate per D-04: `uv run pytest` from the repo root. The full workspace pytest suite MUST pass — no per-package gating. Then run the grep audit per ROADMAP SC#5: `grep -rn "vault_path" agents/graph-wiki-agent/src packages/workspace-io/src` (this excludes test files, eval-harness internals, vault-io's `vault_path` field name in scan_monorepo.py — all Phase 23/24 territory — and the MCP server's Pydantic field declarations which Phase 23 owns). If any hit appears in those two directory trees that is NOT a Pydantic Field declaration in `graph_wiki_mcp/server.py` (which is allowed to stay until Phase 23), investigate and resolve. The MCP server file lives under `agents/graph-wiki-agent/src/graph_wiki_mcp/`, so its 6 `vault_path: str = Field(...)` declarations WILL show in the grep — these are the only permitted residual hits in that subtree. Document the expected residual count and confirm it matches the 6 Pydantic field declarations. If `uv run pytest` reveals a failure that is NOT a kwarg-rename issue (i.e., a pre-existing flake), surface it but do not paper over by rewriting the gate.
  </action>
  <verify>
    <automated>cd /Users/pat/Personal/deep-agents/.claude/worktrees/phase-22-discuss && uv run pytest && test $(grep -rn "vault_path" agents/graph-wiki-agent/src packages/workspace-io/src | grep -v "vault_path: str = Field" | grep -v "input.vault_path" | wc -l) -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest` (from repo root) exits 0
    - `grep -rn "vault_path" agents/graph-wiki-agent/src packages/workspace-io/src` produces output where every hit is either a Pydantic `vault_path: str = Field(...)` declaration in `graph_wiki_mcp/server.py` OR a `input.vault_path` Pydantic field access in the same file (Phase 23 territory)
    - Filtering out those allowed hits yields zero remaining `vault_path` references
    - No regression in test count or pass rate vs. the baseline before the phase
  </acceptance_criteria>
  <done>Workspace-wide pytest green; vault_path absent from in-scope source trees; only Phase 23-owned Pydantic Field declarations + Pydantic field accesses remain (allowlisted).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This phase is a pure internal Python API rename. No new network surface, no new auth path, no new user-input parser. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Tampering | `resolve_wiki_and_repo` semantic regression (e.g., D-05 walk from cwd vs workspace_path missed by tests) | mitigate | Workspace-wide `uv run pytest` gate per D-04 covers every caller; ROADMAP SC#5 grep audit ensures no caller passes the old kwarg name |
| T-22-02 | Denial of Service | Silent fallback when `.graph-wiki.local.yaml` uses the old `graph-wiki-directory` key (D-08 hard cut) — user loses their custom workspace path without an error | accept | Documented in release notes per WSAPI-05; matches Phase 20 `--config` deletion precedent. User-impact assessed: zero affected users (solo developer, no shipped consumers). |
| T-22-03 | Repudiation | Atomic big-bang commit makes `git bisect` less granular for any future regression | accept | D-03 trade-off explicitly accepted; intermediate states are uncompilable, so splitting buys nothing. |
| T-22-04 | Information Disclosure | (none) | n/a | No new I/O surface. |
| T-22-SC | Tampering | npm/pip/cargo installs | n/a | No new package installs in this phase. RESEARCH.md §"Package Legitimacy Audit" not required — this is a pure refactor. |
</threat_model>

<verification>
After all 7 tasks complete:

1. `uv run pytest` from repo root: exits 0, full workspace suite green
2. `grep -rn "vault_path" agents/graph-wiki-agent/src packages/workspace-io/src | grep -v "vault_path: str = Field" | grep -v "input.vault_path"` returns zero lines
3. `grep -n "f\"{workspace_path}\"" packages/vault-io/src/vault_io/_workspace.py` returns zero lines (D-02 fix confirmed)
4. `grep -n "_find_repo_root(Path.cwd())" packages/vault-io/src/vault_io/_workspace.py` returns exactly one line (D-05 fix confirmed)
5. `python -c "from workspace_io.config import resolve_workspace, WORKSPACE_DIRECTORY_KEY; print(resolve_workspace, WORKSPACE_DIRECTORY_KEY)"` succeeds (public symbol export confirmed)
6. `grep -n "graph-wiki-directory" packages/workspace-io/src/workspace_io/config.py` returns zero lines
7. `grep -rn "LATTICE_DIRECTORY_KEY\|_resolve_workspace" packages/ agents/` returns zero non-`.pyc` hits
8. CLI flag literal preservation: `grep -c '"--vault"' agents/graph-wiki-agent/src/graph_wiki_agent/cli.py` returns ≥6 (Phase 23 boundary preserved)
9. MCP Pydantic field preservation: `grep -c "vault_path: str = Field" agents/graph-wiki-agent/src/graph_wiki_mcp/server.py` returns ≥6 (Phase 23 boundary preserved)
</verification>

<success_criteria>
**From ROADMAP §"Phase 22" (5 criteria) + D-locked decisions:**

1. ✅ `uv run pytest` is green workspace-wide; no test references `vault_path=` as a kwarg in any command mock or call site (ROADMAP SC#1)
2. ✅ `resolve_wiki_and_repo(workspace_path=Path("/some/workspace"))` returns `(wiki_dir(workspace_path), repo_root)` without string concatenation inside the vault-io layer (ROADMAP SC#2 + D-02 + D-09)
3. ✅ `workspace_io.config.resolve_workspace` is importable as a public symbol and is called by `workspace_io.init.init()` instead of hardcoding `repo_root / "graph-wiki"` (ROADMAP SC#3 + WSAPI-06)
4. ✅ A `.graph-wiki.local.yaml` containing `graph-wiki-directory: /custom/path` is silently ignored; one containing `workspace-directory: /custom/path` is honored (ROADMAP SC#4 + D-08)
5. ✅ `grep -r "vault_path" agents/graph-wiki-agent/src packages/workspace-io/src` produces no hits outside the Phase 23-owned allowlist (Pydantic Field declarations + `input.vault_path` field accesses in `graph_wiki_mcp/server.py`) (ROADMAP SC#5)

**Additional gates derived from CONTEXT.md decisions:**

6. ✅ `_find_repo_root(Path.cwd())` appears exactly once in `_workspace.py`; `_find_repo_root(workspace_path)` does not appear (D-05)
7. ✅ Signature `resolve_wiki_and_repo(workspace_path: Path | None = None, repo_path: Path | None = None) -> tuple[Path, Path | None]` (D-06)
8. ✅ One atomic commit covering all 6 WSAPI requirements + the test sweep (D-03)
9. ✅ No back-compat shim, no deprecation period (D-07)
10. ✅ `vault-io` package directory and `vault_io` module path unchanged (D-10)
</success_criteria>

<output>
Create `.planning/phases/22-workspace-api-internal-rename/22-01-SUMMARY.md` when done summarizing:
- Files touched (count from `git diff --name-only`)
- WSAPI requirements satisfied (WSAPI-01..06)
- Pytest result + the 9 verification grep outputs (commands + exit codes)
- Any executor-discretion deviations (e.g., docstring rewording choices, whether private query.py helpers were also swept)
- Confirmation that Phase 23-owned surfaces (Typer `--vault` flag literals, MCP Pydantic Field names) are intentionally preserved
</output>
