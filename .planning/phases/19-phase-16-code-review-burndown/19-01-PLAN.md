---
phase: 19-phase-16-code-review-burndown
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/eval-harness/src/eval_harness/divergence/synthesizer.py
  - packages/eval-harness/src/eval_harness/divergence/code_reader.py
autonomous: true
requirements: [REVIEW-01]
must_haves:
  truths:
    - "`[[bedrock]]` and `[[subagent-pool]]` wikilinks no longer satisfy the `_SLUG_ONLY_RE` slug-only check in synthesizer"
    - "Inline path references like `vault/.graph-wiki/foo` are matched by the graph-wiki prefix regex in code_reader"
    - "Bare-filename citations such as `pool.py:115` qualify as `path:line` in `_PATH_LINE_RE`"
  artifacts:
    - path: "packages/eval-harness/src/eval_harness/divergence/synthesizer.py"
      provides: "Synthesizer with target-path slug check"
      contains: "not in target"
    - path: "packages/eval-harness/src/eval_harness/divergence/code_reader.py"
      provides: "code_reader with permissive `_PATH_LINE_RE` + tightened `_GRAPH_WIKI_PREFIX_RE` lookbehind"
      contains: "(?<![A-Za-z0-9_-])\\.graph-wiki/"
  key_links:
    - from: "synthesizer.py `_SLUG_ONLY_RE` usage"
      to: "wikilink target-path test"
      via: "presence-of-`/` check on the link target"
      pattern: "if .*/.* in target"
    - from: "code_reader.py `_PATH_LINE_RE`"
      to: "bare-filename citation matching"
      via: "regex without mandatory `/`"
      pattern: "_PATH_LINE_RE"
---

<objective>
Land three divergence-eval regex fixes from CONTEXT.md D-01, D-02, D-03. All three live under `packages/eval-harness/src/eval_harness/divergence/` and close silent-pass gaps in slug-only detection, `.graph-wiki/` prefix detection inside inline path references, and bare-filename `path:line` citation detection.

Purpose: Close three warning-severity false negatives in the divergence eval pipeline that let bad wikilinks and unscoped citations slip past the existing checks.
Output: Two source files updated; existing `packages/eval-harness/tests/` regression suite passes; no new test files unless a true silent-pass is uncovered (per D-18 exception).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-phase-16-code-review-burndown/19-CONTEXT.md
@.planning/milestones/v1.2-phases/16-carry-forward-debt-cleanup/16-REVIEW.md

@packages/eval-harness/src/eval_harness/divergence/synthesizer.py
@packages/eval-harness/src/eval_harness/divergence/code_reader.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-01 — replace `_SLUG_ONLY_RE` with target-path check in synthesizer</name>
  <files>packages/eval-harness/src/eval_harness/divergence/synthesizer.py</files>
  <read_first>
    - packages/eval-harness/src/eval_harness/divergence/synthesizer.py (full file; locate `_SLUG_ONLY_RE` definition and its use at line ~58)
    - .planning/milestones/v1.2-phases/16-carry-forward-debt-cleanup/16-REVIEW.md (WR-01 narrative + proposed fix)
    - packages/eval-harness/tests/ (grep for `_SLUG_ONLY_RE` and `synthesizer` to find existing coverage)
  </read_first>
  <action>
    Per CONTEXT.md D-01: replace the `_SLUG_ONLY_RE` regex-based slug-only detection in `synthesizer.py` with a presence-of-`/` check on the wikilink target. The target qualifies as "slug-only" when it does not contain a path separator. Remove the `_SLUG_ONLY_RE` module-level constant if it has no other callers (verify via in-file grep). Preserve surrounding behavior: callers that previously branched on `_SLUG_ONLY_RE.match(target)` now branch on the equivalent path-separator check. Do not introduce dead imports (e.g., `re` may now be removable — check before deleting).

    If the existing `packages/eval-harness/tests/` suite does not cover lowercase/hyphenated slug targets (`[[bedrock]]`, `[[subagent-pool]]`), add one targeted assertion to the existing relevant test file per D-18 exception. Do not create a new test file.
  </action>
  <verify>
    <automated>uv sync && uv run pytest packages/eval-harness/tests/ -m "not integration" -x</automated>
  </verify>
  <done>
    - `grep -n "_SLUG_ONLY_RE" packages/eval-harness/src/eval_harness/divergence/synthesizer.py` returns 0 hits OR returns only a removed/replaced reference (i.e., constant is gone or unused).
    - The slug-only branch in synthesizer keys off "no `/` in target" (grep finds the new check, e.g., `"/" not in target` or equivalent).
    - `uv run pytest packages/eval-harness/tests/ -m "not integration"` exits 0.
  </done>
  <acceptance_criteria>
    - `synthesizer.py` no longer defines `_SLUG_ONLY_RE` (or defines it but it is unreferenced) — confirmed via grep.
    - The new check is a string-level `/`-containment test on the wikilink target, NOT a regex.
    - All three known slug cases pass through the slug-only branch: `[[Bedrock]]`, `[[bedrock]]`, `[[subagent-pool]]`. Any wikilink target containing `/` (e.g., `[[wiki/Bedrock]]`) does NOT take the slug-only branch.
    - `uv run pytest packages/eval-harness/tests/ -m "not integration"` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: D-02 + D-03 — tighten prefix regex + loosen path-line regex in code_reader</name>
  <files>packages/eval-harness/src/eval_harness/divergence/code_reader.py</files>
  <read_first>
    - packages/eval-harness/src/eval_harness/divergence/code_reader.py (full file; locate `_PATH_LINE_RE` at line ~21-23 and `_GRAPH_WIKI_PREFIX_RE` at line ~31)
    - .planning/milestones/v1.2-phases/16-carry-forward-debt-cleanup/16-REVIEW.md (WR-02 and WR-03 narratives + proposed fixes)
    - packages/eval-harness/src/eval_harness/divergence/synthesizer.py (sibling file — for `_PATH_LINE_RE` permissiveness alignment per D-03)
    - packages/eval-harness/tests/ (grep `code_reader` and the two regex names to find existing coverage)
  </read_first>
  <action>
    Per CONTEXT.md D-02: update `_GRAPH_WIKI_PREFIX_RE` to use a tightened negative lookbehind so inline path references like `vault/.graph-wiki/...` (where the `.graph-wiki` is preceded by `/`) still match. Target lookbehind is `(?<![A-Za-z0-9_-])\.graph-wiki/` — the previous lookbehind incorrectly excluded these cases.

    Per CONTEXT.md D-03: relax `_PATH_LINE_RE` to drop the mandatory `/` requirement so bare-filename `path:line` citations (`pool.py:115`) qualify. Align with the synthesizer's permissive regex shape — the path-segment portion no longer requires an interior `/`. Verb-check the resulting regex against both forms: `pool.py:115` (bare) and `packages/subagent-runtime/src/subagent_runtime/pool.py:115` (qualified).

    Do not introduce dead imports; if `re` flags or helpers become unused, remove them.

    If existing `packages/eval-harness/tests/` does not cover (a) `vault/.graph-wiki/foo` matching for `_GRAPH_WIKI_PREFIX_RE` or (b) bare-filename `pool.py:115` matching for `_PATH_LINE_RE`, add one targeted assertion to the existing relevant test file per D-18 exception. Do not stand up a new test file.
  </action>
  <verify>
    <automated>uv sync && uv run pytest packages/eval-harness/tests/ -m "not integration" -x</automated>
  </verify>
  <done>
    - `grep -n "_GRAPH_WIKI_PREFIX_RE" packages/eval-harness/src/eval_harness/divergence/code_reader.py` shows the lookbehind contains the literal substring `[A-Za-z0-9_-]`.
    - `grep -n "_PATH_LINE_RE" packages/eval-harness/src/eval_harness/divergence/code_reader.py` shows the path portion does NOT require an interior `/`.
    - `uv run pytest packages/eval-harness/tests/ -m "not integration"` exits 0.
  </done>
  <acceptance_criteria>
    - `_GRAPH_WIKI_PREFIX_RE` matches inline path references like `vault/.graph-wiki/foo` (verifiable by adding a Python REPL assertion: `bool(_GRAPH_WIKI_PREFIX_RE.search("vault/.graph-wiki/foo"))` is True).
    - `_GRAPH_WIKI_PREFIX_RE` does NOT match identifier-bearing contexts like `mygraph-wiki/foo` (the new lookbehind still excludes alphanumeric/underscore/hyphen prefixes — verifiable: `bool(_GRAPH_WIKI_PREFIX_RE.search("mygraph-wiki/foo"))` is False).
    - `_PATH_LINE_RE` matches `pool.py:115` (bare filename). Verifiable: `bool(_PATH_LINE_RE.search("pool.py:115"))` is True.
    - `_PATH_LINE_RE` continues to match qualified paths (`packages/foo/bar.py:42`).
    - `uv run pytest packages/eval-harness/tests/ -m "not integration"` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | All edits are internal regex tweaks in the eval harness; no untrusted input crosses any boundary as a result of this change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | (n/a) | `packages/eval-harness/src/eval_harness/divergence/*` | accept | Regex tightening / loosening only. No new attack surface; no untrusted-input parsing change; eval harness runs against repo-local fixtures and recorded outputs |
| T-19-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies installed by this plan |
</threat_model>

<verification>
- `uv run pytest packages/eval-harness/tests/ -m "not integration"` exits 0 after each task and at plan completion.
- Manual grep gates pass (see `<done>` blocks).
- No new dependencies introduced (`uv.lock` unchanged for this plan).
</verification>

<success_criteria>
- D-01, D-02, D-03 all implemented per CONTEXT.md decisions.
- Per-commit gate (D-18) passes at plan completion: `uv sync && uv run pytest packages/eval-harness/tests/ packages/subagent-runtime/tests/ agents/graph-wiki-agent/tests/ -m "not integration"` exits 0.
- 19-REVIEW-BURNDOWN.md rows for WR-01, WR-02, WR-03 will be populated by the plan-final commit step (with commit SHA) — that plan owns the table itself.
</success_criteria>

<output>
Create `.planning/phases/19-phase-16-code-review-burndown/19-01-SUMMARY.md` when done.
Record commit SHA for use by the 19-REVIEW-BURNDOWN.md table plan (plan 05).
</output>
