---
phase: 19-phase-16-code-review-burndown
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/graph-wiki-agent/tests/integration/test_trace_coverage.py
  - packages/eval-harness/src/eval_harness/divergence/metric.py
  - packages/subagent-runtime/tests/test_trace_io.py
  - packages/eval-harness/tests/test_models_toml_sweep_candidates.py
autonomous: true
requirements: [REVIEW-01, REVIEW-02]
must_haves:
  truths:
    - "The empty/empty-disclaimer record in `test_trace_coverage.py` is counted as covered (mirrors error-record exemption)"
    - "`Union` is no longer imported from `typing` in `divergence/metric.py`"
    - "`test_write_trace_record_swallows_oserror` asserts a WARNING-level log is emitted via `caplog`"
    - "`test_models_toml_sweep_candidates.py` module docstring matches the asserted 5–6 candidate range"
  artifacts:
    - path: "agents/graph-wiki-agent/tests/integration/test_trace_coverage.py"
      provides: "Loosened assertion with empty/empty-fallback exemption"
      contains: "tokens_in is None and tokens_out is None"
    - path: "packages/eval-harness/src/eval_harness/divergence/metric.py"
      provides: "No dead `Union` import"
      contains: "metric.py"
    - path: "packages/subagent-runtime/tests/test_trace_io.py"
      provides: "Strengthened OSError swallow test with caplog assertion"
      contains: "caplog"
    - path: "packages/eval-harness/tests/test_models_toml_sweep_candidates.py"
      provides: "Module docstring matches asserted candidate range"
      contains: "5"
  key_links:
    - from: "test_trace_coverage.py disclaimer exemption"
      to: "existing error-record exemption logic"
      via: "parallel structure (mirror existing branch)"
      pattern: "tokens_in is None and tokens_out is None"
    - from: "test_trace_io.py caplog assertion"
      to: "write_trace_record OSError-swallow path"
      via: "`with caplog.at_level('WARNING')` + record-message check"
      pattern: "caplog.at_level"
---

<objective>
Land four test + integration fixes from CONTEXT.md D-04 (WR-04), D-09 (IN-03), D-10 (IN-04), and D-13 (IN-07). All four touch test files or test-adjacent surface — no production logic changes here.

Purpose: Loosen one fragile integration assertion (D-04), remove a dead import (D-09), strengthen one test's contract (D-10), and correct one stale module docstring (D-13).
Output: Four files updated; existing test suite + per-commit gate pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-phase-16-code-review-burndown/19-CONTEXT.md
@.planning/milestones/v1.2-phases/16-carry-forward-debt-cleanup/16-REVIEW.md

@agents/graph-wiki-agent/tests/integration/test_trace_coverage.py
@packages/eval-harness/src/eval_harness/divergence/metric.py
@packages/subagent-runtime/tests/test_trace_io.py
@packages/subagent-runtime/src/subagent_runtime/trace_io.py
@packages/eval-harness/tests/test_models_toml_sweep_candidates.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-04 — exempt empty-fallback path in test_trace_coverage assertion</name>
  <files>agents/graph-wiki-agent/tests/integration/test_trace_coverage.py</files>
  <read_first>
    - agents/graph-wiki-agent/tests/integration/test_trace_coverage.py (full file; locate the query-summary assertion at lines ~88-95 and the existing error-record exemption logic above/around it)
    - .planning/milestones/v1.2-phases/16-carry-forward-debt-cleanup/16-REVIEW.md (WR-04 narrative + proposed fix)
  </read_first>
  <action>
    Per CONTEXT.md D-04: extend the existing error-record exemption in the query-summary assertion at lines 88-95 to also exempt the disclaimer/empty-fallback path. When a record has `tokens_in is None and tokens_out is None`, count it as covered and continue — mirror the parallel branch structure already used for the error-record exemption.

    Do not introduce a new helper unless one already exists. Match the existing exemption's style (same conditional shape, same `continue`/`count += 1` semantics — whichever the surrounding code uses).
  </action>
  <verify>
    <automated>uv sync && uv run pytest agents/graph-wiki-agent/tests/ -m "not integration" -x</automated>
  </verify>
  <done>
    - `grep -n "tokens_in is None and tokens_out is None" agents/graph-wiki-agent/tests/integration/test_trace_coverage.py` returns a hit in the relevant assertion block.
    - The new exemption sits parallel to the existing error-record exemption (visually and structurally).
    - `uv run pytest agents/graph-wiki-agent/tests/ -m "not integration"` exits 0 (integration test itself stays gated).
  </done>
  <acceptance_criteria>
    - The empty/empty-disclaimer record is now counted (or `continue`d past) in the same way as the existing error-record exemption.
    - No other assertion in the file is loosened — surgical change only.
    - Per-commit gate still passes.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: D-09 — remove dead `Union` import from divergence/metric.py</name>
  <files>packages/eval-harness/src/eval_harness/divergence/metric.py</files>
  <read_first>
    - packages/eval-harness/src/eval_harness/divergence/metric.py (full file; locate the `from typing import Union` line at ~31 and confirm via in-file grep that `Union` has no references)
    - .planning/milestones/v1.2-phases/16-carry-forward-debt-cleanup/16-REVIEW.md (IN-03 narrative)
  </read_first>
  <action>
    Per CONTEXT.md D-09: remove the unused `Union` import from `packages/eval-harness/src/eval_harness/divergence/metric.py`. The module uses PEP-604 `A | B` syntax elsewhere. Per CLAUDE.md hard-cut philosophy, do not retain dead imports.

    If `Union` is the only symbol on its `from typing import ...` line, delete the whole line. If it's part of a multi-symbol import, drop only `Union` and preserve the remaining imports.

    Run `grep -n "Union" packages/eval-harness/src/eval_harness/divergence/metric.py` after the change — must return 0 hits.
  </action>
  <verify>
    <automated>uv sync && uv run pytest packages/eval-harness/tests/ -m "not integration" -x</automated>
  </verify>
  <done>
    - `grep -c "Union" packages/eval-harness/src/eval_harness/divergence/metric.py` returns `0`.
    - `uv run pytest packages/eval-harness/tests/ -m "not integration"` exits 0.
  </done>
  <acceptance_criteria>
    - `Union` is completely absent from `metric.py` (no import, no annotation usage).
    - All other imports preserved.
    - Per-commit gate passes.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: D-10 — add caplog WARNING assertion to test_write_trace_record_swallows_oserror</name>
  <files>packages/subagent-runtime/tests/test_trace_io.py</files>
  <read_first>
    - packages/subagent-runtime/tests/test_trace_io.py (full file; locate `test_write_trace_record_swallows_oserror`)
    - packages/subagent-runtime/src/subagent_runtime/trace_io.py (the `write_trace_record` function — confirm it logs at WARNING level on OSError and confirm the docstring's promise)
    - .planning/milestones/v1.2-phases/16-carry-forward-debt-cleanup/16-REVIEW.md (IN-04 narrative)
  </read_first>
  <action>
    Per CONTEXT.md D-10: extend `test_write_trace_record_swallows_oserror` to use the `caplog` pytest fixture to assert that `write_trace_record` emits a WARNING-level log record when it swallows the OSError. Follow the standard pattern from CONTEXT.md `<code_context>`: `with caplog.at_level("WARNING"):` wrap, then `assert any(<expected fragment> in r.message for r in caplog.records)` after.

    Add `caplog` to the test function's parameter list. Pick a stable substring from the actual log message in `trace_io.py::write_trace_record` (read the source to find the exact phrasing) — the assertion fragment must match what the production code emits.

    Do not change the production code. Do not add a new test function; extend the existing one.
  </action>
  <verify>
    <automated>uv sync && uv run pytest packages/subagent-runtime/tests/test_trace_io.py::test_write_trace_record_swallows_oserror -xvs</automated>
  </verify>
  <done>
    - `grep -n "caplog" packages/subagent-runtime/tests/test_trace_io.py` shows `caplog` is in the test's parameter list and is used inside `test_write_trace_record_swallows_oserror`.
    - The test asserts presence of a WARNING-level record whose message contains a fragment also present in `trace_io.py`'s warning log call.
    - `uv run pytest packages/subagent-runtime/tests/test_trace_io.py::test_write_trace_record_swallows_oserror -xvs` exits 0.
  </done>
  <acceptance_criteria>
    - Test passes (production code unchanged).
    - Test now asserts both: (a) the OSError is swallowed (existing assertion), and (b) a WARNING-level log record is emitted with the expected message substring (new assertion).
    - Per-commit gate passes.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: D-13 — correct module docstring in test_models_toml_sweep_candidates.py</name>
  <files>packages/eval-harness/tests/test_models_toml_sweep_candidates.py</files>
  <read_first>
    - packages/eval-harness/tests/test_models_toml_sweep_candidates.py (full file; locate the module docstring at line ~10 stating "3 vault-thin cases" and confirm the actual asserted range in the test bodies is 5–6)
    - .planning/milestones/v1.2-phases/16-carry-forward-debt-cleanup/16-REVIEW.md (IN-07 narrative)
  </read_first>
  <action>
    Per CONTEXT.md D-13: update the module docstring at line ~10 from "3 vault-thin cases" to wording that matches the actually-asserted 5–6 range in the test bodies. Read the test functions in the file to confirm the actual count/range before deciding the exact phrasing. The new wording should mention the 5–6 range explicitly so a future reader can reconcile the docstring with the assertion.

    Surgical edit — do not rewrite other docstring content, do not edit tests themselves.
  </action>
  <verify>
    <automated>uv sync && uv run pytest packages/eval-harness/tests/test_models_toml_sweep_candidates.py -x</automated>
  </verify>
  <done>
    - `grep -n "vault-thin" packages/eval-harness/tests/test_models_toml_sweep_candidates.py` shows the docstring no longer says "3" — it states a 5–6 range matching the asserted values.
    - `uv run pytest packages/eval-harness/tests/test_models_toml_sweep_candidates.py` exits 0.
  </done>
  <acceptance_criteria>
    - Docstring at line ~10 mentions the correct asserted count/range (5–6, sourced from the actual test bodies).
    - No test logic changes.
    - Per-commit gate passes.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | All edits are test loosening / strengthening / docstring corrections / dead-import removal; no production code path or attack surface is changed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-04 | (n/a) | test + dead-import surface | accept | No production behavior change; D-10 strengthens an existing contract assertion (improves coverage), others are zero-risk corrections |
| T-19-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies installed by this plan |
</threat_model>

<verification>
- Per-commit gate at plan close: `uv sync && uv run pytest packages/eval-harness/tests/ packages/subagent-runtime/tests/ agents/graph-wiki-agent/tests/ -m "not integration"` exits 0.
- Manual grep gates pass (see `<done>` blocks).
</verification>

<success_criteria>
- D-04, D-09, D-10, D-13 implemented per CONTEXT.md decisions.
- Per-commit gate passes.
- 19-REVIEW-BURNDOWN.md rows for WR-04, IN-03, IN-04, IN-07 will be populated by plan 05.
</success_criteria>

<output>
Create `.planning/phases/19-phase-16-code-review-burndown/19-03-SUMMARY.md` when done.
Record commit SHA for plan 05's burndown table.
</output>
