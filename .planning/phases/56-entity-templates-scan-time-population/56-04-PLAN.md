---
phase: 56-entity-templates-scan-time-population
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/graph-io/src/graph_io/packages.py
  - packages/graph-io/tests/test_packages.py
autonomous: true
requirements: [SCAN-02]

must_haves:
  truths:
    - "D-06: `_read_pyproject()` (packages.py:46-72) adds `\"description\": project.get(\"description\", \"\")` to the returned manifest `info` dict, sourcing the value from pyproject `[project].description`"
    - "D-06: in `refresh()`, the per-manifest node `attrs` dict (packages.py:159-164) gains `\"description\": info.get(\"description\", \"\")`, so every emitted `package`/`app` GraphNode carries the description in its attrs_json (and the opposite-kind in-place UPDATE at 176-191 picks it up via attrs_for_db)"
    - "D-05 uniformity: after this change, `package`/`app` nodes carry `attrs[\"description\"]` the same way `domain` nodes already do (domains.py:258-259), so wiki-io's `scanner_frontmatter_for_node` can read `node.attrs.get(\"description\")` uniformly across kinds"
    - "Non-overlap with Phase 55: this plan touches ONLY the manifest-parse region (46-72) and the per-manifest attrs-build region (159-164) of packages.py; it does NOT touch the dep-accumulation pre-pass/loop (218-235) or the edge-emission section (259-272) that 55-01-PLAN.md edits — the two phases edit disjoint regions of refresh()"
    - "Sequencing: 55-01-PLAN.md edits the SAME FILE (packages.py); this plan's graph-io task MUST execute AFTER Phase 55 (at minimum after 55-01 is committed) to avoid an edit race on packages.py"
    - "Empty/absent handling: when pyproject has no `[project].description`, `info[\"description\"]` is the empty string and `attrs[\"description\"]` is empty — the empty case is the SCAN-02 TODO-fallback trigger handled downstream in wiki-io (Plan 01), NOT here; graph-io stores empty string faithfully and does not synthesize a placeholder"
    - "No describe_* change required: the description flows to wiki-io via the NodeRecord attrs returned by list_packages/list_apps; PackageDescription/AppDescription/describe_package/describe_app are NOT modified (optional symmetry only, out of scope here)"
    - "No schema migration: `description` is a free-text key inside the existing attrs_json blob — no new column, no schema.py change"
    - "Self-documenting (Pat preference): a brief inline comment marks the description-population as the SCAN-02 source so a future reader does not prune it as unused (it is consumed cross-package by wiki-io)"
  artifacts:
    - path: packages/graph-io/src/graph_io/packages.py
      provides: "description parsed from pyproject and stored in package/app node attrs (SCAN-02 source, D-06)"
      contains: "description"
    - path: packages/graph-io/tests/test_packages.py
      provides: "unit test: pyproject [project].description lands in the package node attrs_json; absent description -> empty string"
      contains: "description"
  key_links: []
---

<objective>
Populate `attrs["description"]` on `package`/`app` graph nodes from pyproject
`[project].description`, so the SCAN-02 `summary:` derivation in wiki-io (Plan 01) has a real
description to read uniformly across kinds (D-05/D-06). This is a small, additive cross-package
graph-io change confined to the manifest-parse and node-attrs-build regions of
`packages.refresh()`.

Purpose: today only `domain` nodes carry `attrs["description"]`; `package`/`app` nodes do not, so
SCAN-02's "summary derived from the node's description" has nothing to read for packages/apps.
D-06 originates the description at the graph-io scan layer where pyproject is already parsed.

Output: `_read_pyproject()` surfaces `description`; `refresh()` stores it on each package/app
node's attrs; a unit test proves it lands in attrs_json (and absent → empty string). No schema
migration; no `describe_*` change; no overlap with Phase 55's edits to the same function.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/56-entity-templates-scan-time-population/56-CONTEXT.md
@.planning/phases/56-entity-templates-scan-time-population/56-RESEARCH.md
@.planning/phases/55-dependency-classification-fix/55-01-PLAN.md
@packages/graph-io/CLAUDE.md

<interfaces>
<!-- packages.py regions (current line numbers):
       _read_pyproject() 46-72: returns info dict {name, version, dependencies, dep_groups,
         language, scripts_present} — ADD "description": project.get("description", "")
       refresh() per-manifest attrs build 159-164:
         attrs = {version, dependencies, language, uri}  (+ app_kind/app_signals for apps)
         -> ADD attrs["description"] = info.get("description", "")
       node emission 193-213 reads `attrs`; opposite-kind UPDATE 176-191 uses attrs_for_db
         (= attrs minus uri) — both pick up the new key automatically. -->

<!-- Phase 55 region map (DISJOINT — see 55-01-PLAN.md):
       55-01 edits: workspace-name pre-pass (new, before 218), dep loop 218-235,
                    edge-emission 259-272. It does NOT touch 46-72 or 159-164. -->

<!-- Domains already do this (the pattern to mirror): domains.py:258-259 copies
     description into the domain node attrs; DomainDescription.description = queries.py:90-95. -->

<!-- attrs_json is the existing free-text blob; description is a new KEY, not a column.
     No schema.py / migration change. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Parse and store pyproject description on package/app nodes in refresh()</name>
  <files>packages/graph-io/src/graph_io/packages.py</files>
  <read_first>
    - packages/graph-io/src/graph_io/packages.py:46-72 (`_read_pyproject` info-dict shape)
    - packages/graph-io/src/graph_io/packages.py:129-213 (`refresh()` per-manifest attrs build + node emission + opposite-kind UPDATE)
    - packages/graph-io/src/graph_io/domains.py:258-259 (the existing description-on-node pattern to mirror)
    - .planning/phases/55-dependency-classification-fix/55-01-PLAN.md (confirm the disjoint regions it edits — do not collide)
    - .planning/phases/56-entity-templates-scan-time-population/56-CONTEXT.md (D-05, D-06)
    - packages/graph-io/CLAUDE.md (updates run inside the caller's transaction)
  </read_first>
  <behavior>
    - `_read_pyproject()` returns an `info` dict that now includes
      `"description": project.get("description", "")` (empty string when absent). Optionally add
      the same to `_read_package_json()` via `data.get("description", "")` for JS uniformity —
      include only if it does not complicate the diff.
    - In `refresh()`, the per-manifest `attrs` dict gains
      `"description": info.get("description", "")` so the value is serialized into the node's
      attrs_json for both the freshly emitted node and the opposite-kind in-place UPDATE path
      (which builds `attrs_for_db` from the same `attrs`).
    - A brief inline comment marks this as the SCAN-02 / D-06 source consumed cross-package by
      wiki-io, so it is not pruned as locally-unused.
    - No change to `classify()`, URIs, dep accumulation, edges, `describe_*`, or `schema.py`.
  </behavior>
  <action>Edit `packages/graph-io/src/graph_io/packages.py` only. (1) In `_read_pyproject()` (46-72), add `"description": project.get("description", "")` to the returned dict. (2) In `refresh()`'s per-manifest `attrs` dict (159-164), add `attrs["description"] = info.get("description", "")` with a one-line comment noting it is the SCAN-02 source (D-06) read cross-package by wiki-io. Do NOT edit the dep-accumulation loop (218-235), the workspace-name pre-pass, or the edge-emission section (259-272) — those are Phase 55's regions. Do NOT modify schema.py or any describe_* function.</action>
  <verify>
    <automated>uv run --package graph-io pytest packages/graph-io/tests/test_packages.py -q</automated>
  </verify>
  <done>
    - `packages.py` `_read_pyproject` return dict contains the `description` key
    - `refresh()` sets `attrs["description"]` (grep `description` in packages.py shows both the parse and the attrs assignment with the SCAN-02 comment)
    - `schema.py` is unchanged (no migration)
    - existing `test_packages.py` tests still pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Add a unit test that pyproject description lands in the package node attrs</name>
  <files>packages/graph-io/tests/test_packages.py</files>
  <read_first>
    - packages/graph-io/tests/test_packages.py (the `conn` fixture, `_seed_file_node`, `_CTX`, and a manifest-writing test like `test_dependency_ingestion_from_workspace` to mirror)
    - packages/graph-io/src/graph_io/packages.py (the refreshed `_read_pyproject`/`refresh` from Task 1)
    - .planning/phases/56-entity-templates-scan-time-population/56-CONTEXT.md (D-06)
  </read_first>
  <behavior>
    - A new test writes a `pyproject.toml` under `tmp_path` with a `[project].description`, calls
      `packages.refresh(conn, repo_root=tmp_path, ctx=_CTX)`, then asserts the package node's
      attrs_json contains the description (parse attrs_json from the `nodes` row and assert
      `attrs["description"] == "<the value>"`).
    - A second assertion (same or separate test) writes a pyproject WITHOUT a description and
      asserts `attrs["description"] == ""` (faithful empty, not a synthesized placeholder).
    - Mirror the existing manifest-fixture + SQL-assert style; reuse `conn`, `_seed_file_node`,
      `_CTX`. Coordinate to be purely additive alongside Phase 55's new test functions in the same
      file (do not edit theirs).
  </behavior>
  <action>Add new test function(s) to `packages/graph-io/tests/test_packages.py` mirroring the existing manifest-fixture tests. Write a `pyproject.toml` with `[project] name=... version=... description="A test package."` under `tmp_path`, call `packages.refresh(conn, repo_root=tmp_path, ctx=_CTX)`, query `SELECT attrs_json FROM nodes WHERE kind='package' AND name=?`, parse JSON, and assert `attrs["description"] == "A test package."`. Add a no-description case asserting `attrs["description"] == ""`. Use the existing fixtures and SQL-assert style. Do not modify existing tests.</action>
  <verify>
    <automated>uv run --package graph-io pytest packages/graph-io/tests/test_packages.py -q</automated>
  </verify>
  <done>
    - New test function(s) assert the description lands in the package node attrs_json (present and empty-string cases)
    - `grep -n "description" packages/graph-io/tests/test_packages.py` matches the new assertions
    - `uv run --package graph-io pytest packages/graph-io/tests/test_packages.py -q` exits 0
    - Existing tests unmodified (git diff shows only additions)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan touches only graph-io's manifest-scanning path (`refresh()` + `_read_pyproject`), which
reads local repo pyproject files already present and writes a new free-text key into the local
sqlite graph inside the caller's transaction. No network, no untrusted input, no new execution
surface, no schema change.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-04-T1 | Tampering | Edit race with Phase 55 on the same packages.py / refresh() | mitigate | This plan edits disjoint regions (46-72, 159-164) from Phase 55 (pre-pass + 218-235 + 259-272); the plan mandates executing AFTER Phase 55 lands; both changes are small and additive. |
| T-56-04-I1 | Information Disclosure | A package description could carry sensitive text into the graph/wiki | accept | The description is the author's own pyproject `[project].description`, already public in the repo; storing it in the local graph discloses nothing new. |
</threat_model>

<verification>
- `uv run --package graph-io pytest packages/graph-io/tests/test_packages.py -q` exits 0
- After `refresh()` over a workspace whose pyproject has `[project].description`, the package node's attrs_json contains that exact description string (D-06)
- A pyproject without a description yields `attrs["description"] == ""` (no synthesized placeholder; TODO fallback is wiki-io's job)
- `schema.py` unchanged (no migration)
- The edit is confined to packages.py lines 46-72 and 159-164 — no change to the dep-accumulation loop or edge-emission section that Phase 55 edits
</verification>

<success_criteria>
- ROADMAP SC#2 (graph side): package/app graph nodes carry a real description in attrs, sourced
  from pyproject, so the wiki-io scanner can populate `summary:` uniformly across kinds
  (SCAN-02 / D-05 / D-06).
- The change is additive and disjoint from Phase 55's edits to the same function; sequenced to run
  after Phase 55 lands.
</success_criteria>

<output>
Create `.planning/phases/56-entity-templates-scan-time-population/56-04-SUMMARY.md` when done.
Summary captures: the exact `_read_pyproject`/`refresh` edits; confirmation no schema migration
or describe_* change was needed; confirmation the edited regions are disjoint from Phase 55's;
the new test assertions and the `uv run --package graph-io pytest ...` result.
</output>
