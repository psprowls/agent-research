---
phase: 56-entity-templates-scan-time-population
plan: 03
type: execute
wave: 3
depends_on: ["02"]
files_modified:
  - packages/wiki-io/src/wiki_io/assets/page-templates/package/overview.md
  - packages/wiki-io/src/wiki_io/assets/page-templates/domain/overview.md
  - packages/wiki-io/src/wiki_io/assets/page-templates/plugin/overview.md
  - packages/wiki-io/src/wiki_io/assets/page-templates/app/overview.md
  - packages/wiki-io/src/wiki_io/assets/page-templates/app/testing.md
  - packages/wiki-io/tests/test_overview_template_wikilinks.py
autonomous: true
requirements: [ENTITY-03]

must_haves:
  truths:
    - "D-13: the legacy directory-style template assets `page-templates/package/`, `page-templates/domain/`, `page-templates/plugin/`, and `page-templates/app/` (including their `overview.md` and `app/testing.md`) are DELETED from the repo once their content is migrated (Plan 02 owns the migration; this plan runs in a later wave)"
    - "D-14: NO `init_vault.py` code change — `init_vault.py:~251-263` copies `page-templates/` recursively via rglob, so deleting the source subdirs simply stops them being seeded; this plan CONFIRMS via grep that no OTHER code references those dirs by path before deleting"
    - "D-15: the 4 obsolete tests in `packages/wiki-io/tests/test_overview_template_wikilinks.py` are DELETED — they assert wikilinks in the now-deleted package/domain/context overview templates (the 4 pre-existing FileNotFoundError failures Phase 54 flagged as 'slated for removal in Phase 56'); the entire file is removed because all 4 of its test functions target deleted templates"
    - "D-16: dead-link verification is repo GREP ONLY — NO new permanent dead-link regression test is added; the grep confirms (a) no live code references the deleted dirs and (b) generated entity pages have no dead links"
    - "Ordering: this plan depends on Plan 02 (content migrated into the entity templates first); deletion must not precede migration or content is lost"
    - "Backward compat (project rule): entity content is regenerable and the user rebuilds the wiki on migration, so deleting the legacy `.templates/` source subdirs needs NO migration shim"
  artifacts:
    - path: packages/wiki-io/tests/test_overview_template_wikilinks.py
      provides: "removed — all 4 tests targeted deleted legacy templates (D-15)"
      contains: ""
  key_links:
    - "Depends on Plan 02 having migrated all meaningful sections out of the legacy overview/testing pages before they are deleted"
---

<objective>
Delete the legacy directory-style template assets (`page-templates/{package,domain,plugin,app}/`,
including `app/testing.md`) now that Plan 02 has migrated their content into the `entity-<kind>.md`
templates, and delete the 4 obsolete `test_overview_template_wikilinks.py` tests that asserted
against them. Verify via repo grep that no other code references the deleted dirs and that
generated entity pages have no dead links (D-16 — grep only, no new standing test). This is
ENTITY-03.

Purpose: the legacy per-kind overview/testing templates are superseded by the filled
`entity-<kind>.md` templates; leaving them on disk causes the recursive `init_vault` copy to keep
seeding dead scaffolds and keeps 4 tests failing with FileNotFoundError (pre-existing, flagged in
Phase 54 for removal here).

Output: the 4 legacy subdirs removed; `test_overview_template_wikilinks.py` removed; a grep
confirmation that no live code path references the deleted dirs and no dead links remain in
generated pages; no `init_vault.py` change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/56-entity-templates-scan-time-population/56-CONTEXT.md
@.planning/phases/56-entity-templates-scan-time-population/56-RESEARCH.md
@.claude/rules/backward-compatibility.md
@packages/wiki-io/CLAUDE.md

<interfaces>
<!-- Legacy dirs to delete (verified present via ls):
       page-templates/package/overview.md
       page-templates/domain/overview.md
       page-templates/plugin/overview.md
       page-templates/app/overview.md
       page-templates/app/testing.md  -->

<!-- D-14: init_vault.py:~251-263 rglob-copies page-templates/ recursively; deleting subdirs is
     sufficient, NO code change. Verified: grep for legacy-dir paths in wiki-io/src returns
     nothing (no path-based reference). -->

<!-- D-15: test_overview_template_wikilinks.py has 4 test functions, ALL loading
       ASSETS/"package"/"overview.md", ASSETS/"domain"/"overview.md", and a package/context
       template — every function targets a deleted template, so the whole file is removed. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm no live code references the legacy dirs, then delete the 4 legacy template subdirs</name>
  <files>
    packages/wiki-io/src/wiki_io/assets/page-templates/package/overview.md
    packages/wiki-io/src/wiki_io/assets/page-templates/domain/overview.md
    packages/wiki-io/src/wiki_io/assets/page-templates/plugin/overview.md
    packages/wiki-io/src/wiki_io/assets/page-templates/app/overview.md
    packages/wiki-io/src/wiki_io/assets/page-templates/app/testing.md
  </files>
  <read_first>
    - .planning/phases/56-entity-templates-scan-time-population/56-CONTEXT.md (D-13, D-14, D-16)
    - .planning/phases/56-entity-templates-scan-time-population/56-02-PLAN.md (confirm migration completed before deleting)
    - packages/wiki-io/src/wiki_io/init_vault.py (confirm the rglob copy at ~251-263 is path-agnostic — no per-dir reference)
    - .claude/rules/backward-compatibility.md (no migration shim needed)
  </read_first>
  <behavior>
    - Before deletion, grep the wiki-io source (and the wider repo) for any code that references
      the legacy dirs by path (`page-templates/package`, `/domain`, `/plugin`, `/app`,
      `package/overview`, `domain/overview`, `plugin/overview`, `app/overview`, `app/testing`).
      If any LIVE code reference is found (outside the to-be-deleted test file and planning docs),
      STOP and record it — do not delete blindly (D-14 confirmation gate).
    - With no live reference found, delete the four legacy subdirs entirely:
      `page-templates/package/`, `page-templates/domain/`, `page-templates/plugin/`,
      `page-templates/app/` (the latter includes both `overview.md` and `testing.md`).
    - Do NOT modify `init_vault.py` (D-14) — the recursive copy simply stops seeding them.
    - Do NOT delete any `entity-<kind>.md` template or any other `page-templates/*.md`
      (adr.md, architecture.md, concept*.md, dependency.md, index.md, source.md, work.md remain).
  </behavior>
  <action>First run a confirmation grep over `packages/ --include=*.py` for the legacy-dir paths and verify the only matches are in the to-be-deleted `test_overview_template_wikilinks.py` (and/or planning docs). If a live src reference exists, STOP and report it. Then `git rm -r` the four directories `packages/wiki-io/src/wiki_io/assets/page-templates/package`, `.../domain`, `.../plugin`, `.../app`. Do not touch init_vault.py or any entity-*.md template.</action>
  <verify>
    <automated>test ! -d packages/wiki-io/src/wiki_io/assets/page-templates/package && test ! -d packages/wiki-io/src/wiki_io/assets/page-templates/domain && test ! -d packages/wiki-io/src/wiki_io/assets/page-templates/plugin && test ! -d packages/wiki-io/src/wiki_io/assets/page-templates/app && ls packages/wiki-io/src/wiki_io/assets/page-templates/entity-package.md</automated>
  </verify>
  <done>
    - The four legacy subdirs no longer exist on disk (the `test ! -d` checks pass for all four)
    - The 7 `entity-<kind>.md` templates and the other top-level `page-templates/*.md` files still exist
    - No live src code referenced the deleted dirs (grep confirmation recorded in the summary)
    - `init_vault.py` is unchanged
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete the obsolete test_overview_template_wikilinks.py (D-15)</name>
  <files>packages/wiki-io/tests/test_overview_template_wikilinks.py</files>
  <read_first>
    - packages/wiki-io/tests/test_overview_template_wikilinks.py (confirm all 4 test functions load the now-deleted package/domain/context overview templates)
    - .planning/phases/56-entity-templates-scan-time-population/56-CONTEXT.md (D-15)
  </read_first>
  <behavior>
    - All 4 test functions in this file assert wikilinks/render behavior of the deleted
      `package/overview.md`, `domain/overview.md`, and a `package/context` template — every one
      now FileNotFound. Per D-15 they are the pre-existing failures slated for removal here.
    - The entire file is removed (no surviving test in it targets a non-deleted asset). New
      entity-template coverage already lives in `test_entity_templates.py` (Plan 02) and the
      SCAN-01/02 tests (Plan 01) — no replacement test is added here.
  </behavior>
  <action>`git rm packages/wiki-io/tests/test_overview_template_wikilinks.py`. Do not create a replacement file (D-16: no new standing dead-link test; entity coverage lives in test_entity_templates.py and test_entity_writer.py).</action>
  <verify>
    <automated>test ! -f packages/wiki-io/tests/test_overview_template_wikilinks.py && uv run --package wiki-io pytest packages/wiki-io/tests -q</automated>
  </verify>
  <done>
    - `packages/wiki-io/tests/test_overview_template_wikilinks.py` no longer exists
    - `uv run --package wiki-io pytest packages/wiki-io/tests -q` exits 0 (the 4 pre-existing FileNotFoundError failures are gone; remaining suite green)
  </done>
</task>

<task type="auto">
  <name>Task 3: Dead-link grep verification (D-16, no new test)</name>
  <files>packages/wiki-io/src/wiki_io/assets/page-templates/entity-package.md</files>
  <read_first>
    - .planning/phases/56-entity-templates-scan-time-population/56-CONTEXT.md (D-16 — grep only, no standing test)
    - .planning/phases/56-entity-templates-scan-time-population/56-RESEARCH.md (§6 deletion + dead-link verification)
  </read_first>
  <behavior>
    - Confirm via repo grep that, after deletion, no remaining source/test/template references the
      deleted legacy dirs by path. This is a verification step that produces evidence for the
      summary — it does NOT add a permanent regression test (D-16).
    - Confirm the filled entity templates contain no dead `[[...]]` links left as bare stems by
      the migration (any remaining `[[...]]` should be authoring-guidance placeholders inside
      `> TODO:`/Related sections, which are intentional per D-01/D-11, not dead links to deleted
      pages). This is a spot-check, not an assertion-bearing test file.
  </behavior>
  <action>Run a repo-wide grep for the deleted legacy-dir paths (`page-templates/package`, `/domain`, `/plugin`, `/app`, `*/overview.md`, `app/testing`) across `packages/` source and tests, and confirm zero live references remain. Spot-check the 7 entity templates for any `[[...]]` link that points at a deleted page rather than an authoring placeholder. Record the grep output and spot-check result in the plan's summary. Do NOT create any new test file or modify any source (this task only edits nothing — it is a verification gate; the listed file is the read anchor).</action>
  <verify>
    <automated>test -z "$(grep -rln 'page-templates/package\|page-templates/domain\|page-templates/plugin\|page-templates/app\|app/testing' packages/ --include=*.py 2>/dev/null)"</automated>
  </verify>
  <done>
    - Repo grep shows no live `.py` reference to the deleted legacy dirs
    - No dead `[[...]]` link to a deleted page remains in the entity templates (recorded in summary)
    - No new test file was created (D-16)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan deletes first-party static template assets and one obsolete test file from the repo,
and runs read-only grep verification. No runtime code path, no untrusted input, no network. The
only risk surface is deleting too much (a still-referenced asset) — gated by the pre-deletion grep.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-03-T1 | Tampering | Deleting a legacy dir that live code still references by path, breaking init_vault seeding | mitigate | Task 1 runs a pre-deletion grep and STOPS if any live src reference exists (D-14 gate); init_vault's copy is rglob-based and path-agnostic (verified), so deletion only stops seeding the removed scaffolds. |
| T-56-03-T2 | Tampering | Deleting legacy content before Plan 02 migrated it (data loss) | mitigate | `depends_on: ["02"]` + wave 3 sequencing guarantees migration lands first; the must_haves pin the ordering. |
</threat_model>

<verification>
- The four legacy subdirs (`package/`, `domain/`, `plugin/`, `app/`) are absent under `page-templates/`
- The 7 `entity-<kind>.md` templates and the other top-level `page-templates/*.md` remain
- `packages/wiki-io/tests/test_overview_template_wikilinks.py` is deleted
- `uv run --package wiki-io pytest packages/wiki-io/tests -q` exits 0 (the 4 pre-existing failures gone)
- Repo grep shows no live `.py` reference to the deleted legacy dirs (D-16)
- No new dead-link regression test was added (D-16)
- `init_vault.py` is unchanged (D-14)
</verification>

<success_criteria>
- ROADMAP SC#4: the legacy `package/`, `domain/`, `plugin/`, and `app/` template directories no
  longer exist in the repository; no dead links remain in generated entity pages (ENTITY-03).
- The 4 pre-existing FileNotFoundError test failures are resolved by removal (D-15); the wiki-io
  suite is green.
</success_criteria>

<output>
Create `.planning/phases/56-entity-templates-scan-time-population/56-03-SUMMARY.md` when done.
Summary captures: the grep confirmation (no live references) before deletion; the four dirs
removed; the test file removed and the resulting green suite; and the D-16 dead-link spot-check
result. Note no init_vault.py change and no new test was added.
</output>
