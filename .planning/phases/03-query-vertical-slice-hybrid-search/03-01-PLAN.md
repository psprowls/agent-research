---
phase: 03-query-vertical-slice-hybrid-search
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - agents/code-wiki-agent/pyproject.toml
  - agents/code-wiki-agent/src/code_wiki_agent/commands/__init__.py
  - agents/code-wiki-agent/tests/commands/__init__.py
  - agents/code-wiki-agent/tests/conftest.py
  - agents/code-wiki-agent/tests/unit/test_query_search.py
  - agents/code-wiki-agent/tests/unit/test_query_result.py
  - agents/code-wiki-agent/tests/unit/test_cli_query.py
  - agents/code-wiki-agent/tests/unit/test_mcp_query_schema.py
  - agents/code-wiki-agent/tests/integration/test_query_e2e.py
  - uv.lock
autonomous: true
requirements: [SEARCH-01]
tags: [scaffold, wave-0, infra]

must_haves:
  truths:
    - "`uv run --package code-wiki-agent python -c 'import bm25s'` exits 0"
    - "`@pytest.mark.asyncio` tests in code-wiki-agent are recognized by the test runner"
    - "`code_wiki_agent.commands` subpackage is importable"
    - "Every Phase 3 unit + integration test stub exists with a placeholder skip/xfail so the test runner can discover them"
    - "Fixture vault path is reachable via `fixture_vault_path` pytest fixture in code-wiki-agent tests"
  artifacts:
    - path: "agents/code-wiki-agent/pyproject.toml"
      provides: "bm25s==0.3.8 dep + subagent-runtime workspace dep + asyncio_mode=auto"
      contains: "bm25s==0.3.8"
    - path: "agents/code-wiki-agent/src/code_wiki_agent/commands/__init__.py"
      provides: "commands subpackage declaration"
      min_lines: 0
    - path: "agents/code-wiki-agent/tests/conftest.py"
      provides: "fixture_vault_path fixture pointing to cores/vault-io/tests/fixtures/round-trip-vault"
      exports: ["fixture_vault_path"]
    - path: "agents/code-wiki-agent/tests/unit/test_query_search.py"
      provides: "stub tests for SEARCH-01..06 (xfail until Plan 02)"
    - path: "agents/code-wiki-agent/tests/unit/test_query_result.py"
      provides: "stub tests for QueryResult dataclass (xfail until Plan 03)"
    - path: "agents/code-wiki-agent/tests/unit/test_cli_query.py"
      provides: "stub tests for CLI query subcommand (xfail until Plan 03)"
    - path: "agents/code-wiki-agent/tests/unit/test_mcp_query_schema.py"
      provides: "stub tests for wiki_query MCP tool schema (xfail until Plan 04)"
    - path: "agents/code-wiki-agent/tests/integration/test_query_e2e.py"
      provides: "stub integration test for end-to-end query (xfail until Plan 04)"
  key_links:
    - from: "agents/code-wiki-agent/pyproject.toml"
      to: "uv.lock"
      via: "uv add --package code-wiki-agent"
      pattern: "bm25s"
    - from: "agents/code-wiki-agent/tests/conftest.py"
      to: "cores/vault-io/tests/fixtures/round-trip-vault"
      via: "Path(__file__).parent.parent.parent.parent / cores / vault-io / tests / fixtures / round-trip-vault"
      pattern: "round-trip-vault"
---

<objective>
Establish the Wave 0 scaffolding for Phase 3: install `bm25s==0.3.8`, declare the `subagent-runtime` workspace dep, enable `asyncio_mode = "auto"` for code-wiki-agent's pytest config, create the `commands/` subpackage, and stub every Phase 3 unit and integration test file so the test runner can discover them.

Purpose: All downstream plans (search layer, query pipeline, MCP tool) depend on this scaffolding. Without `asyncio_mode = "auto"` async tests silently no-op; without `bm25s` no implementation can run; without test stubs Wave 0 tests have nothing to fail against.

Output: Working test runner that discovers Phase 3 tests (all xfail/skip pending real implementations), `bm25s` importable, `subagent_runtime` importable from code-wiki-agent.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-query-vertical-slice-hybrid-search/03-CONTEXT.md
@.planning/phases/03-query-vertical-slice-hybrid-search/03-RESEARCH.md
@.planning/phases/03-query-vertical-slice-hybrid-search/03-VALIDATION.md
@.planning/phases/03-query-vertical-slice-hybrid-search/03-PATTERNS.md
@agents/code-wiki-agent/pyproject.toml
@cores/subagent-runtime/pyproject.toml
@agents/code-wiki-agent/tests/unit/test_cli_help.py
@agents/code-wiki-agent/tests/integration/test_mcp_stdio.py

<interfaces>
<!-- Existing patterns the executor must follow. -->

From cores/subagent-runtime/pyproject.toml (the reference for asyncio_mode):
[tool.pytest.ini_options]
asyncio_mode = "auto"
markers = ["integration: ..."]

From cores/vault-io/tests/fixtures/round-trip-vault/ (the fixture vault location):
- Contains 30+ concepts/*.md pages, packages/, adrs/, index.md, log.md
- Cross-package path from agents/code-wiki-agent/tests/: parent.parent.parent.parent.parent / "cores" / "vault-io" / "tests" / "fixtures" / "round-trip-vault"

Existing pyproject.toml dependencies array (from current state):
dependencies = [
  "vault-io", "model-adapter", "mcp>=1.27.1",
  "langchain-aws>=1.4.6", "typer>=0.25.1", "pydantic>=2.0",
]
Add: "subagent-runtime" (workspace dep), "bm25s==0.3.8"
Also add under [tool.uv.sources]: subagent-runtime = { workspace = true }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update pyproject.toml — add bm25s dep, subagent-runtime workspace dep, asyncio_mode=auto</name>
  <files>agents/code-wiki-agent/pyproject.toml</files>
  <read_first>
    - agents/code-wiki-agent/pyproject.toml (current contents)
    - cores/subagent-runtime/pyproject.toml (reference for asyncio_mode block)
  </read_first>
  <action>
    Edit `agents/code-wiki-agent/pyproject.toml`:

    1. Under `[project] dependencies`, append two entries (preserve existing entries):
       - `"subagent-runtime"` — workspace dep, needed by `commands/query.py` for `SubagentPool` (Phase 3).
       - `"bm25s==0.3.8"` — exact pin per D-04 / SEARCH-01.

    2. Under `[tool.uv.sources]`, add: `subagent-runtime = { workspace = true }` (alongside existing `vault-io` and `model-adapter` workspace entries).

    3. Under `[tool.pytest.ini_options]`, add a new line `asyncio_mode = "auto"` between `addopts` and `markers`. Required for `@pytest.mark.asyncio` tests in Phase 3 — pitfall #5 in RESEARCH.md.

    After editing, run `uv sync` from the repo root to refresh `uv.lock` and install bm25s.
  </action>
  <verify>
    <automated>uv sync &amp;&amp; uv run --package code-wiki-agent python -c "import bm25s; import subagent_runtime; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'bm25s==0.3.8' agents/code-wiki-agent/pyproject.toml` succeeds
    - `grep -q 'subagent-runtime' agents/code-wiki-agent/pyproject.toml` succeeds (in both [project].dependencies and [tool.uv.sources])
    - `grep -q 'asyncio_mode = "auto"' agents/code-wiki-agent/pyproject.toml` succeeds
    - `uv run --package code-wiki-agent python -c "import bm25s"` exits 0
    - `uv run --package code-wiki-agent python -c "from subagent_runtime.pool import SubagentPool"` exits 0
    - `uv.lock` records bm25s 0.3.8 and subagent-runtime as workspace member
  </acceptance_criteria>
  <done>bm25s 0.3.8 installed; subagent-runtime importable from code-wiki-agent; asyncio_mode=auto active; `uv sync` clean.</done>
</task>

<task type="auto">
  <name>Task 2: Create commands subpackage + tests/commands subpackage + conftest fixture</name>
  <files>
    - agents/code-wiki-agent/src/code_wiki_agent/commands/__init__.py
    - agents/code-wiki-agent/tests/commands/__init__.py
    - agents/code-wiki-agent/tests/conftest.py
  </files>
  <read_first>
    - agents/code-wiki-agent/src/code_wiki_mcp/__init__.py (pattern for empty subpackage init)
    - agents/code-wiki-agent/tests/integration/test_mcp_stdio.py (project-root path pattern)
  </read_first>
  <action>
    Create three files:

    1. `agents/code-wiki-agent/src/code_wiki_agent/commands/__init__.py` — empty file (0 bytes). This declares the `commands` subpackage so `from code_wiki_agent.commands.query import run_query` works in later plans.

    2. `agents/code-wiki-agent/tests/commands/__init__.py` — empty file (0 bytes). Declares the test subpackage symmetrically.

    3. `agents/code-wiki-agent/tests/conftest.py` — define a `fixture_vault_path` pytest fixture that resolves the cross-package fixture vault. Use `Path(__file__).parent.parent.parent.parent / "cores" / "vault-io" / "tests" / "fixtures" / "round-trip-vault"`. The fixture must assert the path exists (skips test with clear message if missing). Include `from __future__ import annotations` per project convention. Also expose a module-level constant `INTEGRATION_GATE = pytest.mark.skipif(not os.environ.get("CODE_WIKI_RUN_INTEGRATION"), reason="Set CODE_WIKI_RUN_INTEGRATION=1 to run real Bedrock invocations")` so integration tests can import it from conftest if convenient, OR redefine locally — either is fine; the pattern itself comes from `cores/subagent-runtime/tests/integration/test_pool_bedrock.py`.

    The fixture must return the Path object (not a string).
  </action>
  <verify>
    <automated>uv run --package code-wiki-agent python -c "from code_wiki_agent.commands import __name__; print(__name__)"</automated>
  </verify>
  <acceptance_criteria>
    - `agents/code-wiki-agent/src/code_wiki_agent/commands/__init__.py` exists (size may be 0)
    - `agents/code-wiki-agent/tests/commands/__init__.py` exists
    - `agents/code-wiki-agent/tests/conftest.py` exists and defines a pytest fixture named `fixture_vault_path`
    - `grep -q 'round-trip-vault' agents/code-wiki-agent/tests/conftest.py` succeeds
    - `uv run --package code-wiki-agent python -c "from code_wiki_agent.commands import __name__"` exits 0
    - `uv run --package code-wiki-agent pytest agents/code-wiki-agent/tests/ --collect-only -q` exits 0 (collection works with new conftest)
  </acceptance_criteria>
  <done>commands subpackage importable; tests can request `fixture_vault_path` fixture to get a Path to the round-trip-vault.</done>
</task>

<task type="auto">
  <name>Task 3: Create five Phase 3 test stub files with xfail placeholders</name>
  <files>
    - agents/code-wiki-agent/tests/unit/test_query_search.py
    - agents/code-wiki-agent/tests/unit/test_query_result.py
    - agents/code-wiki-agent/tests/unit/test_cli_query.py
    - agents/code-wiki-agent/tests/unit/test_mcp_query_schema.py
    - agents/code-wiki-agent/tests/integration/test_query_e2e.py
  </files>
  <read_first>
    - agents/code-wiki-agent/tests/unit/test_trace_viewer.py (pattern for unit test file structure)
    - cores/subagent-runtime/tests/integration/test_pool_bedrock.py (pattern for integration gate)
    - .planning/phases/03-query-vertical-slice-hybrid-search/03-RESEARCH.md §"Phase Requirements → Test Map" lines 880-902
    - .planning/phases/03-query-vertical-slice-hybrid-search/03-PATTERNS.md §"`agents/code-wiki-agent/tests/unit/test_query_search.py`" (file-level structure block)
  </read_first>
  <action>
    Create five test stub files. Each must:
    - Open with `from __future__ import annotations` and a module docstring stating the file's role
    - Import `pytest`
    - Define one xfail or skip placeholder test per requirement listed for that file (below) so the test runner discovers the file and Wave 0 "all green" verification has named tests to bind to in later plans

    Use `@pytest.mark.xfail(reason="Implemented in Plan NN", strict=False)` (not `skip`) so the test appears in the run log and turns to a real pass when implementation lands. Use `def test_xxx(): assert False, "stub — Plan NN"` as the body.

    File contents (each is a stub — actual assertions land in later plans):

    1. `tests/unit/test_query_search.py` — stubs for: `test_bm25_index_build_and_query` (SEARCH-01), `test_embedding_shape_1024` (SEARCH-02, mock embed), `test_rrf_fuse` (SEARCH-03), `test_sqlite_store_wal_mode` (SEARCH-04), `test_incremental_skip_unchanged_hash` (SEARCH-05). All xfail "Implemented in Plan 02".

    2. `tests/unit/test_query_result.py` — stubs for: `test_query_result_is_dataclass`, `test_query_result_asdict_has_required_keys`, `test_search_scores_shape_per_page` (SEARCH-06), `test_json_output_valid_schema` (CMD-07). All xfail "Implemented in Plan 03".

    3. `tests/unit/test_cli_query.py` — stubs for: `test_query_help_exits_zero` (CLI-01), `test_shared_impl_is_imported_from_commands` (CLI-03), `test_state_gate_flag_present` (CMD-08), `test_vault_flag_passes_path` (CLI-05), `test_exit_code_1_on_runtime_error` (CLI-06), `test_headless_mode_non_tty` (CLI-07). All xfail "Implemented in Plan 03".

    4. `tests/unit/test_mcp_query_schema.py` — stubs for: `test_wiki_query_tool_registered` (MCP-02), `test_invalid_input_returns_structured_error` (MCP-04), `test_progress_calls_made` (MCP-06). All xfail "Implemented in Plan 04".

    5. `tests/integration/test_query_e2e.py` — stub for: `test_fixture_vault_has_citations` (CMD-04 SC-5), `test_json_flag_emits_search_scores` (SEARCH-06 + CLI-04). Use the `@pytest.mark.integration` marker AND xfail (so it does not even attempt to run by default). Include the cross-package `FIXTURE_VAULT` constant per RESEARCH Pitfall 6 as a module-level constant for later plans to reuse.

    Do NOT include any business logic in any stub — the body is `assert False, "stub — Plan NN"`. The signatures and decorators are the deliverable.
  </action>
  <verify>
    <automated>uv run --package code-wiki-agent pytest agents/code-wiki-agent/tests/ --collect-only -q 2>&amp;1 | grep -v '^#' | grep -E 'test_(query_search|query_result|cli_query|mcp_query_schema|query_e2e)' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - All five test files exist
    - `pytest --collect-only` discovers at least 18 test items across the five files (5+4+6+3+2 = 20 stubs total; allow ≥18 for minor naming flexibility)
    - `grep -l "xfail" agents/code-wiki-agent/tests/unit/test_query_*.py` returns at least 2 files
    - `grep -q "FIXTURE_VAULT" agents/code-wiki-agent/tests/integration/test_query_e2e.py` succeeds
    - `grep -q "round-trip-vault" agents/code-wiki-agent/tests/integration/test_query_e2e.py` succeeds
    - `uv run --package code-wiki-agent pytest agents/code-wiki-agent/tests/ -m "not integration" -q --no-header 2>&1 | grep -E "xfail|xpassed"` finds expected-fail markers (xfail tests are not failures)
  </acceptance_criteria>
  <done>Test runner discovers all Phase 3 stubs; all stubs xfail (no false greens); FIXTURE_VAULT constant available for Plan 04 integration test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Workspace member install | `uv` resolves and installs `bm25s` from PyPI — supply-chain trust |
| Test fixture vault | Cross-package path traversal must stay within repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | `bm25s` PyPI package install | accept | Exact-version pin (`bm25s==0.3.8`) recorded in pyproject.toml; uv.lock hash-locks the artifact; standard supply chain |
| T-03-02 | Tampering | Cross-package fixture path in conftest.py | mitigate | `Path(__file__).parent...` is anchored to test file location; no user-supplied input; `assert path.exists()` in fixture surfaces tampering immediately |
| T-03-03 | Information Disclosure | Test discovery in CI revealing internal vault structure | accept | round-trip-vault is committed test fixture, not real vault data; no PII or secrets |
</threat_model>

<verification>
After all tasks complete:

```bash
uv sync
uv run --package code-wiki-agent python -c "import bm25s; from subagent_runtime.pool import SubagentPool; from code_wiki_agent.commands import __name__; print('all imports ok')"
uv run --package code-wiki-agent pytest agents/code-wiki-agent/tests/ -m "not integration" -q --no-header
```

The pytest invocation must report ≥18 xfail or xpassed items and exit code 0.
</verification>

<success_criteria>
- `bm25s==0.3.8` installed; importable in code-wiki-agent context
- `subagent_runtime` importable in code-wiki-agent context
- `asyncio_mode = "auto"` recognized by pytest config
- `code_wiki_agent.commands` subpackage importable
- All five Phase 3 test stub files exist with xfail markers
- `pytest --collect-only` exits 0 and discovers all stubs
- conftest exposes `fixture_vault_path` fixture pointing to the round-trip-vault
</success_criteria>

<output>
After completion, create `.planning/phases/03-query-vertical-slice-hybrid-search/03-01-SUMMARY.md`
</output>
