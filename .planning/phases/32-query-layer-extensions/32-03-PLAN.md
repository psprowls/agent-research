---
phase: 32-query-layer-extensions
plan: 03
type: execute
wave: 2
depends_on: [32-01, 32-02]
files_modified:
  - packages/graph-io/src/graph_io/queries.py
  - packages/graph-io/tests/test_queries.py
autonomous: true
requirements:
  - QUERY-04
tags:
  - graph-io
  - wave-2
  - bubble-up
  - recursive-cte
  - cross-cutting

must_haves:
  truths:
    - "queries.py adds a module-private constant `_DOMAIN_DESCENDANTS_CTE` (a string fragment) used by the three bubble-up helpers: `tests_for_domain`, `domain_references`, `domain_depends_on`. The fragment is the recursive CTE that yields the id set of a domain and all its descendants via `domain_contains_domain` edges. Definition: `_DOMAIN_DESCENDANTS_CTE = '''WITH RECURSIVE descendants(id) AS (SELECT id FROM nodes WHERE name = ? AND kind = 'domain' UNION ALL SELECT e.dst FROM edges e JOIN descendants d ON e.src = d.id WHERE e.kind = 'domain_contains_domain')'''`. Notes: SRC is the parent (per Phase 31 emit), DST is the child; the CTE walks `parent.id = e.src JOIN e.dst -> child` to descend. Phase 31 D-15 guarantees acyclicity so the CTE terminates."
    - "queries.py adds `tests_for_package(conn, *, package_name: str) -> list[SuiteDescription]`. SQL: `SELECT ts.name, ts.uri, ts.attrs_json, (SELECT COUNT(*) FROM edges pc WHERE pc.src = ts.id AND pc.kind='physically_contains') AS fc FROM edges t JOIN nodes ts ON t.src = ts.id JOIN nodes p ON t.dst = p.id WHERE t.kind='tests' AND ts.kind='test_suite' AND p.kind='package' AND p.name = ? AND _RESOLVED_FILTER ORDER BY ts.name` where `_RESOLVED_FILTER` is interpolated as `(t.attrs_json IS NULL OR json_extract(t.attrs_json, '$.resolution') != 'unresolved')` per D-17 (reuse of the existing module constant — suites with an unresolved `tests` edge are excluded; the existing edge alias `t` must be substituted into the constant via f-string or string-replace). Each row → `_load_suite_description(...)`. Returns `[]` when the package has no tests edges."
    - "queries.py adds `entry_points_for_package(conn, *, package_name: str) -> list[EntryPointDescription]`. SQL is the same as the EntryPoint subquery used by `describe_package` extension in plan 02: joins package -> declares_entry_point -> entry_point + LEFT JOIN implemented_by -> file. Sort alphabetical by entry name (Claude's Discretion in CONTEXT.md — picked alphabetical for stable shape; planner notes the alternative 'kind=executable first' is rejected to keep the test surface deterministic)."
    - "queries.py adds `tests_for_domain(conn, *, domain_name: str) -> list[SuiteDescription]` implementing the D-09 UNION. SQL uses `_DOMAIN_DESCENDANTS_CTE` and unions two branches: (a) `SELECT DISTINCT ts.id FROM edges e JOIN descendants d ON e.dst = d.id JOIN nodes ts ON e.src = ts.id WHERE e.kind='tests' AND ts.kind='test_suite'` (TestSuites with a direct `TestSuite → Domain` edge per Phase 31 D-12), (b) `SELECT DISTINCT ts.id FROM edges st JOIN nodes p ON st.dst = p.id AND p.kind='package' JOIN edges bt ON bt.src = p.id AND bt.kind='belongs_to_domain' JOIN descendants d ON bt.dst = d.id JOIN nodes ts ON st.src = ts.id WHERE st.kind='tests' AND ts.kind='test_suite'` (TestSuites whose Package edges include a package belonging to a descendant — captures Phase 31 D-13 multi-domain suites). The outer SELECT pulls `(name, uri, attrs_json, file_count)` for each unique suite_id and projects to SuiteDescription via `_load_suite_description`. ORDER BY name ASC."
    - "queries.py adds `domain_references(conn, *, domain_name: str) -> list[tuple[str, int, int]]`. Returns `(package_name, total_usage_count, distinct_domain_count)` rows per CONTEXT.md D-08. SQL uses `_DOMAIN_DESCENDANTS_CTE` then: `SELECT n.name, COALESCE(SUM(json_extract(e.attrs_json, '$.usage_count')), 0) AS total_usage, COUNT(DISTINCT e.src) AS distinct_domains FROM edges e JOIN descendants d ON e.src = d.id JOIN nodes n ON e.dst = n.id WHERE e.kind='references' AND n.kind='package' GROUP BY n.name ORDER BY total_usage DESC, n.name ASC`. NB: per Phase 31 D-07/D-18, `references` edges go Domain → Package via the `src → dst` columns, and `usage_count` lives in `attrs_json`."
    - "queries.py adds `domain_depends_on(conn, *, domain_name: str) -> list[tuple[str, int]]`. Returns `(target_domain_name, total_usage_count)` rows per CONTEXT.md D-08. SQL uses `_DOMAIN_DESCENDANTS_CTE` and: `SELECT n.name, COALESCE(SUM(json_extract(e.attrs_json, '$.usage_count')), 0) AS total_usage FROM edges e JOIN descendants d ON e.src = d.id JOIN nodes n ON e.dst = n.id WHERE e.kind='depends_on' AND n.kind='domain' AND n.id NOT IN (SELECT id FROM descendants) GROUP BY n.name ORDER BY total_usage DESC, n.name ASC`. Self-loops (the named domain or any descendant depending on itself or any other descendant) are excluded via the `NOT IN (SELECT id FROM descendants)` filter — per Phase 31 D-08 and D-09."
    - "queries.py adds `cross_cutting_packages(conn) -> list[tuple[PackageDescription, int]]` per CONTEXT.md D-10, D-11, D-12. Two-step: (1) compute the ranked id list via SQL `SELECT n.id, n.name, COALESCE(SUM(json_extract(e.attrs_json, '$.usage_count')), 0) AS score FROM nodes n LEFT JOIN edges e ON e.dst = n.id AND e.kind='references' WHERE n.kind='package' AND NOT EXISTS (SELECT 1 FROM edges bt WHERE bt.src = n.id AND bt.kind='belongs_to_domain') GROUP BY n.id, n.name ORDER BY score DESC, n.name ASC`. (2) For each row, call `describe_package(conn, name=row[1])` and pair with the score: `[(describe_package(conn, name=name), int(score)) for (_id, name, score) in rows]`. Filter out `None` results (defensive — describe_package should always succeed for a name we just selected from nodes)."
    - "ranking is by SUM(usage_count), NOT distinct-domain count — D-11 explicit divergence from ontology spec §11.4. The function docstring includes a one-line note: 'Ranked by total incoming `references` usage_count, not distinct-domain count (D-11; this is a query-layer rendering choice, not a spec amendment).' so future readers know why the rendering differs from the spec text."
    - "All four bubble-up helpers and `cross_cutting_packages` open NO new connections. All run inside the caller's `conn` argument. NO INSERT/UPDATE/DELETE — D-16."
    - "All SQL uses lowercase node kinds and `src/dst` edge columns — RESEARCH §1."
    - "packages/graph-io/tests/test_queries.py adds the Wave 2 unit tests per VALIDATION.md: `test_tests_for_domain_union`, `test_tests_for_domain_bubbles_to_descendants`, `test_domain_references_bubble`, `test_domain_depends_on_no_self_loop`, `test_cross_cutting_packages_ranking`, `test_entry_points_for_package`, `test_tests_for_package`, `test_cte_cycle_safe`, plus empty-DB variants for each."
    - "`test_cte_cycle_safe` is the defence-in-depth test from RESEARCH §9.4: it uses the existing `_upsert_node` and `_upsert_edge` helpers from graph_io.upsert to manually insert a 2-node cycle (`domain_a domain_contains_domain domain_b` AND `domain_b domain_contains_domain domain_a`) into an empty in-memory DB (bypassing Phase 31's invariant), then calls `tests_for_domain` and asserts it RETURNS WITHIN 5 SECONDS via `pytest-timeout` OR via `signal.alarm`. The test does NOT require the helpers to detect the cycle — only that they don't hang. SQLite's `WITH RECURSIVE ... UNION ALL` will normally loop on a cycle; this test confirms either (a) SQLite's CTE has implicit duplicate-row dedup that breaks the loop, OR (b) we need to switch to `UNION` instead of `UNION ALL` in the CTE. If the test reveals a hang, the remediation is to change `UNION ALL` to `UNION` in `_DOMAIN_DESCENDANTS_CTE` (which adds a per-row dedup at the cost of one hash-table per recursion, fine for the bounded sizes we expect)."
    - "All Wave 2 tests against `seeded_db` use the `_skip_if_phase31_missing` helper from plan 02 to gracefully skip when domains.yaml has not yet shipped."
  artifacts:
    - path: "packages/graph-io/src/graph_io/queries.py"
      provides: "6 bubble-up + per-package helpers + _DOMAIN_DESCENDANTS_CTE constant"
      contains: "def tests_for_domain"
      exports:
        - tests_for_package
        - entry_points_for_package
        - tests_for_domain
        - domain_references
        - domain_depends_on
        - cross_cutting_packages
    - path: "packages/graph-io/tests/test_queries.py"
      provides: "Wave 2 unit tests for the bubble-up helpers + cross-cutting + cycle-safety"
      contains: "def test_tests_for_domain_union"
  key_links:
    - from: "tests_for_domain / domain_references / domain_depends_on"
      to: "_DOMAIN_DESCENDANTS_CTE"
      via: "shared SQL fragment"
      pattern: "_DOMAIN_DESCENDANTS_CTE +"
    - from: "cross_cutting_packages"
      to: "describe_package"
      via: "per-result re-query"
      pattern: "describe_package(conn, name="
    - from: "test_cte_cycle_safe"
      to: "graph_io.upsert._upsert_node / _upsert_edge"
      via: "raw insert for cycle fixture"
      pattern: "_upsert_edge(...domain_contains_domain..."
---

<objective>
Land the recursive-CTE and cross-cutting helpers that complete the
QUERY-04 surface: `tests_for_package`, `entry_points_for_package`,
`tests_for_domain` (the D-09 UNION), `domain_references` (D-08 bubble-up),
`domain_depends_on` (D-08 bubble-up, self-loop excluded),
`cross_cutting_packages` (D-10/D-11/D-12; SUM(usage_count) ranking — a
deliberate divergence from ontology spec §11.4 distinct-domain wording
per D-11).

A single module-private constant `_DOMAIN_DESCENDANTS_CTE` holds the
recursive CTE fragment so the three domain-walking helpers stay in
lockstep. All SQL uses lowercase node kinds and `src/dst` edge columns
per the RESEARCH §1 correction — CONTEXT.md `<specifics>` SQL drafts
must be transliterated to those conventions before they ship.

Tests cover happy-path bubble-up (parent domain includes child's
references/depends_on/tests), self-loop exclusion in `domain_depends_on`,
ranking + tie-break stability in `cross_cutting_packages`, the UNION
distinct-vs-inferred branches in `tests_for_domain`, empty-DB graceful
degradation across all six helpers, and a paranoid cycle-safety test
that confirms `_DOMAIN_DESCENDANTS_CTE` does not hang on a manually
inserted `domain_contains_domain` cycle (defence in depth against future
emitter bugs that violate Phase 31's acyclicity invariant).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-query-layer-extensions/32-CONTEXT.md
@.planning/phases/32-query-layer-extensions/32-RESEARCH.md
@.planning/phases/32-query-layer-extensions/32-VALIDATION.md
@.planning/phases/32-query-layer-extensions/32-01-PLAN.md
@.planning/phases/32-query-layer-extensions/32-02-PLAN.md
@.planning/phases/31-domain-layer-derived-edges/31-CONTEXT.md
@packages/graph-io/src/graph_io/queries.py
@packages/graph-io/src/graph_io/upsert.py
@packages/graph-io/src/graph_io/schema.py

<interfaces>
<!-- After plans 01 + 02 land: -->

From packages/graph-io/src/graph_io/queries.py (post plan 02):
  _VALID_KINDS, _RESOLVED_FILTER                                       # constants
  RepoDescription, DomainDescription, EntryPointDescription,
    SuiteDescription, PackageDescription, PathDescription,
    NodeRecord, CallRecord, ImportRecord                               # dataclasses
  find, callers, callees, imports, imported_by, exports, exported_by   # existing
  describe_package (extended), describe_path (extended)                # plan 02
  describe_repository, describe_domain,
    describe_entry_point, describe_test_suite                          # plan 02
  list_repositories, list_packages, list_entry_points,
    list_test_suites, list_domains, list_scripts                       # plan 02
  _load_entry_point_description, _load_suite_description               # plan 02

From packages/graph-io/src/graph_io/upsert.py:
  _upsert_node, _upsert_edge                  # raw-SQL fixture inserts (test_cte_cycle_safe)

From packages/graph-io/tests/conftest.py (post plan 01):
  seeded_db, empty_db                         # session and function scoped
  _skip_if_phase31_missing                    # added in plan 02 test module
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add _DOMAIN_DESCENDANTS_CTE constant and tests_for_package + entry_points_for_package helpers</name>
  <read_first>
    - packages/graph-io/src/graph_io/queries.py (current state after plan 02 — find a stable insertion point near existing constants for the CTE fragment, and append the helpers to the bottom)
    - .planning/phases/32-query-layer-extensions/32-CONTEXT.md D-07 (CTE pattern), D-06 (which helpers bubble up)
    - .planning/phases/31-domain-layer-derived-edges/31-CONTEXT.md D-15 (acyclic guarantee)
  </read_first>
  <files>packages/graph-io/src/graph_io/queries.py</files>
  <acceptance_criteria>
    - `grep -n "_DOMAIN_DESCENDANTS_CTE = " packages/graph-io/src/graph_io/queries.py` returns one line
    - The CTE string contains `WITH RECURSIVE descendants(id)`, `WHERE name = ? AND kind = 'domain'`, `WHERE e.kind = 'domain_contains_domain'`, and uses `e.src` + `e.dst` (NOT parent_id / child_id) — verified by `grep -A 6 "_DOMAIN_DESCENDANTS_CTE = " packages/graph-io/src/graph_io/queries.py | grep -E "(domain_contains_domain|e\\.src|e\\.dst)"` showing all three patterns
    - `grep -n "def tests_for_package(conn" packages/graph-io/src/graph_io/queries.py` returns one line
    - `grep -n "def entry_points_for_package(conn" packages/graph-io/src/graph_io/queries.py` returns one line
    - Both helpers return `list[<Type>Description]` shape — verified by signature grep
    - `uv run --package graph-io python -c "from graph_io.queries import tests_for_package, entry_points_for_package, _DOMAIN_DESCENDANTS_CTE; assert 'WITH RECURSIVE' in _DOMAIN_DESCENDANTS_CTE"` exits 0
  </acceptance_criteria>
  <action>
    Add to queries.py. Place the constant near the existing
    `_RESOLVED_FILTER` and `_VALID_KINDS` constants (top of the file):

      _DOMAIN_DESCENDANTS_CTE = """
      WITH RECURSIVE descendants(id) AS (
          SELECT id FROM nodes WHERE name = ? AND kind = 'domain'
          UNION
          SELECT e.dst FROM edges e
          JOIN descendants d ON e.src = d.id
          WHERE e.kind = 'domain_contains_domain'
      )
      """

    Note: we use `UNION` (not `UNION ALL`) so SQLite's recursive CTE
    dedups identical id rows. This is defence-in-depth against a future
    `domain_contains_domain` cycle bug — Phase 31 D-15 currently
    guarantees acyclicity, but explicit dedup costs nothing at the
    bounded sizes we expect.

    Append the helpers to the bottom of queries.py:

      def tests_for_package(
          conn: sqlite3.Connection, *, package_name: str
      ) -> list[SuiteDescription]:
          """Return TestSuites that cover the package via `tests` edges.

          Returns [] when the package has no matching edges. Honors
          `_RESOLVED_FILTER` (D-17): suites whose tests edge has
          resolution='unresolved' are excluded.
          """
          # _RESOLVED_FILTER uses alias `e`; substitute to `t` for our query
          tests_resolved_filter = _RESOLVED_FILTER.replace("e.", "t.")
          rows = conn.execute(
              f"SELECT ts.name, ts.uri, ts.attrs_json, "
              f"(SELECT COUNT(*) FROM edges pc "
              f" WHERE pc.src = ts.id AND pc.kind='physically_contains') AS fc "
              f"FROM edges t "
              f"JOIN nodes ts ON t.src = ts.id "
              f"JOIN nodes p ON t.dst = p.id "
              f"WHERE t.kind='tests' AND ts.kind='test_suite' "
              f"AND p.kind='package' AND p.name = ? "
              f"AND {tests_resolved_filter} "
              f"ORDER BY ts.name",
              (package_name,),
          ).fetchall()
          return [_load_suite_description(r) for r in rows]


      def entry_points_for_package(
          conn: sqlite3.Connection, *, package_name: str
      ) -> list[EntryPointDescription]:
          """Return EntryPoints declared by the package, sorted by name."""
          rows = conn.execute(
              "SELECT ep.name, ep.uri, ep.attrs_json, f.path "
              "FROM nodes pkg "
              "JOIN edges de ON de.src = pkg.id AND de.kind='declares_entry_point' "
              "JOIN nodes ep ON ep.id = de.dst AND ep.kind='entry_point' "
              "LEFT JOIN edges ib ON ib.src = ep.id AND ib.kind='implemented_by' "
              "LEFT JOIN nodes f ON f.id = ib.dst AND f.kind='file' "
              "WHERE pkg.kind='package' AND pkg.name = ? "
              "ORDER BY ep.name",
              (package_name,),
          ).fetchall()
          return [_load_entry_point_description(r) for r in rows]
  </action>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add tests_for_domain bubble-up helper with D-09 UNION</name>
  <read_first>
    - packages/graph-io/src/graph_io/queries.py (the helpers from Task 1; tests_for_domain goes next)
    - .planning/phases/32-query-layer-extensions/32-CONTEXT.md D-09 (UNION shape)
    - .planning/phases/32-query-layer-extensions/32-CONTEXT.md `<specifics>` D-09 SQL draft (transliterate to lowercase + src/dst)
    - .planning/phases/31-domain-layer-derived-edges/31-CONTEXT.md D-12 (TestSuite → Domain only for single-domain suites), D-13 (multi-domain suites have NO Domain edge)
  </read_first>
  <files>packages/graph-io/src/graph_io/queries.py</files>
  <acceptance_criteria>
    - `grep -n "def tests_for_domain(conn" packages/graph-io/src/graph_io/queries.py` returns one line
    - The function body contains `_DOMAIN_DESCENDANTS_CTE` interpolation — verified by `grep -A 30 "def tests_for_domain(conn" packages/graph-io/src/graph_io/queries.py | grep "_DOMAIN_DESCENDANTS_CTE"` returning at least one match
    - The SQL contains both UNION branches: `kind='tests'` (direct edge) AND `kind='belongs_to_domain'` (indirect via package) — verified by `grep -A 30 "def tests_for_domain(conn" packages/graph-io/src/graph_io/queries.py | grep -cE "(kind='tests'|kind='belongs_to_domain')"` returning at least 2
    - Returns `list[SuiteDescription]` — verified by signature grep
    - `uv run --package graph-io python -c "from graph_io.queries import tests_for_domain; print('ok')"` exits 0
  </acceptance_criteria>
  <action>
    Append to queries.py (after entry_points_for_package):

      def tests_for_domain(
          conn: sqlite3.Connection, *, domain_name: str
      ) -> list[SuiteDescription]:
          """Return TestSuites covering the domain or any descendant.

          D-09 UNION:
            (a) direct TestSuite -> Domain edge (Phase 31 D-12 single-domain)
            (b) indirect via TestSuite -> Package -> belongs_to_domain
                (Phase 31 D-13 multi-domain inferred at query time)
          """
          sql = _DOMAIN_DESCENDANTS_CTE + """
              SELECT ts_name, ts_uri, ts_attrs, fc FROM (
                  SELECT ts.id AS ts_id, ts.name AS ts_name, ts.uri AS ts_uri,
                         ts.attrs_json AS ts_attrs,
                         (SELECT COUNT(*) FROM edges pc
                          WHERE pc.src = ts.id AND pc.kind='physically_contains') AS fc
                  FROM edges e
                  JOIN descendants d ON e.dst = d.id
                  JOIN nodes ts ON e.src = ts.id
                  WHERE e.kind='tests' AND ts.kind='test_suite'
                  UNION
                  SELECT ts.id AS ts_id, ts.name AS ts_name, ts.uri AS ts_uri,
                         ts.attrs_json AS ts_attrs,
                         (SELECT COUNT(*) FROM edges pc
                          WHERE pc.src = ts.id AND pc.kind='physically_contains') AS fc
                  FROM edges st
                  JOIN nodes p ON st.dst = p.id AND p.kind='package'
                  JOIN edges bt ON bt.src = p.id AND bt.kind='belongs_to_domain'
                  JOIN descendants d ON bt.dst = d.id
                  JOIN nodes ts ON st.src = ts.id
                  WHERE st.kind='tests' AND ts.kind='test_suite'
              )
              ORDER BY ts_name
          """
          rows = conn.execute(sql, (domain_name,)).fetchall()
          return [_load_suite_description(r) for r in rows]
  </action>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add domain_references and domain_depends_on bubble-up helpers</name>
  <read_first>
    - packages/graph-io/src/graph_io/queries.py (the helpers from Tasks 1-2)
    - .planning/phases/32-query-layer-extensions/32-CONTEXT.md D-08 (aggregation strategy)
    - .planning/phases/31-domain-layer-derived-edges/31-CONTEXT.md D-07/D-18 (usage_count attr lives in attrs_json), D-08/D-09 (no self-loops)
  </read_first>
  <files>packages/graph-io/src/graph_io/queries.py</files>
  <acceptance_criteria>
    - `grep -n "def domain_references(conn" packages/graph-io/src/graph_io/queries.py` returns one line
    - `grep -n "def domain_depends_on(conn" packages/graph-io/src/graph_io/queries.py` returns one line
    - `domain_references` returns `list[tuple[str, int, int]]` — package_name, total_usage_count, distinct_domain_count
    - `domain_depends_on` returns `list[tuple[str, int]]` — target_domain_name, total_usage_count
    - `domain_depends_on` excludes self-loops via `NOT IN (SELECT id FROM descendants)` — verified by `grep -A 30 "def domain_depends_on" packages/graph-io/src/graph_io/queries.py | grep -E "NOT IN.*descendants"` returning at least one match
    - Both helpers' SQL uses `json_extract(e.attrs_json, '$.usage_count')` for the usage_count aggregation — verified by grep
  </acceptance_criteria>
  <action>
    Append to queries.py:

      def domain_references(
          conn: sqlite3.Connection, *, domain_name: str
      ) -> list[tuple[str, int, int]]:
          """Bubble-up package references from the domain + its descendants.

          Returns rows of (package_name, total_usage_count, distinct_domain_count)
          ordered by total_usage_count DESC then package_name ASC.
          """
          sql = _DOMAIN_DESCENDANTS_CTE + """
              SELECT n.name,
                     COALESCE(SUM(CAST(
                       json_extract(e.attrs_json, '$.usage_count') AS INTEGER
                     )), 0) AS total_usage,
                     COUNT(DISTINCT e.src) AS distinct_domains
              FROM edges e
              JOIN descendants d ON e.src = d.id
              JOIN nodes n ON e.dst = n.id
              WHERE e.kind='references' AND n.kind='package'
              GROUP BY n.name
              ORDER BY total_usage DESC, n.name ASC
          """
          rows = conn.execute(sql, (domain_name,)).fetchall()
          return [(r[0], int(r[1] or 0), int(r[2] or 0)) for r in rows]


      def domain_depends_on(
          conn: sqlite3.Connection, *, domain_name: str
      ) -> list[tuple[str, int]]:
          """Bubble-up domain dependencies from the domain + its descendants.

          Excludes self-loops (any descendant depending on any other
          descendant of the same root). Returns rows of
          (target_domain_name, total_usage_count) ordered by usage DESC
          then name ASC.
          """
          sql = _DOMAIN_DESCENDANTS_CTE + """
              SELECT n.name,
                     COALESCE(SUM(CAST(
                       json_extract(e.attrs_json, '$.usage_count') AS INTEGER
                     )), 0) AS total_usage
              FROM edges e
              JOIN descendants d ON e.src = d.id
              JOIN nodes n ON e.dst = n.id
              WHERE e.kind='depends_on' AND n.kind='domain'
              AND n.id NOT IN (SELECT id FROM descendants)
              GROUP BY n.name
              ORDER BY total_usage DESC, n.name ASC
          """
          rows = conn.execute(sql, (domain_name,)).fetchall()
          return [(r[0], int(r[1] or 0)) for r in rows]
  </action>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Add cross_cutting_packages helper</name>
  <read_first>
    - packages/graph-io/src/graph_io/queries.py (after Task 3)
    - .planning/phases/32-query-layer-extensions/32-CONTEXT.md D-10, D-11, D-12 (ranking metric, output shape, tie-break)
    - .planning/phases/32-query-layer-extensions/32-RESEARCH.md §8 (D-11 spec divergence — query-layer choice, not spec amendment)
  </read_first>
  <files>packages/graph-io/src/graph_io/queries.py</files>
  <acceptance_criteria>
    - `grep -n "def cross_cutting_packages(conn" packages/graph-io/src/graph_io/queries.py` returns one line
    - The function docstring contains the substring "D-11" AND the phrase "rendering choice" (or similar) acknowledging the deliberate divergence from ontology spec §11.4 — verified by `grep -A 10 "def cross_cutting_packages(conn" packages/graph-io/src/graph_io/queries.py | grep -iE "(D-11|rendering choice|spec amendment)"` returning at least one match
    - Returns `list[tuple[PackageDescription, int]]` — verified by signature grep
    - SQL filters out packages with any `belongs_to_domain` edge via `NOT EXISTS` — verified by `grep -A 20 "def cross_cutting_packages(conn" packages/graph-io/src/graph_io/queries.py | grep -E "NOT EXISTS.*belongs_to_domain"` returning at least one match
    - SQL sums `usage_count` from `references` edges (the `dst` column targets the package; `e.dst = n.id`) — verified by grep
    - Tie-break is `ORDER BY score DESC, n.name ASC` — verified by grep
  </acceptance_criteria>
  <action>
    Append to queries.py:

      def cross_cutting_packages(
          conn: sqlite3.Connection,
      ) -> list[tuple[PackageDescription, int]]:
          """Packages with zero `belongs_to_domain` edges, ranked.

          Ranked by SUM of `usage_count` across incoming `references` edges
          (D-11). This is a deliberate divergence from ontology spec §11.4
          ("ranked by incoming references count from distinct domains"):
          the rendering choice prioritises 'how heavily depended on' over
          'how broadly depended on'. This is a query-layer rendering
          choice, NOT a spec amendment — ONTOLOGY-SPEC.md stays as
          written.

          Returns list of (PackageDescription, score) sorted by score
          descending, ties broken alphabetically by package name (D-12).
          """
          rows = conn.execute(
              "SELECT n.id, n.name, "
              "COALESCE(SUM(CAST("
              "  json_extract(e.attrs_json, '$.usage_count') AS INTEGER"
              ")), 0) AS score "
              "FROM nodes n "
              "LEFT JOIN edges e ON e.dst = n.id AND e.kind='references' "
              "WHERE n.kind='package' "
              "AND NOT EXISTS ("
              "  SELECT 1 FROM edges bt "
              "  WHERE bt.src = n.id AND bt.kind='belongs_to_domain'"
              ") "
              "GROUP BY n.id, n.name "
              "ORDER BY score DESC, n.name ASC"
          ).fetchall()
          out: list[tuple[PackageDescription, int]] = []
          for _id, name, score in rows:
              desc = describe_package(conn, name=name)
              if desc is None:
                  continue  # defensive — shouldn't happen
              out.append((desc, int(score or 0)))
          return out
  </action>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Add Wave 2 tests including bubble-up correctness, ranking, and the CTE cycle-safety test</name>
  <read_first>
    - packages/graph-io/tests/test_queries.py (current contents — Wave 0 + Wave 1 tests)
    - packages/graph-io/src/graph_io/upsert.py (_upsert_node, _upsert_edge signatures for the cycle-safety test)
    - packages/graph-io/src/graph_io/schema.py (apply_schema for cycle-test setup)
    - .planning/phases/32-query-layer-extensions/32-VALIDATION.md (Wave 2 task rows)
  </read_first>
  <files>packages/graph-io/tests/test_queries.py</files>
  <acceptance_criteria>
    - `grep -nE "def test_(tests_for_package|tests_for_domain_union|tests_for_domain_bubbles|entry_points_for_package|domain_references_bubble|domain_depends_on_no_self_loop|cross_cutting_packages_ranking|cte_cycle_safe)" packages/graph-io/tests/test_queries.py | wc -l` returns at least 8
    - At least one test uses `_upsert_node` and `_upsert_edge` (for the cycle-safety test and for targeted-edge-case fixtures) — verified by `grep -c "_upsert_node\|_upsert_edge" packages/graph-io/tests/test_queries.py` returning at least 2
    - `test_cte_cycle_safe` includes a wall-clock guard (`signal.alarm` OR `pytest.mark.timeout(5)` OR a thread-based timer with `threading.Timer`) — verified by `grep -A 30 "def test_cte_cycle_safe" packages/graph-io/tests/test_queries.py | grep -E "(signal.alarm|timeout|Timer)"` returning at least one match
    - All new tests pass green OR skip cleanly: `uv run --package graph-io pytest packages/graph-io/tests/test_queries.py -x` exits 0
    - Empty-DB variants exist for each Wave 2 helper — verified by `grep -cE "_returns_empty_on_empty_db" packages/graph-io/tests/test_queries.py` returning at least 6 (the 6 Wave 2 helpers)
  </acceptance_criteria>
  <action>
    Append to packages/graph-io/tests/test_queries.py:

      import signal
      import sqlite3 as _sqlite3

      from graph_io.queries import (
          cross_cutting_packages,
          domain_depends_on,
          domain_references,
          entry_points_for_package,
          tests_for_domain,
          tests_for_package,
      )
      from graph_io.schema import apply_schema
      from graph_io.upsert import _upsert_edge, _upsert_node


      # --- happy-path tests against seeded_db ----------------------------

      def test_tests_for_package(seeded_db):
          pkgs = list_packages(seeded_db)
          assert pkgs
          # Find a package that has at least one tests edge
          for pkg in pkgs:
              suites = tests_for_package(seeded_db, package_name=pkg.name)
              if suites:
                  for s in suites:
                      assert isinstance(s, SuiteDescription)
                  return
          pytest.skip("seeded_db has no package with tests edges")


      def test_entry_points_for_package(seeded_db):
          pkgs = list_packages(seeded_db)
          assert pkgs
          for pkg in pkgs:
              eps = entry_points_for_package(seeded_db, package_name=pkg.name)
              if eps:
                  for e in eps:
                      assert isinstance(e, EntryPointDescription)
                  # Alphabetical sort
                  names = [e.name for e in eps]
                  assert names == sorted(names)
                  return
          pytest.skip("seeded_db has no package with EntryPoints")


      def test_tests_for_domain_union(seeded_db):
          _skip_if_phase31_missing(seeded_db)
          domains = list_domains(seeded_db)
          assert domains
          # Find a domain that has at least one TestSuite via either branch
          for dom in domains:
              suites = tests_for_domain(seeded_db, domain_name=dom.name)
              if suites:
                  for s in suites:
                      assert isinstance(s, SuiteDescription)
                  # No duplicates (UNION dedup)
                  names = [s.name for s in suites]
                  assert len(names) == len(set(names))
                  return
          pytest.skip("seeded_db has no domain with TestSuites")


      def test_domain_references_bubble(seeded_db):
          _skip_if_phase31_missing(seeded_db)
          # Pick the domain with the deepest hierarchy (heuristic: pick all,
          # call domain_references on each, expect at least one row)
          for dom in list_domains(seeded_db):
              refs = domain_references(seeded_db, domain_name=dom.name)
              if refs:
                  for pkg_name, total, distinct in refs:
                      assert isinstance(pkg_name, str)
                      assert total >= 0
                      assert distinct >= 0
                  # Ordering: total DESC, name ASC
                  totals = [r[1] for r in refs]
                  assert totals == sorted(totals, reverse=True)
                  return
          pytest.skip("seeded_db has no domain with references")


      def test_domain_depends_on_no_self_loop(seeded_db):
          _skip_if_phase31_missing(seeded_db)
          for dom in list_domains(seeded_db):
              deps = domain_depends_on(seeded_db, domain_name=dom.name)
              if deps:
                  # The named domain MUST NOT appear in its own deps list
                  assert dom.name not in [d[0] for d in deps]
                  return
          pytest.skip("seeded_db has no domain with depends_on edges")


      def test_cross_cutting_packages_ranking(seeded_db):
          _skip_if_phase31_missing(seeded_db)
          result = cross_cutting_packages(seeded_db)
          for pkg, score in result:
              assert isinstance(pkg, PackageDescription)
              assert isinstance(score, int)
              assert score >= 0
              # Cross-cutting => zero domains
              assert pkg.domains == []
          # Sort: score DESC, name ASC
          for i in range(len(result) - 1):
              a, b = result[i], result[i + 1]
              assert a[1] >= b[1]
              if a[1] == b[1]:
                  assert a[0].name <= b[0].name


      # --- empty-DB graceful degradation --------------------------------

      def test_wave2_helpers_return_empty_on_empty_db(empty_db):
          assert tests_for_package(empty_db, package_name="x") == []
          assert entry_points_for_package(empty_db, package_name="x") == []
          assert tests_for_domain(empty_db, domain_name="x") == []
          assert domain_references(empty_db, domain_name="x") == []
          assert domain_depends_on(empty_db, domain_name="x") == []
          assert cross_cutting_packages(empty_db) == []


      # --- targeted edge-case: tests_for_domain UNION branches separately --

      def test_tests_for_domain_direct_edge_only(empty_db):
          """Only branch (a): direct TestSuite -> Domain edge."""
          dom_id = _upsert_node(
              empty_db, kind="domain", name="billing", path=None,
              line=None, attrs={"description": "x"}, uri="domain:acme/repo/billing",
          )
          suite_id = _upsert_node(
              empty_db, kind="test_suite", name="test_billing", path="tests/billing",
              line=None, attrs={"suite_kind": "unit"},
              uri="test_suite:acme/repo/test_billing",
          )
          _upsert_edge(empty_db, src=suite_id, dst=dom_id, kind="tests", attrs={})
          empty_db.commit()
          result = tests_for_domain(empty_db, domain_name="billing")
          assert len(result) == 1
          assert result[0].name == "test_billing"


      def test_tests_for_domain_indirect_via_package(empty_db):
          """Only branch (b): TestSuite -> Package -> belongs_to_domain."""
          dom_id = _upsert_node(
              empty_db, kind="domain", name="billing", path=None,
              line=None, attrs={}, uri="domain:acme/repo/billing",
          )
          pkg_id = _upsert_node(
              empty_db, kind="package", name="bill-svc", path="packages/bill-svc",
              line=None, attrs={"language": "python"},
              uri="pkg:acme/repo/bill-svc",
          )
          suite_id = _upsert_node(
              empty_db, kind="test_suite", name="test_multi", path="tests/multi",
              line=None, attrs={"suite_kind": "integration"},
              uri="test_suite:acme/repo/test_multi",
          )
          _upsert_edge(empty_db, src=pkg_id, dst=dom_id, kind="belongs_to_domain", attrs={})
          _upsert_edge(empty_db, src=suite_id, dst=pkg_id, kind="tests", attrs={})
          empty_db.commit()
          result = tests_for_domain(empty_db, domain_name="billing")
          assert len(result) == 1
          assert result[0].name == "test_multi"


      # --- defence in depth: CTE must not hang on cycle ------------------

      def test_cte_cycle_safe(empty_db):
          """Paranoid test: insert a domain_contains_domain cycle and confirm
          tests_for_domain returns within 5 seconds. Phase 31 D-15
          guarantees acyclicity, but we want defence in depth.
          """
          a_id = _upsert_node(
              empty_db, kind="domain", name="a", path=None, line=None,
              attrs={}, uri="domain:acme/repo/a",
          )
          b_id = _upsert_node(
              empty_db, kind="domain", name="b", path=None, line=None,
              attrs={}, uri="domain:acme/repo/b",
          )
          # Cycle: a contains b AND b contains a
          _upsert_edge(empty_db, src=a_id, dst=b_id, kind="domain_contains_domain", attrs={})
          _upsert_edge(empty_db, src=b_id, dst=a_id, kind="domain_contains_domain", attrs={})
          empty_db.commit()

          # Use signal.alarm for the timeout (POSIX only; if Windows is in
          # scope add a threading.Timer fallback)
          def _timeout(*_args):
              raise TimeoutError("CTE hung")

          old = signal.signal(signal.SIGALRM, _timeout)
          signal.alarm(5)
          try:
              result = tests_for_domain(empty_db, domain_name="a")
              # No test suites exist; result is []. The test passes as
              # long as we didn't hit the alarm.
              assert result == []
          finally:
              signal.alarm(0)
              signal.signal(signal.SIGALRM, old)

    Reuse existing imports; add only those not yet imported. If
    `signal` is not POSIX-available (Windows), guard with
    `pytest.skipif(sys.platform == 'win32', reason='POSIX signal')` on
    `test_cte_cycle_safe` and document that the Linux CI run covers it.
  </action>
</task>

</tasks>

<verification>
- All 6 new helpers exist in queries.py with correct signatures
- `_DOMAIN_DESCENDANTS_CTE` constant is module-private and uses src/dst (not parent_id/child_id) with lowercase 'domain' kind
- All SQL fragments use lowercase node kinds and `src`/`dst` edge columns
- `domain_depends_on` excludes self-loops via NOT IN (SELECT id FROM descendants)
- `cross_cutting_packages` ranks by SUM(usage_count) and tie-breaks alphabetically; docstring acknowledges D-11 spec divergence
- All Wave 2 tests pass or skip with the Phase-31-dependency message
- `test_cte_cycle_safe` proves the CTE terminates within 5s even on a manually inserted cycle
- `uv run --package graph-io pytest packages/graph-io/tests/test_queries.py -v` exits 0 with 25+ tests passed/skipped
- Final phase-level check: all 16 helpers from CONTEXT.md `<domain>` paragraph 4 are present (4 describe_*, 6 list_*, 2 *_for_package, 4 domain helpers = 16)
</verification>

<phase_completion_notes>
After plan 03 lands and tests pass green against Phase 31's shipped DB:
- SC#1 surrogate: `find(seeded_db, kind=k)` for k in {repository, domain, entry_point, test_suite} returns non-empty rows (test_find_per_kind)
- SC#2: `describe_package(seeded_db, name='mypkg').domains / entry_points / test_suites` are populated (test_describe_package_extended)
- SC#3: `describe_path(seeded_db, path=<a File path>).role_flags` is a dict with exactly 7 keys (test_describe_path_role_flags)
- SC#4: every helper in the SC#4 list has a unit test passing against `seeded_db` (15+ test functions in test_queries.py)

Phase 33 picks up: wires every Phase 32 helper to a `cg` subcommand. No
new helpers required in Phase 33 — only the CLI surface.
</phase_completion_notes>
