---
phase: 58-entity-page-index-uat-follow-ups
plan: 03
type: execute
wave: 2
depends_on: ["58-02"]
files_modified:
  - packages/wiki-io/src/wiki_io/index_generator.py
  - packages/wiki-io/tests/test_index_generator.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "In the generated index `## By Kind` section, each package/app nests only the test suite(s) that actually test it (SC#3, D-07/D-08)"
    - "Consumer-package resolution keys on the test_suite node URI, not the shared name (D-08)"
    - "A fan-out regression guard proves distinct per-suite results when names would collide but URIs differ"
    - "The live-graph index snapshot is rebaselined against the renamed nodes + uri-keyed renderer (D-10)"
  artifacts:
    - path: "packages/wiki-io/src/wiki_io/index_generator.py"
      provides: "URI-keyed consumer-package resolution in all three queries"
      contains: "ts.uri = ?"
    - path: "packages/wiki-io/tests/test_index_generator.py"
      provides: "Fan-out regression guard"
      contains: "_consumer_pkgs"
  key_links:
    - from: "index_generator.py _place_entities"
      to: "_consumer_pkgs / _consumer_pkgs_in_domain / _compute_qualifying_domains"
      via: "passes node uri (attrs['uri']) instead of node.name"
      pattern: "entity_uri|ts\\.uri = \\?"
---

<objective>
Fix the renderer-side half of Item #3 (SC#3, D-07/D-08). Three SQL queries in `index_generator.py` resolve consumer packages by `ts.name = ?`. Because all package-owned suites previously shared `name='tests'`, every suite matched every other suite's `tests` edges and nested under every package. Switch all three to key on `ts.uri = ?` (URIs are unique and stable — RESEARCH §"URI impact"), and thread `node.attrs["uri"]` (already extracted as `uri` at `_place_entities:353`) through the callsites instead of `node.name`.

RESEARCH found a THIRD query beyond CONTEXT's two: `_compute_qualifying_domains` (line 197) is also name-keyed. Fix all three: `_compute_qualifying_domains` (197), `_consumer_pkgs_in_domain` (251), `_consumer_pkgs` (287).

Add the Wave 0 fan-out regression guard, then rebaseline the live-graph syrupy snapshot (D-10) now that both the graph-io rename (Plan 02) and this renderer fix are in place.

Purpose: Each package nests only its own suites in the generated index — the headline UAT defect.

Output: Edited `index_generator.py` (three queries + three caller threads), a fan-out regression test, and an updated syrupy snapshot.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-entity-page-index-uat-follow-ups/58-CONTEXT.md
@.planning/phases/58-entity-page-index-uat-follow-ups/58-RESEARCH.md
@.planning/phases/58-entity-page-index-uat-follow-ups/58-VALIDATION.md
@.planning/phases/58-entity-page-index-uat-follow-ups/58-02-SUMMARY.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase (verified line numbers). -->
From packages/wiki-io/src/wiki_io/index_generator.py — the three queries to change (all currently `ts.name = ?`):
- `_compute_qualifying_domains` (def line 163; SQL `AND ts.kind='test_suite' AND ts.name = ?` at line 197) — caller at line 354 passes `name=node.name`
- `_consumer_pkgs_in_domain` (def line 224; SQL `... AND ts.name = ?` at line 251; signature `*, kind, entity_name, domain_name`)
- `_consumer_pkgs` (def line 260; SQL `... AND ts.name = ?` at line 287; signature `*, kind, entity_name`) — caller at line 360 passes `entity_name=node.name`

URI already in hand: `_place_entities` extracts `uri = node.attrs.get("uri") or ""` at line 353; `PlacedEntity.uri` (line 131) is populated. `nodes.uri` is a first-class column (Phase 28), so `ts.uri` in SQL is correct.

Sub-nesting plumbing (no SQL, no change to logic): `_build_sub_for_pkg` (598), `sub_for_pkg` dict consumed by `_render_pkg_nested` (574). These read `PlacedEntity.parent_pkg_names` which is populated from the `_consumer_pkgs*` results — fixing the queries fixes the nesting.

DEPENDS ON Plan 02: this fix is validated against the renamed (uniquely-named) suite nodes. URIs are unchanged by the rename, so the uri-keyed query is correct regardless, but the snapshot rebaseline requires Plan 02's rename to be live.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Switch three consumer-resolution queries from ts.name to ts.uri and thread URI through callers</name>
  <files>
    packages/wiki-io/src/wiki_io/index_generator.py
    packages/wiki-io/tests/test_index_generator.py
  </files>
  <read_first>
    - packages/wiki-io/src/wiki_io/index_generator.py (lines 163-221 `_compute_qualifying_domains`; 224-257 `_consumer_pkgs_in_domain`; 260-293 `_consumer_pkgs`; 320-394 `_place_entities` incl. line 353 uri extraction, 354 + 360 callsites)
    - 58-RESEARCH.md §"wiki-io renderer-side fix" (the three exact query/caller changes) and §"Common Pitfalls" Pitfall 3 (passing entity_name instead of entity_uri returns 0 rows)
    - 58-CONTEXT.md D-07/D-08
  </read_first>
  <behavior>
    - Fan-out regression guard: given two package-owned suites with the SAME `name` but DISTINCT `uri` values (and distinct `tests` edges to different packages), `_consumer_pkgs` returns the correct single consumer package for each suite — not the union
    - `_consumer_pkgs_in_domain` exhibits the same per-suite correctness when called with distinct URIs
    - When called with a URI that matches no suite, the queries return an empty result (not a fallback to name)
  </behavior>
  <action>
    In `index_generator.py`, change the SQL in all THREE queries from `ts.name = ?` to `ts.uri = ?`: `_compute_qualifying_domains` (line 197), `_consumer_pkgs_in_domain` (line 251), and `_consumer_pkgs` (line 287). Rename the corresponding function parameter from `entity_name`/`name` to `entity_uri` (or a clearly uri-named param) in `_consumer_pkgs` and `_consumer_pkgs_in_domain`, and for `_compute_qualifying_domains` change the `name=` parameter to pass the uri. At the callsites in `_place_entities` (line 354 `_compute_qualifying_domains(conn, kind=kind, name=node.name)` and line 360 `_consumer_pkgs(conn, kind=kind, entity_name=node.name)`), pass the already-extracted `uri` (from line 353 `uri = node.attrs.get("uri") or ""`) instead of `node.name` (RESEARCH Pitfall 3 — passing name to a uri-keyed query silently returns 0 rows). Trace any other callsite of these three functions (grep the file) and update each to pass uri. Do NOT change `test_suite_uri` or any graph-io code. Add a fan-out regression guard test to `test_index_generator.py` per the `<behavior>` block, using a hand-built fixture graph with two same-named suites carrying distinct URIs and distinct `tests` edges (the existing test fixtures in that file use arbitrary suite names like `"suite-a"` — follow that pattern).
  </action>
  <verify>
    <automated>uv run --package wiki-io pytest packages/wiki-io/tests/test_index_generator.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ts.name = ?' packages/wiki-io/src/wiki_io/index_generator.py` returns 0 (no name-keyed suite resolution remains)
    - `grep -c 'ts.uri = ?' packages/wiki-io/src/wiki_io/index_generator.py` returns 3 (all three queries switched)
    - No callsite of `_consumer_pkgs`/`_consumer_pkgs_in_domain`/`_compute_qualifying_domains` passes `node.name` for the suite-resolution argument (`grep -n 'entity_name=node.name\|name=node.name' packages/wiki-io/src/wiki_io/index_generator.py` returns nothing for these three calls)
    - A new fan-out regression test exists and passes (two same-named, distinct-URI suites resolve to distinct consumer packages)
    - `uv run --package wiki-io pytest packages/wiki-io/tests/test_index_generator.py -x` exits 0
  </acceptance_criteria>
  <done>All three consumer-resolution queries key on `ts.uri`; every callsite passes the node URI; a fan-out regression guard proves distinct per-suite results; the index_generator test module is green.</done>
</task>

<task type="auto">
  <name>Task 2: Rebaseline the live-graph index snapshot and run full cross-package suite (D-10)</name>
  <files>
    packages/wiki-io/tests/test_index_generator.py
  </files>
  <read_first>
    - packages/wiki-io/tests/test_index_generator.py (line ~1143 `test_snapshot_against_agent_research` — syrupy snapshot, skipped when no live graph)
    - 58-RESEARCH.md §"Fixture Cascade and Rebaseline Scope" → "wiki-io fixtures" + "Regeneration commands"
    - 58-02-SUMMARY.md (confirms the graph-io rename landed + the `cg update --full` stale-node resolution)
    - 58-CONTEXT.md D-10 (regenerate from the fixed generator) and .claude/rules/backward-compatibility.md (entity content regenerated at will)
  </read_first>
  <action>
    Ensure the live graph reflects the Plan 02 rename: if the snapshot test runs against a live `graph.db`, perform a fresh `cg update --full` first (per 58-02-SUMMARY OQ#3 resolution) so no stale `tests`-named suite nodes remain. Then regenerate the syrupy snapshot for `test_snapshot_against_agent_research` with `--snapshot-update`. If no live graph is available in the execution environment (the test is skip-guarded), record that the snapshot regeneration is deferred to a live-graph run and rely on the hand-built fixture tests + the fan-out regression guard (Task 1) as the automated proof; note this in the SUMMARY. Other `test_index_generator.py` tests use hand-built fixture graphs with arbitrary suite names and need no change (RESEARCH §"wiki-io fixtures"). Finally run the full cross-package suite to confirm Item #3 holds end to end across graph-io + wiki-io.
  </action>
  <verify>
    <automated>uv run pytest -x -q</automated>
  </verify>
  <acceptance_criteria>
    - If a live graph is present: `uv run --package wiki-io pytest packages/wiki-io/tests/test_index_generator.py::test_snapshot_against_agent_research -x` exits 0 against the regenerated snapshot (and the snapshot shows each package nesting only its own suites)
    - If no live graph is present: the snapshot test is skipped (not failed), and the SUMMARY records the deferral with the hand-built fan-out guard as the standing proof
    - `uv run pytest -x -q` exits 0 (full cross-package suite green — Item #3 spans graph-io + wiki-io)
    - The generated index no longer nests the same nine `tests`-named suites under every package (verified by the fan-out guard from Task 1 and, where available, the regenerated snapshot)
  </acceptance_criteria>
  <done>The index snapshot is rebaselined against renamed nodes + uri-keyed renderer (or deferral recorded with the fan-out guard standing in); the full cross-package suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | The `ts.name = ?` → `ts.uri = ?` change is a column swap within already-parameterized SQL against a local trusted SQLite graph; no string interpolation, no new input source |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-03 | Tampering | index_generator consumer-resolution SQL | accept | No new attack surface — queries remain parameter-bound (`ts.uri = ?`), only the bound column changed; per RESEARCH §"Security Domain" no auth/input-validation/crypto change |
| T-58-SC | Tampering | npm/pip/cargo installs | accept | No new package installs; pytest + syrupy already present (VALIDATION.md) |
</threat_model>

<verification>
- `uv run pytest -x -q` is green (full cross-package suite)
- All three consumer-resolution queries key on `ts.uri`; no `ts.name = ?` remains in index_generator.py
- Fan-out regression guard passes; snapshot rebaselined (or deferral recorded)
</verification>

<success_criteria>
- SC#3: In the generated index `## By Kind` section, each package/app nests only the test suite(s) that actually test it; resolution keys on test_suite uri
</success_criteria>

<output>
Create `.planning/phases/58-entity-page-index-uat-follow-ups/58-03-SUMMARY.md` when done
</output>
