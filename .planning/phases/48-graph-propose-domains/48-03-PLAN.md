---
phase: 48-graph-propose-domains
plan: 03
type: execute
wave: 3
depends_on: ["48-02"]
files_modified:
  - agents/graph-wiki-agent/src/graph_wiki_agent/commands/graph.py
  - agents/graph-wiki-agent/src/graph_wiki_agent/commands/propose_domains.py
  - agents/graph-wiki-agent/tests/integration/test_propose_domains_e2e.py
  - agents/graph-wiki-agent/tests/integration/test_propose_domains_isolation.py
autonomous: true
requirements: [PROPOSE-01, PROPOSE-05, PROPOSE-06]
must_haves:
  truths:
    - "D-17: Research confirms `graph_io.domains._load_domains_yaml` reads `domains.yaml` by literal filename and no other graph-io code path references `domains.proposed.yaml`; no allowlist edit needed; the isolation guarantee is regression-tested by Task 48-03-02"
    - "D-18: PROPOSE-05 acceptance test is implemented as `tests/integration/test_propose_domains_isolation.py` with three cases; running real `cg update` against a workspace containing both files produces zero `belongs_to_domain` edges from `domains.proposed.yaml`"
    - "D-22: `propose-domains` is registered under `graph_app` via `@graph_app.command(name='propose-domains')`; placement and error-handling style mirror `graph build` / `graph describe` registrations in `commands/graph.py`"
    - "D-23: In-process invocation of `compute_clusters` is verified end-to-end by Task 48-03-01 (no subprocess; direct dataclass return)"
    - "`graph-wiki-agent graph propose-domains --workspace <path>` runs end-to-end against a fixture workspace with a stubbed LLM, writes a valid `domains.proposed.yaml`, and writes per-cluster trace records to `<workspace>/.graph-wiki/traces/`"
    - "After running `graph propose-domains` and then `cg update` again, the graph contains zero `belongs_to_domain` edges sourced from `domains.proposed.yaml` — only `domains.yaml` contributes domain edges (PROPOSE-05)"
    - "`--model my-model-id` flag is honored: the trace record's `model_id` field equals `my-model-id` (PROPOSE-06)"
    - "Running `graph propose-domains` against a workspace WITHOUT `domains.yaml` does not crash; existing domains default to empty"
    - "Partial-success: when one cluster's LLM call raises, the other clusters still produce proposals AND the failed cluster id is recorded in `metadata.llm_failures` of `domains.proposed.yaml`"
  artifacts:
    - path: "agents/graph-wiki-agent/src/graph_wiki_agent/commands/graph.py"
      provides: "Registration of the `propose-domains` subcommand under `graph_app`"
      contains: "from graph_wiki_agent.commands.propose_domains import propose_domains_cmd"
    - path: "agents/graph-wiki-agent/tests/integration/test_propose_domains_e2e.py"
      provides: "End-to-end test with stubbed LLM"
      contains: "def test_propose_domains_e2e_stubbed_llm"
    - path: "agents/graph-wiki-agent/tests/integration/test_propose_domains_isolation.py"
      provides: "PROPOSE-05 isolation acceptance test"
      contains: "def test_proposed_yaml_produces_zero_graph_edges"
  key_links:
    - from: "agents/graph-wiki-agent/src/graph_wiki_agent/commands/graph.py"
      to: "agents/graph-wiki-agent/src/graph_wiki_agent/commands/propose_domains.py"
      via: "@graph_app.command(name='propose-domains') registration"
      pattern: "graph_app.command\\(name=[\"']propose-domains[\"']"
    - from: "agents/graph-wiki-agent/tests/integration/test_propose_domains_isolation.py"
      to: "packages/graph-io/src/graph_io/domains.py"
      via: "Asserts that `domains.proposed.yaml` produces no edges in `belongs_to_domain` after `cg update`"
      pattern: "belongs_to_domain"
---

<decisions_implemented>
- **D-17:** Research (RESEARCH F-2) confirmed that `graph_io.domains._load_domains_yaml` reads `repo_root / "domains.yaml"` by literal filename, and no other graph-io module references the `domains.proposed.yaml` filename or any `.proposed.yaml` suffix. **No allowlist edit is needed** — PROPOSE-05 isolation is structurally guaranteed by exact-filename matching. The Task 48-03-02 integration test makes this guarantee a regression-tested invariant rather than an inspection-only claim.
- **D-18:** PROPOSE-05 acceptance test is implemented as `tests/integration/test_propose_domains_isolation.py` with three test cases: (1) a domain-edge assertion proving `domains.proposed.yaml` produces zero `belongs_to_domain` edges; (2) a unique-fake-package belt-and-suspenders test proving the proposed file's contents never appear in the graph; (3) a `cg list-domains` smoke test proving the proposed file does not appear in the listed-domains output.
- **D-22:** `propose-domains` subcommand registered under `graph_app` via `@graph_app.command(name="propose-domains")`; placement and error-handling style mirror `graph build` / `graph describe` registrations in `commands/graph.py`.
- **D-23:** In-process invocation of `compute_clusters` (no subprocess) — verified by Task 48-03-01's e2e test which imports `graph_io.cluster.compute_clusters` and inspects the in-memory `ClusterResult` rather than parsing JSON.
</decisions_implemented>

<objective>
Register the `propose-domains` Typer subcommand under `graph_app`, then verify the full pipeline with two integration tests: a stubbed-LLM end-to-end test (PROPOSE-01, PROPOSE-04, PROPOSE-06) and the PROPOSE-05 isolation acceptance test.

Purpose: PROPOSE-01 needs the CLI surface wired up. PROPOSE-05 needs proof that placing `domains.proposed.yaml` next to `domains.yaml` has zero effect on the graph. This plan delivers both as integration tests run against real `cg update`.
Output: Subcommand registered + 2 integration tests (multiple cases each) passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-graph-propose-domains/48-CONTEXT.md
@.planning/phases/48-graph-propose-domains/48-RESEARCH.md
@.planning/phases/48-graph-propose-domains/48-01-PLAN.md
@.planning/phases/48-graph-propose-domains/48-02-PLAN.md
@CLAUDE.md
@agents/graph-wiki-agent/src/graph_wiki_agent/commands/graph.py

<interfaces>
<!-- Symbols Plan 02 produced that Plan 03 consumes. -->

From agents/graph-wiki-agent/src/graph_wiki_agent/commands/propose_domains.py (created by Plan 02):
```python
def propose_domains_cmd(
    workspace: str = typer.Option(..., "--workspace"),
    hub_threshold: float = typer.Option(0.5, "--hub-threshold"),
    model: Optional[str] = typer.Option(None, "--model"),
) -> None: ...

# Helpers also exposed for test reuse:
ProposedDomain, ProposeResult, _PROPOSE_DOMAIN_TOOL
```

From agents/graph-wiki-agent/src/graph_wiki_agent/commands/graph.py:
```python
graph_app = typer.Typer(name="graph", ...)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 48-03-01: Register `propose-domains` subcommand and write end-to-end test with stubbed LLM</name>
  <read_first>
    - agents/graph-wiki-agent/src/graph_wiki_agent/commands/graph.py (existing `@graph_app.command(name="build")` and `@graph_app.command(name="query")` patterns — mirror the registration style; lines 174-260)
    - agents/graph-wiki-agent/src/graph_wiki_agent/commands/propose_domains.py (created by Plan 02 — contains the unregistered `propose_domains_cmd` function)
    - agents/graph-wiki-agent/tests/ (look for existing integration test patterns; if a `tests/integration/` directory does not exist yet, create it)
    - packages/graph-io/tests/fixtures/sample_monorepo/ (existing fixture workspace shape — pyproject.toml manifests, .graph-wiki/ layout)
    - packages/subagent-runtime/src/subagent_runtime/pool.py (FanOutResult shape — for understanding what `pool.run_all` returns under stubbed task)
  </read_first>
  <files>agents/graph-wiki-agent/src/graph_wiki_agent/commands/graph.py, agents/graph-wiki-agent/src/graph_wiki_agent/commands/propose_domains.py, agents/graph-wiki-agent/tests/integration/test_propose_domains_e2e.py, agents/graph-wiki-agent/tests/integration/__init__.py</files>
  <action>
    Step 1 — Register the subcommand. Two compatible options; pick whichever matches the existing repo style after reading `graph.py`:
      (a) In `propose_domains.py`, decorate `propose_domains_cmd` with `@graph_app.command(name="propose-domains")` after adding `from graph_wiki_agent.commands.graph import graph_app` at the top of the module. Then add ONE import line in `commands/graph.py` (or in the package `__init__.py` if cleaner) to ensure `propose_domains.py` is imported at startup so the decorator runs: `from graph_wiki_agent.commands import propose_domains  # noqa: F401` (registration side effect).
      (b) Alternatively, leave `propose_domains_cmd` undecorated and add the decorator in `graph.py` directly: `from .propose_domains import propose_domains_cmd` followed by `graph_app.command(name="propose-domains")(propose_domains_cmd)`.

    Either way: after this task, `uv run --package graph-wiki-agent graph-wiki-agent graph propose-domains --help` (or whatever the CLI entry-point is — check the agent's `[project.scripts]`) must print the help text including `--workspace`, `--hub-threshold`, `--model`. If the CLI entry-point name differs, use the actual entry-point.

    Step 2 — Create `tests/integration/__init__.py` if missing (empty file is fine).

    Step 3 — Write `tests/integration/test_propose_domains_e2e.py` with these test cases:
      - `test_propose_domains_e2e_stubbed_llm`: builds a temp workspace by copying `packages/graph-io/tests/fixtures/sample_monorepo/` to a `tmp_path`; runs `cg update` (or the equivalent helper) to populate `<tmp_path>/.graph-wiki/graph.db`; monkey-patches `model_adapter.loader.make_llm` to return a stub whose `bind_tools(...).ainvoke(...)` yields a deterministic AIMessage with a canned tool-call response per cluster (use `unittest.mock.AsyncMock`); invokes `propose_domains_cmd(workspace=str(tmp_path), hub_threshold=0.5, model=None)`; asserts `(tmp_path / "domains.proposed.yaml").exists()`; loads it with `yaml.safe_load`; asserts top-level keys `proposed_domains` and `metadata`; asserts each `proposed_domains[<name>]` has the five required keys (packages, description, parent, confidence, llm_origin); asserts `metadata.total_cost_usd` is a float >= 0; asserts at least one `.jsonl` file in `<tmp_path>/.graph-wiki/traces/` containing per-cluster cost records.
      - `test_propose_domains_e2e_model_override`: same setup; passes `model="us.amazon.nova-lite-v1:0"`; asserts at least one trace JSONL record has `model_id == "us.amazon.nova-lite-v1:0"` AND the `metadata.model` field in `domains.proposed.yaml` matches.
      - `test_propose_domains_e2e_no_domains_yaml`: temp workspace WITHOUT `domains.yaml`; otherwise same as above; asserts command does not crash; asserts `domains.proposed.yaml` is still written; asserts proposed domain entries have `parent: null` (no existing domain names to suggest).
      - `test_propose_domains_e2e_partial_failure`: stub one cluster's LLM call to raise; assert other clusters still produce proposals; assert `domains.proposed.yaml` `metadata.llm_failures` contains the failed cluster's id; assert exit code is 0 (partial-success per D-01).

    For all four tests, use the standard `pytest.fixture` style. Mark async tests with `@pytest.mark.asyncio` if `propose_domains_cmd` is itself async; otherwise call it synchronously (Typer wraps `asyncio.run` internally — confirm by reading the function signature created in Plan 02).
  </action>
  <verify>
    <automated>uv run --package graph-wiki-agent pytest tests/integration/test_propose_domains_e2e.py -x</automated>
  </verify>
  <done>
    `graph propose-domains` is a registered Typer subcommand under `graph_app`. All four e2e tests pass. `uv run --package graph-wiki-agent pytest -x` exits 0 (no regressions). Help text shows `--workspace`, `--hub-threshold`, `--model` options.
  </done>
</task>

<task type="auto">
  <name>Task 48-03-02: Write PROPOSE-05 isolation acceptance test</name>
  <read_first>
    - packages/graph-io/src/graph_io/domains.py (lines 41-61 — `_load_domains_yaml` reads `domains.yaml` by literal filename; the isolation guarantee depends on this)
    - packages/graph-io/src/graph_io/cli/q_list_domains.py (how domain edges are queried after `cg update`)
    - packages/graph-io/tests/fixtures/sample_monorepo/ (fixture workspace shape)
    - .planning/phases/48-graph-propose-domains/48-CONTEXT.md D-18 (the test recipe)
    - .planning/phases/48-graph-propose-domains/48-RESEARCH.md F-2 (the isolation rationale — exact-filename match in `_load_domains_yaml`)
  </read_first>
  <files>agents/graph-wiki-agent/tests/integration/test_propose_domains_isolation.py</files>
  <action>
    Write `tests/integration/test_propose_domains_isolation.py` with three test cases enforcing PROPOSE-05.

    Setup helper (shared by all tests via a `pytest.fixture`): build a temp workspace under `tmp_path` containing:
      - `pyproject.toml` declaring three Python packages: `foo`, `bar`, `baz` (use the same minimal layout as `sample_monorepo`).
      - `domains.yaml` declaring exactly: `domains: {core: {packages: [foo], parent: null}}`.
      - Run `cg update` (in-process import from `graph_io.cli.ops_update`) to populate the graph at `<tmp_path>/.graph-wiki/graph.db`. Capture the baseline edge set.

    Test cases:
      1. `test_proposed_yaml_produces_zero_graph_edges`: write a manually-constructed `<tmp_path>/domains.proposed.yaml` with body `proposed_domains: {data: {packages: [bar, baz], parent: null, description: x, confidence: 0.5, llm_origin: fan_out}}`. Run `cg update` again. Query the graph: `SELECT src_kind, src_name, dst_kind, dst_name FROM edges WHERE kind = 'belongs_to_domain'` (use sqlite3 directly via `graph_io.store` open helper). Assert: exactly ONE `belongs_to_domain` edge exists (`foo -> core`); `bar -> data` and `baz -> data` are absent.

      2. `test_proposed_yaml_with_fake_package_never_appears`: write a `domains.proposed.yaml` containing a proposed domain with a UNIQUE fake package name like `__FAKE_PROPOSED_PACKAGE_48__`. Run `cg update`. Query ALL nodes and ALL edges in the graph. Assert: `__FAKE_PROPOSED_PACKAGE_48__` appears in zero nodes and zero edge endpoints.

      3. `test_proposed_yaml_does_not_break_normal_domain_loading`: write `domains.proposed.yaml` as in test 1. Run `cg update`. Run `cg list-domains` (or query domains the same way `q_list_domains` does). Assert: exactly one domain `core` is listed; `data` is NOT listed.

    All three tests must NOT mock anything in `graph_io` — they exercise the real `cg update` code path because that is the integrity guarantee being verified.
  </action>
  <verify>
    <automated>uv run --package graph-wiki-agent pytest tests/integration/test_propose_domains_isolation.py -x</automated>
  </verify>
  <done>
    All three isolation tests pass. `uv run --package graph-wiki-agent pytest -x` exits 0. The acceptance gate from CONTEXT D-18 is satisfied: `domains.proposed.yaml` provably has zero effect on the graph.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `domains.proposed.yaml` on disk → `_load_domains_yaml` | File coexists with `domains.yaml` in the workspace root; an accidental schema collision or filename glob would silently promote proposals to authoritative config. |
| Stubbed LLM in tests → command body | Test stub replaces a real network boundary; an over-permissive stub could hide breakages that real Bedrock would surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-02 | Tampering | `domains.proposed.yaml` → graph state | mitigate | Task 48-03-02's `test_proposed_yaml_produces_zero_graph_edges` is the structural defense — it runs the real `cg update` against a workspace containing both files and asserts no contamination. Belt-and-suspenders test (`__FAKE_PROPOSED_PACKAGE_48__`) catches any future change that introduces glob-based domain-file discovery. |
| T-48-03 | Elevation of privilege | `propose_domains_cmd` write path | accept | Command writes only to `<workspace>/domains.proposed.yaml` and `<workspace>/.graph-wiki/traces/`. Paths are resolved via `workspace_io.config.resolve` (existing audited helper). No arbitrary write surface. |
| T-48-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies. |
</threat_model>

<verification>
- `uv run --package graph-wiki-agent pytest tests/integration/ -x` — all integration tests pass.
- `uv run --package graph-wiki-agent pytest -x` — no regressions anywhere.
- CLI manual check (one-shot): `uv run --package graph-wiki-agent graph-wiki-agent graph propose-domains --help` (or actual entry-point) returns help text including `--workspace`, `--hub-threshold`, `--model`.
- `grep -rn "domains.proposed\|\\.proposed\\.yaml\|proposed_domains" packages/graph-io/src/graph_io/` returns zero results — confirming graph-io does not reference the proposed file, the isolation invariant from RESEARCH F-2.
</verification>

<success_criteria>
- `graph propose-domains` is a registered Typer subcommand under `graph_app`.
- 4 e2e tests + 3 isolation tests pass (7 integration tests total).
- PROPOSE-05 isolation is proven by real-`cg update` integration test (not just by inspection).
- Partial-success semantics work (one cluster failure does not abort the run).
- `--model` flag round-trips through to trace records and metadata.
- All five ROADMAP success criteria for Phase 48 are observably true.
</success_criteria>

<output>
Create `.planning/phases/48-graph-propose-domains/48-03-SUMMARY.md` when done.
</output>
