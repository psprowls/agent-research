---
phase: 18-plugin-command-rename
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/code-wiki-agent/src/code_wiki_agent/cli.py
  - agents/code-wiki-agent/tests/unit/test_commands_init.py
  - agents/code-wiki-agent/tests/unit/test_commands_bootstrap.py
  - agents/code-wiki-agent/tests/unit/test_cli_help.py
autonomous: true
requirements: [CMD-02]
must_haves:
  truths:
    - "Typer CLI subcommand is registered as `bootstrap` (not `init`); `code-wiki-agent --help` lists `bootstrap` and does NOT list `init`"
    - "No `def init(` line remains in `agents/code-wiki-agent/src/code_wiki_agent/cli.py`"
    - "Test file `tests/unit/test_commands_init.py` is renamed to `tests/unit/test_commands_bootstrap.py` via git mv; assertions reference `bootstrap` subcommand name"
    - "Help-text smoke test in `test_cli_help.py` asserts `bootstrap` appears, `init` does not"
  artifacts:
    - path: "agents/code-wiki-agent/src/code_wiki_agent/cli.py"
      provides: "Typer `bootstrap` subcommand"
      contains: "def bootstrap("
  key_links:
    - from: "agents/code-wiki-agent/src/code_wiki_agent/cli.py"
      to: "agents/code-wiki-agent/src/code_wiki_agent/commands/init.run_init"
      via: "Function call inside the renamed bootstrap subcommand"
      pattern: "run_init\\("
---

<objective>
Rename the Bedrock CLI Typer subcommand from `init` to `bootstrap` in `agents/code-wiki-agent/src/code_wiki_agent/cli.py`. This is the third user-facing surface (D-02). Includes renaming the function (line 437), updating the docstring/help text if it references the old name, renaming the corresponding test file `test_commands_init.py` → `test_commands_bootstrap.py` via `git mv` (resolving open question Q1), and updating `test_cli_help.py` to assert the new help-text shape.

Purpose: D-06 step 3 in the staged cutover. Closes the Typer CLI portion of CMD-02 (the MCP portion is closed by plan 18-02). Per-commit pytest gate green.

Output: One commit, `code-wiki-agent bootstrap --topic ... --tool ...` works, `code-wiki-agent init` exits with Typer's "no such command" error, file rename preserves git history.

The underlying `from .commands.init import run_init` IMPORT and the `commands/init.py` Python module stay — internal, machine-facing, out of scope per D-02.

**Q1 resolved:** Yes, rename the test file via `git mv`. The file is behavior-focused but small; matching the surface rename gives `grep test_commands_bootstrap` parity with `grep bootstrap` and prevents the test surface drifting from the production surface.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-plugin-command-rename/18-CONTEXT.md
@CLAUDE.md
@agents/code-wiki-agent/src/code_wiki_agent/cli.py

<interfaces>
From `agents/code-wiki-agent/src/code_wiki_agent/cli.py` (lines 437–459, current state):

```
@app.command()
def init(
    topic: str = typer.Option(..., "--topic", ...),
    tool: str = typer.Option(..., "--tool", ...),
    force: bool = typer.Option(False, "--force", ...),
    vault: str = typer.Option("", "--vault", ...),
    json_output: bool = typer.Option(False, "--json", ...),
) -> None:
    """Bootstrap a wiki vault structure (creates raw/ and work/ siblings)."""
    vault_path = Path(vault) if vault else None
    try:
        result = asyncio.run(run_init(topic=topic, tool=tool, force=force, vault_path=vault_path))
    except (RuntimeError, FileNotFoundError, SystemExit) as e:
        typer.echo(f"Error: {e}", err=True)
        raise typer.Exit(code=1)

    if json_output:
        typer.echo(json.dumps(dataclasses.asdict(result), indent=2))
    else:
        typer.echo(f"[ok] Initialized wiki at: {result.wiki_path}")
        typer.echo(f"     raw/: {result.raw_path}")
        typer.echo(f"     work/: {result.work_path}")
```

The `run_init` symbol is imported elsewhere in the file (top of file) from `code_wiki_agent.commands.init`. That import stays.

The function docstring already says "Bootstrap a wiki vault structure (...)" — which is now the Typer auto-help text. After the rename, that docstring becomes the help text for `code-wiki-agent bootstrap --help`, semantically aligned.

The success-line `[ok] Initialized wiki at:` is descriptive prose and may stay as-is OR be updated to `[ok] Bootstrapped wiki at:` — Claude's discretion. The acceptance criteria does not pin this.

From `agents/code-wiki-agent/tests/unit/test_cli_help.py`: this file asserts the subcommand list in `--help` output. Locate the assertions via grep before editing.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rename Typer subcommand `init` → `bootstrap` in cli.py</name>
  <files>agents/code-wiki-agent/src/code_wiki_agent/cli.py</files>
  <read_first>
    - agents/code-wiki-agent/src/code_wiki_agent/cli.py (full file — need to confirm there is no second `def init` anywhere; scan with grep too)
    - .planning/phases/18-plugin-command-rename/18-CONTEXT.md (D-01, D-02, D-04, D-06; brand-gate enforcement decision D-05 — the `def init(` pattern will be enforced by the gate in plan 18-06)
  </read_first>
  <behavior>
    - Test: After rename, running `uv run --package code-wiki-agent code-wiki-agent --help` lists `bootstrap` as a subcommand and does NOT list `init`.
    - Test: `uv run --package code-wiki-agent code-wiki-agent init --help` exits non-zero (Typer "no such command" / `code=2`).
    - Test: `uv run --package code-wiki-agent code-wiki-agent bootstrap --help` exits 0 and prints the docstring "Bootstrap a wiki vault structure" or similar.
    - Test: Source contains exactly one `def bootstrap(` at module level decorated with `@app.command()`; zero `def init(` lines at module level.
  </behavior>
  <action>
    Step 1: Apply the following exact substitution in `cli.py` at the Typer subcommand block currently at lines 437–459:
      - `def init(` → `def bootstrap(` (the function definition name).
      - The `@app.command()` decorator above the function stays untouched — Typer infers the subcommand slug from the function name by default, so renaming the function renames the subcommand.

    Step 2: Confirm no other `def init` exists anywhere in `cli.py` after the rename via `grep -nE '^\s*def init\b' agents/code-wiki-agent/src/code_wiki_agent/cli.py`. Must return 0.

    Step 3: Do NOT rename the import line at the top of the file `from .commands.init import run_init` (or whatever the exact import is). The internal module `commands/init.py` is out of scope per D-02. The body of the renamed `bootstrap` function continues to call `run_init(...)` unchanged.

    Step 4: Do NOT add a Typer alias (no `@app.command(name="init")` second registration). Hard cut per D-04. The brand-gate in plan 18-06 will enforce this via a `def init(` pattern check inside `cli.py`.

    Step 5: The function docstring `"""Bootstrap a wiki vault structure (creates raw/ and work/ siblings)."""` is already semantically aligned — leave it. The `[ok] Initialized wiki at:` print line at the success path may stay as-is (descriptive prose, not a slug) — Claude's discretion per CONTEXT.md.
  </action>
  <verify>
    <automated>[ "$(grep -cE '^\s*def init\b' agents/code-wiki-agent/src/code_wiki_agent/cli.py)" = "0" ] && [ "$(grep -cE '^\s*def bootstrap\(' agents/code-wiki-agent/src/code_wiki_agent/cli.py)" = "1" ] && uv run --package code-wiki-agent code-wiki-agent --help 2>&1 | grep -qE '^\s+bootstrap\b' && ! (uv run --package code-wiki-agent code-wiki-agent --help 2>&1 | grep -qE '^\s+init\b')</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '^\s*def init\b' agents/code-wiki-agent/src/code_wiki_agent/cli.py` returns 0.
    - `grep -cE '^\s*def bootstrap\(' agents/code-wiki-agent/src/code_wiki_agent/cli.py` returns 1.
    - `uv run --package code-wiki-agent code-wiki-agent --help` exits 0; stdout contains a line of the form `  bootstrap` (Typer command list); stdout does NOT contain a line of the form `  init` (where `init` is the entire token — distinguish from `ingest` which IS a sibling subcommand).
    - `uv run --package code-wiki-agent code-wiki-agent init --help` exits non-zero (Typer "no such command", typically `code=2`).
    - `uv run --package code-wiki-agent code-wiki-agent bootstrap --help` exits 0 and prints help text including the substring `Bootstrap a wiki vault`.
    - Top-of-file import `from .commands.init import run_init` (or equivalent) is unchanged.
  </acceptance_criteria>
  <done>
    Typer subcommand renamed; `--help` reflects new name; old name produces a Typer "no such command" error; no compat alias.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rename test file + update CLI test assertions</name>
  <files>agents/code-wiki-agent/tests/unit/test_commands_init.py, agents/code-wiki-agent/tests/unit/test_commands_bootstrap.py, agents/code-wiki-agent/tests/unit/test_cli_help.py</files>
  <read_first>
    - agents/code-wiki-agent/tests/unit/test_commands_init.py (full file — note: plan 18-02 already updated the MCP-surface assertions inside this file; this task picks up the CLI-surface assertions PLUS the file rename)
    - agents/code-wiki-agent/tests/unit/test_cli_help.py (full file — locate the assertion that enumerates Typer subcommands)
    - .planning/phases/18-plugin-command-rename/18-CONTEXT.md (Q1 resolution: rename file via git mv to match surface)
  </read_first>
  <behavior>
    - Test: File `tests/unit/test_commands_init.py` no longer exists.
    - Test: File `tests/unit/test_commands_bootstrap.py` exists with the renamed content, git records this as a rename (`R{score}`).
    - Test: Test names referencing the Typer `init` subcommand are renamed to reference `bootstrap` (e.g., `test_init_command_invokes_run_init` → `test_bootstrap_command_invokes_run_init`).
    - Test: `test_cli_help.py` asserts `bootstrap` appears in the Typer help output and `init` (as a top-level subcommand, distinct from `ingest`) does NOT appear.
    - Test: `uv run --package code-wiki-agent pytest agents/code-wiki-agent/tests/ -m "not integration"` exits 0.
  </behavior>
  <action>
    Step 1: Run `git mv agents/code-wiki-agent/tests/unit/test_commands_init.py agents/code-wiki-agent/tests/unit/test_commands_bootstrap.py`. Plain `mv` is forbidden — must record as a rename so git log preserves provenance.

    Step 2: Edit the renamed `test_commands_bootstrap.py`. Plan 18-02 already substituted the MCP-surface references (`wiki_init` → `wiki_bootstrap`, `WikiInitInput` → `WikiBootstrapInput`). This task addresses any remaining Typer-CLI-surface references inside the same file. Locate them via `grep -nE '\binit\b' agents/code-wiki-agent/tests/unit/test_commands_bootstrap.py` and apply identity renames where the token refers to the Typer subcommand or the test of the Typer command. For example: any `test_init_command_*` function name → `test_bootstrap_command_*`; any docstring saying "init command" → "bootstrap command"; any subcommand-invocation string in a CliRunner test (`runner.invoke(app, ["init", "--topic", ...])`) → `["bootstrap", "--topic", ...]`.

    DO NOT rename references that are about the internal Python module `commands.init` (e.g., `from code_wiki_agent.commands.init import run_init` — that import is out of scope per D-02). The disambiguation rule: the word `init` referring to the user-typed subcommand renames; the word `init` referring to the Python module path stays.

    Step 3: In `tests/unit/test_cli_help.py`, locate the help-text assertion(s). Update them so:
      - The assertion that `bootstrap` appears in `--help` output is added or strengthened.
      - The assertion that `init` does NOT appear as a standalone Typer subcommand is added.

    Concrete pattern: if the file currently asserts something like `assert "init" in result.stdout`, change it to `assert "bootstrap" in result.stdout` AND add `assert " init" not in result.stdout.split("Commands:")[1]` (or equivalent — guarding against `ingest` substring false positive by either word-boundary regex or scoping to the Commands section of the help output). The exact mechanism is at executor discretion; the acceptance criteria below pins the observable behavior.

    Step 4: Run the per-commit gate `uv run --package code-wiki-agent pytest agents/code-wiki-agent/tests/ -m "not integration" -q`. Must exit 0.

    Step 5: Commit Task 1 + Task 2 together as a single commit. Subject: `refactor(18): rename CLI subcommand init → bootstrap`. Body references CMD-02 (CLI portion), notes the test file rename via git mv, notes that `commands/init.py` Python module is intentionally unchanged per D-02.
  </action>
  <verify>
    <automated>test ! -e agents/code-wiki-agent/tests/unit/test_commands_init.py && test -e agents/code-wiki-agent/tests/unit/test_commands_bootstrap.py && git log -1 --name-status 2>/dev/null | grep -qE '^R[0-9]+\s+agents/code-wiki-agent/tests/unit/test_commands_init\.py\s+agents/code-wiki-agent/tests/unit/test_commands_bootstrap\.py$' && uv run --package code-wiki-agent pytest agents/code-wiki-agent/tests/unit/test_commands_bootstrap.py agents/code-wiki-agent/tests/unit/test_cli_help.py -q 2>&1 | tail -3 | grep -qE 'passed'</automated>
  </verify>
  <acceptance_criteria>
    - `test -e agents/code-wiki-agent/tests/unit/test_commands_init.py` exits non-zero (file gone).
    - `test -e agents/code-wiki-agent/tests/unit/test_commands_bootstrap.py` exits 0.
    - `git log -1 --name-status` for the staged commit shows a rename line `R{score}\tagents/code-wiki-agent/tests/unit/test_commands_init.py\tagents/code-wiki-agent/tests/unit/test_commands_bootstrap.py`.
    - `grep -cE '\btest_init_command\b|\btest_init_calls\b' agents/code-wiki-agent/tests/unit/test_commands_bootstrap.py` returns 0 (no stale CLI-subcommand test names).
    - `grep -cE '\btest_bootstrap_command\b|\btest_bootstrap_calls\b|\btest_bootstrap_input\b' agents/code-wiki-agent/tests/unit/test_commands_bootstrap.py` returns ≥ 2 (at least two renamed function names; exact count depends on how many CLI- and MCP-surface tests live in this file).
    - `agents/code-wiki-agent/tests/unit/test_cli_help.py` contains an assertion that `bootstrap` appears in `--help` output: `grep -cE 'bootstrap' agents/code-wiki-agent/tests/unit/test_cli_help.py` returns ≥ 1.
    - `agents/code-wiki-agent/tests/unit/test_cli_help.py` contains an assertion that the `init` Typer subcommand does NOT appear: `grep -cE 'not in.*init|init.*not in' agents/code-wiki-agent/tests/unit/test_cli_help.py` returns ≥ 1 (negative assertion present).
    - `uv run --package code-wiki-agent pytest agents/code-wiki-agent/tests/ -m "not integration"` exits 0.
    - Single commit subject contains `refactor(18)` and the literal `init → bootstrap` or `init -> bootstrap`.
    - Import line `from code_wiki_agent.commands.init import ...` (the Python module path) is still present somewhere in the test file or in cli.py (unchanged per D-02).
  </acceptance_criteria>
  <done>
    Test file renamed via git mv; CLI-surface assertions reference `bootstrap`; help-text assertions cover both positive (bootstrap appears) and negative (init does not) cases; per-commit pytest gate green; single commit lands.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User terminal → code-wiki-agent CLI | Subcommand slug `bootstrap` is the addressable Typer command |
| CLI subcommand → internal commands module | `run_init` imported from `code_wiki_agent.commands.init` (internal, out of scope) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-08 | Tampering | cli.py — Typer alias re-introducing `init` slug | mitigate | Acceptance criteria asserts zero `def init(` lines + `code-wiki-agent init --help` exits non-zero; brand-gate (plan 18-06) adds a static regex against `^\s*def init\(` in cli.py to prevent regression. |
| T-18-09 | Repudiation | Test file rename audit trail | mitigate | Require `git mv` (not delete+add); acceptance criteria asserts `git log` shows R rename entry. |
| T-18-10 | Tampering | Test assertions on `--help` could silently pass after rename if they don't actually invoke Typer | mitigate | Acceptance requires both a positive (`bootstrap` present) and a negative (`init` absent as standalone subcommand) assertion; both must be greppable in source. |
</threat_model>

<verification>
- `grep -c '^\s*def init\b' agents/code-wiki-agent/src/code_wiki_agent/cli.py` returns 0.
- `code-wiki-agent --help` lists `bootstrap`, not `init`.
- `code-wiki-agent init --help` exits non-zero.
- File `test_commands_init.py` removed via git mv; `test_commands_bootstrap.py` present.
- Per-commit gate `uv run --package code-wiki-agent pytest agents/code-wiki-agent/tests/ -m "not integration"` exits 0.
</verification>

<success_criteria>
- Typer subcommand renamed cleanly; old slug rejected by Typer.
- Test file renamed via git mv; test assertions cover both directions.
- Internal `commands/init.py` module unchanged per D-02.
- No backwards-compat alias per D-04.
- CMD-02 CLI portion closed (MCP portion was closed in plan 18-02).
</success_criteria>

<output>
Create `.planning/phases/18-plugin-command-rename/18-03-SUMMARY.md` when done. Capture: confirmation that `def init` is gone and `def bootstrap` appears once; `--help` output snippet showing `bootstrap` in the Commands list and `init` absent; git rename entry for the test file; pytest exit code; commit SHA.
</output>
