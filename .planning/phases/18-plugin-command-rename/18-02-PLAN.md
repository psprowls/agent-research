---
phase: 18-plugin-command-rename
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/code-wiki-agent/src/code_wiki_mcp/server.py
  - agents/code-wiki-agent/tests/unit/test_commands_init.py
  - agents/code-wiki-agent/tests/integration/test_mcp_e2e.py
autonomous: true
requirements: [CMD-02]
must_haves:
  truths:
    - "MCP tool is registered under the name 'wiki_bootstrap' (not 'wiki_init')"
    - "Pydantic input/output models are renamed WikiBootstrapInput / WikiBootstrapOutput"
    - "from code_wiki_mcp.server import wiki_bootstrap succeeds; importing wiki_init raises AttributeError"
    - "Unit + integration test assertions reference the new tool name and import the new symbols"
  artifacts:
    - path: "agents/code-wiki-agent/src/code_wiki_mcp/server.py"
      provides: "wiki_bootstrap MCP tool registration + WikiBootstrapInput/Output models"
      contains: "@mcp.tool(name=\"wiki_bootstrap\""
  key_links:
    - from: "agents/code-wiki-agent/src/code_wiki_mcp/server.py"
      to: "agents/code-wiki-agent/src/code_wiki_agent/commands/init.run_init"
      via: "Function call inside wiki_bootstrap"
      pattern: "run_init\\("
---

<objective>
Rename the MCP tool surface from `wiki_init` to `wiki_bootstrap` inside `agents/code-wiki-agent/src/code_wiki_mcp/server.py`. This is the second of the three user-facing surfaces (D-02). Includes renaming the registration name string, the async function definition, both Pydantic input/output models (`WikiInitInput` → `WikiBootstrapInput`, `WikiInitOutput` → `WikiBootstrapOutput`), and updating all consumer assertions in unit + integration tests.

Purpose: D-06 step 2 in the staged cutover. Closes CMD-02 for the MCP surface (the CLI surface is closed by plan 18-03). Per-commit gate green.

Output: One commit, MCP tool reachable as `wiki_bootstrap` only, no `wiki_init` symbols remain in the agent package source or tests.

The underlying `from code_wiki_agent.commands.init import InitResult, run_init` IMPORT stays — the implementation module (`commands/init.py`) is internal Python and is out of scope per D-02 (machine-facing, not user-typed). Only the MCP tool's exposed identity changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-plugin-command-rename/18-CONTEXT.md
@CLAUDE.md
@agents/code-wiki-agent/src/code_wiki_mcp/server.py
@agents/code-wiki-agent/tests/unit/test_commands_init.py

<interfaces>
From `agents/code-wiki-agent/src/code_wiki_mcp/server.py` (lines 187–234, current state):
- Comment header: `# --- wiki_init tool ---`
- Import: `from code_wiki_agent.commands.init import InitResult, run_init` (UNCHANGED — internal module)
- Class: `class WikiInitInput(BaseModel)` — fields topic, tool, force, vault_path
- Class: `class WikiInitOutput(BaseModel)` — fields status, wiki_path, repo_path, topic, tool, date, installed_files, page_templates_copied, layers, raw_path, work_path
- Decorator: `@mcp.tool(name="wiki_init", description="Bootstrap a wiki vault structure.")`
- Function: `async def wiki_init(input: WikiInitInput, ctx: Context) -> WikiInitOutput:`

From `agents/code-wiki-agent/tests/unit/test_commands_init.py` (relevant assertions):
- Line 132: `def test_wiki_init_input_rejects_missing_required_fields() -> None:`
- Line 143: comment `# MCP wiki_init calls run_init`
- Line 148: `async def test_wiki_init_calls_run_init() -> None:`
- Line 149: docstring `"""wiki_init MCP tool calls run_init with passed args."""`
- Line 150: `from code_wiki_mcp.server import WikiInitInput, wiki_init`
- Line 162: usage `result = await wiki_init(...)`

In `agents/code-wiki-agent/tests/integration/test_mcp_e2e.py`: any `wiki_init` references must be located by grep and updated.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rename MCP tool + Pydantic models in server.py</name>
  <files>agents/code-wiki-agent/src/code_wiki_mcp/server.py</files>
  <read_first>
    - agents/code-wiki-agent/src/code_wiki_mcp/server.py (full file — focus on lines 180–240 for the wiki_init block; scan the entire file for any other `wiki_init` references)
    - .planning/phases/18-plugin-command-rename/18-CONTEXT.md (D-01 verb choice, D-02 surface coverage, D-04 hard cut)
  </read_first>
  <behavior>
    - Test: `from code_wiki_mcp.server import wiki_bootstrap, WikiBootstrapInput, WikiBootstrapOutput` succeeds (no ImportError).
    - Test: `from code_wiki_mcp.server import wiki_init` raises ImportError / AttributeError (symbol gone).
    - Test: `wiki_bootstrap` is a registered MCP tool whose registration name is `wiki_bootstrap` (the existing `test_wiki_init_calls_run_init` style assertion in `test_commands_init.py`, renamed in Task 2, exercises this).
    - Test: Existing logic — `await wiki_bootstrap(WikiBootstrapInput(topic=..., tool=...), ctx)` calls `run_init` with the same kwargs as before (no behavioral change beyond identity rename).
  </behavior>
  <action>
    Step 1: Locate the `wiki_init` block in `server.py` (currently at lines ~187–234 per the interfaces extract). Apply the following exact substitutions within that block (and anywhere else they appear in the file):
      - Comment header `# --- wiki_init tool ---` → `# --- wiki_bootstrap tool ---`.
      - Class name `WikiInitInput` → `WikiBootstrapInput` (definition + every reference, including type hints).
      - Class name `WikiInitOutput` → `WikiBootstrapOutput` (definition + every reference, including return-type hint and the constructor call inside the function body around line 222).
      - Decorator `@mcp.tool(name="wiki_init", description="Bootstrap a wiki vault structure.")` → `@mcp.tool(name="wiki_bootstrap", description="Bootstrap a wiki vault structure.")`. Description text stays — it is descriptive prose, not the slug.
      - Function name `async def wiki_init(` → `async def wiki_bootstrap(`.

    Step 2: Do NOT rename the import line `from code_wiki_agent.commands.init import InitResult, run_init` — the internal command module is out of scope per D-02. The wrapping MCP surface renames; the wrapped function does not.

    Step 3: Do NOT add a backwards-compat alias (no `wiki_init = wiki_bootstrap`, no second `@mcp.tool` registration). Hard cut per D-04.

    Step 4: After substitutions, run `grep -nE '\bwiki_init\b|\bWikiInitInput\b|\bWikiInitOutput\b' agents/code-wiki-agent/src/code_wiki_mcp/server.py` — must return zero matches. Run `grep -nE '\bwiki_bootstrap\b|\bWikiBootstrapInput\b|\bWikiBootstrapOutput\b' agents/code-wiki-agent/src/code_wiki_mcp/server.py` — must return ≥ 6 matches (class def WikiBootstrapInput, class def WikiBootstrapOutput, decorator name=, function def, return-type annotation, constructor call inside body).
  </action>
  <verify>
    <automated>[ "$(grep -cE '\bwiki_init\b|\bWikiInitInput\b|\bWikiInitOutput\b' agents/code-wiki-agent/src/code_wiki_mcp/server.py)" = "0" ] && [ "$(grep -cE '\bwiki_bootstrap\b' agents/code-wiki-agent/src/code_wiki_mcp/server.py)" -ge 3 ] && uv run --package code-wiki-agent python -c "from code_wiki_mcp.server import wiki_bootstrap, WikiBootstrapInput, WikiBootstrapOutput; print('ok')" && ! uv run --package code-wiki-agent python -c "from code_wiki_mcp.server import wiki_init" 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '\bwiki_init\b|\bWikiInitInput\b|\bWikiInitOutput\b' agents/code-wiki-agent/src/code_wiki_mcp/server.py` returns 0.
    - `grep -cE '\bwiki_bootstrap\b' agents/code-wiki-agent/src/code_wiki_mcp/server.py` returns ≥ 3 (decorator, function def, comment header minimum).
    - `grep -cE '\bWikiBootstrapInput\b' agents/code-wiki-agent/src/code_wiki_mcp/server.py` returns ≥ 3 (class def, function signature, anywhere else referenced).
    - `grep -cE '\bWikiBootstrapOutput\b' agents/code-wiki-agent/src/code_wiki_mcp/server.py` returns ≥ 3 (class def, return-type hint, constructor call).
    - `grep -cE '@mcp\.tool\(name="wiki_bootstrap"' agents/code-wiki-agent/src/code_wiki_mcp/server.py` returns 1.
    - `uv run --package code-wiki-agent python -c "from code_wiki_mcp.server import wiki_bootstrap, WikiBootstrapInput, WikiBootstrapOutput"` exits 0.
    - `uv run --package code-wiki-agent python -c "from code_wiki_mcp.server import wiki_init"` exits non-zero (ImportError).
    - The `from code_wiki_agent.commands.init import InitResult, run_init` import line is still present unchanged: `grep -cE 'from code_wiki_agent\.commands\.init import' agents/code-wiki-agent/src/code_wiki_mcp/server.py` returns 1.
  </acceptance_criteria>
  <done>
    MCP tool registered as `wiki_bootstrap` only; old symbols fully removed; internal `commands/init` import preserved.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update unit + integration test assertions for renamed MCP surface</name>
  <files>agents/code-wiki-agent/tests/unit/test_commands_init.py, agents/code-wiki-agent/tests/integration/test_mcp_e2e.py</files>
  <read_first>
    - agents/code-wiki-agent/tests/unit/test_commands_init.py (full file — locate every `wiki_init` / `WikiInit*` reference)
    - agents/code-wiki-agent/tests/integration/test_mcp_e2e.py (full file — locate every `wiki_init` reference; this file will be exercised only when CODE_WIKI_RUN_INTEGRATION=1 so adjust assertions to compile-clean)
    - .planning/phases/18-plugin-command-rename/18-CONTEXT.md (open-question Q1 resolved: file rename is plan 18-03's responsibility — this plan only updates content of test_commands_init.py; the file is renamed test_commands_bootstrap.py in plan 18-03 as part of CLI rename)
  </read_first>
  <behavior>
    - Existing test names referencing `wiki_init` are renamed to reference `wiki_bootstrap` so the test surface mirrors the production surface (grep-ability).
    - Imports updated: `from code_wiki_mcp.server import WikiInitInput, wiki_init` → `from code_wiki_mcp.server import WikiBootstrapInput, wiki_bootstrap`.
    - Test bodies invoke `wiki_bootstrap(WikiBootstrapInput(...))` and assert the same behavior they previously asserted (e.g., that the wrapper calls `run_init` with the passed kwargs).
    - The whole suite `uv run --package code-wiki-agent pytest agents/code-wiki-agent/tests/ -m "not integration"` passes.
  </behavior>
  <action>
    Step 1: In `agents/code-wiki-agent/tests/unit/test_commands_init.py`, apply these exact substitutions:
      - Line 132 function name: `test_wiki_init_input_rejects_missing_required_fields` → `test_wiki_bootstrap_input_rejects_missing_required_fields`.
      - Line 143 comment: `# MCP wiki_init calls run_init` → `# MCP wiki_bootstrap calls run_init`.
      - Line 148 function name: `test_wiki_init_calls_run_init` → `test_wiki_bootstrap_calls_run_init`.
      - Line 149 docstring: `"""wiki_init MCP tool calls run_init with passed args."""` → `"""wiki_bootstrap MCP tool calls run_init with passed args."""`.
      - Line 150 import: `from code_wiki_mcp.server import WikiInitInput, wiki_init` → `from code_wiki_mcp.server import WikiBootstrapInput, wiki_bootstrap`.
      - Body usages (line 162 and any nearby invocations): `wiki_init(WikiInitInput(...))` → `wiki_bootstrap(WikiBootstrapInput(...))`.
      - Any other `wiki_init` / `WikiInitInput` / `WikiInitOutput` literal in the file.

    Step 2: In `agents/code-wiki-agent/tests/integration/test_mcp_e2e.py`, locate every `wiki_init` reference via `grep -nE '\bwiki_init\b|\bWikiInit(Input|Output)\b' agents/code-wiki-agent/tests/integration/test_mcp_e2e.py` and apply the same identity renames (`wiki_init` → `wiki_bootstrap`, `WikiInitInput` → `WikiBootstrapInput`, `WikiInitOutput` → `WikiBootstrapOutput`). If the test invokes the tool by string name via the MCP protocol (e.g., `tools/call` with `name="wiki_init"`), update that string literal to `"wiki_bootstrap"`.

    Step 3: Do NOT rename the file `test_commands_init.py` in this plan — its file rename is bundled with the CLI rename in plan 18-03 (the file currently mixes MCP-surface and Typer-CLI assertions; the rename to `test_commands_bootstrap.py` happens after both surfaces are renamed).

    Step 4: Run `uv run --package code-wiki-agent pytest agents/code-wiki-agent/tests/ -m "not integration" -q` and confirm exit 0.

    Step 5: Commit the server.py change from Task 1 + the test updates from this task as a single commit. Subject: `refactor(18): rename MCP tool wiki_init → wiki_bootstrap`. Body references CMD-02 (MCP surface portion).
  </action>
  <verify>
    <automated>[ "$(grep -cE '\bwiki_init\b|\bWikiInit(Input|Output)\b' agents/code-wiki-agent/tests/unit/test_commands_init.py)" = "0" ] && [ "$(grep -cE '\bwiki_init\b|\bWikiInit(Input|Output)\b' agents/code-wiki-agent/tests/integration/test_mcp_e2e.py)" = "0" ] && [ "$(grep -cE '\bwiki_bootstrap\b' agents/code-wiki-agent/tests/unit/test_commands_init.py)" -ge 4 ] && uv run --package code-wiki-agent pytest agents/code-wiki-agent/tests/ -m "not integration" -q 2>&1 | tail -3 | grep -qE '(passed|no tests ran)'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '\bwiki_init\b|\bWikiInit(Input|Output)\b' agents/code-wiki-agent/tests/unit/test_commands_init.py` returns 0.
    - `grep -cE '\bwiki_init\b|\bWikiInit(Input|Output)\b' agents/code-wiki-agent/tests/integration/test_mcp_e2e.py` returns 0.
    - `grep -cE '\bwiki_bootstrap\b' agents/code-wiki-agent/tests/unit/test_commands_init.py` returns ≥ 4 (two function names, one comment, one docstring/import minimum).
    - `uv run --package code-wiki-agent pytest agents/code-wiki-agent/tests/ -m "not integration"` exits 0; pytest summary shows the renamed tests `test_wiki_bootstrap_input_rejects_missing_required_fields` and `test_wiki_bootstrap_calls_run_init` ran and passed (verify with `pytest -v` if needed).
    - File `agents/code-wiki-agent/tests/unit/test_commands_init.py` still exists (rename deferred to plan 18-03).
    - Single commit lands with subject containing `refactor(18)` and the literal `wiki_init → wiki_bootstrap` or `wiki_init -> wiki_bootstrap`.
  </acceptance_criteria>
  <done>
    All MCP-surface tests reference the new tool name; per-commit pytest gate green; CMD-02 MCP portion closed (Typer portion in plan 18-03).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP host (DeepAgents CLI / Claude Code) → MCP server | Tool name `wiki_bootstrap` is the addressable identity over the MCP protocol |
| MCP server → internal Python commands module | `run_init` import from `code_wiki_agent.commands.init` (internal, out of scope) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-04 | Tampering | server.py — backwards-compat shim slipping in | mitigate | Acceptance criteria asserts ZERO occurrences of `wiki_init` / `WikiInitInput` / `WikiInitOutput` in server.py; D-04 explicitly forbids dual registration. |
| T-18-05 | Information Disclosure | MCP tool registration metadata | accept | Description string ("Bootstrap a wiki vault structure.") is unchanged and contains no sensitive info; the rename only changes the registration name. |
| T-18-06 | Denial of Service | Existing MCP clients calling `wiki_init` | accept | Hard cut per D-04 + Phase 20 precedent; user reinstalls the plugin and any downstream clients re-resolve tool list. Documented in CONTEXT.md decisions; no in-scope mitigation. |
| T-18-07 | Tampering | Test assertions could be loosened to match either name | mitigate | Acceptance criteria asserts ZERO `wiki_init` references in test files + minimum count of `wiki_bootstrap` references, so a half-rename can't pass. |
</threat_model>

<verification>
- `grep -rnE '\bwiki_init\b|\bWikiInit(Input|Output)\b' agents/code-wiki-agent/src agents/code-wiki-agent/tests` returns 0.
- `uv run --package code-wiki-agent python -c "from code_wiki_mcp.server import wiki_bootstrap"` exits 0.
- `uv run --package code-wiki-agent pytest agents/code-wiki-agent/tests/ -m "not integration"` exits 0.
- Single rename commit on git log.
</verification>

<success_criteria>
- MCP tool reachable only as `wiki_bootstrap`; no compat alias.
- Pydantic models renamed.
- Tests reference new surface; per-commit gate green.
- Internal `run_init` import preserved (out of scope per D-02).
- CMD-02 MCP portion closed.
</success_criteria>

<output>
Create `.planning/phases/18-plugin-command-rename/18-02-SUMMARY.md` when done. Capture: exact substitution counts in server.py and the two test files; confirmation that `from code_wiki_mcp.server import wiki_init` raises (proves no alias slipped in); per-commit pytest exit code; commit SHA.
</output>
