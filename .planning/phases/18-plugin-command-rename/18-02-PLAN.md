---
phase: 18-plugin-command-rename
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/code-wiki-agent/src/code_wiki_mcp/server.py
  - agents/code-wiki-agent/tests/unit/test_mcp_new_tools.py
  - agents/code-wiki-agent/tests/integration/test_mcp_e2e.py
autonomous: true
requirements: [CMD-02]
must_haves:
  truths:
    - "MCP tool is registered under the name 'wiki_bootstrap' (not 'wiki_init')"
    - "Pydantic input/output models are renamed WikiBootstrapInput / WikiBootstrapOutput"
    - "from code_wiki_mcp.server import wiki_bootstrap succeeds; importing wiki_init raises AttributeError"
    - "Unit + integration test assertions reference the new tool name and import the new symbols"
  artifacts:
    - path: "agents/code-wiki-agent/src/code_wiki_mcp/server.py"
      provides: "wiki_bootstrap MCP tool registration + WikiBootstrapInput/Output models"
      contains: "@mcp.tool(name=\"wiki_bootstrap\""
  key_links:
    - from: "agents/code-wiki-agent/src/code_wiki_mcp/server.py"
      to: "agents/code-wiki-agent/src/code_wiki_agent/commands/init.run_init"
      via: "Function call inside wiki_bootstrap"
      pattern: "run_init\\("
---

<objective>
Rename the MCP tool surface from `wiki_init` to `wiki_bootstrap` inside `agents/code-wiki-agent/src/code_wiki_mcp/server.py`. This is the second of the three user-facing surfaces (D-02). Includes renaming the registration name string, the async function definition, both Pydantic input/output models (`WikiInitInput` → `WikiBootstrapInput`, `WikiInitOutput` → `WikiBootstrapOutput`), and updating all consumer assertions in unit + integration tests.

Purpose: D-06 step 2 in the staged cutover. Closes CMD-02 for the MCP surface (the CLI surface is closed by plan 18-03). Per-commit gate green.

Output: One commit, MCP tool reachable as `wiki_bootstrap` only, no `wiki_init` symbols remain in `server.py`, `test_mcp_new_tools.py`, or `test_mcp_e2e.py`.

**File-ownership boundary with plan 18-03 (Wave 1 parallel-safety):** This plan does NOT touch `agents/code-wiki-agent/tests/unit/test_commands_init.py`. The two MCP-surface tests currently inside that file (`test_wiki_init_input_rejects_missing_required_fields` at line 132 and `test_wiki_init_calls_run_init` at line 148) are **copied** into `test_mcp_new_tools.py` (where they belong logically — alongside the existing `wiki_scan` / `wiki_ingest` MCP tests) under their renamed forms. Plan 18-03 then performs the `git mv test_commands_init.py → test_commands_bootstrap.py` and is free to delete the now-duplicate MCP-surface tests from the renamed file. Wave 1 coordination: this plan's commit fails the pytest gate in isolation (the obsolete MCP tests in test_commands_init.py will ImportError after the symbol rename). The gate is green only at the Wave 1 merged state, after plan 18-03's commit deletes those tests via git mv cleanup. Plans 18-02 and 18-03 must merge together — see Task 2 Step 6 for the coordination rule.

The underlying `from code_wiki_agent.commands.init import InitResult, run_init` IMPORT stays — the implementation module (`commands/init.py`) is internal Python and is out of scope per D-02 (machine-facing, not user-typed). Only the MCP tool's exposed identity changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-plugin-command-rename/18-CONTEXT.md
@CLAUDE.md
@agents/code-wiki-agent/src/code_wiki_mcp/server.py
@agents/code-wiki-agent/tests/unit/test_mcp_new_tools.py

<interfaces>
From `agents/code-wiki-agent/src/code_wiki_mcp/server.py` (lines 187–234, current state):
- Comment header: `# --- wiki_init tool ---`
- Import: `from code_wiki_agent.commands.init import InitResult, run_init` (UNCHANGED — internal module)
- Class: `class WikiInitInput(BaseModel)` — fields topic, tool, force, vault_path
- Class: `class WikiInitOutput(BaseModel)` — fields status, wiki_path, repo_path, topic, tool, date, installed_files, page_templates_copied, layers, raw_path, work_path
- Decorator: `@mcp.tool(name="wiki_init", description="Bootstrap a wiki vault structure.")`
- Function: `async def wiki_init(input: WikiInitInput, ctx: Context) -> WikiInitOutput:`

From `agents/code-wiki-agent/tests/unit/test_commands_init.py` (READ-ONLY in this plan — plan 18-03 owns the file; DO NOT edit content here beyond Task 2 Step 6 skip-decorator bridge):
- Line 132: `def test_wiki_init_input_rejects_missing_required_fields() -> None:` — copy to `test_mcp_new_tools.py` under the new name `test_wiki_bootstrap_input_rejects_missing_required_fields`.
- Line 148: `async def test_wiki_init_calls_run_init() -> None:` — copy to `test_mcp_new_tools.py` under the new name `test_wiki_bootstrap_calls_run_init`.
- Body imports `WikiInitInput, wiki_init` from `code_wiki_mcp.server` — translate to `WikiBootstrapInput, wiki_bootstrap` in the copies.
- After Task 1 renames the symbols in server.py, these in-place tests would fail; Task 2 Step 6 applies `@pytest.mark.skip` decorators to keep the per-commit pytest gate green. Plan 18-03 deletes them outright.

From `agents/code-wiki-agent/tests/unit/test_mcp_new_tools.py` (DESTINATION for the two copied MCP-surface tests, plus a new registration test):
- File already contains MCP-tool registration tests for `wiki_scan` (lines ~18+) and `wiki_ingest`. The header docstring reads `"""MCP tool registration tests for wiki_scan (Plan 05-04) and wiki_ingest (Plan 05-05)."""`. Update to also name `wiki_bootstrap (Phase 18-02)`.
- Existing template: `test_wiki_scan_tool_registered` asserts the imported symbol is callable and `__name__ == "wiki_scan"`. Mirror this for `wiki_bootstrap`.
- Add a new section divider (`# wiki_bootstrap tool registration` style) before the new tests for readability.

In `agents/code-wiki-agent/tests/integration/test_mcp_e2e.py`: any `wiki_init` references must be located by grep and updated.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rename MCP tool + Pydantic models in server.py</name>
  <files>agents/code-wiki-agent/src/code_wiki_mcp/server.py</files>
  <read_first>
    - agents/code-wiki-agent/src/code_wiki_mcp/server.py (full file — focus on lines 180–240 for the wiki_init block; scan the entire file for any other `wiki_init` references)
    - .planning/phases/18-plugin-command-rename/18-CONTEXT.md (D-01 verb choice, D-02 surface coverage, D-04 hard cut)
  </read_first>
  <behavior>
    - Test: `from code_wiki_mcp.server import wiki_bootstrap, WikiBootstrapInput, WikiBootstrapOutput` succeeds (no ImportError).
    - Test: `from code_wiki_mcp.server import wiki_init` raises ImportError / AttributeError (symbol gone).
    - Test: `wiki_bootstrap` is a registered MCP tool whose registration name is `wiki_bootstrap` (the existing `test_wiki_init_calls_run_init` style assertion in `test_commands_init.py`, renamed in Task 2, exercises this).
    - Test: Existing logic — `await wiki_bootstrap(WikiBootstrapInput(topic=..., tool=...), ctx)` calls `run_init` with the same kwargs as before (no behavioral change beyond identity rename).
  </behavior>
  <action>
    Step 1: Locate the `wiki_init` block in `server.py` (currently at lines ~187–234 per the interfaces extract). Apply the following exact substitutions within that block (and anywhere else they appear in the file):
      - Comment header `# --- wiki_init tool ---` → `# --- wiki_bootstrap tool ---`.
      - Class name `WikiInitInput` → `WikiBootstrapInput` (definition + every reference, including type hints).
      - Class name `WikiInitOutput` → `WikiBootstrapOutput` (definition + every reference, including return-type hint and the constructor call inside the function body around line 222).
      - Decorator `@mcp.tool(name="wiki_init", description="Bootstrap a wiki vault structure.")` → `@mcp.tool(name="wiki_bootstrap", description="Bootstrap a wiki vault structure.")`. Description text stays — it is descriptive prose, not the slug.
      - Function name `async def wiki_init(` → `async def wiki_bootstrap(`.

    Step 2: Do NOT rename the import line `from code_wiki_agent.commands.init import InitResult, run_init` — the internal command module is out of scope per D-02. The wrapping MCP surface renames; the wrapped function does not.

    Step 3: Do NOT add a backwards-compat alias (no `wiki_init = wiki_bootstrap`, no second `@mcp.tool` registration). Hard cut per D-04.

    Step 4: After substitutions, run `grep -nE '\bwiki_init\b|\bWikiInitInput\b|\bWikiInitOutput\b' agents/code-wiki-agent/src/code_wiki_mcp/server.py` — must return zero matches. Run `grep -nE '\bwiki_bootstrap\b|\bWikiBootstrapInput\b|\bWikiBootstrapOutput\b' agents/code-wiki-agent/src/code_wiki_mcp/server.py` — must return ≥ 6 matches (class def WikiBootstrapInput, class def WikiBootstrapOutput, decorator name=, function def, return-type annotation, constructor call inside body).
  </action>
  <verify>
    <automated>[ "$(grep -cE '\bwiki_init\b|\bWikiInitInput\b|\bWikiInitOutput\b' agents/code-wiki-agent/src/code_wiki_mcp/server.py)" = "0" ] && [ "$(grep -cE '\bwiki_bootstrap\b' agents/code-wiki-agent/src/code_wiki_mcp/server.py)" -ge 3 ] && uv run --package code-wiki-agent python -c "from code_wiki_mcp.server import wiki_bootstrap, WikiBootstrapInput, WikiBootstrapOutput; print('ok')" && ! uv run --package code-wiki-agent python -c "from code_wiki_mcp.server import wiki_init" 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '\bwiki_init\b|\bWikiInitInput\b|\bWikiInitOutput\b' agents/code-wiki-agent/src/code_wiki_mcp/server.py` returns 0.
    - `grep -cE '\bwiki_bootstrap\b' agents/code-wiki-agent/src/code_wiki_mcp/server.py` returns ≥ 3 (decorator, function def, comment header minimum).
    - `grep -cE '\bWikiBootstrapInput\b' agents/code-wiki-agent/src/code_wiki_mcp/server.py` returns ≥ 3 (class def, function signature, anywhere else referenced).
    - `grep -cE '\bWikiBootstrapOutput\b' agents/code-wiki-agent/src/code_wiki_mcp/server.py` returns ≥ 3 (class def, return-type hint, constructor call).
    - `grep -cE '@mcp\.tool\(name="wiki_bootstrap"' agents/code-wiki-agent/src/code_wiki_mcp/server.py` returns 1.
    - `uv run --package code-wiki-agent python -c "from code_wiki_mcp.server import wiki_bootstrap, WikiBootstrapInput, WikiBootstrapOutput"` exits 0.
    - `uv run --package code-wiki-agent python -c "from code_wiki_mcp.server import wiki_init"` exits non-zero (ImportError).
    - The `from code_wiki_agent.commands.init import InitResult, run_init` import line is still present unchanged: `grep -cE 'from code_wiki_agent\.commands\.init import' agents/code-wiki-agent/src/code_wiki_mcp/server.py` returns 1.
  </acceptance_criteria>
  <done>
    MCP tool registered as `wiki_bootstrap` only; old symbols fully removed; internal `commands/init` import preserved.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add wiki_bootstrap MCP tests to test_mcp_new_tools.py + update test_mcp_e2e.py</name>
  <files>agents/code-wiki-agent/tests/unit/test_mcp_new_tools.py, agents/code-wiki-agent/tests/integration/test_mcp_e2e.py</files>
  <read_first>
    - agents/code-wiki-agent/tests/unit/test_mcp_new_tools.py (full file — existing wiki_scan / wiki_ingest registration tests are the template)
    - agents/code-wiki-agent/tests/unit/test_commands_init.py (READ-ONLY; the two MCP-surface tests at lines 132 and 148 are the SOURCE for the copies into test_mcp_new_tools.py. This plan does NOT modify this file. Plan 18-03 owns it and will delete the now-duplicate MCP-surface tests during its git mv.)
    - agents/code-wiki-agent/tests/integration/test_mcp_e2e.py (full file — locate every wiki_init reference; integration tests gated by CODE_WIKI_RUN_INTEGRATION=1)
    - .planning/phases/18-plugin-command-rename/18-CONTEXT.md (D-04 hard cut; D-06 per-commit gate)
  </read_first>
  <behavior>
    - test_mcp_new_tools.py gains a new wiki_bootstrap section mirroring the wiki_scan section: a registration test plus the two MCP-surface tests copied from test_commands_init.py.
    - Imports inside those tests reference WikiBootstrapInput / WikiBootstrapOutput / wiki_bootstrap (not the old names).
    - test_mcp_new_tools.py header docstring is updated to include wiki_bootstrap alongside wiki_scan and wiki_ingest.
    - test_mcp_e2e.py wiki_init references are renamed in place.
    - test_commands_init.py is COMPLETELY untouched by this plan (Wave 1 file-ownership boundary with plan 18-03 — verified via git diff).
    - Per-commit gate (pytest -m "not integration") passes when this plan's commit is squashed with plan 18-03's commit at Wave 1 merge time (see Step 6 note).
  </behavior>
  <action>
    Step 1: Read the existing test_mcp_new_tools.py wiki_scan section (around lines 14-130). The pattern is: (a) section divider comment, (b) `test_wiki_scan_tool_registered` asserting callable + `__name__ == "wiki_scan"`, (c) input-validation tests, (d) behavioral tests using `@pytest.mark.asyncio` and `async def`.

    Step 2: Append a new wiki_bootstrap section to test_mcp_new_tools.py (end of file). Three tests minimum:
      (a) `test_wiki_bootstrap_tool_registered` — imports `wiki_bootstrap` from `code_wiki_mcp.server`; asserts callable + `wiki_bootstrap.__name__ == "wiki_bootstrap"`.
      (b) `test_wiki_bootstrap_input_rejects_missing_required_fields` — COPY the body from test_commands_init.py lines 132-141, replacing every `WikiInitInput` with `WikiBootstrapInput`.
      (c) `test_wiki_bootstrap_calls_run_init` — COPY the body from test_commands_init.py starting at line 148, replacing `wiki_init` → `wiki_bootstrap` and `WikiInitInput` → `WikiBootstrapInput`. Use `@pytest.mark.asyncio` + `async def` matching `test_wiki_scan_calls_run_scan_and_returns_output`. The patched target stays `code_wiki_agent.commands.init.run_init` (internal module, out of scope per D-02).

    Step 3: Update test_mcp_new_tools.py header docstring (line 3) from `"""MCP tool registration tests for wiki_scan (Plan 05-04) and wiki_ingest (Plan 05-05)."""` to `"""MCP tool registration tests for wiki_scan (Plan 05-04), wiki_ingest (Plan 05-05), and wiki_bootstrap (Phase 18-02)."""`. Update the Requirements line at top from `MCP-01, MCP-03.` to `MCP-01, MCP-03, CMD-02.`.

    Step 4: In agents/code-wiki-agent/tests/integration/test_mcp_e2e.py, locate every wiki_init reference via `grep -nE '\bwiki_init\b|\bWikiInit(Input|Output)\b' agents/code-wiki-agent/tests/integration/test_mcp_e2e.py` and apply identity renames (wiki_init → wiki_bootstrap, WikiInitInput → WikiBootstrapInput, WikiInitOutput → WikiBootstrapOutput). Update any `name="wiki_init"` string literal to `"wiki_bootstrap"`.

    Step 5: This plan does NOT touch `agents/code-wiki-agent/tests/unit/test_commands_init.py` at all — no skip decorators, no content edits, nothing. Plan 18-03 owns it (git mv + content cleanup).

    Step 6: **Wave 1 coordination note (file-ownership boundary):** Plans 18-01 + 18-02 + 18-03 all run in Wave 1 with disjoint `files_modified`. The per-commit gate `uv run --package code-wiki-agent pytest agents/code-wiki-agent/tests/ -m "not integration"` for THIS plan's commit (18-02) will FAIL in isolation because after Task 1 renames server.py symbols, the two obsolete MCP-surface tests still present in test_commands_init.py will raise ImportError. The gate passes only after plan 18-03's commit (which deletes those tests during git mv) lands. Executor behavior: run all three Wave 1 plans in parallel branches, merge them as a Wave 1 squash or rebase-chain such that the per-commit gate runs against the union of (18-01 + 18-02 + 18-03) commits. If the orchestrator runs plans sequentially within Wave 1, run order MUST be `18-02 first → 18-03 second` so the failing-then-fixing transition is atomic with respect to the gate. Plan 18-03 `depends_on` does NOT formally list 18-02 because they touch disjoint files; the dependency is at the test-suite-greenness level, not the file-modification level. Document this in the commit body for 18-02.

    Step 7: Commit Task 1 (server.py) + Task 2 (test_mcp_new_tools.py additions + test_mcp_e2e.py rename) as a single commit. Subject: `refactor(18): rename MCP tool wiki_init → wiki_bootstrap`. Body references CMD-02 (MCP portion), names test_mcp_new_tools.py as the new test home, and notes the Wave 1 coordination dependency on plan 18-03 (test_commands_init.py cleanup).
  </action>
  <verify>
    <automated>[ "$(grep -cE '\bwiki_init\b|\bWikiInit(Input|Output)\b' agents/code-wiki-agent/tests/unit/test_mcp_new_tools.py)" = "0" ] && [ "$(grep -cE '\bwiki_init\b|\bWikiInit(Input|Output)\b' agents/code-wiki-agent/tests/integration/test_mcp_e2e.py)" = "0" ] && [ "$(grep -cE '\bwiki_bootstrap\b' agents/code-wiki-agent/tests/unit/test_mcp_new_tools.py)" -ge 3 ] && [ -z "$(git diff HEAD~1 HEAD -- agents/code-wiki-agent/tests/unit/test_commands_init.py)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '\bwiki_init\b|\bWikiInit(Input|Output)\b' agents/code-wiki-agent/tests/unit/test_mcp_new_tools.py` returns 0.
    - `grep -cE '\bwiki_init\b|\bWikiInit(Input|Output)\b' agents/code-wiki-agent/tests/integration/test_mcp_e2e.py` returns 0.
    - `grep -cE '\bwiki_bootstrap\b' agents/code-wiki-agent/tests/unit/test_mcp_new_tools.py` returns ≥ 3.
    - `grep -cE 'def test_wiki_bootstrap_tool_registered\b' agents/code-wiki-agent/tests/unit/test_mcp_new_tools.py` returns 1.
    - `grep -cE 'def test_wiki_bootstrap_input_rejects_missing_required_fields\b' agents/code-wiki-agent/tests/unit/test_mcp_new_tools.py` returns 1.
    - `grep -cE 'def test_wiki_bootstrap_calls_run_init\b' agents/code-wiki-agent/tests/unit/test_mcp_new_tools.py` returns 1.
    - **File-ownership boundary with plan 18-03 (zero-touch):** `git diff HEAD~1 HEAD -- agents/code-wiki-agent/tests/unit/test_commands_init.py` returns EMPTY. The file is byte-identical pre- and post-commit. (This is the BLOCKER 1 closure assertion — no overlap on this file.)
    - Single commit subject contains `refactor(18)` and the literal `wiki_init → wiki_bootstrap` or `wiki_init -> wiki_bootstrap`.
    - Wave-1-merged-state pytest gate: when commits from plans 18-01 + 18-02 + 18-03 are merged together, `uv run --package code-wiki-agent pytest agents/code-wiki-agent/tests/ -m "not integration"` exits 0. (Verified after Wave 1 merges — owned by orchestrator wave-completion check, not by 18-02 alone.)
  </acceptance_criteria>
  <done>
    wiki_bootstrap MCP tests live in test_mcp_new_tools.py; test_mcp_e2e.py integration assertions updated; test_commands_init.py byte-identical (Wave 1 file-ownership boundary with plan 18-03 enforced); Wave-1-merged pytest gate green.
  </done>
</task>


</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP host (DeepAgents CLI / Claude Code) → MCP server | Tool name `wiki_bootstrap` is the addressable identity over the MCP protocol |
| MCP server → internal Python commands module | `run_init` import from `code_wiki_agent.commands.init` (internal, out of scope) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-04 | Tampering | server.py — backwards-compat shim slipping in | mitigate | Acceptance criteria asserts ZERO occurrences of `wiki_init` / `WikiInitInput` / `WikiInitOutput` in server.py; D-04 explicitly forbids dual registration. |
| T-18-05 | Information Disclosure | MCP tool registration metadata | accept | Description string ("Bootstrap a wiki vault structure.") is unchanged and contains no sensitive info; the rename only changes the registration name. |
| T-18-06 | Denial of Service | Existing MCP clients calling `wiki_init` | accept | Hard cut per D-04 + Phase 20 precedent; user reinstalls the plugin and any downstream clients re-resolve tool list. Documented in CONTEXT.md decisions; no in-scope mitigation. |
| T-18-07 | Tampering | Test assertions could be loosened to match either name | mitigate | Acceptance criteria asserts ZERO `wiki_init` references in test files + minimum count of `wiki_bootstrap` references, so a half-rename can't pass. |
</threat_model>

<verification>
- `grep -rnE '\bwiki_init\b|\bWikiInit(Input|Output)\b' agents/code-wiki-agent/src agents/code-wiki-agent/tests/unit/test_mcp_new_tools.py agents/code-wiki-agent/tests/integration/test_mcp_e2e.py` returns 0. (NOTE: test_commands_init.py is excluded — that file is plan 18-03's and is UNTOUCHED by this plan. Its two MCP-surface tests remain present until plan 18-03's git mv cleanup deletes them. Wave 1 must merge 18-02 + 18-03 together for the pytest gate to be green.)
- `uv run --package code-wiki-agent python -c "from code_wiki_mcp.server import wiki_bootstrap"` exits 0.
- `uv run --package code-wiki-agent pytest agents/code-wiki-agent/tests/ -m "not integration"` exits 0.
- Single rename commit on git log.
</verification>

<success_criteria>
- MCP tool reachable only as `wiki_bootstrap`; no compat alias.
- Pydantic models renamed.
- Tests reference new surface; per-commit gate green.
- Internal `run_init` import preserved (out of scope per D-02).
- CMD-02 MCP portion closed.
</success_criteria>

<output>
Create `.planning/phases/18-plugin-command-rename/18-02-SUMMARY.md` when done. Capture: exact substitution counts in server.py and test_mcp_e2e.py; the three new wiki_bootstrap test names added to test_mcp_new_tools.py; confirmation that test_commands_init.py was UNTOUCHED (git diff HEAD~1 HEAD on that file returns empty); confirmation that `from code_wiki_mcp.server import wiki_init` raises (no alias slipped in); per-commit pytest exit code; commit SHA.
</output>
