# 61-03: tests + fixtures + mono-repo-live verification

wave: 3
depends_on: [61-01, 61-02]
files_modified:
  - packages/source-parser/fixtures/typescript/
  - packages/source-parser/tests/
  - packages/graph-io/tests/
autonomous: false

requirements:
- "REQ-06: source-parser has fixture-driven TS tests covering bare interface, exported interface, type alias, enum (exported + non-exported variants), with paired *.expected.json (parser output) and *.graph.expected.json (graph projection output), parametrized"
- "REQ-07: graph-io has a resolve test asserting a `type` placeholder export edge resolves to a real `type` node via the cross-kind fallback"
- "REQ-08: rebuilding the mono-repo-live code graph shows APIGatewayProxyEvent as kind=type with ts_kind=interface (NOT function), and both package test suites are green"

## Task 1: Confirm the existing fixture/test conventions

<read_first>
- packages/source-parser/tests/_fixture_loader.py (the loader the parametrized tests use)
- packages/source-parser/tests/test_parser_typescript.py (TS parser fixture parametrization — *.expected.json)
- packages/source-parser/tests/test_projection_graph.py (graph-projection fixture parametrization — *.graph.expected.json)
- packages/source-parser/fixtures/typescript/ (existing fixtures, e.g. `interface_call.ts` + `interface_call.expected.json` + `interface_call.graph.expected.json`, and `class_with_methods.ts` — copy this exact paired-file shape)
</read_first>

<action>
The harness already exists and is fixture-driven (it is NOT a stub). Confirm the
convention from the read_first files: each `fixtures/typescript/<name>.ts` has a paired
`<name>.expected.json` (parser SourceNode/Reference output) and OPTIONALLY a
`<name>.graph.expected.json` (graph-projection output), discovered and parametrized by
`_fixture_loader.py` / `test_parser_typescript.py` / `test_projection_graph.py`. New
fixtures slot into this existing harness — author the `.ts` plus its paired expected
JSON file(s) in the same shape as `interface_call.*`. No new test scaffolding is needed;
the parametrized collectors pick up new fixtures automatically. To author exact expected
JSON, generate it by running the parser/projection on the fixture and hand-verifying the
`kind`/`ts_kind`/edge `dst` values before saving (do NOT blindly snapshot).
</action>

<acceptance_criteria>
- The existing parametrized collectors discover every new `fixtures/typescript/*.ts` and run parser (+ projection where a `.graph.expected.json` is present) against the paired expected JSON.
- `cd packages/source-parser && uv run pytest -q` collects the new parametrized cases (visible in `-v` node ids).
</acceptance_criteria>

## Task 2: Add TypeScript `type` fixtures

<read_first>
- packages/source-parser/src/source_parser/parsers/typescript.py
- packages/source-parser/src/source_parser/parsers/_generic.py
</read_first>

<action>
Add TS fixtures under `fixtures/typescript/` covering:
1. a bare `interface Foo { ... }` (non-exported)
2. an `export interface Bar { ... }`
3. a `type Alias = ...` (and an `export type ...` variant)
4. an `enum E { ... }` (and an `export enum ...` variant)
For each, author the paired `*.expected.json` (expecting `kind: "type"` with the correct
`ts_kind`) and `*.graph.expected.json` (expecting projected `type` nodes; for exported
variants, an `exports` edge whose dst kind is `type`; for a bare re-export fixture, the
dst kind defaults to `function`). Keep fixtures small and focused.
</action>

<acceptance_criteria>
- Bare interface fixture expects `kind == "type"`, `ts_kind == "interface"`, no `exports` edge.
- Exported interface fixture expects a `type` node AND an `exports` edge with dst kind `type`.
- type-alias fixture → `ts_kind == "type_alias"`; enum fixture → `ts_kind == "enum"`.
- `cd packages/source-parser && uv run pytest -q` is green with the new fixtures.
</acceptance_criteria>

## Task 3: graph-io cross-kind resolve test for `type` (+ `_VALID_KINDS`)

<read_first>
- packages/graph-io/tests/test_resolve.py (existing cross-kind resolve test conventions — fixtures, DB seeding, the sweep entry point used; mirror these)
- packages/graph-io/src/graph_io/resolve.py (the cross-kind fallback in the sweep; `_CROSS_KIND_RESOLVABLE`)
- packages/graph-io/tests/test_queries.py (the existing `_VALID_KINDS` / find-kind test pattern, if present, to mirror for the `type` assertion)
</read_first>

<action>
Add a graph-io resolve test (in `tests/test_resolve.py`, mirroring the existing cross-kind
resolution test) that: seeds a graph DB with one real `type` node named (e.g.)
`APIGatewayProxyEvent` and a placeholder code-symbol export-edge destination for that name
(NULL path, the placeholder shape the projection emits for unresolved exports), then runs
the same sweep entry point the existing tests call and asserts the placeholder resolves to
the real `type` node via the cross-kind fallback. Add a second case: a placeholder
defaulted to `function` (bare re-export) for a name that exists graph-wide ONLY as a
`type` also resolves (single-candidate cross-kind match). ALSO add a small assertion
(here or in `tests/test_queries.py`) that `"type" in graph_io.queries._VALID_KINDS` and
that `find(kind="type")` does not raise.
</action>

<acceptance_criteria>
- After the sweep, a `type` placeholder export edge has its dst repointed to the real `type` node (`resolution` reflects the match).
- A bare-re-export placeholder defaulted to `function` also resolves to the lone real `type` node of that name.
- `"type" in graph_io.queries._VALID_KINDS` and `find(kind="type")` raises no ValueError.
- `cd packages/graph-io && uv run pytest -q` is green.
</acceptance_criteria>

## Task 4: Verify against mono-repo-live + full suites

<read_first>
- packages/graph-io/src/graph_io/schema.py (DERIVER_VERSION — a mismatch with the stored stamp auto-forces full=True)
- packages/graph-io/src/graph_io/update.py (the `run(repo_root, *, workspace=..., full=...)` entry point)
- the project CLAUDE.md graph-rebuild notes + the memory note `graph-build-resolves-repo-from-workspace` (the repo≠workspace gotcha below)
</read_first>

<action>
1. Run both package suites: `cd packages/source-parser && uv run pytest -q` and
   `cd packages/graph-io && uv run pytest -q` — both must be green.
2. Rebuild the code graph for the workspace `~/Personal/graph-wiki/mono-repo-live`
   against its SOURCE repo (`~/Personal/mono-repo`). A FULL rebuild is required because
   the derivation logic changed (DERIVER_VERSION 4 → 5); incremental `cg update` will NOT
   re-derive. Recommended invocation (per the `graph-build-resolves-repo-from-workspace`
   memory note — `graph build` mis-resolves repo from the commit-less workspace and dies
   with `fatal: ambiguous argument 'HEAD'` on this layout): call the library directly with
   explicit paths to bypass `resolve()`:
   `uv run --package graph-io python -c "from pathlib import Path; from graph_io import update; update.run(Path.home()/'Personal/mono-repo', workspace=Path.home()/'Personal/graph-wiki/mono-repo-live', full=True)"`.
   (The DERIVER_VERSION 4→5 mismatch would also auto-force full=True, but pass `full=True`
   explicitly.) Confirm the Bedrock daily-token quota is irrelevant here — this is a
   pure-parser graph rebuild, no LLM calls.
3. Query the rebuilt graph for `APIGatewayProxyEvent` and confirm it is now
   `kind=type` with `ts_kind=interface` (NOT `function`) — e.g.
   `uv run --package graph-io python -c "import sqlite3; from pathlib import Path; db=sqlite3.connect(Path.home()/'Personal/graph-wiki/mono-repo-live/.graph-wiki/code.db'); print(db.execute(\"SELECT kind, attrs_json FROM nodes WHERE name='APIGatewayProxyEvent'\").fetchall())"`
   (confirm the actual code.db path from `update.run` / workspace_io before running).
</action>

<acceptance_criteria>
- `cd packages/source-parser && uv run pytest -q` — green.
- `cd packages/graph-io && uv run pytest -q` — green.
- Full rebuild of the `mono-repo-live` graph completes without error (direct
  `update.run(..., full=True)` library call, NOT `graph build`, to avoid the repo≠workspace
  `ambiguous argument HEAD` failure).
- Querying `APIGatewayProxyEvent` in the rebuilt graph returns `kind=type` and
  `ts_kind=interface` in attrs (explicitly NOT `kind=function`).
</acceptance_criteria>

## Verification

- [ ] Both package pytest suites green.
- [ ] mono-repo-live full rebuild succeeds.
- [ ] `APIGatewayProxyEvent` resolves to `kind=type` / `ts_kind=interface` in the rebuilt graph.

## Must-Haves

- [ ] source-parser TS fixtures (bare/exported interface, type alias, enum) with paired expected JSON, parametrized and green.
- [ ] graph-io resolve test proving a `type` placeholder export edge resolves via cross-kind fallback.
- [ ] mono-repo-live verification: APIGatewayProxyEvent is `kind=type` / `ts_kind=interface`, not `function`.
- [ ] Full (not incremental) rebuild used, because DERIVER_VERSION changed.
