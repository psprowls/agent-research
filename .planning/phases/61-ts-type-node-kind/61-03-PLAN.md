# 61-03: tests + fixtures + mono-repo-live verification

wave: 3
depends_on: [61-01, 61-02]
files_modified:
  - packages/source-parser/fixtures/typescript/
  - packages/source-parser/tests/
  - packages/graph-io/tests/
autonomous: false

requirements:
- "REQ-06: source-parser has fixture-driven TS tests covering bare interface, exported interface, type alias, enum (exported + non-exported variants), with paired *.expected.json (parser output) and *.graph.expected.json (graph projection output), parametrized"
- "REQ-07: graph-io has a resolve test asserting a `type` placeholder export edge resolves to a real `type` node via the cross-kind fallback"
- "REQ-08: rebuilding the mono-repo-live code graph shows APIGatewayProxyEvent as kind=type with ts_kind=interface (NOT function), and both package test suites are green"

## Task 1: Match existing fixture/test conventions

<read_first>
- packages/source-parser/src/source_parser/parsers/_generic.py
- packages/source-parser/src/source_parser/projections/graph.py
- packages/source-parser/   (inspect for any existing fixtures/<lang>/ and tests/ layout; if none exist yet, follow the documented convention: fixtures/<lang>/ with paired *.expected.json + *.graph.expected.json, parametrized in tests/)
</read_first>

<action>
Inspect the source-parser package for the existing fixture/test layout. The documented
convention is: fixtures live under `fixtures/<lang>/` with paired `*.expected.json`
(parser SourceNode/Reference output) and `*.graph.expected.json` (graph projection
output), parametrized in the test module. If no fixtures/tests exist yet (the package is
an early stub), CREATE the minimal harness that loads each fixture source file, runs the
parser + projection, and snapshot-compares against the paired expected JSON. Match the
repo's pytest invocation convention (`cd packages/<pkg> && uv run pytest`).
</action>

<acceptance_criteria>
- The fixture loader/parametrization discovers every `fixtures/typescript/*.ts` and runs both parser and graph projection against the paired expected JSON.
- `cd packages/source-parser && uv run pytest -q` collects the new parametrized cases.
</acceptance_criteria>

## Task 2: Add TypeScript `type` fixtures

<read_first>
- packages/source-parser/src/source_parser/parsers/typescript.py
- packages/source-parser/src/source_parser/parsers/_generic.py
</read_first>

<action>
Add TS fixtures under `fixtures/typescript/` covering:
1. a bare `interface Foo { ... }` (non-exported)
2. an `export interface Bar { ... }`
3. a `type Alias = ...` (and an `export type ...` variant)
4. an `enum E { ... }` (and an `export enum ...` variant)
For each, author the paired `*.expected.json` (expecting `kind: "type"` with the correct
`ts_kind`) and `*.graph.expected.json` (expecting projected `type` nodes; for exported
variants, an `exports` edge whose dst kind is `type`; for a bare re-export fixture, the
dst kind defaults to `function`). Keep fixtures small and focused.
</action>

<acceptance_criteria>
- Bare interface fixture expects `kind == "type"`, `ts_kind == "interface"`, no `exports` edge.
- Exported interface fixture expects a `type` node AND an `exports` edge with dst kind `type`.
- type-alias fixture → `ts_kind == "type_alias"`; enum fixture → `ts_kind == "enum"`.
- `cd packages/source-parser && uv run pytest -q` is green with the new fixtures.
</acceptance_criteria>

## Task 3: graph-io cross-kind resolve test for `type`

<read_first>
- packages/graph-io/src/graph_io/resolve.py
- packages/graph-io/src/graph_io/schema.py
</read_first>

<action>
Add a graph-io test that: seeds an in-memory graph DB with one real `type` node named
(e.g.) `APIGatewayProxyEvent` and an `exports` edge whose dst is a placeholder
`(type, "APIGatewayProxyEvent")` with NULL dst_id (and a second case: a placeholder
defaulted to `function` for a bare re-export of a name that exists only as a `type`).
Run `resolve_placeholder_edges` and assert the edge's dst resolves to the real `type`
node via the cross-kind fallback. Follow the repo's graph-io test layout/convention
(create `tests/` if the package is still a stub).
</action>

<acceptance_criteria>
- After `resolve_placeholder_edges`, a `type` placeholder export edge has its dst_id set to the real `type` node.
- A bare-re-export placeholder defaulted to `function` also resolves to the lone real `type` node of that name.
- `cd packages/graph-io && uv run pytest -q` is green.
</acceptance_criteria>

## Task 4: Verify against mono-repo-live + full suites

<read_first>
- packages/graph-io/src/graph_io/schema.py
- (the graph-rebuild CLI entry point — locate it before running: candidates are `cg update --full`, `graph build`, or `graph-wiki-agent graph ...`; confirm the exact invocation against the installed CLI and the project CLAUDE.md before executing)
</read_first>

<action>
1. Run both package suites: `cd packages/source-parser && uv run pytest -q` and
   `cd packages/graph-io && uv run pytest -q` — both must be green.
2. Rebuild the code graph for the workspace `~/Personal/graph-wiki/mono-repo-live`. A
   FULL rebuild is required because the derivation logic changed (DERIVER_VERSION 4 → 5);
   an incremental update will NOT re-derive. Use the full-rebuild command — primary:
   `cg update --full` (the documented full rebuild; per project memory, `cg update` is
   incremental and classification/derivation-logic changes need a full rebuild). If `cg`
   is unavailable, use the equivalent `graph build` / `graph-wiki-agent graph` full
   rebuild. NOTE (project memory): `graph build` resolves the repo by walking up from the
   workspace dir for `.git`, so run it with the workspace pointed at mono-repo-live.
3. Query the rebuilt graph for `APIGatewayProxyEvent` and confirm it is now
   `kind=type` with `ts_kind=interface` (NOT `function`).
</action>

<acceptance_criteria>
- `cd packages/source-parser && uv run pytest -q` — green.
- `cd packages/graph-io && uv run pytest -q` — green.
- Full rebuild of the `mono-repo-live` graph completes without error.
- Querying `APIGatewayProxyEvent` in the rebuilt graph returns `kind=type`, `ts_kind=interface` (explicitly NOT `kind=function`).
</acceptance_criteria>

## Verification

- [ ] Both package pytest suites green.
- [ ] mono-repo-live full rebuild succeeds.
- [ ] `APIGatewayProxyEvent` resolves to `kind=type` / `ts_kind=interface` in the rebuilt graph.

## Must-Haves

- [ ] source-parser TS fixtures (bare/exported interface, type alias, enum) with paired expected JSON, parametrized and green.
- [ ] graph-io resolve test proving a `type` placeholder export edge resolves via cross-kind fallback.
- [ ] mono-repo-live verification: APIGatewayProxyEvent is `kind=type` / `ts_kind=interface`, not `function`.
- [ ] Full (not incremental) rebuild used, because DERIVER_VERSION changed.
