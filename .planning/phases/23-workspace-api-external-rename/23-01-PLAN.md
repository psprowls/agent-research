---
phase: 23-workspace-api-external-rename
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/graph-wiki-agent/src/graph_wiki_mcp/server.py
  - agents/graph-wiki-agent/src/graph_wiki_agent/cli.py
  - packages/vault-io/src/vault_io/scan_monorepo.py
  - agents/graph-wiki-agent/src/graph_wiki_agent/commands/scan.py
  - agents/graph-wiki-agent/tests/integration/test_mcp_e2e.py
  - plugins/graph-wiki/agents/scanner.md
  - plugins/graph-wiki/skills/graph-wiki/references/scan-workflow.md
  - packages/prompt-sources/references/scan-workflow.md
  - scripts/check-brand.sh
  - .brand-grep-allow
autonomous: true
requirements:
  - WSMCP-01
  - WSMCP-02
  - WSMCP-03
  - WSMCP-04
  - WSMCP-05
  - WSMCP-06
  - WSMCP-07
tags:
  - refactor
  - rename
  - mcp-schema
  - cli-flag
  - brand-gate
  - workspace_path

must_haves:
  truths:
    - "graph-wiki-agent CLI exposes --workspace (not --vault) on all 7 commands; bootstrap also exposes --repo"
    - "MCP tool call with field workspace_path succeeds; call with vault_path fails Pydantic schema validation (extra='forbid')"
    - "Scan JSON output emits wiki_relative_path per package entry; vault_path no longer appears as a JSON key"
    - "Plugin docs and packages/prompt-sources/references mirrors are rename-mirrored (vault_path / --vault references removed in lockstep on both halves)"
    - "DA-CLI integration test test_mcp_e2e.py uses the new field/flag names mechanically and is runnable under GRAPH_WIKI_RUN_INTEGRATION=1"
    - "scripts/check-brand.sh exits non-zero when vault_path Pydantic Field, --vault Typer flag, or vault_path JSON key is reintroduced in agents/ packages/ plugins/"
  artifacts:
    - path: "agents/graph-wiki-agent/src/graph_wiki_mcp/server.py"
      provides: "6 MCP input classes with workspace_path field + extra='forbid' (no vault_path)"
      contains: "workspace_path: str"
    - path: "agents/graph-wiki-agent/src/graph_wiki_agent/cli.py"
      provides: "7 Typer commands with --workspace flag + bootstrap --repo"
      contains: "\"--workspace\""
    - path: "packages/vault-io/src/vault_io/scan_monorepo.py"
      provides: "_wiki_relative_path_for helper + 3 wiki_relative_path emissions"
      contains: "_wiki_relative_path_for"
    - path: "agents/graph-wiki-agent/tests/integration/test_mcp_e2e.py"
      provides: "Integration test exercising new field + flag names"
      contains: "\"workspace_path\""
    - path: "scripts/check-brand.sh"
      provides: "CHECK 4 block banning the 3 WSMCP-07 patterns"
      contains: "BRAND-WSAPI"
  key_links:
    - from: "MCP Pydantic field workspace_path"
      to: "run_* command kwargs workspace_path="
      via: "vault = Path(input.workspace_path) if input.workspace_path else None"
      pattern: "Path\\(input\\.workspace_path\\)"
    - from: "Typer flag --workspace"
      to: "run_* command kwargs workspace_path="
      via: "workspace_path = Path(workspace) if workspace else None"
      pattern: "workspace_path = Path\\(workspace\\)"
    - from: "bootstrap CLI --repo flag"
      to: "run_init(repo_path=...)"
      via: "repo_path = Path(repo).resolve() if repo else None"
      pattern: "repo_path=repo_path"
    - from: "scan_monorepo._wiki_relative_path_for"
      to: "commands/scan.py pkg.get(\"wiki_relative_path\", ...)"
      via: "scan JSON dict key"
      pattern: "wiki_relative_path"
    - from: "scripts/check-brand.sh CHECK 4"
      to: ".brand-grep-allow"
      via: "grep -vF -f allowlist filter"
      pattern: "BRAND-WSAPI"
---

<objective>
Hard-rename every externally visible workspace API surface from `vault_path` / `--vault` to
`workspace_path` / `--workspace`, rename the scan JSON output field to `wiki_relative_path`,
add the `--repo` flag to `bootstrap` only, update the DA-CLI integration test mechanically,
sync plugin docs and prompt-source mirrors, and extend `scripts/check-brand.sh` so the
patterns cannot be reintroduced. One atomic commit per the big-bang locked decision (D-01)
— the integration test exercises both the MCP field rename and the Typer flag rename in
the same E2E flow, so any split creates a self-failing intermediate state.

Purpose: Complete the v1.4 workspace-path resolution cleanup at the public surface,
finishing what Phase 22 started internally. Bans reintroduction via brand-gate.

Output: One commit modifying ~10 files; all 7 WSMCP requirements satisfied; brand-gate
extended.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-workspace-api-external-rename/23-CONTEXT.md
@.planning/phases/23-workspace-api-external-rename/23-PATTERNS.md
@.planning/phases/22-workspace-api-internal-rename/22-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Source-of-truth signatures the executor will be touching. Read these in the per-task
read_first lists; do not re-derive from grep. -->

From `agents/graph-wiki-agent/src/graph_wiki_agent/commands/init.py` (already accepts
`repo_path` after Phase 22 — confirmed at L42-L94):
```python
async def run_init(
    *,
    topic: str,
    tool: str,
    force: bool = False,
    workspace_path: Path | None = None,
    repo_path: Path | None = None,
    ...
) -> dict:
    ...
    repo_root = repo_path if repo_path is not None else Path.cwd()
    ...
```

From `agents/graph-wiki-agent/src/graph_wiki_mcp/server.py` (post-Phase-22 boundary state):
- 6 input classes carry `vault_path: str` (Pydantic Field or bare default)
- Current imports at L60: `from pydantic import BaseModel, Field` — Task 1 must EXTEND
  this to `from pydantic import BaseModel, ConfigDict, Field` to enable `extra='forbid'`.
- Pydantic v2 `BaseModel` default is `extra='ignore'`: an unknown field on a payload
  (e.g. `{"vault_path": "/x"}` after the rename) is SILENTLY DROPPED — the model
  instantiates with the default `workspace_path=""` instead of failing. This is
  ROADMAP SC#2's "old field call MUST fail schema validation" requirement: every
  Wiki*Input class needs `model_config = ConfigDict(extra='forbid')` to make
  validation reject the legacy field.
- Internal handlers read `input.vault_path` and bind to local `vault: Path | None`
- 2 tool description strings contain the literal `vault_path`

From `agents/graph-wiki-agent/src/graph_wiki_agent/cli.py`:
- 7 commands carry `vault: str = typer.Option("", "--vault", help="Vault path ...")`
- Each command body bridges to `workspace_path = Path(vault) if vault else None`
- `bootstrap` (L438-L458) currently has no `--repo` flag

From `packages/vault-io/src/vault_io/scan_monorepo.py`:
- L399 helper `_vault_path_for(pkg, vault_dir=None) -> str` (parameter `vault_dir` is
  out of scope per D-07 — only the function name and dict key rename)
- L395 emission inside `scan_workspaces`: `w["vault_path"] = _vault_path_for(...)`
- L666 + L717 emissions inside `_load_existing_pages`: `pages[name] = {"vault_path": ...}`
- L616 docstring: `Return dict of workspace name → {vault_path, package_path, category}`

From `agents/graph-wiki-agent/src/graph_wiki_agent/commands/scan.py`:
- Single read site: `pkg.get("vault_path", ...)` — must move to
  `pkg.get("wiki_relative_path", ...)` in lockstep with scan_monorepo.py

From `scripts/check-brand.sh` (verified L38-L80):
- 3 existing CHECK blocks (CHECK 1 lattice/code-wiki, CHECK 2 graph-wiki:init/wiki_init,
  CHECK 3 def init( cli guard)
- Tail `echo "BRAND-04 OK: ..."` parenthetical at L80
- Allowlist filter: `grep -vF -f <(grep -vE '^[[:space:]]*(#|$)' "$ALLOWLIST")`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename MCP Pydantic input fields + add extra='forbid' + internal reads (WSMCP-01)</name>
  <files>agents/graph-wiki-agent/src/graph_wiki_mcp/server.py</files>
  <read_first>
    - agents/graph-wiki-agent/src/graph_wiki_mcp/server.py
    - .planning/phases/23-workspace-api-external-rename/23-PATTERNS.md (§"MCP Pydantic Input Classes (WSMCP-01)")
    - .planning/phases/22-workspace-api-internal-rename/22-01-SUMMARY.md (V7, V8 verification — confirms what Phase 22 preserved at this boundary)
  </read_first>
  <action>
    In `agents/graph-wiki-agent/src/graph_wiki_mcp/server.py`, perform the following
    targeted edits per D-01 (single commit) and D-05 (hard rename, no shim):

    **(0) Extend the pydantic import (L60).** The file currently has:
    `from pydantic import BaseModel, Field`
    Change to:
    `from pydantic import BaseModel, ConfigDict, Field`
    `ConfigDict` is required so each `Wiki*Input` class can set
    `model_config = ConfigDict(extra='forbid')`. Without `extra='forbid'`, Pydantic v2
    `BaseModel` defaults to `extra='ignore'`, meaning a payload with the legacy field
    `{"vault_path": "/x"}` will SILENTLY succeed (instantiating with `workspace_path=""`
    via env-var fallback) — that violates ROADMAP SC#2.

    **(1) Six Pydantic field declarations — rename `vault_path` → `workspace_path`
    in each of the 6 input classes:**
    - `WikiQueryInput` (~L103-L108): change `vault_path: str = ""` (Shape A) to
      `workspace_path: str = ""`. Update the trailing comment if it references "vault".
    - `WikiLogInput` (~L150-L156): change
      `vault_path: str = Field("", description="Vault path (default: GRAPH_WIKI_WORKSPACE env var)")`
      to
      `workspace_path: str = Field("", description="Workspace path (default: GRAPH_WIKI_WORKSPACE env var)")`.
    - `WikiBootstrapInput` (~L192-L198): same Shape B rename.
    - `WikiScanInput` (~L242-L249): same Shape B rename. The adjacent `repo_path` field
      STAYS but its description text currently says
      `"Override repo root for scanner (default: resolved from vault_path). Use for testing."`
      — change `vault_path` → `workspace_path` inside that description string only.
    - `WikiIngestInput` (~L299-L309): same Shape B rename.
    - `WikiLintInput` (~L374-L381): same Shape B rename.

    **(1a) Add `model_config = ConfigDict(extra='forbid')` to EACH of the 6
    `Wiki*Input` classes** (`WikiQueryInput`, `WikiLogInput`, `WikiBootstrapInput`,
    `WikiScanInput`, `WikiIngestInput`, `WikiLintInput`). Place it as the first line
    inside the class body (above the field declarations), e.g. for `WikiQueryInput`:
    ```python
    class WikiQueryInput(BaseModel):
        model_config = ConfigDict(extra='forbid')
        query: str
        workspace_path: str = ""
        top_k: int = Field(default=5, ge=3, le=10)
    ```
    This is what makes a payload `{"vault_path": "/x"}` raise `pydantic.ValidationError`
    rather than silently coerce to the env-var default. Do NOT add it to `PingInput` or
    `PingOutput` or to the `Wiki*Output` classes — only the 6 input classes whose
    schemas are reached from over-the-wire MCP payloads.

    **(2) Six internal field reads — rename `input.vault_path` → `input.workspace_path`
    at the 6 sites identified in PATTERNS.md (L125, L169, L215, L266, L332, L411).**
    Each site has the uniform shape:
    `vault = Path(input.vault_path) if input.vault_path else None`
    Rename to:
    `vault = Path(input.workspace_path) if input.workspace_path else None`
    Keep the local var name `vault` unchanged per Phase 22 boundary convention (Pattern 1
    in 23-PATTERNS.md) — only the field-read on the RHS moves. The downstream
    `await run_*(workspace_path=vault, ...)` call was already updated in Phase 22.

    **(3) Two tool description strings — at L121 (`wiki_query` decorator-string) and
    L328 (`wiki_ingest` decorator-string) the literal `vault_path` appears in prose.**
    Rename to `workspace_path` in both strings.

    DO NOT rename: the `repo_path` field on `WikiScanInput`; any `vault_dir` keyword
    parameter (out of scope per D-07); the local var `vault` (boundary convention).
    DO NOT add back-compat aliases or deprecation warnings (D-05).
  </action>
  <verify>
    <automated>
      uv run pytest agents/graph-wiki-agent/tests -x -q 2>&1 | tail -20 &&
      ! grep -nE '^\s+vault_path:\s+(str|Path)' agents/graph-wiki-agent/src/graph_wiki_mcp/server.py &&
      ! grep -nE 'input\.vault_path' agents/graph-wiki-agent/src/graph_wiki_mcp/server.py &&
      [ "$(grep -c 'workspace_path: str' agents/graph-wiki-agent/src/graph_wiki_mcp/server.py)" = "6" ] &&
      [ "$(grep -c \"extra='forbid'\" agents/graph-wiki-agent/src/graph_wiki_mcp/server.py)" = "6" ] &&
      grep -qE 'from pydantic import BaseModel, ConfigDict, Field' agents/graph-wiki-agent/src/graph_wiki_mcp/server.py &&
      ! grep -nE '\bvault_path\b' agents/graph-wiki-agent/src/graph_wiki_mcp/server.py | grep -v workspace_path
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE '^\s+vault_path:\s+(str|Path)' agents/graph-wiki-agent/src/graph_wiki_mcp/server.py` returns 0 matches (no Pydantic Field declaration of `vault_path` remains).
    - `grep -nE 'input\.vault_path' agents/graph-wiki-agent/src/graph_wiki_mcp/server.py` returns 0 matches (all 6 internal reads renamed).
    - `grep -c 'workspace_path: str' agents/graph-wiki-agent/src/graph_wiki_mcp/server.py` equals exactly `6` (each input class has the new field).
    - **Six `Wiki*Input` classes set `model_config = ConfigDict(extra='forbid')`**: `grep -c "extra='forbid'" agents/graph-wiki-agent/src/graph_wiki_mcp/server.py` equals exactly `6`.
    - **Pydantic import extended**: `grep -qE 'from pydantic import BaseModel, ConfigDict, Field' agents/graph-wiki-agent/src/graph_wiki_mcp/server.py` succeeds.
    - **No bare `vault_path` token (excluding the substring inside `workspace_path`) remains anywhere in server.py**: `grep -nE '\bvault_path\b' agents/graph-wiki-agent/src/graph_wiki_mcp/server.py | grep -v workspace_path` returns 0 lines (catches the 2 in-string narrative references at L121, L328 in addition to the Field declarations).
    - `grep -nE '"Vault path"|\"vault_path\"' agents/graph-wiki-agent/src/graph_wiki_mcp/server.py` returns 0 matches in tool description strings.
    - `repo_path: str = Field(` still appears once on `WikiScanInput` (the adjacent field is preserved); its description string no longer contains the substring `vault_path`.
    - `uv run pytest agents/graph-wiki-agent/tests/unit -x -q` exits 0 (no MCP-layer unit test breaks; integration test will be updated in Task 5).
  </acceptance_criteria>
  <done>All 6 MCP input classes use `workspace_path` field AND `model_config = ConfigDict(extra='forbid')`; pydantic import extended with `ConfigDict`; all 6 internal handlers read `input.workspace_path`; both decorator-string narrative references say `workspace_path`; no bare `vault_path` token remains anywhere in server.py; `repo_path` on `WikiScanInput` preserved with updated description; unit tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Rename 7 Typer --vault flags to --workspace and add bootstrap --repo (WSMCP-02, WSMCP-03)</name>
  <files>agents/graph-wiki-agent/src/graph_wiki_agent/cli.py</files>
  <read_first>
    - agents/graph-wiki-agent/src/graph_wiki_agent/cli.py
    - agents/graph-wiki-agent/src/graph_wiki_agent/commands/init.py (confirms `run_init` already accepts `repo_path: Path | None = None` at L47 and passes it through — no plumbing needed)
    - .planning/phases/23-workspace-api-external-rename/23-PATTERNS.md (§"Typer CLI Flags (WSMCP-02)", §"Additive `--repo` Flag on `bootstrap` Only (WSMCP-03)")
    - .planning/phases/23-workspace-api-external-rename/23-CONTEXT.md (D-02 — bootstrap-only scope)
  </read_first>
  <action>
    In `agents/graph-wiki-agent/src/graph_wiki_agent/cli.py`, for each of the 7 commands
    `query` (~L382), `log` (~L420), `bootstrap` (~L442), `scan` (~L463),
    `ingest_source` (~L501), `ingest_work_item` (~L526), `lint` (~L560):

    **(A) Three-way uniform rename per command (Pattern 2 in 23-PATTERNS.md):**
    1. Python parameter name: `vault: str = typer.Option(...)` → `workspace: str = typer.Option(...)`
    2. Flag literal (positional arg #2 to `typer.Option`): `"--vault"` → `"--workspace"`
    3. Help text: replace the substring `Vault path` with `Workspace path`
    The end shape per command is:
    `workspace: str = typer.Option("", "--workspace", help="Workspace path (default: GRAPH_WIKI_WORKSPACE env var)"),`

    **(B) Local-var bridge in each command body — rename the RHS variable:**
    `workspace_path = Path(vault) if vault else None`
    becomes
    `workspace_path = Path(workspace) if workspace else None`
    The local var `workspace_path` already exists post-Phase-22; only the source name
    on the RHS changes.

    **(C) Bootstrap-only additive `--repo` flag (WSMCP-03 / D-02):** In the
    `bootstrap` command signature (and ONLY `bootstrap` — D-02 prohibits adding `--repo`
    to scan/lint/ingest/query/log), append a new Typer option after `--workspace`:
    ```
    repo: str = typer.Option("", "--repo", help="Override repo root (default: CWD walk-up)"),
    ```
    In the bootstrap body, after the `workspace_path = Path(workspace) if workspace else None`
    bridge, add:
    ```
    repo_path = Path(repo).resolve() if repo else None
    ```
    Then in the `asyncio.run(run_init(...))` call, add the kwarg `repo_path=repo_path`.
    `run_init` already accepts `repo_path: Path | None = None` and threads it to
    `workspace_io.init.init(repo_root=...)` — confirmed in interfaces section — so no
    plumbing changes are needed downstream of cli.py.

    DO NOT add `--repo` to any other command (D-02). DO NOT rename the local
    `workspace_path` variable. DO NOT rename any `repo_path` symbol — only ADD the new
    flag wiring.

    Note: `test_cli_query.py::test_vault_flag_in_help` is a pre-existing failure from
    Phase 22 — this rename will make that test name semantically misleading but should
    leave the test body verifying that some flag (now `--workspace`) appears in help.
    If the test asserts `--vault` literally, update the assertion to `--workspace`
    (this is mechanical sweep, allowed under D-04 mechanical-update posture).
  </action>
  <verify>
    <automated>
      [ "$(grep -c '\"--vault\"' agents/graph-wiki-agent/src/graph_wiki_agent/cli.py)" = "0" ] &&
      [ "$(grep -c '\"--workspace\"' agents/graph-wiki-agent/src/graph_wiki_agent/cli.py)" = "7" ] &&
      [ "$(grep -c '\"--repo\"' agents/graph-wiki-agent/src/graph_wiki_agent/cli.py)" = "1" ] &&
      [ "$(grep -nE 'run_init\([^)]*repo_path=repo_path' agents/graph-wiki-agent/src/graph_wiki_agent/cli.py | wc -l | tr -d ' ')" -ge "1" ] &&
      uv run graph-wiki-agent bootstrap --help 2>&1 | grep -qE -- '--workspace' &&
      uv run graph-wiki-agent bootstrap --help 2>&1 | grep -qE -- '--repo' &&
      uv run graph-wiki-agent scan --help 2>&1 | grep -qE -- '--workspace' &&
      ! uv run graph-wiki-agent scan --help 2>&1 | grep -qE -- '--repo\b'
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"--vault"' agents/graph-wiki-agent/src/graph_wiki_agent/cli.py` equals `0`.
    - `grep -c '"--workspace"' agents/graph-wiki-agent/src/graph_wiki_agent/cli.py` equals exactly `7` (one per command).
    - `grep -c '"--repo"' agents/graph-wiki-agent/src/graph_wiki_agent/cli.py` equals exactly `1` (bootstrap only).
    - **`--repo` wired through to `run_init`**: `grep -nE 'run_init\([^)]*repo_path=repo_path' agents/graph-wiki-agent/src/graph_wiki_agent/cli.py` returns at least 1 match (the bootstrap body must pass `repo_path=repo_path` into `run_init(...)`).
    - `uv run graph-wiki-agent bootstrap --help` lists both `--workspace` and `--repo`; help text mentions `GRAPH_WIKI_WORKSPACE`.
    - `uv run graph-wiki-agent scan --help` lists `--workspace` but NOT `--repo` (D-02 scope check).
    - Every command body's bridge line is `workspace_path = Path(workspace) if workspace else None` (no remaining `Path(vault)`).
    - `grep -nE 'Path\(vault\)' agents/graph-wiki-agent/src/graph_wiki_agent/cli.py` returns 0 matches.
    - ROADMAP success criterion #1 satisfied: `graph-wiki-agent scan --help` shows `--workspace`, `graph-wiki-agent bootstrap --help` also shows `--repo`.
  </acceptance_criteria>
  <done>7 Typer commands use `--workspace`; only `bootstrap` exposes `--repo`; `--repo` is wired through `run_init(repo_path=repo_path)`; all command bodies bridge through `workspace_path = Path(workspace) ...`; `--help` output reflects the rename on all commands.</done>
</task>

<task type="auto">
  <name>Task 3: Rename scan JSON field to wiki_relative_path in scan_monorepo and its consumer (WSMCP-04)</name>
  <files>packages/vault-io/src/vault_io/scan_monorepo.py, agents/graph-wiki-agent/src/graph_wiki_agent/commands/scan.py</files>
  <read_first>
    - packages/vault-io/src/vault_io/scan_monorepo.py
    - agents/graph-wiki-agent/src/graph_wiki_agent/commands/scan.py
    - .planning/phases/23-workspace-api-external-rename/23-PATTERNS.md (§"Scan JSON Output Field (WSMCP-04)")
    - .planning/REQUIREMENTS.md (§WSMCP-04 — exact line numbers 395, 399, 616, 666, 717)
  </read_first>
  <action>
    Per D-05 (hard rename, no shim) and the milestone-locked semantic
    (`wiki_relative_path` correctly names what these strings actually are — relative
    paths under the wiki directory, NOT paths to a vault root):

    **In `packages/vault-io/src/vault_io/scan_monorepo.py`:**

    (1) **Helper function rename at L399:** `_vault_path_for` → `_wiki_relative_path_for`.
    Update the function definition's docstring's first line from
    `"Return the canonical vault page path for a discovered workspace."` to
    `"Return the wiki-relative page path for a discovered workspace."`.
    The function's parameter `vault_dir: str | None = None` STAYS unchanged — that's a
    `vault-io` package-internal name and out of scope per D-07.

    (2) **Three dict-key emission sites — change every `"vault_path"` JSON key to
    `"wiki_relative_path"`:**
    - L395 inside `scan_workspaces`:
      `w["vault_path"] = _vault_path_for(w, vault_dir=vault_dir)`
      becomes
      `w["wiki_relative_path"] = _wiki_relative_path_for(w, vault_dir=vault_dir)`
    - L666 inside `_load_existing_pages` (first pass): change key `"vault_path"` to
      `"wiki_relative_path"`.
    - L717 inside `_load_existing_pages` (domains pass): same key rename.

    (3) **Docstring at L616** in `_load_existing_pages`:
    `"""Return dict of workspace name → {vault_path, package_path, category}."""`
    becomes
    `"""Return dict of workspace name → {wiki_relative_path, package_path, category}."""`

    **In `agents/graph-wiki-agent/src/graph_wiki_agent/commands/scan.py`:**

    (4) Find the consumer site (Phase 22 SUMMARY V8 documents this as the single read
    site): `pkg.get("vault_path", ...)`. Change to `pkg.get("wiki_relative_path", ...)`.

    DO NOT touch the `WikiConfig.vault_path` field in `graph_wiki_agent/config.py`
    (per Phase 22 SUMMARY V8 — out of scope, separate plugin config dataclass).
    DO NOT touch private helpers in `query.py` (per Phase 22 SUMMARY V8 — out of scope).
    DO NOT touch prompt string literals in `prompts/_fragments/page_categories.py`
    (per Phase 22 SUMMARY V8 — they're prose, not scan JSON keys).
  </action>
  <verify>
    <automated>
      [ "$(grep -cE '"vault_path"' packages/vault-io/src/vault_io/scan_monorepo.py)" = "0" ] &&
      [ "$(grep -cE '_vault_path_for' packages/vault-io/src/vault_io/scan_monorepo.py)" = "0" ] &&
      [ "$(grep -cE '"wiki_relative_path"' packages/vault-io/src/vault_io/scan_monorepo.py)" = "3" ] &&
      [ "$(grep -cE '_wiki_relative_path_for' packages/vault-io/src/vault_io/scan_monorepo.py)" -ge "2" ] &&
      [ "$(grep -cE 'pkg\.get\("vault_path"' agents/graph-wiki-agent/src/graph_wiki_agent/commands/scan.py)" = "0" ] &&
      uv run pytest packages/vault-io/tests -x -q 2>&1 | tail -5
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '"vault_path"' packages/vault-io/src/vault_io/scan_monorepo.py` equals `0`.
    - `grep -cE '"wiki_relative_path"' packages/vault-io/src/vault_io/scan_monorepo.py` equals exactly `3` (three emission sites).
    - `grep -cE '_vault_path_for' packages/vault-io/src/vault_io/scan_monorepo.py` equals `0` (function rename complete, including the call site at L395).
    - `_wiki_relative_path_for` appears at least twice (definition + at least one call).
    - `grep -nE 'vault_path' packages/vault-io/src/vault_io/scan_monorepo.py` returns hits ONLY in the `vault_dir` parameter (which is out of scope) — no `"vault_path"` string literal hits remain.
    - In `agents/graph-wiki-agent/src/graph_wiki_agent/commands/scan.py`, the read site is `pkg.get("wiki_relative_path", ...)`; no `pkg.get("vault_path", ...)` remains.
    - `uv run pytest packages/vault-io/tests -x -q` exits 0 (vault-io scan tests still pass).
    - ROADMAP success criterion #3 satisfied: scan JSON output contains `wiki_relative_path`; `vault_path` does not appear in any scan result field.
  </acceptance_criteria>
  <done>scan_monorepo emits `wiki_relative_path` at all 3 sites via `_wiki_relative_path_for`; docstring updated; the lone in-tree consumer in commands/scan.py reads the new key; vault-io tests pass.</done>
</task>

<task type="auto">
  <name>Task 4: Sync plugin docs and prompt-source mirror (WSMCP-05)</name>
  <files>plugins/graph-wiki/agents/scanner.md, plugins/graph-wiki/skills/graph-wiki/references/scan-workflow.md, packages/prompt-sources/references/scan-workflow.md</files>
  <read_first>
    - plugins/graph-wiki/agents/scanner.md
    - plugins/graph-wiki/skills/graph-wiki/references/scan-workflow.md
    - packages/prompt-sources/references/scan-workflow.md
    - .planning/phases/23-workspace-api-external-rename/23-PATTERNS.md (§"Plugin Docs + Prompt-Source Mirrors (WSMCP-05)", §"Pattern 5: Plugin Doc ↔ Prompt-Source Mirror Pair")
    - .planning/phases/23-workspace-api-external-rename/23-CONTEXT.md (D-08 — mirror sync is mandatory)
  </read_first>
  <action>
    Sweep plugin markdown docs and their runtime-loaded mirrors so the doc layer reflects
    the new field name. Pre-scan via PATTERNS.md confirmed only THREE files actually
    contain hits — `detection-workflow.md`, `wiki-schema.md`, and `commands/scan.md` had
    no hits and may be skipped:

    **(1) `plugins/graph-wiki/agents/scanner.md` (L48):**
    The line contains the prose "the scanner emits `vault_path` per workspace — use it"
    embedded in a longer sentence about routing rules. Replace the literal token
    `` `vault_path` `` with `` `wiki_relative_path` `` (the backtick-wrapped identifier).
    This file has NO mirror in `packages/prompt-sources/references/` per
    23-PATTERNS.md §"Pattern 5" — `agents/scanner.md` is a Claude Code subagent prompt
    source, not a reference doc.

    **(2) `plugins/graph-wiki/skills/graph-wiki/references/scan-workflow.md`:**
    - L68: `"The scanner emits an explicit \`vault_path\` for every workspace — write the page there."` — replace `` `vault_path` `` with `` `wiki_relative_path` ``.
    - L102: `"If \`domain:\` is set on the row, each pkg's \`vault_path\` lands at ..."` — replace `` `vault_path` `` with `` `wiki_relative_path` ``.

    **(3) `packages/prompt-sources/references/scan-workflow.md` (the runtime-loaded
    mirror per D-08):**
    The mirror is intended to be byte-identical with the plugin half per D-08 (1:1
    rename-mirror invariant). Apply the SAME edits at the same line numbers as the
    plugin half above, then verify with a full content `diff` (see verify block).

    After editing, run a verification grep across both halves to confirm zero remaining
    hits, AND a `diff` of the two files to confirm the mirror invariant holds.

    DO NOT touch `plugins/graph-wiki/skills/graph-wiki/references/detection-workflow.md`,
    `plugins/graph-wiki/skills/graph-wiki/references/wiki-schema.md`, or
    `plugins/graph-wiki/commands/scan.md` — pre-grep confirmed no hits, and editing
    would violate Karpathy guideline 3 (surgical changes). The same applies to other
    `packages/prompt-sources/references/*.md` files that have no current hits.

    DO NOT change any references to `--vault` in prose (re-grep confirmed there are
    none in this file set — but if any surface, rename them mechanically too).
  </action>
  <verify>
    <automated>
      [ "$(grep -cnE '\bvault_path\b|--vault\b' plugins/graph-wiki/agents/scanner.md)" = "0" ] &&
      [ "$(grep -cnE '\bvault_path\b|--vault\b' plugins/graph-wiki/skills/graph-wiki/references/scan-workflow.md)" = "0" ] &&
      [ "$(grep -cnE '\bvault_path\b|--vault\b' packages/prompt-sources/references/scan-workflow.md)" = "0" ] &&
      diff plugins/graph-wiki/skills/graph-wiki/references/scan-workflow.md packages/prompt-sources/references/scan-workflow.md
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -cnE '\bvault_path\b|--vault\b' plugins/graph-wiki/agents/scanner.md` returns `0`.
    - `grep -cnE '\bvault_path\b|--vault\b' plugins/graph-wiki/skills/graph-wiki/references/scan-workflow.md` returns `0`.
    - `grep -cnE '\bvault_path\b|--vault\b' packages/prompt-sources/references/scan-workflow.md` returns `0`.
    - **Plugin half and mirror half of `scan-workflow.md` are byte-identical**: `diff plugins/graph-wiki/skills/graph-wiki/references/scan-workflow.md packages/prompt-sources/references/scan-workflow.md` exits 0 with empty output (D-08 1:1 invariant).
    - `grep -rnE '\bvault_path\b|--vault\b' plugins/graph-wiki/ packages/prompt-sources/references/ 2>/dev/null` returns 0 hits (per 23-PATTERNS.md final verification grep).
  </acceptance_criteria>
  <done>Three doc files updated, plugin half and prompt-source mirror are byte-identical for scan-workflow.md, no `vault_path` or `--vault` references remain anywhere under `plugins/graph-wiki/` or `packages/prompt-sources/references/`.</done>
</task>

<task type="auto">
  <name>Task 5: Update DA-CLI integration test mechanically + opportunistic live run + SC#2 smoke (WSMCP-06)</name>
  <files>agents/graph-wiki-agent/tests/integration/test_mcp_e2e.py</files>
  <read_first>
    - agents/graph-wiki-agent/tests/integration/test_mcp_e2e.py
    - .planning/phases/23-workspace-api-external-rename/23-PATTERNS.md (§"Integration Test (WSMCP-06)")
    - .planning/phases/23-workspace-api-external-rename/23-CONTEXT.md (D-04 — mechanical update + opportunistic live run)
  </read_first>
  <action>
    Update the integration test's JSON payloads to use the new MCP field names and
    Typer flag literals, per D-04 (mechanical update + opportunistic live run).

    **(1) Six payload-builder helpers (PATTERNS.md L165-258) — each currently has a
    `"vault_path": vault_path` JSON key inside an `"arguments": {"input": {...}}`
    dict. Rename the JSON key `"vault_path"` → `"workspace_path"` in all 6 builders:**
    - `_send_wiki_bootstrap` (~L165)
    - `_send_wiki_scan` (~L181) — note: this builder ALSO carries a `"repo_path"` key
      (~L190); LEAVE that key unchanged, only the `vault_path` key moves.
    - `_send_wiki_ingest` (~L197)
    - `_send_wiki_query` (~L214)
    - `_send_wiki_lint` (~L230)
    - `_send_wiki_log` (~L244)

    **(2) Python parameter name on each builder** (e.g. `def _send_wiki_bootstrap(request_id: int, vault_path: str)`) MAY stay as `vault_path` (Claude's Discretion per 23-CONTEXT.md) — but renaming for consistency to `workspace_path` is the cleaner pattern and is preferred. Update all call sites in the test body if you rename the parameter.

    **(3) Any `--vault` flag literal** in subprocess invocations (search the test file
    for `"--vault"`) — rename to `"--workspace"`. If `--repo` is used in any bootstrap
    invocation as part of test scaffolding, leave it as is or add a new line if the
    test wants to exercise WSMCP-03 (executor's discretion).

    **(4) Any assertion or fixture name** like `vault_path = tmp_path / ...` referring
    to a workspace root path — rename mechanically to `workspace_path` for consistency
    (this is a non-load-bearing local name; renaming keeps the diff intent-revealing).

    **(5) SC#2 smoke assertion (mechanical, no Bedrock required).** Add a small
    standalone test function (or inline assertion in an existing test that runs in the
    default pytest pass) that constructs a `WikiScanInput` with the legacy field name
    and asserts it raises `pydantic.ValidationError`. This mechanically verifies that
    Task 1's `model_config = ConfigDict(extra='forbid')` is doing its job at the
    schema layer, without needing the env-gated live Bedrock run. Example shape:
    ```python
    import pytest
    from pydantic import ValidationError
    from graph_wiki_mcp.server import WikiScanInput

    def test_legacy_vault_path_field_rejected_by_schema():
        with pytest.raises(ValidationError):
            WikiScanInput(**{"vault_path": "/tmp/x"})
    ```
    Place this in `test_mcp_e2e.py` or an adjacent unit test file (executor's choice)
    so it runs in the default `uv run pytest` gate (no `GRAPH_WIKI_RUN_INTEGRATION`
    needed). This satisfies ROADMAP SC#2 mechanically.

    **(6) Opportunistic live run (D-04):** The plan executor SHOULD do:
    ```
    if [ "${GRAPH_WIKI_RUN_INTEGRATION:-}" = "1" ]; then
      uv run pytest agents/graph-wiki-agent/tests/integration/test_mcp_e2e.py -x -q || exit 1
    else
      echo "UAT-01: Pat to run integration test manually before phase close:"
      echo "  GRAPH_WIKI_RUN_INTEGRATION=1 uv run pytest agents/graph-wiki-agent/tests/integration/test_mcp_e2e.py"
      echo "(record result in SUMMARY.md)"
    fi
    ```
    Add the UAT line to SUMMARY.md if the env var is not set, per D-04. If it IS set
    and the test fails, the plan fails.

    DO NOT touch any other file in this task beyond the integration test and (if you
    choose) a single adjacent unit-test file for the SC#2 smoke.
  </action>
  <verify>
    <automated>
      [ "$(grep -cnE '"vault_path"' agents/graph-wiki-agent/tests/integration/test_mcp_e2e.py)" = "0" ] &&
      [ "$(grep -cnE '"workspace_path"' agents/graph-wiki-agent/tests/integration/test_mcp_e2e.py)" = "6" ] &&
      [ "$(grep -cnE '"--vault"' agents/graph-wiki-agent/tests/integration/test_mcp_e2e.py)" = "0" ] &&
      uv run pytest agents/graph-wiki-agent/tests -x -q 2>&1 | tail -10 &&
      uv run pytest agents/graph-wiki-agent/tests -x -q -k 'legacy_vault_path_field_rejected_by_schema' 2>&1 | tail -5 &&
      if [ "${GRAPH_WIKI_RUN_INTEGRATION:-}" = "1" ]; then
        uv run pytest agents/graph-wiki-agent/tests/integration/test_mcp_e2e.py -x -q || exit 1;
      else
        echo "UAT-01 deferred (GRAPH_WIKI_RUN_INTEGRATION not set)";
      fi
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -cnE '"vault_path"' agents/graph-wiki-agent/tests/integration/test_mcp_e2e.py` returns `0`.
    - `grep -cnE '"workspace_path"' agents/graph-wiki-agent/tests/integration/test_mcp_e2e.py` returns exactly `6` (one JSON key per builder).
    - `grep -cnE '"--vault"' agents/graph-wiki-agent/tests/integration/test_mcp_e2e.py` returns `0`.
    - **SC#2 smoke assertion present**: `uv run pytest agents/graph-wiki-agent/tests -k 'legacy_vault_path_field_rejected_by_schema' -x -q` discovers ≥1 test and exits 0 (the test asserts `WikiScanInput(**{"vault_path": "/x"})` raises `ValidationError`).
    - Default `uv run pytest agents/graph-wiki-agent/tests -x -q` (no GRAPH_WIKI_RUN_INTEGRATION) exits 0 (integration test guarded by skipif marker stays skipped; SC#2 smoke runs and passes).
    - If `GRAPH_WIKI_RUN_INTEGRATION=1` is set AND AWS creds present, `uv run pytest agents/graph-wiki-agent/tests/integration/test_mcp_e2e.py -x -q` exits 0 (failure propagates explicitly via `|| exit 1`); otherwise, executor records a `UAT-01` line in SUMMARY.md naming the manual run command.
    - ROADMAP success criterion #4 satisfied (test references new field/flag names mechanically; opportunistic live-run pass OR UAT line recorded).
  </acceptance_criteria>
  <done>All 6 payload builders use `"workspace_path"` JSON key; any `--vault` test refs renamed to `--workspace`; SC#2 smoke assertion present and green; default pytest green; opportunistic live integration run executed (with explicit failure propagation) if env var set, otherwise UAT-01 recorded.</done>
</task>

<task type="auto">
  <name>Task 6: Extend scripts/check-brand.sh with CHECK 4 + seed .brand-grep-allow (WSMCP-07)</name>
  <files>scripts/check-brand.sh, .brand-grep-allow</files>
  <read_first>
    - scripts/check-brand.sh
    - .brand-grep-allow
    - .planning/phases/23-workspace-api-external-rename/23-PATTERNS.md (§"Brand-Gate Extension (WSMCP-07)", §"`.brand-grep-allow` Seeding")
    - .planning/phases/23-workspace-api-external-rename/23-CONTEXT.md (D-03 — exact scope/anchors)
  </read_first>
  <action>
    **STEP A — Dry-run FIRST, before editing the script's enforcement logic.**
    Run a discovery grep with the three target regexes against `packages/ agents/ plugins/`
    (the locked scope per D-03):
    ```
    grep -rEln --exclude-dir=__pycache__ --exclude='*.pyc' -E \
        '^[[:space:]]+vault_path:[[:space:]]+(str|Path|int|bool)|"--vault"|"vault_path"' \
        packages/ agents/ plugins/ 2>/dev/null
    ```
    After Tasks 1-5 have shipped, this should ideally return zero hits in production
    code. Any hits that remain are candidates for either (a) a missed rename (go fix in
    its task's file scope before continuing) or (b) a narrowly-allowlisted historical
    reference (rare; D-03 says don't pre-seed).

    **STEP B — Append CHECK 4 block to `scripts/check-brand.sh` (Pattern 3 in
    23-PATTERNS.md).** Insert after the existing CHECK 3 block, BEFORE the final
    `echo "BRAND-04 OK: ..."` line:

    ```bash
    # CHECK 4 — Phase 23 §WSMCP-07: ban reintroduction of the three workspace-API
    # legacy patterns:
    #   (1) `vault_path:` Pydantic Field declaration (anchored to class-body indent)
    #   (2) `"--vault"` Typer flag literal
    #   (3) `"vault_path"` JSON/dict key
    # Path scope is packages/ agents/ plugins/ only — .planning/ historical docs
    # are excluded per D-03 (this phase's CONTEXT, PATTERNS, prior SUMMARYs and
    # REQUIREMENTS legitimately reference the old name and must not be edited).
    HITS4=$(grep -rEln --exclude-dir=__pycache__ --exclude='*.pyc' -E \
        '^[[:space:]]+vault_path:[[:space:]]+(str|Path|int|bool)|"--vault"|"vault_path"' \
        packages/ agents/ plugins/ 2>/dev/null \
        | grep -vF -f <(grep -vE '^[[:space:]]*(#|$)' "$ALLOWLIST") || true)

    if [ -n "$HITS4" ]; then
      echo "$HITS4"
      COUNT4=$(printf '%s\n' "$HITS4" | wc -l | tr -d ' ')
      echo "BRAND-WSAPI FAIL: ${COUNT4} unallowlisted hits for vault_path Field|--vault flag|\"vault_path\" key" >&2
      exit 1
    fi
    ```

    **STEP C — Update the final OK echo at L80** of `scripts/check-brand.sh`. Currently:
    ```
    echo "BRAND-04 OK: zero unallowlisted hits (BRAND-04 lattice + BRAND-CMD graph-wiki:init|wiki_init + BRAND-CMD-CLI def init( all clean)"
    ```
    Append `+ BRAND-WSAPI vault_path|--vault|\"vault_path\"` inside the parenthetical so
    the success line reflects CHECK 4. The closing parenthesis is preserved.

    **STEP D — Run `bash scripts/check-brand.sh` AFTER editing.** It must exit 0.
    If it fails:
    - If failure is on a real code file that should have been renamed in Tasks 1-5,
      go back and fix that file (cite the line in the SUMMARY's auto-fix log).
    - If failure is on a truly unavoidable historical reference (e.g. a fixture file
      that documents the OLD format on purpose), add an allowlist entry to
      `.brand-grep-allow` mirroring the Phase 21 style:
      ```
      # rationale: WSMCP-07 D-03 — <one-line reason>
      <path-fragment>
      ```
      Then re-run the gate until it exits 0.

    Important regex anchors to remember (per 23-PATTERNS.md):
    - `^[[:space:]]+vault_path:` requires indented class-body context — won't catch
      bare `vault_path` in comments/docstrings.
    - `"--vault"` is the double-quoted literal — won't catch single-quoted variants
      (verified the codebase only uses double quotes).
    - `"vault_path"` catches dict-literal keys; per D-03, tests are NOT excluded.

    DO NOT extend CHECK 1's alternation (use a new block per Pattern 3).
    DO NOT broaden the path scope to `.planning/` or `scripts/` (D-03 narrows it).
    DO NOT pre-seed allowlist entries — add only what the dry-run surfaces.
  </action>
  <verify>
    <automated>
      bash scripts/check-brand.sh 2>&1 | tail -5 &&
      grep -q 'BRAND-WSAPI' scripts/check-brand.sh &&
      grep -q 'CHECK 4' scripts/check-brand.sh &&
      grep -q 'HITS4=' scripts/check-brand.sh &&
      # Negative test: synthesize a fixture file inside packages/ that contains a
      # banned pattern, assert the gate fails, and ALWAYS clean up the fixture
      # whether the gate behaves correctly or not (no leaked file in the worktree).
      printf '    vault_path: str\n' > packages/vault-io/src/vault_io/_wsmcp07_negative_test.py
      if bash scripts/check-brand.sh >/dev/null 2>&1; then
        rm -f packages/vault-io/src/vault_io/_wsmcp07_negative_test.py
        echo "FAIL: gate missed synthesized reintroduction" >&2
        exit 1
      fi
      rm -f packages/vault-io/src/vault_io/_wsmcp07_negative_test.py
      # Confirm clean tree still passes after fixture removed.
      bash scripts/check-brand.sh >/dev/null 2>&1
    </automated>
  </verify>
  <acceptance_criteria>
    - `scripts/check-brand.sh` contains a `# CHECK 4` block with `HITS4=` and the literal regex `'^[[:space:]]+vault_path:[[:space:]]+(str|Path|int|bool)|"--vault"|"vault_path"'`.
    - `BRAND-WSAPI FAIL` and `BRAND-WSAPI` appear in the script (failure message + final OK line addition).
    - Path scope on the new block is `packages/ agents/ plugins/` only (no `.planning/`, no `scripts/`, no `docs/`).
    - `bash scripts/check-brand.sh` (clean tree) exits 0.
    - **Negative test cleans up unconditionally**: dropping a file containing `    vault_path: str` into `packages/vault-io/src/vault_io/` causes the gate to exit non-zero; the synthesized fixture file `_wsmcp07_negative_test.py` is removed by the verify block in BOTH the gate-fails-correctly branch AND the gate-misbehaves branch (no leaked file in the worktree after Task 6 completes).
    - ROADMAP success criterion #5 satisfied: gate exits non-zero on test-file reintroduction of `vault_path` Field or `--vault` Typer flag.
  </acceptance_criteria>
  <done>CHECK 4 block live in `scripts/check-brand.sh`; final OK echo mentions BRAND-WSAPI; allowlist (if seeded) carries rationale comments per Phase 21 style; gate green on clean tree, red on synthesized reintroduction, fixture cleaned up unconditionally.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP host → MCP server | Untrusted JSON payloads cross into Pydantic schema validation (`graph_wiki_mcp/server.py`). Schema validation is the only authoritative gate. |
| User shell → Typer CLI | Untrusted CLI argv crosses into Typer flag parsing (`graph_wiki_agent/cli.py`). Typer validates types but not paths. |
| Scanner output → ingestor input | Internal trust boundary: scan JSON dict-key shape is a contract between `vault_io.scan_monorepo` and `commands/scan.py` / downstream plugin consumers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Tampering | MCP Pydantic schema (server.py) | mitigate | Each `Wiki*Input` class sets `model_config = ConfigDict(extra='forbid')` (Task 1 §1a). With this, a payload carrying the legacy `vault_path` key raises `pydantic.ValidationError` at schema-validation time rather than being silently dropped (Pydantic v2 default is `extra='ignore'`). The Task 5 SC#2 smoke test asserts this behavior mechanically. No new attack surface — the rename HARDENS validation by removing a previously-accepted field. |
| T-23-02 | Information Disclosure | scan JSON output (`wiki_relative_path` emission) | accept | Field rename does not change what data is emitted — same wiki-relative path string, new key name. No new info-disclosure risk. |
| T-23-03 | Tampering | `bootstrap --repo` flag (new attack surface) | mitigate | The `repo: str` flag resolves via `Path(repo).resolve()` before passing to `run_init(repo_path=...)`. `run_init` already validates repo root via `_find_repo_root` walk-up semantics — no new path-traversal vector beyond what Phase 22 audited. Typer ensures only string input; no shell-eval risk. |
| T-23-04 | Denial of Service | brand-gate (`scripts/check-brand.sh`) | accept | The gate is a developer-time check, not a runtime path. A pathological regex could DoS CI but the regex set is finite and bounded. Acceptable. |
| T-23-05 | Repudiation | integration test live-run env-gating | accept | `GRAPH_WIKI_RUN_INTEGRATION=1` opt-in is a developer convenience; no audit-log impact. The UAT-01 line in SUMMARY.md provides documented trail when env var absent. |
| T-23-SC | Tampering | npm/pip/cargo installs | mitigate | **N/A — no package installs in this phase.** Pure rename + brand-gate; no new dependencies added to any `pyproject.toml`. RESEARCH.md not present (this is Level-0 discovery — mechanical refactor with established codebase patterns), so no package legitimacy audit is required. If any future task adds a dependency, route through researcher first per the package legitimacy gate. |
</threat_model>

<verification>
**Phase-level verification — all 5 ROADMAP success criteria as checkable assertions:**

**SC #1 — CLI help reflects rename:**
```bash
uv run graph-wiki-agent scan --help 2>&1 | grep -qE -- '--workspace'
uv run graph-wiki-agent scan --help 2>&1 | grep -qvE -- '--vault'
uv run graph-wiki-agent bootstrap --help 2>&1 | grep -qE -- '--workspace'
uv run graph-wiki-agent bootstrap --help 2>&1 | grep -qE -- '--repo'
for cmd in lint ingest-source ingest-work-item query log; do
  uv run graph-wiki-agent "$cmd" --help 2>&1 | grep -qE -- '--workspace' || exit 1
done
```

**SC #2 — MCP schema accepts new field, rejects old (now mechanically enforced by
`extra='forbid'` from Task 1 §1a and asserted by Task 5 §5 smoke):**
```bash
# Positive: new field accepted
uv run python -c "
from graph_wiki_mcp.server import WikiScanInput
WikiScanInput(workspace_path='/tmp/foo')  # must not raise
print('OK: workspace_path accepted')
"
# Negative: old field REJECTED with ValidationError (extra='forbid' enabled)
uv run python -c "
import sys
from pydantic import ValidationError
from graph_wiki_mcp.server import WikiScanInput
try:
    WikiScanInput(**{'vault_path': '/tmp/foo'})
except ValidationError:
    print('OK: vault_path rejected by extra=forbid')
    sys.exit(0)
print('FAIL: vault_path was silently accepted', file=sys.stderr)
sys.exit(1)
"
# Mechanical smoke via pytest (added in Task 5):
uv run pytest agents/graph-wiki-agent/tests -k 'legacy_vault_path_field_rejected_by_schema' -x -q
```

**SC #3 — Scan JSON emits new key:**
```bash
grep -cE '"wiki_relative_path"' packages/vault-io/src/vault_io/scan_monorepo.py  # expect 3
grep -cE '"vault_path"' packages/vault-io/src/vault_io/scan_monorepo.py          # expect 0
grep -cE 'pkg\.get\("wiki_relative_path"' agents/graph-wiki-agent/src/graph_wiki_agent/commands/scan.py  # expect ≥1
```

**SC #4 — Integration test runnable under env-gate:**
```bash
# Mechanical sweep verifiable always:
grep -cE '"workspace_path"' agents/graph-wiki-agent/tests/integration/test_mcp_e2e.py  # expect 6
grep -cE '"vault_path"' agents/graph-wiki-agent/tests/integration/test_mcp_e2e.py      # expect 0
# Opportunistic live run (D-04):
if [ "${GRAPH_WIKI_RUN_INTEGRATION:-}" = "1" ]; then
  uv run pytest agents/graph-wiki-agent/tests/integration/test_mcp_e2e.py -x -q || exit 1
else
  echo "UAT-01: manual integration run pending — record in SUMMARY.md"
fi
```

**SC #5 — Brand-gate active and rejects reintroduction:**
```bash
# Clean tree: gate passes
bash scripts/check-brand.sh
# Reintroduction smoke (negative test, unconditional cleanup):
printf '    vault_path: str = Field("")\n' > packages/vault-io/src/vault_io/_negative.py
if bash scripts/check-brand.sh >/dev/null 2>&1; then
  rm -f packages/vault-io/src/vault_io/_negative.py
  echo "FAIL: gate missed reintroduction" >&2
  exit 1
fi
rm -f packages/vault-io/src/vault_io/_negative.py
bash scripts/check-brand.sh  # clean again
```

**Overall pytest gate (D-04 plan-completion gate):**
```bash
uv run pytest 2>&1 | tail -10
# Must show same or better than Phase 22's baseline:
#   582 passed, 33 skipped, 6 pre-existing failures
# The 6 pre-existing failures are documented in 22-01-SUMMARY.md and are NOT caused
# by Phase 23. If any new failures appear, the plan is incomplete.
```
</verification>

<success_criteria>
Phase 23 is complete when ALL of the following hold:

1. **External surface coherence:** No production source file under `agents/` `packages/`
   `plugins/` contains a `vault_path:` Pydantic Field declaration, a `"--vault"` Typer
   flag literal, or a `"vault_path"` JSON dict key. Verified via `grep` and via the
   new brand-gate CHECK 4.
2. **MCP schema rename complete + strict:** 6 input classes use `workspace_path: str`
   AND `model_config = ConfigDict(extra='forbid')`; 6 internal handlers read
   `input.workspace_path`; 2 tool-description strings updated; pydantic import
   extended with `ConfigDict`.
3. **CLI flag rename complete:** 7 commands use `--workspace`; only `bootstrap` adds
   `--repo`; `--repo` is threaded into `run_init(repo_path=repo_path)`; all command
   bodies bridge through `workspace_path = Path(workspace) ...`.
4. **Scan JSON contract migrated:** `_wiki_relative_path_for` defined and called once;
   `"wiki_relative_path"` emitted at 3 sites in `scan_monorepo.py`; consumer in
   `commands/scan.py` reads the new key.
5. **Plugin docs + prompt-source mirrors synced:** 3 doc files updated; plugin half
   and mirror half of `scan-workflow.md` are byte-identical (D-08 1:1 invariant).
6. **Integration test mechanically updated + SC#2 smoke green:** 6 payload builders
   use `"workspace_path"`; `--vault` literals replaced; SC#2 smoke test
   `test_legacy_vault_path_field_rejected_by_schema` passes in default pytest run;
   default `uv run pytest` green; either live integration run passed under
   `GRAPH_WIKI_RUN_INTEGRATION=1` (with `|| exit 1` failure propagation) OR UAT-01
   line recorded in SUMMARY.md.
7. **Brand-gate enforces reintroduction ban:** `scripts/check-brand.sh` contains
   CHECK 4; clean tree → exit 0; synthesized reintroduction → exit non-zero; verify
   block cleans up the fixture file unconditionally.
8. **Pytest baseline preserved:** Workspace-wide `uv run pytest` shows no new
   failures relative to the Phase 22 baseline (582 passed, 33 skipped, 6 pre-existing).
9. **Single atomic commit per D-01:** All file changes land in one commit (the task
   sequence in this plan is for executor mental model, but final commit consolidates).
</success_criteria>

<output>
Create `.planning/phases/23-workspace-api-external-rename/23-01-SUMMARY.md` when done.

Required SUMMARY contents:
- Files touched (~10 file paths)
- WSMCP-01 through WSMCP-07 requirements satisfied (cite per-requirement file:line evidence)
- Pytest result vs Phase 22 baseline
- All 5 ROADMAP SC verification grep outputs (including SC#2 smoke test result)
- Integration test status (live-run result if env var set, else UAT-01 line)
- Brand-gate CHECK 4 dry-run + post-rename verification (and confirmation that the negative-test fixture was cleaned up)
- Any `.brand-grep-allow` entries added (with rationale)
- Deviations / auto-fixes
- Decisions referenced (D-01 big-bang, D-02 bootstrap-only --repo, D-03 narrow gate, D-04 opportunistic live run, D-05 hard rename, D-06 wiki_dir helper, D-07 vault-io stays, D-08 mirror sync)
</output>
</content>
</invoke>