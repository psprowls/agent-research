---
phase: 21-rename-code-wiki-agent-to-graph-wiki-agent-update-all-code-r
plan: 02
type: execute
wave: 2
depends_on:
  - 21-01
files_modified:
  - agents/graph-wiki-agent/pyproject.toml
  - uv.lock
autonomous: true
requirements: []  # Phase 21 has no REQ-IDs
must_haves:
  truths:
    - "`agents/graph-wiki-agent/pyproject.toml` declares `name = \"graph-wiki-agent\"` (was `\"code-wiki-agent\"`)."
    - "`[project.scripts]` declares `graph-wiki-agent = \"graph_wiki_agent.cli:app\"` and `graph-wiki-mcp = \"graph_wiki_mcp.server:main\"` (no `code-wiki-*` keys, no `code_wiki_*` targets)."
    - "`uv.lock` regenerated; workspace install succeeds (`uv sync` green)."
    - "Layer 2 gate is `uv sync` only (D-11 relaxed); pytest remains deferred to layer 3 because the script targets point at not-yet-renamed Python modules until layer 3 sweeps imports."
  artifacts:
    - path: "agents/graph-wiki-agent/pyproject.toml"
      provides: "Renamed package name + console scripts"
      contains: "name = \"graph-wiki-agent\""
    - path: "uv.lock"
      provides: "Workspace lockfile regenerated to reflect the renamed package"
  key_links:
    - from: "agents/graph-wiki-agent/pyproject.toml [project.scripts]"
      to: "agents/graph-wiki-agent/src/graph_wiki_agent/cli.py + src/graph_wiki_mcp/server.py"
      via: "console-script entry points"
      pattern: "graph_wiki_agent\\.cli:app|graph_wiki_mcp\\.server:main"
---

<objective>
D-09 layer 2: rename the package `name` and the two `[project.scripts]` console-script keys + their entry-point targets in `agents/graph-wiki-agent/pyproject.toml`. Regenerate `uv.lock`. Land as ONE commit.

Purpose: lets `uv sync` rebind the workspace + install the renamed console scripts (`graph-wiki-agent`, `graph-wiki-mcp`) on `$PATH`. The script targets (`graph_wiki_agent.cli:app`, `graph_wiki_mcp.server:main`) point at modules that won't EXIST as importable Python until layer 3 sweeps the actual `from code_wiki_agent` imports — but the directories themselves were renamed by layer 1, so the Python *file* `agents/graph-wiki-agent/src/graph_wiki_agent/cli.py` exists. What still says `code_wiki_agent` inside that file is its `from code_wiki_agent.X` import lines.

Per 21-PATTERNS.md `pyproject-manifest`: "the per-commit gate at layer 2 is just `uv sync` (regenerates the lockfile + installs the renamed package); `uv run pytest` is not required to pass until layer 3 (D-11 'or equivalent scope')."

**SP-3 reminder:** any error during `uv sync` after the edit triggers `git reset --hard HEAD~1` + diagnose + retry — do not stack a fix commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/21-rename-code-wiki-agent-to-graph-wiki-agent-update-all-code-r/21-CONTEXT.md
@.planning/phases/21-rename-code-wiki-agent-to-graph-wiki-agent-update-all-code-r/21-PATTERNS.md
@.planning/phases/21-rename-code-wiki-agent-to-graph-wiki-agent-update-all-code-r/21-01-SUMMARY.md
@agents/graph-wiki-agent/pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Edit pyproject.toml and regenerate uv.lock</name>
  <files>agents/graph-wiki-agent/pyproject.toml, uv.lock</files>
  <read_first>
    - 21-PATTERNS.md §"Class: `pyproject-manifest`" — the full pattern + layer-2 gate-relaxation rationale + the PIPESTATUS idiom for future use
    - 21-CONTEXT.md §"Cutover Strategy" D-09 layer 2 + D-11 "or equivalent scope"
    - .planning/milestones/v1.2-phases/12-drift-backport-ecosystem-rebrand-m2/12-03-rebrand-sweep-PLAN.md Task 1 `<verify>` block (lines 113-115) — source of the PIPESTATUS idiom inherited by Phase 21 from layer 3 onward
    - `agents/graph-wiki-agent/pyproject.toml` (current contents — already moved by layer 1; shows the old `name = "code-wiki-agent"` and `code-wiki-agent`/`code-wiki-mcp` script keys still in place)
  </read_first>
  <action>
    Working directory: `../deep-agents-rename` (the worktree from layer 1). Start every Bash invocation with `cd ../deep-agents-rename` because the harness resets cwd between calls.

    Edit `agents/graph-wiki-agent/pyproject.toml` — change exactly three lines (and only those lines; SP-3 + "Surgical Changes" — every line touched must trace to the rename):

    Before:
    ```toml
    [project]
    name = "code-wiki-agent"
    ...
    [project.scripts]
    code-wiki-agent = "code_wiki_agent.cli:app"
    code-wiki-mcp   = "code_wiki_mcp.server:main"
    ```

    After:
    ```toml
    [project]
    name = "graph-wiki-agent"
    ...
    [project.scripts]
    graph-wiki-agent = "graph_wiki_agent.cli:app"
    graph-wiki-mcp   = "graph_wiki_mcp.server:main"
    ```

    Leave everything else (`version`, `description`, `dependencies`, `[tool.uv.sources]`, `[build-system]`, `[tool.pytest.ini_options]`) verbatim — none of those reference the package name.

    Regenerate the lockfile (creates a diff in `uv.lock` reflecting the renamed workspace member):

    ```bash
    uv sync 2>&1 | tail -20
    SYNC_RC=${PIPESTATUS[0]}
    test "$SYNC_RC" -eq 0 || { echo "UV SYNC FAILED rc=$SYNC_RC"; exit 1; }
    ```

    Stage and commit:

    ```bash
    git add agents/graph-wiki-agent/pyproject.toml uv.lock
    git status   # sanity — should show ONLY these two files modified
    git commit -m "refactor(21): rename package name + console scripts to graph-wiki-agent"
    ```

    **Layer-2 gate is `uv sync` only.** `uv run pytest` will fail because the freshly-installed console scripts try to import `graph_wiki_agent.cli` but that module's source still says `from code_wiki_agent.config import ...` etc. — layer 3 fixes that. Do NOT attempt to run pytest as a gate at this layer; per D-11 the "equivalent scope" gate is `uv sync` for layers 1-2.
  </action>
  <verify>
    <automated>cd ../deep-agents-rename && grep -q '^name = "graph-wiki-agent"$' agents/graph-wiki-agent/pyproject.toml || { echo "pyproject.toml name= not updated"; exit 1; }; grep -qE '^graph-wiki-agent = "graph_wiki_agent\.cli:app"' agents/graph-wiki-agent/pyproject.toml || { echo "graph-wiki-agent script key missing or wrong target"; exit 1; }; grep -qE '^graph-wiki-mcp\s+= "graph_wiki_mcp\.server:main"' agents/graph-wiki-agent/pyproject.toml || { echo "graph-wiki-mcp script key missing or wrong target"; exit 1; }; ! grep -qE 'code[-_]wiki[-_](agent|mcp)' agents/graph-wiki-agent/pyproject.toml || { echo "stale code-wiki-* token still present in pyproject.toml"; grep -nE 'code[-_]wiki[-_](agent|mcp)' agents/graph-wiki-agent/pyproject.toml; exit 1; }; uv sync 2>&1 | tail -10; SYNC_RC=${PIPESTATUS[0]}; test "$SYNC_RC" -eq 0 || { echo "UV SYNC FAILED rc=$SYNC_RC"; exit 1; }; HEAD_SUBJECT=$(git log -1 --format='%s'); echo "$HEAD_SUBJECT" | grep -qE '^refactor\(21\): rename package name \+ console scripts to graph-wiki-agent' || { echo "HEAD subject mismatch: $HEAD_SUBJECT"; exit 1; }; CHANGED_FILES=$(git show --stat HEAD --format= | awk 'NF>0 && !/^ [0-9]+ file/ {print $1}' | sort -u); echo "$CHANGED_FILES" | grep -qF 'agents/graph-wiki-agent/pyproject.toml' || { echo "pyproject.toml not in HEAD"; exit 1; }; echo "$CHANGED_FILES" | grep -qF 'uv.lock' || { echo "uv.lock not in HEAD"; exit 1; }; EXTRA=$(echo "$CHANGED_FILES" | grep -vE '^(agents/graph-wiki-agent/pyproject\.toml|uv\.lock)$' | grep -v '^$' || true); test -z "$EXTRA" || { echo "HEAD touches unexpected files: $EXTRA"; exit 1; }; echo "LAYER-2 OK (pytest deliberately not run — expected red per D-11 relaxation; layer 3 sweeps imports)"</automated>
  </verify>
  <acceptance_criteria>
    - `agents/graph-wiki-agent/pyproject.toml` contains `name = "graph-wiki-agent"` (exactly).
    - `[project.scripts]` block contains `graph-wiki-agent = "graph_wiki_agent.cli:app"` and `graph-wiki-mcp = "graph_wiki_mcp.server:main"` — old keys/values gone.
    - `grep -E 'code[-_]wiki[-_](agent|mcp)' agents/graph-wiki-agent/pyproject.toml` returns zero matches.
    - `uv sync` exits 0 (captured via `${PIPESTATUS[0]}`).
    - HEAD commit subject is `refactor(21): rename package name + console scripts to graph-wiki-agent`.
    - HEAD commit touches exactly two files: `agents/graph-wiki-agent/pyproject.toml` and `uv.lock`. No collateral edits (Surgical Changes).
    - `uv run pytest` is **not** run at this layer.
  </acceptance_criteria>
  <done>pyproject.toml + uv.lock committed; `uv sync` green; layer-2 commit is surgical (2 files); pytest deferred to layer 3.</done>
</task>

</tasks>

<verification>
- HEAD commit on `rename-21` is exactly the pyproject + lockfile edit (no collateral).
- `uv sync` green; console scripts `graph-wiki-agent` / `graph-wiki-mcp` are now installed on `$PATH` inside the worktree's venv (they will be importable-and-runnable after layer 3 fixes their imports).
- SP-3 observed: no fix-forward commit was needed because the edit was small and the gate was the targeted-narrow `uv sync`.
</verification>

<success_criteria>
Phase 21 goal SC#3 surface (console scripts `code-wiki-agent` / `code-wiki-mcp` are gone) lands in `pyproject.toml`. End-to-end verification of the renamed scripts (CLI help renders, MCP `python -c "from graph_wiki_mcp import server"` succeeds) is gated by layer 3's import sweep.
</success_criteria>

<output>
Create `.planning/phases/21-rename-code-wiki-agent-to-graph-wiki-agent-update-all-code-r/21-02-SUMMARY.md` with:
- HEAD commit SHA + subject
- `git show --stat HEAD` output (proving exactly 2 files: pyproject.toml + uv.lock)
- `uv sync` tail output (proving green)
- Explicit note: "pytest deferred to layer 3 per D-11 relaxation; script targets point at not-yet-fixed modules"
- Pointer to plan 21-03 (the import sweep that restores pytest-green)
</output>
