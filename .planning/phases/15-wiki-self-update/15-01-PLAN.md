---
phase: 15-wiki-self-update
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - models-claude.toml
  - wiki-config-claude.toml
  - .planning/phases/15-wiki-self-update/15-VERIFICATION.md
autonomous: false
requirements:
  - BRAND-03
must_haves:
  truths:
    - "models-claude.toml exists at repo root with all 10 role tables matching the Phase 15 Haiku/Sonnet split"
    - "wiki-config-claude.toml exists at repo root pointing at models-claude.toml and the existing vault path"
    - "code-wiki-agent scan against ~/Personal/wiki/deep-agents completes cleanly with --config wiki-config-claude.toml"
    - "Scan-log entries appended by this run reference new package names (workspace-io / prompt-sources / graph-wiki) without `lattice` artifacts"
    - "code-wiki-agent ingest of ~/Personal/wiki/raw/OTel — Story of observability.md completes cleanly"
    - "code-wiki-agent query 'what is workspace-io?' returns a librarian-synthesized answer citing post-rebrand code paths"
    - "~/Personal/wiki/deep-agents/packages/workspace-io/ page produced by scan has parseable frontmatter, 2-3 key claims in body, and at least one resolving [[wikilink]]"
    - "15-VERIFICATION.md documents SC#1, SC#2, SC#3 evidence with the query transcript in a four-backtick fenced block"
  artifacts:
    - path: "models-claude.toml"
      provides: "Claude role-override profile for Phase 15 (Haiku fan-out + Sonnet synth)"
      contains: "roles.scanner"
    - path: "wiki-config-claude.toml"
      provides: "One-off WikiConfig pointing models_path at models-claude.toml + vault_path at ~/Personal/wiki/deep-agents"
      contains: "models-claude.toml"
    - path: ".planning/phases/15-wiki-self-update/15-VERIFICATION.md"
      provides: "Phase 15 SC#1/#2/#3 evidence: scan-log excerpt, workspace-io page spot-check, query transcript"
      contains: "BRAND-03"
  key_links:
    - from: "wiki-config-claude.toml"
      to: "models-claude.toml"
      via: "models_path absolute path"
      pattern: "models_path\\s*=\\s*\"/Users/pat/Personal/deep-agents/models-claude.toml\""
    - from: "code-wiki-agent CLI invocation"
      to: "models-claude.toml"
      via: "--config wiki-config-claude.toml top-level callback flag"
      pattern: "--config.*wiki-config-claude.toml"
    - from: "15-VERIFICATION.md"
      to: "code-wiki-agent query transcript"
      via: "four-backtick fenced block under '## Smoke transcript'"
      pattern: "````"
---

<objective>
Bring `~/Personal/wiki/deep-agents` into alignment with the post-rebrand codebase (Phases 11-14) by driving a `code-wiki-agent` scan + ingest pass through a one-off Claude role-override profile (Haiku 4.5 fan-out + Sonnet 4.6 synthesis), then verifying the result with one librarian query and a single-page spot-check. Closes BRAND-03 — the only requirement in Phase 15.

Purpose: The vault is meaningfully drifted (missing `workspace-io` and `prompt-sources` package pages, stale references in pages that scan will refresh). One scan + one ingest + one query produces a post-rebrand baseline of the wiki and proves the Bedrock CLI surface still works against the live `~/Personal/wiki/deep-agents` vault after the rebrand.

Output: Two new repo configs (`models-claude.toml`, `wiki-config-claude.toml`), one new phase verification doc (`.planning/phases/15-wiki-self-update/15-VERIFICATION.md`), and external mutations to the wiki vault (new package pages, refreshed scan-log, updated OTel source summary) captured indirectly via VERIFICATION.md evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

@.planning/phases/15-wiki-self-update/15-CONTEXT.md
@.planning/phases/15-wiki-self-update/15-PATTERNS.md
@.planning/phases/14-plugin-port-m3b/14-VERIFICATION.md

@models-qwen.toml
@wiki-config.toml

<interfaces>
<!-- Key contracts the executor will need. Extracted from CONTEXT and PATTERNS. -->
<!-- Use these directly — no codebase exploration needed for these surfaces. -->

CLI override wiring (from agents/code-wiki-agent/src/code_wiki_agent/cli.py:35-45):
- Top-level callback option: `--config <PATH-TO-WIKICONFIG-TOML>` (NOT `--models-config`)
- The callback calls `model_adapter.loader.set_models_path()` with the file pointed at by `models_path` inside the WikiConfig TOML.
- WikiConfig shape (from wiki-config.toml): exactly two keys at top level — `models_path` and `vault_path`.

Role TOML shape (from models-qwen.toml):
- 10 `[roles.<name>]` tables in order: haiku, sonnet, librarian, scanner, linter, ingestor, code_reader, synthesizer, judge_a, judge_b.
- Each table has exactly four keys: `model_id`, `region`, `max_tokens`, `max_concurrency`.
- Loaded raw via tomllib in packages/model-adapter/src/model_adapter/loader.py (indexed by role-name string).

Per D-02 + PATTERNS.md role-by-role assignment for models-claude.toml:
- roles.haiku           model_id = "us.anthropic.claude-haiku-4-5-20251001-v1:0"  (verbatim from qwen)
- roles.sonnet          model_id = "us.anthropic.claude-sonnet-4-6"               (verbatim from qwen)
- roles.librarian       model_id = "us.anthropic.claude-sonnet-4-6"               (CHANGED from qwen.qwen3-next-80b-a3b)
- roles.scanner         model_id = "us.anthropic.claude-haiku-4-5-20251001-v1:0"  (CHANGED from qwen.qwen3-32b-v1:0)
- roles.linter          model_id = "us.anthropic.claude-haiku-4-5-20251001-v1:0"  (CHANGED from qwen.qwen3-32b-v1:0)
- roles.ingestor        model_id = "us.anthropic.claude-haiku-4-5-20251001-v1:0"  (CHANGED from qwen.qwen3-32b-v1:0)
- roles.code_reader     model_id = "us.anthropic.claude-haiku-4-5-20251001-v1:0"  (CHANGED from qwen.qwen3-32b-v1:0)
- roles.synthesizer     model_id = "us.anthropic.claude-sonnet-4-6"               (CHANGED from qwen.qwen3-next-80b-a3b)
- roles.judge_a         model_id = "us.anthropic.claude-sonnet-4-6"               (verbatim from qwen)
- roles.judge_b         model_id = "us.amazon.nova-pro-v1:0"                      (verbatim from qwen)

For every role: preserve the existing `region`, `max_tokens`, `max_concurrency` values from models-qwen.toml unless a Claude-specific reason emerges (D-03 Discretion).

VERIFICATION.md frontmatter (from 14-VERIFICATION.md:1-7):
- 5 keys: phase, verified (ISO-8601 UTC), status (passed|partial|failed), score (free-text SC summary), overrides_applied (int).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write models-claude.toml (Phase 15 Claude role-override profile)</name>
  <read_first>
    - models-qwen.toml (line-by-line analog — schema, key order, alignment style, region/max_tokens/max_concurrency values to preserve)
    - .planning/phases/15-wiki-self-update/15-CONTEXT.md (D-02, D-03 — role assignment and judge-row preservation rules)
    - .planning/phases/15-wiki-self-update/15-PATTERNS.md (role-by-role assignment table; provenance comment header pattern)
  </read_first>
  <files>models-claude.toml</files>
  <action>
    Create models-claude.toml at the repo root as a sibling of models-qwen.toml. Open with a 3-line provenance comment block naming the profile and the role-tier split (mirror models-qwen.toml:1-3 shape):

    Line 1: "# Model role overrides — Claude (Phase 15 wiki self-update profile)"
    Line 2: "# Fan-out roles (scanner, linter, ingestor, code_reader): Claude Haiku 4.5"
    Line 3: "# Reasoning roles (librarian, synthesizer): Claude Sonnet 4.6"

    Follow with exactly 10 `[roles.<name>]` tables in the same order as models-qwen.toml (haiku, sonnet, librarian, scanner, linter, ingestor, code_reader, synthesizer, judge_a, judge_b). Each table has exactly four keys: model_id, region, max_tokens, max_concurrency. Use aligned `=` formatting matching models-qwen.toml:5-9.

    Apply the model_id mapping from the `<interfaces>` block above (per D-02 + PATTERNS.md). Preserve region / max_tokens / max_concurrency verbatim from the corresponding qwen rows. Judge rows (judge_a, judge_b) are copied verbatim per D-03.

    Do NOT modify models-qwen.toml or wiki-config.toml. Do NOT add any tables or keys beyond the 10-role, 4-key schema.
  </action>
  <verify>
    <automated>test -f models-claude.toml && grep -q 'roles.scanner' models-claude.toml && grep -q 'roles.synthesizer' models-claude.toml && grep -q 'roles.judge_b' models-claude.toml && python3 -c "import tomllib; d=tomllib.load(open('models-claude.toml','rb')); assert set(d['roles'].keys()) == {'haiku','sonnet','librarian','scanner','linter','ingestor','code_reader','synthesizer','judge_a','judge_b'}, d['roles'].keys(); assert d['roles']['scanner']['model_id'] == 'us.anthropic.claude-haiku-4-5-20251001-v1:0'; assert d['roles']['synthesizer']['model_id'] == 'us.anthropic.claude-sonnet-4-6'; assert d['roles']['librarian']['model_id'] == 'us.anthropic.claude-sonnet-4-6'; assert d['roles']['judge_b']['model_id'] == 'us.amazon.nova-pro-v1:0'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - models-claude.toml exists at repo root
    - File parses as valid TOML (tomllib.load succeeds)
    - All 10 `[roles.*]` tables present: haiku, sonnet, librarian, scanner, linter, ingestor, code_reader, synthesizer, judge_a, judge_b
    - roles.scanner.model_id == "us.anthropic.claude-haiku-4-5-20251001-v1:0"
    - roles.linter.model_id == "us.anthropic.claude-haiku-4-5-20251001-v1:0"
    - roles.ingestor.model_id == "us.anthropic.claude-haiku-4-5-20251001-v1:0"
    - roles.code_reader.model_id == "us.anthropic.claude-haiku-4-5-20251001-v1:0"
    - roles.librarian.model_id == "us.anthropic.claude-sonnet-4-6"
    - roles.synthesizer.model_id == "us.anthropic.claude-sonnet-4-6"
    - roles.judge_a.model_id == "us.anthropic.claude-sonnet-4-6" (verbatim from qwen)
    - roles.judge_b.model_id == "us.amazon.nova-pro-v1:0" (verbatim from qwen)
    - region/max_tokens/max_concurrency for each role match the corresponding row in models-qwen.toml
    - File opens with a 3-line `#` provenance header naming "Phase 15 wiki self-update"
  </acceptance_criteria>
  <done>
    models-claude.toml committed to the repo, parses as valid TOML, and the four critical role assignments (scanner, librarian, synthesizer, judge_b) match the D-02/D-03 mapping.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write wiki-config-claude.toml (one-off WikiConfig pointing at models-claude.toml)</name>
  <read_first>
    - wiki-config.toml (exact 2-line analog — same shape, same vault_path value)
    - .planning/phases/15-wiki-self-update/15-PATTERNS.md (Shared Pattern §CLI override wiring — confirms --config takes a WikiConfig TOML, not a bare models path)
  </read_first>
  <files>wiki-config-claude.toml</files>
  <action>
    Create wiki-config-claude.toml at the repo root. Mirror wiki-config.toml exactly except `models_path` points at the new Claude profile. Optional one-line `#` header comment naming the use case (e.g., "# Phase 15 wiki self-update — Claude override profile") is allowed but not required.

    Required keys (two lines after any optional header):

    - `models_path = "/Users/pat/Personal/deep-agents/models-claude.toml"`
    - `vault_path  = "/Users/pat/Personal/wiki/deep-agents"`

    Do NOT modify wiki-config.toml — the existing Qwen default stays as-is per D-06 (this is an additive sibling config, not a replacement).
  </action>
  <verify>
    <automated>test -f wiki-config-claude.toml && python3 -c "import tomllib; d=tomllib.load(open('wiki-config-claude.toml','rb')); assert d['models_path'] == '/Users/pat/Personal/deep-agents/models-claude.toml', d['models_path']; assert d['vault_path'] == '/Users/pat/Personal/wiki/deep-agents', d['vault_path']; print('OK')" && test -f /Users/pat/Personal/deep-agents/models-claude.toml</automated>
  </verify>
  <acceptance_criteria>
    - wiki-config-claude.toml exists at repo root
    - File parses as valid TOML
    - models_path == "/Users/pat/Personal/deep-agents/models-claude.toml" (absolute path)
    - vault_path == "/Users/pat/Personal/wiki/deep-agents" (absolute path, same as wiki-config.toml)
    - Pointed-at models-claude.toml file actually exists on disk
    - wiki-config.toml is unchanged (no edits to the default Qwen profile)
  </acceptance_criteria>
  <done>
    wiki-config-claude.toml committed; loading it via tomllib resolves both paths to existing files.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run code-wiki-agent scan against ~/Personal/wiki/deep-agents with the Claude override</name>
  <read_first>
    - agents/code-wiki-agent/src/code_wiki_agent/cli.py (confirm the exact scan subcommand signature — does it take the vault path positionally, via --vault, or read it from the WikiConfig's vault_path?)
    - .planning/phases/15-wiki-self-update/15-CONTEXT.md (D-01, D-05 — Bedrock CLI only, scan first in sequence)
    - .planning/phases/15-wiki-self-update/15-PATTERNS.md (Shared Pattern §CLI override wiring — confirms --config is the top-level callback flag, applied BEFORE the subcommand)
  </read_first>
  <files></files>
  <action>
    Invoke the scan subcommand via the Claude override config. Exact invocation shape:

    `uv run code-wiki-agent --config /Users/pat/Personal/deep-agents/wiki-config-claude.toml scan` (with the vault path passed positionally if the CLI requires it; otherwise read from the WikiConfig's vault_path — confirm during scout by reading cli.py).

    The scan auto-detects post-rebrand workspace state: new packages `workspace-io` (Phase 11) and `prompt-sources` will be added to `~/Personal/wiki/deep-agents/packages/`; the four existing package pages (`vault-io`, `eval-harness`, `model-adapter`, `subagent-runtime`) will be refreshed.

    Capture both (a) the scan command's stdout/stderr to a buffer for inclusion in 15-VERIFICATION.md SC#1 evidence, and (b) the new entries appended to `~/Personal/wiki/deep-agents/scan-log.md` (or whatever log file the CLI writes — the executor reads vault to confirm filename). Per D-07: only the NEWLY appended entries need to be free of `lattice` artifacts; pre-existing historical lines in scan-log.md may contain `lattice` and are out of scope.

    On Bedrock auth failure, throttling, or non-zero exit: stop, capture the error transcript, and record honestly under "## Deviation from spec" in 15-VERIFICATION.md (do not retry silently or fabricate evidence).
  </action>
  <verify>
    <automated>test -d /Users/pat/Personal/wiki/deep-agents/packages/workspace-io && ls /Users/pat/Personal/wiki/deep-agents/packages/workspace-io/*.md 2>/dev/null | head -1 | grep -q '.md'</automated>
  </verify>
  <acceptance_criteria>
    - scan invocation completes with exit code 0
    - `~/Personal/wiki/deep-agents/packages/workspace-io/` directory exists after scan
    - At least one .md file inside `~/Personal/wiki/deep-agents/packages/workspace-io/` exists (a package page produced by scan)
    - Scan-log file in the vault has new entries timestamped from this run
    - Newly-appended scan-log entries contain the literal substring `workspace-io` (or `prompt-sources` or `graph-wiki`) and contain NO `lattice` substring (case-insensitive grep on the appended block only — pre-existing lines are out of scope per D-07)
    - The stdout/stderr buffer (or scan-log excerpt) is preserved for inclusion in 15-VERIFICATION.md
  </acceptance_criteria>
  <done>
    Scan completed cleanly; new workspace-io package directory exists in the wiki vault; new scan-log entries captured and free of `lattice` artifacts.
  </done>
</task>

<task type="auto">
  <name>Task 4: Run code-wiki-agent ingest for the OTel source doc with the Claude override</name>
  <read_first>
    - agents/code-wiki-agent/src/code_wiki_agent/cli.py (confirm exact ingest subcommand signature — `ingest source <PATH>` vs `ingest <PATH>` vs other)
    - .planning/phases/15-wiki-self-update/15-CONTEXT.md (D-04 — OTel is the ONLY source doc; sources/ is empty; raw/ has only this one file)
  </read_first>
  <files></files>
  <action>
    Invoke the ingest subcommand against the one existing source doc per D-04. Exact invocation shape (executor confirms during scout):

    `uv run code-wiki-agent --config /Users/pat/Personal/deep-agents/wiki-config-claude.toml ingest "/Users/pat/Personal/wiki/raw/OTel — Story of observability.md"` (or the equivalent `ingest source <path>` subcommand shape if that is what cli.py exposes).

    Capture stdout/stderr for inclusion in 15-VERIFICATION.md. The ingest will land an OTel summary under `~/Personal/wiki/deep-agents/sources/` (currently empty) per PATTERNS.md §External vault mutations.

    Do NOT ingest any other doc. Do NOT bootstrap new post-rebrand sources from `.planning/` per D-04 (rebrand surface lands via scan-detected package pages, not via source-summary ingests).
  </action>
  <verify>
    <automated>ls /Users/pat/Personal/wiki/deep-agents/sources/*.md 2>/dev/null | head -1 | grep -qE '\.md$'</automated>
  </verify>
  <acceptance_criteria>
    - ingest invocation completes with exit code 0
    - At least one .md file now exists under `~/Personal/wiki/deep-agents/sources/` (the OTel summary)
    - The ingest output transcript is captured for 15-VERIFICATION.md (used as optional evidence; SC#2 spot-checks the workspace-io scan page, not the OTel summary)
  </acceptance_criteria>
  <done>
    OTel summary landed under `~/Personal/wiki/deep-agents/sources/`; ingest completed cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 5: Run code-wiki-agent query "what is workspace-io?" with the Claude override and capture the full transcript</name>
  <read_first>
    - agents/code-wiki-agent/src/code_wiki_agent/cli.py (confirm exact query subcommand signature — quoted positional argument)
    - .planning/phases/15-wiki-self-update/15-CONTEXT.md (D-09 — literal SC#3 wording, single query only, no diff vs Phase 14)
    - .planning/phases/14-plugin-port-m3b/14-VERIFICATION.md (shape of the captured transcript — used as reference only, Phase 15 does NOT diff against it)
  </read_first>
  <files></files>
  <action>
    Invoke the query subcommand with the literal SC#3 question, captured verbatim from ROADMAP.md Phase 15 SC#3:

    `uv run code-wiki-agent --config /Users/pat/Personal/deep-agents/wiki-config-claude.toml query "what is workspace-io?"`

    Capture the FULL transcript: user question, fan-out evidence (Read/Grep tool-use traces from the librarian subagent), the synthesized answer including `[[wikilinks]]` and `code-path:line` citations. The transcript will likely contain triple-backtick code blocks for citations (matching Phase 14's pattern), so when this transcript is embedded in 15-VERIFICATION.md (Task 7) it MUST be wrapped in four-backtick fences per PATTERNS.md §VERIFICATION.md.

    Per D-09: no additional queries beyond this one literal question; no semantic diff against `14-VERIFICATION.md`.
  </action>
  <verify>
    <automated>echo "Transcript capture is verified by Task 7 (15-VERIFICATION.md contains the transcript block); no standalone automated check for the query subcommand exit since transcript content is the verification."</automated>
  </verify>
  <acceptance_criteria>
    - query subcommand completes with exit code 0
    - Captured transcript contains the literal substring `workspace-io` in the synthesized answer
    - Captured transcript shows fan-out evidence (at least one `Read:` or `Grep:` line, or equivalent tool-use trace)
    - Captured transcript contains at least one `[[wikilink]]` and at least one `code-path:line` citation referencing the post-rebrand surface (`packages/workspace-io/...` — NOT `lattice-workspace`)
    - Transcript is preserved as text ready for embedding in 15-VERIFICATION.md (Task 7)
  </acceptance_criteria>
  <done>
    Full query transcript captured; references post-rebrand `packages/workspace-io/` code paths; ready for inclusion in 15-VERIFICATION.md.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: Spot-check the workspace-io scan-produced page</name>
  <files>~/Personal/wiki/deep-agents/packages/workspace-io/ (read-only; produced by Task 3 scan)</files>
  <action>
    Open the scan-produced workspace-io page set under `~/Personal/wiki/deep-agents/packages/workspace-io/` and capture the four SC#2 data points described in `<how-to-verify>` below: absolute page path, frontmatter excerpt, 2-3 key-claim excerpts, and at least one resolved wikilink + its target file path. Hand these data points to Task 7 for embedding in `15-VERIFICATION.md` SC#2 sub-section. No vault files are modified by this task — it is read-only spot-checking.
  </action>
  <what-built>
    Task 3's scan produced a new `~/Personal/wiki/deep-agents/packages/workspace-io/` directory containing one or more package pages (`workspace-io.md` and possibly sub-pages like `api.md`, `patterns.md`, `context.md`).
  </what-built>
  <how-to-verify>
    Per D-08 minimum bar — three checks against the workspace-io page produced by scan:

    1. Confirm the absolute path of the primary page (likely `/Users/pat/Personal/wiki/deep-agents/packages/workspace-io/workspace-io.md` — adjust if scan writes a different filename). Record the exact path.

    2. Frontmatter check — open the file, confirm the YAML frontmatter block at top parses cleanly (no malformed keys, no truncation). Excerpt 1-2 frontmatter fields for the verification doc.

    3. Body check — read the body; confirm it contains a package summary plus at least 2-3 key claims about workspace-io's responsibilities (manifest IO, workspace discovery, config resolution, path accessors — claims drawn from the actual source). Excerpt 2-3 key claims verbatim for the verification doc.

    4. Wikilink check — find at least one `[[...]]` wikilink in the body; resolve it by opening the target page (e.g., `[[vault-io]]` → `/Users/pat/Personal/wiki/deep-agents/packages/vault-io/vault-io.md`); confirm the target file exists.

    All four data points (path, frontmatter excerpt, key-claim excerpts, resolved wikilink + target path) feed into the SC#2 sub-section of 15-VERIFICATION.md (Task 7).

    Per D-08: only the workspace-io page is required. The `prompt-sources` page (which scan will also create) MAY be mentioned as supplementary evidence but is not required.
  </how-to-verify>
  <acceptance_criteria>
    - Absolute path of the workspace-io page recorded
    - Frontmatter parses (excerpt captured)
    - Body has 2-3 key claims (verbatim excerpts captured)
    - At least one [[wikilink]] in body resolves to an existing file in the vault (target path recorded)
    - All four data points are ready for embedding in 15-VERIFICATION.md SC#2 sub-section
  </acceptance_criteria>
  <verify>
    <automated>test -d /Users/pat/Personal/wiki/deep-agents/packages/workspace-io && find /Users/pat/Personal/wiki/deep-agents/packages/workspace-io -name '*.md' -maxdepth 1 | head -1 | xargs -I {} test -f {}</automated>
    <human-check>Human confirms: (1) page path recorded; (2) frontmatter parses with excerpt captured; (3) 2-3 key-claim body excerpts captured verbatim; (4) at least one [[wikilink]] resolves to an existing target file (path recorded).</human-check>
  </verify>
  <done>Four SC#2 data points (page path, frontmatter excerpt, key claims, resolved wikilink + target) captured and ready for embedding in 15-VERIFICATION.md.</done>
  <resume-signal>Type "spot-check passed" with the four data points, or describe what failed (missing frontmatter, no wikilinks, no claims, etc.)</resume-signal>
</task>

<task type="auto">
  <name>Task 7: Write 15-VERIFICATION.md with SC#1/#2/#3 evidence</name>
  <read_first>
    - .planning/phases/14-plugin-port-m3b/14-VERIFICATION.md (exact template — frontmatter shape, section ordering, four-backtick fence convention for transcript blocks)
    - .planning/phases/15-wiki-self-update/15-CONTEXT.md (D-08, D-09 — SC#2 and SC#3 evidence scope)
    - .planning/phases/15-wiki-self-update/15-PATTERNS.md (Pattern Assignment §15-VERIFICATION.md — section ordering, per-SC mapping)
  </read_first>
  <files>.planning/phases/15-wiki-self-update/15-VERIFICATION.md</files>
  <action>
    Create `.planning/phases/15-wiki-self-update/15-VERIFICATION.md` following the 14-VERIFICATION.md template (PATTERNS.md §15-VERIFICATION.md).

    Frontmatter (5 keys, YAML fenced):
    - `phase: 15-wiki-self-update`
    - `verified: <ISO-8601 UTC timestamp at write time>`
    - `status: passed` (or `partial` / `failed` if any SC did not pass — record honestly)
    - `score: SC#1+#2+#3 captured`
    - `overrides_applied: 0` (unless the executor was manually overridden)

    Section ordering:
    1. H1: `# Phase 15: Wiki Self-Update — SC#1/#2/#3 Verification`
    2. `**Phase Goal:**` one-paragraph restatement of the three SCs (cite from ROADMAP Phase 15)
    3. Short metadata block: `**Verified:** <date>` / `**Status:** ...` / `**Vault used:** /Users/pat/Personal/wiki/deep-agents` / `**Models config:** /Users/pat/Personal/deep-agents/models-claude.toml`
    4. (Optional) `## Deviation from spec` — only if any Task 3/4/5/6 deviated (e.g., Bedrock throttle, vault not present, etc.)
    5. `## SC#1 — Scan-log evidence` — prose intro + fenced excerpt of the newly-appended scan-log entries from Task 3 (use a four-backtick fence). Explicitly note: only the appended block is in scope per D-07; pre-existing historical lines containing `lattice` are out of scope.
    6. `## SC#2 — workspace-io page spot-check` — capture the four data points from Task 6: absolute path, frontmatter excerpt, 2-3 key-claim excerpts, resolved wikilink with target path. Optional supplementary note about `prompt-sources` if also spot-checked.
    7. `## SC#3 — Query transcript` — prose intro + the FULL transcript from Task 5 wrapped in four-backtick fences (because the transcript itself contains triple-backtick code blocks for citations, matching the 14-VERIFICATION.md:31-104 pattern).
    8. `## Result` — one-paragraph narration of which SCs passed and which acceptance tokens are literally present in the captured evidence. Close with: `BRAND-03 satisfied. Phase 15 closes.`

    Critical formatting rule: any transcript or scan-log block that contains triple-backtick code blocks MUST be wrapped in four-backtick (````) fences, per PATTERNS.md and 14-VERIFICATION.md:31, 14-VERIFICATION.md:104.
  </action>
  <verify>
    <automated>test -f .planning/phases/15-wiki-self-update/15-VERIFICATION.md && grep -q '^phase: 15-wiki-self-update' .planning/phases/15-wiki-self-update/15-VERIFICATION.md && grep -q '## SC#1' .planning/phases/15-wiki-self-update/15-VERIFICATION.md && grep -q '## SC#2' .planning/phases/15-wiki-self-update/15-VERIFICATION.md && grep -q '## SC#3' .planning/phases/15-wiki-self-update/15-VERIFICATION.md && grep -q 'BRAND-03 satisfied' .planning/phases/15-wiki-self-update/15-VERIFICATION.md && grep -q '^````' .planning/phases/15-wiki-self-update/15-VERIFICATION.md && grep -q 'workspace-io' .planning/phases/15-wiki-self-update/15-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/phases/15-wiki-self-update/15-VERIFICATION.md`
    - Frontmatter has all 5 keys: phase, verified, status, score, overrides_applied
    - `phase: 15-wiki-self-update` is in frontmatter
    - H1 heading and the three SC section headings (`## SC#1`, `## SC#2`, `## SC#3`) all present
    - At least one four-backtick fenced block (`^````` at start of a line) present (for the query transcript)
    - SC#1 section contains scan-log evidence (newly-appended entries quoted)
    - SC#2 section contains the workspace-io page path, frontmatter excerpt, 2-3 key claims, and at least one resolved wikilink with target
    - SC#3 section contains the FULL query transcript with literal substring `workspace-io` and at least one `[[wikilink]]` and at least one `code-path:line` citation
    - Result section ends with literal: `BRAND-03 satisfied. Phase 15 closes.`
  </acceptance_criteria>
  <done>
    15-VERIFICATION.md committed; all three SC sections present; transcript wrapped in four-backtick fences; closing line names BRAND-03 and Phase 15.
  </done>
</task>

<task type="auto">
  <name>Task 8: Commit the three new repo files in atomic commits</name>
  <read_first>
    - Recent commit messages (git log -10 --oneline) to match this repo's commit message style
    - .planning/phases/15-wiki-self-update/15-CONTEXT.md (D-10 — single plan; atomic per-step commits within it, matching Phase 14 Plan 3 shape)
  </read_first>
  <files></files>
  <action>
    Per CONTEXT §code_context Established Patterns ("Atomic per-step commits within an atomic plan"), commit the three new repo-side files in this order:

    1. `git add models-claude.toml wiki-config-claude.toml && git commit -m "feat(15-01): add models-claude.toml + wiki-config-claude.toml for Phase 15 wiki self-update"`
    2. `git add .planning/phases/15-wiki-self-update/15-VERIFICATION.md && git commit -m "docs(15-01): capture Phase 15 SC#1/#2/#3 evidence — closes BRAND-03"`

    Do NOT amend prior commits. Do NOT force-push. Do NOT include any files that contain secrets. Do NOT commit changes to the wiki vault from within this repo (the vault is its own git repo per CONTEXT §code_context Integration Points; any vault-side commits are out of scope for this plan).

    Match the existing commit message style observed via `git log -10 --oneline` (typically `<type>(<phase>-<plan>): <summary>` per recent history like `docs(14-03): capture SC#4 smoke transcript`).
  </action>
  <verify>
    <automated>git log -2 --oneline | grep -E '(15-01|BRAND-03|wiki self-update|wiki-self-update)' && git status --short | wc -l | awk '{ if ($1 == "0") print "clean"; else exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - Two new commits in `git log` whose subject lines include `15-01` (or equivalent phase/plan identifier)
    - At least one of the commit subjects references `BRAND-03` or `Phase 15`
    - `git status --short` shows zero unstaged or untracked changes for the three new files (models-claude.toml, wiki-config-claude.toml, 15-VERIFICATION.md)
    - No amend operations; no force-push
    - wiki-config.toml is NOT touched (still points at models-qwen.toml)
  </acceptance_criteria>
  <done>
    Two atomic commits land the Phase 15 repo-side artifacts; working tree clean for the three new files; existing default Qwen profile untouched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo ↔ Wiki vault | Phase 15 drives `code-wiki-agent` (existing CLI) against `~/Personal/wiki/deep-agents` (separate git repo). No new code path crosses this boundary; the boundary itself is unchanged by Phase 15. |
| Repo ↔ AWS Bedrock | Existing model_adapter calls Bedrock with credentials from the developer's environment. Phase 15 swaps role configs only — does not change auth flow. |

## STRIDE Threat Register

N/A — content-only phase. No new code surface, no new auth path, no input parsing, no new endpoints, no new dependencies. The phase writes two static TOML configs (consumed by an existing loader) and one markdown verification doc, then orchestrates three invocations of an already-shipped CLI against an already-existing wiki vault. The CLI, the model adapter, the Bedrock client, and the vault filesystem layout are all unchanged.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | (N/A) | (N/A — content-only run) | accept | No new code surface introduced in this phase. Existing CLI, model adapter, vault IO, and Bedrock auth flows are unchanged. The two new TOML configs are static data consumed by an already-tested loader (`packages/model-adapter/src/model_adapter/loader.py`). |
</threat_model>

<verification>
## Overall Phase Checks

1. Three new repo files present and committed: `models-claude.toml`, `wiki-config-claude.toml`, `.planning/phases/15-wiki-self-update/15-VERIFICATION.md`.
2. `wiki-config.toml` (default Qwen profile) is unchanged.
3. `~/Personal/wiki/deep-agents/packages/workspace-io/` directory exists and has at least one .md page produced by Task 3's scan.
4. `~/Personal/wiki/deep-agents/sources/` has at least one .md file (the OTel summary) produced by Task 4's ingest.
5. `15-VERIFICATION.md` documents SC#1 (scan-log clean of `lattice` in newly-appended entries), SC#2 (workspace-io page well-formed), and SC#3 (full query transcript citing post-rebrand code paths), with the query transcript in a four-backtick fenced block.
6. Closing line of 15-VERIFICATION.md reads: `BRAND-03 satisfied. Phase 15 closes.`
7. No `agents/code-wiki-agent/` or `packages/` source code was modified in this phase.
</verification>

<success_criteria>
## Measurable Completion (BRAND-03 / ROADMAP Phase 15)

- **SC#1** — `code-wiki-agent scan ~/Personal/wiki/deep-agents` completes against the post-rebrand repo and the resulting scan-log entries appended by this run show the new package names (`workspace-io`, `graph-wiki` references) without `lattice` artifacts. Evidence: scan-log excerpt in `15-VERIFICATION.md` SC#1 section.
- **SC#2** — `code-wiki-agent ingest` re-ingests the OTel source doc; the scan-produced `workspace-io` package page exists and is well-formed (parseable frontmatter, 2-3 key claims in body, at least one resolving wikilink). Evidence: spot-check sub-section in `15-VERIFICATION.md` SC#2 section.
- **SC#3** — `code-wiki-agent query "what is workspace-io?"` returns a librarian-synthesized answer that cites the new code paths (`packages/workspace-io/...`, NOT `lattice-workspace`). Evidence: full transcript in `15-VERIFICATION.md` SC#3 section, wrapped in four-backtick fences.

All three SCs documented in a single `15-VERIFICATION.md` artifact per D-10 (single atomic plan).
</success_criteria>

<output>
On completion, create `.planning/phases/15-wiki-self-update/15-01-SUMMARY.md` recording:
- The three new repo files committed and their final SHAs (or commit subjects)
- Vault-side artifacts produced by scan/ingest (workspace-io page path, OTel summary path)
- Pointer to `15-VERIFICATION.md` for SC#1/#2/#3 evidence
- BRAND-03 marked satisfied; Phase 15 marked closed
</output>
