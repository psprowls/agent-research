---
phase: 49-builtin-kind-graph-io
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/graph-io/src/graph_io/queries.py
  - packages/graph-io/src/graph_io/uri.py
  - packages/wiki-io/src/wiki_io/entity_writer.py
  - packages/graph-io/tests/test_queries.py
autonomous: true
requirements:
  - BUILTIN-04

must_haves:
  truths:
    - "D-14: `'builtin' in graph_io.queries._VALID_KINDS` evaluates True (`\"builtin\"` admitted to the frozenset at `queries.py:9`)"
    - "D-15 / BUILTIN-04: `graph_io.uri.builtin_uri('python', 'pathlib') == 'builtin:python/pathlib'` and `builtin_uri('javascript', 'fs') == 'builtin:javascript/fs'` (URI scheme with language + module_name)"
    - "D-10: `SCHEMA_VERSION` in `packages/graph-io/src/graph_io/schema.py` is still `2` (no schema bump; kinds are text strings, Python-side admission only)"
    - "D-16: `'builtin' not in wiki_io.entity_writer.ADMITTED_KINDS` evaluates True; ADMITTED_KINDS docstring/comment explicitly documents the Phase 49 D-16 exclusion (Builtin pages not rendered to the wiki)"
  artifacts:
    - path: "packages/graph-io/src/graph_io/queries.py"
      provides: "Builtin admitted as a valid node kind via _VALID_KINDS frozenset"
      contains: '"builtin"'
    - path: "packages/graph-io/src/graph_io/uri.py"
      provides: "builtin_uri(language, module_name) URI builder"
      contains: "def builtin_uri"
    - path: "packages/wiki-io/src/wiki_io/entity_writer.py"
      provides: "ADMITTED_KINDS docstring/comment annotating the Builtin exclusion (D-16)"
      contains: "builtin"
  key_links:
    - from: "graph_io.uri.builtin_uri"
      to: "graph_io.queries._VALID_KINDS"
      via: "URI scheme prefix matches the kind name"
      pattern: "f\"builtin:"
    - from: "wiki_io.entity_writer.ADMITTED_KINDS"
      to: "wiki_io tests/test_entity_templates.py bijection invariant"
      via: "frozenset content unchanged → bijection still holds"
      pattern: "ADMITTED_KINDS ="
---

<objective>
Establish the schema-level foundation for the new `builtin` node kind: add `"builtin"` to `graph_io.queries._VALID_KINDS`, add the `builtin_uri(language, module_name)` URI builder mirroring `dependency_uri`, and annotate `wiki_io.entity_writer.ADMITTED_KINDS` to explicitly document the Phase 49 D-16 exclusion (Builtin is intentionally NOT rendered to the wiki).

Purpose: Plans 02 and 03 import `builtin_uri` and rely on `"builtin"` being admitted to `_VALID_KINDS`. This plan is the smallest possible foundation slice — no scanner logic, no queries, no CLI. Three single-token / single-line additions plus a documentation comment.

Output: Foundation in place so Plan 02 can emit Builtin nodes and Plan 03 can query them. Test additions in `test_queries.py` lock the invariants.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-builtin-kind-graph-io/49-CONTEXT.md
@.planning/phases/49-builtin-kind-graph-io/49-RESEARCH.md
@.planning/phases/49-builtin-kind-graph-io/49-PATTERNS.md
@packages/graph-io/CLAUDE.md
@packages/graph-io/src/graph_io/queries.py
@packages/graph-io/src/graph_io/uri.py
@packages/wiki-io/src/wiki_io/entity_writer.py

<interfaces>
<!-- Key contracts the executor needs. Extracted from the codebase. -->

From `packages/graph-io/src/graph_io/queries.py:9-25` (current `_VALID_KINDS`):
```python
_VALID_KINDS = frozenset(
    {
        "function", "class", "method",
        "file", "package", "repository", "subpackage",
        "entry_point", "test_suite", "domain",
        # Phase 43 (v1.8): admitted entity kinds for the wiki entity writer.
        "dependency",
        "plugin",
    }
)
```

From `packages/graph-io/src/graph_io/uri.py:53` (`dependency_uri` shape to mirror):
```python
def dependency_uri(ecosystem: str, name: str) -> str:
    return f"dependency:{ecosystem}/{name}"
```

From `packages/wiki-io/src/wiki_io/entity_writer.py:52-66` (`ADMITTED_KINDS` — must remain a 7-element frozenset; only the leading comment changes):
```python
# v1.8 admitted entity kinds — the 7 graph-derived kinds the wiki materializes
# as standalone pages under `wiki/entities/`. Underscore-form per D-02 matches
# `graph_io.queries._VALID_KINDS` casing. Phase 43+ imports this constant when
# routing graph rows to the correct template / URI builder.
ADMITTED_KINDS: frozenset[str] = frozenset(
    {
        "repository", "domain", "package", "package_family",
        "plugin", "dependency", "test_suite",
    }
)
```

From `packages/wiki-io/tests/test_entity_templates.py:38-68` (bijection invariant that already defends ADMITTED_KINDS — should continue passing after this plan):
- `test_template_kinds_match_admitted_kinds` and the kinds-bijection test must remain green.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Admit "builtin" to _VALID_KINDS and add builtin_uri URI builder</name>
  <files>packages/graph-io/src/graph_io/queries.py, packages/graph-io/src/graph_io/uri.py</files>

  <read_first>
    - packages/graph-io/src/graph_io/queries.py (current _VALID_KINDS at lines 9-25)
    - packages/graph-io/src/graph_io/uri.py (dependency_uri at line 53 — exact pattern to mirror)
    - packages/graph-io/CLAUDE.md (project conventions)
  </read_first>

  <action>
    In `packages/graph-io/src/graph_io/queries.py`, extend the `_VALID_KINDS` frozenset (lines 9-25) with the string `"builtin"`. Add it on its own line after `"plugin",` with an inline comment `# Phase 49 D-14: stdlib module imports (Python via sys.stdlib_module_names; Node via require('module').builtinModules)`. Preserve the existing Phase 43 comment.

    In `packages/graph-io/src/graph_io/uri.py`, add `builtin_uri(language: str, module_name: str) -> str` immediately after the existing `dependency_uri` function (around line 53). The function body is exactly `return f"builtin:{language}/{module_name}"`. Match the surrounding style — one-line pure function, no docstring needed beyond what `dependency_uri` carries (currently none). Order matters: keep all `*_uri` builders grouped.

    Per D-14 and D-15 / BUILTIN-04: the URI scheme is `builtin:<language>/<module_name>` (e.g. `builtin:python/pathlib`, `builtin:javascript/fs`). Do NOT change `SCHEMA_VERSION` in `schema.py` (D-10 — schema stays at 2; kinds are text strings, Python-side admission only).
  </action>

  <verify>
    <automated>uv run --package graph-io python -c "from graph_io import queries, schema; from graph_io.uri import builtin_uri; assert 'builtin' in queries._VALID_KINDS; assert builtin_uri('python', 'pathlib') == 'builtin:python/pathlib'; assert builtin_uri('javascript', 'fs') == 'builtin:javascript/fs'; assert schema.SCHEMA_VERSION == 2; print('OK')"</automated>
  </verify>

  <acceptance_criteria>
    - `python -c "from graph_io import queries; assert 'builtin' in queries._VALID_KINDS"` exits 0
    - `python -c "from graph_io.uri import builtin_uri; assert builtin_uri('python', 'pathlib') == 'builtin:python/pathlib'"` exits 0
    - `python -c "from graph_io.uri import builtin_uri; assert builtin_uri('javascript', 'fs') == 'builtin:javascript/fs'"` exits 0
    - `grep -c '"builtin"' packages/graph-io/src/graph_io/queries.py` returns at least 1
    - `grep -c '^def builtin_uri' packages/graph-io/src/graph_io/uri.py` returns 1
    - `python -c "from graph_io import schema; assert schema.SCHEMA_VERSION == 2"` exits 0 (D-10 invariant)
  </acceptance_criteria>

  <done>
    `_VALID_KINDS` admits `"builtin"`; `builtin_uri` returns the spec'd URI for python and javascript inputs; SCHEMA_VERSION unchanged at 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Document Builtin exclusion in wiki_io.ADMITTED_KINDS</name>
  <files>packages/wiki-io/src/wiki_io/entity_writer.py</files>

  <read_first>
    - packages/wiki-io/src/wiki_io/entity_writer.py (lines 50-70, the ADMITTED_KINDS frozenset and its leading comment)
    - packages/wiki-io/tests/test_entity_templates.py (the bijection invariant test — must keep passing)
    - .planning/phases/49-builtin-kind-graph-io/49-CONTEXT.md (D-16: Builtin is excluded from ADMITTED_KINDS)
  </read_first>

  <action>
    Extend the comment block immediately above the `ADMITTED_KINDS` frozenset assignment (currently lines 52-55 in `packages/wiki-io/src/wiki_io/entity_writer.py`) with an explicit annotation that the `builtin` kind is intentionally NOT admitted, per Phase 49 D-16.

    Append a paragraph to the existing comment block (do not replace the Phase 43 / Phase 42 comments — extend them). The new paragraph MUST mention: (1) "Phase 49 D-16", (2) that `builtin` is intentionally excluded, (3) the reason in one sentence (stdlib modules are inspectable via `cg list-builtins` / `cg describe-builtin` but do not warrant standalone wiki pages — would dilute the entity surface).

    DO NOT modify the frozenset's contents. The 7-element set stays at `{repository, domain, package, package_family, plugin, dependency, test_suite}`. The bijection invariant in `packages/wiki-io/tests/test_entity_templates.py` must continue to pass unchanged.
  </action>

  <verify>
    <automated>uv run --package wiki-io pytest packages/wiki-io/tests/test_entity_templates.py -x</automated>
  </verify>

  <acceptance_criteria>
    - `python -c "from wiki_io.entity_writer import ADMITTED_KINDS; assert 'builtin' not in ADMITTED_KINDS"` exits 0
    - `python -c "from wiki_io.entity_writer import ADMITTED_KINDS; assert len(ADMITTED_KINDS) == 7"` exits 0
    - `grep -c "Phase 49" packages/wiki-io/src/wiki_io/entity_writer.py` returns at least 1
    - `grep -c "builtin" packages/wiki-io/src/wiki_io/entity_writer.py` returns at least 1 (the new annotation mentions it)
    - `uv run --package wiki-io pytest packages/wiki-io/tests/test_entity_templates.py -x` exits 0
  </acceptance_criteria>

  <done>
    `ADMITTED_KINDS` content unchanged at 7 entries (bijection intact); leading comment explicitly documents the Phase 49 D-16 exclusion of the `builtin` kind.
  </done>
</task>

<task type="auto">
  <name>Task 3: Lock invariants with unit tests in test_queries.py</name>
  <files>packages/graph-io/tests/test_queries.py</files>

  <read_first>
    - packages/graph-io/tests/test_queries.py (existing test style — pick a section near `test_describe_dependency_*` around line 1074 OR the top-level section that imports queries)
    - packages/graph-io/src/graph_io/uri.py (the function being tested)
    - packages/graph-io/src/graph_io/queries.py (_VALID_KINDS being tested)
  </read_first>

  <action>
    Add two test functions to `packages/graph-io/tests/test_queries.py` (place them adjacent to the other URI / kind invariant tests — search the file for the existing `_VALID_KINDS` references; if none exist, add a new section near the top of the file after the imports):

    1. `test_valid_kinds_includes_builtin()` — imports `queries` and asserts `"builtin" in queries._VALID_KINDS`. Three lines including the def.
    2. `test_builtin_uri_shape()` — imports `builtin_uri` from `graph_io.uri` and asserts:
       - `builtin_uri("python", "pathlib") == "builtin:python/pathlib"`
       - `builtin_uri("javascript", "fs") == "builtin:javascript/fs"`
       - `builtin_uri("python", "os.path") == "builtin:python/os.path"` (only top-level callers normalize — the builder itself is a pure formatter)

    Match the existing test style in the file (free-function tests using fixtures from conftest where needed; the two new tests need no fixtures). No conftest changes.
  </action>

  <verify>
    <automated>uv run --package graph-io pytest packages/graph-io/tests/test_queries.py::test_valid_kinds_includes_builtin packages/graph-io/tests/test_queries.py::test_builtin_uri_shape -x</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "def test_valid_kinds_includes_builtin" packages/graph-io/tests/test_queries.py` returns 1
    - `grep -c "def test_builtin_uri_shape" packages/graph-io/tests/test_queries.py` returns 1
    - `uv run --package graph-io pytest packages/graph-io/tests/test_queries.py::test_valid_kinds_includes_builtin packages/graph-io/tests/test_queries.py::test_builtin_uri_shape -x` exits 0
    - `uv run --package graph-io pytest packages/graph-io/tests/test_queries.py -x` exits 0 (no regression in existing queries tests)
  </acceptance_criteria>

  <done>
    Both new tests exist and pass; full `test_queries.py` suite is green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source-code edits → committed git history | All edits are developer-authored Python; no external input is read or executed at edit time. |
| `_VALID_KINDS` frozenset → SQL layer | SQL accepts arbitrary kind strings; admission is Python-side. Adding `"builtin"` here does NOT introduce SQL-injection surface because no untrusted data flows into a SQL kind value. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-01-S | Spoofing | N/A — internal code edits | accept | No actor identity in scope; foundational edits only |
| T-49-01-T | Tampering | `_VALID_KINDS` frozenset | mitigate | frozenset is immutable; bijection test in `test_entity_templates.py` defends against accidental ADMITTED_KINDS drift. Plan 03 adds parallel tests on `queries._VALID_KINDS`. |
| T-49-01-I | Information Disclosure | ADMITTED_KINDS comment | accept | Comment is non-secret documentation; reveals only intentional exclusion |
| T-49-01-D | Denial of Service | N/A — single-line code edits | accept | No runtime cost; frozenset constant-time membership |
| T-49-01-E | Elevation of Privilege | N/A | accept | No auth / privilege surface in this plan |
</threat_model>

<verification>
After all three tasks complete, run:

```bash
uv run --package graph-io pytest packages/graph-io/tests/test_queries.py -x
uv run --package wiki-io pytest packages/wiki-io/tests/test_entity_templates.py -x
```

Both must exit 0. The full `graph-io` test suite need not be re-run by this plan — Plans 02 and 03 own that gate.

Manual sanity check: open `packages/wiki-io/src/wiki_io/entity_writer.py` and confirm the comment paragraph above `ADMITTED_KINDS` clearly mentions Phase 49 and the Builtin exclusion in one place.
</verification>

<success_criteria>
- `"builtin" in graph_io.queries._VALID_KINDS` evaluates True
- `graph_io.uri.builtin_uri("python", "pathlib") == "builtin:python/pathlib"`
- `graph_io.uri.builtin_uri("javascript", "fs") == "builtin:javascript/fs"`
- `"builtin" not in wiki_io.entity_writer.ADMITTED_KINDS` evaluates True
- ADMITTED_KINDS comment block explicitly references Phase 49 / D-16 and explains the Builtin exclusion
- `SCHEMA_VERSION` in `packages/graph-io/src/graph_io/schema.py` is still `2` (D-10)
- All existing tests in `packages/graph-io/tests/test_queries.py` and `packages/wiki-io/tests/test_entity_templates.py` continue to pass
</success_criteria>

<output>
Create `.planning/phases/49-builtin-kind-graph-io/49-01-SUMMARY.md` when done.
</output>
</content>
</invoke>