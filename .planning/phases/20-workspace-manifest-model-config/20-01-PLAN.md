---
phase: 20-workspace-manifest-model-config
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/workspace-io/src/workspace_io/manifest.py
  - packages/workspace-io/src/workspace_io/__init__.py
  - packages/workspace-io/tests/test_manifest.py
  - packages/workspace-io/tests/test_manifest_v2_roundtrip.py
  - packages/workspace-io/README.md
autonomous: true
requirements: [WMC-01, WMC-05a]
tags: [workspace-io, manifest, yaml, schema]

must_haves:
  truths:
    - "`workspace_io.manifest.read()` returns a `plugins[]` list where each plugin dict preserves any `roles[]` sub-list verbatim (no field drop, no normalization)."
    - "`workspace_io.manifest.write()` round-trips a populated `plugins[].roles[]` block such that `write` → `read` returns the same nested structure."
    - "`workspace_io.read_roles(plugin_name, workspace=...)` returns the list of role-config dicts for the named plugin, or `[]` when the plugin is absent or has no `roles:` key."
    - "Each role dict from `read_roles` carries the four fields the loader consumes downstream: `name` (str), `model_id` (str), `region` (str), `max_tokens` (int), `max_concurrency` (int). Missing optional fields are absent from the dict (not silently defaulted at the IO layer)."
    - "Existing `test_manifest.py` and `test_manifest_v2_roundtrip.py` tests still pass — no behavioral regression on the v2 manifest envelope, plugin block, or version handling."
    - "`packages/workspace-io/README.md` documents the `roles:` schema with field names, types, and a copy-pasteable example."
  artifacts:
    - path: packages/workspace-io/src/workspace_io/manifest.py
      provides: "Nested `roles[]` round-trip on the per-plugin record in `read()` and `write()`."
      contains: "roles"
    - path: packages/workspace-io/src/workspace_io/__init__.py
      provides: "Public `read_roles` accessor export."
      exports: ["read_roles"]
    - path: packages/workspace-io/tests/test_manifest_v2_roundtrip.py
      provides: "New populated-roles round-trip test."
      contains: "test_v2_roles_roundtrip"
    - path: packages/workspace-io/README.md
      provides: "Documented `roles:` schema section."
      contains: "roles:"
  key_links:
    - from: "packages/workspace-io/src/workspace_io/__init__.py"
      to: "packages/workspace-io/src/workspace_io/manifest.py::read_roles"
      via: "from-import + __all__ entry"
      pattern: "from workspace_io.manifest import .*read_roles"
    - from: "packages/workspace-io/src/workspace_io/manifest.py::write"
      to: "packages/workspace-io/src/workspace_io/manifest.py::read"
      via: "round-trip preserves nested roles[] structure"
      pattern: "roles"
---

<objective>
Extend `workspace_io.manifest` to read and write the nested `plugins[].roles[]` block, expose a `read_roles(plugin_name)` accessor on the public API, and document the schema in `packages/workspace-io/README.md`.

Purpose: Closes SC#1 of Phase 20 — the manifest must round-trip a populated `plugins[].roles[]` block with PyYAML so subsequent plans can wire `model-adapter.make_llm` against this surface. Also delivers the docs portion of SC#5.

Output: An updated `manifest.py` that no longer strips per-plugin `roles[]` during `write()`, a `read_roles()` accessor on the public API, new round-trip tests in `test_manifest_v2_roundtrip.py`, and a `roles:` schema section in `README.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-workspace-manifest-model-config/20-CONTEXT.md
@packages/workspace-io/src/workspace_io/manifest.py
@packages/workspace-io/src/workspace_io/__init__.py
@packages/workspace-io/src/workspace_io/config.py
@packages/workspace-io/tests/test_manifest.py
@packages/workspace-io/tests/test_manifest_v2_roundtrip.py
@packages/workspace-io/README.md
@packages/model-adapter/src/model_adapter/models.toml
@graph-wiki/.graph-wiki.yaml

<interfaces>
<!-- Contracts the executor needs to honor. Do not re-derive. -->

From `packages/workspace-io/src/workspace_io/manifest.py` (current):
- `read(path: Path) -> dict` — returns full manifest dict. Currently passes `plugins[]` entries through PyYAML and post-processes the optional `plugin` (singular) block. Per-plugin `roles[]` is currently NOT dropped on read (PyYAML preserves unknown nested keys), but the test in `test_manifest_v2_roundtrip.py` does not exercise this — verify with a populated fixture.
- `write(path: Path, data: dict) -> None` — currently DROPS per-plugin `roles[]` because the payload comprehension at lines 69-75 only emits `{name, installed_version, applied_version}`. THIS IS THE BUG TO FIX.

From `packages/workspace-io/src/workspace_io/__init__.py` (current):
- Current exports: `GraphWikiConfig`, `PendingUpdate`, `init`, `pending_updates`, `resolve`, `warn_if_stale`. `read_roles` to be added.

From `packages/model-adapter/src/model_adapter/models.toml`:
- Role record shape (current TOML keys, to mirror in YAML): `model_id`, `region`, `max_tokens`, `max_concurrency`. `sweep_candidates` is an eval-harness-only key — out of scope for the workspace manifest.

From `graph-wiki/.graph-wiki.yaml` (current single-role example):
```
plugins:
- name: code-wiki-agent
  installed_version: 0.1.0
  applied_version: 0.1.0
  roles:
  - name: preflight
    model_id: "us.anthropic.claude-haiku-4-5-20251001-v1:0"
    region: "us-east-1"
    max_tokens: 64
    max_concurrency: 1
```

Confirmed RoleConfig field set (locked):
- `name` (str)         — role identifier (e.g. "preflight", "librarian")
- `model_id` (str)     — Bedrock model ARN
- `region` (str)       — AWS region
- `max_tokens` (int)   — passed to ChatBedrockConverse(**kwargs)
- `max_concurrency` (int) — consumed by `load_role_config` callers (subagent runtime, eval-harness); not consumed by `ChatBedrockConverse` itself but mirrors the packaged-defaults schema and must round-trip.
No optional fields (`temperature`, `top_p`, `stop_sequences`) at this layer — `loader.py:96-102` does not read them today, so adding them would be speculative scope per Karpathy guideline §2.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add roles[] round-trip to manifest.write + read_roles accessor + tests</name>
  <files>packages/workspace-io/src/workspace_io/manifest.py, packages/workspace-io/src/workspace_io/__init__.py, packages/workspace-io/tests/test_manifest_v2_roundtrip.py, packages/workspace-io/tests/test_manifest.py</files>
  <read_first>
    - packages/workspace-io/src/workspace_io/manifest.py (full file — read once; the file is 82 lines)
    - packages/workspace-io/src/workspace_io/__init__.py (full file — 14 lines)
    - packages/workspace-io/tests/test_manifest_v2_roundtrip.py (full file — 53 lines)
    - packages/workspace-io/tests/test_manifest.py (full file — 130 lines)
    - graph-wiki/.graph-wiki.yaml (the live single-role example; confirms the exact on-disk shape)
  </read_first>
  <behavior>
    - Test 1: `test_v2_roles_roundtrip` — write a manifest with one plugin carrying a populated `roles: [{name, model_id, region, max_tokens, max_concurrency}, ...]` list (use two roles, e.g. preflight + librarian); read it back; the returned `plugins[0]["roles"]` equals the input list verbatim (same dicts, same order).
    - Test 2: `test_v2_roles_absent_round_trips_cleanly` — write a manifest with a plugin that has no `roles` key; read it back; the returned plugin dict has no `roles` key (and no `KeyError` on access via `.get("roles")`). Pre-fix behavior preserved.
    - Test 3: `test_read_roles_returns_list_for_named_plugin` — given a manifest dict with `plugins: [{name: code-wiki-agent, roles: [{name: preflight, ...}]}]`, `read_roles("code-wiki-agent", manifest_path)` returns the single-element list.
    - Test 4: `test_read_roles_returns_empty_for_missing_plugin` — calling `read_roles("does-not-exist", manifest_path)` returns `[]` (not raises).
    - Test 5: `test_read_roles_returns_empty_when_plugin_has_no_roles_key` — calling `read_roles` on a plugin entry whose dict has no `roles` key returns `[]`.
    - Test 6: `test_read_roles_returns_empty_when_manifest_missing` — calling `read_roles` against a non-existent manifest path returns `[]` (matches `read()`'s "missing file → empty dict" contract; no exception).
    - All existing tests in both files continue to pass unchanged.
  </behavior>
  <action>
    Implement nested-`roles[]` round-trip in `manifest.py`, add a `read_roles` accessor, export it on the package public API, and add the six tests above. Do NOT touch the v1-rejection path, the `plugin` (singular) block validation, or the `initialized_at` normalization — they stay as-is.

    Implementation steps:

    1. Edit `packages/workspace-io/src/workspace_io/manifest.py` `write()` (lines 63-81). The current per-plugin payload comprehension at lines 69-75 drops everything except `{name, installed_version, applied_version}`. Replace that comprehension so each emitted plugin entry preserves the `roles` key when present on the input dict, while continuing to emit `name`, `installed_version`, `applied_version` in that order. Concretely: build each plugin entry as a dict with the three existing keys, then conditionally add `"roles": p["roles"]` when the input plugin dict has a non-empty `roles` value. Empty list and absent key both result in NO `roles:` key in the output (avoids writing `roles: []` for plugins that have no role overrides).

    2. Do NOT touch `read()`. PyYAML's `safe_load` already preserves nested keys; the existing read path returns whatever YAML structure was on disk. Confirm this is true by writing Test 1 first; if it fails on the read side, the fix belongs in `write()` only (see step 1).

    3. Add a new function `read_roles(plugin_name: str, manifest_path: Path) -> list[dict]` to `manifest.py`. Implementation: call the existing `read(manifest_path)`, locate the plugin entry in `plugins` whose `name == plugin_name`, return its `roles` list when present and non-empty, else return `[]`. Handle the missing-file case by relying on `read()`'s "returns `{}`" contract (the empty dict's `.get("plugins", [])` yields `[]`, so the loop short-circuits and `read_roles` returns `[]`).

       Signature and docstring:
       ```python
       def read_roles(plugin_name: str, manifest_path: Path) -> list[dict]:
           """Return the `roles[]` list for the named plugin, or [] when absent.

           Resolution: read the manifest, find the first plugin entry whose `name`
           matches, and return its `roles` list. Returns [] when the manifest is
           missing, the plugin is absent, or the plugin entry has no `roles` key.

           This is a read-only accessor — does not mutate disk or validate role-dict
           field shape. Callers (model_adapter.loader) decide how to merge with
           packaged defaults on a per-role basis.
           """
       ```

       Per-role field validation belongs in the loader (Plan 02), NOT here. This accessor is a thin lookup. Returning a plain list of dicts (not a dataclass) keeps the IO layer stable across future role-field additions.

    4. Edit `packages/workspace-io/src/workspace_io/__init__.py`:
       - Add `from workspace_io.manifest import read_roles`
       - Append `"read_roles"` to `__all__` (keep alphabetical order: `GraphWikiConfig`, `PendingUpdate`, `init`, `pending_updates`, `read_roles`, `resolve`, `warn_if_stale`).

    5. Add the six tests to the appropriate file:
       - `test_v2_roles_roundtrip`, `test_v2_roles_absent_round_trips_cleanly` → append to `packages/workspace-io/tests/test_manifest_v2_roundtrip.py` (this file is the canonical round-trip suite).
       - `test_read_roles_returns_list_for_named_plugin`, `test_read_roles_returns_empty_for_missing_plugin`, `test_read_roles_returns_empty_when_plugin_has_no_roles_key`, `test_read_roles_returns_empty_when_manifest_missing` → append to `packages/workspace-io/tests/test_manifest.py` (this file is the canonical manifest-API suite).

       Mirror the existing test style: sync defs, `tmp_path` fixture, `_v2()` helper at top of `test_manifest.py` if you need an analogous helper for roles-populated data. Do NOT add fixtures to `conftest.py` — these tests are self-contained.

       Example fixture data for Test 1 (paste into the test body, not as a fixture):
       ```python
       {
           "version": 2,
           "initialized_at": "2026-05-19",
           "plugins": [
               {
                   "name": "code-wiki-agent",
                   "installed_version": "0.7.0",
                   "applied_version": "0.7.0",
                   "roles": [
                       {"name": "preflight", "model_id": "us.anthropic.claude-haiku-4-5-20251001-v1:0", "region": "us-east-1", "max_tokens": 64, "max_concurrency": 1},
                       {"name": "librarian", "model_id": "us.anthropic.claude-haiku-4-5-20251001-v1:0", "region": "us-east-1", "max_tokens": 2048, "max_concurrency": 5},
                   ],
               }
           ],
       }
       ```

    6. Do NOT extend `manifest.py` with role-field validation, defaulting, or schema enforcement. The IO layer is a transport; semantics live in the loader. Karpathy guideline §2 — no speculative validation.

    7. Do NOT migrate the `plugin` (singular) block, the `version`/`initialized_at` envelope, or the v1-rejection logic. Only `write()`'s per-plugin payload comprehension and the new `read_roles` accessor change.
  </action>
  <verify>
    <automated>uv run --package workspace-io pytest tests/test_manifest.py tests/test_manifest_v2_roundtrip.py -x -v</automated>
    <automated>grep -c '^def test_v2_roles_roundtrip\|^def test_v2_roles_absent_round_trips_cleanly' packages/workspace-io/tests/test_manifest_v2_roundtrip.py</automated>
    <automated>grep -c '^def test_read_roles_' packages/workspace-io/tests/test_manifest.py</automated>
    <automated>uv run --package workspace-io python -c "from workspace_io import read_roles; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '"roles"' packages/workspace-io/src/workspace_io/manifest.py | grep -v '^#'` returns at least 2 matches (one in `write` payload, one in `read_roles`).
    - `grep -n 'def read_roles' packages/workspace-io/src/workspace_io/manifest.py` returns exactly 1 match.
    - `grep -n 'read_roles' packages/workspace-io/src/workspace_io/__init__.py | grep -v '^#'` returns at least 2 matches (import + `__all__`).
    - `grep -c '^def test_v2_roles_roundtrip' packages/workspace-io/tests/test_manifest_v2_roundtrip.py` returns 1.
    - `grep -c '^def test_v2_roles_absent_round_trips_cleanly' packages/workspace-io/tests/test_manifest_v2_roundtrip.py` returns 1.
    - `grep -c '^def test_read_roles_' packages/workspace-io/tests/test_manifest.py` returns at least 4.
    - `uv run --package workspace-io pytest tests/test_manifest.py tests/test_manifest_v2_roundtrip.py -x` exits 0 with all tests passing.
    - `uv run --package workspace-io pytest -x` exits 0 (no regression in the broader workspace-io suite).
  </acceptance_criteria>
  <done>
    `manifest.write()` preserves nested `roles[]` for each plugin, `read_roles(plugin_name, manifest_path)` returns the list of role dicts (or `[]`), the public API exports it, and six new tests pin the round-trip + accessor contract.
  </done>
</task>

<task type="auto">
  <name>Task 2: Document the roles: schema in workspace-io README</name>
  <files>packages/workspace-io/README.md</files>
  <read_first>
    - packages/workspace-io/README.md (current 10-line stub)
    - packages/model-adapter/src/model_adapter/models.toml (lines 1-30 — shows the canonical role record shape that the YAML must mirror)
    - graph-wiki/.graph-wiki.yaml (live single-role example)
  </read_first>
  <action>
    Replace the current 10-line stub in `packages/workspace-io/README.md` with a documentation block that covers the `roles:` schema. Keep the existing provenance text (the "Ported from `lattice-workspace`" note and the `11-01` plan reference) at the bottom for history. The new content above it MUST include:

    1. A one-sentence purpose: "Workspace bootstrap, manifest IO (`.graph-wiki.yaml`), and config resolution for the graph-wiki ecosystem."

    2. A "Manifest schema" section that documents the full v2 envelope at a high level: `version`, `initialized_at`, `plugins[]`, and the optional `plugin` (singular) block. Reference but do not duplicate the v1-rejection / backend-overrides validation — point the reader at the source.

    3. A "Per-plugin `roles:` block" subsection that documents:
       - Purpose: per-workspace model overrides for the plugin's role tiers.
       - Field list with types: `name: str`, `model_id: str`, `region: str`, `max_tokens: int`, `max_concurrency: int`. State that these are the only fields the loader currently consumes — additions require a code change in `model_adapter.loader`.
       - Fallback behavior: roles absent from the workspace manifest fall back to the packaged `model_adapter/models.toml` per-role (not all-or-nothing). Defer the fallback details to `packages/model-adapter/` (it owns the resolution).
       - A copy-pasteable example block matching the live `graph-wiki/.graph-wiki.yaml` shape with two roles (preflight + librarian) to make the nesting unambiguous.

    4. A short "Reading roles programmatically" note pointing at `workspace_io.read_roles(plugin_name, manifest_path) -> list[dict]`.

    Do NOT document the `--config` flag, `CODE_WIKI_CONFIG`, or `wiki-config.toml` — they are being deleted in Plan 03. The README must reflect the post-deletion world.

    Match existing project README tone — concrete, terse, no marketing language.
  </action>
  <verify>
    <automated>grep -c '^## ' packages/workspace-io/README.md</automated>
    <automated>grep -n 'roles:' packages/workspace-io/README.md | grep -v '^#'</automated>
    <automated>grep -n 'read_roles' packages/workspace-io/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## ' packages/workspace-io/README.md` returns at least 2 (Manifest schema, Reading roles programmatically — or equivalent section headers).
    - `grep -n '## Per-plugin\|## .*roles' packages/workspace-io/README.md` returns at least 1 match (the roles subsection exists).
    - `grep -n 'model_id\|max_concurrency\|max_tokens' packages/workspace-io/README.md` returns at least 3 matches (the four documented fields are named).
    - `grep -c 'read_roles' packages/workspace-io/README.md` returns at least 1 match.
    - `grep -c 'wiki-config.toml\|--config\|CODE_WIKI_CONFIG' packages/workspace-io/README.md` returns 0 (no references to deleted surface area).
  </acceptance_criteria>
  <done>
    `packages/workspace-io/README.md` documents the v2 manifest envelope, the per-plugin `roles:` schema with all four fields and types, a copy-pasteable two-role example, and a pointer to `read_roles`. No references to deprecated `--config`/`CODE_WIKI_CONFIG`/`wiki-config.toml` remain.
  </done>
</task>

</tasks>

<verification>
- Both new tests in `test_manifest_v2_roundtrip.py` pass.
- All four `test_read_roles_*` tests in `test_manifest.py` pass.
- Full workspace-io suite stays green: `uv run --package workspace-io pytest -x`.
- `read_roles` is importable as `from workspace_io import read_roles`.
- README documents the schema fields and references `read_roles`.
</verification>

<success_criteria>
- SC#1 (`manifest` round-trips populated `plugins[].roles[]`): satisfied by the new `test_v2_roles_roundtrip` test and the `write()` fix.
- SC#5 (workspace-io README documents the `roles:` schema): satisfied by the README rewrite.
- `read_roles` accessor exists on the public API, ready for `model-adapter.loader` consumption in Plan 02.
- No behavioral change to v1 rejection, plugin block validation, or `initialized_at` normalization.
</success_criteria>

<output>
Create `.planning/phases/20-workspace-manifest-model-config/20-01-SUMMARY.md` when done.
</output>
</content>
</invoke>