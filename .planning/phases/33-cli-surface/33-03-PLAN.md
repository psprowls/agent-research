---
phase: 33-cli-surface
plan: 03
type: execute
wave: 1
depends_on:
  - 33-01
files_modified:
  - packages/graph-io/src/graph_io/cli/q_list_suites.py
  - packages/graph-io/src/graph_io/cli/q_describe_suite.py
  - packages/graph-io/src/graph_io/cli/q_what_tests.py
autonomous: true
requirements:
  - CLI-05
  - CLI-06
  - CLI-07
  - CLI-08
tags:
  - graph-io
  - cli
  - q-modules
  - test-suites
  - what-tests-dispatch

must_haves:
  truths:
    - "packages/graph-io/src/graph_io/cli/q_list_suites.py exists. `add_arguments` takes no args, no flags. `run` calls `queries.list_test_suites(conn)` (returns list[NodeRecord] per Phase 32 plan 32-02 task 3). Human format: one `r.name` per line (D-08), sorted by helper. JSON: `[asdict(r) for r in records]`. Empty-state human stderr: `\"No TestSuites in graph.\"` + exit 0 (D-03). Empty json: `[]`."
    - "packages/graph-io/src/graph_io/cli/q_describe_suite.py exists. `add_arguments` adds a required positional `name`. `run` calls `queries.describe_test_suite(conn, suite_name=args.name)` (returns `SuiteDescription | None`). On `None`: stderr `\"error: not found: <name>\"`, return GENERIC (D-04). On found: human emits compact key-value lines per D-10 — `suite: <name>` / `uri: <uri>` / `kind: <kind>` / `framework: <framework or '(none)'>` / `files: <file_count>`. JSON: `_json.dumps(asdict(desc), default=str)`. Note: the Phase 32 SuiteDescription is minimal — `name, uri, kind, file_count`. There is no `framework` field on the dataclass yet; if the planner finds framework data is needed for D-10 prototype completeness, source it from `attrs_json` via a small inline lookup on the same conn — but only if `describe_test_suite` doesn't already include it (verify before adding). If framework is not available without a new helper, omit the framework line entirely and document the omission in <objective>."
    - "packages/graph-io/src/graph_io/cli/q_what_tests.py exists. `add_arguments` adds a required positional `name` AND optional `--kind` with `choices=(\"package\", \"domain\")`, default None (D-02)."
    - "q_what_tests.py `run` flow when `args.kind == 'package'`: skip probe, call `queries.tests_for_package(conn, package_name=args.name)`, render, return SUCCESS."
    - "q_what_tests.py `run` flow when `args.kind == 'domain'`: skip probe, call `queries.tests_for_domain(conn, domain_name=args.name)`, render, return SUCCESS."
    - "q_what_tests.py `run` flow when `args.kind is None` (default): execute probe-both per D-01. Probe via two simple SQL queries (inline, NOT new helpers): `pkg_exists = bool(conn.execute(\"SELECT 1 FROM nodes WHERE kind='package' AND name = ? LIMIT 1\", (args.name,)).fetchone())` and `dom_exists = bool(conn.execute(\"SELECT 1 FROM nodes WHERE kind='domain' AND name = ? LIMIT 1\", (args.name,)).fetchone())`. Decision tree: (a) both true → stderr `\"error: ambiguous: '<name>' is both a Package and a Domain. Use --kind package or --kind domain.\"`, return `exit_codes.AMBIGUOUS`; (b) only pkg → dispatch to tests_for_package; (c) only domain → dispatch to tests_for_domain; (d) neither → stderr `\"error: no Package or Domain named '<name>'\"`, return `exit_codes.GENERIC`."
    - "q_what_tests.py render layer (shared between package + domain dispatch): both helpers return `list[SuiteDescription]`. Human: one `desc.name` per line (D-08). JSON: `_json.dumps([asdict(d) for d in results], default=str)`. Empty result + human: stderr `\"No TestSuites cover <kind> '<name>'.\"` where `<kind>` is `package` or `domain` (whichever path was taken — track via local variable). JSON empty: `[]`."
    - "All three modules import the canonical set: `from workspace_io.paths import graph_dir; from graph_io import exit_codes, queries, store`. q_what_tests.py uses `exit_codes.AMBIGUOUS` (added by plan 33-01, value 7 — NOT 2 per the deviation in 33-01)."
    - "q_what_tests.py probes are read-only and small (each query returns at most one row via `LIMIT 1`). No N+1 — exactly 2 probe queries before dispatch when --kind is omitted."
    - "When `--kind package` is given and the package doesn't exist, q_what_tests.py invokes `tests_for_package` which returns `[]` per Phase 32 conventions. The CLI does NOT emit a separate 'not found' error in this path — it gracefully emits the empty-state message `No TestSuites cover package '<name>'.` and exits 0. This matches list-* convention (D-03) over describe-* convention (D-04) because `cg what-tests` semantically asks 'which tests cover X', not 'tell me about X'."
  artifacts:
    - path: "packages/graph-io/src/graph_io/cli/q_list_suites.py"
      provides: "cg list-suites subcommand (CLI-05)"
      contains: "list_test_suites"
      exports:
        - add_arguments
        - run
    - path: "packages/graph-io/src/graph_io/cli/q_describe_suite.py"
      provides: "cg describe-suite subcommand (CLI-06)"
      contains: "describe_test_suite"
      exports:
        - add_arguments
        - run
    - path: "packages/graph-io/src/graph_io/cli/q_what_tests.py"
      provides: "cg what-tests subcommand with probe-both dispatch (CLI-07/CLI-08)"
      contains: "AMBIGUOUS"
      exports:
        - add_arguments
        - run
  key_links:
    - from: "q_what_tests.py"
      to: "exit_codes.AMBIGUOUS"
      via: "depends_on 33-01"
      pattern: "return exit_codes\\.AMBIGUOUS"
    - from: "q_what_tests.py probe-both"
      to: "two inline SQL LIMIT-1 queries"
      via: "decision tree before dispatch"
      pattern: "SELECT 1 FROM nodes WHERE kind='(package|domain)'"
---

<objective>
Create 3 new q_* CLI modules for the TestSuite + what-tests surface:

- `q_list_suites.py` (CLI-05) → `list_test_suites(conn)`
- `q_describe_suite.py` (CLI-06) → `describe_test_suite(conn, suite_name=...)`
- `q_what_tests.py` (CLI-07/08) → probe-both dispatch per D-01/D-02

Depends on plan 33-01 because `q_what_tests.py` uses `exit_codes.AMBIGUOUS`
which 33-01 introduces. No `main.py` registration (that's plan 33-05).

The framework field on `cg describe-suite` is conditional on whether
Phase 32's `SuiteDescription` dataclass already exposes it. If the
dataclass has only `(name, uri, kind, file_count)` (per Phase 32 plan
32-01), then `framework` is dropped from the human output. The planner
documents the choice in the verify task — do not invent a new helper or
add a field to the Phase 32 dataclass.

Output: 3 new files (`packages/graph-io/src/graph_io/cli/q_*.py`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/33-cli-surface/33-CONTEXT.md
@.planning/phases/33-cli-surface/33-01-PLAN.md
@.planning/phases/32-query-layer-extensions/32-02-PLAN.md
@.planning/phases/32-query-layer-extensions/32-03-PLAN.md
@packages/graph-io/src/graph_io/cli/q_describe_package.py
@packages/graph-io/src/graph_io/queries.py
@packages/graph-io/src/graph_io/exit_codes.py
</context>

<interfaces>
<!-- Phase 32 helpers this plan binds to -->

From graph_io.queries (post-Phase 32):
```python
def list_test_suites(conn: sqlite3.Connection) -> list[NodeRecord]
def describe_test_suite(conn: sqlite3.Connection, *, suite_name: str) -> SuiteDescription | None
# SuiteDescription(name: str, uri: str, kind: str, file_count: int)   # frozen
def tests_for_package(conn: sqlite3.Connection, *, package_name: str) -> list[SuiteDescription]
def tests_for_domain(conn: sqlite3.Connection, *, domain_name: str) -> list[SuiteDescription]
```

Phase 33 plan 33-01 adds:
```python
exit_codes.AMBIGUOUS = 7
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create q_list_suites.py and q_describe_suite.py</name>
  <read_first>
    - packages/graph-io/src/graph_io/cli/q_describe_package.py (template)
    - packages/graph-io/src/graph_io/queries.py — confirm SuiteDescription fields are `(name, uri, kind, file_count)` per Phase 32 plan 32-01. If a `framework` field exists, surface it in the human output; if not, omit (see <objective>).
    - .planning/phases/33-cli-surface/33-CONTEXT.md D-03 (list-* empty-state), D-04 (describe-* not-found), D-08 (list-* one-per-line), D-10 (describe-* compact kv)
  </read_first>
  <files>packages/graph-io/src/graph_io/cli/q_list_suites.py, packages/graph-io/src/graph_io/cli/q_describe_suite.py</files>
  <action>
    Create `q_list_suites.py` following q_describe_package.py template:
    - `add_arguments`: no-op.
    - `run`: open read-only conn, call `queries.list_test_suites(conn)`, close conn.
    - Empty + human: stderr `No TestSuites in graph.`, return SUCCESS (D-03).
    - Empty + json: stdout `[]`, no stderr.
    - Non-empty + human: one `r.name` per line.
    - Non-empty + json: `_json.dumps([dataclasses.asdict(r) for r in records], default=str)`.

    Create `q_describe_suite.py`:
    - `add_arguments`: `parser.add_argument("name")`.
    - `run`: open conn, call `queries.describe_test_suite(conn, suite_name=args.name)`, close conn.
    - None: stderr `error: not found: <name>`, return GENERIC (D-04).
    - Found + human: 4 print lines (compact kv per D-10):
      `suite:  <name>` / `uri:    <uri>` / `kind:   <kind>` / `files:  <file_count>`.
      Pad colons to width of longest label (4 chars before colon — `suite/uri/kind/files` are all ≤5 letters, so 2 spaces after colon suffices).
      If the Phase 32 SuiteDescription includes a `framework` field, add `framework: <framework or '(none)'>` line BEFORE `files:`. If not, omit silently and add a comment in the source file: `# SuiteDescription lacks framework field as of Phase 32 — re-add when helper grows it`.
    - Found + json: `_json.dumps(dataclasses.asdict(desc), default=str)`.
    - Return SUCCESS.

    Both files ~40-50 lines each.
  </action>
  <verify>
    <automated>uv run --package graph-io python -c "from graph_io.cli import q_list_suites, q_describe_suite; assert hasattr(q_list_suites, 'run') and hasattr(q_describe_suite, 'run'); import argparse; p = argparse.ArgumentParser(); q_describe_suite.add_arguments(p); ns = p.parse_args(['my-suite']); assert ns.name == 'my-suite'; print('ok')"</automated>
  </verify>
  <done>
    Both files exist, importable. argparse on describe-suite accepts a required positional.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create q_what_tests.py with probe-both dispatch (D-01/D-02)</name>
  <read_first>
    - packages/graph-io/src/graph_io/cli/q_describe_package.py (template)
    - packages/graph-io/src/graph_io/queries.py — confirm `tests_for_package` and `tests_for_domain` both return `list[SuiteDescription]` (verified above)
    - .planning/phases/33-cli-surface/33-CONTEXT.md D-01 (probe-both decision tree), D-02 (--kind flag), D-03 (empty-state)
    - .planning/phases/33-cli-surface/33-CONTEXT.md <specifics> "D-01 ambiguity message" and "D-03 empty-state message variants" → `cg what-tests <name>` empty-state wording
    - packages/graph-io/src/graph_io/exit_codes.py — confirm `AMBIGUOUS = 7` (added by 33-01)
  </read_first>
  <files>packages/graph-io/src/graph_io/cli/q_what_tests.py</files>
  <action>
    Create `q_what_tests.py`:

    ```
    "cg what-tests <name> [--kind {package,domain}]"
    ```

    - `add_arguments`:
      ```
      parser.add_argument("name")
      parser.add_argument("--kind", choices=("package", "domain"), default=None)
      ```
    - `run`:
      1. Open read-only conn (standard try/except wrappers per template).
      2. Inside try/finally that closes conn:
         - If `args.kind == "package"`: results = tests_for_package(conn, package_name=args.name); kind_label = "package".
         - Elif `args.kind == "domain"`: results = tests_for_domain(conn, domain_name=args.name); kind_label = "domain".
         - Else (probe-both per D-01):
             ```
             pkg_exists = bool(conn.execute(
                 "SELECT 1 FROM nodes WHERE kind='package' AND name = ? LIMIT 1",
                 (args.name,)
             ).fetchone())
             dom_exists = bool(conn.execute(
                 "SELECT 1 FROM nodes WHERE kind='domain' AND name = ? LIMIT 1",
                 (args.name,)
             ).fetchone())
             ```
           Decision tree:
             - both: stderr `error: ambiguous: '<name>' is both a Package and a Domain. Use --kind package or --kind domain.`, return AMBIGUOUS (7). Close conn first via the finally.
             - pkg only: results = tests_for_package(conn, package_name=args.name); kind_label = "package".
             - dom only: results = tests_for_domain(conn, domain_name=args.name); kind_label = "domain".
             - neither: stderr `error: no Package or Domain named '<name>'`, return GENERIC. Close conn first via the finally.
      3. After dispatch, render:
         - Empty results + human: stderr `No TestSuites cover <kind_label> '<name>'.`; return SUCCESS.
         - Empty results + json: stdout `[]`; no stderr; return SUCCESS.
         - Non-empty + human: one `r.name` per line.
         - Non-empty + json: `_json.dumps([dataclasses.asdict(r) for r in results], default=str)`.

    Implementation note: the ambiguous + not-found error paths exit before
    the render block. Use early-return after closing the conn (write
    explicit `conn.close(); return exit_codes.AMBIGUOUS` rather than relying
    on the try/finally — keeps control flow obvious). Suggested skeleton:

      try:
          conn = store.read_only_connect(db)
      except ...:
          ...
      try:
          if args.kind == 'package':
              results, kind_label = queries.tests_for_package(...), 'package'
          elif args.kind == 'domain':
              results, kind_label = queries.tests_for_domain(...), 'domain'
          else:
              pkg = bool(...); dom = bool(...)
              if pkg and dom:
                  print('error: ambiguous: ...', file=sys.stderr)
                  return exit_codes.AMBIGUOUS
              if not pkg and not dom:
                  print('error: no Package or Domain named ...', file=sys.stderr)
                  return exit_codes.GENERIC
              if pkg:
                  results, kind_label = queries.tests_for_package(...), 'package'
              else:
                  results, kind_label = queries.tests_for_domain(...), 'domain'
      finally:
          conn.close()
      # render results...
      return exit_codes.SUCCESS

    File is ~80 lines.
  </action>
  <verify>
    <automated>uv run --package graph-io python -c "from graph_io.cli import q_what_tests; from graph_io import exit_codes; import argparse; assert hasattr(q_what_tests, 'run'); assert exit_codes.AMBIGUOUS == 7; p = argparse.ArgumentParser(); q_what_tests.add_arguments(p); ns = p.parse_args(['my-thing', '--kind', 'domain']); assert ns.name == 'my-thing' and ns.kind == 'domain'; print('ok')"</automated>
  </verify>
  <done>
    Module exists. argparse accepts `<name>` + optional `--kind {package,domain}`. Decision tree per D-01 handles all 4 cases. Uses exit_codes.AMBIGUOUS=7.
  </done>
</task>

</tasks>

<verification>
- 3 new files exist under `packages/graph-io/src/graph_io/cli/`
- Each exports `add_arguments(parser)` and `run(args) -> int`
- q_what_tests.py probe-both flow runs exactly 2 LIMIT-1 SELECT queries when --kind is None
- q_what_tests.py exit code paths: AMBIGUOUS (7) when both kinds match, GENERIC (1) when neither, SUCCESS (0) on dispatch + render
- No N+1 SQL anywhere; helpers do the heavy lifting
</verification>

<success_criteria>
- [ ] CLI-05 → q_list_suites.py created with D-03 empty-state
- [ ] CLI-06 → q_describe_suite.py created with D-04 not-found
- [ ] CLI-07/CLI-08 → q_what_tests.py created with D-01 probe-both + D-02 --kind flag
- [ ] `exit_codes.AMBIGUOUS` is the only newly-referenced exit code (introduced by 33-01)
- [ ] Each module ≤ 80 lines, follows q_describe_package.py template
</success_criteria>

<output>
Create `.planning/phases/33-cli-surface/33-03-SUMMARY.md` when done.
</output>
