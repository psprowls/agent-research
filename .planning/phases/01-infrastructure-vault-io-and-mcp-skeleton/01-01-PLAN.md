---
phase: 01-infrastructure-vault-io-and-mcp-skeleton
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - .python-version
  - .gitignore
  - LICENSE
  - README.md
  - .pre-commit-config.yaml
  - .github/workflows/ci.yml
  - .github/workflows/eval.yml
  - CLAUDE.md
  - .planning/REQUIREMENTS.md
  - cores/vault-io/pyproject.toml
  - cores/vault-io/src/vault_io/__init__.py
  - cores/vault-io/tests/__init__.py
  - cores/model-adapter/pyproject.toml
  - cores/model-adapter/src/model_adapter/__init__.py
  - cores/model-adapter/tests/__init__.py
  - agents/code-wiki-agent/pyproject.toml
  - agents/code-wiki-agent/src/code_wiki_agent/__init__.py
  - agents/code-wiki-agent/src/code_wiki_agent/cli.py
  - agents/code-wiki-agent/src/code_wiki_mcp/__init__.py
  - agents/code-wiki-agent/tests/__init__.py
  - agents/code-wiki-agent/tests/unit/__init__.py
  - agents/code-wiki-agent/tests/unit/test_cli_help.py
  - agents/code-wiki-agent/tests/integration/__init__.py
autonomous: true
requirements: [INFRA-01, INFRA-02, INFRA-03, INFRA-04, INFRA-05, INFRA-06]

must_haves:
  truths:
    - "uv sync from a fresh clone exits 0 and produces a single repo-root uv.lock"
    - "uv run code-wiki-agent --help exits 0 and prints help text mentioning the program name"
    - "uv run --package vault-io pytest --collect-only collects only vault-io tests (no code_wiki_* test names)"
    - "ruff check . and ruff format --check . both exit 0 against a freshly synced workspace"
    - "GitHub Actions ci.yml workflow file exists at .github/workflows/ci.yml and references uv sync + ruff + per-member pytest"
    - "CLAUDE.md Workspace layout section uses 'cores/' (no 'packages/' references in the layout section)"
    - "REQUIREMENTS.md MCP-07 references 'code-wiki-mcp' (not 'code-wiki-agent-mcp')"
  artifacts:
    - path: pyproject.toml
      provides: "uv workspace root with members=['cores/*','agents/*'], no [project] table, ruff config, dev dependency group"
      contains: "[tool.uv.workspace]"
    - path: .python-version
      provides: "Python 3.11 floor pin"
      contains: "3.11"
    - path: LICENSE
      provides: "MIT license"
      contains: "MIT License"
    - path: .pre-commit-config.yaml
      provides: "Pre-commit ruff hooks (ruff + ruff-format) v0.15.12"
      contains: "astral-sh/ruff-pre-commit"
    - path: .github/workflows/ci.yml
      provides: "Push-triggered uv sync + ruff lint/format + per-member pytest"
      contains: "uv sync"
    - path: cores/vault-io/pyproject.toml
      provides: "vault-io member with python-frontmatter + tiktoken deps + per-member testpaths"
      contains: "name = \"vault-io\""
    - path: cores/model-adapter/pyproject.toml
      provides: "model-adapter member with langchain-aws + boto3 deps + per-member testpaths"
      contains: "name = \"model-adapter\""
    - path: agents/code-wiki-agent/pyproject.toml
      provides: "agent member with workspace deps on vault-io+model-adapter, [project.scripts] for code-wiki-agent + code-wiki-mcp"
      contains: "code-wiki-mcp"
    - path: agents/code-wiki-agent/src/code_wiki_agent/cli.py
      provides: "Typer app exporting `app`, --help works, version subcommand"
      contains: "typer.Typer"
    - path: agents/code-wiki-agent/tests/unit/test_cli_help.py
      provides: "Subprocess test asserting uv run code-wiki-agent --help exits 0"
      contains: "test_cli_help"
  key_links:
    - from: pyproject.toml
      to: cores/vault-io, cores/model-adapter, agents/code-wiki-agent
      via: "[tool.uv.workspace] members glob 'cores/*' and 'agents/*'"
      pattern: "members = \\[\"cores/\\*\", \"agents/\\*\"\\]"
    - from: agents/code-wiki-agent/pyproject.toml
      to: vault-io, model-adapter
      via: "[tool.uv.sources] workspace=true"
      pattern: "workspace = true"
    - from: agents/code-wiki-agent/pyproject.toml
      to: code_wiki_agent.cli:app
      via: "[project.scripts] code-wiki-agent entry point"
      pattern: "code-wiki-agent\\s*=\\s*\"code_wiki_agent.cli:app\""
---

<objective>
Bootstrap the uv workspace, scaffold the three Phase 1 members (`cores/vault-io`, `cores/model-adapter`, `agents/code-wiki-agent`), install open-source hygiene (LICENSE, README, .gitignore, ruff, pre-commit, GitHub Actions CI), get `uv run code-wiki-agent --help` working, and fix the two known doc inconsistencies (CLAUDE.md "packages/" → "cores/"; REQUIREMENTS.md MCP-07 entry-point name).

Purpose: Wave 1 is the bootstrap that unblocks Waves 2 and 3. Plans 01-02, 01-03, 01-04 cannot start until `uv sync` resolves and the three member skeletons exist. No real logic ships here — only the empty-but-correct project shell plus the Typer CLI stub that satisfies INFRA-06.

Output: A fresh-clone-ready repository where `uv sync && uv run code-wiki-agent --help` works, `uv run --package <member> pytest` is properly scoped, CI runs lint+tests on push, and the two source-of-truth docs (CLAUDE.md, REQUIREMENTS.md) are consistent with locked decisions D-01 and D-14.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-infrastructure-vault-io-and-mcp-skeleton/01-CONTEXT.md
@.planning/phases/01-infrastructure-vault-io-and-mcp-skeleton/01-RESEARCH.md
@.planning/phases/01-infrastructure-vault-io-and-mcp-skeleton/01-PATTERNS.md
@.planning/phases/01-infrastructure-vault-io-and-mcp-skeleton/01-SKELETON.md
@CLAUDE.md

<interfaces>
<!-- The cli.py public surface other plans rely on. -->

agents/code-wiki-agent/src/code_wiki_agent/cli.py exports:
  - `app: typer.Typer` — the application object referenced by the `[project.scripts]` entry point `code-wiki-agent = "code_wiki_agent.cli:app"`

agents/code-wiki-agent/pyproject.toml declares (under `[project.scripts]`):
  - `code-wiki-agent = "code_wiki_agent.cli:app"` — used by Plan 01-01 itself
  - `code-wiki-mcp   = "code_wiki_mcp.server:main"` — entry point reserved for Plan 01-04 (the module does not exist yet in this plan; the script line is pre-declared so 01-04 only adds the implementation)

cores/model-adapter/src/model_adapter/__init__.py and cores/vault-io/src/vault_io/__init__.py are intentionally empty in this plan — Plans 01-02 and 01-03 fill them.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold uv workspace root + open-source hygiene + CI</name>
  <read_first>
    - /Users/pat/Personal/lattice/pyproject.toml — exact workspace-root pattern (analog per PATTERNS.md)
    - /Users/pat/Personal/deep-agents/.planning/phases/01-infrastructure-vault-io-and-mcp-skeleton/01-RESEARCH.md §"Pattern 1: Workspace Root pyproject.toml" and §"CI and Pre-Commit Configuration"
    - /Users/pat/Personal/deep-agents/.planning/phases/01-infrastructure-vault-io-and-mcp-skeleton/01-PATTERNS.md §"pyproject.toml (workspace root, config)"
    - /Users/pat/Personal/deep-agents/CLAUDE.md (note: its "Workspace layout" section uses `packages/*` — that is the very inconsistency Task 3 fixes; do not propagate it here)
  </read_first>
  <files>
    pyproject.toml,
    .python-version,
    .gitignore,
    LICENSE,
    README.md,
    .pre-commit-config.yaml,
    .github/workflows/ci.yml,
    .github/workflows/eval.yml
  </files>
  <action>
    Create the workspace root `pyproject.toml` exactly per PATTERNS.md §"pyproject.toml (workspace root)": NO `[project]` table; `[tool.uv.workspace] members = ["cores/*", "agents/*"]`; `[dependency-groups] dev = ["pytest>=8.3", "pytest-asyncio==1.3.0", "ruff>=0.15", "pre-commit", "syrupy==5.1.0"]`; `[tool.ruff] line-length = 120` with `exclude = ["**/fixtures/**"]` (per PATTERNS.md — ruff must not lint the committed vault fixture); `[tool.ruff.lint] select = ["E", "F", "I"]`; `[tool.pytest.ini_options] addopts = "--import-mode=importlib"` with NO `testpaths` at the root.

    Create `.python-version` containing only `3.11` so `uv` selects the right interpreter.

    Create `.gitignore` covering standard Python (`__pycache__/`, `*.pyc`, `*.egg-info/`, `.pytest_cache/`, `.ruff_cache/`), `uv` (`.venv/`), env (`.env`, `.env.*`), IDE (`.vscode/`, `.idea/`), and OS (`.DS_Store`). Do NOT gitignore `uv.lock` — it is committed (INFRA-02).

    Create `LICENSE` as the standard MIT License with copyright line `Copyright (c) 2026 Patrick Sprowls` (Pat's name per gitconfig, email per CLAUDE.md userEmail).

    Create `README.md` with: project name, one-line description from PROJECT.md Core Value, quickstart (`uv sync && uv run code-wiki-agent --help`), workspace layout (cores/vault-io, cores/model-adapter, agents/code-wiki-agent), license note. Keep under 80 lines — README detail is deferred.

    Create `.pre-commit-config.yaml` using `https://github.com/astral-sh/ruff-pre-commit` at `rev: v0.15.12` with the two hooks `ruff` (args `[--fix]`) and `ruff-format`, exactly per RESEARCH.md §"Pre-Commit (Recommended: ruff only)".

    Create `.github/workflows/ci.yml` exactly per RESEARCH.md §"GitHub Actions CI": `on: [push, pull_request]`, `runs-on: ubuntu-latest`, steps `actions/checkout@v4`, `astral-sh/setup-uv@v5` pinned to `0.11.14`, then `uv sync`, `uv run ruff check .`, `uv run ruff format --check .`, then `uv run --package vault-io pytest`, `uv run --package model-adapter pytest`, `uv run --package code-wiki-agent pytest -m "not integration"`. Single Python version (3.11). Use Python 3.11 only and Linux only per RESEARCH §"CI matrix decision".

    Create `.github/workflows/eval.yml` as a STUB: `on: workflow_dispatch` (manually triggered only) with a single `echo "Eval workflow stub - implemented in Phase 4"` step. This satisfies INFRA-05's "eval suite is opt-in (separate workflow, not blocking)" requirement without committing to eval-harness work that lands in Phase 4.

    Do not run `uv sync` yet — that happens after Task 2 creates the member directories (otherwise `uv sync` fails on missing members declared by the glob).
  </action>
  <acceptance_criteria>
    - `test -f pyproject.toml && grep -q 'members = \["cores/\*", "agents/\*"\]' pyproject.toml` exits 0
    - `grep -q '\[project\]' pyproject.toml` exits 1 (NO [project] table at root)
    - `grep -q '\[tool.uv.workspace\]' pyproject.toml` exits 0
    - `grep -q 'exclude = \["\*\*\/fixtures\/\*\*"\]' pyproject.toml` exits 0
    - `cat .python-version` outputs `3.11`
    - `test -f LICENSE && grep -q 'MIT License' LICENSE` exits 0
    - `test -f .pre-commit-config.yaml && grep -q 'astral-sh/ruff-pre-commit' .pre-commit-config.yaml && grep -q 'v0.15.12' .pre-commit-config.yaml` exits 0
    - `test -f .github/workflows/ci.yml && grep -q 'uv sync' .github/workflows/ci.yml && grep -q 'ruff format --check' .github/workflows/ci.yml` exits 0
    - `test -f .github/workflows/eval.yml && grep -q 'workflow_dispatch' .github/workflows/eval.yml` exits 0
    - `test -f .gitignore && grep -q '__pycache__' .gitignore && ! grep -q '^uv.lock' .gitignore` (lock is NOT ignored)
  </acceptance_criteria>
  <verify>
    <automated>test -f pyproject.toml && test -f LICENSE && test -f .pre-commit-config.yaml && test -f .github/workflows/ci.yml && grep -q 'members = \["cores/\*", "agents/\*"\]' pyproject.toml && grep -q 'astral-sh/ruff-pre-commit' .pre-commit-config.yaml && grep -q 'uv sync' .github/workflows/ci.yml</automated>
  </verify>
  <done>
    Workspace root config + open-source hygiene files exist; ruff config excludes fixtures; CI workflow runs lint + per-member pytest with integration skipped; eval workflow is a non-blocking stub.
  </done>
</task>

<task type="auto">
  <name>Task 2: Scaffold three workspace members + CLI stub + Wave 0 cli-help test</name>
  <read_first>
    - /Users/pat/Personal/lattice/packages/lattice-wiki-core/pyproject.toml — per-member pattern (analog per PATTERNS.md)
    - /Users/pat/Personal/deep-agents/.planning/phases/01-infrastructure-vault-io-and-mcp-skeleton/01-PATTERNS.md §"cores/vault-io/pyproject.toml", §"cores/model-adapter/pyproject.toml", §"agents/code-wiki-agent/pyproject.toml", §"agents/code-wiki-agent/src/code_wiki_agent/cli.py", §"agents/code-wiki-agent/tests/unit/test_cli_help.py"
    - /Users/pat/Personal/deep-agents/.planning/phases/01-infrastructure-vault-io-and-mcp-skeleton/01-RESEARCH.md §"Pattern 2", §"Pattern 3", §"Pattern 4", §"Recommended Project Structure"
    - /Users/pat/Personal/deep-agents/CLAUDE.md §"Technology Stack" — pinned dep versions (python-frontmatter 1.1.0, tiktoken ≥0.7, langchain-aws 1.4.6, boto3 ≥1.38, mcp 1.27.1, typer 0.25.1)
  </read_first>
  <files>
    cores/vault-io/pyproject.toml,
    cores/vault-io/src/vault_io/__init__.py,
    cores/vault-io/tests/__init__.py,
    cores/model-adapter/pyproject.toml,
    cores/model-adapter/src/model_adapter/__init__.py,
    cores/model-adapter/tests/__init__.py,
    agents/code-wiki-agent/pyproject.toml,
    agents/code-wiki-agent/src/code_wiki_agent/__init__.py,
    agents/code-wiki-agent/src/code_wiki_agent/cli.py,
    agents/code-wiki-agent/src/code_wiki_mcp/__init__.py,
    agents/code-wiki-agent/tests/__init__.py,
    agents/code-wiki-agent/tests/unit/__init__.py,
    agents/code-wiki-agent/tests/unit/test_cli_help.py,
    agents/code-wiki-agent/tests/integration/__init__.py
  </files>
  <action>
    Create `cores/vault-io/pyproject.toml` exactly per PATTERNS.md §"cores/vault-io/pyproject.toml": `name = "vault-io"`, `version = "0.1.0"`, `requires-python = ">=3.11"`, `dependencies = ["python-frontmatter>=1.1", "tiktoken>=0.7"]`, build-system using `uv_build>=0.11.14,<0.12`, `[tool.pytest.ini_options] testpaths = ["tests"]` + `addopts = "--import-mode=importlib"`.

    Create `cores/model-adapter/pyproject.toml` per PATTERNS.md §"cores/model-adapter/pyproject.toml": `name = "model-adapter"`, deps `["langchain-aws>=1.4.6", "boto3>=1.38"]`, same build-system + testpaths pattern.

    Create `agents/code-wiki-agent/pyproject.toml` per PATTERNS.md §"agents/code-wiki-agent/pyproject.toml":
      - `name = "code-wiki-agent"`, `version = "0.1.0"`, `requires-python = ">=3.11"`
      - `dependencies = ["vault-io", "model-adapter", "mcp>=1.27.1", "langchain-aws>=1.4.6", "typer>=0.25.1", "pydantic>=2.0"]`
      - `[project.scripts]` declares BOTH entry points NOW (so 01-04 only adds the server implementation, not the script declaration):
        - `code-wiki-agent = "code_wiki_agent.cli:app"`
        - `code-wiki-mcp = "code_wiki_mcp.server:main"`
      - `[tool.uv.sources] vault-io = { workspace = true }` and `model-adapter = { workspace = true }`
      - `[tool.pytest.ini_options] testpaths = ["tests"]`, `addopts = "--import-mode=importlib"`, `markers = ["integration: requires real Bedrock or subprocess (skipped in CI by default)"]`

    Create `agents/code-wiki-agent/src/code_wiki_agent/cli.py` exactly per PATTERNS.md §"agents/code-wiki-agent/src/code_wiki_agent/cli.py" — Typer app with `name="code-wiki-agent"`, `no_args_is_help=True`, one `version` subcommand printing `"code-wiki-agent 0.1.0"`. Start file with `from __future__ import annotations` per the project-wide convention noted in PATTERNS.md §"Shared Patterns".

    Create empty `__init__.py` files in every package src tree and every test tree. The `code_wiki_mcp/__init__.py` is empty for now — 01-04 adds `server.py` next to it. The `model_adapter/__init__.py` and `vault_io/__init__.py` are empty — Plans 01-02 and 01-03 fill them.

    Create `agents/code-wiki-agent/tests/unit/test_cli_help.py` exactly per PATTERNS.md §"agents/code-wiki-agent/tests/unit/test_cli_help.py" — `subprocess.run(["uv", "run", "--package", "code-wiki-agent", "code-wiki-agent", "--help"], capture_output=True, text=True)`, assert `returncode == 0` and `"code-wiki-agent" in result.stdout.lower()`.

    After ALL files exist, run `uv sync` from the repo root to materialize the shared `uv.lock` and prove the three members + their deps + the workspace dependency edges resolve cleanly.
  </action>
  <acceptance_criteria>
    - `test -f cores/vault-io/pyproject.toml && test -f cores/model-adapter/pyproject.toml && test -f agents/code-wiki-agent/pyproject.toml` exits 0
    - `grep -q 'name = "vault-io"' cores/vault-io/pyproject.toml` exits 0
    - `grep -q 'name = "model-adapter"' cores/model-adapter/pyproject.toml` exits 0
    - `grep -q 'name = "code-wiki-agent"' agents/code-wiki-agent/pyproject.toml` exits 0
    - `grep -q 'code-wiki-mcp.*=.*"code_wiki_mcp.server:main"' agents/code-wiki-agent/pyproject.toml` exits 0
    - `grep -q 'code-wiki-agent.*=.*"code_wiki_agent.cli:app"' agents/code-wiki-agent/pyproject.toml` exits 0
    - `grep -q 'vault-io = { workspace = true }' agents/code-wiki-agent/pyproject.toml` exits 0
    - All three member `pyproject.toml` files contain `testpaths = ["tests"]` (verifiable: `grep -lc 'testpaths = \["tests"\]' cores/vault-io/pyproject.toml cores/model-adapter/pyproject.toml agents/code-wiki-agent/pyproject.toml` outputs 3 nonzero counts)
    - `uv sync` exits 0 and `test -f uv.lock`
    - `uv run code-wiki-agent --help` exits 0 and stdout contains "code-wiki-agent"
    - `uv run --package code-wiki-agent pytest tests/unit/test_cli_help.py -x -q` exits 0
    - `uv run --package vault-io pytest --collect-only 2>&1 | grep -v -E '(code_wiki|model_adapter)'` produces output (proving collection is scoped to vault-io; no agent tests leak in)
  </acceptance_criteria>
  <verify>
    <automated>uv sync && test -f uv.lock && uv run code-wiki-agent --help > /dev/null && uv run --package code-wiki-agent pytest tests/unit/test_cli_help.py -x -q</automated>
  </verify>
  <done>
    Three member skeletons exist with correct pyproject.toml shape; `uv sync` produces a single uv.lock; `code-wiki-agent --help` works; per-member pytest scoping confirmed; entry points for both `code-wiki-agent` and `code-wiki-mcp` are pre-declared (the latter awaits 01-04's server.py).
  </done>
</task>

<task type="auto">
  <name>Task 3: Fix CLAUDE.md packages→cores doc inconsistency and amend REQUIREMENTS.md MCP-07</name>
  <read_first>
    - /Users/pat/Personal/deep-agents/CLAUDE.md — find the "Workspace layout" section and any other instance of `packages/*` that refers to the deep-agents workspace tier (NOT references to `packages/lattice-wiki-core/` which is the upstream lattice repo — leave those alone)
    - /Users/pat/Personal/deep-agents/.planning/REQUIREMENTS.md — find MCP-07 (line ~75), which currently says `code-wiki-agent-mcp`
    - /Users/pat/Personal/deep-agents/.planning/phases/01-infrastructure-vault-io-and-mcp-skeleton/01-CONTEXT.md §"Implementation Decisions" — D-01 (cores not packages) and D-14 (entry point is `code-wiki-mcp`)
    - /Users/pat/Personal/deep-agents/.planning/phases/01-infrastructure-vault-io-and-mcp-skeleton/01-SKELETON.md §"Architectural Decisions" — both decisions are locked there
  </read_first>
  <files>
    CLAUDE.md,
    .planning/REQUIREMENTS.md
  </files>
  <action>
    Edit `CLAUDE.md` "Workspace layout" / "1. Monorepo / Packaging" section: replace any occurrence of `packages/` (when used as the deep-agents tier name) with `cores/`. Do NOT change references to upstream `/Users/pat/Personal/lattice/packages/lattice-wiki-core/` paths (those are external source-of-truth paths). Also fix any prose that says "tiered `packages/` + `agents/`" to "tiered `cores/` + `agents/`". The intent is alignment with D-01.

    Edit `.planning/REQUIREMENTS.md` line containing "**MCP-07**" — change the launch command reference from `uv run code-wiki-agent-mcp` to `uv run code-wiki-mcp`, per D-14. Keep the rest of the MCP-07 sentence intact. Add a trailing parenthetical note at the end of the bullet: `(entry point name amended per Phase 1 CONTEXT D-14)`.

    Do not touch other sections of either file.
  </action>
  <acceptance_criteria>
    - `grep -E 'packages/\*' CLAUDE.md | grep -v 'lattice/packages' | wc -l` outputs 0 (no remaining deep-agents-tier `packages/*` references)
    - `grep -c 'cores/' CLAUDE.md` outputs a value ≥ 2 (the tier reference now uses cores/)
    - `grep -q 'code-wiki-agent-mcp' .planning/REQUIREMENTS.md` exits 1 (the old name is gone)
    - `grep -q 'code-wiki-mcp' .planning/REQUIREMENTS.md` exits 0 (the new name is present)
    - `grep -q 'amended per Phase 1 CONTEXT D-14' .planning/REQUIREMENTS.md` exits 0 (provenance note present)
  </acceptance_criteria>
  <verify>
    <automated>! grep -E 'packages/\*' CLAUDE.md | grep -v 'lattice/packages' | grep -q . && ! grep -q 'code-wiki-agent-mcp' .planning/REQUIREMENTS.md && grep -q 'code-wiki-mcp' .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>
    CLAUDE.md uses cores/ for the deep-agents tier name; REQUIREMENTS.md MCP-07 references the new entry point name with a D-14 provenance note. Source-of-truth docs are now consistent with locked decisions.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → CI runner | The workspace ships an Actions workflow; nothing here writes secrets to logs |
| pre-commit hooks → repo | Hooks only read+rewrite files; no network calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-03 | Information Disclosure | repo root | mitigate | `.gitignore` covers `.env*`; no credentials are committed in `pyproject.toml`, `LICENSE`, README, or workflow files. CI uses GitHub-Actions-provided runners, no static AWS keys. |
| T-1-04 | Tampering | CI workflow | accept | `ci.yml` pins `astral-sh/setup-uv@v5` (major-version pin) — acceptable for v1; full SHA pinning is a hardening item for later. |
</threat_model>

<verification>
After all three tasks complete, the following must all hold from a fresh shell at the repo root:

```
test -f pyproject.toml && \
test -f .python-version && \
test -f LICENSE && \
test -f .pre-commit-config.yaml && \
test -f .github/workflows/ci.yml && \
test -f .github/workflows/eval.yml && \
test -f cores/vault-io/pyproject.toml && \
test -f cores/model-adapter/pyproject.toml && \
test -f agents/code-wiki-agent/pyproject.toml && \
test -f agents/code-wiki-agent/src/code_wiki_agent/cli.py && \
uv sync && \
test -f uv.lock && \
uv run code-wiki-agent --help && \
uv run --package code-wiki-agent pytest tests/unit/test_cli_help.py -x -q && \
! grep -q 'code-wiki-agent-mcp' .planning/REQUIREMENTS.md && \
grep -q 'code-wiki-mcp' .planning/REQUIREMENTS.md
```
</verification>

<success_criteria>
- INFRA-01: workspace root pyproject declares `members = ["cores/*", "agents/*"]` and has no `[project]` table
- INFRA-02: `uv sync` produces a single `uv.lock` at the repo root
- INFRA-03: each member has its own `testpaths = ["tests"]`; `uv run --package vault-io pytest --collect-only` does NOT collect agent tests
- INFRA-04: MIT LICENSE, README, .gitignore, and pre-commit config (ruff + ruff-format) all present at repo root
- INFRA-05: `.github/workflows/ci.yml` runs `uv sync` + ruff lint/format + per-member pytest with integration skipped; `.github/workflows/eval.yml` exists as a non-blocking opt-in stub
- INFRA-06: `uv run code-wiki-agent --help` exits 0 from a fresh `uv sync`
- Doc consistency: CLAUDE.md uses `cores/` for the deep-agents tier; REQUIREMENTS.md MCP-07 references `code-wiki-mcp`
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-vault-io-and-mcp-skeleton/01-01-SUMMARY.md` per the template at `$HOME/.claude/get-shit-done/templates/summary.md`. The summary must list files created, the resolved tree of `uv.lock` member entries, the `code-wiki-agent --help` stdout snippet, and confirmation that the two doc edits landed.
</output>
