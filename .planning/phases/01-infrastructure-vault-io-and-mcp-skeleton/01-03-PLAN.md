---
phase: 01-infrastructure-vault-io-and-mcp-skeleton
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - cores/vault-io/src/vault_io/__init__.py
  - cores/vault-io/src/vault_io/_workspace.py
  - cores/vault-io/src/vault_io/layout_io.py
  - cores/vault-io/src/vault_io/update_tokens.py
  - cores/vault-io/src/vault_io/detect_containers.py
  - cores/vault-io/src/vault_io/append_log.py
  - cores/vault-io/src/vault_io/update_index.py
  - cores/vault-io/src/vault_io/graph_analyzer.py
  - cores/vault-io/src/vault_io/scan_monorepo.py
  - cores/vault-io/src/vault_io/init_vault.py
  - cores/vault-io/src/vault_io/lint/__init__.py
  - cores/vault-io/src/vault_io/lint/common.py
  - cores/vault-io/tests/conftest.py
  - cores/vault-io/tests/fixtures/round-trip-vault/
  - cores/vault-io/tests/test_round_trip.py
  - cores/vault-io/tests/test_truncated_frontmatter.py
  - cores/vault-io/tests/test_wikilink_predicate.py
  - cores/vault-io/tests/test_layout_io_smoke.py
  - cores/vault-io/tests/test_ports_importable.py
autonomous: true
requirements: [VAULT-01, VAULT-02, VAULT-03, VAULT-04, VAULT-05, VAULT-06, VAULT-07]

must_haves:
  truths:
    - "Reading every fixture vault page with python-frontmatter and re-writing it via update_tokens.update_vault produces an empty git diff against the original on the second pass"
    - "A page whose frontmatter is missing its closing '---' returns ('skipped', 0) from update_page, leaves the file bytes unchanged, and emits 'no closing frontmatter fence' to stderr"
    - "_is_placeholder_target('wiki/...') returns True; _is_placeholder_target('wiki/<container>/<name>') returns True; _is_placeholder_target('wiki/foo/bar') returns False"
    - "layout_io.write_layout writes a deterministic byte string given the same input dict (round-trip on layout block alone is byte-stable)"
    - "All ported modules import cleanly: from vault_io import layout_io, update_tokens, detect_containers, append_log, update_index, graph_analyzer, scan_monorepo, init_vault, lint.common, _workspace"
    - "No source file in cores/vault-io/src/vault_io/**/*.py imports lattice_wiki_core or lattice_workspace"
    - "frontmatter.dumps() does not appear in any write path under cores/vault-io/src/"
  artifacts:
    - path: cores/vault-io/src/vault_io/layout_io.py
      provides: "Hand-rolled YAML emitter for the layout block (VAULT-02); stdlib-only"
      contains: "def write_layout"
    - path: cores/vault-io/src/vault_io/update_tokens.py
      provides: "Token counter port with truncated-frontmatter guard (VAULT-05) and raw-string write path (VAULT-03)"
      contains: "no closing frontmatter fence"
    - path: cores/vault-io/src/vault_io/lint/common.py
      provides: "_is_placeholder_target predicate (VAULT-06) + WIKILINK_RE"
      contains: "_is_placeholder_target"
    - path: cores/vault-io/src/vault_io/_workspace.py
      provides: "Adapted resolve_wiki_and_repo — explicit Path arg or CODE_WIKI_REAL_VAULT_PATH; raises on misconfig"
      contains: "CODE_WIKI_REAL_VAULT_PATH"
    - path: cores/vault-io/tests/fixtures/round-trip-vault/
      provides: "Committed snapshot of real lattice-wiki vault (~148 pages) for the round-trip gate"
      contains: "directory exists and contains at least 100 .md files"
    - path: cores/vault-io/tests/test_round_trip.py
      provides: "VAULT-04 gate: empty git diff after round-trip on every fixture page"
      contains: "git diff"
    - path: cores/vault-io/tests/test_truncated_frontmatter.py
      provides: "VAULT-05 test: skipped + stderr warning + unchanged bytes"
      contains: "test_update_page_skips_truncated_frontmatter"
    - path: cores/vault-io/tests/test_wikilink_predicate.py
      provides: "VAULT-06 test: placeholder detection (ellipsis, angle brackets) + rejection of normal links"
      contains: "_is_placeholder_target"
    - path: cores/vault-io/tests/test_ports_importable.py
      provides: "Smoke test that every ported module imports cleanly (VAULT-07 surface check)"
      contains: "import vault_io"
  key_links:
    - from: cores/vault-io/src/vault_io/update_tokens.py
      to: vault_io._workspace.resolve_wiki_and_repo
      via: "import after import surgery from lattice_wiki_core._workspace"
      pattern: "from vault_io._workspace import resolve_wiki_and_repo"
    - from: cores/vault-io/src/vault_io/scan_monorepo.py
      to: vault_io.layout_io.read_layout
      via: "import after import surgery"
      pattern: "from vault_io.layout_io import read_layout"
    - from: cores/vault-io/tests/test_round_trip.py
      to: vault_io.update_tokens.update_vault
      via: "import + invocation against fixture vault copy"
      pattern: "from vault_io.update_tokens import"
    - from: cores/vault-io/tests/test_round_trip.py
      to: cores/vault-io/tests/fixtures/round-trip-vault/
      via: "shutil.copytree then git diff --no-index"
      pattern: "git.*diff.*no-index"
---

<objective>
Land the vault IO round-trip golden gate — the single most important Phase 1 deliverable per STATE.md. Port the lattice-wiki-core modules into `cores/vault-io/src/vault_io/`, install the truncated-frontmatter guard, port the wikilink placeholder predicate, and prove with a real-vault fixture that reading-then-writing every page produces byte-identical output (empty git diff).

Purpose: STATE.md decisions §"Vault round-trip golden test gates all write-path code" and §"python-frontmatter read-only; all writes via ported layout_io.py emitter" make this the gate for ALL future vault writes. If this round-trip drifts even by quote style or key order, every Phase 5 lint/init/scan command will silently corrupt vaults.

Output: A complete `cores/vault-io` package with all 11 ported modules (verbatim copies + import surgery + two adapted ports), the wikilink placeholder predicate in `lint/common.py`, the committed real-vault fixture, and the three blocking tests (round-trip, truncated-frontmatter, wikilink-predicate) all green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-infrastructure-vault-io-and-mcp-skeleton/01-CONTEXT.md
@.planning/phases/01-infrastructure-vault-io-and-mcp-skeleton/01-RESEARCH.md
@.planning/phases/01-infrastructure-vault-io-and-mcp-skeleton/01-PATTERNS.md
@.planning/phases/01-infrastructure-vault-io-and-mcp-skeleton/01-SKELETON.md
@.planning/phases/01-infrastructure-vault-io-and-mcp-skeleton/01-01-PLAN.md
@/Users/pat/Personal/lattice/packages/lattice-wiki-core/src/lattice_wiki_core/layout_io.py
@/Users/pat/Personal/lattice/packages/lattice-wiki-core/src/lattice_wiki_core/update_tokens.py
@/Users/pat/Personal/lattice/packages/lattice-wiki-core/src/lattice_wiki_core/_workspace.py
@/Users/pat/Personal/lattice/packages/lattice-wiki-core/src/lattice_wiki_core/detect_containers.py

<interfaces>
<!-- Public surface this plan creates, consumed by Phase 3 (query reads index.md + pages) and Phase 5 (init/scan/ingest/lint/log all route through these). -->

cores/vault-io/src/vault_io/layout_io.py exports:
  - `read_layout(schema_path: Path) -> Optional[dict]`
  - `write_layout(schema_path: Path, layout: dict) -> None`

cores/vault-io/src/vault_io/update_tokens.py exports:
  - `update_page(path: Path, enc, *, dry_run: bool=False) -> tuple[str, int]` — returns ("skipped"|"updated"|"unchanged", token_count)
  - `update_vault(vault_path: Path) -> dict` — walks vault, calls update_page; returns {"updated": [...], "skipped": [...], "unchanged": [...]}

cores/vault-io/src/vault_io/detect_containers.py exports:
  - `detect(repo_root: Path) -> list[dict]` — returns sorted classifications

cores/vault-io/src/vault_io/_workspace.py exports:
  - `resolve_wiki_and_repo(vault_path: Path | None = None) -> tuple[Path, Path | None]` — raises RuntimeError on misconfig

cores/vault-io/src/vault_io/lint/common.py exports:
  - `_is_placeholder_target(target: str) -> bool`
  - `WIKILINK_RE: re.Pattern`

Other ports (Phase 5 will use these; Phase 1 only confirms they import + their public functions are callable on a sample input):
  - `append_log`, `update_index`, `graph_analyzer`, `scan_monorepo`, `init_vault`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit real-vault fixture and write Wave-0 conftest + the three blocking tests</name>
  <read_first>
    - /Users/pat/Personal/lattice/lattice/wiki/ — the real vault (148 pages); confirm directory exists and is readable
    - /Users/pat/Personal/deep-agents/.planning/phases/01-infrastructure-vault-io-and-mcp-skeleton/01-PATTERNS.md §"cores/vault-io/tests/conftest.py", §"cores/vault-io/tests/test_round_trip.py", §"cores/vault-io/tests/test_truncated_frontmatter.py", §"cores/vault-io/tests/test_wikilink_predicate.py"
    - /Users/pat/Personal/deep-agents/.planning/phases/01-infrastructure-vault-io-and-mcp-skeleton/01-CONTEXT.md §"Round-Trip Fixture Strategy" — D-05 (commit as-is, no sanitization), D-06 (env var override), D-07 (truncated-frontmatter gets dedicated test)
    - /Users/pat/Personal/lattice/packages/lattice-wiki-core/tests/test_lint_wikilink_placeholders.py — exact test analog for wikilink predicate
    - /Users/pat/Personal/lattice/packages/lattice-wiki-core/tests/test_update_tokens.py lines 127-137 — exact truncated-frontmatter analog
  </read_first>
  <files>
    cores/vault-io/tests/conftest.py,
    cores/vault-io/tests/fixtures/round-trip-vault/,
    cores/vault-io/tests/test_round_trip.py,
    cores/vault-io/tests/test_truncated_frontmatter.py,
    cores/vault-io/tests/test_wikilink_predicate.py
  </files>
  <action>
    Copy the real vault to the fixture path: `mkdir -p cores/vault-io/tests/fixtures && cp -r /Users/pat/Personal/lattice/lattice/wiki cores/vault-io/tests/fixtures/round-trip-vault` (D-05; no sanitization). Verify the copy contains at least 100 `.md` files via `find cores/vault-io/tests/fixtures/round-trip-vault -name '*.md' | wc -l` — must be ≥ 100. Stage the fixture into git via `git add cores/vault-io/tests/fixtures/round-trip-vault`.

    Confirm `.gitignore` does NOT exclude the fixture (Task 1 of Plan 01-01 already validated this).

    Create `cores/vault-io/tests/conftest.py` per PATTERNS.md §"cores/vault-io/tests/conftest.py":
      - `tmp_repo(tmp_path) -> Path` fixture returning `tmp_path`
      - module-level helper `write_file(path, content)` (not a fixture)
      - `vault_path(tmp_path) -> Path` fixture returning `tmp_path / "wiki"` with the dir created
      - `round_trip_vault() -> Path` fixture — returns `Path(__file__).parent / "fixtures" / "round-trip-vault"`, unless `CODE_WIKI_REAL_VAULT_PATH` env var is set in which case return that path (D-06)
      - `from __future__ import annotations` first

    Create `cores/vault-io/tests/test_round_trip.py` per PATTERNS.md §"cores/vault-io/tests/test_round_trip.py":
      - Import `shutil`, `subprocess`, `Path`, `pytest`
      - Test function `test_round_trip_all_fixture_pages(round_trip_vault, tmp_path)`:
        1. `copy = tmp_path / "vault"`; `shutil.copytree(round_trip_vault, copy)`
        2. `from vault_io.update_tokens import update_vault`
        3. First pass: `update_vault(copy)` — stamps tokens (mutates files; expected on first pass since fixture may have stale or absent `tokens:` keys)
        4. Second pass: `result = update_vault(copy)` — must produce no further updates: `assert result["updated"] == []`
        5. After second pass: run `git diff --no-index <original-fixture> <copy>`; on second pass the diff should be empty IF the first pass produced stable output. The KEY assertion is that the SECOND `update_vault` call leaves the working copy byte-identical to itself at start of the call — meaning the function is idempotent.
        6. Idempotency assertion (strongest test): hash every file in `copy` before the second pass and after; both hashes must match.
      - This is the VAULT-04 gate per STATE.md.

    Create `cores/vault-io/tests/test_truncated_frontmatter.py` per PATTERNS.md §"cores/vault-io/tests/test_truncated_frontmatter.py". Two test functions:
      1. `test_update_page_skips_truncated_frontmatter(tmp_path)` — write `"---\ntitle: No closing fence\ncategory: concept\n"` to a temp file; call `update_page(path, enc, dry_run=False)`; assert `(status, count) == ("skipped", 0)` and the file bytes equal what was written.
      2. `test_truncated_frontmatter_emits_stderr_warning(tmp_path, capsys)` — same setup; after calling `update_page`, assert `capsys.readouterr().err` contains `"no closing frontmatter fence"` and the page name.

    Create `cores/vault-io/tests/test_wikilink_predicate.py` per PATTERNS.md §"cores/vault-io/tests/test_wikilink_predicate.py" — copy the four test functions from the lattice analog (`test_lint_wikilink_placeholders.py`) verbatim, only swapping the import to `from vault_io.lint.common import _is_placeholder_target`. Four cases:
      - ellipsis returns True (e.g., `"wiki/..."`)
      - angle brackets return True (e.g., `"work/<slug>"`)
      - normal wikilink returns False (e.g., `"wiki/packages/foo/foo"`)
      - empty/simple targets return False

    Note: These tests will FAIL at this point because Task 2 has not ported the modules yet. That is correct Wave 0 / RED-state behavior — Task 2 turns them GREEN.
  </action>
  <acceptance_criteria>
    - `test -d cores/vault-io/tests/fixtures/round-trip-vault` exits 0
    - `find cores/vault-io/tests/fixtures/round-trip-vault -name '*.md' | wc -l` outputs a value ≥ 100
    - `test -f cores/vault-io/tests/conftest.py && grep -q 'round_trip_vault' cores/vault-io/tests/conftest.py && grep -q 'CODE_WIKI_REAL_VAULT_PATH' cores/vault-io/tests/conftest.py` exits 0
    - `test -f cores/vault-io/tests/test_round_trip.py && grep -q 'git diff' cores/vault-io/tests/test_round_trip.py` exits 0
    - `test -f cores/vault-io/tests/test_truncated_frontmatter.py && grep -q 'test_update_page_skips_truncated_frontmatter' cores/vault-io/tests/test_truncated_frontmatter.py && grep -q 'test_truncated_frontmatter_emits_stderr_warning' cores/vault-io/tests/test_truncated_frontmatter.py` exits 0
    - `test -f cores/vault-io/tests/test_wikilink_predicate.py && grep -q '_is_placeholder_target' cores/vault-io/tests/test_wikilink_predicate.py` exits 0
    - Tests collect (even though they will fail until Task 2): `uv run --package vault-io pytest --collect-only -q` exits 0 and lists `test_round_trip`, `test_truncated_frontmatter`, `test_wikilink_predicate`
  </acceptance_criteria>
  <verify>
    <automated>test -d cores/vault-io/tests/fixtures/round-trip-vault && [ "$(find cores/vault-io/tests/fixtures/round-trip-vault -name '*.md' | wc -l)" -ge 100 ] && uv run --package vault-io pytest --collect-only -q | grep -q test_round_trip && uv run --package vault-io pytest --collect-only -q | grep -q test_truncated_frontmatter && uv run --package vault-io pytest --collect-only -q | grep -q test_wikilink_predicate</automated>
  </verify>
  <done>
    Real-vault fixture committed (≥100 pages); conftest + three blocking tests exist and collect; tests are red until Task 2 (expected).
  </done>
</task>

<task type="auto">
  <name>Task 2: Port all vault-io modules with import surgery + adapt _workspace.py and init_vault.py</name>
  <read_first>
    - /Users/pat/Personal/lattice/packages/lattice-wiki-core/src/lattice_wiki_core/layout_io.py — verbatim port source
    - /Users/pat/Personal/lattice/packages/lattice-wiki-core/src/lattice_wiki_core/update_tokens.py — verbatim port source; lines 88-92 contain the truncated-frontmatter guard, lines 108-128 contain the raw-string write pattern
    - /Users/pat/Personal/lattice/packages/lattice-wiki-core/src/lattice_wiki_core/_workspace.py — original (will be ADAPTED, not verbatim, per PATTERNS.md §"cores/vault-io/src/vault_io/_workspace.py")
    - /Users/pat/Personal/lattice/packages/lattice-wiki-core/src/lattice_wiki_core/init_vault.py — original (ADAPTED, lattice_workspace.init stub removed)
    - /Users/pat/Personal/lattice/packages/lattice-wiki-core/src/lattice_wiki_core/detect_containers.py — verbatim port
    - /Users/pat/Personal/lattice/packages/lattice-wiki-core/src/lattice_wiki_core/append_log.py — verbatim port
    - /Users/pat/Personal/lattice/packages/lattice-wiki-core/src/lattice_wiki_core/update_index.py — verbatim port
    - /Users/pat/Personal/lattice/packages/lattice-wiki-core/src/lattice_wiki_core/graph_analyzer.py — verbatim port
    - /Users/pat/Personal/lattice/packages/lattice-wiki-core/src/lattice_wiki_core/scan_monorepo.py — verbatim port
    - /Users/pat/Personal/lattice/packages/lattice-wiki-core/src/lattice_wiki_core/lint/common.py — verbatim port; contains WIKILINK_RE
    - /Users/pat/Personal/lattice/packages/lattice-wiki-core/src/lattice_wiki_core/lint_wiki.py — read ONLY to extract `_is_placeholder_target` and confirm it is currently in lint_wiki.py (the predicate lives there in the source; we relocate it to `lint/common.py` per PATTERNS.md §"cores/vault-io/src/vault_io/lint/common.py")
    - /Users/pat/Personal/deep-agents/.planning/phases/01-infrastructure-vault-io-and-mcp-skeleton/01-PATTERNS.md §"Import Surgery Rule (all verbatim-port files)" — the canonical rule for what to change on every port
    - /Users/pat/Personal/deep-agents/.planning/phases/01-infrastructure-vault-io-and-mcp-skeleton/01-PATTERNS.md §"cores/vault-io/src/vault_io/_workspace.py" — full adapted source
  </read_first>
  <files>
    cores/vault-io/src/vault_io/__init__.py,
    cores/vault-io/src/vault_io/_workspace.py,
    cores/vault-io/src/vault_io/layout_io.py,
    cores/vault-io/src/vault_io/update_tokens.py,
    cores/vault-io/src/vault_io/detect_containers.py,
    cores/vault-io/src/vault_io/append_log.py,
    cores/vault-io/src/vault_io/update_index.py,
    cores/vault-io/src/vault_io/graph_analyzer.py,
    cores/vault-io/src/vault_io/scan_monorepo.py,
    cores/vault-io/src/vault_io/init_vault.py,
    cores/vault-io/src/vault_io/lint/__init__.py,
    cores/vault-io/src/vault_io/lint/common.py
  </files>
  <action>
    **Port `layout_io.py` VERBATIM**: copy `lattice_wiki_core/layout_io.py` → `vault_io/layout_io.py`. Change ONLY the module docstring's first line from "lattice-wiki layout block" to "vault-io layout block". Preserve all sentinel strings (`<!-- lattice-wiki:layout:start -->`) verbatim — existing vault files use them. No other edits. Stdlib-only (no PyYAML). [PATTERNS.md §"cores/vault-io/src/vault_io/layout_io.py"]

    **Adapt `_workspace.py`** per PATTERNS.md §"cores/vault-io/src/vault_io/_workspace.py" — do NOT copy verbatim. Replace the entire implementation with the adapted version: `resolve_wiki_and_repo(vault_path: Path | None = None) -> tuple[Path, Path | None]` that uses (1) the arg if provided, (2) `CODE_WIKI_REAL_VAULT_PATH` env var, (3) raises `RuntimeError` with the actionable message specified in PATTERNS.md. No `lattice_workspace` import. No silent-fallback Path. [Rationale: D-09 actionable-error philosophy.]

    **Port `update_tokens.py` VERBATIM** with import surgery: copy source → `vault_io/update_tokens.py`. Apply imports surgery per PATTERNS.md §"Import Surgery Rule":
      - Delete: `from lattice_wiki_core._version_check import check_for_updates`
      - Replace: `from lattice_wiki_core._workspace import resolve_wiki_and_repo` → `from vault_io._workspace import resolve_wiki_and_repo`
      - Delete any `check_for_updates(...)` call sites (typically in `main()`)
      - Keep the truncated-frontmatter guard at lines 88-92 EXACTLY as source: `parts = raw.split("---", 2); if len(parts) < 3: print(f"[warn] skipping {path}: no closing frontmatter fence", file=sys.stderr); return ("skipped", 0)`. This is the VAULT-05 behavior.
      - Keep the raw-string write pattern at lines 108-128 EXACTLY — never call `frontmatter.dumps()`. This is the VAULT-03 behavior.
      - Confirm the public functions `update_page(path, enc, *, dry_run=False) -> (str, int)` and `update_vault(vault_path) -> dict` are present with their original signatures.

    **Port `detect_containers.py`, `append_log.py`, `update_index.py`, `graph_analyzer.py`, `scan_monorepo.py` VERBATIM** with import surgery per PATTERNS.md §"Import Surgery Rule":
      - Replace all `from lattice_wiki_core.X import Y` → `from vault_io.X import Y`
      - Delete `from lattice_wiki_core._version_check import check_for_updates` lines and all call sites
      - For `scan_monorepo.py`: ensure `from vault_io.layout_io import read_layout` is the replacement
      - Behavior must be byte-identical to source

    **Adapt `init_vault.py`** per PATTERNS.md §"cores/vault-io/src/vault_io/init_vault.py":
      - Copy source
      - Delete `from lattice_workspace.init import init as workspace_init` and its call site in `main()`; replace the call site with the comment `# TODO Phase 5: workspace init (lattice-workspace equivalent)`
      - Replace `from lattice_wiki_core.*` imports with `from vault_io.*`
      - Keep `FIXED_VAULT_DIRS` constant, template-copying logic, `write_layout` invocation — these are the parts Phase 5 init command will glue to

    **Port `lint/common.py`** per PATTERNS.md §"cores/vault-io/src/vault_io/lint/common.py":
      - Create `cores/vault-io/src/vault_io/lint/__init__.py` as empty file
      - Copy the source `lint/common.py` (which contains `WIKILINK_RE`)
      - Add `_is_placeholder_target(target: str) -> bool` per PATTERNS.md (relocated from `lint_wiki.py` — the function body is `return "..." in target or "<" in target or ">" in target`)
      - Use a `from __future__ import annotations` first line

    **Wire `cores/vault-io/src/vault_io/__init__.py`** to re-export key entry points:
      - `from vault_io.layout_io import read_layout, write_layout`
      - `from vault_io._workspace import resolve_wiki_and_repo`
      - `__all__ = ["read_layout", "write_layout", "resolve_wiki_and_repo"]`
      - Do NOT eagerly re-export update_tokens / scan_monorepo functions — keep them addressable as `vault_io.update_tokens.update_page` to match the source's namespace shape; consumers in tests use the explicit module paths.

    After all ports: verify no `lattice_wiki_core` or `lattice_workspace` import remains anywhere in `cores/vault-io/src/`. Verify no `frontmatter.dumps(` appears in any write path under `cores/vault-io/src/`.

    Run the three Wave-0 tests created in Task 1 — they must turn GREEN now:
      - `uv run --package vault-io pytest tests/test_wikilink_predicate.py -x -q`
      - `uv run --package vault-io pytest tests/test_truncated_frontmatter.py -x -q`
      - `uv run --package vault-io pytest tests/test_round_trip.py -x -q` — this is the big one; expect it may take 10-30 seconds on 148 pages.

    If `test_round_trip.py` fails with cosmetic diff (quotes, key order), DO NOT change the test — the failure means the port did not preserve byte-identical behavior. Most likely cause: an accidental `frontmatter.dumps()` call slipped in, or a YAML serialization library got involved somewhere. Trace and fix the port.
  </action>
  <acceptance_criteria>
    - `! grep -rn 'lattice_wiki_core\|lattice_workspace' cores/vault-io/src/` produces no matches (import surgery complete)
    - `! grep -rn 'frontmatter\.dumps' cores/vault-io/src/` produces no matches (VAULT-03 invariant: no PyYAML round-trip in write paths)
    - `test -f cores/vault-io/src/vault_io/layout_io.py && grep -q 'def write_layout' cores/vault-io/src/vault_io/layout_io.py && grep -q '<!-- lattice-wiki:layout:start -->' cores/vault-io/src/vault_io/layout_io.py` exits 0
    - `test -f cores/vault-io/src/vault_io/update_tokens.py && grep -q 'no closing frontmatter fence' cores/vault-io/src/vault_io/update_tokens.py && grep -q 'raw.split("---", 2)' cores/vault-io/src/vault_io/update_tokens.py` exits 0
    - `test -f cores/vault-io/src/vault_io/_workspace.py && grep -q 'CODE_WIKI_REAL_VAULT_PATH' cores/vault-io/src/vault_io/_workspace.py && grep -q 'raise RuntimeError' cores/vault-io/src/vault_io/_workspace.py` exits 0
    - `test -f cores/vault-io/src/vault_io/lint/common.py && grep -q 'def _is_placeholder_target' cores/vault-io/src/vault_io/lint/common.py && grep -q 'WIKILINK_RE' cores/vault-io/src/vault_io/lint/common.py` exits 0
    - All ported modules listed in files_modified exist as files
    - `uv run --package vault-io pytest tests/test_wikilink_predicate.py tests/test_truncated_frontmatter.py -x -q` exits 0
    - **VAULT-04 round-trip gate**: `uv run --package vault-io pytest tests/test_round_trip.py -x -q` exits 0 — this is the most important assertion in Phase 1.
  </acceptance_criteria>
  <verify>
    <automated>! grep -rn 'lattice_wiki_core\|lattice_workspace' cores/vault-io/src/ && ! grep -rn 'frontmatter\.dumps' cores/vault-io/src/ && uv run --package vault-io pytest tests/test_round_trip.py tests/test_truncated_frontmatter.py tests/test_wikilink_predicate.py -x -q</automated>
  </verify>
  <done>
    All 11 ported modules in place with import surgery clean; the round-trip gate is GREEN on the committed fixture; truncated-frontmatter test green; wikilink predicate test green; no lattice imports leak; no `frontmatter.dumps()` in any write path.
  </done>
</task>

<task type="auto">
  <name>Task 3: Layout-IO byte-stability test + port-importability smoke + VAULT-07 surface coverage</name>
  <read_first>
    - cores/vault-io/src/vault_io/layout_io.py (created in Task 2)
    - /Users/pat/Personal/lattice/packages/lattice-wiki-core/tests/test_layout_io.py (if exists) — analog for layout-io tests
    - /Users/pat/Personal/deep-agents/.planning/phases/01-infrastructure-vault-io-and-mcp-skeleton/01-RESEARCH.md §"Layout Block (CLAUDE.md / AGENTS.md)" — confirms layout_io is deterministic given same input
    - /Users/pat/Personal/deep-agents/.planning/phases/01-infrastructure-vault-io-and-mcp-skeleton/01-VALIDATION.md — full per-task verification map
  </read_first>
  <files>
    cores/vault-io/tests/test_layout_io_smoke.py,
    cores/vault-io/tests/test_ports_importable.py
  </files>
  <action>
    Create `cores/vault-io/tests/test_layout_io_smoke.py`:
      - One test: `test_write_layout_is_byte_stable(tmp_path)`:
        - Build a layout dict matching the schema in RESEARCH §"Layout Block": `{"version": 1, "detected_at": "2026-05-13", "repo_root": "..", "containers": [{"source": "apps", "vault_dir": "apps", "classification": "app", "children_count": 3}]}`
        - Write a temp schema file containing some preamble + `<!-- lattice-wiki:layout:start --> ... <!-- lattice-wiki:layout:end -->` block
        - Call `write_layout(path, layout)`; read result bytes
        - Call `write_layout(path, layout)` AGAIN (idempotency); read result bytes
        - Assert the two byte sequences are identical
        - Assert `read_layout(path)` round-trips: returns a dict that, when passed through `_emit_yaml` (use the internal helper or just re-write and re-read), produces the same bytes
      - One test: `test_write_layout_replaces_existing_block(tmp_path)`:
        - Write a file with a layout block; call `write_layout` with a different layout; read the file
        - Assert the layout block content changed; the preamble outside the sentinels is unchanged
      - One test: `test_write_layout_handles_null_vault_dir(tmp_path)`:
        - Layout dict with `vault_dir: None`; assert the emitted block contains `vault_dir: null` (per RESEARCH §"Edge Cases Confirmed in Source")

    Create `cores/vault-io/tests/test_ports_importable.py` — the VAULT-07 surface check:
      - One test function `test_all_ports_importable` that imports every ported module:
        ```
        from vault_io import layout_io, _workspace
        from vault_io.update_tokens import update_page, update_vault
        from vault_io.detect_containers import detect
        from vault_io.append_log import _error  # or whatever the public surface is
        from vault_io.update_index import ...  # smoke import only
        from vault_io.graph_analyzer import ...
        from vault_io.scan_monorepo import ...
        from vault_io.init_vault import FIXED_VAULT_DIRS  # or the constant defined there
        from vault_io.lint.common import _is_placeholder_target, WIKILINK_RE
        ```
        For each, assert the imported name is callable/non-None.
      - One test function `test_detect_containers_smoke(tmp_path)` — create a tmp repo with a `package.json` and a `pyproject.toml`; call `detect(tmp_path)`; assert at least one container is returned with a `source` key in the expected vocabulary. (This proves `detect_containers.py` did not break during import surgery; cheap to write.)
      - One test function `test_resolve_wiki_and_repo_raises_on_no_config(monkeypatch)` — set `monkeypatch.delenv("CODE_WIKI_REAL_VAULT_PATH", raising=False)`; call `resolve_wiki_and_repo()` with no args; assert `RuntimeError` with message containing `"CODE_WIKI_REAL_VAULT_PATH"`.

    Run both new test files; both must be green.
  </action>
  <acceptance_criteria>
    - `test -f cores/vault-io/tests/test_layout_io_smoke.py` exits 0
    - `test -f cores/vault-io/tests/test_ports_importable.py` exits 0
    - `uv run --package vault-io pytest tests/test_layout_io_smoke.py -x -q` exits 0
    - `uv run --package vault-io pytest tests/test_ports_importable.py -x -q` exits 0
    - Full vault-io suite green: `uv run --package vault-io pytest -x -q` exits 0
  </acceptance_criteria>
  <verify>
    <automated>uv run --package vault-io pytest -x -q</automated>
  </verify>
  <done>
    Layout block is provably byte-stable on repeated writes; every ported module imports cleanly and a smoke call works for the container detector; `_workspace.resolve_wiki_and_repo` raises the actionable error when misconfigured; full vault-io suite green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem (vault) → process | Vault pages are user-authored markdown; treated as trusted input (Pat's own content) |
| process → filesystem (vault) | Writes go through the hand-rolled emitter only; no PyYAML re-normalization |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-06 | Tampering | Vault round-trip | mitigate | The VAULT-04 round-trip test is the explicit mitigation: any code path that mutates vault bytes beyond the intended diff is caught by the empty-`git diff` assertion on every fixture page. The Phase 5 lint/init/scan commands inherit this gate by routing all writes through these ported modules. |
| T-1-03 | Information Disclosure | Committed vault fixture | accept | D-05 explicitly accepted this: 148 pages of Pat's technical wiki content are committed as-is. The content is about Pat's own tools (no PII, no credentials). If a privacy concern emerges later, revisit. |
| T-1-07 | Denial of Service | Truncated frontmatter on a real page | mitigate | The VAULT-05 guard (`if len(parts) < 3: skip`) prevents a malformed page from crashing the token counter. Acceptance criterion: `test_truncated_frontmatter.py` proves the skip-and-warn behavior. |
</threat_model>

<verification>
After all three tasks complete:

```
# Ports clean:
! grep -rn 'lattice_wiki_core\|lattice_workspace' cores/vault-io/src/ && \
! grep -rn 'frontmatter\.dumps' cores/vault-io/src/ && \

# Full vault-io suite:
uv run --package vault-io pytest -x -q && \

# Fixture present:
[ "$(find cores/vault-io/tests/fixtures/round-trip-vault -name '*.md' | wc -l)" -ge 100 ]
```
</verification>

<success_criteria>
- VAULT-01: `python-frontmatter.load()` on every fixture page returns the same in-memory shape that the source `lattice-wiki-core` produces (proven indirectly by the round-trip gate)
- VAULT-02: `layout_io.py` ported verbatim; hand-rolled emitter preserves byte-identical output (layout-io smoke test)
- VAULT-03: no `frontmatter.dumps()` anywhere in `cores/vault-io/src/`
- VAULT-04: round-trip golden gate green against 148-page committed fixture
- VAULT-05: truncated-frontmatter page returns `("skipped", 0)` with stderr warning
- VAULT-06: `_is_placeholder_target` in `lint/common.py`; four-test predicate suite green
- VAULT-07: container detection, scan, init, index, log, token counter, graph analyzer all importable; container detect smoke test green
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-vault-io-and-mcp-skeleton/01-03-SUMMARY.md`. Include: list of ported modules with confirmation of import-surgery diffs (one line per module: "verbatim + import surgery" or "adapted"), the fixture page count, the round-trip test runtime, and the full `uv run --package vault-io pytest -q` output (truncated to last 30 lines).
</output>
