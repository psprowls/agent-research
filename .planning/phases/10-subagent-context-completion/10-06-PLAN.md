---
phase: 10-subagent-context-completion
plan: 06
type: execute
wave: 4
depends_on:
  - 10-04
  - 10-05
files_modified:
  - agents/code-wiki-agent/src/code_wiki_agent/commands/scan.py
  - agents/code-wiki-agent/src/code_wiki_agent/commands/lint.py
  - agents/code-wiki-agent/src/code_wiki_agent/commands/ingest.py
autonomous: true
requirements:
  - CTX-03
must_haves:
  truths:
    - "commands/scan.py calls render_project_context(wiki) once after wiki is resolved, passes the result to build_scanner_system(project_context=...) at SystemMessage construction"
    - "commands/lint.py calls render_project_context(wiki) once and passes the result to build_linter_page_quality_system / _adr_chain / _stale_claims when constructing the semantic_groups tuple"
    - "commands/ingest.py calls render_project_context(wiki) once and passes the result to build_ingestor_system at SystemMessage construction"
    - "Running scan/lint/ingest against a vault that has no CLAUDE.md or AGENTS.md does not crash (render_project_context returns empty string, builders accept the empty string and produce a non-empty prompt)"
  artifacts:
    - path: "agents/code-wiki-agent/src/code_wiki_agent/commands/scan.py"
      provides: "Updated scan command wiring render_project_context + build_scanner_system"
      contains: "render_project_context"
    - path: "agents/code-wiki-agent/src/code_wiki_agent/commands/lint.py"
      provides: "Updated lint command wiring render_project_context + build_linter_*_system"
      contains: "render_project_context"
    - path: "agents/code-wiki-agent/src/code_wiki_agent/commands/ingest.py"
      provides: "Updated ingest command wiring render_project_context + build_ingestor_system"
      contains: "render_project_context"
  key_links:
    - from: "agents/code-wiki-agent/src/code_wiki_agent/commands/scan.py"
      to: "agents/code-wiki-agent/src/code_wiki_agent/prompts/project_context.py"
      via: "from code_wiki_agent.prompts.project_context import render_project_context"
      pattern: "from code_wiki_agent.prompts.project_context import render_project_context"
    - from: "agents/code-wiki-agent/src/code_wiki_agent/commands/lint.py"
      to: "agents/code-wiki-agent/src/code_wiki_agent/prompts/project_context.py"
      via: "from code_wiki_agent.prompts.project_context import render_project_context"
      pattern: "from code_wiki_agent.prompts.project_context import render_project_context"
    - from: "agents/code-wiki-agent/src/code_wiki_agent/commands/ingest.py"
      to: "agents/code-wiki-agent/src/code_wiki_agent/prompts/project_context.py"
      via: "from code_wiki_agent.prompts.project_context import render_project_context"
      pattern: "from code_wiki_agent.prompts.project_context import render_project_context"
---

<objective>
Wire `render_project_context()` into `commands/scan.py`, `commands/lint.py`, `commands/ingest.py`. Each command calls the renderer once at entry (immediately after `wiki` is resolved by `resolve_wiki_and_repo`) and threads the resulting string into the corresponding `build_X_system(project_context=...)` builder at the point of `SystemMessage` construction.

Purpose: This is the command-side half of CTX-03. After plan 10-05 the prompt builders accept a `project_context` kwarg but the commands still rely on the backward-compat constants. This plan switches the commands over to calling the builders explicitly with the rendered project context.

Output: Three modified command files. No new modules. No tests added in this plan — the smoke test for the wiring is covered by existing command-level tests (which should still pass because `render_project_context` on a vault without CLAUDE.md returns the empty string and `build_X_system(project_context="")` equals the backward-compat constant). Snapshot tests covering the wired-with-context path live in plan 10-07.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-subagent-context-completion/10-CONTEXT.md
@.planning/phases/10-subagent-context-completion/10-PATTERNS.md
@agents/code-wiki-agent/src/code_wiki_agent/commands/scan.py
@agents/code-wiki-agent/src/code_wiki_agent/commands/lint.py
@agents/code-wiki-agent/src/code_wiki_agent/commands/ingest.py

<interfaces>
Existing SystemMessage assembly sites to refactor.

From agents/code-wiki-agent/src/code_wiki_agent/commands/scan.py:
- Line 34: `from code_wiki_agent.prompts.scanner import SCANNER_SYSTEM  # noqa: F401`
- Line 265: `wiki, resolved_repo = resolve_wiki_and_repo(vault_path)` is the canonical wiki-resolution point. Call render_project_context(wiki) immediately after this line.
- Lines 339-346: `generate_stub` closure builds `msgs = [SystemMessage(content=SCANNER_SYSTEM), HumanMessage(content=prompt)]`. The closure already captures outer-scope variables; adding `project_ctx` to the outer scope and referencing it inside the closure is the cleanest path.

From agents/code-wiki-agent/src/code_wiki_agent/commands/lint.py:
- The 3-group semantic pass at lines 422-426 constructs `semantic_groups = [("page_quality", LINTER_PAGE_QUALITY_SYSTEM, pages_sample), ("adr_chain", LINTER_ADR_CHAIN_SYSTEM, adr_pages), ("stale_claims", LINTER_STALE_CLAIMS_SYSTEM, pages_with_source)]`.
- Line 505 inside `run_lint`: `wiki, repo = resolve_wiki_and_repo(vault_path)` is the canonical resolution point.
- The `_semantic_pass` helper at line 395 takes `(wiki, pages, pool, cfg, model_override)`. It does NOT currently receive `project_ctx`. The cleanest wiring is to add a `project_context: str = ""` parameter to `_semantic_pass` and have `run_lint` pass `render_project_context(wiki)` into it. The three tuple entries in `semantic_groups` then call the builders explicitly.

From agents/code-wiki-agent/src/code_wiki_agent/commands/ingest.py:
- Line 35: `from code_wiki_agent.prompts.ingestor import INGESTOR_SYSTEM  # noqa: F401`
- Line 391 inside `run_ingest_source`: `wiki, repo = resolve_wiki_and_repo(vault_path)` is the canonical resolution point.
- Line 434: `resp = await llm.ainvoke([SystemMessage(INGESTOR_SYSTEM), HumanMessage(prompt)])` is the SystemMessage construction site.
- A second public function `run_ingest_work_item(frontmatter_text, body, ...)` is exported but does NOT use INGESTOR_SYSTEM (it operates on already-formatted frontmatter/body via `_parse_frontmatter, _validate, file_work_item` from `vault_io.ingest_work_item`). So `run_ingest_work_item` does NOT need wiring; only `run_ingest_source` does. Verify by grepping for INGESTOR_SYSTEM references in ingest.py before editing.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire render_project_context into commands/scan.py</name>
  <read_first>
    - agents/code-wiki-agent/src/code_wiki_agent/commands/scan.py (full file, 449 lines; focus on import block L34, wiki-resolution L265, generate_stub closure L339-346)
    - agents/code-wiki-agent/src/code_wiki_agent/prompts/scanner.py (post-10-05 state — build_scanner_system function and SCANNER_SYSTEM backward-compat constant)
    - agents/code-wiki-agent/src/code_wiki_agent/prompts/project_context.py (the function being wired in)
    - .planning/phases/10-subagent-context-completion/10-PATTERNS.md section `commands/scan.py` (target wiring pattern)
  </read_first>
  <files>agents/code-wiki-agent/src/code_wiki_agent/commands/scan.py</files>
  <action>
    Modify agents/code-wiki-agent/src/code_wiki_agent/commands/scan.py.

    Step 1: Replace the line `from code_wiki_agent.prompts.scanner import SCANNER_SYSTEM  # noqa: F401` (currently around line 34) with the two imports: `from code_wiki_agent.prompts.project_context import render_project_context` and `from code_wiki_agent.prompts.scanner import build_scanner_system`. Before dropping the SCANNER_SYSTEM re-export, check whether anything else imports it via `grep -rn 'from code_wiki_agent.commands.scan import' agents/ tests/`. If any caller imports SCANNER_SYSTEM from commands.scan, keep the re-export by adding a second import line `from code_wiki_agent.prompts.scanner import SCANNER_SYSTEM  # noqa: F401` so that backward compatibility is preserved. The module docstring at L7 currently mentions SCANNER_SYSTEM; update it to mention build_scanner_system + render_project_context.

    Step 2: Immediately after the `wiki, resolved_repo = resolve_wiki_and_repo(vault_path)` line (currently L265 inside run_scan), add a single line: `project_ctx = render_project_context(wiki)`. Place it before the `if repo_path is not None:` branch (which begins at L266). `project_ctx` is captured by the generate_stub closure defined later in the same function.

    Step 3: In the `generate_stub` closure (currently L339-346), replace `SystemMessage(content=SCANNER_SYSTEM)` with `SystemMessage(content=build_scanner_system(project_context=project_ctx))`. The closure already captures `project_ctx` from the enclosing scope — no parameter changes to generate_stub are needed.

    Step 4: Do NOT touch the model-override branch at L330-337 — that path is unaffected by the prompt wiring.

    Step 5: Do NOT touch anything else in scan.py — leave file_map, diff computation, fan-out, write-back logic untouched.
  </action>
  <verify>
    <automated>uv run --package code-wiki-agent python -c "import ast, pathlib; src=pathlib.Path('agents/code-wiki-agent/src/code_wiki_agent/commands/scan.py').read_text(); assert 'render_project_context' in src; assert 'build_scanner_system(project_context=project_ctx)' in src; assert 'project_ctx = render_project_context(wiki)' in src; ast.parse(src)" &amp;&amp; uv run --package code-wiki-agent pytest agents/code-wiki-agent/tests -k scan -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'from code_wiki_agent.prompts.project_context import render_project_context' agents/code-wiki-agent/src/code_wiki_agent/commands/scan.py` reports 1.
    - `grep -c 'from code_wiki_agent.prompts.scanner import build_scanner_system' agents/code-wiki-agent/src/code_wiki_agent/commands/scan.py` reports 1.
    - `grep -c 'project_ctx = render_project_context(wiki)' agents/code-wiki-agent/src/code_wiki_agent/commands/scan.py` reports 1.
    - `grep -c 'build_scanner_system(project_context=project_ctx)' agents/code-wiki-agent/src/code_wiki_agent/commands/scan.py` reports 1.
    - `grep -c 'SystemMessage(content=SCANNER_SYSTEM)' agents/code-wiki-agent/src/code_wiki_agent/commands/scan.py` reports 0 (legacy usage replaced).
    - Python file parses without SyntaxError (verified by `ast.parse(src)` in the verify script).
    - `uv run --package code-wiki-agent pytest agents/code-wiki-agent/tests -k scan -x` exits 0 (existing scan tests still pass — render_project_context on a vault without CLAUDE.md returns empty and build_scanner_system with empty context equals the prior SCANNER_SYSTEM constant).
    - `grep -c '^from deepagents' agents/code-wiki-agent/src/code_wiki_agent/commands/scan.py` reports 0 (Scope Fence 1).
    - `git diff --name-only HEAD -- cores/subagent-runtime/pool.py | wc -l` reports 0 (Scope Fence 6).
  </acceptance_criteria>
  <done>scan.py wires render_project_context once after wiki resolution and uses build_scanner_system at the SystemMessage construction site. Existing scan tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Wire render_project_context into commands/lint.py for the 3-group semantic pass</name>
  <read_first>
    - agents/code-wiki-agent/src/code_wiki_agent/commands/lint.py (full file, 541 lines; focus on the _semantic_pass helper at L395-470 and run_lint at L478-541)
    - agents/code-wiki-agent/src/code_wiki_agent/prompts/linter.py (post-10-05 state — three build_linter_*_system functions and backward-compat constants)
    - agents/code-wiki-agent/src/code_wiki_agent/prompts/project_context.py
    - .planning/phases/10-subagent-context-completion/10-PATTERNS.md section `commands/lint.py` (target wiring pattern)
  </read_first>
  <files>agents/code-wiki-agent/src/code_wiki_agent/commands/lint.py</files>
  <action>
    Modify agents/code-wiki-agent/src/code_wiki_agent/commands/lint.py.

    Step 1: Update the imports block. Change the existing import that pulls LINTER_PAGE_QUALITY_SYSTEM, LINTER_ADR_CHAIN_SYSTEM, LINTER_STALE_CLAIMS_SYSTEM (grep for the exact line first since the import may be a single-line or multi-line tuple-import) to import the three builder functions instead: `from code_wiki_agent.prompts.linter import (build_linter_adr_chain_system, build_linter_page_quality_system, build_linter_stale_claims_system)`. Add a new import line: `from code_wiki_agent.prompts.project_context import render_project_context`. If `LINT_PRIORITY_ORDER` was previously imported (check via grep before editing), keep that import — it is a separate constant unrelated to wiring.

    Step 2: Modify the `_semantic_pass` helper signature (currently around L395) to accept a `project_context: str = ""` keyword argument. Inside `_semantic_pass`, change the `semantic_groups` list construction (currently L422-426) from passing the `*_SYSTEM` constants to calling the builders with the `project_context` argument. The new construction is `semantic_groups = [("page_quality", build_linter_page_quality_system(project_context=project_context), pages_sample), ("adr_chain", build_linter_adr_chain_system(project_context=project_context), adr_pages), ("stale_claims", build_linter_stale_claims_system(project_context=project_context), pages_with_source)]`. The tuple shape `(name, system_prompt_str, pages_input)` is unchanged — only the second element changes from constant lookup to builder call.

    Step 3: Inside `run_lint` (the public entry point at L478), immediately after `wiki, repo = resolve_wiki_and_repo(vault_path)` (currently L505), add: `project_ctx = render_project_context(wiki)`. Then update the existing `await _semantic_pass(wiki, pages, pool, cfg, model_override=model_override)` call (around L520) to pass `project_context=project_ctx` as a new keyword argument so it becomes `await _semantic_pass(wiki, pages, pool, cfg, model_override=model_override, project_context=project_ctx)`.

    Step 4: Do NOT modify `_mechanical_pass`, `_module_pass`, or any of the inline 7-module-checks logic. Those are LLM-free passes.

    Step 5: Do NOT modify the `run_linter_group` async closure other than the inputs it receives — the system_prompt string is already pre-built by the time the closure runs.
  </action>
  <verify>
    <automated>uv run --package code-wiki-agent python -c "import ast, pathlib; src=pathlib.Path('agents/code-wiki-agent/src/code_wiki_agent/commands/lint.py').read_text(); assert 'render_project_context' in src; assert 'build_linter_page_quality_system' in src; assert 'build_linter_adr_chain_system' in src; assert 'build_linter_stale_claims_system' in src; assert 'project_ctx = render_project_context(wiki)' in src; assert 'project_context=project_ctx' in src; ast.parse(src)" &amp;&amp; uv run --package code-wiki-agent pytest agents/code-wiki-agent/tests -k lint -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'from code_wiki_agent.prompts.project_context import render_project_context' agents/code-wiki-agent/src/code_wiki_agent/commands/lint.py` reports 1.
    - `grep -c 'build_linter_page_quality_system' agents/code-wiki-agent/src/code_wiki_agent/commands/lint.py` reports at least 2 (one import, one call in semantic_groups).
    - `grep -c 'build_linter_adr_chain_system' agents/code-wiki-agent/src/code_wiki_agent/commands/lint.py` reports at least 2.
    - `grep -c 'build_linter_stale_claims_system' agents/code-wiki-agent/src/code_wiki_agent/commands/lint.py` reports at least 2.
    - `grep -c 'project_ctx = render_project_context(wiki)' agents/code-wiki-agent/src/code_wiki_agent/commands/lint.py` reports 1.
    - `grep -c 'project_context=project_ctx' agents/code-wiki-agent/src/code_wiki_agent/commands/lint.py` reports 1 (the kwarg passed into _semantic_pass).
    - `grep -c 'project_context=project_context' agents/code-wiki-agent/src/code_wiki_agent/commands/lint.py` reports at least 3 (inside _semantic_pass, one per builder call).
    - The legacy `*_SYSTEM` constants should not appear in `semantic_groups`. Verify with `grep -E 'LINTER_(PAGE_QUALITY|ADR_CHAIN|STALE_CLAIMS)_SYSTEM' agents/code-wiki-agent/src/code_wiki_agent/commands/lint.py | grep -v '^#' | wc -l` reports 0 (if the legacy import was kept anywhere, the grep above should still report 0 because we removed it).
    - Python file parses without SyntaxError.
    - `uv run --package code-wiki-agent pytest agents/code-wiki-agent/tests -k lint -x` exits 0 (existing lint tests still pass).
    - `grep -c '^from deepagents' agents/code-wiki-agent/src/code_wiki_agent/commands/lint.py` reports 0 (Scope Fence 1).
  </acceptance_criteria>
  <done>lint.py wires render_project_context once after wiki resolution; _semantic_pass accepts the project_context kwarg and threads it into all three build_linter_*_system calls. Existing lint tests still pass.</done>
</task>

<task type="auto">
  <name>Task 3: Wire render_project_context into commands/ingest.py (run_ingest_source only)</name>
  <read_first>
    - agents/code-wiki-agent/src/code_wiki_agent/commands/ingest.py (full file, 566 lines; focus on import block L35, wiki resolution at L391, SystemMessage construction at L434)
    - agents/code-wiki-agent/src/code_wiki_agent/prompts/ingestor.py (post-10-05 state — build_ingestor_system function and INGESTOR_SYSTEM backward-compat constant)
    - agents/code-wiki-agent/src/code_wiki_agent/prompts/project_context.py
    - .planning/phases/10-subagent-context-completion/10-PATTERNS.md section `commands/ingest.py` (target wiring pattern)
  </read_first>
  <files>agents/code-wiki-agent/src/code_wiki_agent/commands/ingest.py</files>
  <action>
    Modify agents/code-wiki-agent/src/code_wiki_agent/commands/ingest.py.

    Step 1: Replace the line `from code_wiki_agent.prompts.ingestor import INGESTOR_SYSTEM  # noqa: F401` (currently around line 35) with two imports: `from code_wiki_agent.prompts.project_context import render_project_context` and `from code_wiki_agent.prompts.ingestor import build_ingestor_system`. Check whether anything else imports INGESTOR_SYSTEM via `grep -rn 'from code_wiki_agent.commands.ingest import' agents/ tests/` — if any caller exists, restore the legacy re-export with a second import line.

    Step 2: Inside `run_ingest_source` (the function defined around L380), immediately after `wiki, repo = resolve_wiki_and_repo(vault_path)` (currently L391), add: `project_ctx = render_project_context(wiki)`.

    Step 3: At the LLM invocation (currently L434 — the line `resp = await llm.ainvoke([SystemMessage(INGESTOR_SYSTEM), HumanMessage(prompt)])`), replace `SystemMessage(INGESTOR_SYSTEM)` with `SystemMessage(build_ingestor_system(project_context=project_ctx))`. The HumanMessage(prompt) part is unchanged.

    Step 4: Do NOT touch `run_ingest_work_item` — it does not use INGESTOR_SYSTEM (verified by grep before editing). Verify with `grep -c 'INGESTOR_SYSTEM' agents/code-wiki-agent/src/code_wiki_agent/commands/ingest.py` reports 0 after the edit (both the import and the usage site are replaced).

    Step 5: Do NOT modify the model-override branch (L426-433), `_parse_ingestor_response`, `_route_target_path`, `_rewrite_target_slug_in_body`, `_resolve_wikilinks`, or any post-LLM body processing.
  </action>
  <verify>
    <automated>uv run --package code-wiki-agent python -c "import ast, pathlib; src=pathlib.Path('agents/code-wiki-agent/src/code_wiki_agent/commands/ingest.py').read_text(); assert 'render_project_context' in src; assert 'build_ingestor_system(project_context=project_ctx)' in src; assert 'project_ctx = render_project_context(wiki)' in src; ast.parse(src)" &amp;&amp; uv run --package code-wiki-agent pytest agents/code-wiki-agent/tests -k ingest -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'from code_wiki_agent.prompts.project_context import render_project_context' agents/code-wiki-agent/src/code_wiki_agent/commands/ingest.py` reports 1.
    - `grep -c 'from code_wiki_agent.prompts.ingestor import build_ingestor_system' agents/code-wiki-agent/src/code_wiki_agent/commands/ingest.py` reports 1.
    - `grep -c 'project_ctx = render_project_context(wiki)' agents/code-wiki-agent/src/code_wiki_agent/commands/ingest.py` reports 1.
    - `grep -c 'build_ingestor_system(project_context=project_ctx)' agents/code-wiki-agent/src/code_wiki_agent/commands/ingest.py` reports 1.
    - `grep -c 'SystemMessage(INGESTOR_SYSTEM)' agents/code-wiki-agent/src/code_wiki_agent/commands/ingest.py` reports 0 (legacy usage replaced).
    - `run_ingest_work_item` is unchanged in behavior: verify by `grep -n 'def run_ingest_work_item' agents/code-wiki-agent/src/code_wiki_agent/commands/ingest.py` returns a line number, and the function body does NOT contain `build_ingestor_system` or `INGESTOR_SYSTEM` references after the edit.
    - Python file parses without SyntaxError.
    - `uv run --package code-wiki-agent pytest agents/code-wiki-agent/tests -k ingest -x` exits 0 (existing ingest tests still pass — a vault without CLAUDE.md yields project_ctx empty, and build_ingestor_system with empty context equals the prior INGESTOR_SYSTEM constant).
    - `grep -c '^from deepagents' agents/code-wiki-agent/src/code_wiki_agent/commands/ingest.py` reports 0 (Scope Fence 1).
    - No new dependencies added: `git diff --name-only HEAD | grep -c 'pyproject.toml'` reports 0 (Scope Fence 2).
  </acceptance_criteria>
  <done>ingest.py wires render_project_context once after wiki resolution and uses build_ingestor_system at the SystemMessage construction site inside run_ingest_source. run_ingest_work_item is untouched. Existing ingest tests still pass.</done>
</task>

</tasks>

<verification>
1. All three command files import render_project_context and the corresponding build_*_system function(s).
2. Each command calls render_project_context(wiki) exactly once, right after wiki resolution.
3. SystemMessage construction sites pass the rendered context to the builder via project_context= kwarg.
4. Existing tests (scan, lint, ingest) all pass — on a vault without CLAUDE.md the rendered string is empty and the assembled prompt equals the legacy constant.
5. Scope fences: no deepagents import, no pyproject.toml change, no pool.py change, no Phase-6 baseline regen.
</verification>

<success_criteria>
- All three commands invoke render_project_context once at entry and pass the result into the per-role prompt builders.
- run_ingest_work_item is unchanged (not in scope — it does not use the ingestor system prompt).
- Existing command-level tests pass without snapshot regeneration (because render_project_context on a no-CLAUDE.md fixture returns empty string).
- After this plan plus plan 10-05 wiring is complete, CTX-01 and CTX-03 are end-to-end satisfied. Verification of the wired-with-context path is plan 10-07.
</success_criteria>

<output>
Create `.planning/phases/10-subagent-context-completion/10-06-SUMMARY.md` when done.
</output>
