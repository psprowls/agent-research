---
phase: 09-trace-observability-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/trace-schema.md
  - docs/cancellation.md
autonomous: true
requirements:
  - OBS-04
tags:
  - trace
  - documentation
  - schema-version

must_haves:
  truths:
    - "`docs/trace-schema.md` exists at repo root and documents the three record shapes (per-item, batch_cancelled, query_summary)."
    - "The doc explains the `schema_version` field, its lenient-consumer / strict-producer policy, and the bump rules."
    - "The doc covers the v0 (unversioned) compatibility note and references the Phase 8 additive-shape rule."
    - "`docs/cancellation.md` carries a one-line cross-link pointing at `docs/trace-schema.md` as the authoritative field-table source."
  artifacts:
    - path: "docs/trace-schema.md"
      provides: "Versioned, OSS-release-friendly trace schema reference"
      min_lines: 120
      contains: "schema_version"
    - path: "docs/cancellation.md"
      provides: "Cross-link to docs/trace-schema.md added"
      contains: "trace-schema.md"
  key_links:
    - from: "docs/cancellation.md §Trace Shapes"
      to: "docs/trace-schema.md"
      via: "markdown link"
      pattern: "\\[.*\\]\\(.*trace-schema\\.md.*\\)"
---

<objective>
Write `docs/trace-schema.md` — the authoritative, OSS-release-friendly reference for the `.code-wiki/traces/` JSONL schema — and add a one-line cross-link from `docs/cancellation.md` back to it. This is the documentation half of OBS-04.

Purpose: Per locked decision D-05, the schema is documented as a sibling to `docs/cancellation.md`: directory layout, three record shapes with field tables, the `schema_version` field plus bump rules, the additive-shape rule cross-referencing Phase 8 D-06/D-07, the v0 compatibility note, and examples copied from real fixtures. Per D-06, `docs/cancellation.md` gets a one-line link rather than a duplicated field table.

Output: A new `docs/trace-schema.md` (~150-250 lines) and a single-line edit to `docs/cancellation.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-trace-observability-polish/09-CONTEXT.md
@.planning/phases/09-trace-observability-polish/09-PATTERNS.md
@.planning/phases/08-host-reliability/08-CONTEXT.md
@docs/cancellation.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/trace-schema.md with all six required sections</name>
  <files>docs/trace-schema.md</files>
  <read_first>
    - docs/cancellation.md (full read — this is the analog/sibling doc; mirror its structure, tone, OSS-friendly prose, and bottom-of-file `*Source:*` line)
    - .planning/phases/09-trace-observability-polish/09-CONTEXT.md (decisions D-01..D-06, especially D-05 wording — required sections are binding)
    - .planning/phases/09-trace-observability-polish/09-PATTERNS.md (`docs/trace-schema.md` section — section ordering and excerpt examples)
    - .planning/phases/08-host-reliability/08-CONTEXT.md (D-06, D-07 — the additive-shape rule cross-referenced from §4 of the new doc; D-08 — the per-item `status: cancelled` shape)
    - cores/vault-io/tests/fixtures/round-trip-vault/.code-wiki/traces/1778766775_3d8c7377.jsonl (one real per-item record example to lift)
    - cores/vault-io/tests/fixtures/round-trip-vault/.code-wiki/traces/ (list directory; identify a fixture file that contains a `kind: query_summary` record if any exists, otherwise the example can be a constructed snippet labeled "example")
    - cores/subagent-runtime/src/subagent_runtime/pool.py (skim `_write_trace` lines ~211-230 and `_write_batch_terminal` lines ~249-263 — field list, types, and required-vs-optional semantics for the doc's field tables)
    - agents/code-wiki-agent/src/code_wiki_agent/commands/query.py (skim the `summary_record` writer near line 976 for the query_summary field list)
  </read_first>
  <acceptance_criteria>
    - File `docs/trace-schema.md` exists.
    - File contains six H2/H1-numbered sections matching D-05, verifiable by `grep -E '^(#|##) ' docs/trace-schema.md` returning entries that cover (in any order, case-insensitive substring match acceptable): (a) directory layout / filename convention, (b) per-record shapes, (c) `schema_version` field + policy, (d) additive-shape rule, (e) v0 compatibility, (f) examples.
    - File contains the literal string `schema_version` at least 5 times (intro + policy + each of the three shape tables).
    - File contains the literal string `batch_cancelled` (per-record-shape spec for the terminator).
    - File contains the literal string `query_summary` (per-record-shape spec for the per-query record).
    - File contains a markdown reference back to `cancellation.md` from the `event: batch_cancelled` section (substring `cancellation.md` appears at least once).
    - File contains a markdown reference forward to `.planning/phases/09-trace-observability-polish/09-CONTEXT.md` in the bottom-of-file `*Source:*` line (mirrors cancellation.md style).
    - File contains at least three fenced ` ```json ` blocks — one example per record shape.
    - Each example block includes `"schema_version": 1` as the first key.
    - Line count between 120 and 320 (target ~150-250 per D-05, with cushion).
    - File contains no project-internal jargon that isn't introduced (audience is OSS-release-friendly per PATTERNS.md).
  </acceptance_criteria>
  <action>
    Create `docs/trace-schema.md` at repo root. Mirror the structure, voice, and OSS-release-friendly prose of `docs/cancellation.md`. Required content per D-05:

    1. Title and opening paragraph naming the audience (operators / OSS consumers / future maintainers) and the in-scope versus deferred markers (mirror cancellation.md's `**v1.1 scope:**` callout — note that `schema_version: 1` is the first formal version; pre-Phase-9 records are treated as v0 and rendered best-effort, never rewritten).

    2. **Directory layout and filename convention** — describe `.code-wiki/traces/` location relative to the wiki root, and the two filename patterns observed: `{int_timestamp}_{uuid8}.jsonl` for per-batch fan-out traces (from `SubagentPool._write_trace`) and `query_{query_id}.jsonl` for per-query summaries (from `query.py`).

    3. **Per-record-shape spec** with three subsections, each a field table (columns: name, type, required?, semantics) plus a fenced ```json``` example block lifted from a real fixture (with `"schema_version": 1` prepended to the example).
       - Per-item subagent record (no `event`, no `kind`).
       - Batch terminal record (`event: batch_cancelled`) — include a one-sentence pointer to `docs/cancellation.md` for the propagation chain that produces this record, per D-06.
       - Query summary record (`kind: query_summary`).

    4. **`schema_version` field** — its purpose (D-01 self-describing lines), the integer format (D-02), the lenient-consumer / strict-producer policy (D-03 — quote the exact warning string `warning: trace schema_version N is newer than supported (M); rendering best-effort`), and the bump rules (D-02 — additive changes don't bump; renames/removals/meaning-changes do).

    5. **Additive-shape rule** — short section explaining that new optional fields and new record kinds are free (do not bump the integer), with an explicit cross-reference to Phase 8 D-06/D-07 in `.planning/phases/08-host-reliability/08-CONTEXT.md`. State that this is why `kind: query_summary` and `event: batch_cancelled` could be introduced without a version bump.

    6. **v0 (unversioned) compatibility** — short section explaining that pre-Phase-9 records lack `schema_version`; the renderer infers `schema_version: 0` and emits a one-time stderr warning per file (D-04). Existing fixture traces are NOT rewritten. Producers always emit `schema_version: 1` going forward.

    Close with a bottom-of-file italic source line: `*Source: Phase 9 (Trace/Observability Polish) — see .planning/phases/09-trace-observability-polish/09-CONTEXT.md for the design record.*` (mirroring cancellation.md's bottom line).

    Stylistic constraints: use H2 (`##`) for the numbered top-level sections, fenced ```json``` blocks for examples, em-dashes for asides; do NOT introduce a TOC; do NOT include any Python code blocks (this is consumer-facing schema doc, not implementation reference). Quote token-level field names with backticks. Avoid second-person ("you") — match cancellation.md's third-person operator voice. Do NOT use the words "trivial" / "obviously" / "simply" — OSS-release tone.
  </action>
  <verify>
    <automated>test -f docs/trace-schema.md</automated>
    <automated>grep -c "schema_version" docs/trace-schema.md | awk '{exit ($1>=5)?0:1}'</automated>
    <automated>grep -q "batch_cancelled" docs/trace-schema.md && grep -q "query_summary" docs/trace-schema.md && grep -q "cancellation.md" docs/trace-schema.md</automated>
    <automated>grep -c '^```json' docs/trace-schema.md | awk '{exit ($1>=3)?0:1}'</automated>
    <automated>wc -l docs/trace-schema.md | awk '{exit ($1>=120 && $1<=320)?0:1}'</automated>
  </verify>
  <done>
    - All five `<verify>` automated checks pass.
    - The six required sections are present (D-05).
    - Cross-link to `cancellation.md` appears in the `event: batch_cancelled` section.
    - Bottom-of-file `*Source:*` line points to 09-CONTEXT.md.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add one-line cross-link from cancellation.md to trace-schema.md</name>
  <files>docs/cancellation.md</files>
  <read_first>
    - docs/cancellation.md (full read; locate Section 3 "Trace Shapes" — the cross-link sentence belongs there per D-06)
    - .planning/phases/09-trace-observability-polish/09-CONTEXT.md (D-06 — surgical one-line addition only)
    - .planning/phases/09-trace-observability-polish/09-PATTERNS.md (`docs/cancellation.md` section — change spec is a one-line surgical addition, no content rewrite)
  </read_first>
  <acceptance_criteria>
    - `docs/cancellation.md` contains a markdown link or reference whose target substring is `trace-schema.md` and which appears inside or adjacent to the "Trace Shapes" section (section 3 in the existing file).
    - `git diff docs/cancellation.md` shows exactly one line added (or one line modified to add the link); no other content is moved, reformatted, or rewritten.
    - The existing JSON examples in cancellation.md §3 are not removed or altered (they remain illustrative per PATTERNS.md).
    - The cross-link sentence is concise: ~1 sentence pointing readers to `docs/trace-schema.md` as the authoritative source for the field tables.
  </acceptance_criteria>
  <action>
    Open `docs/cancellation.md`. Locate Section 3 "Trace Shapes". Insert a single sentence — at the top of section 3 (above the existing per-record JSON examples is the natural spot) — that names `docs/trace-schema.md` as the authoritative source for the field tables, and notes that cancellation.md keeps its inline JSON examples for illustration only. Use the relative path `docs/trace-schema.md` (or `./trace-schema.md` since they're siblings) in a markdown link. Make NO other edits to this file: do not reflow paragraphs, do not adjust headings, do not touch the existing JSON blocks. Per D-06.
  </action>
  <verify>
    <automated>grep -q "trace-schema.md" docs/cancellation.md</automated>
    <automated>git diff --stat docs/cancellation.md | grep -E "1 file changed, [1-3] insertion" || git diff --stat docs/cancellation.md | grep -E "1 file changed, [1-3] insertions"</automated>
  </verify>
  <done>
    - `grep "trace-schema.md" docs/cancellation.md` returns a hit.
    - `git diff docs/cancellation.md` shows a small surgical addition (≤3 lines added).
    - No deletions or modifications to existing JSON example blocks.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan only produces documentation files in `docs/`. No new code, no untrusted input handling, no new attack surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Information Disclosure | `docs/trace-schema.md` examples lifted from real fixtures | accept | Real fixture examples come from `cores/vault-io/tests/fixtures/round-trip-vault/.code-wiki/traces/` — already a committed public-test fixture with no PII or secrets. Doc examples are equivalent to the fixture content. No new exposure. |
| T-09-05 | Tampering | Schema doc drift vs producer code | mitigate | Plan 09-01 adds unit tests that lock the field set on each writer (success, error, batch-terminal, query-summary). If the schema diverges from the doc, the unit tests still pin the producer side; the doc remains the authoritative consumer-facing reference and is reviewed at release time. |
</threat_model>

<verification>
- `docs/trace-schema.md` exists, ~120-320 lines, contains all six required sections per D-05.
- `docs/cancellation.md` contains a single-sentence cross-link to `docs/trace-schema.md`.
- No other documentation files are modified.
- The new doc reads cleanly to an external operator who has never seen `code-wiki-agent` internals (subjective check at PR review; not auto-asserted).
</verification>

<success_criteria>
1. `docs/trace-schema.md` is the authoritative reference for `.code-wiki/traces/*.jsonl` and covers the three record shapes, `schema_version` policy, additive-shape rule, v0 compatibility, and examples.
2. `docs/cancellation.md` cross-links to `docs/trace-schema.md` without duplication of field tables (D-06).
3. ROADMAP success criterion 1 ("the schema is documented with a breaking-change policy") is met by §4 of the new doc.
</success_criteria>

<output>
Create `.planning/phases/09-trace-observability-polish/09-02-SUMMARY.md` when done. Summary must list: the section titles actually used in `docs/trace-schema.md`, the line count of the new doc, the exact location in `docs/cancellation.md` where the cross-link sentence was inserted, and the link target syntax used.
</output>
