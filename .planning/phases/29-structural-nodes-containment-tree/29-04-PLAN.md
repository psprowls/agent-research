---
phase: 29-structural-nodes-containment-tree
plan: 04
type: execute
wave: 3
depends_on:
  - 29-03
files_modified:
  - packages/graph-io/src/graph_io/update.py
  - packages/graph-io/tests/test_structural_nodes.py
  - packages/graph-io/tests/fixtures/sample_monorepo/pyproject.toml
  - packages/graph-io/tests/fixtures/sample_monorepo/packages/mypkg/pyproject.toml
  - packages/graph-io/tests/fixtures/sample_monorepo/packages/mypkg/src/mypkg/__init__.py
  - packages/graph-io/tests/fixtures/sample_monorepo/packages/mypkg/src/mypkg/foo.py
  - packages/graph-io/tests/fixtures/sample_monorepo/packages/mypkg/src/mypkg/sub/__init__.py
  - packages/graph-io/tests/fixtures/sample_monorepo/packages/mypkg/src/mypkg/sub/bar.py
  - packages/graph-io/tests/fixtures/sample_monorepo/packages/mypkg/src/mypkg/sub/deep/__init__.py
  - packages/graph-io/tests/fixtures/sample_monorepo/packages/mypkg/src/mypkg/sub/deep/baz.py
  - packages/graph-io/tests/fixtures/sample_monorepo/packages/mypkg/src/mypkg/proto/data_pb2.py
  - packages/graph-io/tests/fixtures/sample_monorepo/packages/mypkg/scripts/run.sh
  - packages/graph-io/tests/fixtures/sample_monorepo/packages/mypkg/tests/test_foo.py
  - packages/graph-io/tests/fixtures/sample_monorepo/packages/jspkg/package.json
  - packages/graph-io/tests/fixtures/sample_monorepo/packages/jspkg/index.js
  - packages/graph-io/tests/fixtures/sample_monorepo/packages/jspkg/types.d.ts
  - packages/graph-io/tests/fixtures/sample_monorepo/packages/jspkg/gen/data.gen.ts
  - packages/graph-io/tests/fixtures/sample_monorepo/tests/integration/test_top.py
autonomous: true
requirements:
  - STRUCT-04
tags:
  - graph-io
  - update-orchestration
  - integration-test
  - strict-tree-invariant

must_haves:
  truths:
    - "cg update --full now calls structural_nodes.emit after packages.refresh and before resolve.sweep, inside the same store.transaction (D-23)"
    - "Every node in the graph has exactly ONE physically_contains parent edge (strict tree)"
    - "Test files in Phase 29 are physically_contains-parented by Repository, not Package or SubPackage"
    - "Sample monorepo fixture exists under tests/fixtures/sample_monorepo/ and exercises Python src-layout, Python __init__.py at depths 1/2/3, JS package, top-level tests/ dir, .d.ts, _pb2.py with @generated marker, shebang script, *.gen.ts (D-22)"
    - "STRUCT-04 strict-tree invariant test test_physically_contains_is_strict_tree lives in tests/test_structural_nodes.py, runs cg update --full on the fixture, and asserts: (1) GROUP BY child_id HAVING COUNT>1 returns zero rows; (2) exactly one Repository node; (3) test files have only Repository parents in physically_contains (D-22)"
  artifacts:
    - path: "packages/graph-io/src/graph_io/update.py"
      provides: "structural_nodes.emit call wired in run() after packages.refresh, before resolve.sweep"
      contains: "structural_nodes.emit"
    - path: "packages/graph-io/tests/fixtures/sample_monorepo/"
      provides: "Hand-curated mini-repo for the STRUCT-04 strict-tree invariant test (D-22)"
    - path: "packages/graph-io/tests/test_structural_nodes.py"
      provides: "test_physically_contains_is_strict_tree end-to-end fixture test"
      contains: "test_physically_contains_is_strict_tree"
  key_links:
    - from: "update.run()"
      to: "structural_nodes.emit"
      via: "single function call between packages.refresh and resolve.sweep"
      pattern: "structural_nodes\\.emit"
    - from: "test_physically_contains_is_strict_tree"
      to: "sample_monorepo fixture"
      via: "copytree to tmp_path + init_repo + write_and_commit + update.run(full=True)"
      pattern: "sample_monorepo"
---

<objective>
Wire `structural_nodes.emit` into `update.run()` (D-23), build the
hand-curated `sample_monorepo` fixture (D-22), and add the end-to-end
strict-tree invariant test that proves STRUCT-04 holds: every node has
exactly one `physically_contains` parent.

Purpose: STRUCT-04 (strict tree) is the most important invariant of Phase 29
— it is what makes the containment subtree queryable as a tree (unique
parent → DFS terminates, describe-package returns one subtree). The unit
tests in Plan 03 prove each emission rule; this plan proves the rules
compose into a tree when wired end-to-end through `cg update`.

Output: `cg update --full` against the sample_monorepo fixture produces a
graph where SQL-aggregating `physically_contains` edges by `dst` yields zero
rows with COUNT > 1. Test files appear under Repository containment (not
Package). Repository node count is exactly 1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-structural-nodes-containment-tree/29-CONTEXT.md
@.planning/phases/29-structural-nodes-containment-tree/29-PATTERNS.md
@packages/graph-io/src/graph_io/update.py
@packages/graph-io/src/graph_io/structural_nodes.py
@packages/graph-io/tests/test_structural_nodes.py
@packages/graph-io/tests/_git_repo.py
@packages/graph-io/tests/test_cli_smoke.py

<interfaces>
<!-- Existing helpers used by the integration test. Extracted from codebase. -->

From packages/graph-io/tests/_git_repo.py:
  init_repo(root: Path) -> None
  write_and_commit(root: Path, files: dict[str, str], message: str) -> str  (returns commit sha)

From packages/graph-io/src/graph_io/update.py:
  run(repo_root: Path, *, full: bool = False, workspace: Path | None = None,
      lock_timeout_ms: int | None = None) -> None

From packages/graph-io/src/graph_io/structural_nodes.py (Plan 03):
  emit(conn, *, repo_root, ctx, skip_dirs) -> None
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire structural_nodes.emit into update.run() per D-23</name>
  <read_first>
    - .planning/phases/29-structural-nodes-containment-tree/29-CONTEXT.md (D-23 — exact insertion site: after packages.refresh, before resolve.sweep; ctx and skip_dirs already in scope)
    - .planning/phases/29-structural-nodes-containment-tree/29-PATTERNS.md (Section "update.py MODIFY — D-23 single-line insertion" — exact before/after, import edit, ordering rationale)
    - packages/graph-io/src/graph_io/update.py (full file — confirm exact line numbers of `packages.refresh(conn, repo_root=repo_root, ctx=ctx)` call, the `if full:` block that follows, and `resolve.sweep(conn)`)
    - packages/graph-io/src/graph_io/structural_nodes.py (final state from Plan 03 — confirm emit signature)
  </read_first>
  <files>packages/graph-io/src/graph_io/update.py</files>
  <behavior>
    - update.run() with full=True calls structural_nodes.emit between the `if full:` deletion block and resolve.sweep
    - update.run() with full=False (incremental) ALSO calls structural_nodes.emit at the same point (per D-23 the call is unconditional)
    - ctx and skip_dirs are already-bound locals at the insertion point — no new variables introduced
    - All pre-existing update tests (test_update_full, test_update_incremental, test_update_idempotent, test_update_interrupt) continue to pass
    - cg update --full on a real repo succeeds and writes a Repository node
  </behavior>
  <action>
    Wire `structural_nodes.emit` into `update.run()` per D-23. Two edits in
    `packages/graph-io/src/graph_io/update.py`:

    1. Extend the existing graph_io import line to include `structural_nodes`.
       Current import is roughly:
           from graph_io import _ignore, packages, resolve, schema, store, upsert
       After:
           from graph_io import _ignore, packages, resolve, schema, store, structural_nodes, upsert
       Verify the exact import line while reading; add `structural_nodes` in
       alphabetical order.

    2. Insert the emit call inside `update.run()` AFTER the `if full:` deletion
       block (the block that deletes non-package nodes with path NOT NULL when
       doing a full rebuild) and BEFORE `resolve.sweep(conn)`. The surrounding
       context (from 29-PATTERNS.md) is:

         with store.transaction(conn):
             _process_files(conn, repo_root, changed, skip_dirs)
             packages.refresh(conn, repo_root=repo_root, ctx=ctx)
             if full:
                 # existing delete block
                 ...
             structural_nodes.emit(conn, repo_root=repo_root, ctx=ctx, skip_dirs=skip_dirs)  # NEW per D-23
             resolve.sweep(conn)
             _set_metadata(...)
             _set_metadata(...)

    Rationale (D-23 + 29-PATTERNS.md):
      - AFTER packages.refresh: structural_nodes.emit reads Package nodes
        (language attr) to decide SubPackage walk vs JS/TS direct File emit
      - AFTER the if-full delete: emit re-creates File nodes with role flags;
        if it ran before the delete, the full-rebuild delete would wipe them
      - BEFORE resolve.sweep: sweep must see the new Repository/SubPackage
        nodes (with their uri attr) so it does not delete them; Plan 02's
        predicate edit (`AND uri IS NULL`) is the second half of that contract

    Do NOT change anything else in update.py. Do NOT alter the if-full block
    contents, the metadata writes, or the exception handling. Do NOT add the
    emit call outside the `with store.transaction(conn):` block — it must be
    inside the single transaction per 29-PATTERNS.md "Transaction scope".
  </action>
  <verify>
    <automated>cd packages/graph-io && uv run --package graph-io pytest tests/test_update_full.py tests/test_update_incremental.py tests/test_update_idempotent.py tests/test_update_interrupt.py tests/test_resolve.py tests/test_packages.py tests/test_structural_nodes.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -nE 'structural_nodes\.emit' packages/graph-io/src/graph_io/update.py returns at least 1 line
    - grep -nE 'from graph_io import .*structural_nodes' packages/graph-io/src/graph_io/update.py returns 1 line
    - The emit call appears between packages.refresh and resolve.sweep in the file. Verify with: awk '/packages.refresh\(conn,/{p=NR} /structural_nodes\.emit\(conn,/{s=NR} /resolve\.sweep\(conn\)/{r=NR} END{print (p<s && s<r) ? "OK" : "BAD"}' packages/graph-io/src/graph_io/update.py outputs OK
    - All pre-existing update-flow tests pass (no regressions)
    - All Plan 02 tests pass (sweep still preserves URI-bearing nodes)
    - All Plan 03 tests pass (structural_nodes unit tests still green)
  </acceptance_criteria>
  <done>
    structural_nodes.emit is called inside update.run()'s transaction, between
    packages.refresh and resolve.sweep. Full + incremental + idempotency +
    interrupt tests all still pass. End-to-end pipeline is now wired.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build sample_monorepo fixture tree per D-22</name>
  <read_first>
    - .planning/phases/29-structural-nodes-containment-tree/29-CONTEXT.md (D-22 — fixture spec; Specifics section lines 185-206 for layout; Claude's Discretion section for extra heuristic-coverage files)
    - .planning/phases/29-structural-nodes-containment-tree/29-PATTERNS.md (Section "tests/fixtures/sample_monorepo/" — verbatim layout + heuristic-coverage additions)
    - packages/graph-io/src/graph_io/packages.py (lines 1-100 — confirm what pyproject.toml fields are required for a Package to be detected; manifest discovery rules; _SKIP_REPO_PREFIXES gating)
    - packages/graph-io/tests/_git_repo.py (init_repo + write_and_commit signatures)
  </read_first>
  <files>
    packages/graph-io/tests/fixtures/sample_monorepo/pyproject.toml,
    packages/graph-io/tests/fixtures/sample_monorepo/packages/mypkg/pyproject.toml,
    packages/graph-io/tests/fixtures/sample_monorepo/packages/mypkg/src/mypkg/__init__.py,
    packages/graph-io/tests/fixtures/sample_monorepo/packages/mypkg/src/mypkg/foo.py,
    packages/graph-io/tests/fixtures/sample_monorepo/packages/mypkg/src/mypkg/sub/__init__.py,
    packages/graph-io/tests/fixtures/sample_monorepo/packages/mypkg/src/mypkg/sub/bar.py,
    packages/graph-io/tests/fixtures/sample_monorepo/packages/mypkg/src/mypkg/sub/deep/__init__.py,
    packages/graph-io/tests/fixtures/sample_monorepo/packages/mypkg/src/mypkg/sub/deep/baz.py,
    packages/graph-io/tests/fixtures/sample_monorepo/packages/mypkg/src/mypkg/proto/data_pb2.py,
    packages/graph-io/tests/fixtures/sample_monorepo/packages/mypkg/scripts/run.sh,
    packages/graph-io/tests/fixtures/sample_monorepo/packages/mypkg/tests/test_foo.py,
    packages/graph-io/tests/fixtures/sample_monorepo/packages/jspkg/package.json,
    packages/graph-io/tests/fixtures/sample_monorepo/packages/jspkg/index.js,
    packages/graph-io/tests/fixtures/sample_monorepo/packages/jspkg/types.d.ts,
    packages/graph-io/tests/fixtures/sample_monorepo/packages/jspkg/gen/data.gen.ts,
    packages/graph-io/tests/fixtures/sample_monorepo/tests/integration/test_top.py
  </files>
  <behavior>
    - Fixture tree exists at packages/graph-io/tests/fixtures/sample_monorepo/
    - Tree covers: Python src-layout package (mypkg), JS package (jspkg),
      Python __init__.py at depths 1, 2, 3 (mypkg, mypkg.sub, mypkg.sub.deep),
      package-local tests/ (mypkg/tests/test_foo.py), repo-root tests/
      (tests/integration/test_top.py), .d.ts file (types.d.ts), _pb2.py
      generated file (proto/data_pb2.py), shebang script (scripts/run.sh)
    - Files have minimal contents (single line `pass` or `# placeholder`)
    - mypkg/pyproject.toml is valid TOML with [project] name = "mypkg"
    - jspkg/package.json is valid JSON with "name": "jspkg"
  </behavior>
  <action>
    Build the sample_monorepo fixture tree under
    packages/graph-io/tests/fixtures/sample_monorepo/. Layout per
    29-CONTEXT.md Specifics block + 29-PATTERNS.md "sample_monorepo" section.

    File contents (minimal):

    1. Root pyproject.toml — workspace marker only, NO [project] table, so it
       is NOT detected as a Package by packages.py manifest discovery. Use:
         [tool.uv.workspace]
         members = ["packages/*"]
       Verify by reading packages.py: if root manifests without [project] are
       skipped, this works. If they are not, OMIT the root pyproject.toml file
       entirely — the test should result in exactly 2 Packages (mypkg + jspkg).

    2. packages/mypkg/pyproject.toml:
         [project]
         name = "mypkg"
         version = "0.0.0"

    3. packages/mypkg/src/mypkg/__init__.py:
         """mypkg top level."""

    4. packages/mypkg/src/mypkg/foo.py:
         def foo() -> int:
             return 1

    5. packages/mypkg/src/mypkg/sub/__init__.py:
         """mypkg.sub."""

    6. packages/mypkg/src/mypkg/sub/bar.py:
         def bar() -> int:
             return 2

    7. packages/mypkg/src/mypkg/sub/deep/__init__.py:
         """mypkg.sub.deep."""

    8. packages/mypkg/src/mypkg/sub/deep/baz.py:
         def baz() -> int:
             return 3

    9. packages/mypkg/src/mypkg/proto/data_pb2.py (D-11 is_generated coverage):
         # @generated by protoc
         DESCRIPTOR = None
       NOTE: no __init__.py in proto/. That keeps it from being a SubPackage
       per D-04 — intentional to verify the generated file lives at the right
       tree layer.

    10. packages/mypkg/scripts/run.sh (D-12 shebang coverage):
         #!/bin/bash
         echo "run"

    11. packages/mypkg/tests/test_foo.py (D-09 test_*.py, D-14 parented by Repo):
         def test_foo() -> None:
             assert True

    12. packages/jspkg/package.json:
         {
           "name": "jspkg",
           "version": "0.0.0",
           "main": "index.js"
         }

    13. packages/jspkg/index.js:
         module.exports = function () { return 1; };

    14. packages/jspkg/types.d.ts (D-11 is_type_only coverage):
         export declare const x: number;

    15. packages/jspkg/gen/data.gen.ts (D-11 is_generated filename coverage):
         // generated
         export const data = [];

    16. tests/integration/test_top.py (D-09 + D-14 repo-root tests/ coverage):
         def test_top() -> None:
             assert True

    Do NOT add files beyond this list — extra files dilute the invariant test
    and slow it down. Do NOT commit run.sh as executable in the fixture (that
    is set via os.chmod by the test if needed; checking in exec bits is
    platform-fragile).
  </action>
  <verify>
    <automated>ls packages/graph-io/tests/fixtures/sample_monorepo/packages/mypkg/src/mypkg/sub/deep/__init__.py packages/graph-io/tests/fixtures/sample_monorepo/packages/jspkg/types.d.ts packages/graph-io/tests/fixtures/sample_monorepo/packages/mypkg/proto/data_pb2.py packages/graph-io/tests/fixtures/sample_monorepo/tests/integration/test_top.py packages/graph-io/tests/fixtures/sample_monorepo/packages/mypkg/scripts/run.sh && python -c "import tomllib; tomllib.loads(open('packages/graph-io/tests/fixtures/sample_monorepo/packages/mypkg/pyproject.toml').read())" && python -c "import json; json.loads(open('packages/graph-io/tests/fixtures/sample_monorepo/packages/jspkg/package.json').read())"</automated>
  </verify>
  <acceptance_criteria>
    - All 16 fixture files listed in files_modified exist on disk
    - mypkg/pyproject.toml parses as valid TOML and has [project] name="mypkg"
    - jspkg/package.json parses as valid JSON and has "name": "jspkg"
    - mypkg has __init__.py at depths 1, 2, 3 (mypkg, mypkg/sub, mypkg/sub/deep)
    - data_pb2.py first non-blank line contains "@generated"
    - run.sh first line is "#!/bin/bash"
    - types.d.ts has the .d.ts extension
    - data.gen.ts file path ends in ".gen.ts"
  </acceptance_criteria>
  <done>
    Sixteen fixture files created with minimal contents that exercise every
    role-flag heuristic and tree-shape rule in Plan 03's emitter. No file is
    executable. TOML and JSON manifests parse cleanly.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: STRUCT-04 strict-tree invariant test against sample_monorepo</name>
  <read_first>
    - .planning/phases/29-structural-nodes-containment-tree/29-CONTEXT.md (D-22 — exact assertions: (1) GROUP BY child_id HAVING COUNT > 1 returns zero rows; (2) describe-package subtree contains zero is_test=true files; (3) single Repository node with expected URI)
    - .planning/phases/29-structural-nodes-containment-tree/29-PATTERNS.md (Section "test_structural_nodes.py" — test_physically_contains_is_strict_tree analog code; uses copytree + init_repo + update.run; SQL aggregation pattern)
    - packages/graph-io/tests/_git_repo.py (init_repo, write_and_commit — used to give the fixture a git history so update.run sees a tracked HEAD)
    - packages/graph-io/tests/test_cli_smoke.py (analog for tmp_path + init_repo + update.run integration pattern, lines 22-45)
    - packages/graph-io/src/graph_io/update.py (run() signature; how it derives workspace + db_path; how RepoContext is derived in local mode)
    - workspace_io.paths.graph_dir signature for locating code.db after update.run completes
  </read_first>
  <files>packages/graph-io/tests/test_structural_nodes.py</files>
  <behavior>
    - test_physically_contains_is_strict_tree copies sample_monorepo to tmp_path,
      init-repos it, commits, then calls update.run(tmp_path, full=True)
    - After update.run, SQL: SELECT dst, COUNT(*) FROM edges WHERE kind='physically_contains' GROUP BY dst HAVING COUNT(*) > 1 returns zero rows
    - Same test asserts: SELECT COUNT(*) FROM nodes WHERE kind='repository' = 1
    - Same test asserts: there is at least one Repository → Package physically_contains edge (Repository → mypkg, Repository → jspkg)
    - Same test asserts: every node with kind='file' AND attrs has is_test=true has its physically_contains parent edge from a Repository node (NOT from a Package or SubPackage)
    - Same test asserts: at least one SubPackage node exists (mypkg.sub or mypkg.sub.deep) — proves the Python walk fires
    - Same test asserts: zero SubPackage nodes have a name matching "jspkg" or any js-related path — proves D-18 language gating
    - Same test asserts: no node has name in {"packages", "tests", "libs", "apps", "shared", "common"} (D-15 — generic containers never become nodes)
  </behavior>
  <action>
    Add `test_physically_contains_is_strict_tree` to
    packages/graph-io/tests/test_structural_nodes.py. This is the canonical
    Phase 29 invariant test (D-22, STRUCT-04). Mirror the pattern in
    29-PATTERNS.md "test_structural_nodes.py" section + the
    tmp_path + init_repo + update.run pattern from test_cli_smoke.py:22-45.

    Test body sketch:

      def test_physically_contains_is_strict_tree(tmp_path: Path) -> None:
          """STRUCT-04 / D-22: every node has exactly one physically_contains parent."""
          import shutil
          import sqlite3
          from graph_io import update
          from workspace_io.paths import graph_dir
          from workspace_io.workspace import resolve_workspace

          fixture_src = Path(__file__).parent / "fixtures" / "sample_monorepo"
          shutil.copytree(fixture_src, tmp_path, dirs_exist_ok=True)
          init_repo(tmp_path)
          write_and_commit(tmp_path, {}, "init")   # commit the copied tree

          update.run(tmp_path, full=True)

          ws = resolve_workspace(tmp_path, require_manifest=False).workspace
          db_path = graph_dir(ws) / "code.db"
          with sqlite3.connect(db_path) as probe:
              # Assertion 1: strict tree -- no child has >1 structural parent
              dupes = probe.execute(
                  "SELECT dst, COUNT(*) FROM edges WHERE kind='physically_contains' "
                  "GROUP BY dst HAVING COUNT(*) > 1"
              ).fetchall()
              assert dupes == [], f"Nodes with multiple structural parents: {dupes}"

              # Assertion 2: exactly one Repository node
              n_repos = probe.execute(
                  "SELECT COUNT(*) FROM nodes WHERE kind='repository'"
              ).fetchone()[0]
              assert n_repos == 1, f"Expected 1 Repository node, got {n_repos}"

              # Assertion 3: Repository contains Packages (mypkg + jspkg)
              pkg_parents = probe.execute(
                  "SELECT n.kind FROM edges e "
                  "JOIN nodes n ON n.id = e.src "
                  "WHERE e.kind='physically_contains' "
                  "AND e.dst IN (SELECT id FROM nodes WHERE kind='package')"
              ).fetchall()
              assert all(row[0] == 'repository' for row in pkg_parents), \
                  f"Some Packages have non-Repository parents: {pkg_parents}"

              # Assertion 4: test files are parented by Repository, not Package/SubPackage
              test_file_parents = probe.execute(
                  "SELECT DISTINCT n.kind FROM edges e "
                  "JOIN nodes n ON n.id = e.src "
                  "WHERE e.kind='physically_contains' "
                  "AND e.dst IN ("
                  "  SELECT id FROM nodes WHERE kind='file' "
                  "  AND json_extract(attrs_json, '$.is_test') = 1"
                  ")"
              ).fetchall()
              parent_kinds = {row[0] for row in test_file_parents}
              assert parent_kinds == {'repository'} or parent_kinds == set(), \
                  f"Test files have non-Repository parents: {parent_kinds}"

              # Assertion 5: SubPackage walk fires for Python (mypkg)
              n_subpkgs = probe.execute(
                  "SELECT COUNT(*) FROM nodes WHERE kind='subpackage'"
              ).fetchone()[0]
              assert n_subpkgs >= 2, f"Expected >=2 SubPackage nodes (mypkg.sub, mypkg.sub.deep), got {n_subpkgs}"

              # Assertion 6: D-18 — no SubPackage for jspkg
              jspkg_subpkgs = probe.execute(
                  "SELECT name FROM nodes WHERE kind='subpackage' AND name LIKE 'jspkg%'"
              ).fetchall()
              assert jspkg_subpkgs == [], f"JS package should not produce SubPackages: {jspkg_subpkgs}"

              # Assertion 7: D-15 — generic container dirs are never node names
              bad = probe.execute(
                  "SELECT kind, name FROM nodes WHERE name IN "
                  "('packages', 'tests', 'libs', 'apps', 'shared', 'common')"
              ).fetchall()
              assert bad == [], f"Generic container dirs leaked into nodes: {bad}"

    Notes:
      - If json_extract is not available in the test sqlite (depends on
        sqlite build), substitute a Python-side filter: pull all is_test=true
        file IDs by parsing attrs_json in Python, then SQL-join their parents.
      - resolve_workspace import path may differ; check workspace_io package
        for the right entry point. Adjust on read.
      - write_and_commit with an empty dict will fail if it requires at least
        one file; if so, write a dummy .keep file or use `git add . && git commit`
        directly. Verify by reading _git_repo.py.

    Total test addition: ~70-90 lines. Keep imports at the test top alongside
    the file's existing imports — do NOT duplicate `import sqlite3`.
  </action>
  <verify>
    <automated>cd packages/graph-io && uv run --package graph-io pytest tests/test_structural_nodes.py::test_physically_contains_is_strict_tree -x -v</automated>
  </verify>
  <acceptance_criteria>
    - grep -n 'def test_physically_contains_is_strict_tree' packages/graph-io/tests/test_structural_nodes.py returns 1 line
    - grep -n 'sample_monorepo' packages/graph-io/tests/test_structural_nodes.py returns at least 1 line
    - grep -n 'GROUP BY dst HAVING COUNT' packages/graph-io/tests/test_structural_nodes.py returns 1 line
    - grep -n "kind='repository'" packages/graph-io/tests/test_structural_nodes.py returns at least 1 line
    - grep -n "kind='subpackage'" packages/graph-io/tests/test_structural_nodes.py returns at least 1 line
    - uv run --package graph-io pytest tests/test_structural_nodes.py::test_physically_contains_is_strict_tree -x exits 0
    - uv run --package graph-io pytest -q exits 0 (full graph-io test suite green, no regressions)
  </acceptance_criteria>
  <done>
    The strict-tree invariant test exists and passes against the
    sample_monorepo fixture, asserting all five Phase 29 ROADMAP success
    criteria touched by Plan 04 (single Repository, SubPackage for Python
    only, no test files under Package, no generic containers as nodes,
    physically_contains is a strict tree).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internal-only | All changes are inside scanner code + test fixtures + test runner. No external trust boundary crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-12 | Tampering | sample_monorepo fixture committed to repo could be modified by a malicious contributor to bypass the invariant test | accept | code review process is the gate. The fixture is small enough (~16 files, all trivial contents) that any modification is human-visible in a diff. |
| T-29-13 | Tampering | shutil.copytree call in the test could be tricked into traversing a symlink out of the fixture | accept | fixture is committed source; no untrusted input. dirs_exist_ok=True does not follow symlinks by default in Python 3.11+. |
| T-29-14 | Denial of Service | the integration test runs full cg update on a real-ish tree; could be slow | accept | fixture is intentionally tiny (16 files); test should complete in <2s. If it grows past 5s in CI, profile before adding more files. |
| T-29-15 | Information Disclosure | n/a — test runs only on fixture data | accept | no real secrets in fixture |
| T-29-SC | Tampering | npm/pip/cargo installs | n/a | no package installs in this plan |
</threat_model>

<verification>
- uv run --package graph-io pytest tests/test_structural_nodes.py -v — Plan 03 unit tests + Plan 04 invariant test all pass
- uv run --package graph-io pytest -q — full graph-io suite green (no regressions in test_update_*, test_resolve, test_packages, test_upsert, test_uri, test_smoke, test_e2e, test_cli_*)
- Spot smoke: cd to a tmp clone of agent-research itself, run uv run --package graph-io cg update --full, then sqlite3 ~/.graph-wiki/.../code.db 'SELECT COUNT(*) FROM nodes WHERE kind="repository"' returns 1
- ROADMAP SC verification — Phase 29 success criterion #5 (cg describe-package graph-io lists no test files in its physically_contains subtree) is exercised by Assertion 4 in the invariant test; Criterion #3 (Repository preserved across two sweeps) is covered by Plan 02's sentinel test plus the fact that update.run runs sweep after every emit
</verification>

<success_criteria>
- structural_nodes.emit is wired into update.run() at the correct position
- update.run() with full=True or full=False both produce a Repository node
- sample_monorepo fixture exists and covers all 7 role-flag rules + tree-shape rules
- test_physically_contains_is_strict_tree passes — STRUCT-04 invariant proven
- Repository count = 1 after update; survives a second resolve.sweep
- No test files appear under Package physically_contains subtree
- Generic container directories never appear as node names
- Full graph-io test suite green; no regressions
</success_criteria>

<output>
Create `.planning/phases/29-structural-nodes-containment-tree/29-04-SUMMARY.md` when done.
</output>
