---
phase: 29-structural-nodes-containment-tree
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/graph-io/src/graph_io/resolve.py
  - packages/graph-io/tests/test_resolve.py
autonomous: true
requirements:
  - STRUCT-06
tags:
  - graph-io
  - resolve-sweep
  - structural-guard

must_haves:
  truths:
    - "resolve.sweep deletes orphan AST nodes (path IS NULL AND uri IS NULL) (D-16)"
    - "resolve.sweep PRESERVES nodes with path=NULL but uri!=NULL (Repository, Domain, TestSuite, EntryPoint) (D-16)"
    - "Sentinel regression test asserts URI-bearing Repository (path=NULL, uri='repo:test/x') survives sweep while orphan AST node (path=NULL, uri=NULL) is deleted (D-17)"
    - "Pre-existing test_resolve.py tests still pass (no regressions)"
  artifacts:
    - path: "packages/graph-io/src/graph_io/resolve.py"
      provides: "sweep() with WHERE clause extended `AND uri IS NULL` (D-16)"
      contains: "uri IS NULL"
    - path: "packages/graph-io/tests/test_resolve.py"
      provides: "Sentinel test test_sweep_preserves_uri_bearing_structural_nodes (D-17)"
      contains: "test_sweep_preserves_uri_bearing_structural_nodes"
  key_links:
    - from: "resolve.sweep DELETE statement"
      to: "nodes table"
      via: "WHERE path IS NULL AND uri IS NULL AND kind != 'package' AND id NOT IN (SELECT dst FROM edges)"
      pattern: "uri IS NULL"
---

<objective>
Implement STRUCT-06 (D-16) and lock it with the D-17 sentinel regression test.
This is the structural-node safety net for Phase 29 onward: `resolve.sweep`
must distinguish "URI-bearing structural node with no filesystem path" (keep)
from "orphan AST node left behind by a deleted source file" (delete).

Purpose: PITFALL 5 — `resolve.sweep` silently deleting Repository/Domain/
TestSuite/EntryPoint nodes — is the centerpiece risk Phase 29 prevents. This
single one-line WHERE-clause edit, locked by a sentinel test, makes the sweep
self-maintaining for all future structural kinds added in v1.7+.

Output: `resolve.sweep` predicate updated; sentinel test asserts a URI-bearing
Repository node with `path=NULL` survives a sweep while a URI-less orphan AST
function is deleted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-structural-nodes-containment-tree/29-CONTEXT.md
@.planning/phases/29-structural-nodes-containment-tree/29-PATTERNS.md
@.planning/phases/28-schema-v2-uri-foundation/28-CONTEXT.md
@packages/graph-io/src/graph_io/resolve.py
@packages/graph-io/tests/test_resolve.py
@packages/graph-io/src/graph_io/upsert.py

<interfaces>
<!-- Existing sweep() signature and the pop-uri-to-column path (Phase 28 D-10) -->

From packages/graph-io/src/graph_io/resolve.py (lines ~53-56):
```
def sweep(conn: sqlite3.Connection) -> None:
    # Existing DELETE:
    conn.execute(
        "DELETE FROM nodes WHERE path IS NULL AND kind != 'package' "
        "AND id NOT IN (SELECT dst FROM edges)"
    )
```

From packages/graph-io/src/graph_io/upsert.py (lines ~48-59) — Phase 28 D-10 locked this:
```
def _upsert_node(conn: sqlite3.Connection, node: GraphNode) -> int:
    attrs_for_json = dict(node.attrs)
    uri_value = attrs_for_json.pop("uri", None)  # pops URI from attrs to dedicated column
    # ... writes uri_value to nodes.uri column ...
```

This means: any GraphNode with `attrs={"uri": "..."}` lands the URI in the
`uri` column, not inside `attrs_json`. The new sweep predicate reads the
column directly.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add D-17 sentinel test (RED) before edit</name>
  <read_first>
    - .planning/phases/29-structural-nodes-containment-tree/29-CONTEXT.md (D-16 and D-17 — exact predicate change rationale, exact sentinel test specification)
    - .planning/phases/29-structural-nodes-containment-tree/29-PATTERNS.md (Section "tests/test_structural_nodes.py" — sentinel test shape; note this test lives in test_resolve.py NOT test_structural_nodes.py because it tests resolve.sweep)
    - packages/graph-io/tests/test_resolve.py (full file — conn fixture lines 15-20; existing test_sweep_is_idempotent at line 159; sweep test patterns)
    - packages/graph-io/src/graph_io/resolve.py (current sweep() implementation — verify exact line numbers of WHERE clause)
    - packages/graph-io/src/graph_io/upsert.py (confirm GraphNode upsert path; _upsert_node pops uri attr → uri column)
  </read_first>
  <files>packages/graph-io/tests/test_resolve.py</files>
  <behavior>
    - Test: insert one node kind=repository, path=None, attrs={"uri": "repo:test/x"} via upsert.upsert_records
    - Test: insert one node kind=function, path=None, attrs={} via the same call (orphan AST node)
    - Action: call resolve.sweep(conn)
    - Assertion 1: SELECT kind FROM nodes shows 'repository' still present
    - Assertion 2: SELECT kind FROM nodes shows 'function' is gone
    - Test name: test_sweep_preserves_uri_bearing_structural_nodes
    - This test MUST FAIL before Task 2 runs (current predicate deletes both)
  </behavior>
  <action>
    Add the D-17 sentinel test to `packages/graph-io/tests/test_resolve.py` per
    the pattern in 29-PATTERNS.md "tests/test_structural_nodes.py" section
    (re-located here because the sweep is the unit under test). Mirror the
    existing conn fixture + test-naming convention already in the file.

    Add this test function near the existing `test_sweep_is_idempotent` (around
    line 159 — verify exact location while reading):

    ```
    def test_sweep_preserves_uri_bearing_structural_nodes(conn: sqlite3.Connection) -> None:
        """STRUCT-06 / D-17: Repository (path=NULL, uri='repo:...') survives sweep;
        orphan AST node (path=NULL, uri=NULL) is deleted."""
        from source_parser.projections.graph import GraphNode, GraphRecords
        from graph_io import upsert
        upsert.upsert_records(
            conn,
            GraphRecords(
                nodes=[
                    GraphNode(
                        kind="repository", name="x", path=None, line=None,
                        attrs={"uri": "repo:test/x"},
                    ),
                    GraphNode(
                        kind="function", name="orphan", path=None, line=None, attrs={},
                    ),
                ],
                edges=[],
            ),
        )
        resolve.sweep(conn)
        kinds = {row[0] for row in conn.execute("SELECT kind FROM nodes").fetchall()}
        assert "repository" in kinds, "Repository node was deleted by sweep (STRUCT-06 violated)"
        assert "function" not in kinds, "Orphan AST node was not cleaned by sweep"
    ```

    Imports: verify whether GraphNode/GraphRecords are already imported at the
    top of test_resolve.py; if so, hoist these imports to the top. If not,
    in-test imports are acceptable for a one-off sentinel.

    Confirm the test FAILS at this point by running pytest (this is the RED
    half of TDD — Task 2 makes it GREEN).
  </action>
  <verify>
    <automated>cd packages/graph-io && uv run --package graph-io pytest tests/test_resolve.py::test_sweep_preserves_uri_bearing_structural_nodes -x -q || echo "EXPECTED-RED"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'def test_sweep_preserves_uri_bearing_structural_nodes' packages/graph-io/tests/test_resolve.py` returns 1 line
    - `grep -n 'attrs={"uri": "repo:test/x"}' packages/graph-io/tests/test_resolve.py` returns 1 line
    - Running the test BEFORE Task 2's edit yields a FAIL (Repository was deleted) — confirms the test actually exercises the predicate, not a vacuous pass
  </acceptance_criteria>
  <done>
    Sentinel test added; currently FAILING (RED state); Task 2 will turn it GREEN.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Edit resolve.sweep WHERE clause to add `AND uri IS NULL` (GREEN)</name>
  <read_first>
    - .planning/phases/29-structural-nodes-containment-tree/29-CONTEXT.md (D-16 — exact text "append `AND uri IS NULL` to the WHERE clause")
    - .planning/phases/29-structural-nodes-containment-tree/29-PATTERNS.md (Section "resolve.py MODIFY — D-16 one-clause SQL edit" — before/after SQL)
    - packages/graph-io/src/graph_io/resolve.py (current sweep() implementation — locate exact line of DELETE statement)
    - packages/graph-io/tests/test_resolve.py (existing test_sweep_is_idempotent and other sweep tests — these must continue to pass after the predicate change)
  </read_first>
  <files>packages/graph-io/src/graph_io/resolve.py</files>
  <behavior>
    - sweep() DELETE statement WHERE clause is `WHERE path IS NULL AND uri IS NULL AND kind != 'package' AND id NOT IN (SELECT dst FROM edges)`
    - test_sweep_preserves_uri_bearing_structural_nodes passes (GREEN)
    - All pre-existing tests in test_resolve.py continue to pass (no regression)
    - test_sweep_is_idempotent still passes (idempotency preserved)
  </behavior>
  <action>
    Implement D-16 per 29-PATTERNS.md "resolve.py MODIFY" section. Edit the
    single DELETE statement in `packages/graph-io/src/graph_io/resolve.py`
    (the `sweep` function, around lines 53-56) — append `AND uri IS NULL` to
    the WHERE clause:

    BEFORE:
        conn.execute(
            "DELETE FROM nodes WHERE path IS NULL AND kind != 'package' "
            "AND id NOT IN (SELECT dst FROM edges)"
        )

    AFTER:
        conn.execute(
            "DELETE FROM nodes WHERE path IS NULL AND uri IS NULL AND kind != 'package' "
            "AND id NOT IN (SELECT dst FROM edges)"
        )

    This is the entire functional change. Do NOT refactor surrounding code; do
    NOT change function signature; do NOT add comments unrelated to D-16. If a
    brief inline comment helps, ONE line referencing D-16 / STRUCT-06 is fine:

        # D-16 / STRUCT-06: spare URI-bearing structural nodes (Repository,
        # Domain, TestSuite, EntryPoint) — they have no path but are not orphans.

    Self-maintaining property (D-16): adding a new structural kind in v1.7
    requires no edit here as long as that kind carries a `uri`.
  </action>
  <verify>
    <automated>cd packages/graph-io && uv run --package graph-io pytest tests/test_resolve.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'uri IS NULL' packages/graph-io/src/graph_io/resolve.py` returns at least 1
    - `grep -n 'DELETE FROM nodes WHERE path IS NULL AND uri IS NULL AND kind' packages/graph-io/src/graph_io/resolve.py` returns 1 line (the full new predicate)
    - `uv run --package graph-io pytest tests/test_resolve.py::test_sweep_preserves_uri_bearing_structural_nodes -x` exits 0 (GREEN)
    - `uv run --package graph-io pytest tests/test_resolve.py -x` exits 0 (all pre-existing tests still pass — no regression)
    - `uv run --package graph-io pytest tests/test_resolve.py::test_sweep_is_idempotent -x` exits 0 (idempotency invariant preserved)
  </acceptance_criteria>
  <done>
    sweep() predicate updated, sentinel test GREEN, no regressions in test_resolve.py.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internal-only | resolve.sweep is internal scanner code reading/writing the local SQLite store. No external trust boundary crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-04 | Tampering | `uri IS NULL` predicate could be defeated by a malicious actor inserting a fake `uri` on an orphan AST node to prevent cleanup | accept | the scanner is the only writer; no external input reaches `_upsert_node`. Defending against a malicious scanner requires defending against arbitrary code execution, out of scope at ASVS L1 |
| T-29-05 | Denial of Service | the extended WHERE clause adds one boolean test, no extra subquery — performance is identical to the prior predicate | accept | no change to query plan |
| T-29-06 | Information Disclosure | none — sweep deletes rows; reads no new data | accept | no surface area |
| T-29-SC | Tampering | npm/pip/cargo installs | n/a | no package installs in this plan |
</threat_model>

<verification>
- `uv run --package graph-io pytest tests/test_resolve.py -v` — all tests pass including the new sentinel
- `uv run --package graph-io pytest tests/test_upsert.py -v` — confirms `_upsert_node` pop-uri-to-column path still works (Phase 28 D-10 invariant)
- Spot-check the WHERE clause is exactly the expected form:
  `grep -n 'DELETE FROM nodes' packages/graph-io/src/graph_io/resolve.py`
</verification>

<success_criteria>
- `resolve.sweep` deletes path-NULL uri-NULL non-package non-referenced nodes (orphan AST)
- `resolve.sweep` preserves path-NULL uri-bearing nodes (structural nodes)
- D-17 sentinel test `test_sweep_preserves_uri_bearing_structural_nodes` exists and passes
- No regressions in existing test_resolve.py tests (including `test_sweep_is_idempotent`)
- Plan 03 can safely emit Repository / SubPackage with `path=NULL` (Repository) or
  `path=<rel>` (SubPackage/File) — neither will be deleted by the sweep
</success_criteria>

<output>
Create `.planning/phases/29-structural-nodes-containment-tree/29-02-SUMMARY.md` when done.
</output>
