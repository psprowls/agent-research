---
phase: 29-structural-nodes-containment-tree
plan: 03
type: execute
wave: 2
depends_on:
  - 29-01
  - 29-02
files_modified:
  - packages/graph-io/src/graph_io/structural_nodes.py
  - packages/graph-io/tests/test_structural_nodes.py
autonomous: true
requirements:
  - STRUCT-01
  - STRUCT-02
  - STRUCT-03
  - STRUCT-05
  - SPARSER-02
tags:
  - graph-io
  - structural-nodes
  - emitter

must_haves:
  truths:
    - "structural_nodes.emit emits exactly one Repository node per call with uri=repo_uri(ctx), and attrs include name=ctx.repo, owner=ctx.org, url (git origin or local FS path), default_branch from git symbolic-ref (NULL on detached HEAD) (D-01)"
    - "Local-fallback mode (no parseable origin remote) STILL emits a Repository node with owner='local' and url=str(repo_root.absolute()) — the structural tree always has a root (D-02)"
    - "Repository has path=None and is the unique root of physically_contains edges; every Package node is physically_contains-parented by Repository in this phase (D-03)"
    - "SubPackage nodes are emitted ONLY for __init__.py-containing subdirectories; PEP 420 namespace packages are NOT emitted in v1.6 (D-04)"
    - "SubPackage walk depth is unlimited — every __init__.py below a Package's import root produces a SubPackage node, no depth cap (D-05)"
    - "Per-Package import-root discovery uses src-layout probe (<pkg.path>/src/<importable>/__init__.py) then flat-layout probe (<pkg.path>/<importable>/__init__.py), with <importable> = pkg_name with '-' → '_' (D-06)"
    - "SubPackage dotted_path is the full Python import path including the top-level importable (e.g. graph_io.cli, not just cli) (D-07)"
    - "Each SubPackage's physically_contains parent is the deepest enclosing SubPackage, or the Package if it sits directly under the import root (top-level SubPackage is contained by Package, not Repository) (D-08)"
    - "File.is_test is true if path traverses a tests/, __tests__/, or test/ directory at any depth, OR filename matches test_*.py / *_test.py / *.test.{js,ts,tsx,jsx} / *.spec.{js,ts,tsx,jsx} (D-09)"
    - "File.is_config is true for an exact-match allow-list of Python + JS/TS ecosystem manifests (pyproject.toml, package.json, tsconfig*.json, *.config.{js,ts,mjs,cjs}, Makefile, etc.) (D-10)"
    - "File.is_generated combines filename patterns (*_pb2.py, *.gen.{ts,go}, *.generated.{ts,go}, files in __generated__/ or generated/) with a content-marker scan of the first 20 lines for @generated, 'Code generated by', or 'DO NOT EDIT', skipping files over 1 MB (D-11)"
    - "File.is_type_only is true for filenames ending in .d.ts or .pyi (D-11)"
    - "File.is_executable is true if the OS executable bit is set OR the first line begins with '#!' (shebang scan only for extensions .py/.sh/.bash/.zsh/.js/.ts/.rb/.pl or no extension) (D-12)"
    - "physically_contains edges form the strict tree: Repository → Package; Python: Package → SubPackage [→ SubPackage]* → File; JS/TS: Package → File (no SubPackage layer); Package-level files (setup.py, root conftest.py, pyproject.toml, package.json) are contained by Package directly (D-13)"
    - "Test files emit as File nodes with is_test=true and are physically_contains-parented by Repository in Phase 29 (NOT by Package or SubPackage) — Phase 30 will re-parent them under TestSuite (D-14)"
    - "Generic container directories (packages/, libs/, tests/, apps/, shared/, common/) are NEVER emitted as nodes — they only appear inside path attrs (D-15)"
    - "structural_nodes.emit decides per-Package whether to walk for SubPackages by reading the Package node's language attr — only language=='python' triggers the SubPackage walk; JS/TS and any other language emits Files directly under Package with no SubPackage layer (D-18)"
    - "structural_nodes.emit reads _has_main_block and _has_importable_symbols from each Python File's SourceNode attrs (landed by SPARSER-01) and writes them as has_main and is_importable on the corresponding File node; JS/TS files default has_main=False, is_importable=True (D-20)"
    - "Every File node carries all seven role-flag attrs: is_importable, is_executable, has_main, is_test, is_config, is_generated, is_type_only"
    - "Wave ordering (29-01 before 29-03) honors D-21 sequencing: SPARSER-01 attrs ship in Wave 1 before structural_nodes.emit consumes them in Wave 2 (D-21)"
  artifacts:
    - path: "packages/graph-io/src/graph_io/structural_nodes.py"
      provides: "emit(conn, *, repo_root, ctx, skip_dirs) -- Repository + SubPackage + File emission with role flags"
      contains: "def emit"
      exports:
        - emit
    - path: "packages/graph-io/tests/test_structural_nodes.py"
      provides: "Unit tests for Repository emission, SubPackage walk, role-flag heuristics, language gating"
      contains: "test_emit_repository_node_single"
  key_links:
    - from: "structural_nodes.emit"
      to: "Repository node (kind='repository')"
      via: "_detect_default_branch + repo_uri(ctx) -> upsert with attrs[uri]"
      pattern: "kind=\"repository\""
    - from: "structural_nodes.emit"
      to: "SubPackage nodes"
      via: "_resolve_import_root + os.walk for __init__.py"
      pattern: "kind=\"subpackage\""
    - from: "structural_nodes.emit"
      to: "File nodes"
      via: "FS walk + _ignore.should_skip + role-flag heuristics"
      pattern: "kind=\"file\""
    - from: "Python File.attrs"
      to: "source-parser SourceNode.attrs"
      via: "read nodes WHERE kind='file' AND path=? attrs_json -> _has_main_block / _has_importable_symbols"
      pattern: "_has_main_block|_has_importable_symbols"
---

<objective>
Implement the structural-nodes emitter: a new module
`packages/graph-io/src/graph_io/structural_nodes.py` with a single public
`emit(conn, *, repo_root, ctx, skip_dirs)` function that produces

  - exactly one Repository node (STRUCT-01 / D-01..D-03)
  - SubPackage nodes for every `__init__.py`-containing subdir below each
    Python Package's import root (STRUCT-02 / D-04..D-08, D-18)
  - File nodes with the seven role-flag attrs (STRUCT-03 / D-09..D-12, D-20)

…and ensures generic container directories (`packages/`, `tests/`, etc.) are
never emitted as nodes (STRUCT-05 / D-15).

Purpose: This is the bulk of Phase 29. The emitter is the load-bearing piece
that all later structural queries (Phase 30 EntryPoint, Phase 31 Domain,
Phase 32 query layer) build on. The strict-tree invariant (STRUCT-04) is
tested in Plan 04's end-to-end fixture; this plan tests each emission rule
in isolation.

Output: `structural_nodes.emit` exists, is unit-tested per emission rule,
and consumes SPARSER attrs landed by Plan 01.

Note: `update.py` is NOT wired in this plan — Plan 04 inserts the call. The
emitter is independently invocable from tests via a seeded `conn` + mini
`tmp_path` tree.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-structural-nodes-containment-tree/29-CONTEXT.md
@.planning/phases/29-structural-nodes-containment-tree/29-PATTERNS.md
@packages/graph-io/src/graph_io/packages.py
@packages/graph-io/src/graph_io/update.py
@packages/graph-io/src/graph_io/uri.py
@packages/graph-io/src/graph_io/upsert.py
@packages/graph-io/src/graph_io/_ignore.py
@packages/graph-io/tests/test_packages.py
@packages/graph-io/tests/test_resolve.py

<interfaces>
<!-- Existing interfaces structural_nodes.emit consumes. Extracted from codebase. -->
<!-- Executor uses these directly — no exploration needed. -->

From packages/graph-io/src/graph_io/uri.py (Phase 28):
  RepoContext(org: str, repo: str)
  repo_uri(ctx) -> str                            # "repo:org/repo"
  pkg_uri(ctx, name) -> str                       # "pkg:org/repo/name"
  subpkg_uri(ctx, pkg_name, dotted) -> str        # "subpkg:org/repo/pkg/dotted.path"
  file_uri(ctx, rel_path) -> str                  # "file:org/repo/rel/path.py"

From packages/graph-io/src/graph_io/upsert.py (Phase 28 D-10 locked):
  upsert_records(conn, GraphRecords(nodes=[GraphNode...], edges=[GraphEdge...]))
  _upsert_node pops attrs["uri"] -> nodes.uri column automatically

From packages/graph-io/src/graph_io/packages.py (already in DB after packages.refresh):
  Package node attrs include language: "python" | "javascript"
  Package node has `name` (manifest name, e.g. "graph-io") and `path` (relative dir)

From packages/graph-io/src/graph_io/update.py:
  _git(["symbolic-ref", "--short", "refs/remotes/origin/HEAD"], cwd=repo_root)
    -> str  (raises NotInGitRepoError on failure; reuse this, do NOT import subprocess)
  NotInGitRepoError                              # exception class to catch

From packages/graph-io/src/graph_io/_ignore.py:
  should_skip(path_str: str, skip_dirs: frozenset[str]) -> bool
  load_skip_dirs(repo_root: Path) -> frozenset[str]

From source_parser.projections.graph (already imported by packages.py):
  GraphNode(kind: str, name: str, path: str|None, line: int|None, attrs: dict)
  GraphEdge(src: tuple, dst: tuple, kind: str, attrs: dict)
  GraphRecords(nodes: list, edges: list)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create structural_nodes.py with Repository emission + role-flag heuristics + module constants</name>
  <read_first>
    - .planning/phases/29-structural-nodes-containment-tree/29-CONTEXT.md (D-01 through D-15 and D-18, D-20 — exact attr names, exact heuristic rules, what's NOT emitted)
    - .planning/phases/29-structural-nodes-containment-tree/29-PATTERNS.md (Section "structural_nodes.py (NEW emitter)" — verbatim imports, module-constants pattern, signature, _detect_default_branch helper, file-reading safety pattern, edge tuples)
    - packages/graph-io/src/graph_io/packages.py (analog — full file; signature, refresh() shape, node/edge construction, _SKIP_REPO_PREFIXES module-private constant pattern, language attr)
    - packages/graph-io/src/graph_io/uri.py (full file — confirm repo_uri/pkg_uri/subpkg_uri/file_uri signatures from Phase 28)
    - packages/graph-io/src/graph_io/upsert.py (lines around _upsert_node — confirm uri-pop-to-column path from Phase 28 D-10)
    - packages/graph-io/src/graph_io/update.py (lines around _git, _derive_repo_context — reuse _git for default_branch query; lines 141-157)
    - packages/graph-io/src/graph_io/_ignore.py (should_skip signature)
  </read_first>
  <files>packages/graph-io/src/graph_io/structural_nodes.py</files>
  <behavior>
    - emit(conn, *, repo_root, ctx, skip_dirs) is the single public callable
    - Calling emit on a tmp_path with one Package node already in conn produces:
      - 1 Repository node, kind="repository", path=None, attrs.uri = repo_uri(ctx), attrs has owner, name, url, default_branch keys
      - 1 physically_contains edge from Repository to the existing Package
      - 0 SubPackage nodes (Task 2 covers SubPackage walk)
      - File nodes for every tracked file under repo_root that survives _ignore.should_skip
    - Role-flag heuristic helpers are pure functions on Path, individually unit-testable:
      - _is_test_path(rel_path: str) -> bool — matches D-09 rules
      - _is_config_file(name: str) -> bool — matches D-10 allow-list + globs
      - _is_generated(path: Path, name: str) -> bool — D-11 patterns + content scan, 1MB cap, errors=ignore
      - _is_type_only(name: str) -> bool — D-11 .d.ts / .pyi
      - _is_executable(path: Path, name: str) -> bool — D-12 exec bit + shebang
    - Generic container directories (packages/, libs/, tests/, apps/, shared/, common/) never produce nodes (their paths only appear inside file rel paths)
  </behavior>
  <action>
    Create `packages/graph-io/src/graph_io/structural_nodes.py` implementing
    Repository emission + role-flag heuristic helpers + module constants. Mirror
    `packages.py` style exactly (29-PATTERNS.md "structural_nodes.py" section).

    Implements D-01..D-03 (Repository), D-09..D-12 (role flags), D-15 (no generic
    container nodes). Does NOT yet implement SubPackage walk or File emission
    (Task 2). Does NOT yet read SPARSER attrs (Task 2).

    Structure:

    1. Module docstring + imports per 29-PATTERNS.md "structural_nodes.py" imports block:
         from __future__ import annotations
         import fnmatch, json, os, sqlite3
         from pathlib import Path
         from typing import Any
         from source_parser.projections.graph import GraphEdge, GraphNode, GraphRecords
         from graph_io import _ignore, upsert
         from graph_io.update import NotInGitRepoError, _git
         from graph_io.uri import RepoContext, file_uri, pkg_uri, repo_uri, subpkg_uri

    2. Module-private constants per 29-PATTERNS.md "Module-private constants pattern":
         _CONFIG_FILENAMES: frozenset[str] = frozenset({...})  # D-10 exact-name allow-list
         _CONFIG_GLOBS: tuple[str, ...] = (...)                # D-10 *.config.{js,ts,...} family
         _GENERATED_FILENAME_PATTERNS: tuple[str, ...] = (...) # D-11 generated patterns
         _GENERATED_DIRS: frozenset[str] = frozenset({"__generated__", "generated"})  # D-11
         _SHEBANG_EXTENSIONS: frozenset[str] = frozenset({".py", ".sh", ".bash", ".zsh", ".js", ".ts", ".rb", ".pl", ""})  # D-12
         _TEST_DIR_NAMES: frozenset[str] = frozenset({"tests", "__tests__", "test"})  # D-09
         _TEST_FILENAME_GLOBS: tuple[str, ...] = ("test_*.py", "*_test.py", "*.test.js", "*.test.ts", "*.test.tsx", "*.test.jsx", "*.spec.js", "*.spec.ts", "*.spec.tsx", "*.spec.jsx")  # D-09
         _GENERIC_CONTAINER_DIRS: frozenset[str] = frozenset({"packages", "libs", "tests", "apps", "shared", "common"})  # D-15

    3. Helper: _detect_default_branch(repo_root: Path) -> str | None  (D-01)
       - try _git(["symbolic-ref", "--short", "refs/remotes/origin/HEAD"], cwd=repo_root).strip().removeprefix("origin/")
       - on NotInGitRepoError, fall back to _git(["symbolic-ref", "--short", "HEAD"], cwd=repo_root).strip()
       - on second NotInGitRepoError, return None (detached HEAD)
       - empty string -> None

    4. Helper: _detect_repo_url(repo_root: Path, ctx: RepoContext) -> str  (D-01, D-02)
       - if ctx.org == "local": return str(repo_root.absolute())
       - else: try _git(["remote", "get-url", "origin"], cwd=repo_root).strip(); on NotInGitRepoError return str(repo_root.absolute())

    5. Helpers: _is_test_path, _is_config_file, _is_generated, _is_type_only, _is_executable
       per D-09..D-12 exact rules. Use fnmatch.fnmatch for glob matching (stdlib,
       no new deps). For _is_generated content scan use the pattern from
       29-PATTERNS.md "File-reading safety pattern" verbatim (errors="ignore",
       1MB cap, read 20 lines, match @generated case-sensitive + "code generated by"
       and "do not edit" case-insensitive). For _is_executable use os.access(path,
       os.X_OK) OR (extension in _SHEBANG_EXTENSIONS AND first line starts with "#!")
       — open with errors="ignore", catch OSError, return False on failure.

    6. Public function: emit(conn, *, repo_root, ctx, skip_dirs) -> None
       - At this task: emit Repository node + physically_contains edges from
         Repository → each existing Package node. Do NOT yet walk for files or
         SubPackages (Task 2).
       - Build Repository GraphNode: kind="repository", name=ctx.repo, path=None, line=None,
         attrs={"uri": repo_uri(ctx), "owner": ctx.org, "name": ctx.repo,
                "url": _detect_repo_url(repo_root, ctx),
                "default_branch": _detect_default_branch(repo_root)}
       - Query existing Package nodes:
           rows = conn.execute("SELECT name, path FROM nodes WHERE kind='package'").fetchall()
       - Build physically_contains edges from Repository to each Package:
           GraphEdge(
             src=("repository", ctx.repo, None),
             dst=("package", pkg_name, pkg_path),
             kind="physically_contains",
             attrs={},
           )
       - Call upsert.upsert_records(conn, GraphRecords(nodes=[repo_node], edges=edges))

    7. Add a module-level TODO comment marking where Task 2 will insert
       SubPackage + File emission (so the diff in Task 2 is locally cohesive).

    All work consistent with packages.py module conventions (29-PATTERNS.md
    "Public function signature pattern" — keyword-only args, Path input
    re-resolved at top of function via `repo_root = Path(repo_root).resolve()`).
  </action>
  <verify>
    <automated>cd packages/graph-io && uv run --package graph-io python -c "from graph_io import structural_nodes; assert callable(structural_nodes.emit); assert callable(structural_nodes._detect_default_branch); assert callable(structural_nodes._is_test_path); assert callable(structural_nodes._is_config_file); assert callable(structural_nodes._is_generated); assert callable(structural_nodes._is_type_only); assert callable(structural_nodes._is_executable); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '^def emit' packages/graph-io/src/graph_io/structural_nodes.py` returns 1 line
    - `grep -nE '^def _(detect_default_branch|detect_repo_url|is_test_path|is_config_file|is_generated|is_type_only|is_executable)' packages/graph-io/src/graph_io/structural_nodes.py` returns 7 lines
    - `grep -n 'kind="repository"' packages/graph-io/src/graph_io/structural_nodes.py` returns at least 1 line
    - `grep -n 'physically_contains' packages/graph-io/src/graph_io/structural_nodes.py` returns at least 1 line
    - `grep -n 'repo_uri(ctx)' packages/graph-io/src/graph_io/structural_nodes.py` returns at least 1 line
    - `grep -n '_GENERIC_CONTAINER_DIRS' packages/graph-io/src/graph_io/structural_nodes.py` returns at least 1 line (D-15)
    - module imports cleanly (verify command exits 0)
    - `grep -c 'import subprocess' packages/graph-io/src/graph_io/structural_nodes.py` returns 0 (must use _git not direct subprocess per 29-PATTERNS.md)
  </acceptance_criteria>
  <done>
    structural_nodes.py exists with module constants, role-flag helpers, Repository
    emission, and a stub spot where Task 2 inserts SubPackage/File walks. No
    subprocess import. Module imports cleanly in Python.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add SubPackage walk + File emission + SPARSER attr consumption to structural_nodes.emit</name>
  <read_first>
    - .planning/phases/29-structural-nodes-containment-tree/29-CONTEXT.md (D-04 through D-08 — SubPackage rules, _resolve_import_root spec; D-13, D-14 — containment tree shape; D-15 — generic container exclusion; D-18 — language gating; D-20 — SPARSER attr read pattern)
    - .planning/phases/29-structural-nodes-containment-tree/29-PATTERNS.md (Section "structural_nodes.py" — "Package-language read pattern", "Source-parser attrs read", "URI helpers in use", "Edge-emission pattern")
    - packages/graph-io/src/graph_io/structural_nodes.py (current state from Task 1 — extend it)
    - packages/graph-io/src/graph_io/packages.py (full file — Package node `path` and `name` attrs shape; _SKIP_REPO_PREFIXES gating idea)
    - packages/graph-io/src/graph_io/uri.py (subpkg_uri + file_uri signatures)
  </read_first>
  <files>packages/graph-io/src/graph_io/structural_nodes.py</files>
  <behavior>
    - For each Package node with attrs.language=="python":
      - _resolve_import_root probes <pkg.path>/src/<importable>/__init__.py first, then <pkg.path>/<importable>/__init__.py (D-06)
      - <importable> = pkg_name.replace("-", "_")
      - If neither exists, no SubPackage nodes emitted for that Package
    - For each __init__.py-containing subdir below the import root (unlimited depth, D-05):
      - Emits SubPackage node with name=dotted_path, path=<rel to repo_root>, attrs={uri: subpkg_uri(ctx, pkg_name, dotted_path), dotted_path: dotted, language: "python"}
      - dotted_path includes top-level importable: graph_io.cli.main (D-07)
      - physically_contains edge from deepest enclosing SubPackage (or Package if directly under import root) → this SubPackage (D-08)
    - For each non-test file under each Package (tracked, _ignore.should_skip=false):
      - Emits File node with kind="file", name=basename, path=rel_to_repo, attrs={uri: file_uri(ctx, rel), is_importable, is_executable, has_main, is_test, is_config, is_generated, is_type_only, language}
      - Python files: has_main and is_importable read from existing nodes.attrs_json at (kind='file', path=rel) → look up _has_main_block / _has_importable_symbols (D-20). Default both False if attrs missing.
      - JS/TS files: has_main=False, is_importable=True (D-20)
      - physically_contains edge from deepest enclosing SubPackage (Python) OR Package (JS/TS or Python file at Package root, D-13)
    - Test files (is_test=true): physically_contains edge from Repository (D-14), NOT from Package or SubPackage
    - Generic container directories (packages/, tests/, libs/, apps/, shared/, common/) NEVER produce a node (D-15) — only File nodes whose path traverses them
    - JS/TS packages emit Files directly under Package, zero SubPackage nodes (D-18)
  </behavior>
  <action>
    Extend `structural_nodes.py` with SubPackage walk + File emission + SPARSER
    attr consumption. Insert this code at the TODO marker left by Task 1,
    inside `emit()` after the Repository node is built.

    Implements D-04..D-08, D-13..D-15, D-18, D-20, SPARSER-02.

    1. Helper: _resolve_import_root(pkg_dir: Path, importable: str) -> Path | None  (D-06)
       - if (pkg_dir / "src" / importable / "__init__.py").exists(): return pkg_dir / "src" / importable
       - elif (pkg_dir / importable / "__init__.py").exists(): return pkg_dir / importable
       - else: return None

    2. Helper: _walk_subpackages(import_root: Path) -> Iterator[Path]  (D-04, D-05)
       - Walk import_root recursively for __init__.py files
       - Yield each Path containing __init__.py (the directory)
       - Includes import_root itself (it has __init__.py) so dotted_path starts at importable
       - Honor _ignore.should_skip via the skip_dirs frozenset (pass-through arg)

    3. Helper: _dotted_path(subpkg_dir: Path, import_root: Path, importable: str) -> str  (D-07)
       - rel = subpkg_dir.relative_to(import_root.parent).as_posix()
       - parts = rel.split("/")
       - return ".".join(parts)
       - Example: import_root=packages/graph-io/src/graph_io, subpkg_dir=packages/graph-io/src/graph_io/cli
         → rel = "graph_io/cli", dotted = "graph_io.cli"

    4. Inside emit(), after the Repository node block, extend the algorithm:

       a. Re-read Package rows including attrs to get language:
          rows = conn.execute(
              "SELECT name, path, attrs_json FROM nodes WHERE kind='package'"
          ).fetchall()

       b. For each Package row:
          - parse language from attrs_json
          - if language == "python":
              importable = name.replace("-", "_")
              pkg_dir = repo_root / path  (path is relative)
              import_root = _resolve_import_root(pkg_dir, importable)
              if import_root: collect SubPackage Paths via _walk_subpackages
          - else: SubPackage list is empty for this Package
          - Collect all tracked files under pkg_dir (use repo_root.glob or os.walk;
            filter via _ignore.should_skip(rel, skip_dirs))

       c. Build SubPackage GraphNodes for each subpkg dir (D-07, D-08):
          - name = dotted_path
          - path = subpkg_dir.relative_to(repo_root).as_posix()
          - attrs = {"uri": subpkg_uri(ctx, pkg_name, dotted_path), "dotted_path": dotted_path, "language": "python"}
          - parent for physically_contains edge: deepest enclosing SubPackage (longest dotted_path prefix match) OR Package if subpkg_dir.parent == import_root.parent (top-level subpkg is contained by Package)

       d. Build File GraphNodes for each tracked file under the Package
          (D-13, D-14, D-20, SPARSER-02):
          - rel = file.relative_to(repo_root).as_posix()
          - name = file.name
          - For Python files (path.endswith(".py")): query existing File node
            attrs_json (it was already upserted by _process_files in update.py
            before this emit runs — Plan 04 wires the order). Read
            attrs.get("_has_main_block", False) and attrs.get("_has_importable_symbols", False).
            For JS/TS: has_main=False, is_importable=True.
          - Compute role-flag attrs via the helpers from Task 1.
          - attrs = {"uri": file_uri(ctx, rel), "is_importable": ..., "is_executable": ...,
                    "has_main": ..., "is_test": ..., "is_config": ..., "is_generated": ...,
                    "is_type_only": ..., "language": language}
          - parent for physically_contains edge:
              * if is_test=true: Repository (D-14)
              * elif Python and file is under a SubPackage dir: that SubPackage (deepest match)
              * else: Package

       e. ASSERT no generic container directory appears as a node (D-15). All
          generic container names live only inside `path` strings, never as
          a node `name`. Do not emit any node whose name is in
          _GENERIC_CONTAINER_DIRS.

       f. Collect all nodes and edges and call upsert.upsert_records ONCE at
          the end of emit() (combine with the Repository node from Task 1's
          step — single upsert keeps within the existing transaction scope).

    5. Note for executor: the Python file role-flag read (step d) relies on
       Plan 01 (SPARSER-01) having already landed in source-parser. This is
       guaranteed by the wave dependency (depends_on: [29-01, 29-02]).

    Do NOT call upsert twice inside emit() — collect everything and call
    upsert_records once. Do NOT open a new transaction (the caller's
    transaction wraps this call per 29-PATTERNS.md "Transaction scope").
  </action>
  <verify>
    <automated>cd packages/graph-io && uv run --package graph-io python -c "from graph_io import structural_nodes; assert callable(structural_nodes._resolve_import_root); assert callable(structural_nodes._dotted_path); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'def _resolve_import_root' packages/graph-io/src/graph_io/structural_nodes.py` returns 1 line
    - `grep -n 'def _dotted_path' packages/graph-io/src/graph_io/structural_nodes.py` returns 1 line
    - `grep -n 'kind="subpackage"' packages/graph-io/src/graph_io/structural_nodes.py` returns at least 1 line
    - `grep -n 'kind="file"' packages/graph-io/src/graph_io/structural_nodes.py` returns at least 1 line
    - `grep -n 'subpkg_uri' packages/graph-io/src/graph_io/structural_nodes.py` returns at least 1 line
    - `grep -n 'file_uri' packages/graph-io/src/graph_io/structural_nodes.py` returns at least 1 line
    - `grep -n '_has_main_block' packages/graph-io/src/graph_io/structural_nodes.py` returns at least 1 line (SPARSER-02 read)
    - `grep -n '_has_importable_symbols' packages/graph-io/src/graph_io/structural_nodes.py` returns at least 1 line (SPARSER-02 read)
    - `grep -c 'upsert.upsert_records' packages/graph-io/src/graph_io/structural_nodes.py` returns 1 (single upsert call)
    - module imports cleanly
  </acceptance_criteria>
  <done>
    structural_nodes.emit fully implements Repository + SubPackage + File emission
    with role-flag attrs and SPARSER-02 consumption. Single upsert call. Module
    imports cleanly. Unit tests in Task 3 will exercise each emission rule.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Unit tests for Repository emission, role-flag heuristics, language gating, and SPARSER read</name>
  <read_first>
    - .planning/phases/29-structural-nodes-containment-tree/29-CONTEXT.md (D-01..D-15, D-18, D-20 — drives the assertions)
    - .planning/phases/29-structural-nodes-containment-tree/29-PATTERNS.md (Section "tests/test_structural_nodes.py" — imports template, conn fixture, test-naming convention; reuse _CTX)
    - packages/graph-io/tests/test_resolve.py (conn fixture lines 15-20 — verbatim reuse)
    - packages/graph-io/tests/test_packages.py (assertion style for graph-io node tests, RepoContext setup, attrs_json parsing)
    - packages/graph-io/src/graph_io/structural_nodes.py (the module under test — read final state from Task 2)
    - packages/graph-io/tests/_git_repo.py (init_repo + write_and_commit helpers if needed for git-context tests; lines 1-30)
  </read_first>
  <files>packages/graph-io/tests/test_structural_nodes.py</files>
  <behavior>
    - test_emit_repository_node_single: emit produces exactly one kind=repository node with the expected uri, owner, name, url, default_branch attrs
    - test_emit_repository_path_is_null: Repository node has path=None
    - test_role_flag_is_test_directory_traversal: _is_test_path returns True for "tests/foo.py", "pkg/__tests__/x.py", "test/y.py"
    - test_role_flag_is_test_filename: _is_test_path returns True for "test_foo.py", "bar_test.py", "x.test.ts", "y.spec.js"
    - test_role_flag_is_config_exact: _is_config_file returns True for "pyproject.toml", "package.json", "Makefile"
    - test_role_flag_is_config_glob: _is_config_file returns True for "vite.config.ts", "jest.config.js", "tsconfig.base.json"
    - test_role_flag_is_generated_filename: _is_generated returns True for "foo_pb2.py", "data.gen.ts", "x.generated.go"
    - test_role_flag_is_generated_content_marker: writing a tmp file with first line "# @generated by ..." returns True
    - test_role_flag_is_type_only: _is_type_only True for "types.d.ts" and "stubs.pyi", False for "x.ts"
    - test_role_flag_is_executable_exec_bit: file with chmod +x returns True
    - test_role_flag_is_executable_shebang: file starting with "#!/bin/sh" returns True even without exec bit (when extension is .sh)
    - test_subpackage_python_src_layout: a tmp pkg with src/mypkg/__init__.py + src/mypkg/sub/__init__.py emits 2 SubPackage nodes (mypkg, mypkg.sub) plus physically_contains edges
    - test_subpackage_python_flat_layout: a tmp pkg with mypkg/__init__.py emits 1 SubPackage node
    - test_subpackage_jsts_emits_zero_subpackages: a Package with language=javascript produces zero SubPackage nodes
    - test_file_python_reads_sparser_attrs: when a File node already exists in conn with attrs_json containing _has_main_block=true, the emitted File ends up with has_main=true
    - test_file_jsts_default_flags: a .js File gets has_main=False, is_importable=True
    - test_test_file_parented_by_repository: a file at path "tests/test_x.py" (is_test=true) has its physically_contains edge from Repository, not Package
    - test_generic_container_dirs_never_emitted: after emit, no node has name in {"packages", "tests", "libs", "apps", "shared", "common"}
  </behavior>
  <action>
    Create `packages/graph-io/tests/test_structural_nodes.py` with unit tests
    covering each emission rule and each role-flag heuristic in isolation.
    Mirror the test conventions from test_resolve.py + test_packages.py per
    29-PATTERNS.md "tests/test_structural_nodes.py" section.

    Test categories (use flat module-level test_<verb>_<subject> names, no
    test classes):

    A. Repository emission (D-01..D-03):
       - test_emit_repository_node_single
       - test_emit_repository_path_is_null
       - test_emit_repository_attrs_include_owner_name_url
       - test_emit_repository_default_branch_from_git_or_null

    B. Role-flag heuristics (D-09..D-12) — call the private helpers directly:
       - test_is_test_path_directory_traversal
       - test_is_test_path_filename_patterns
       - test_is_test_path_negative (regular non-test paths return False)
       - test_is_config_file_exact_names
       - test_is_config_file_globs
       - test_is_config_file_negative
       - test_is_generated_filename_patterns
       - test_is_generated_content_marker_at_generated (tmp_path file w/ "# @generated")
       - test_is_generated_content_marker_do_not_edit (case-insensitive)
       - test_is_generated_content_marker_skipped_above_1mb (large dummy file returns False)
       - test_is_type_only_d_ts_and_pyi
       - test_is_executable_exec_bit (os.chmod 0o755 then check)
       - test_is_executable_shebang_only (no exec bit, but #! first line)
       - test_is_executable_negative

    C. SubPackage emission (D-04..D-08, D-18):
       - test_subpackage_python_src_layout (3 levels deep)
       - test_subpackage_python_flat_layout
       - test_subpackage_no_init_emits_none
       - test_subpackage_jsts_package_emits_zero (D-18)
       - test_subpackage_dotted_path_includes_importable (D-07: graph_io.cli, not just cli)
       - test_subpackage_parent_is_package_for_top_level
       - test_subpackage_parent_is_enclosing_subpackage_for_nested

    D. File emission with SPARSER read (D-13, D-14, D-20, SPARSER-02):
       - test_file_python_reads_sparser_has_main (pre-seed nodes.attrs_json with _has_main_block=true; emit; assert File.has_main=true)
       - test_file_python_reads_sparser_is_importable
       - test_file_python_defaults_when_sparser_attrs_missing (both False)
       - test_file_jsts_default_has_main_false_is_importable_true (D-20)
       - test_test_file_parented_by_repository_not_package (D-14)
       - test_non_test_python_file_parented_by_subpackage (D-13)

    E. Generic container exclusion (D-15):
       - test_generic_container_dirs_never_emitted_as_nodes

    Test setup pattern (verbatim from test_resolve.py conn fixture + a helper
    builder for a tmp Package + Repository state):

        @pytest.fixture()
        def conn(tmp_path: Path) -> sqlite3.Connection:
            db = tmp_path / "code.db"
            c = store.connect(db, create=True)
            yield c
            c.close()

        _CTX = RepoContext(org="test", repo="repo")

        def _seed_package(conn, name="mypkg", path="packages/mypkg", language="python"):
            upsert.upsert_records(conn, GraphRecords(
                nodes=[GraphNode(kind="package", name=name, path=path, line=None,
                                 attrs={"uri": f"pkg:test/repo/{name}", "language": language})],
                edges=[]))

    For tests that need a real on-disk Package (SubPackage walk), construct
    the tree under tmp_path before calling emit. Use minimal files —
    `__init__.py` with one line, regular .py with `pass`.

    For Git-context tests (default_branch, url), use the
    `init_repo` + `write_and_commit` helpers from tests/_git_repo.py (per
    test_cli_smoke.py:24-25 usage pattern) — or, to avoid the git dependency
    on most tests, monkeypatch `structural_nodes._git` to return canned
    output. Choose monkeypatch for tests B/C/D/E (they don't need real git);
    use init_repo for the dedicated Repository default_branch test only.

    Total expected new test count: ~25 tests, each ~5-15 lines. Aim for clear
    assertion failure messages: include the actual emitted node/edge in the
    assertion error when feasible.
  </action>
  <verify>
    <automated>cd packages/graph-io && uv run --package graph-io pytest tests/test_structural_nodes.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^def test_' packages/graph-io/tests/test_structural_nodes.py` returns at least 20
    - `grep -n 'test_emit_repository_node_single' packages/graph-io/tests/test_structural_nodes.py` returns 1 line
    - `grep -n 'test_subpackage_jsts_package_emits_zero' packages/graph-io/tests/test_structural_nodes.py` returns 1 line
    - `grep -n 'test_test_file_parented_by_repository' packages/graph-io/tests/test_structural_nodes.py` returns 1 line
    - `grep -n 'test_generic_container_dirs_never_emitted' packages/graph-io/tests/test_structural_nodes.py` returns 1 line
    - `grep -n 'test_file_python_reads_sparser' packages/graph-io/tests/test_structural_nodes.py` returns at least 2 lines
    - `uv run --package graph-io pytest tests/test_structural_nodes.py -q` exits 0 (all new tests pass)
    - `uv run --package graph-io pytest -q` exits 0 (no regressions to any graph-io tests)
  </acceptance_criteria>
  <done>
    test_structural_nodes.py contains comprehensive unit tests for all role-flag
    heuristics, Repository emission, SubPackage walk (Python + JS/TS gating),
    File emission with SPARSER attr read, and generic-container exclusion. All
    tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem→scanner | structural_nodes walks the local filesystem via _ignore.should_skip; no network or auth boundary crossed. The one real risk is path-traversal through a malicious symlink or filename. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-07 | Tampering | symlink loop in repo_root could send the FS walk into infinite recursion | accept | per 29-CONTEXT.md Deferred: "assume no symlinks for now; if a real repo grows them, _ignore.py can be extended". Use Path.rglob / os.walk default behavior (does NOT follow symlinks for os.walk; rglob does by default — prefer os.walk OR pass followlinks=False explicitly). Verified by checking _ignore.should_skip is the upstream gate. |
| T-29-08 | Tampering | malicious filename containing path separators reaching upsert | mitigate | rel paths produced via path.relative_to(repo_root).as_posix() canonicalize; _ignore.should_skip is the FS-walk gate; no user-supplied path strings reach SQL except through parameterized queries (sqlite3 ? placeholders) |
| T-29-09 | Information Disclosure | reading first 20 lines of every file for @generated marker could expose sensitive file content to logs | mitigate | no logging of file content in this plan; helper returns bool only; errors="ignore" suppresses decode errors silently |
| T-29-10 | Denial of Service | content-marker scan on huge files | mitigate | D-11 1MB size cap (`path.stat().st_size > 1_000_000`) — verified by acceptance criterion test_is_generated_content_marker_skipped_above_1mb |
| T-29-11 | Denial of Service | _is_executable trying to read first line of every file | mitigate | only opens files whose extension is in _SHEBANG_EXTENSIONS frozenset (.py .sh .bash .zsh .js .ts .rb .pl + no-ext); skips all binaries, images, etc.; OSError caught and returns False |
| T-29-SC | Tampering | npm/pip/cargo installs | n/a | no package installs in this plan (uses only stdlib + already-installed source-parser) |
</threat_model>

<verification>
- `uv run --package graph-io pytest tests/test_structural_nodes.py -v` — full test suite passes
- `uv run --package graph-io pytest tests/ -q` — no regressions in any existing graph-io tests
- Spot-check the module structure: `grep -nE '^def |^_[A-Z_]+:' packages/graph-io/src/graph_io/structural_nodes.py`
  should show module constants near the top, then helpers, then emit() last (packages.py-style ordering)
</verification>

<success_criteria>
- `structural_nodes.emit(conn, repo_root=..., ctx=..., skip_dirs=...)` is importable and callable
- Repository emission produces exactly 1 node per call with attrs (uri, owner, name, url, default_branch)
- SubPackage emission honors D-04 (only __init__.py-containing dirs), D-05 (unlimited depth),
  D-06 (src + flat layout probe), D-07 (dotted path includes importable), D-08 (parent resolution),
  D-18 (Python-only)
- File emission produces all 7 role-flag attrs per D-09..D-12
- Python File.has_main and File.is_importable are read from source-parser attrs (SPARSER-02)
- JS/TS File defaults: has_main=False, is_importable=True
- Generic container directories never appear as nodes (D-15)
- Test files are parented by Repository, not Package or SubPackage (D-14)
- All new tests in test_structural_nodes.py pass; no regressions in any existing test
</success_criteria>

<output>
Create `.planning/phases/29-structural-nodes-containment-tree/29-03-SUMMARY.md` when done.
</output>
