---
phase: 14-plugin-port-m3b
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/vault-io/src/vault_io/wiki_search.py
  - packages/vault-io/tests/test_wiki_search.py
autonomous: true
requirements: [PLUGIN-04]
tags: [port, vault-io, search, bm25, brand-rebrand]

must_haves:
  truths:
    - "`packages/vault-io/src/vault_io/wiki_search.py` exists and is importable as `from vault_io.wiki_search import main`"
    - "`uv run --package vault-io pytest packages/vault-io/tests/test_wiki_search.py` exits 0"
    - "`scripts/check-brand.sh` exits 0 with the new file in tree (no `lattice_*` identifiers, no `lattice-wiki` brand strings)"
    - "Module shape matches in-tree convention (clean docstring + plain imports — NO `# Source: / # Anchor: / # Source-commit:` provenance headers)"
    - "Standard-library only (no new third-party deps); matches upstream `lattice_wiki_core.wiki_search`"
  artifacts:
    - path: "packages/vault-io/src/vault_io/wiki_search.py"
      provides: "BM25 search over a Code Wiki (stdlib-only)"
      exports: ["main"]
    - path: "packages/vault-io/tests/test_wiki_search.py"
      provides: "Importability + structural smoke for `vault_io.wiki_search`"
  key_links:
    - from: "packages/vault-io/src/vault_io/wiki_search.py"
      to: "vault_io._workspace.resolve_wiki_and_repo"
      via: "direct import"
      pattern: "from vault_io\\._workspace import"
---

<objective>
Verbatim port of upstream `lattice_wiki_core.wiki_search` (~194 LOC) into `vault_io.wiki_search` under the Phase 12 SR-01 rubric and the Phase 14 VP-01 prerequisite. This is the BM25 shell-out fallback target referenced by the `/graph-wiki:query` per-command spec.

Purpose: VP-01 says the plugin's `wiki_search.py` shim cannot land until `vault_io.wiki_search` exists. This plan satisfies that prerequisite by porting the module, retargeting imports from `lattice_wiki_core` → `vault_io`, dropping the upstream `_version_check` dependency (not in `vault_io` scope), and clearing the BRAND-04 grep gate.

Output: `packages/vault-io/src/vault_io/wiki_search.py` + accompanying test file. The `main()` entry, CLI argparse surface, and search return shape match upstream byte-for-byte modulo brand strings and the dropped `_version_check` import. Stdlib-only (no `bm25s` dependency — upstream uses a hand-rolled BM25; we preserve that to keep `vault-io` dependency-clean per the SR-01 verbatim rubric).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-plugin-port-m3b/14-CONTEXT.md
@.planning/phases/14-plugin-port-m3b/14-PATTERNS.md
@.planning/phases/12-drift-backport-ecosystem-rebrand-m2/12-CONTEXT.md
@packages/vault-io/src/vault_io/_workspace.py
@packages/vault-io/src/vault_io/graph_analyzer.py
@packages/vault-io/tests/test_ports_importable.py

<interfaces>
<!-- Existing in-tree exports the port depends on -->

From packages/vault-io/src/vault_io/_workspace.py:
```python
def resolve_wiki_and_repo(
    *, wiki: Path | None = None, repo: Path | None = None
) -> tuple[Path, Path]: ...
```

In-tree stdlib-only + argparse module shape analog:
- `packages/vault-io/src/vault_io/graph_analyzer.py` (only-stdlib + argparse + JSON-output module)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 2.1: Port wiki_search.py from upstream lattice_wiki_core to vault_io</name>
  <files>packages/vault-io/src/vault_io/wiki_search.py</files>
  <read_first>
    - /Users/pat/Personal/lattice/packages/lattice-wiki-core/src/lattice_wiki_core/wiki_search.py (full file — verbatim port source, ~194 LOC)
    - packages/vault-io/src/vault_io/graph_analyzer.py (in-tree shape analog — stdlib-only + argparse + JSON output)
    - packages/vault-io/src/vault_io/_workspace.py (delegation idiom + env-var phrasing for error strings)
    - .planning/phases/14-plugin-port-m3b/14-PATTERNS.md (§"Pattern Assignments" → wiki_search section — brand-rename rules)
    - .planning/phases/12-drift-backport-ecosystem-rebrand-m2/12-CONTEXT.md (§SR-01 PORT-vs-LEAVE rubric — verbatim port philosophy)
  </read_first>
  <action>
    Copy upstream `wiki_search.py` into `packages/vault-io/src/vault_io/wiki_search.py`. Apply the brand-rename token table from 14-PATTERNS.md:
      - `from lattice_wiki_core._workspace import resolve_wiki_and_repo` → `from vault_io._workspace import resolve_wiki_and_repo`
      - `from lattice_wiki_core._version_check import check_for_updates` → DELETE the import and any call site (not in `vault_io` scope per 14-PATTERNS.md)
      - `lattice-wiki` → `graph-wiki` (in prose / docstrings)
      - `LATTICE_WIKI_ROOT` / `LATTICE_WORKSPACE` → `GRAPH_WIKI_ROOT` / `GRAPH_WIKI_WORKSPACE` (in env-var references)
      - `.lattice-wiki.json` → `.graph-wiki.yaml` (in any prose ref)
    Match in-tree module shape: clean module docstring + plain imports. Do NOT introduce `# Source: / # Anchor: / # Source-commit:` provenance headers.
    Preserve `main()` entry shape, argparse surface (`--query`, `--limit`, `--json`), BM25 implementation, and return shape verbatim modulo the renames above. Stdlib-only — do NOT introduce `bm25s` or any other new third-party dep (the SR-01 verbatim rubric explicitly preserves upstream's hand-rolled BM25).
  </action>
  <verify>
    <automated>uv run --package vault-io python -c "from vault_io.wiki_search import main; assert callable(main)"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `packages/vault-io/src/vault_io/wiki_search.py`
    - `grep -c "lattice" packages/vault-io/src/vault_io/wiki_search.py` returns 0
    - `grep -c "_version_check" packages/vault-io/src/vault_io/wiki_search.py` returns 0 (import dropped)
    - `grep -c "^# Source:" packages/vault-io/src/vault_io/wiki_search.py` returns 0 (no provenance header)
    - `grep -E "^import bm25s|^from bm25s" packages/vault-io/src/vault_io/wiki_search.py | wc -l` is 0 (stdlib-only preserved)
    - `grep -c "from vault_io._workspace import" packages/vault-io/src/vault_io/wiki_search.py` is at least 1
    - `uv run --package vault-io python -c "from vault_io.wiki_search import main"` exits 0
    - `bash scripts/check-brand.sh` exits 0 (no new entries in `.brand-grep-allow`)
  </acceptance_criteria>
  <done>
    `vault_io.wiki_search` is importable, exports `main` with the same CLI signature as upstream, remains stdlib-only, and clears the BRAND-04 grep gate without allowlist additions.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2.2: Add structural test for vault_io.wiki_search</name>
  <files>packages/vault-io/tests/test_wiki_search.py</files>
  <read_first>
    - packages/vault-io/tests/test_ports_importable.py (importability pattern)
    - packages/vault-io/tests/test_lint_modules.py (fixture-vault structural pattern — for the FIXTURES path constant)
    - packages/vault-io/tests/conftest.py (any shared fixtures)
    - .planning/phases/14-plugin-port-m3b/14-PATTERNS.md (§"test_wiki_search.py" — VP-02 scope: importability + structural smoke)
  </read_first>
  <action>
    Create `packages/vault-io/tests/test_wiki_search.py` with two test functions:
      1. `test_wiki_search_importable()` — asserts `from vault_io.wiki_search import main` and that `main` is callable. Mirrors `test_ports_importable.py`.
      2. `test_wiki_search_runs_on_fixture_vault()` — uses `FIXTURES = Path(__file__).parent / "fixtures"` and picks an existing fixture vault. Invokes the search entry point programmatically (call whatever non-`main` helper upstream exposes, OR shell out to `python -m vault_io.wiki_search --query "test" --json` via `subprocess.run` with `check=True`) against the fixture, and asserts the result is structurally well-formed (parseable JSON or expected dict keys). Per VP-02: scope is structural, NOT relevance-quality parity with upstream.
    Module docstring should declare the scope explicitly: importability + structural smoke; relevance-quality / score parity with upstream is out of Phase 14 scope.
  </action>
  <verify>
    <automated>uv run --package vault-io pytest packages/vault-io/tests/test_wiki_search.py -x</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `packages/vault-io/tests/test_wiki_search.py`
    - `uv run --package vault-io pytest packages/vault-io/tests/test_wiki_search.py -x` exits 0
    - At least 2 test functions present (`grep -c '^def test_' packages/vault-io/tests/test_wiki_search.py` >= 2)
    - No `lattice_` tokens (`grep -c 'lattice_' packages/vault-io/tests/test_wiki_search.py` returns 0)
    - `bash scripts/check-brand.sh` still exits 0
  </acceptance_criteria>
  <done>
    Test file ships green; relevance-quality parity is explicitly scoped out per VP-02; brand gate clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cwd → fs reads | `vault_io.wiki_search` reads markdown files under the resolved wiki path; read-only |
| argv → argparse | CLI args (`--query`, `--limit`, `--json`); standard argparse boundary |
| stdout → caller | JSON or text search results emitted to stdout |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02-01 | Tampering | `vault_io.wiki_search` file reads | accept | Read-only operation over user-owned vault. No write paths introduced. |
| T-14-02-02 | Information Disclosure | `--json` stdout output | accept | Same surface as upstream `lattice_wiki_core.wiki_search`. Output goes to user's terminal. |
| T-14-02-03 | Denial of Service | Very large vault could slow BM25 index build | accept | Behavior-preserving port; user controls vault size. Same characteristic as upstream. |
| T-14-02-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — port preserves stdlib-only constraint. No new package dependencies. No legitimacy gate required. |

ASVS L1: no new attack surface introduced; verbatim port of an existing CLI tool; stdlib-only preserved.
</threat_model>

<verification>
After both tasks complete:

```bash
uv run --package vault-io pytest packages/vault-io/tests/
bash scripts/check-brand.sh
grep -rn 'lattice_' packages/vault-io/src/vault_io/wiki_search.py packages/vault-io/tests/test_wiki_search.py | wc -l  # must equal 0
```
</verification>

<success_criteria>
- [ ] `packages/vault-io/src/vault_io/wiki_search.py` exists and exports `main`
- [ ] `packages/vault-io/tests/test_wiki_search.py` exists with importability + structural smoke tests
- [ ] `uv run --package vault-io pytest packages/vault-io/tests/test_wiki_search.py -x` passes
- [ ] No `lattice_` tokens; no `_version_check` import; no `# Source:` provenance header; no new third-party deps
- [ ] `bash scripts/check-brand.sh` exits 0
- [ ] VP-01 prerequisite satisfied (the `wiki_search.py` shim in Plan 3 can now import `from vault_io.wiki_search import main`)
</success_criteria>

<output>
Create `.planning/phases/14-plugin-port-m3b/14-02-SUMMARY.md` when done.
</output>
