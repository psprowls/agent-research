---
phase: 17-vault-io-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/vault-io/src/vault_io/scan_monorepo.py
  - packages/vault-io/tests/test_scan_companion_fold.py
  - packages/vault-io/tests/conftest.py
autonomous: true
requirements: [SCAN-01, SCAN-02]
tags: [vault-io, scan, companion-folding, python, pytest]

must_haves:
  truths:
    - "`_load_existing_pages` returns a dict with no entries for companion stems (`api`, `context`, `patterns`, `work`) when iterating over `wiki/packages/<pkg>/` directories whose parent overview declares those stems in its `workflow_hints` frontmatter."
    - "Parent overview pages (e.g. `lattice-curator-core/lattice-curator-core.md`) ARE present in `_load_existing_pages` output keyed by their slug."
    - "`compute_diff(workspaces, existing)` reports 0 entries in `deleted` for companion stems on the `round-trip-vault` fixture when workspaces matches the fixture's 7 packages."
    - "`wiki/apps/**/*.md` pages are NOT folded — an `apps/foo/api.md` still appears as a page in the output (companion filter does not apply to apps)."
    - "Layout-pinned `package` containers (per `wiki/CLAUDE.md` containers list with `classification == 'package'`) ALSO receive the companion filter."
    - "Empty workflow_hints (or missing parent overview frontmatter) yields empty companion set — pre-fix behavior preserved for vaults without the declaration."
  artifacts:
    - path: packages/vault-io/src/vault_io/scan_monorepo.py
      provides: "Companion-fold filter inside `_collect()` (and the domains_dir loop) that reads parent-overview `workflow_hints` via `_parse_workflow_hints`."
      contains: "_parse_workflow_hints"
    - path: packages/vault-io/tests/test_scan_companion_fold.py
      provides: "Unit tests covering companion fold, layout-pinned package fold, apps-not-folded, and zero-phantom-deletes against `round-trip-vault` fixture."
      exports: ["test_load_existing_skips_companions", "test_layout_pinned_package_skips_companions", "test_apps_not_filtered", "test_compute_diff_no_phantom_deletes"]
  key_links:
    - from: "packages/vault-io/src/vault_io/scan_monorepo.py::_collect"
      to: "packages/vault-io/src/vault_io/lint/workflow_hints.py::_parse_workflow_hints"
      via: "from-import of the existing parser; NOT re-implemented"
      pattern: "from vault_io\\.lint\\.workflow_hints import _parse_workflow_hints"
    - from: "packages/vault-io/tests/test_scan_companion_fold.py"
      to: "packages/vault-io/tests/fixtures/round-trip-vault/"
      via: "FIXTURES path constant + read"
      pattern: "round-trip-vault"

reconciliations:
  - id: D-02
    context_md_claim: "Source the companion filename set from `wiki/CLAUDE.md`'s `workflow_hints` layout block via `layout_io.read_layout()`."
    reconciled_truth: "`workflow_hints` does NOT live in the layout block. It is per-page YAML frontmatter on each parent overview (e.g. `wiki/packages/<pkg>/<pkg>.md`). Use `vault_io.lint.workflow_hints._parse_workflow_hints` to read it per-page inside `_collect`. Do NOT call `layout_io.read_layout()` for companion discovery. (See 17-RESEARCH.md Pitfall 1; verified by direct file inspection of `~/Personal/wiki/deep-agents/CLAUDE.md` and `.templates/package/overview.md`.)"
---

<objective>
Fold companion files (`api.md`, `context.md`, `patterns.md`, `work.md`) inside `wiki/packages/<pkg>/` and `wiki/domains/<d>/packages/<pkg>/` directories into their parent slug so `compute_diff` stops reporting 28 false-positive `deleted` entries on a healthy 7-package vault.

Purpose: Address SCAN-01 + SCAN-02. The scan diff is currently unusable on the deep-agents wiki because every companion file (4 per package × 7 packages = 28) appears in the `deleted` set on each scan run.

Output: A surgical edit to `scan_monorepo._load_existing_pages._collect()` that reuses `vault_io.lint.workflow_hints._parse_workflow_hints` for per-page companion discovery, plus a new `tests/test_scan_companion_fold.py` covering four behavioral cases against the existing `round-trip-vault` fixture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-vault-io-bug-fixes/17-CONTEXT.md
@.planning/phases/17-vault-io-bug-fixes/17-RESEARCH.md
@.planning/phases/17-vault-io-bug-fixes/17-PATTERNS.md
@packages/vault-io/src/vault_io/scan_monorepo.py
@packages/vault-io/src/vault_io/lint/workflow_hints.py
@packages/vault-io/tests/conftest.py
@packages/vault-io/tests/test_lint_modules.py
@packages/vault-io/tests/test_round_trip.py

<interfaces>
<!-- Key contracts extracted from the codebase. Do not re-discover. -->

From `packages/vault-io/src/vault_io/lint/workflow_hints.py:13-43`:
- `_parse_workflow_hints(text: str) -> dict[str, list[str]]` — returns mapping like `{"brainstorming": ["context.md"], "planning": ["api.md", "patterns.md"], "debugging": ["api.md", "work.md"]}`. Returns `{}` when frontmatter or workflow_hints block is absent. Already handles the multi-line block form without pyyaml.

From `packages/vault-io/src/vault_io/scan_monorepo.py:590-672`:
- `_parse_frontmatter(text: str) -> dict` — existing helper, already used in `_collect`
- `_safe_read_text(path: Path) -> str` — existing helper, already used in `_collect`
- `read_layout(path: Path) -> dict | None` — used at line 644 for layout-pinned containers (`classification in ("package", "app")`)
- `_collect(root, default_category)` — nested function inside `_load_existing_pages`; current signature must change to accept a companion-folding flag
- Call sites:
  - line 640: `_collect(vault / "apps", "app")` — DO NOT FOLD (apps are single-file)
  - line 641: `_collect(vault / "packages", "package")` — FOLD
  - line 654 (inside layout loop): `_collect(vault / vault_dir, classification)` — FOLD only when `classification == "package"`
  - `domains_dir` loop (lines 658-672): inline iteration walking `wiki/domains/<d>/**/*.md` filtering by `category in ("package", "app")` — FOLD when `category == "package"`

From `packages/vault-io/tests/fixtures/round-trip-vault/packages/lattice-curator-core/`:
- `lattice-curator-core.md` — parent overview; frontmatter declares `workflow_hints` with companions
- `api.md`, `context.md`, `patterns.md`, `work.md` — sibling companion files

From `packages/vault-io/tests/conftest.py:11-29`:
- `tmp_repo(tmp_path)` — bare tmp_path fixture
- `vault_path(tmp_path)` — empty `tmp_path/wiki/` dir
- `write_file(path, content)` — module-level helper (not a fixture)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add companion-fold filter to _collect (SCAN-01)</name>
  <files>packages/vault-io/src/vault_io/scan_monorepo.py</files>
  <read_first>
    - packages/vault-io/src/vault_io/scan_monorepo.py (lines 580-680 — `_parse_frontmatter`, `_load_existing_pages`, `_collect`, domains_dir loop)
    - packages/vault-io/src/vault_io/lint/workflow_hints.py (lines 1-50 — `_parse_workflow_hints` signature and behavior)
    - .planning/phases/17-vault-io-bug-fixes/17-PATTERNS.md (`scan_monorepo._load_existing_pages._collect()` section, lines 22-100)
    - .planning/phases/17-vault-io-bug-fixes/17-RESEARCH.md (Pitfall 1, Pattern 1, Common Pitfalls section)
  </read_first>
  <behavior>
    - Test 1: For a vault where `packages/pkg-a/pkg-a.md` has frontmatter declaring `workflow_hints: { planning: [api.md, patterns.md], brainstorming: [context.md], debugging: [work.md] }`, calling `_load_existing_pages(vault)` returns a dict where `api`, `context`, `patterns`, `work` are NOT keys, but `pkg-a` (or whatever the title is) IS a key.
    - Test 2: For a vault where `apps/foo/api.md` exists, `_load_existing_pages` returns a dict that INCLUDES `api` (or the title) — apps are not folded.
    - Test 3: For a vault where `packages/pkg-a/` lacks a parent overview OR its overview has no `workflow_hints` block, all four companion files appear as separate page entries (empty-set fallback per D-03).
    - Test 4: For a vault where `CLAUDE.md` declares a layout-pinned container with `vault_dir: custom-pkgs` and `classification: package`, files inside `wiki/custom-pkgs/pkg-x/` matching workflow_hints stems are folded just like `packages/`.
  </behavior>
  <action>
    Modify `_load_existing_pages` in `packages/vault-io/src/vault_io/scan_monorepo.py` (lines 602-672) to fold companion files declared via per-page `workflow_hints` frontmatter on the parent overview.

    Implementation steps (per D-01, D-02 reconciled, D-03, D-04):

    1. Add a module-level import at the top of `scan_monorepo.py`: `from vault_io.lint.workflow_hints import _parse_workflow_hints` (per RESEARCH.md A6 and CLAUDE.md "Don't Hand-Roll" — do NOT reimplement the parser). Place the import alongside the other intra-package imports already in the file.

    2. Change the signature of the nested `_collect` function from `_collect(root, default_category)` to `_collect(root, default_category, fold_companions=False)`. The new parameter defaults to `False` so existing callers default to old behavior.

    3. Inside `_collect`, when `fold_companions=True`, perform a first pass over `root.rglob("*.md")` to build a per-directory companion-stem set:
       - For each `md`, if `md.stem == md.parent.name` (parent overview convention), read its text via `_safe_read_text(md)`, parse `hints = _parse_workflow_hints(text)`, and compute `companion_stems = {Path(p).stem for sub in hints.values() for p in sub}`. Store `companions_by_dir[md.parent] = companion_stems` only when `companion_stems` is non-empty.
       - This first pass uses the canonical parser. Do NOT add a new YAML parser.

    4. In the existing second pass (the `for md in root.rglob("*.md"):` loop at line 628), before the existing `fm = _parse_frontmatter(...)` line, add: if `fold_companions` and `md.stem in companions_by_dir.get(md.parent, set())`, `continue`. The rest of the loop body stays unchanged.

    5. Update call sites:
       - Line 640: `_collect(vault / "apps", "app")` — leave as-is (no fold). Per D-04, apps are NEVER folded.
       - Line 641: change to `_collect(vault / "packages", "package", fold_companions=True)`.
       - Line 654 (inside layout `for c in layout.get("containers", []):` loop): change to `_collect(vault / vault_dir, classification, fold_companions=(classification == "package"))`. Per D-04, only the `package` classification gets the filter; `app` classification stays single-file.

    6. The `domains_dir` loop at lines 658-672 uses inline iteration (NOT `_collect`). Apply the same logic inline within that block:
       - Walk `domains_dir.rglob("*.md")` once to collect `domain_companions_by_dir` for parent overviews (`md.stem == md.parent.name`) whose declared category is `package`. (You may filter the first pass by reading the overview's `_parse_frontmatter` to check `category`, or simply collect for every overview and skip the companion-add only when the category-check during the second pass shows `app`. Simpler: collect for all overviews, then in the second pass skip files where `md.stem` is in the parent's companion set AND `category == "package"` was determined for the overview.)
       - In the second `for md in domains_dir.rglob("*.md"):` pass, add the skip BEFORE the existing `fm = _parse_frontmatter(...)` line. Note: the existing code already filters by `category in ("package", "app")`; the companion skip must check the parent overview's category (NOT the companion file's own category, which may be empty).

       Implementation freedom: if the simplest correct approach is to NOT fold inside `domains_dir` for now (and only address it if a real domain-scoped package fixture surfaces it), document the choice in a code comment but the test must still cover `wiki/packages/` and layout-pinned `package` containers (the primary bug repro path). Per RESEARCH.md A2, the deep-agents wiki uses `wiki/packages/` exclusively; the domain-scoped case is theoretical.

       Default decision: implement the fold for `wiki/domains/<d>/packages/` as well (full D-04 coverage) — the parser reuse makes this small.

    7. Do NOT modify `_parse_frontmatter`, `_safe_read_text`, `read_layout`, `compute_diff`, or any other helper. Only `_load_existing_pages` (and its nested `_collect` + the `domains_dir` block) change.

    8. Do NOT bake in `{api, context, patterns, work}` as a default — per D-03, empty workflow_hints means empty companion set. Test 3 covers this.

    Reconciliation note (D-02): CONTEXT.md says read companions from `wiki/CLAUDE.md`'s layout block via `layout_io.read_layout()`. RESEARCH.md Pitfall 1 confirms by file inspection that `workflow_hints` lives in per-page frontmatter, NOT the layout block. `read_layout()` would return None for this lookup. Source from per-page frontmatter using `_parse_workflow_hints` per RESEARCH.md.
  </action>
  <verify>
    <automated>uv run --package vault-io python -c "from vault_io.scan_monorepo import _load_existing_pages; from vault_io.lint.workflow_hints import _parse_workflow_hints; print('imports OK')"</automated>
    <automated>cd packages/vault-io &amp;&amp; grep -n "_parse_workflow_hints" src/vault_io/scan_monorepo.py | grep -v '^#' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "from vault_io.lint.workflow_hints import _parse_workflow_hints" packages/vault-io/src/vault_io/scan_monorepo.py` returns exactly 1 match (the import was added).
    - `grep -n "fold_companions" packages/vault-io/src/vault_io/scan_monorepo.py` returns at least 4 matches (signature + at least 3 call sites: `packages`, layout-pinned `package`, domains).
    - `grep -n "_collect(vault / \"apps\", \"app\")" packages/vault-io/src/vault_io/scan_monorepo.py` still returns 1 match with NO `fold_companions=True` on that line (apps are not folded, per D-04).
    - `grep -n "{api, context, patterns, work}\\|\"api\".*\"context\".*\"patterns\".*\"work\"" packages/vault-io/src/vault_io/scan_monorepo.py` returns 0 matches (no hardcoded companion set, per D-03).
    - `grep -n "layout_io.read_layout" packages/vault-io/src/vault_io/scan_monorepo.py` count is unchanged from the original (no new call added for companion lookup; the only `read_layout` usage remains the layout-pinned-containers usage).
    - `uv run --package vault-io python -c "import vault_io.scan_monorepo"` exits 0 (no import or syntax errors).
  </acceptance_criteria>
  <done>
    `_load_existing_pages._collect` (and the domains_dir block) reads parent-overview `workflow_hints` via `_parse_workflow_hints` and skips companion-stem files when iterating `wiki/packages/`, `wiki/domains/<d>/packages/`, and layout-pinned `package` containers. Apps and the empty-workflow_hints fallback are preserved.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add unit tests for companion fold (SCAN-02)</name>
  <files>packages/vault-io/tests/test_scan_companion_fold.py, packages/vault-io/tests/conftest.py</files>
  <read_first>
    - packages/vault-io/tests/conftest.py (existing fixtures: `tmp_repo`, `vault_path`, `write_file`, `FIXTURE_VAULT`)
    - packages/vault-io/tests/test_lint_modules.py (header pattern + FIXTURES constant style)
    - packages/vault-io/tests/test_round_trip.py (load + assert idiom against round-trip-vault)
    - packages/vault-io/tests/fixtures/round-trip-vault/packages/lattice-curator-core/lattice-curator-core.md (confirm workflow_hints frontmatter shape)
    - .planning/phases/17-vault-io-bug-fixes/17-PATTERNS.md (`tests/test_scan_companion_fold.py` section, lines 225-298)
    - .planning/phases/17-vault-io-bug-fixes/17-VALIDATION.md (Per-Task Verification Map — exact test names)
  </read_first>
  <behavior>
    - `test_load_existing_skips_companions`: load `round-trip-vault` fixture via `_load_existing_pages`; assert `api`, `context`, `patterns`, `work` are NOT keys of the returned dict; assert at least one package overview slug (e.g. `lattice-curator-core`) IS a key.
    - `test_layout_pinned_package_skips_companions`: build a synthetic vault under `tmp_path/wiki/` with a `CLAUDE.md` declaring a layout-pinned container `vault_dir: custom-pkgs` `classification: package`, place a parent overview with workflow_hints + 4 companions under `wiki/custom-pkgs/pkg-x/`; assert companions are folded.
    - `test_apps_not_filtered`: build a synthetic vault with `wiki/apps/foo/foo.md` (overview with workflow_hints) and `wiki/apps/foo/api.md`; assert `api` IS still a key (apps are not folded per D-04).
    - `test_compute_diff_no_phantom_deletes`: load `round-trip-vault`; synthesize a `workspaces` list matching the 7 fixture packages (the test may inline-build the expected workspace tuples or call `discover_workspaces` against a synthetic repo that mirrors the fixture); call `compute_diff(workspaces, existing)`; assert no companion stem appears in `diff["deleted"]`.
  </behavior>
  <action>
    Create `packages/vault-io/tests/test_scan_companion_fold.py` as a new file. Follow the header pattern from `test_lint_modules.py` and the load idiom from `test_round_trip.py`. Use `from __future__ import annotations`, `from pathlib import Path`, `import pytest`, and the canonical `FIXTURES = Path(__file__).parent / "fixtures"` + `ROUND_TRIP_VAULT = FIXTURES / "round-trip-vault"` constants.

    Implement the four test functions per the `<behavior>` block above.

    For `test_compute_diff_no_phantom_deletes`: read `_load_existing_pages` and `compute_diff` from `vault_io.scan_monorepo`. Construct the workspaces argument by either (a) reading the fixture's 7 package directories under `round-trip-vault/packages/` and building synthetic workspace dicts with `name` matching each package slug, or (b) constructing a minimal list of 1-2 workspaces matching specific fixture packages. The diff's `deleted` set must not contain `"api"`, `"context"`, `"patterns"`, `"work"`.

    For the synthetic-vault tests (`test_layout_pinned_package_skips_companions`, `test_apps_not_filtered`): use the `tmp_path` builtin + `mkdir(parents=True)` + `write_text` pattern from `conftest.py::write_file`. Frontmatter for the parent overview must declare `workflow_hints` in the multi-line block form parseable by `_parse_workflow_hints` (refer to `packages/vault-io/src/vault_io/lint/workflow_hints.py:13-43` for the exact accepted form). Example overview frontmatter shape (block style with phase keys mapping to inline lists):
    ```
    ---
    workflow_hints:
      planning: [api.md, patterns.md]
      brainstorming: [context.md]
      debugging: [work.md]
    ---
    ```
    Do not invent a different YAML shape — `_parse_workflow_hints` only accepts the documented form.

    For the layout-pinned-package test, write a `CLAUDE.md` to the synthetic vault using the schema documented in `layout_io.read_layout` (look at the existing `wiki/CLAUDE.md` in `~/Personal/wiki/deep-agents` OR an existing test fixture; the sentinel block + YAML form is what `read_layout` expects). Keep the file minimal — only enough to make `read_layout` return a `containers` entry with `vault_dir: custom-pkgs` and `classification: package`. Reference `layout_io.py` directly to confirm the sentinel comment markers.

    Do NOT extend `conftest.py` unless a fixture is genuinely reused across multiple test files. The four tests above can be self-contained inside `test_scan_companion_fold.py`. If a shared synthetic-vault builder helper IS reused (e.g. layout-pinned and apps-not-filtered tests both build similar scaffolds), promote it to `conftest.py` as a fixture; otherwise inline it.

    All tests must be sync (no `async def`). Per the project standard, no syrupy snapshots are needed for these assertions — set equality is the right shape.
  </action>
  <verify>
    <automated>uv run --package vault-io pytest tests/test_scan_companion_fold.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/vault-io/tests/test_scan_companion_fold.py` exists.
    - `grep -c "^def test_" packages/vault-io/tests/test_scan_companion_fold.py` returns at least 4 (the four behavior tests).
    - `grep -n "test_load_existing_skips_companions\|test_layout_pinned_package_skips_companions\|test_apps_not_filtered\|test_compute_diff_no_phantom_deletes" packages/vault-io/tests/test_scan_companion_fold.py` returns exactly 4 matches (one per required test name from VALIDATION.md).
    - `uv run --package vault-io pytest tests/test_scan_companion_fold.py -x` exits 0 with all 4 tests passing.
    - `uv run --package vault-io pytest tests/test_scan_companion_fold.py::test_compute_diff_no_phantom_deletes -x` exits 0.
    - The full vault-io suite still passes: `uv run --package vault-io pytest -x` exits 0 (no regression in existing tests).
  </acceptance_criteria>
  <done>
    Four passing unit tests exist in `tests/test_scan_companion_fold.py` covering SCAN-02 (zero phantom companion deletions) and SCAN-01 (companion fold applies to `packages/`, layout-pinned `package` containers, but NOT to `apps/` and falls through to empty-set when workflow_hints is absent).
  </done>
</task>

</tasks>

<verification>
- All 4 new tests in `test_scan_companion_fold.py` pass: `uv run --package vault-io pytest tests/test_scan_companion_fold.py -x`
- Existing vault-io suite unaffected: `uv run --package vault-io pytest -x`
- Import wiring verified: `uv run --package vault-io python -c "from vault_io.scan_monorepo import _load_existing_pages; print('OK')"`
- Static checks against scan_monorepo.py (see acceptance_criteria of Task 1).
</verification>

<success_criteria>
- `_load_existing_pages` returns no companion-stem keys when scanning a vault whose parent overviews declare them in `workflow_hints` frontmatter.
- `compute_diff` reports 0 `deleted` entries for companion stems against the `round-trip-vault` fixture.
- Apps directory pages remain un-folded.
- The companion source is per-page `workflow_hints` frontmatter parsed via `vault_io.lint.workflow_hints._parse_workflow_hints` — NOT `layout_io.read_layout()` (D-02 reconciliation).
- No hardcoded `{api, context, patterns, work}` default (D-03 preserved).
- Phase requirements covered: SCAN-01 (implementation), SCAN-02 (test coverage).
</success_criteria>

<output>
Create `.planning/phases/17-vault-io-bug-fixes/17-01-SUMMARY.md` when done.
</output>
