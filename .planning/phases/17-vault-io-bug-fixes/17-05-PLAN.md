---
phase: 17-vault-io-bug-fixes
plan: 05
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - packages/vault-io/src/vault_io/init_vault.py
  - packages/vault-io/src/vault_io/scan_monorepo.py
  - packages/vault-io/tests/test_init_vault.py
  - packages/vault-io/tests/test_scan_monorepo.py
autonomous: true
requirements: [WSRES-02]
tags: [vault-io, wsres, gap-closure, workspace-exclusion, v2-layout]

must_haves:
  truths:
    - "When `/graph-wiki:init` runs against a v2-layout repo, the workspace directory (e.g. `<repo>/graph-wiki/`) is excluded from the detector's results — so `wiki/CLAUDE.md`'s layout block can never list the workspace dir as a `docs` (or any) container."
    - "When `/graph-wiki:scan` runs against a v2-layout repo with no pinned layout, `_discover_heuristic` does NOT pick up a `pyproject.toml` or `.claude-plugin/plugin.json` that lives under the workspace dir."
    - "The v1-layout guard from D-11 is preserved: when `workspace_path == repo_root` (v1 layout) OR `workspace_dir` is None, neither call path over-excludes — behavior is identical to today."
    - "WSRES-02 is fully wired across all three call paths: `detect_containers.main()` (already done in 17-03), `init_vault._resolve_pinned_containers`, and `scan_monorepo._discover_heuristic`."
    - "Full vault-io unit suite passes under `uv run --package vault-io pytest -q` with at least 88 tests (current 86 + new tests in this plan)."
  artifacts:
    - path: packages/vault-io/src/vault_io/init_vault.py
      provides: "`_resolve_pinned_containers` accepts and forwards `workspace_path` to `_detect_containers`; `init_wiki` passes `workspace_path` (already computed at line 162) through."
      contains: "_detect_containers(repo, workspace_path="
    - path: packages/vault-io/src/vault_io/scan_monorepo.py
      provides: "`_discover_heuristic` accepts a `workspace_dir` kwarg and skips any path whose parts include the workspace dir's name (analogous to the existing `node_modules` / `.venv` / `fixture_segments` filter on both rglob walks)."
      contains: "workspace_segments"
    - path: packages/vault-io/tests/test_init_vault.py
      provides: "Tests proving `_resolve_pinned_containers` plumbs `workspace_path` through (v2 layout excludes workspace dir; v1 layout where `workspace_path == repo` does NOT over-exclude)."
      contains: "_resolve_pinned_containers"
    - path: packages/vault-io/tests/test_scan_monorepo.py
      provides: "Tests proving `_discover_heuristic` skips stray `pyproject.toml` / `.claude-plugin/plugin.json` under the workspace dir; v1 layout (workspace_dir == repo or None) yields no over-exclusion."
      contains: "_discover_heuristic"
  key_links:
    - from: "init_vault._resolve_pinned_containers"
      to: "detect_containers.detect"
      via: "_detect_containers(repo, workspace_path=workspace_path) call"
      pattern: "_detect_containers\\(repo, workspace_path="
    - from: "init_vault.init_wiki"
      to: "_resolve_pinned_containers"
      via: "positional/kwarg passing workspace_path (already computed at line 162 as wiki_path.parent)"
      pattern: "_resolve_pinned_containers\\(.*workspace_path"
    - from: "scan_monorepo._discover_heuristic"
      to: "rglob('pyproject.toml') / rglob('.claude-plugin/plugin.json') filters"
      via: "workspace_segments skip set"
      pattern: "workspace_segments"
    - from: "scan_monorepo.main"
      to: "discover_workspaces"
      via: "workspace_dir=wiki.parent kwarg at the call site (line 1148)"
      pattern: "discover_workspaces\\(.*workspace_dir"
---

<objective>
Close the BLOCKER gap surfaced by 17-VERIFICATION.md on SC#4 (WSRES-02): the
`workspace_path` exclusion landed in plan 17-03 is wired through
`detect_containers.main()` but bypassed by two production call paths that drive
real wiki bootstrap and scan — `init_vault._resolve_pinned_containers` and
`scan_monorepo._discover_heuristic`. The verifier's recommendation is a
targeted plumb-through with no design changes. This plan does exactly that and
adds unit tests for both call paths so the regression is locked.

Purpose: Make SC#4 ("The workspace directory itself does not appear in its own
layout block as a `docs` container") true across `init_wiki` and `scan` — not
just the CLI entry point.

Output: Two surgical source edits (`init_vault.py`, `scan_monorepo.py`), two
new test files (`test_init_vault.py`, `test_scan_monorepo.py`), and a green
full unit suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-vault-io-bug-fixes/17-CONTEXT.md
@.planning/phases/17-vault-io-bug-fixes/17-VERIFICATION.md
@.planning/phases/17-vault-io-bug-fixes/17-REVIEW.md

# Reference implementation for the workspace_path / D-11 guard pattern
@packages/vault-io/src/vault_io/detect_containers.py

# Test-fixture pattern this plan must mirror
@packages/vault-io/tests/test_detect_containers.py

# Files to be modified
@packages/vault-io/src/vault_io/init_vault.py
@packages/vault-io/src/vault_io/scan_monorepo.py

<interfaces>
<!--
Key contracts the executor needs. Extracted from the codebase so the executor
does not have to re-explore. The fix is a surgical plumb-through; no signature
breaks expected for any public callable.
-->

From `packages/vault-io/src/vault_io/detect_containers.py` (already correct, used as the reference pattern — DO NOT modify in this plan):

  def detect(repo_root: Path, workspace_path: Path | None = None) -> list[dict]
  # D-11 guard (lines 153-158):
  #   exclude: Path | None = None
  #   if workspace_path is not None:
  #       wp = Path(workspace_path).resolve()
  #       if wp != repo_root and wp.parent == repo_root:
  #           exclude = wp

From `packages/vault-io/src/vault_io/init_vault.py` (existing — to be edited):

  # CURRENT (line 84-86):
  def _resolve_pinned_containers(repo: Path, non_interactive: bool) -> list[dict]:
      records = _detect_containers(repo)
      ...

  # TARGET signature:
  def _resolve_pinned_containers(
      repo: Path, non_interactive: bool, workspace_path: Path | None = None
  ) -> list[dict]:
      records = _detect_containers(repo, workspace_path=workspace_path)
      ...

  # Caller at line 167 — workspace_path is ALREADY computed at line 162
  # (`workspace_path = wiki_path.parent`); only the call needs to pass it:
  pinned = _resolve_pinned_containers(
      repo_path, non_interactive, workspace_path=workspace_path
  )

From `packages/vault-io/src/vault_io/scan_monorepo.py` (existing — to be edited):

  # CURRENT (line 384):
  def discover_workspaces(repo, pinned_containers=None):
      ...
      else:
          workspaces = _discover_heuristic(repo)

  # CURRENT (line 512):
  def _discover_heuristic(repo):
      ...
      for pp in repo.rglob("pyproject.toml"):
          if "node_modules" in pp.parts or ".venv" in pp.parts:
              continue
          if any(part in fixture_segments for part in pp.parts):
              continue
          ...
      for manifest in repo.rglob(".claude-plugin/plugin.json"):
          # same shape of filters

  # TARGET signatures (additive — default keeps current behavior):
  def discover_workspaces(repo, pinned_containers=None, workspace_dir=None):
  def _discover_heuristic(repo, workspace_dir=None):

  # TARGET filter (inside _discover_heuristic, applied to BOTH rglob loops):
  workspace_segments: set[str] = set()
  if workspace_dir is not None:
      wd = Path(workspace_dir).resolve()
      repo_r = Path(repo).resolve()
      # D-11 guard parity: only filter when workspace is a proper subdir of repo
      if wd != repo_r and wd.parent == repo_r:
          workspace_segments = {wd.name}
  ...
  for pp in repo.rglob("pyproject.toml"):
      if "node_modules" in pp.parts or ".venv" in pp.parts:
          continue
      if any(part in fixture_segments for part in pp.parts):
          continue
      if workspace_segments and any(part in workspace_segments for part in pp.parts):
          continue
      ...
  # Identical added guard for the .claude-plugin/plugin.json rglob.

  # Caller at line 1148 — `wiki, _` already unpacked at line 1120,
  # `workspace = wiki.parent` already computed at line 1121:
  workspaces = discover_workspaces(repo, pinned_containers=pinned, workspace_dir=workspace)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Plumb workspace_path through init_vault._resolve_pinned_containers and add tests</name>
  <files>
    packages/vault-io/src/vault_io/init_vault.py,
    packages/vault-io/tests/test_init_vault.py
  </files>
  <read_first>
    - packages/vault-io/src/vault_io/init_vault.py (lines 60-260 — the `_resolve_pinned_containers` def at line 84-86 and the `init_wiki` caller at line 162-167)
    - packages/vault-io/src/vault_io/detect_containers.py (entire file — the reference D-11 guard implementation at lines 148-174)
    - packages/vault-io/tests/test_detect_containers.py (entire file — the synthetic v2/v1 fixture pattern to mirror, esp. the `v2_workspace` fixture and `test_v1_layout_guard`)
    - .planning/phases/17-vault-io-bug-fixes/17-REVIEW.md §CR-01 (the verifier's concrete patch sketch for init_vault)
    - .planning/phases/17-vault-io-bug-fixes/17-VERIFICATION.md (the gap definition)
    - packages/vault-io/tests/conftest.py (if it exists — note whether any shared fixtures are reusable; do not duplicate them)
  </read_first>
  <behavior>
    - Test 1 (`test_resolve_pinned_containers_v2_excludes_workspace`): Build a synthetic v2 tmp_path repo (mirroring `v2_workspace` fixture from test_detect_containers.py): `repo/graph-wiki/wiki/`, `repo/graph-wiki/.graph-wiki.yaml`, `repo/packages/pkg-a/pyproject.toml`, `repo/packages/pkg-b/pyproject.toml`, `repo/.git`. Call `_resolve_pinned_containers(repo, non_interactive=True, workspace_path=repo/'graph-wiki')`. Assert: returned list contains a record with `source == 'packages'` AND no record has `source == 'graph-wiki'`.
    - Test 2 (`test_resolve_pinned_containers_v1_guard`): Build a synthetic v1 tmp_path repo: `repo/wiki/`, `repo/packages/pkg-a/pyproject.toml`, `repo/.git`. Call `_resolve_pinned_containers(repo, non_interactive=True, workspace_path=repo)` — the D-11 case where workspace == repo. Compare to calling with `workspace_path=None`. Assert: the set of `source` values is identical (no spurious self-skip). Both calls must contain `packages`.
    - Test 3 (`test_resolve_pinned_containers_default_workspace_path_none`): Build the same v2 fixture but call `_resolve_pinned_containers(repo, non_interactive=True)` — no `workspace_path` arg. Assert: workspace dir IS present (`graph-wiki` in sources) — proving the new param is purely additive and defaults to the pre-fix behavior. This documents the contract that callers MUST opt in.
  </behavior>
  <action>
    Edit `packages/vault-io/src/vault_io/init_vault.py`:

    1. Change the `_resolve_pinned_containers` signature at line 84 to add a third keyword argument `workspace_path: Path | None = None`. Keep `non_interactive: bool` as the second positional argument (no breaking change).
    2. Inside the function body at line 86, change `records = _detect_containers(repo)` to `records = _detect_containers(repo, workspace_path=workspace_path)`. The D-11 v1-layout guard is inherited from `detect_containers.detect()` — do NOT replicate the guard here; the reference implementation already handles `workspace_path == repo_root` and `workspace_path is None`.
    3. Update the caller inside `init_wiki` at line 167: change `pinned = _resolve_pinned_containers(repo_path, non_interactive)` to `pinned = _resolve_pinned_containers(repo_path, non_interactive, workspace_path=workspace_path)`. The local `workspace_path = wiki_path.parent` is already computed at line 162 — reuse it as-is. Do not introduce a duplicate computation.

    Create `packages/vault-io/tests/test_init_vault.py` (new file). Mirror the synthetic-fixture style from `tests/test_detect_containers.py` — a `tmp_path` fixture builds a v2 repo with `graph-wiki/wiki/` and `packages/pkg-{a,b}/pyproject.toml`. Three test functions per the `<behavior>` block above. Do NOT call `init_wiki` itself (no template rendering, no AWS, no real bootstrap) — call `_resolve_pinned_containers` directly so the test is unit-scoped and hermetic.

    Per project CLAUDE.md/karpathy-guidelines: do not refactor adjacent code; do not "improve" the rest of `_resolve_pinned_containers`; do not delete pre-existing dead code; touch only the signature line, the call line at line 86, and the call site at line 167.
  </action>
  <verify>
    <automated>uv run --package vault-io pytest packages/vault-io/tests/test_init_vault.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "_detect_containers(repo, workspace_path=" packages/vault-io/src/vault_io/init_vault.py` returns a match on the line inside `_resolve_pinned_containers` (previously line 86).
    - `grep -n "workspace_path=workspace_path" packages/vault-io/src/vault_io/init_vault.py` returns a match on the `_resolve_pinned_containers(repo_path, non_interactive, ...)` caller inside `init_wiki` (previously line 167).
    - `grep -n "def _resolve_pinned_containers" packages/vault-io/src/vault_io/init_vault.py` shows the signature includes `workspace_path: Path | None = None`.
    - `packages/vault-io/tests/test_init_vault.py` exists with the three test functions named in the `<behavior>` block.
    - `uv run --package vault-io pytest packages/vault-io/tests/test_init_vault.py -v` exits 0 with all three tests passing.
  </acceptance_criteria>
  <done>
    `_resolve_pinned_containers` accepts `workspace_path`, forwards it to `_detect_containers`, and the caller in `init_wiki` plumbs `workspace_path = wiki_path.parent` through. New `test_init_vault.py` proves v2 exclusion fires, v1 guard prevents self-skip, and the default (no `workspace_path` arg) preserves pre-fix behavior. Three tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add workspace_dir filter to scan_monorepo._discover_heuristic and add tests</name>
  <files>
    packages/vault-io/src/vault_io/scan_monorepo.py,
    packages/vault-io/tests/test_scan_monorepo.py
  </files>
  <read_first>
    - packages/vault-io/src/vault_io/scan_monorepo.py (lines 384-396 — `discover_workspaces` signature and call dispatch; lines 512-588 — `_discover_heuristic` body with both rglob loops at lines 550 and 565; lines 1120-1150 — `main()` where `wiki.parent` is already computed and `discover_workspaces` is called)
    - packages/vault-io/src/vault_io/detect_containers.py (entire file — reuse the D-11 guard pattern at lines 153-158 for the `workspace_dir == repo_root` parity check)
    - packages/vault-io/tests/test_detect_containers.py (entire file — synthetic-fixture style to mirror)
    - .planning/phases/17-vault-io-bug-fixes/17-REVIEW.md §CR-01 (the verifier's concrete patch sketch for scan_monorepo)
    - .planning/phases/17-vault-io-bug-fixes/17-VERIFICATION.md (the gap definition)
  </read_first>
  <behavior>
    - Test 1 (`test_discover_heuristic_v2_skips_workspace_pyproject`): Build a synthetic v2 tmp_path repo: `repo/graph-wiki/pyproject.toml` (the offending stray manifest), `repo/packages/pkg-a/pyproject.toml`. Call `_discover_heuristic(repo, workspace_dir=repo/'graph-wiki')`. Assert: returned workspaces include `pkg-a` AND no workspace has its `path` resolving under `graph-wiki/`.
    - Test 2 (`test_discover_heuristic_v2_skips_workspace_plugin_manifest`): Build a synthetic v2 tmp_path repo: `repo/graph-wiki/.claude-plugin/plugin.json` (the offending stray plugin manifest with minimal valid JSON), `repo/plugins/real-plugin/.claude-plugin/plugin.json`. Call `_discover_heuristic(repo, workspace_dir=repo/'graph-wiki')`. Assert: `real-plugin` is present in the returned workspaces; no returned workspace has its `path` resolving under `graph-wiki/`.
    - Test 3 (`test_discover_heuristic_v1_guard_workspace_eq_repo`): Build a synthetic v1 tmp_path repo: `repo/pyproject.toml` would equal the repo root — instead create `repo/packages/pkg-a/pyproject.toml`. Call `_discover_heuristic(repo, workspace_dir=repo)` (the D-11 case). Compare to `_discover_heuristic(repo, workspace_dir=None)`. Assert: workspace name sets are identical (`pkg-a` in both); no over-exclusion.
    - Test 4 (`test_discover_heuristic_default_workspace_dir_none`): Same v2 fixture from Test 1, but call `_discover_heuristic(repo)` with no `workspace_dir`. Assert: workspaces include BOTH `pkg-a` and the stray manifest under `graph-wiki/` (proving the new param is additive and defaults preserve pre-fix behavior).
  </behavior>
  <action>
    Edit `packages/vault-io/src/vault_io/scan_monorepo.py`:

    1. Extend `_discover_heuristic` at line 512 from `def _discover_heuristic(repo):` to `def _discover_heuristic(repo, workspace_dir=None):`. Inside the function body, just before the `# Node / pnpm` block, compute the workspace segment set with the D-11 parity guard:

       Build `workspace_segments` as an empty set by default. If `workspace_dir is not None`, resolve it via `Path(workspace_dir).resolve()`, resolve `repo` via `Path(repo).resolve()`, and ONLY when `wd != repo_r AND wd.parent == repo_r` set `workspace_segments = {wd.name}`. This mirrors the D-11 guard semantics from `detect_containers.detect()` exactly — no exclusion when v1 layout (workspace IS repo root) or when workspace_dir is None.

    2. Add the workspace segment skip inside BOTH rglob loops (analogous to the existing `node_modules` / `.venv` / `fixture_segments` filter). At line 550 (`for pp in repo.rglob("pyproject.toml")`) add a third filter line: `if workspace_segments and any(part in workspace_segments for part in pp.parts): continue`. At line 565 (`for manifest in repo.rglob(".claude-plugin/plugin.json")`) add the identical filter line.

    3. Extend `discover_workspaces` at line 384 from `def discover_workspaces(repo, pinned_containers=None):` to `def discover_workspaces(repo, pinned_containers=None, workspace_dir=None):`. In the `else` branch at line 392, change `workspaces = _discover_heuristic(repo)` to `workspaces = _discover_heuristic(repo, workspace_dir=workspace_dir)`. The pinned-container branch (`_discover_from_pinned`) is unaffected — the workspace dir is excluded BEFORE pinning happens, via the init-time fix in Task 1.

    4. Update the caller in `main()` at line 1148: `workspaces = discover_workspaces(repo, pinned_containers=pinned)` becomes `workspaces = discover_workspaces(repo, pinned_containers=pinned, workspace_dir=workspace)`. The local `workspace = wiki.parent` is already computed at line 1121 — reuse it.

    Create `packages/vault-io/tests/test_scan_monorepo.py` (new file — check first with `ls packages/vault-io/tests/test_scan_monorepo.py` to confirm absence; if a file by that name appears, ADD to it rather than overwriting). Mirror the synthetic-fixture style from `test_detect_containers.py`. Four test functions per the `<behavior>` block above. Use minimal valid JSON for `.claude-plugin/plugin.json` (e.g. `{"name": "real-plugin", "version": "0.0.1"}`) — enough to satisfy `_collect_claude_plugin`. For `pyproject.toml` use the minimal `[project]\nname="<pkg>"\nversion="0.0.1"\n` shape that `_collect_python_package` accepts.

    Per project CLAUDE.md/karpathy-guidelines: surgical changes only. Touch the `_discover_heuristic` signature, the segment-set computation, the two rglob loop filters, the `discover_workspaces` signature, the dispatch line, and the `main()` call site. Do NOT refactor either rglob loop into a single helper; do not consolidate the three filter conditions; do not touch any unrelated code in this 1170-line file.
  </action>
  <verify>
    <automated>uv run --package vault-io pytest packages/vault-io/tests/test_scan_monorepo.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "workspace_segments" packages/vault-io/src/vault_io/scan_monorepo.py` returns at least three matches (one assignment near the top of `_discover_heuristic`, two skip lines inside the rglob loops at the former lines 550 and 565).
    - `grep -n "def _discover_heuristic" packages/vault-io/src/vault_io/scan_monorepo.py` shows the signature includes `workspace_dir=None`.
    - `grep -n "def discover_workspaces" packages/vault-io/src/vault_io/scan_monorepo.py` shows the signature includes `workspace_dir=None`.
    - `grep -n "workspace_dir=workspace" packages/vault-io/src/vault_io/scan_monorepo.py` returns a match on the `discover_workspaces(repo, pinned_containers=pinned, ...)` caller inside `main()` (previously line 1148).
    - `packages/vault-io/tests/test_scan_monorepo.py` exists with the four test functions named in the `<behavior>` block.
    - `uv run --package vault-io pytest packages/vault-io/tests/test_scan_monorepo.py -v` exits 0 with all four tests passing.
  </acceptance_criteria>
  <done>
    `_discover_heuristic` accepts `workspace_dir` and skips paths whose parts contain the workspace dir name (only when v2 — D-11 guard preserved). `discover_workspaces` plumbs the kwarg through. `main()` passes `wiki.parent` as `workspace_dir`. Four new tests in `test_scan_monorepo.py` prove v2 exclusion on both rglob loops, v1 guard parity, and additive-default behavior.
  </done>
</task>

<task type="auto">
  <name>Task 3: Full-suite verification — confirm no regressions and gap closed across all WSRES-02 call paths</name>
  <files>
    .planning/phases/17-vault-io-bug-fixes/17-VERIFICATION.md
  </files>
  <read_first>
    - .planning/phases/17-vault-io-bug-fixes/17-VERIFICATION.md (the gap-source — Task 3 appends a closure section, does NOT overwrite the existing per-SC sections)
    - packages/vault-io/tests/test_detect_containers.py (must still pass — proves no regression in the original 17-03 fix)
    - packages/vault-io/tests/test_scan_companion_fold.py (must still pass — proves no regression in 17-01)
    - packages/vault-io/tests/test_update_tokens.py (must still pass — proves no regression in 17-02)
  </read_first>
  <action>
    1. Run the full vault-io unit suite: `uv run --package vault-io pytest -q` (no -m filter — unit only by default, integration tests are marked and skipped). Capture exit code and the passed/failed line. The pre-fix baseline is 86 passed; this plan adds at least 3 (init_vault) + 4 (scan_monorepo) = 7 new tests, so the expected count is at least 93. If any pre-existing test fails, STOP — the fix has regressed something and must be diagnosed; do not paper over with a skip or a try/except.

    2. Run the targeted regression-check commands and capture their output:
       - `grep -n "_detect_containers(repo)" packages/vault-io/src/vault_io/init_vault.py` — MUST return zero matches (the unguarded call site is gone).
       - `grep -n "_detect_containers(repo, workspace_path=" packages/vault-io/src/vault_io/init_vault.py` — MUST return at least one match.
       - `grep -n "workspace_segments" packages/vault-io/src/vault_io/scan_monorepo.py` — MUST return at least three matches.
       - `grep -n "workspace_dir=workspace" packages/vault-io/src/vault_io/scan_monorepo.py` — MUST return at least one match (the call site in `main()`).
       - `grep -nE "_discover_heuristic\\(repo\\)" packages/vault-io/src/vault_io/scan_monorepo.py` — MUST return zero matches in production code (the unguarded call is gone from `discover_workspaces`'s dispatch). Note: test files MAY still call `_discover_heuristic(repo)` to verify the additive-default behavior — that is intentional, so this grep is on the src tree only.

    3. Append a closure section to `.planning/phases/17-vault-io-bug-fixes/17-VERIFICATION.md` (do NOT rewrite the file — append a new `## Gap Closure (Plan 17-05)` section at the END). Include:
       - The date of closure (today).
       - The previously-failing behavioral spot-check from line 112 of 17-VERIFICATION.md (`init_vault._resolve_pinned_containers passes workspace_path`) flipped from FAIL to PASS, with the new grep output as evidence.
       - The new behavioral spot-check for `scan_monorepo._discover_heuristic workspace_segments filter present` marked PASS, with grep output.
       - The pytest output line for the full vault-io unit suite (passed count, time).
       - One sentence stating: "SC#4 (WSRES-02) now satisfied across all three call paths: `detect_containers.main()` (17-03), `init_vault._resolve_pinned_containers` (17-05), `scan_monorepo._discover_heuristic` (17-05)."
       - Do NOT change the existing `status: gaps_found` frontmatter — that is the verifier's territory. The next `/gsd:verify-phase` run will re-score and flip status to `passed`.

    Per project CLAUDE.md/karpathy-guidelines: this task is verification-only — do NOT modify any source under `packages/`. The only file written is `17-VERIFICATION.md`, appended-not-overwritten.
  </action>
  <verify>
    <automated>uv run --package vault-io pytest -q && grep -c "Gap Closure (Plan 17-05)" .planning/phases/17-vault-io-bug-fixes/17-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --package vault-io pytest -q` exits 0 with at least 93 passed (86 baseline + 7 new from Tasks 1+2). If the count is lower, a pre-existing test has regressed and the task FAILS.
    - `grep -n "_detect_containers(repo)" packages/vault-io/src/vault_io/init_vault.py | grep -v '_detect_containers(repo, workspace_path='` returns zero matches (no remaining unguarded call sites).
    - `grep -n "Gap Closure (Plan 17-05)" .planning/phases/17-vault-io-bug-fixes/17-VERIFICATION.md` returns exactly one match (the new section header was appended; the file was not overwritten).
    - The original per-SC #1-#5 sections in `17-VERIFICATION.md` are intact (`grep -c "^### SC#" 17-VERIFICATION.md` still returns 5).
  </acceptance_criteria>
  <done>
    Full vault-io unit suite passes (93+); the previously-failing behavioral spot-check is flipped to PASS; `17-VERIFICATION.md` carries an appended `Gap Closure (Plan 17-05)` section documenting the closure across all three WSRES-02 call paths.
  </done>
</task>

</tasks>

<verification>
- All 7+ new tests across `test_init_vault.py` and `test_scan_monorepo.py` pass.
- Full vault-io unit suite still green: `uv run --package vault-io pytest -q` exits 0 with at least 93 passed.
- No production source has an unguarded `_detect_containers(repo)` or `_discover_heuristic(repo)` call left (regex check via grep).
- `17-VERIFICATION.md` has a `Gap Closure (Plan 17-05)` appended section.
- D-11 v1-layout guard is preserved on both call paths (covered by `test_resolve_pinned_containers_v1_guard` and `test_discover_heuristic_v1_guard_workspace_eq_repo`).
</verification>

<success_criteria>
- SC#4 (WSRES-02) is satisfied at all three call paths — not just the CLI:
  1. `detect_containers.main()` ✓ (already landed in 17-03)
  2. `init_vault._resolve_pinned_containers` ✓ (this plan, Task 1)
  3. `scan_monorepo._discover_heuristic` ✓ (this plan, Task 2)
- A subsequent `/gsd:verify-phase 17` re-run will see the gap closed and can flip `status: gaps_found` to `passed` without changing source again.
- No regression in the 86 pre-existing unit tests; 7+ net-new tests pass.
- No new MCP surface, no new modules, no signature changes that break existing callers (all new params are keyword-only with defaults that preserve pre-fix behavior).
</success_criteria>

<output>
Create `.planning/phases/17-vault-io-bug-fixes/17-05-SUMMARY.md` when done.
</output>
