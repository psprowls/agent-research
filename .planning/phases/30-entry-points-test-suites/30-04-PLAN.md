---
phase: 30-entry-points-test-suites
plan: 04
type: execute
wave: 3
depends_on:
  - 30-01
  - 30-02
  - 30-03
files_modified:
  - packages/graph-io/src/graph_io/update.py
  - packages/graph-io/tests/test_test_suites.py
  - packages/graph-io/tests/fixtures/sample_monorepo/packages/mypkg/tests/test_foo.py
  - packages/graph-io/tests/fixtures/sample_monorepo/packages/jspkg/__tests__/index.test.js
  - packages/graph-io/tests/fixtures/sample_monorepo/packages/jspkg/__tests__/__init__placeholder
autonomous: true
requirements: []
tags:
  - graph-io
  - update-orchestration
  - strict-tree-invariant
  - call-order-pitfall
  - fixture-extension

must_haves:
  truths:
    - "update.run() calls entry_points.emit and test_suites.emit between structural_nodes.emit and resolve.sweep, in that order, inside the same store.transaction (D-21)"
    - "Two enforcement mechanisms ship together per D-19: (a) the test_call_order_pitfall fixture regression test in tests/test_test_suites.py (D-19a) which proves the strict-tree shape against the sample_monorepo fixture; (b) the always-on _enforce_strict_tree_invariant runtime check in update.run (D-19b) which fires on every cg update"
    - "A new module-private helper _enforce_strict_tree_invariant(conn) is defined in update.py; it runs at the end of update.run AFTER resolve.sweep, INSIDE the same store.transaction; raising propagates and rolls back the transaction (D-19b, D-20)"
    - "_enforce_strict_tree_invariant executes the literal query: SELECT child_id, COUNT(*) AS n FROM (SELECT dst AS child_id FROM edges WHERE kind='physically_contains') GROUP BY child_id HAVING n > 1; if any rows are returned, raise StrictTreeInvariantError"
    - "Note: the schema uses 'dst' as the column name (Phase 29 D-13), not 'child_id'; the helper renames via a subquery to keep the documented query shape readable, OR uses GROUP BY dst directly with HAVING COUNT(*) > 1 — either is acceptable as long as the SQL targets the correct column"
    - "StrictTreeInvariantError is a new exception class in update.py, alongside NotInGitRepoError and UpdateInProgressError; the message includes the count of offending nodes and a hint per D-20: 'physically_contains tree invariant violated for N node(s). Likely cause: an emitter inserted a duplicate parent edge, or test re-parenting failed to delete the prior edge. Offending child node ids: [...]'"
    - "The sample_monorepo fixture (created by Phase 29-04) is extended in this plan with: packages/mypkg/tests/test_foo.py (package-local Python test) and packages/jspkg/__tests__/index.test.js (JS package-local test) — these exercise TEST-03 + D-19a; the existing tests/integration/test_top.py from Phase 29-04 covers TEST-02 + repo-root suite"
    - "A new fixture regression test test_call_order_pitfall in tests/test_test_suites.py runs the full update.run cycle on the extended sample_monorepo fixture and asserts: (1) every File node with is_test=true has exactly one physically_contains parent edge; (2) that parent is a TestSuite node (NOT Repository, NOT Package, NOT SubPackage); (3) cg describe-package mypkg does not include test_foo.py in its physically_contains subtree (anti-regression for TEST-04); (4) the integration suite has TestSuite.kind = 'integration'; (5) re-running update.run produces identical physically_contains and tests edge counts (idempotency) — this is SC#3"
    - "Convention-based shebang scripts in the fixture (packages/mypkg/scripts/run.sh from Phase 29-04) do NOT produce EntryPoint nodes — the call-order pitfall test includes an assertion: SELECT COUNT(*) FROM nodes WHERE kind='entry_point' for that script's file is zero (ENTRY-05 anti-test)"
    - "A new regression test test_anti_regression_describe_package_smoke confirms a SQL surrogate for SC#5 — after the full pipeline runs on the agent-research repo's own packages/graph-io tree, SELECT * FROM nodes WHERE kind='package' AND name='graph-io' returns exactly one row (cg describe-package still finds the package). The test does NOT invoke the CLI — that's Phase 33 — but exercises the same data path."
    - "The runtime invariant fires on a deliberately-corrupted DB: a new test test_strict_tree_invariant_raises_on_duplicate_parent inserts a second physically_contains edge for some child_id and confirms that _enforce_strict_tree_invariant raises StrictTreeInvariantError"
    - "All Phase 29's existing tests continue to pass after Phase 30 wiring lands — full test suite run is the final verification step"
  artifacts:
    - path: "packages/graph-io/src/graph_io/update.py"
      provides: "_enforce_strict_tree_invariant helper, StrictTreeInvariantError exception, and entry_points.emit + test_suites.emit insertion in update.run"
      contains: "_enforce_strict_tree_invariant"
      exports:
        - StrictTreeInvariantError
    - path: "packages/graph-io/tests/test_test_suites.py"
      provides: "test_call_order_pitfall fixture-driven test (SC#3), test_strict_tree_invariant_raises_on_duplicate_parent (D-19b), test_anti_regression_describe_package_smoke (SC#5 surrogate), and ENTRY-05 anti-test"
      contains: "test_call_order_pitfall"
    - path: "packages/graph-io/tests/fixtures/sample_monorepo/packages/mypkg/tests/test_foo.py"
      provides: "Package-local Python test file for the call-order pitfall fixture"
    - path: "packages/graph-io/tests/fixtures/sample_monorepo/packages/jspkg/__tests__/index.test.js"
      provides: "JS package-local test file for the call-order pitfall fixture"
  key_links:
    - from: "update.run"
      to: "entry_points.emit + test_suites.emit"
      via: "import + call between structural_nodes.emit and resolve.sweep, inside store.transaction"
      pattern: "entry_points\\.emit|test_suites\\.emit"
    - from: "update.run"
      to: "_enforce_strict_tree_invariant"
      via: "call after resolve.sweep, inside store.transaction; raises on violation"
      pattern: "_enforce_strict_tree_invariant"
    - from: "test_call_order_pitfall"
      to: "extended sample_monorepo fixture"
      via: "copytree to tmp_path + init_repo + write_and_commit + update.run(full=True)"
      pattern: "sample_monorepo"
---

<objective>
This plan is the **integration + enforcement** layer for Phase 30:

1. Wire `entry_points.emit` and `test_suites.emit` into `update.run()` between
   `structural_nodes.emit` (Phase 29 D-23) and `resolve.sweep`, inside the
   existing `store.transaction`. (D-21)

2. Add an always-on runtime invariant check at the end of `update.run`
   (after `resolve.sweep`, still inside the transaction): one SQL query that
   verifies every node has at most one `physically_contains` parent; raise
   `StrictTreeInvariantError` on violation. (D-19b, D-20)

3. Extend the Phase 29-04 `sample_monorepo` fixture with the two test files
   needed to exercise Phase 30's call-order pitfall (D-19a). The repo-root
   `tests/integration/test_top.py` is already added by Phase 29-04 — this
   plan adds the package-local Python test and the JS package-local test.

4. Add the **call-order pitfall regression test** (`test_call_order_pitfall`)
   that runs the full `update.run` cycle on the extended fixture and asserts
   every Phase 30 success criterion that can be verified via SQL (SC#3 + SC#4
   + SC#5 surrogate + ENTRY-05 anti-test + idempotency).

Phase 30 success criteria mention CLI commands (`cg list-entry-points`,
`cg list-suites`, `cg describe-package`) that are Phase 33's responsibility.
This plan validates the underlying graph data via direct SQL — the same data
the future CLI will surface.

Output: `update.run()` is the canonical writer for Phase 30 data; the
strict-tree invariant runs every `cg update`; the fixture regression test
proves SC#3 + SC#4 + SC#5; all Phase 29 tests still pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-entry-points-test-suites/30-CONTEXT.md
@.planning/phases/30-entry-points-test-suites/30-RESEARCH.md
@.planning/phases/29-structural-nodes-containment-tree/29-04-PLAN.md
@packages/graph-io/src/graph_io/update.py
@packages/graph-io/src/graph_io/structural_nodes.py
@packages/graph-io/src/graph_io/entry_points.py
@packages/graph-io/src/graph_io/test_suites.py
@packages/graph-io/src/graph_io/resolve.py
@packages/graph-io/tests/test_structural_nodes.py
@packages/graph-io/tests/_git_repo.py

<interfaces>
<!-- New + existing surfaces consumed by this plan. -->

After Plans 30-01 / 30-02 / 30-03:
  graph_io.entry_points.emit(conn, *, repo_root, ctx, skip_dirs) -> None
  graph_io.test_suites.emit(conn, *, repo_root, ctx, skip_dirs) -> None

After Phase 29-04 (must land BEFORE Phase 30 execute-phase runs):
  packages/graph-io/tests/fixtures/sample_monorepo/  — sample monorepo with
    pyproject.toml, packages/mypkg/, packages/jspkg/, tests/integration/test_top.py
  packages/graph-io/tests/_git_repo.py  — _run_cli or _init_repo helper for
    fixture-driven CLI/update tests (Phase 29's test_structural_nodes.py
    establishes the pattern)

From graph_io.update (existing):
  NotInGitRepoError
  UpdateInProgressError
  run(repo_root, *, workspace, full, lock_timeout_ms) -> None
  store.transaction(conn) context manager (from graph_io.store)

Existing structural_nodes.emit insertion point in update.run (lines 252-255):
  from graph_io import structural_nodes  # noqa: PLC0415
  structural_nodes.emit(conn, repo_root=repo_root, ctx=ctx, skip_dirs=skip_dirs)
  resolve.sweep(conn)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add StrictTreeInvariantError + _enforce_strict_tree_invariant to update.py</name>
  <read_first>
    - .planning/phases/30-entry-points-test-suites/30-CONTEXT.md (D-19b — exact SQL; D-20 — exception class name + message format)
    - .planning/phases/30-entry-points-test-suites/30-RESEARCH.md (§2 integration points — placement and rollback behavior)
    - packages/graph-io/src/graph_io/update.py (current state — NotInGitRepoError and UpdateInProgressError definitions, update.run() body)
  </read_first>
  <files>packages/graph-io/src/graph_io/update.py</files>
  <behavior>
    - A new exception class StrictTreeInvariantError(Exception) is defined in update.py near the other two exception classes (after UpdateInProgressError)
    - The exception accepts a keyword arg offending_child_ids: list[int] in __init__ and stores it on self.offending_child_ids; its str() includes the count + the hint message verbatim from D-20
    - A new module-private helper _enforce_strict_tree_invariant(conn: sqlite3.Connection) -> None executes the SQL: SELECT dst, COUNT(*) FROM edges WHERE kind='physically_contains' GROUP BY dst HAVING COUNT(*) > 1; if any rows return, raise StrictTreeInvariantError(offending_child_ids=[row[0] for row in rows])
    - The helper is called inside update.run's store.transaction block, AFTER resolve.sweep(conn), BEFORE the _set_metadata calls (so a violation rolls back the metadata writes too — the on-disk DB stays at the previous indexed commit)
  </behavior>
  <action>
    Modify packages/graph-io/src/graph_io/update.py:

    1. After UpdateInProgressError, add:

         class StrictTreeInvariantError(Exception):
             """Raised when the physically_contains containment tree has a
             child node with more than one parent edge — violates the strict
             tree invariant (Phase 29 STRUCT-04 / Phase 30 D-19b).

             Most commonly caused by an emitter inserting a duplicate parent
             edge or by test_suites.emit's re-parenting failing to DELETE the
             prior physically_contains edge before INSERTing the new one.
             """

             def __init__(self, *, offending_child_ids: list[int]):
                 self.offending_child_ids = offending_child_ids
                 count = len(offending_child_ids)
                 # Truncate the id list in the message at 20 to keep it usable
                 # in CI logs (D-20 hint format).
                 sample = offending_child_ids[:20]
                 super().__init__(
                     f"physically_contains tree invariant violated for {count} node(s). "
                     f"Likely cause: an emitter inserted a duplicate parent edge, or "
                     f"test re-parenting failed to delete the prior edge. "
                     f"Offending child node ids (first {min(count, 20)}): {sample}"
                 )

    2. Add the module-private helper somewhere after _get_metadata (near the
       other module-private helpers, before run()):

         def _enforce_strict_tree_invariant(conn) -> None:
             """D-19b: raise StrictTreeInvariantError if any child has >1
             physically_contains parents. Always on. Always runs at the end
             of update.run inside the transaction."""
             rows = conn.execute(
                 "SELECT dst, COUNT(*) "
                 "FROM edges "
                 "WHERE kind = 'physically_contains' "
                 "GROUP BY dst "
                 "HAVING COUNT(*) > 1"
             ).fetchall()
             if rows:
                 raise StrictTreeInvariantError(
                     offending_child_ids=[row[0] for row in rows]
                 )

    Do NOT call _enforce_strict_tree_invariant from run() yet — Task 2 handles
    wiring entry_points.emit + test_suites.emit AND the invariant call site
    in one edit (so the diff to run() is atomic).
  </action>
  <verify>
    <automated>cd /Users/pat/Personal/agent-research && uv run --package graph-io python -c "from graph_io.update import StrictTreeInvariantError, _enforce_strict_tree_invariant; assert callable(_enforce_strict_tree_invariant); e = StrictTreeInvariantError(offending_child_ids=[1, 2]); assert e.offending_child_ids == [1, 2]; assert 'tree invariant violated' in str(e); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '^class StrictTreeInvariantError' packages/graph-io/src/graph_io/update.py` returns 1 line
    - `grep -n '^def _enforce_strict_tree_invariant' packages/graph-io/src/graph_io/update.py` returns 1 line
    - `grep -n 'GROUP BY dst' packages/graph-io/src/graph_io/update.py` returns 1 line (the invariant SQL)
    - `grep -n 'offending_child_ids' packages/graph-io/src/graph_io/update.py` returns at least 3 lines (kwarg + attr + message + raise)
    - verify command exits 0
  </acceptance_criteria>
  <done>
    StrictTreeInvariantError class and _enforce_strict_tree_invariant helper
    exist; both importable; the exception's message matches D-20.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire entry_points.emit + test_suites.emit + invariant call into update.run (D-21)</name>
  <read_first>
    - .planning/phases/30-entry-points-test-suites/30-CONTEXT.md (D-21 — exact ordering)
    - .planning/phases/30-entry-points-test-suites/30-RESEARCH.md (§2 update.run integration points — post-Phase-29 baseline + Phase 30 diff)
    - packages/graph-io/src/graph_io/update.py (current state after Task 1, plus the Phase 29-04 wiring of structural_nodes.emit at lines 252-255)
  </read_first>
  <files>packages/graph-io/src/graph_io/update.py</files>
  <behavior>
    - update.run inserts a single deferred-import statement that pulls entry_points + test_suites alongside structural_nodes
    - The call order inside the store.transaction is exactly: _process_files, packages.refresh, [if full block], structural_nodes.emit, entry_points.emit, test_suites.emit, resolve.sweep, _enforce_strict_tree_invariant, _set_metadata(...)
    - The import is deferred to avoid the same kind of import cycle that the Phase 29 comment notes (entry_points and test_suites both import from graph_io.structural_nodes which imports from graph_io.update)
    - No new sibling functions added — only the import line and the 3 new call statements
  </behavior>
  <action>
    In packages/graph-io/src/graph_io/update.py, locate the structural_nodes
    insertion block in run() (around lines 252-256):

      from graph_io import structural_nodes  # noqa: PLC0415
      structural_nodes.emit(conn, repo_root=repo_root, ctx=ctx, skip_dirs=skip_dirs)
      resolve.sweep(conn)

    Replace with:

      # Deferred imports to avoid the structural/entry_points/test_suites -> update cycle.
      from graph_io import entry_points, structural_nodes, test_suites  # noqa: PLC0415
      structural_nodes.emit(conn, repo_root=repo_root, ctx=ctx, skip_dirs=skip_dirs)
      entry_points.emit(conn, repo_root=repo_root, ctx=ctx, skip_dirs=skip_dirs)
      test_suites.emit(conn, repo_root=repo_root, ctx=ctx, skip_dirs=skip_dirs)
      resolve.sweep(conn)
      _enforce_strict_tree_invariant(conn)

    The _set_metadata calls remain immediately after, still inside the
    transaction — a StrictTreeInvariantError raise will propagate up and
    cause store.transaction to roll back the entire write.
  </action>
  <verify>
    <automated>cd /Users/pat/Personal/agent-research && uv run --package graph-io python -c "
    import inspect
    from graph_io import update
    src = inspect.getsource(update.run)
    assert 'entry_points.emit' in src
    assert 'test_suites.emit' in src
    assert '_enforce_strict_tree_invariant' in src
    # Confirm ordering: structural_nodes -> entry_points -> test_suites -> resolve.sweep -> invariant
    i_struct = src.index('structural_nodes.emit')
    i_entry  = src.index('entry_points.emit')
    i_test   = src.index('test_suites.emit')
    i_resolve = src.index('resolve.sweep')
    i_inv    = src.index('_enforce_strict_tree_invariant')
    assert i_struct < i_entry < i_test < i_resolve < i_inv, f'wrong order: struct={i_struct} entry={i_entry} test={i_test} resolve={i_resolve} inv={i_inv}'
    print('ok')
    "</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'entry_points, structural_nodes, test_suites' packages/graph-io/src/graph_io/update.py` returns 1 line (combined deferred import)
    - `grep -n 'entry_points.emit(conn' packages/graph-io/src/graph_io/update.py` returns 1 line
    - `grep -n 'test_suites.emit(conn' packages/graph-io/src/graph_io/update.py` returns 1 line
    - `grep -n '_enforce_strict_tree_invariant(conn)' packages/graph-io/src/graph_io/update.py` returns 1 line
    - The verify command exits 0 (asserts the ordering)
    - All existing Phase 29 tests still pass: `uv run --package graph-io pytest packages/graph-io/tests/test_structural_nodes.py packages/graph-io/tests/test_update_full.py packages/graph-io/tests/test_update_idempotent.py -q` exits 0
  </acceptance_criteria>
  <done>
    update.run has all four Phase 30 call sites wired in the correct order;
    Phase 29's tests still pass; the deferred-import comment is updated.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend sample_monorepo fixture with Phase 30 test files (D-19a)</name>
  <read_first>
    - .planning/phases/30-entry-points-test-suites/30-CONTEXT.md (D-19a — fixture extension content + assertions)
    - .planning/phases/29-structural-nodes-containment-tree/29-04-PLAN.md (sample_monorepo file list — confirm tests/integration/test_top.py is already added by Phase 29-04 so this plan does NOT duplicate it)
    - packages/graph-io/tests/fixtures/sample_monorepo/ (existing tree from Phase 29-04 — confirm the file layout before adding to it)
  </read_first>
  <files>
    - packages/graph-io/tests/fixtures/sample_monorepo/packages/mypkg/tests/test_foo.py
    - packages/graph-io/tests/fixtures/sample_monorepo/packages/jspkg/__tests__/index.test.js
  </files>
  <behavior>
    - packages/mypkg/tests/test_foo.py exists with minimal contents that import mypkg (so the tests-edge derivation in Plan 30-03 produces a TestSuite -> Package(mypkg) edge)
    - packages/jspkg/__tests__/index.test.js exists with minimal contents that import the JS package (relative import from ../index.js so the tests-edge derivation produces a TestSuite -> Package(jspkg) edge)
    - Neither file references content that Phase 29's sample_monorepo doesn't already provide — they re-import from existing fixture files
  </behavior>
  <action>
    Create packages/graph-io/tests/fixtures/sample_monorepo/packages/mypkg/tests/test_foo.py:

      """Phase 30 D-19a fixture extension — package-local Python test."""

      from mypkg.foo import hello   # noqa: F401  (imports first-party package)


      def test_smoke():
          # placeholder; the fixture isn't executed, only walked for is_test=true
          assert True

    Create packages/graph-io/tests/fixtures/sample_monorepo/packages/jspkg/__tests__/index.test.js:

      // Phase 30 D-19a fixture extension — JS package-local test.
      // Imports the package's index.js via a relative path so the
      // test_suites.emit tests-edge derivation produces TestSuite -> Package(jspkg).

      const main = require('../index.js');

      test('smoke', () => {
        expect(main).toBeDefined();
      });

    Both files MUST be committed to the repo via git (the fixture is a real
    tracked directory). Add them with `git add`. Do not modify any other
    fixture file — the Phase 29-04 work is the source of truth for the
    pre-existing tree.

    NOTE: If Phase 29-04 has NOT yet shipped at the time this plan runs,
    abort the task with a clear error message:
      "Plan 30-04 Task 3 cannot run until Phase 29-04 ships the
      sample_monorepo fixture. Wait for Phase 29 to complete."

    Detect by checking for the existence of
    packages/graph-io/tests/fixtures/sample_monorepo/packages/mypkg/src/mypkg/foo.py
    (which is part of Phase 29-04's deliverable per its files_modified list).
  </action>
  <verify>
    <automated>test -f packages/graph-io/tests/fixtures/sample_monorepo/packages/mypkg/tests/test_foo.py && test -f packages/graph-io/tests/fixtures/sample_monorepo/packages/jspkg/__tests__/index.test.js && grep -q 'from mypkg' packages/graph-io/tests/fixtures/sample_monorepo/packages/mypkg/tests/test_foo.py && grep -q "require('../index.js')" packages/graph-io/tests/fixtures/sample_monorepo/packages/jspkg/__tests__/index.test.js && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - `test -f packages/graph-io/tests/fixtures/sample_monorepo/packages/mypkg/tests/test_foo.py` exits 0
    - `test -f packages/graph-io/tests/fixtures/sample_monorepo/packages/jspkg/__tests__/index.test.js` exits 0
    - The Python test imports from mypkg: `grep -n 'from mypkg' packages/graph-io/tests/fixtures/sample_monorepo/packages/mypkg/tests/test_foo.py` returns 1 line
    - The JS test requires the relative index.js: `grep -n "require\('../index" packages/graph-io/tests/fixtures/sample_monorepo/packages/jspkg/__tests__/index.test.js` returns 1 line
    - Phase 29-04's foundational file exists: `test -f packages/graph-io/tests/fixtures/sample_monorepo/packages/mypkg/src/mypkg/foo.py` exits 0 (validates the Phase 29 dependency landed)
  </acceptance_criteria>
  <done>
    The two new test files exist in the fixture. The Python file imports
    from mypkg; the JS file requires the relative index.js — both produce
    deterministic tests-edges when test_suites.emit runs.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Add call-order pitfall fixture regression test (SC#3, SC#4) + invariant violation test (D-19b) + SC#5 surrogate</name>
  <read_first>
    - .planning/phases/30-entry-points-test-suites/30-CONTEXT.md (D-19a assertions list verbatim; SC#3, SC#4, SC#5 from ROADMAP)
    - .planning/ROADMAP.md (Phase 30 block — exact success criteria text)
    - packages/graph-io/tests/test_structural_nodes.py (analog test_physically_contains_is_strict_tree pattern from Phase 29-04)
    - packages/graph-io/tests/_git_repo.py (init_repo / write_and_commit helpers used by fixture-driven tests)
    - packages/graph-io/tests/conftest.py (existing fixtures; _run_cli helper if any)
  </read_first>
  <files>packages/graph-io/tests/test_test_suites.py</files>
  <behavior>
    - Add test_call_order_pitfall: copies sample_monorepo to tmp_path; init_repo + commit; calls update.run(repo_root, full=True); asserts the five assertions per D-19a:
      (1) every File with is_test=true has exactly one physically_contains parent edge
      (2) that parent is a TestSuite (kind='test_suite')
      (3) the Package(mypkg)'s physically_contains subtree excludes packages/mypkg/tests/test_foo.py — i.e. no edge with src=Package(mypkg) and dst=File(packages/mypkg/tests/test_foo.py)
      (4) the integration suite (path='tests/integration') has attrs.suite_kind = 'integration'
      (5) running update.run a second time produces identical physically_contains edge count and identical tests edge count (idempotency)
    - Add test_strict_tree_invariant_raises_on_duplicate_parent: copies sample_monorepo to tmp_path; runs update.run successfully; then directly INSERTs a duplicate physically_contains edge into the DB; then calls _enforce_strict_tree_invariant(conn) and asserts it raises StrictTreeInvariantError with offending_child_ids non-empty
    - Add test_anti_regression_describe_package_smoke: runs update.run on a tmp_path copy of sample_monorepo; asserts SELECT * FROM nodes WHERE kind='package' AND name='mypkg' returns exactly 1 row — surrogate for SC#5 (the CLI is Phase 33)
    - Add test_shebang_script_in_fixture_does_not_emit_entry_point: runs update.run on sample_monorepo; asserts NO EntryPoint node exists referencing packages/mypkg/scripts/run.sh (the Phase 29-04 fixture's shebang script) — ENTRY-05 anti-test
    - Add test_pyproject_scripts_entry_point_resolves_to_file: if sample_monorepo's packages/mypkg/pyproject.toml has [project.scripts] declarations (per Phase 29-04 — verify in PLAN 29-04), assert the implemented_by edge points at the correct file (SC#4)
    - All five tests use the same fixture-copy pattern; consider extracting a pytest fixture for the copied tmp_path repo
  </behavior>
  <action>
    Append to packages/graph-io/tests/test_test_suites.py (do NOT replace the
    Plan 30-03 tests; this task extends the file). Add these tests:

      import shutil
      from graph_io import update
      from graph_io.update import StrictTreeInvariantError, _enforce_strict_tree_invariant
      from graph_io.tests._git_repo import init_repo, write_and_commit  # confirm import path; adapt if helpers are exposed differently


      FIXTURE_SRC = Path(__file__).parent / "fixtures" / "sample_monorepo"


      @pytest.fixture
      def fixture_repo(tmp_path):
          """Copy sample_monorepo to tmp_path and initialize as a git repo."""
          dest = tmp_path / "repo"
          shutil.copytree(FIXTURE_SRC, dest)
          # Initialize git + commit using the existing helper. Inspect
          # _git_repo.py to choose the right helper signature.
          init_repo(dest)
          write_and_commit(dest, "initial fixture")
          return dest


      def test_call_order_pitfall(fixture_repo):
          """SC#3 + SC#4 — fixture regression: re-parenting + suite kind + idempotency."""
          # First run
          update.run(fixture_repo, full=True)

          import sqlite3
          from workspace_io.paths import graph_dir
          db_path = graph_dir(fixture_repo) / "code.db"
          conn = sqlite3.connect(db_path)
          try:
              # Assertion 1: every is_test=true file has exactly one physically_contains parent
              rows = conn.execute("""
                  SELECT f.id, COUNT(e.id)
                  FROM nodes f
                  LEFT JOIN edges e
                    ON e.dst = f.id AND e.kind = 'physically_contains'
                  WHERE f.kind = 'file'
                    AND json_extract(f.attrs_json, '$.is_test') = 1
                  GROUP BY f.id
              """).fetchall()
              assert rows, "no is_test=true files found — fixture or emitter broken"
              for fid, n in rows:
                  assert n == 1, f"file id={fid} has {n} physically_contains parents"

              # Assertion 2: that parent is a TestSuite node
              rows = conn.execute("""
                  SELECT parent.kind, f.path
                  FROM nodes f
                  JOIN edges e   ON e.dst = f.id AND e.kind='physically_contains'
                  JOIN nodes parent ON e.src = parent.id
                  WHERE f.kind = 'file'
                    AND json_extract(f.attrs_json, '$.is_test') = 1
              """).fetchall()
              for parent_kind, path in rows:
                  assert parent_kind == "test_suite", (
                      f"test file {path} has parent kind {parent_kind!r}, expected 'test_suite'"
                  )

              # Assertion 3: Package(mypkg)'s physically_contains subtree EXCLUDES the package-local test
              row = conn.execute("""
                  SELECT 1
                  FROM edges e
                  JOIN nodes p ON e.src = p.id AND p.kind='package' AND p.name='mypkg'
                  JOIN nodes f ON e.dst = f.id AND f.kind='file'
                  WHERE e.kind = 'physically_contains'
                    AND f.path LIKE '%tests/test_foo.py'
              """).fetchone()
              assert row is None, "Package(mypkg) still has physically_contains edge to its test file"

              # Assertion 4: integration suite has suite_kind='integration'
              row = conn.execute("""
                  SELECT json_extract(attrs_json, '$.suite_kind')
                  FROM nodes
                  WHERE kind = 'test_suite' AND path = 'tests/integration'
              """).fetchone()
              assert row is not None, "tests/integration TestSuite not emitted"
              assert row[0] == "integration"

              # Capture edge counts for idempotency assertion
              pc_count_1 = conn.execute("SELECT COUNT(*) FROM edges WHERE kind='physically_contains'").fetchone()[0]
              tests_count_1 = conn.execute("SELECT COUNT(*) FROM edges WHERE kind='tests'").fetchone()[0]
          finally:
              conn.close()

          # Second run — idempotency check
          update.run(fixture_repo, full=True)
          conn = sqlite3.connect(db_path)
          try:
              pc_count_2 = conn.execute("SELECT COUNT(*) FROM edges WHERE kind='physically_contains'").fetchone()[0]
              tests_count_2 = conn.execute("SELECT COUNT(*) FROM edges WHERE kind='tests'").fetchone()[0]
              assert pc_count_1 == pc_count_2, (
                  f"physically_contains edge count changed across runs: {pc_count_1} -> {pc_count_2}"
              )
              assert tests_count_1 == tests_count_2, (
                  f"tests edge count changed across runs: {tests_count_1} -> {tests_count_2}"
              )
          finally:
              conn.close()


      def test_strict_tree_invariant_raises_on_duplicate_parent(fixture_repo):
          """D-19b: deliberately corrupt the DB and confirm the runtime check fires."""
          update.run(fixture_repo, full=True)
          import sqlite3
          from workspace_io.paths import graph_dir
          db_path = graph_dir(fixture_repo) / "code.db"
          conn = sqlite3.connect(db_path)
          try:
              # Pick any non-test File node and add a second physically_contains
              # parent (the Repository node id).
              file_row = conn.execute("""
                  SELECT f.id, e.src
                  FROM nodes f
                  JOIN edges e ON e.dst = f.id AND e.kind = 'physically_contains'
                  WHERE f.kind = 'file'
                  LIMIT 1
              """).fetchone()
              assert file_row is not None
              file_id, existing_parent = file_row
              repo_row = conn.execute("SELECT id FROM nodes WHERE kind='repository' LIMIT 1").fetchone()
              assert repo_row is not None
              repo_id = repo_row[0]
              if repo_id == existing_parent:
                  # Need a different parent — pick a Package id
                  pkg_row = conn.execute("SELECT id FROM nodes WHERE kind='package' LIMIT 1").fetchone()
                  assert pkg_row is not None
                  fake_parent = pkg_row[0]
              else:
                  fake_parent = repo_id
              conn.execute(
                  "INSERT INTO edges (src, dst, kind, attrs_json) "
                  "VALUES (?, ?, 'physically_contains', NULL)",
                  (fake_parent, file_id),
              )
              conn.commit()

              with pytest.raises(StrictTreeInvariantError) as exc:
                  _enforce_strict_tree_invariant(conn)
              assert file_id in exc.value.offending_child_ids
          finally:
              conn.close()


      def test_anti_regression_describe_package_smoke(fixture_repo):
          """SC#5 surrogate — after the full pipeline runs, Package(mypkg) is still findable."""
          update.run(fixture_repo, full=True)
          import sqlite3
          from workspace_io.paths import graph_dir
          db_path = graph_dir(fixture_repo) / "code.db"
          conn = sqlite3.connect(db_path)
          try:
              n = conn.execute(
                  "SELECT COUNT(*) FROM nodes WHERE kind='package' AND name='mypkg'"
              ).fetchone()[0]
              assert n == 1
          finally:
              conn.close()


      def test_shebang_script_in_fixture_does_not_emit_entry_point(fixture_repo):
          """ENTRY-05 anti-test — shebang scripts in scripts/ stay as is_executable Files, no EntryPoint."""
          update.run(fixture_repo, full=True)
          import sqlite3
          from workspace_io.paths import graph_dir
          db_path = graph_dir(fixture_repo) / "code.db"
          conn = sqlite3.connect(db_path)
          try:
              row = conn.execute("""
                  SELECT 1 FROM edges e
                  JOIN nodes ep ON e.src = ep.id AND ep.kind = 'entry_point'
                  JOIN nodes f  ON e.dst = f.id  AND f.kind = 'file'
                  WHERE e.kind = 'implemented_by'
                    AND f.path LIKE '%/scripts/run.sh'
              """).fetchone()
              assert row is None, "shebang script run.sh is implemented_by an EntryPoint — ENTRY-05 violated"
          finally:
              conn.close()


      def test_pyproject_scripts_entry_point_resolves_to_file(fixture_repo):
          """SC#4 — pyproject [project.scripts] resolves implemented_by strictly path-qualified."""
          update.run(fixture_repo, full=True)
          import sqlite3
          from workspace_io.paths import graph_dir
          db_path = graph_dir(fixture_repo) / "code.db"
          conn = sqlite3.connect(db_path)
          try:
              # If Phase 29-04's sample_monorepo includes a [project.scripts]
              # entry in packages/mypkg/pyproject.toml — verify implemented_by.
              # If not, this test should skip (Phase 29-04 may or may not
              # include a scripts entry).
              row = conn.execute("""
                  SELECT ep.name, f.path
                  FROM nodes ep
                  JOIN edges e ON e.src = ep.id AND e.kind = 'implemented_by'
                  JOIN nodes f ON e.dst = f.id AND f.kind = 'file'
                  WHERE ep.kind = 'entry_point'
                    AND json_extract(ep.attrs_json, '$.source') = 'pyproject.scripts'
              """).fetchone()
              if row is None:
                  pytest.skip("sample_monorepo pyproject has no [project.scripts] — fixture-dependent assertion skipped")
              ep_name, file_path = row
              # The implemented_by file must live inside mypkg's import root —
              # SC#4 "path-qualified resolution, not ambiguous name=main match"
              assert "mypkg" in file_path, (
                  f"implemented_by file '{file_path}' is outside mypkg's tree — strict resolution failed"
              )
          finally:
              conn.close()

    Imports: handle the case where `init_repo` / `write_and_commit` aren't
    exposed under that name by inspecting tests/_git_repo.py and adapting.
    If the helper module exports them differently (e.g. as
    `_init_repo` / `_write_and_commit`), import accordingly.
  </action>
  <verify>
    <automated>cd /Users/pat/Personal/agent-research && uv run --package graph-io pytest packages/graph-io/tests/test_test_suites.py::test_call_order_pitfall packages/graph-io/tests/test_test_suites.py::test_strict_tree_invariant_raises_on_duplicate_parent packages/graph-io/tests/test_test_suites.py::test_anti_regression_describe_package_smoke packages/graph-io/tests/test_test_suites.py::test_shebang_script_in_fixture_does_not_emit_entry_point packages/graph-io/tests/test_test_suites.py::test_pyproject_scripts_entry_point_resolves_to_file -v</automated>
    <automated>cd /Users/pat/Personal/agent-research && uv run --package graph-io pytest packages/graph-io/tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'def test_call_order_pitfall\|def test_strict_tree_invariant_raises_on_duplicate_parent\|def test_anti_regression_describe_package_smoke\|def test_shebang_script_in_fixture_does_not_emit_entry_point\|def test_pyproject_scripts_entry_point_resolves_to_file' packages/graph-io/tests/test_test_suites.py` returns 5
    - All five new tests pass: `uv run --package graph-io pytest packages/graph-io/tests/test_test_suites.py -k "call_order or strict_tree or describe_package or shebang or pyproject_scripts_entry_point" -q` exits 0
    - The entire graph-io test suite still passes: `uv run --package graph-io pytest packages/graph-io/tests/ -q` exits 0
    - The fixture exists at the expected location: `test -d packages/graph-io/tests/fixtures/sample_monorepo` exits 0
  </acceptance_criteria>
  <done>
    Five integration-grade tests prove Phase 30's success criteria via SQL
    against the post-update.run DB; the strict-tree invariant fires when
    corrupted; the entire test suite passes.
  </done>
</task>

</tasks>

<verification>
- Full graph-io test suite passes: `uv run --package graph-io pytest packages/graph-io/tests/ -v` exits 0.
- update.py has all four Phase 30 surfaces: `grep -c 'entry_points.emit\|test_suites.emit\|_enforce_strict_tree_invariant\|StrictTreeInvariantError' packages/graph-io/src/graph_io/update.py` returns at least 5.
- The fixture extension files exist and are correctly imported by their tests.
- All Phase 30 success criteria (SC#1 — entry points; SC#2 — suites; SC#3 — call-order pitfall; SC#4 — path-qualified resolution; SC#5 — anti-regression) are exercised by SQL surrogates in `test_test_suites.py` (CLI verification is Phase 33).
</verification>
