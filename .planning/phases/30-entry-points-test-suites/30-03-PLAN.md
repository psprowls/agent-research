---
phase: 30-entry-points-test-suites
plan: 03
type: execute
wave: 2
depends_on:
  - 30-01
files_modified:
  - packages/graph-io/src/graph_io/test_suites.py
  - packages/graph-io/tests/test_test_suites.py
autonomous: true
requirements:
  - TEST-01
  - TEST-02
  - TEST-03
  - TEST-04
  - TEST-05
  - TEST-06
  - TEST-07
tags:
  - graph-io
  - test-suites
  - emitter
  - re-parenting

must_haves:
  truths:
    - "A new module packages/graph-io/src/graph_io/test_suites.py exists with a single public emit(conn, *, repo_root, ctx, skip_dirs) function"
    - "TestSuite nodes are emitted from filesystem layout: each immediate subdir of repo-root tests/ becomes one suite; tests/ itself becomes a suite if it holds test files directly (TEST-02, D-15 case 3)"
    - "Package-local tests/ subdirs (and __tests__/ for JS) become TestSuite nodes contained by their Package via physically_contains (TEST-03)"
    - "TestSuites are flat — no TestSuite -> TestSuite nesting even when tests/integration/auth/ suggests it (TEST-07, D-16)"
    - "TestSuite node naming: repo-root suites get name=<rel path from repo-root> (e.g. 'tests/integration'); package-local suites get name='tests' or '__tests__' with path=<rel path from repo-root> as the discriminator (D-16, Claude's discretion exercised)"
    - "Every TestSuite carries uri=test_suite_uri(ctx, <rel path>) and attrs including: kind (unit/integration/e2e/contract/unknown per D-17), path, language hint (python/javascript/null if mixed)"
    - "TestSuite.kind classification follows D-17 priority order: directory name contains 'integration' -> integration; 'e2e' or 'system' -> e2e; 'contract' or filenames matching *_spec.{py,js,ts} -> contract; filenames matching test_*.py / *_test.{py,js,ts} / *.test.{js,ts} -> unit; otherwise unknown"
    - "Test files are re-parented from physically_contains Repository -> File (Phase 29 D-14 placement) to physically_contains TestSuite -> File using DELETE-then-INSERT atomic within the outer update.run transaction (D-14)"
    - "Re-parenting is idempotent: a second emit() run produces identical edge sets (re-running cg update --full --force produces byte-identical physically_contains and tests edge tables)"
    - "Test files outside any tests/ ancestor cannot occur after Plan 30-01's D-01 amendment (is_test=false); if they do reach re-parenting, log an error to stderr and leave the Repository -> File edge in place (D-15 case 4)"
    - "Suite assignment for a test_file follows D-15 cascade: (1) inside Package's tests/ subdir -> Package's TestSuite; (2) inside repo-root tests/<subdir>/ -> the <subdir> suite; (3) inside repo-root tests/ flat -> the single tests/ suite contained by Repository; (4) error case described above"
    - "tests edges are derived from imports in every File contained by the TestSuite: TestSuite -> Package for each first-party package imported by any test file in the suite (D-09)"
    - "First-party package matching is map-driven (D-10, D-11): map of importable_name -> Package built once at start of emit() from existing Package nodes; Python imports look up top-level module name (D-10); JS/TS imports look up bare-spec against Package.name from package.json (D-11), and resolve relative imports via _owning_package + ancestry lookup (D-11)"
    - "Unknown imports (stdlib, third-party) are silently skipped — no warning, no edge (D-10)"
    - "When a suite imports from >=5 distinct first-party packages, emit one additional TestSuite -> Repository edge alongside the per-package edges (D-12); the threshold is exposed as _REPOSITORY_EDGE_THRESHOLD = 5 module-private constant"
    - "TestSuite -> Domain edges are NOT emitted in Phase 30 — deferred to Phase 31 after Domain nodes exist (D-13)"
    - "Framework config (pyproject [tool.pytest.ini_options] testpaths, pytest.ini, jest/vitest config 'roots') contributes only ADDITIONAL test root directories beyond conventional tests/ — config does not override suite naming or kind (D-18)"
    - "Malformed framework config logs a warning and falls back to filesystem-only detection (D-18)"
    - "Unit tests in tests/test_test_suites.py cover: (a) repo-root tests/<subdir>/ creates one TestSuite per subdir; (b) repo-root tests/ flat with no subdirs creates a single suite; (c) package-local tests/ creates a Package-contained suite; (d) JS __tests__/ same as (c); (e) re-parenting moves test File from Repository to TestSuite; (f) tests edges TestSuite -> Package via Python imports; (g) tests edges via JS imports; (h) relative imports resolve via _owning_package; (i) >=5 distinct package imports adds TestSuite -> Repository edge; (j) TestSuite.kind classification for integration/e2e/contract/unit/unknown; (k) framework config testpaths adds extra root; (l) malformed framework config does not crash; (m) idempotency — running emit twice produces identical edges"
  artifacts:
    - path: "packages/graph-io/src/graph_io/test_suites.py"
      provides: "emit(conn, *, repo_root, ctx, skip_dirs) — TestSuite node + physically_contains re-parenting + tests edges"
      contains: "def emit"
      exports:
        - emit
    - path: "packages/graph-io/tests/test_test_suites.py"
      provides: "Per-rule unit tests for test_suites.emit"
      contains: "test_repo_root_subdirs_become_suites"
  key_links:
    - from: "test_suites.emit"
      to: "TestSuite nodes"
      via: "FS scan of conventional + config-discovered roots; one per subdir; flat (no nesting)"
      pattern: "kind=\"test_suite\""
    - from: "Test File nodes"
      to: "TestSuite (physically_contains parent)"
      via: "DELETE from edges WHERE kind='physically_contains' AND dst=<file_id>; INSERT (suite_id, file_id, 'physically_contains')"
      pattern: "DELETE FROM edges"
    - from: "TestSuite nodes"
      to: "Package nodes (tests edges)"
      via: "import scan of test files + map lookup against importable_name -> Package"
      pattern: "kind=\"tests\""
---

<objective>
Create a new emitter module
`packages/graph-io/src/graph_io/test_suites.py` with a single public
`emit(conn, *, repo_root, ctx, skip_dirs)` function that:

1. Discovers test root directories — conventional (`tests/`, `__tests__/`,
   package-local `<pkg>/tests/`) plus framework-config-declared roots
   (pyproject `[tool.pytest.ini_options] testpaths`, jest/vitest `roots`).
2. Emits one flat `TestSuite` node per discovered root, with `kind`
   classified per D-17 and contained (`physically_contains`) by the
   appropriate Repository or Package.
3. Re-parents every test File node from Phase 29's
   `physically_contains Repository → File` placement to
   `physically_contains TestSuite → File` using DELETE-then-INSERT atomic
   within the existing `update.run` transaction.
4. Derives `tests` edges from imports: `TestSuite → Package` for every
   first-party package imported by any test file in the suite; plus an
   additional `TestSuite → Repository` edge when ≥5 distinct first-party
   packages are imported (D-12).

This module is independently invocable from tests; wiring into `update.run`
happens in Plan 30-04.

Output: `test_suites.emit` exists, exhaustively unit-tested per rule, and
re-parenting produces the strict-tree shape that the runtime invariant in
Plan 30-04 enforces.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-entry-points-test-suites/30-CONTEXT.md
@.planning/phases/30-entry-points-test-suites/30-RESEARCH.md
@.planning/research/ONTOLOGY-SPEC.md
@packages/graph-io/src/graph_io/structural_nodes.py
@packages/graph-io/src/graph_io/packages.py
@packages/graph-io/src/graph_io/update.py
@packages/graph-io/src/graph_io/uri.py
@packages/graph-io/src/graph_io/upsert.py
@packages/graph-io/src/graph_io/_ignore.py
@packages/graph-io/src/graph_io/schema.py

<interfaces>
<!-- Surfaces test_suites.emit consumes. -->

From packages/graph-io/src/graph_io/structural_nodes.py (after Plan 30-01):
  _owning_package(rel_path: str, pkg_index: list[tuple[str, str, str|None]]) -> tuple[str, str|None] | None
  _resolve_import_root(pkg_dir: Path, importable: str) -> Path | None
  _TEST_DIR_NAMES: frozenset = {"tests", "__tests__", "test"}

From packages/graph-io/src/graph_io/uri.py:
  test_suite_uri(ctx: RepoContext, suite_name: str) -> str   # already present, Phase 28

From packages/graph-io/src/graph_io/upsert.py:
  upsert_records(conn, GraphRecords)

From packages/graph-io/src/graph_io/_ignore.py:
  should_skip(path_str, skip_dirs) -> bool

Existing schema:
  CREATE TABLE nodes (id, kind, name, path, line, attrs_json, uri, UNIQUE(kind, name, path))
  CREATE TABLE edges (id, src, dst, kind, attrs_json, UNIQUE(src, dst, kind))

Existing Package + File rows shape after Phase 29:
  Package: kind='package', name=<manifest name>, path=<repo-rel dir>, attrs.language='python'|'javascript'|'typescript'
  File: kind='file', name=<rel path>, path=<rel path>, attrs.is_test bool, attrs.language

Existing Repository row shape after Phase 29:
  kind='repository', name=ctx.repo, path=None, attrs.uri=repo_uri(ctx)

stdlib only (per STACK.md):
  tomllib, json, re, sys
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create test_suites.py module skeleton with test-root discovery</name>
  <read_first>
    - .planning/phases/30-entry-points-test-suites/30-CONTEXT.md (D-15 — suite assignment cascade; D-16 — naming; D-17 — kind classification; D-18 — framework config rules)
    - .planning/phases/30-entry-points-test-suites/30-RESEARCH.md (§5 TestSuite emission strategy — exhaustive)
    - packages/graph-io/src/graph_io/structural_nodes.py (analog module shape; _TEST_DIR_NAMES, _resolve_import_root, _owning_package)
    - packages/graph-io/src/graph_io/entry_points.py (sibling module from Plan 30-02 — module structure analog)
  </read_first>
  <files>packages/graph-io/src/graph_io/test_suites.py</files>
  <behavior>
    - test_suites.py exists with module docstring, imports, module-private constants (_REPOSITORY_EDGE_THRESHOLD=5, _SPEC_FILENAME_GLOBS, _PYTEST_INI_FILENAMES = {"pytest.ini"}, _JS_TEST_CONFIG_GLOBS = ("jest.config.*", "vitest.config.*"))
    - Helper _discover_test_roots(repo_root, skip_dirs, pkg_rows) -> list[TestRoot] returns a list of typed/tuple structures: TestRoot(rel_path, owner: "repository" | ("package", pkg_name, pkg_rel), language: str|None)
    - Conventional discovery: walk every Package's directory looking for tests/ and __tests__/ subdirs at depth 1; walk repo-root tests/ — if it has subdirs, each subdir becomes a TestRoot; if it has files directly (no subdirs), tests/ itself becomes one TestRoot owned by Repository
    - Config discovery (D-18): for each Package, try to parse pyproject.toml [tool.pytest.ini_options] testpaths; pytest.ini [pytest] testpaths; package.json's adjacent jest.config.* (regex match testpaths/roots strings via simple eval — JS configs that aren't JSON-parseable get skipped with warning); pyproject testpaths produce TestRoots owned by Repository if testpath is relative to repo root, or by the owning Package per _owning_package
    - Helper _classify_suite_kind(suite_rel_path, file_rel_paths) -> str returns the D-17 kind based on directory name precedence and filename pattern fallback
    - Skip dirs from _ignore.should_skip respected throughout the FS walks
    - Public emit() signature in place but body is just `pass` (filled in subsequent tasks)
  </behavior>
  <action>
    Create packages/graph-io/src/graph_io/test_suites.py. Module shape:

      """TestSuite emitter: TestSuite nodes + physically_contains re-parenting + tests edges (TEST-01..07).

      Discovers test root directories from filesystem layout (conventional
      tests/, __tests__/ and package-local equivalents) and framework config
      (pyproject [tool.pytest.ini_options] testpaths, pytest.ini, jest/vitest
      'roots'). Emits one TestSuite per root, re-parents every is_test=true
      File node from Repository to its suite, and derives tests edges from
      import scans of the test files.

      Suites are flat (TEST-07, D-16): tests/integration/auth/ produces ONE
      suite named tests/integration/auth/, not nested suites.

      Phase 30 emits only TestSuite -> Package and TestSuite -> Repository
      edges; TestSuite -> Domain is Phase 31's responsibility (D-13).
      """

      from __future__ import annotations

      import json
      import re
      import sqlite3
      import sys
      import tomllib
      from dataclasses import dataclass
      from pathlib import Path
      from typing import Iterable

      from source_parser.projections.graph import GraphEdge, GraphNode, GraphRecords

      from graph_io import _ignore, upsert
      from graph_io.structural_nodes import (
          _TEST_DIR_NAMES,
          _owning_package,
          _resolve_import_root,
      )
      from graph_io.uri import RepoContext, test_suite_uri

      # --- Module-private constants ---

      _REPOSITORY_EDGE_THRESHOLD = 5   # D-12: K=5 for whole-system tests edge

      _SPEC_FILENAME_GLOBS = ("*_spec.py", "*_spec.js", "*_spec.ts")  # D-17 contract
      _UNIT_FILENAME_GLOBS = (
          "test_*.py", "*_test.py",
          "*.test.js", "*.test.ts", "*.test.jsx", "*.test.tsx",
          "*_test.js", "*_test.ts",
      )

      _PYTEST_INI_FILENAMES = frozenset({"pytest.ini"})
      _JS_TEST_CONFIG_GLOBS = ("jest.config.*", "vitest.config.*")

      _PYTHON_IMPORT_RE = re.compile(r"^\s*(?:from|import)\s+([\w\.]+)", re.MULTILINE)
      _JS_IMPORT_RE = re.compile(
          r"""(?:from|import|require)\s*\(?\s*['"]([^'"]+)['"]""",
          re.MULTILINE,
      )


      @dataclass(frozen=True)
      class _TestRoot:
          rel_path: str                              # e.g. "tests/integration" or "packages/foo/tests"
          owner_kind: str                            # "repository" or "package"
          owner_name: str | None                     # pkg_name when owner_kind=='package'
          owner_pkg_rel: str | None                  # pkg_rel when owner_kind=='package'
          language: str | None                       # 'python'|'javascript'|'typescript'|None (mixed)


      # --- Test root discovery (TEST-01, TEST-02, TEST-03, D-18) ---

      def _discover_test_roots(
          repo_root: Path,
          skip_dirs: frozenset[str],
          pkg_rows: list[tuple[str, str | None, str | None]],
      ) -> list[_TestRoot]:
          """Discover conventional + config-declared test root directories."""
          roots: list[_TestRoot] = []
          seen: set[str] = set()

          def _add(rel: str, owner_kind: str, owner_name=None, owner_pkg_rel=None, language=None):
              if rel in seen:
                  return
              if _ignore.should_skip(rel, skip_dirs):
                  return
              seen.add(rel)
              roots.append(_TestRoot(
                  rel_path=rel,
                  owner_kind=owner_kind,
                  owner_name=owner_name,
                  owner_pkg_rel=owner_pkg_rel,
                  language=language,
              ))

          # Repo-root tests/ — TEST-02: subdirs become suites; flat becomes one suite.
          repo_tests = repo_root / "tests"
          if repo_tests.is_dir():
              subdirs = [
                  d for d in repo_tests.iterdir()
                  if d.is_dir() and not _ignore.should_skip(d.name, skip_dirs)
              ]
              if subdirs:
                  for sub in sorted(subdirs):
                      rel = sub.relative_to(repo_root).as_posix()
                      _add(rel, "repository", language=None)
              else:
                  # Flat: tests/ itself is the suite (only if it holds files)
                  files = [f for f in repo_tests.iterdir() if f.is_file()]
                  if files:
                      _add("tests", "repository", language=None)

          # Package-local tests/ and __tests__/ — TEST-03.
          for pkg_name, pkg_rel, pkg_attrs_json in pkg_rows:
              pkg_attrs = json.loads(pkg_attrs_json) if pkg_attrs_json else {}
              lang = pkg_attrs.get("language")
              pkg_dir = (repo_root / pkg_rel) if pkg_rel else repo_root
              if not pkg_rel:
                  # Root package — already covered by repo-root tests/ scan above
                  continue
              for candidate in ("tests", "__tests__"):
                  cdir = pkg_dir / candidate
                  if cdir.is_dir():
                      rel = cdir.relative_to(repo_root).as_posix()
                      _add(rel, "package", owner_name=pkg_name, owner_pkg_rel=pkg_rel, language=lang)

          # Config-declared roots (D-18). Best-effort; malformed configs warn + skip.
          for pkg_name, pkg_rel, pkg_attrs_json in pkg_rows:
              pkg_attrs = json.loads(pkg_attrs_json) if pkg_attrs_json else {}
              lang = pkg_attrs.get("language")
              pkg_dir = (repo_root / pkg_rel) if pkg_rel else repo_root
              if lang == "python":
                  for cfg_paths in _read_pytest_testpaths(pkg_dir):
                      for tp_rel in cfg_paths:
                          full = (pkg_dir / tp_rel).resolve()
                          try:
                              rel = full.relative_to(repo_root).as_posix()
                          except ValueError:
                              continue
                          if not full.is_dir():
                              continue
                          # Determine owner by re-running owning-package lookup.
                          pkg_index_owner = _owning_package(rel, _build_pkg_index(pkg_rows))
                          if pkg_index_owner is None:
                              _add(rel, "repository", language="python")
                          else:
                              own_name, own_rel = pkg_index_owner
                              _add(rel, "package", owner_name=own_name, owner_pkg_rel=own_rel, language="python")

          return roots


      def _build_pkg_index(
          pkg_rows: list[tuple[str, str | None, str | None]],
      ) -> list[tuple[str, str, str | None]]:
          """Build the (pkg_prefix, pkg_name, pkg_rel) index sorted deepest-first
          for _owning_package consumption."""
          return sorted(
              (
                  (pkg_rel or "", pkg_name, pkg_rel)
                  for pkg_name, pkg_rel, _ in pkg_rows
              ),
              key=lambda t: len(t[0]),
              reverse=True,
          )


      def _read_pytest_testpaths(pkg_dir: Path) -> Iterable[list[str]]:
          """Yield testpaths lists from pyproject.toml and pytest.ini if present."""
          pp = pkg_dir / "pyproject.toml"
          if pp.exists():
              try:
                  data = tomllib.loads(pp.read_text(encoding="utf-8"))
                  testpaths = (
                      data.get("tool", {})
                          .get("pytest", {})
                          .get("ini_options", {})
                          .get("testpaths")
                  )
                  if isinstance(testpaths, list):
                      yield [str(x) for x in testpaths if isinstance(x, str)]
                  elif isinstance(testpaths, str):
                      yield testpaths.split()
              except tomllib.TOMLDecodeError as exc:
                  print(f"[test_suites] warning: {pp} malformed: {exc}", file=sys.stderr)

          ini = pkg_dir / "pytest.ini"
          if ini.exists():
              try:
                  text = ini.read_text(encoding="utf-8")
                  # Minimal ini parser: find `testpaths = X Y Z` on a single line
                  m = re.search(r"^\s*testpaths\s*=\s*(.+)$", text, re.MULTILINE)
                  if m:
                      yield m.group(1).split()
              except OSError as exc:
                  print(f"[test_suites] warning: {ini} unreadable: {exc}", file=sys.stderr)


      def _classify_suite_kind(suite_rel: str, file_rels: list[str]) -> str:
          """D-17 kind classification: dir-name precedence then filename fallback."""
          parts = suite_rel.lower().split("/")
          for p in parts:
              if "integration" in p:
                  return "integration"
              if p in {"e2e", "system"}:
                  return "e2e"
              if "contract" in p:
                  return "contract"
          # Filename fallback
          import fnmatch
          has_spec = any(
              fnmatch.fnmatch(Path(f).name, g)
              for f in file_rels for g in _SPEC_FILENAME_GLOBS
          )
          if has_spec:
              return "contract"
          has_unit = any(
              fnmatch.fnmatch(Path(f).name, g)
              for f in file_rels for g in _UNIT_FILENAME_GLOBS
          )
          if has_unit:
              return "unit"
          return "unknown"


      # --- Public emit() ---

      def emit(
          conn: sqlite3.Connection,
          *,
          repo_root: Path,
          ctx: RepoContext,
          skip_dirs: frozenset[str],
      ) -> None:
          """Emit TestSuite nodes + physically_contains re-parenting + tests edges."""
          # Filled in by Tasks 2 and 3.
          pass

    Verify module imports cleanly.
  </action>
  <verify>
    <automated>cd /Users/pat/Personal/agent-research && uv run --package graph-io python -c "from graph_io import test_suites; assert callable(test_suites.emit); assert callable(test_suites._discover_test_roots); assert callable(test_suites._classify_suite_kind); assert test_suites._REPOSITORY_EDGE_THRESHOLD == 5; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f packages/graph-io/src/graph_io/test_suites.py`
    - `grep -nE '^def emit' packages/graph-io/src/graph_io/test_suites.py` returns 1 line
    - `grep -nE '^def _discover_test_roots' packages/graph-io/src/graph_io/test_suites.py` returns 1 line
    - `grep -nE '^def _classify_suite_kind' packages/graph-io/src/graph_io/test_suites.py` returns 1 line
    - `grep -n '_REPOSITORY_EDGE_THRESHOLD = 5' packages/graph-io/src/graph_io/test_suites.py` returns 1 line
    - `grep -n '_owning_package' packages/graph-io/src/graph_io/test_suites.py` returns at least 2 lines (import + usage)
    - Module imports cleanly: verify command exits 0
  </acceptance_criteria>
  <done>
    Module skeleton with test-root discovery and kind classification in place.
    Tasks 2 and 3 fill in emit() body.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement emit body — TestSuite node emission + physically_contains re-parenting</name>
  <read_first>
    - .planning/phases/30-entry-points-test-suites/30-CONTEXT.md (D-14 — DELETE-then-INSERT pattern, atomic per file; D-15 — assign_suite cascade; D-16 — naming)
    - .planning/phases/30-entry-points-test-suites/30-RESEARCH.md (§6 re-parenting with corrected SQL using src/dst column names)
    - packages/graph-io/src/graph_io/test_suites.py (skeleton from Task 1)
    - packages/graph-io/src/graph_io/structural_nodes.py (emit() pattern — upsert.upsert_records call + GraphNode construction)
  </read_first>
  <files>packages/graph-io/src/graph_io/test_suites.py</files>
  <behavior>
    - emit() reads Package rows + Repository node id, discovers test roots via _discover_test_roots, builds TestSuite GraphNodes (one per root), and physically_contains edges (parent = Repository or owning Package)
    - After upserting TestSuite nodes via upsert.upsert_records, query their fresh row ids from the DB
    - Read all is_test=true File rows; for each, determine its assigning TestSuite via D-15 cascade; DELETE its existing physically_contains parent edge (whatever it is) and INSERT a new physically_contains edge from the suite to the file
    - If no suite matches (D-15 case 4 — shouldn't happen after D-01 amendment but be defensive): log error to stderr, leave the file untouched
    - Idempotent: a second call produces zero net edge changes
  </behavior>
  <action>
    Fill in the emit() body in test_suites.py:

      def emit(conn, *, repo_root, ctx, skip_dirs):
          repo_root = Path(repo_root).resolve()

          pkg_rows = conn.execute(
              "SELECT name, path, attrs_json FROM nodes WHERE kind='package'"
          ).fetchall()
          # Cast pkg_rows to typed list for downstream consumers
          pkg_rows_typed: list[tuple[str, str | None, str | None]] = [
              (r[0], r[1], r[2]) for r in pkg_rows
          ]

          # Discover test roots
          roots = _discover_test_roots(repo_root, skip_dirs, pkg_rows_typed)

          # Map repo-rel path -> set of test file rel-paths inside that root
          # (used by kind classification AND tests-edge derivation in Task 3)
          root_files: dict[str, list[str]] = {}
          for r in roots:
              root_files[r.rel_path] = []

          test_file_rows = conn.execute(
              "SELECT id, path FROM nodes WHERE kind='file' AND json_extract(attrs_json, '$.is_test') = 1"
          ).fetchall()

          def _assign_root(file_rel: str) -> _TestRoot | None:
              # D-15 cascade — deepest-prefix match wins
              best = None
              best_len = -1
              for r in roots:
                  if file_rel == r.rel_path or file_rel.startswith(r.rel_path + "/"):
                      if len(r.rel_path) > best_len:
                          best_len = len(r.rel_path)
                          best = r
              return best

          for file_id, file_rel in test_file_rows:
              r = _assign_root(file_rel)
              if r is None:
                  print(
                      f"[test_suites] warning: test file {file_rel} not under any "
                      f"discovered test root — leaving Repository -> File edge in place "
                      f"(D-15 case 4)",
                      file=sys.stderr,
                  )
                  continue
              root_files[r.rel_path].append(file_rel)

          # Build TestSuite nodes
          nodes: list[GraphNode] = []
          edges: list[GraphEdge] = []

          # Find Repository node id (needed for parent edges)
          repo_row = conn.execute(
              "SELECT id, name FROM nodes WHERE kind='repository'"
          ).fetchone()
          if repo_row is None:
              # Defensive: structural_nodes.emit hasn't run yet — abort.
              return
          repo_name = repo_row[1]
          repo_key = ("repository", repo_name, None)

          for r in roots:
              # Naming: repo-rel path for repository-owned suites; basename
              # for package-owned suites (path discriminator)
              if r.owner_kind == "repository":
                  suite_name = r.rel_path
              else:
                  suite_name = Path(r.rel_path).name  # 'tests' or '__tests__'

              kind_attr = _classify_suite_kind(r.rel_path, root_files[r.rel_path])

              attrs = {
                  "uri": test_suite_uri(ctx, r.rel_path),
                  "suite_kind": kind_attr,
                  "path": r.rel_path,
                  "owner_kind": r.owner_kind,
              }
              if r.language is not None:
                  attrs["language"] = r.language
              suite_node = GraphNode(
                  kind="test_suite",
                  name=suite_name,
                  path=r.rel_path,
                  line=None,
                  attrs=attrs,
              )
              nodes.append(suite_node)

              if r.owner_kind == "repository":
                  parent_src = repo_key
              else:
                  parent_src = ("package", r.owner_name, r.owner_pkg_rel)
              edges.append(GraphEdge(
                  src=parent_src,
                  dst=("test_suite", suite_name, r.rel_path),
                  kind="physically_contains",
                  attrs={},
              ))

          # Upsert TestSuite nodes + parent edges
          upsert.upsert_records(conn, GraphRecords(nodes=nodes, edges=edges))

          # Re-parent test files: DELETE then INSERT atomic (D-14)
          # The outer update.run already wraps emit in a transaction, so the
          # per-file DELETE+INSERT is atomic with the rest of the cg update.
          for r in roots:
              if r.owner_kind == "repository":
                  suite_key_name = r.rel_path
              else:
                  suite_key_name = Path(r.rel_path).name
              suite_row = conn.execute(
                  "SELECT id FROM nodes WHERE kind='test_suite' AND name=? AND path=?",
                  (suite_key_name, r.rel_path),
              ).fetchone()
              if suite_row is None:
                  continue
              suite_id = suite_row[0]

              for file_rel in root_files[r.rel_path]:
                  file_id_row = conn.execute(
                      "SELECT id FROM nodes WHERE kind='file' AND path=?",
                      (file_rel,),
                  ).fetchone()
                  if file_id_row is None:
                      continue
                  file_id = file_id_row[0]
                  conn.execute(
                      "DELETE FROM edges WHERE kind='physically_contains' AND dst=?",
                      (file_id,),
                  )
                  conn.execute(
                      "INSERT INTO edges (src, dst, kind, attrs_json) "
                      "VALUES (?, ?, 'physically_contains', NULL)",
                      (suite_id, file_id),
                  )

          # tests-edge derivation handled in Task 3 — for now, return.
          # Pass root_files forward via a helper function call.
          _emit_tests_edges(conn, repo_root, ctx, pkg_rows_typed, roots, root_files, repo_key)


      def _emit_tests_edges(
          conn, repo_root, ctx, pkg_rows_typed, roots, root_files, repo_key,
      ) -> None:
          """Stub — Task 3 fills this in."""
          pass

  </action>
  <verify>
    <automated>cd /Users/pat/Personal/agent-research && uv run --package graph-io python -c "from graph_io import test_suites; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'DELETE FROM edges' packages/graph-io/src/graph_io/test_suites.py` returns 1 line
    - `grep -n "INSERT INTO edges" packages/graph-io/src/graph_io/test_suites.py` returns 1 line
    - `grep -n 'kind="test_suite"' packages/graph-io/src/graph_io/test_suites.py` returns at least 1 line
    - `grep -n '_emit_tests_edges' packages/graph-io/src/graph_io/test_suites.py` returns at least 2 lines (call site + def)
    - `grep -n 'is_test' packages/graph-io/src/graph_io/test_suites.py` returns at least 1 line (filter query)
    - Module imports cleanly.
  </acceptance_criteria>
  <done>
    emit() emits TestSuite nodes, parent edges, and re-parents test files
    atomically inside the outer transaction.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement _emit_tests_edges — import scan + TestSuite -> Package + Repository edges</name>
  <read_first>
    - .planning/phases/30-entry-points-test-suites/30-CONTEXT.md (D-09, D-10, D-11 — derivation rules; D-12 — K=5 threshold; D-13 — no Domain edges in Phase 30)
    - .planning/phases/30-entry-points-test-suites/30-RESEARCH.md (§5 tests-edge derivation with regex import scans)
    - packages/graph-io/src/graph_io/test_suites.py (current state after Tasks 1-2)
  </read_first>
  <files>packages/graph-io/src/graph_io/test_suites.py</files>
  <behavior>
    - _emit_tests_edges scans every test file inside each TestSuite for imports, looks up first-party packages, and emits one tests edge per (suite, package) pair
    - Build importable -> Package map once: Python uses pkg_name.replace('-', '_') as the importable; JS/TS uses package.json "name" directly (already in Package.name)
    - Python imports: regex _PYTHON_IMPORT_RE against the test file contents extracts top-level module name (X from X.Y.Z); look up X in the map; if found, candidate edge
    - JS/TS imports: regex _JS_IMPORT_RE extracts the specifier; if it starts with "." or "/", treat as relative — resolve against importing file's dir, then look up _owning_package(resolved_rel, pkg_index); if it doesn't start with "." or "/", treat as bare spec — look up directly in the importable map
    - Deduplicate per (suite_id, package_id); emit one TestSuite -> Package edge per pair
    - When >=_REPOSITORY_EDGE_THRESHOLD (5) distinct packages were imported by a suite, also emit one TestSuite -> Repository edge
    - Edge kind is "tests" (matches ontology spec); attrs are empty
    - Read file contents inside a try/except for OSError; skip unreadable files
  </behavior>
  <action>
    Replace the _emit_tests_edges stub with the full implementation:

      def _emit_tests_edges(
          conn, repo_root, ctx, pkg_rows_typed, roots, root_files, repo_key,
      ) -> None:
          # Build importable -> (pkg_name, pkg_rel) map
          py_importable_to_pkg: dict[str, tuple[str, str | None]] = {}
          js_name_to_pkg: dict[str, tuple[str, str | None]] = {}

          for pkg_name, pkg_rel, pkg_attrs_json in pkg_rows_typed:
              attrs = json.loads(pkg_attrs_json) if pkg_attrs_json else {}
              lang = attrs.get("language")
              if lang == "python":
                  importable = pkg_name.replace("-", "_")
                  # Confirm import root exists; if not, still register the
                  # mapping — packages.refresh wrote the row, the test file
                  # might import it speculatively.
                  py_importable_to_pkg[importable] = (pkg_name, pkg_rel)
              elif lang in {"javascript", "typescript"}:
                  js_name_to_pkg[pkg_name] = (pkg_name, pkg_rel)

          pkg_index = _build_pkg_index(pkg_rows_typed)

          edges_out: list[GraphEdge] = []

          for r in roots:
              if r.owner_kind == "repository":
                  suite_key_name = r.rel_path
              else:
                  suite_key_name = Path(r.rel_path).name
              suite_key = ("test_suite", suite_key_name, r.rel_path)

              matched_pkgs: set[tuple[str, str | None]] = set()
              for file_rel in root_files[r.rel_path]:
                  fpath = repo_root / file_rel
                  try:
                      content = fpath.read_text(encoding="utf-8", errors="ignore")
                  except OSError:
                      continue

                  ext = Path(file_rel).suffix
                  if ext == ".py":
                      for m in _PYTHON_IMPORT_RE.finditer(content):
                          mod = m.group(1).split(".")[0]
                          if mod in py_importable_to_pkg:
                              matched_pkgs.add(py_importable_to_pkg[mod])
                  elif ext in {".js", ".jsx", ".ts", ".tsx", ".mjs", ".cjs"}:
                      for m in _JS_IMPORT_RE.finditer(content):
                          spec = m.group(1)
                          if spec.startswith(".") or spec.startswith("/"):
                              # Relative import — resolve against importing file dir
                              try:
                                  resolved = (fpath.parent / spec).resolve()
                              except Exception:
                                  continue
                              # Try common JS resolution extensions
                              for cand in (
                                  resolved,
                                  resolved.with_suffix(".ts"),
                                  resolved.with_suffix(".js"),
                                  resolved.with_suffix(".tsx"),
                                  resolved.with_suffix(".jsx"),
                                  resolved / "index.ts",
                                  resolved / "index.js",
                              ):
                                  if cand.exists():
                                      try:
                                          rel = cand.relative_to(repo_root).as_posix()
                                      except ValueError:
                                          continue
                                      owner = _owning_package(rel, pkg_index)
                                      if owner is not None:
                                          matched_pkgs.add(owner)
                                      break
                          else:
                              # Bare spec — look up in JS name map; first segment for scoped
                              key = spec
                              if spec.startswith("@"):
                                  key = "/".join(spec.split("/", 2)[:2])
                              else:
                                  key = spec.split("/", 1)[0]
                              if key in js_name_to_pkg:
                                  matched_pkgs.add(js_name_to_pkg[key])

              # Emit TestSuite -> Package edges (one per distinct package)
              for pkg_name, pkg_rel in matched_pkgs:
                  edges_out.append(GraphEdge(
                      src=suite_key,
                      dst=("package", pkg_name, pkg_rel),
                      kind="tests",
                      attrs={},
                  ))

              # D-12: K=5 threshold for whole-system edge
              if len(matched_pkgs) >= _REPOSITORY_EDGE_THRESHOLD:
                  edges_out.append(GraphEdge(
                      src=suite_key,
                      dst=repo_key,
                      kind="tests",
                      attrs={},
                  ))

          if edges_out:
              upsert.upsert_records(conn, GraphRecords(nodes=[], edges=edges_out))

  </action>
  <verify>
    <automated>cd /Users/pat/Personal/agent-research && uv run --package graph-io python -c "from graph_io import test_suites; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'kind="tests"' packages/graph-io/src/graph_io/test_suites.py` returns at least 2 lines (Package edge + Repository edge)
    - `grep -n '_REPOSITORY_EDGE_THRESHOLD' packages/graph-io/src/graph_io/test_suites.py` returns at least 2 lines (constant + comparison)
    - `grep -n 'py_importable_to_pkg' packages/graph-io/src/graph_io/test_suites.py` returns at least 2 lines
    - `grep -n '_PYTHON_IMPORT_RE\|_JS_IMPORT_RE' packages/graph-io/src/graph_io/test_suites.py` returns at least 4 lines (constants + regex usage)
    - Module imports cleanly.
  </acceptance_criteria>
  <done>
    _emit_tests_edges scans Python and JS/TS imports, looks up first-party
    packages, deduplicates, and emits the K=5 whole-system edge when warranted.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Add comprehensive unit tests in tests/test_test_suites.py</name>
  <read_first>
    - packages/graph-io/src/graph_io/test_suites.py (full file after Tasks 1-3)
    - packages/graph-io/tests/test_structural_nodes.py (analog test patterns)
    - packages/graph-io/tests/test_entry_points.py (sibling test module from Plan 30-02 if visible — same pattern)
  </read_first>
  <files>packages/graph-io/tests/test_test_suites.py</files>
  <behavior>
    - tests/test_test_suites.py exists with at least 13 distinct test functions per the must_haves truths (a)..(m)
    - Each test follows the pattern: tmp_path → write manifests + source + test files → store.connect + schema init → packages.refresh → structural_nodes.emit → test_suites.emit → SQL assertions
    - Tests must use the actual Phase 29 structural_nodes.emit (since the re-parenting depends on the Repository->File edges existing first) — this means each test runs the full structural+TestSuite chain
    - Idempotency test (m): run emit() twice, snapshot the edges table after each, assert identical edge sets
    - For (i), set up a fixture suite that imports from 5+ Python packages and verify both per-package edges AND the Repository edge exist
  </behavior>
  <action>
    Create packages/graph-io/tests/test_test_suites.py. Skeleton:

      """Unit tests for test_suites.emit (TEST-01..07)."""

      from __future__ import annotations

      import json
      import sqlite3
      from pathlib import Path

      import pytest

      from graph_io import packages, structural_nodes, store, test_suites
      from graph_io.uri import RepoContext

      CTX = RepoContext(org="testorg", repo="testrepo")


      def _setup(tmp_path: Path) -> sqlite3.Connection:
          conn = store.connect(tmp_path / "code.db", create=True)
          return conn


      def _run_emit_pipeline(conn, repo_root: Path):
          """Run packages.refresh + structural_nodes.emit + test_suites.emit
          inside a single transaction (mirrors update.run order)."""
          with store.transaction(conn):
              packages.refresh(conn, repo_root=repo_root, ctx=CTX)
              structural_nodes.emit(conn, repo_root=repo_root, ctx=CTX, skip_dirs=frozenset())
              test_suites.emit(conn, repo_root=repo_root, ctx=CTX, skip_dirs=frozenset())


      # ... test functions per the must_haves list (a-m) ...

    Implement all 13 tests:

      (a) test_repo_root_subdirs_become_suites — repo with tests/integration/
          and tests/unit/ both holding test files; emit produces exactly 2
          TestSuite nodes named "tests/integration" and "tests/unit".

      (b) test_repo_root_flat_tests_creates_single_suite — repo with tests/
          containing test_foo.py directly (no subdirs); emit produces exactly
          1 TestSuite named "tests".

      (c) test_package_local_tests_dir_is_package_contained — repo with
          packages/foo/tests/test_bar.py; emit produces a TestSuite with name
          "tests" path "packages/foo/tests" and the parent edge is
          Package(foo) -> TestSuite, not Repository -> TestSuite.

      (d) test_jsts_underscores_tests_dir_same_as_c — repo with
          packages/jspkg/__tests__/index.test.js; same shape as (c) but with
          __tests__ as the dir.

      (e) test_test_file_re_parented_from_repository_to_suite — assert that
          after emit, the test file's only physically_contains parent is the
          TestSuite, NOT the Repository (the Phase 29 placement edge is gone).

      (f) test_tests_edge_python_imports — a Python test file with
          `from foo_pkg import bar` produces a TestSuite -> Package(foo_pkg)
          tests edge.

      (g) test_tests_edge_js_bare_imports — a JS test file with
          `import { x } from 'jspkg'` produces a TestSuite -> Package(jspkg).

      (h) test_tests_edge_js_relative_imports — a JS test file with
          `import x from '../src/foo'` resolves via _owning_package to the
          containing JS package and emits a tests edge.

      (i) test_tests_edge_repository_threshold — a test file importing from
          5 distinct first-party Python packages produces 5 per-package edges
          AND 1 TestSuite -> Repository edge.

      (j) test_suite_kind_classification — verify each branch of D-17:
            tests/integration/ -> kind="integration"
            tests/e2e/ -> kind="e2e"
            tests/contract/ -> kind="contract"
            tests/_spec_files (containing *_spec.py) -> kind="contract"
            tests/unit/ (containing test_x.py) -> kind="unit"
            tests/misc/ (containing empty.txt) -> kind="unknown"

      (k) test_framework_config_testpaths_adds_root — a pyproject.toml with
          [tool.pytest.ini_options] testpaths = ["spec"] makes spec/ a
          discovered TestSuite.

      (l) test_malformed_pyproject_does_not_crash — invalid TOML in a
          [tool.pytest.ini_options] block; emit() completes normally; stderr
          captures the warning.

      (m) test_idempotency_two_runs_identical_edges — run emit() twice in
          sequence (inside separate transactions); snapshot physically_contains
          + tests edges after each; assert byte-identical row sets.

    For tests that need 5+ Python packages (i), define helper that creates
    each with packages/<n>/pyproject.toml + packages/<n>/src/<n>/__init__.py,
    using a parameter loop.
  </action>
  <verify>
    <automated>cd /Users/pat/Personal/agent-research && uv run --package graph-io pytest packages/graph-io/tests/test_test_suites.py -v</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f packages/graph-io/tests/test_test_suites.py`
    - `grep -cE '^def test_' packages/graph-io/tests/test_test_suites.py` returns at least 13
    - All named tests exist: `grep -cE 'def test_(repo_root_subdirs_become_suites|repo_root_flat_tests_creates_single_suite|package_local_tests_dir_is_package_contained|jsts_underscores_tests_dir_same_as_c|test_file_re_parented_from_repository_to_suite|tests_edge_python_imports|tests_edge_js_bare_imports|tests_edge_js_relative_imports|tests_edge_repository_threshold|suite_kind_classification|framework_config_testpaths_adds_root|malformed_pyproject_does_not_crash|idempotency_two_runs_identical_edges)' packages/graph-io/tests/test_test_suites.py` returns 13
    - `uv run --package graph-io pytest packages/graph-io/tests/test_test_suites.py -q` exits 0
  </acceptance_criteria>
  <done>
    test_test_suites.py exercises every rule with happy + edge cases; all
    tests pass.
  </done>
</task>

</tasks>

<verification>
- `uv run --package graph-io pytest packages/graph-io/tests/test_test_suites.py -v` passes all 13+ tests.
- `uv run --package graph-io python -c "from graph_io.test_suites import emit; print('ok')"` exits 0.
- `grep -c 'kind="test_suite"' packages/graph-io/src/graph_io/test_suites.py` returns at least 1.
- `grep -c '_REPOSITORY_EDGE_THRESHOLD' packages/graph-io/src/graph_io/test_suites.py` returns at least 2.
- No new dependency added: `grep -E '^(import|from)' packages/graph-io/src/graph_io/test_suites.py | grep -vE '(__future__|json|re|sqlite3|sys|tomllib|fnmatch|dataclasses|pathlib|typing|source_parser|graph_io)' | wc -l` returns 0.
</verification>
