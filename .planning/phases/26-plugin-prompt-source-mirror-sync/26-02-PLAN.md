---
phase: 26-plugin-prompt-source-mirror-sync
plan: 02
type: execute
wave: 2
depends_on:
  - 26-01
files_modified:
  - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/architecture_overview.py
  - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/citation_rules.py
  - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/claude_md_disambiguation.py
  - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/frontmatter_rules.py
  - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/iron_rules.py
  - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/log_format.py
  - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/page_categories.py
  - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/style_rules.py
  - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/scanner.py
  - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/linter.py
  - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/ingestor.py
  - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/librarian.py
  - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/code_reader.py
  - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/synthesizer.py
  - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/sources/code_reader.md
  - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/sources/synthesizer.md
  - packages/workspace-io/src/workspace_io/assets/CLAUDE.md.template
  - packages/eval-harness/src/eval_harness/divergence/scanner.py
  - packages/eval-harness/src/eval_harness/divergence/ingestor.py
  - packages/eval-harness/src/eval_harness/divergence/librarian.py
  - packages/eval-harness/src/eval_harness/divergence/linter.py
  - packages/eval-harness/src/eval_harness/divergence/synthesizer.py
  - packages/eval-harness/src/eval_harness/divergence/code_reader.py
  - packages/eval-harness/src/eval_harness/divergence/check.py
  - packages/eval-harness/src/eval_harness/divergence/rubrics/ingestor.md
  - packages/eval-harness/src/eval_harness/divergence/rubrics/librarian.md
  - packages/eval-harness/src/eval_harness/divergence/rubrics/linter.md
  - packages/eval-harness/src/eval_harness/divergence/rubrics/scanner.md
  - packages/eval-harness/src/eval_harness/divergence/rubrics/synthesizer.md
  - packages/eval-harness/src/eval_harness/divergence/rubrics/code_reader.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "D-01: After this plan, ZERO `# Source:` / `<!-- Source: -->` / `source_anchor=` / prose `Anchors ...` literals reference `packages/prompt-sources/` anywhere in the working tree (the directory still exists but is fully un-referenced; deletion happens in Plan 04)."
    - "D-03: Every re-anchored pointer uses a GitHub-slug-compatible section name; no custom slugs; no HTML `<a id=...>` markers added to plugin .md."
    - "D-05: Every `# Source:` comment is the Option A 1-line shape `# Source: <path> §<section>` (or `<!-- Source: <path> §<section> -->` for HTML). No `# Anchor:` line. No `# Source-commit:` line. No `(L<n>-L<m>)` line-range pin."
    - "D-06: The new tree `agents/graph-wiki-agent/src/graph_wiki_agent/prompts/sources/` exists with two files: `code_reader.md` and `synthesizer.md`, both verbatim ports of `packages/prompt-sources/agents/{code_reader,synthesizer}.md` with the `lattice` → `graph-wiki` rebrand applied (frontmatter `skills: [lattice-wiki, ...]` → `skills: [graph-wiki, ...]`). No `__init__.py` (markdown-only tree per Claude's Discretion + PATTERNS recommendation)."
    - "D-07: `packages/workspace-io/src/workspace_io/assets/CLAUDE.md.template` gains a `## Log format` heading and a `## Style` heading with content sourced from the current `_fragments/log_format.py` / `_fragments/style_rules.py` constants (this is the `restore content` resolution from `26-AUDIT.md`)."
    - "D-07: After restoring the headings, the `# Source:` comments in `_fragments/log_format.py` and `_fragments/style_rules.py` resolve cleanly to the template's new headings via GitHub-slug match (slug `log-format`, slug `style`)."
    - "PATTERNS issue 1 resolved: `## Log format` and `## Style` exist in `CLAUDE.md.template` and contain the corresponding fragment-level content."
    - "PATTERNS issue 2 resolved: Option A 1-line provenance shape applied uniformly across all 8 fragment files, 4 prompt-builder source-comment sites, 2 newly-added Bedrock-only `# Source:` comments, and all 6 rubric HTML headers."
    - "PATTERNS issue 3 acknowledged: linter.py L55 carries the em-dash literal `§Pass 2 — Semantic (read and think), §Rules` exactly; Plan 03's `test_provenance.py` rewrite must verify slug `pass-2--semantic-read-and-think` resolves."
    - "D-13 step 2: re-anchor is complete and committed before Plan 03 runs."
    - "Every eval-harness `source_anchor=` literal (~23 across 6 files) is re-pointed per the audit; `grep -rn 'packages/prompt-sources' packages/eval-harness/src/eval_harness/divergence/` returns 0 hits after this plan."
    - "Every divergence/check.py docstring example (L59-60) is re-pointed to `plugins/graph-wiki/skills/graph-wiki/SKILL.md#iron-rules`."
    - "Every prose `Anchors ...` line in divergence/synthesizer.py and divergence/code_reader.py (10 sites) is re-pointed to the agent-local sources tree."
    - "Every rubric HTML header (6 files) is collapsed to 1-line shape with `<!-- Source-commit: -->` dropped and body-level `lattice-wiki` → `graph-wiki` rebrand applied per the audit."
  artifacts:
    - path: "agents/graph-wiki-agent/src/graph_wiki_agent/prompts/sources/code_reader.md"
      provides: "NEW agent-local source (verbatim port of packages/prompt-sources/agents/code_reader.md, with skills frontmatter rebrand to graph-wiki)."
      contains: "graph-wiki"
    - path: "agents/graph-wiki-agent/src/graph_wiki_agent/prompts/sources/synthesizer.md"
      provides: "NEW agent-local source (verbatim port with rebrand)."
      contains: "graph-wiki"
    - path: "packages/workspace-io/src/workspace_io/assets/CLAUDE.md.template"
      provides: "Adds ## Log format and ## Style headings with restored content (D-07)."
      contains: "## Log format"
  key_links:
    - from: "agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/log_format.py"
      to: "packages/workspace-io/src/workspace_io/assets/CLAUDE.md.template"
      via: "# Source: ... §Log format"
      pattern: "Source: packages/workspace-io/src/workspace_io/assets/CLAUDE.md.template §Log format"
    - from: "agents/graph-wiki-agent/src/graph_wiki_agent/prompts/code_reader.py"
      to: "agents/graph-wiki-agent/src/graph_wiki_agent/prompts/sources/code_reader.md"
      via: "# Source: ... §Rules, §Outputs, §Red flags"
      pattern: "Source: agents/graph-wiki-agent/src/graph_wiki_agent/prompts/sources/code_reader.md"
    - from: "packages/eval-harness/src/eval_harness/divergence/scanner.py"
      to: "plugins/graph-wiki/agents/scanner.md"
      via: "source_anchor= literals"
      pattern: "source_anchor=\"plugins/graph-wiki/agents/scanner\\.md#"
---

<objective>
Apply every resolution in `.planning/phases/26-plugin-prompt-source-mirror-sync/26-AUDIT.md`. Three coordinated sweeps: (1) re-anchor the 8 fragment + 6 prompt-builder + 2 Bedrock-only `# Source:` comments in `agents/graph-wiki-agent/src/graph_wiki_agent/prompts/` AND restore `## Log format` / `## Style` to `CLAUDE.md.template` AND create the two new `prompts/sources/*.md` files; (2) re-anchor every eval-harness `source_anchor=` literal, every prose `Anchors ...` line, and the `check.py` docstring example; (3) re-anchor every rubric HTML header and apply the `lattice-wiki` → `graph-wiki` body rebrand.

Purpose: After this plan, the working tree's anchor pointers all reference the canonical surfaces (plugin docs, `CLAUDE.md.template`, agent-local sources). The `packages/prompt-sources/` tree is now fully un-referenced — Plan 04 can delete it without orphaning any anchor. Plan 03 can rewrite `test_provenance.py` against the new shape and have a chance of passing.

Output: ~30 file edits (mechanical search-and-replace per the audit) + 2 new .md files + 2 new headings + restored content in `CLAUDE.md.template`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-plugin-prompt-source-mirror-sync/26-CONTEXT.md
@.planning/phases/26-plugin-prompt-source-mirror-sync/26-PATTERNS.md
@.planning/phases/26-plugin-prompt-source-mirror-sync/26-AUDIT.md

<interfaces>
The audit table is the single source of truth for resolutions. Each task below applies one section of the audit. Do NOT re-derive resolutions; copy them from the audit.

Heading-slug rule (D-03; needed for restoring CLAUDE.md.template headings): lowercase the heading text after stripping leading `#`+space; strip punctuation except hyphens/underscores; replace whitespace with `-`. So `## Log format` slugs to `log-format`; `## Style` slugs to `style`.

CLAUDE.md.template content to restore (sourced from current `_fragments/log_format.py` and `_fragments/style_rules.py` string constants — read those files to get the canonical text; do not rewrite from memory). The headings go inside the `## Conventions for LLM agents` section flow — insert `## Log format` and `## Style` as siblings to `## Conventions for LLM agents` (top-level `##`) at the end of the template, so they are discoverable via `grep -E '^## ' CLAUDE.md.template`.

Rebrand sweep on ported content (D-06): in BOTH new `prompts/sources/{code_reader,synthesizer}.md` files, replace `lattice-wiki` with `graph-wiki` (verified at frontmatter `skills:` line). Also scan the body for any other `lattice` literal and rewrite to `graph-wiki` for consistency — but preserve unrelated occurrences if any (none are expected per PATTERNS § Bedrock-only prompt constants).
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer commit → re-anchored tree | Mechanical search-and-replace introduces drift risk if a row is missed or applied to the wrong line |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-03 | Tampering | Missed or partial re-anchor | mitigate | acceptance grep gates assert zero `packages/prompt-sources` references remain in scope after the plan |
| T-26-04 | Tampering | `CLAUDE.md.template` content drift | mitigate | content for new `## Log format` / `## Style` is sourced verbatim from the existing fragment Python constants (no rewrite from memory) |

Severity: low. Mechanical edits with deterministic acceptance criteria.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Re-anchor prompts/ tree; restore CLAUDE.md.template headings; create prompts/sources/ files</name>
  <files>agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/architecture_overview.py, agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/citation_rules.py, agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/claude_md_disambiguation.py, agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/frontmatter_rules.py, agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/iron_rules.py, agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/log_format.py, agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/page_categories.py, agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/style_rules.py, agents/graph-wiki-agent/src/graph_wiki_agent/prompts/scanner.py, agents/graph-wiki-agent/src/graph_wiki_agent/prompts/linter.py, agents/graph-wiki-agent/src/graph_wiki_agent/prompts/ingestor.py, agents/graph-wiki-agent/src/graph_wiki_agent/prompts/librarian.py, agents/graph-wiki-agent/src/graph_wiki_agent/prompts/code_reader.py, agents/graph-wiki-agent/src/graph_wiki_agent/prompts/synthesizer.py, agents/graph-wiki-agent/src/graph_wiki_agent/prompts/sources/code_reader.md, agents/graph-wiki-agent/src/graph_wiki_agent/prompts/sources/synthesizer.md, packages/workspace-io/src/workspace_io/assets/CLAUDE.md.template</files>
  <read_first>
    - .planning/phases/26-plugin-prompt-source-mirror-sync/26-AUDIT.md (Tables 1, 2, 3 — fragment + prompt-builder + Bedrock-only)
    - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/iron_rules.py (full file — current 3-line header L1-3, then rest of the constant)
    - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/{architecture_overview,citation_rules,claude_md_disambiguation,frontmatter_rules,log_format,page_categories,style_rules}.py (each full file)
    - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/scanner.py (full file — L15-17 + the constant the header annotates)
    - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/linter.py (full file — sites at L17 docstring, L26, L55)
    - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/ingestor.py (L1-30 — the docstring at L5)
    - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/librarian.py (L1-30 — the docstring at L7)
    - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/code_reader.py (L1-10 — to confirm no current `# Source:` line; add one)
    - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/synthesizer.py (L1-10 — same)
    - packages/workspace-io/src/workspace_io/assets/CLAUDE.md.template (full file — confirms `## Layout`, `## Plugins installed`, `## Conventions for LLM agents` are the current top-level `##` headings; `## Log format` and `## Style` are absent)
    - packages/prompt-sources/agents/code_reader.md (full file — source content for the new `prompts/sources/code_reader.md`)
    - packages/prompt-sources/agents/synthesizer.md (full file — source content for `prompts/sources/synthesizer.md`)
  </read_first>
  <action>
For each of the 8 fragment files, replace the current 3-line `# Source: ... / # Anchor: ... / # Source-commit: ...` block at the top of the file with the single-line shape from the audit (per D-05 + Option A). Copy the exact target string from the audit's `proposed new 1-line shape` column. Preserve all subsequent module content (the string constant) unchanged.

For `prompts/scanner.py` L15-17: collapse the 3-line block to `# Source: plugins/graph-wiki/agents/scanner.md §Role, §Rules, §Red flags` (per audit Table 2). Verify the constant beneath the comment is unchanged.

For `prompts/linter.py`: rewrite the three sites per audit Table 2 rows: L17 docstring path-prefix rebrand only (`packages/prompt-sources/` → `plugins/graph-wiki/` with section text updated to `Pass 2 — Semantic (read and think) and Rules`); L26 → single-line `# Source: plugins/graph-wiki/agents/linter.md §Rules`; L55 → single-line `# Source: plugins/graph-wiki/agents/linter.md §Pass 2 — Semantic (read and think), §Rules`. The em-dash character is U+2014 — copy from `plugins/graph-wiki/agents/linter.md` heading verbatim.

For `prompts/ingestor.py` L5 docstring: rebrand path prefix `packages/prompt-sources/agents/ingestor.md` → `plugins/graph-wiki/agents/ingestor.md` (single string replace).

For `prompts/librarian.py` L7 docstring: same — `packages/prompt-sources/agents/librarian.md` → `plugins/graph-wiki/agents/librarian.md`.

For `prompts/code_reader.py`: ADD a new line immediately after the module docstring (after L3) reading exactly `# Source: agents/graph-wiki-agent/src/graph_wiki_agent/prompts/sources/code_reader.md §Rules, §Outputs, §Red flags` (per audit Table 3, per D-06).

For `prompts/synthesizer.py`: ADD a new line immediately after the module docstring (after L3) reading exactly `# Source: agents/graph-wiki-agent/src/graph_wiki_agent/prompts/sources/synthesizer.md §Rules` (per audit Table 3, per D-06).

Create the new directory `agents/graph-wiki-agent/src/graph_wiki_agent/prompts/sources/` with two files:
  - `code_reader.md` — verbatim content of `packages/prompt-sources/agents/code_reader.md` with `lattice-wiki` → `graph-wiki` in frontmatter `skills:` (and anywhere else in body if present).
  - `synthesizer.md` — verbatim content of `packages/prompt-sources/agents/synthesizer.md` with the same rebrand.
NO `__init__.py` in this directory (markdown-only tree per PATTERNS recommendation + Claude's Discretion).

Edit `packages/workspace-io/src/workspace_io/assets/CLAUDE.md.template`: append two new top-level sections at the end of the file (after the existing `## Conventions for LLM agents` block):
  - `## Log format` — body content sourced verbatim from the current string constant in `_fragments/log_format.py` (the LOG_FORMAT string after its provenance header). Do not rewrite or paraphrase; copy from the Python file.
  - `## Style` — body content sourced verbatim from the current string constant in `_fragments/style_rules.py` (the STYLE_RULES string).
Both headings use level-2 (`## `) so they slug to `log-format` and `style` respectively (D-07 + D-03 slug rule).

Do NOT modify any file outside this task's `<files>` list. Do NOT touch the `packages/prompt-sources/` tree itself (read-only inputs for the .md ports; deletion happens in Plan 04).
  </action>
  <verify>
    <automated>grep -rln 'packages/prompt-sources' agents/graph-wiki-agent/src/graph_wiki_agent/prompts/ | wc -l | awk '$1 == 0 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rln 'packages/prompt-sources' agents/graph-wiki-agent/src/graph_wiki_agent/prompts/` returns 0 lines (no remaining references in the prompts tree).
    - `grep -rc '^# Anchor: ' agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/ | awk -F: '{s+=$2} END {if (s==0) exit 0; else exit 1}'` succeeds (no `# Anchor:` lines remain in any fragment).
    - `grep -rc '^# Source-commit: ' agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/ | awk -F: '{s+=$2} END {if (s==0) exit 0; else exit 1}'` succeeds (no `# Source-commit:` lines remain in any fragment).
    - `grep -rcE '^# Source: ' agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/ | awk -F: '{s+=$2} END {if (s>=8) exit 0; else exit 1}'` succeeds (each of 8 fragments has at least one `# Source:` line).
    - `grep -E '^# Source: ' agents/graph-wiki-agent/src/graph_wiki_agent/prompts/code_reader.py | wc -l` returns at least 1 (D-06 new comment added).
    - `grep -E '^# Source: ' agents/graph-wiki-agent/src/graph_wiki_agent/prompts/synthesizer.py | wc -l` returns at least 1.
    - `test -f agents/graph-wiki-agent/src/graph_wiki_agent/prompts/sources/code_reader.md && test -f agents/graph-wiki-agent/src/graph_wiki_agent/prompts/sources/synthesizer.md` succeeds.
    - `grep -i 'lattice-wiki' agents/graph-wiki-agent/src/graph_wiki_agent/prompts/sources/*.md` returns 0 hits (rebrand applied).
    - `grep -E 'skills:.*graph-wiki' agents/graph-wiki-agent/src/graph_wiki_agent/prompts/sources/code_reader.md` returns at least 1 line (D-06 rebrand verified in frontmatter).
    - `grep -E '^## Log format$' packages/workspace-io/src/workspace_io/assets/CLAUDE.md.template` returns 1 line (D-07 heading restored).
    - `grep -E '^## Style$' packages/workspace-io/src/workspace_io/assets/CLAUDE.md.template` returns 1 line.
    - `test ! -f agents/graph-wiki-agent/src/graph_wiki_agent/prompts/sources/__init__.py` succeeds (markdown-only tree per Claude's Discretion).
    - `uv run --package graph-wiki-agent pytest -q --no-header 2>&1 | tail -5` shows a green summary, OR documents that `test_provenance.py` is the only failure (Plan 03 fixes it; other tests must remain green).
  </acceptance_criteria>
  <done>
    All 14 prompts/ files updated with Option A 1-line shape. Two new `prompts/sources/*.md` files created with rebrand. `CLAUDE.md.template` has `## Log format` and `## Style` headings with restored content. All other tests still pass (test_provenance.py may temporarily fail — Plan 03 is the fix).
  </done>
</task>

<task type="auto">
  <name>Task 2: Re-anchor eval-harness divergence/*.py (source_anchor literals, prose Anchors lines, check.py docstring)</name>
  <files>packages/eval-harness/src/eval_harness/divergence/scanner.py, packages/eval-harness/src/eval_harness/divergence/ingestor.py, packages/eval-harness/src/eval_harness/divergence/librarian.py, packages/eval-harness/src/eval_harness/divergence/linter.py, packages/eval-harness/src/eval_harness/divergence/synthesizer.py, packages/eval-harness/src/eval_harness/divergence/code_reader.py, packages/eval-harness/src/eval_harness/divergence/check.py</files>
  <read_first>
    - .planning/phases/26-plugin-prompt-source-mirror-sync/26-AUDIT.md (Tables 4 and 5)
    - packages/eval-harness/src/eval_harness/divergence/scanner.py (all `source_anchor=` literals — should be 4 per PATTERNS)
    - packages/eval-harness/src/eval_harness/divergence/ingestor.py (4 literals)
    - packages/eval-harness/src/eval_harness/divergence/librarian.py (4 literals)
    - packages/eval-harness/src/eval_harness/divergence/linter.py (3 literals)
    - packages/eval-harness/src/eval_harness/divergence/synthesizer.py (4 source_anchor= + 5 prose `Anchors ...` lines at L20, L36, L49, L66, L89)
    - packages/eval-harness/src/eval_harness/divergence/code_reader.py (4 source_anchor= + 5 prose lines at L15, L44, L61, L76, L91)
    - packages/eval-harness/src/eval_harness/divergence/check.py L55-65 (the docstring example at L59-60)
  </read_first>
  <action>
For each of the 6 `divergence/*.py` files: rewrite every `source_anchor="packages/prompt-sources/..."` literal to the new anchor string from the audit. The mapping is per Table 4 of `26-AUDIT.md`:
  - scanner.py: 4 literals → `plugins/graph-wiki/agents/scanner.md#<slug>` (one is the `#workflow-step-3` → `#3-create-stubs-for-new-packages` audit case)
  - ingestor.py: 3 → `plugins/graph-wiki/agents/ingestor.md#<slug>`, 1 → `plugins/graph-wiki/skills/graph-wiki/SKILL.md#<slug>` (the `#iron-rules` or whichever SKILL.md anchor)
  - librarian.py: 1 → `plugins/graph-wiki/skills/graph-wiki/SKILL.md#<slug>`, 3 → `plugins/graph-wiki/agents/librarian.md#<slug>`
  - linter.py: 3 → `plugins/graph-wiki/agents/linter.md#<slug>` (one is the `#workflow-pass-3` → `#pass-3--report` audit case)
  - synthesizer.py: 4 → `agents/graph-wiki-agent/src/graph_wiki_agent/prompts/sources/synthesizer.md#<slug>` (slugs preserved from verbatim port: `#rules`, `#red-flags`, etc.)
  - code_reader.py: 4 → `agents/graph-wiki-agent/src/graph_wiki_agent/prompts/sources/code_reader.md#<slug>` (same)

For the prose `Anchors ...` lines in `divergence/synthesizer.py` (L20, L36, L49, L66, L89): path-prefix rewrite `packages/prompt-sources/agents/synthesizer.md` → `agents/graph-wiki-agent/src/graph_wiki_agent/prompts/sources/synthesizer.md`. Section anchors after the `#` unchanged (verbatim ports preserve them).

Same for `divergence/code_reader.py` (L15, L44, L61, L76, L91): `packages/prompt-sources/agents/code_reader.md` → `agents/graph-wiki-agent/src/graph_wiki_agent/prompts/sources/code_reader.md`.

For `divergence/check.py` L59-60 docstring example: rewrite `packages/prompt-sources/SKILL.md#iron-rules` → `plugins/graph-wiki/skills/graph-wiki/SKILL.md#iron-rules` (mechanical single-site).

Do NOT modify the rubric .md files in this task — Task 3 handles them. Do NOT modify any test file in this task — Plan 03 handles `test_provenance.py`; other eval-harness tests should continue to pass without modification because their assertions don't pin specific anchor strings (verify with `uv run --package eval-harness pytest`).
  </action>
  <verify>
    <automated>grep -rln 'packages/prompt-sources' packages/eval-harness/src/eval_harness/divergence/ | wc -l | awk '$1 == 0 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rln 'packages/prompt-sources' packages/eval-harness/src/eval_harness/divergence/` returns 0 lines.
    - `grep -rcE 'source_anchor=' packages/eval-harness/src/eval_harness/divergence/*.py | awk -F: '{s+=$2} END {if (s>=20) exit 0; else exit 1}'` succeeds (~23 literals still present, just re-pointed).
    - `grep -E 'source_anchor="plugins/graph-wiki/agents/scanner\.md#3-create-stubs-for-new-packages"' packages/eval-harness/src/eval_harness/divergence/scanner.py` returns 1 line (audit case verified).
    - `grep -E 'source_anchor="plugins/graph-wiki/agents/linter\.md#pass-3--report"' packages/eval-harness/src/eval_harness/divergence/linter.py` returns 1 line (audit case verified).
    - `grep -E 'agents/graph-wiki-agent/src/graph_wiki_agent/prompts/sources/synthesizer\.md' packages/eval-harness/src/eval_harness/divergence/synthesizer.py | wc -l | awk '$1 >= 9 {exit 0} {exit 1}'` succeeds (4 source_anchor + 5 prose = 9 sites).
    - `grep -E 'agents/graph-wiki-agent/src/graph_wiki_agent/prompts/sources/code_reader\.md' packages/eval-harness/src/eval_harness/divergence/code_reader.py | wc -l | awk '$1 >= 9 {exit 0} {exit 1}'` succeeds.
    - `grep -E 'plugins/graph-wiki/skills/graph-wiki/SKILL\.md#iron-rules' packages/eval-harness/src/eval_harness/divergence/check.py` returns 1 line.
    - `uv run --package eval-harness pytest -q --no-header 2>&1 | tail -5` shows a green summary (eval-harness tests don't pin anchor strings; mechanical rewrite should not regress them).
  </acceptance_criteria>
  <done>
    All eval-harness divergence Python files re-pointed. ~33 sites total (23 source_anchor literals + 10 prose Anchors lines + 1 check.py docstring). Eval-harness pytest is green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Re-anchor rubric HTML headers; drop Source-commit; apply lattice→graph-wiki body rebrand</name>
  <files>packages/eval-harness/src/eval_harness/divergence/rubrics/ingestor.md, packages/eval-harness/src/eval_harness/divergence/rubrics/librarian.md, packages/eval-harness/src/eval_harness/divergence/rubrics/linter.md, packages/eval-harness/src/eval_harness/divergence/rubrics/scanner.md, packages/eval-harness/src/eval_harness/divergence/rubrics/synthesizer.md, packages/eval-harness/src/eval_harness/divergence/rubrics/code_reader.md</files>
  <read_first>
    - .planning/phases/26-plugin-prompt-source-mirror-sync/26-AUDIT.md (Table 6 — rubric HTML headers)
    - packages/eval-harness/src/eval_harness/divergence/rubrics/scanner.md (full file — L1-3 HTML header + body for `lattice` rebrand)
    - packages/eval-harness/src/eval_harness/divergence/rubrics/ingestor.md (same)
    - packages/eval-harness/src/eval_harness/divergence/rubrics/librarian.md (same)
    - packages/eval-harness/src/eval_harness/divergence/rubrics/linter.md (same)
    - packages/eval-harness/src/eval_harness/divergence/rubrics/synthesizer.md (same)
    - packages/eval-harness/src/eval_harness/divergence/rubrics/code_reader.md (same)
  </read_first>
  <action>
For each of the 6 rubric .md files, replace the current 3-line `<!-- Source: ... --> / <!-- Anchor: ... --> / <!-- Source-commit: ... -->` HTML header at L1-3 with a single new HTML comment line per audit Table 6:
  - rubrics/scanner.md → `<!-- Source: plugins/graph-wiki/agents/scanner.md §Rules, §Red flags -->`
  - rubrics/ingestor.md → `<!-- Source: plugins/graph-wiki/agents/ingestor.md §Rules, §Red flags -->`
  - rubrics/librarian.md → `<!-- Source: plugins/graph-wiki/agents/librarian.md §Rules, §Red flags -->`
  - rubrics/linter.md → `<!-- Source: plugins/graph-wiki/agents/linter.md §Rules, §Red flags -->`
  - rubrics/synthesizer.md → `<!-- Source: agents/graph-wiki-agent/src/graph_wiki_agent/prompts/sources/synthesizer.md §Rules, §Red flags -->`
  - rubrics/code_reader.md → `<!-- Source: agents/graph-wiki-agent/src/graph_wiki_agent/prompts/sources/code_reader.md §Rules, §Red flags -->`

The new single-line HTML comment replaces the existing 3 lines (drop both `<!-- Anchor: ... -->` and `<!-- Source-commit: ... -->`).

Then for each file, body-rebrand: replace any `lattice-wiki` literal in the prose with `graph-wiki`. Verify per-file via `grep -in lattice <file>` BEFORE editing — only edit if hits exist. PATTERNS explicitly notes `rubrics/scanner.md` L9 reads `from the canonical lattice-wiki scanner spec` — change to `from the canonical graph-wiki scanner spec`. Similar likely exists in the other 5 files.

Do NOT modify any other content in the rubric files (the judge-LLM prompts and rubric text are not in scope for re-shape). Do NOT add or remove rubric files.
  </action>
  <verify>
    <automated>grep -rln 'packages/prompt-sources' packages/eval-harness/src/eval_harness/divergence/rubrics/ | wc -l | awk '$1 == 0 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rln 'packages/prompt-sources' packages/eval-harness/src/eval_harness/divergence/rubrics/` returns 0 lines.
    - `grep -rln 'Source-commit' packages/eval-harness/src/eval_harness/divergence/rubrics/` returns 0 lines (Source-commit lines fully dropped).
    - `grep -rln '<!-- Anchor:' packages/eval-harness/src/eval_harness/divergence/rubrics/` returns 0 lines (Anchor lines fully dropped — collapsed into Source line).
    - `grep -rcE '^<!-- Source: ' packages/eval-harness/src/eval_harness/divergence/rubrics/ | awk -F: '{s+=$2} END {if (s>=6) exit 0; else exit 1}'` succeeds (every rubric has its 1-line Source header).
    - `grep -rin 'lattice-wiki' packages/eval-harness/src/eval_harness/divergence/rubrics/ | wc -l | awk '$1 == 0 {exit 0} {exit 1}'` succeeds (body rebrand applied to every file that had a hit).
    - `grep -E 'agents/graph-wiki-agent/src/graph_wiki_agent/prompts/sources/synthesizer\.md' packages/eval-harness/src/eval_harness/divergence/rubrics/synthesizer.md` returns 1 line.
    - `grep -E 'agents/graph-wiki-agent/src/graph_wiki_agent/prompts/sources/code_reader\.md' packages/eval-harness/src/eval_harness/divergence/rubrics/code_reader.md` returns 1 line.
    - `uv run --package eval-harness pytest -q --no-header 2>&1 | tail -5` shows a green summary (rubric content is judge-LLM context, not asserted by pytest).
  </acceptance_criteria>
  <done>
    All 6 rubric files carry the new 1-line `<!-- Source: -->` header with no `<!-- Anchor: -->` and no `<!-- Source-commit: -->`. Body rebrand applied where needed.
  </done>
</task>

</tasks>

<verification>
After this plan: zero residual `packages/prompt-sources` references in any code, prompt, divergence, rubric, or fragment file (the tree itself still exists for Plan 04 to delete). `uv run --package graph-wiki-agent pytest` green except possibly `test_provenance.py` (Plan 03 fixes). `uv run --package eval-harness pytest` green.

Global sanity:
- `grep -rln 'packages/prompt-sources' agents/ packages/eval-harness/ packages/workspace-io/ | wc -l` → 0
- The `packages/prompt-sources/` directory itself still exists with original content (deletion is Plan 04).
</verification>

<success_criteria>
- All acceptance grep gates in all 3 tasks pass.
- `CLAUDE.md.template` has `## Log format` and `## Style` headings.
- `prompts/sources/code_reader.md` and `prompts/sources/synthesizer.md` exist with rebrand applied.
- All non-`test_provenance.py` tests green.
</success_criteria>

<output>
Create `.planning/phases/26-plugin-prompt-source-mirror-sync/26-02-SUMMARY.md` when done.
</output>
