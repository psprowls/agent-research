---
phase: 26-plugin-prompt-source-mirror-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/26-plugin-prompt-source-mirror-sync/26-AUDIT.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "D-04: An audit table exists at .planning/phases/26-plugin-prompt-source-mirror-sync/26-AUDIT.md with three columns per row: `current anchor`, `target file state` (heading present / heading absent / file absent / drift detected), `proposed resolution` (re-point to <slug> / restore content to <target> / drop the check)."
    - "D-04: Every current `source_anchor=` literal in packages/eval-harness/src/eval_harness/divergence/*.py is rowed in the table (~23 literals across 6 files per PATTERNS § Eval-harness divergence-rules)."
    - "D-04: Every current `# Source:` comment in agents/graph-wiki-agent/src/graph_wiki_agent/prompts/*.py and prompts/_fragments/*.py is rowed."
    - "D-04: Every current `<!-- Source: -->` line in packages/eval-harness/src/eval_harness/divergence/rubrics/*.md (6 files) is rowed."
    - "D-04: Every prose `Anchors packages/prompt-sources/...` line in divergence/synthesizer.py (L20,36,49,66,89) and divergence/code_reader.py (L15,44,61,76,91) is rowed (10 sites)."
    - "D-04: The check.py docstring example at L59-60 is rowed."
    - "D-07: The two `CLAUDE.md.template` audit rows (`log_format.py` § Log format; `style_rules.py` § Style) explicitly carry the `restore content to packages/workspace-io/src/workspace_io/assets/CLAUDE.md.template` resolution — this is the load-bearing call PATTERNS § Critical surfaces. Alternative resolutions (re-point to SKILL.md, drop the anchor) are explicitly rejected in the row's notes."
    - "D-03: For every `re-point` resolution, the row records the GitHub-slug of the proposed new section (lowercase, hyphenated, punctuation stripped) and confirms that slug resolves against `grep -nE '^#+ ' <target-file>`."
    - "D-13 step 1: The audit is produced before any re-anchor work; the table is the planning input for Plan 02."
    - "Issue surfaced in PATTERNS § Bedrock-only prompt constants: the audit rows for the NEW provenance comments to be added to prompts/code_reader.py and prompts/synthesizer.py are present (they currently have no `# Source:` line; adding one is per D-06)."
    - "PATTERNS-surfaced issue 1 (CLAUDE.md.template heading mismatch with D-07) — explicitly resolved in the audit with rows for `§ Log format` and `§ Style` decisioned as `restore content`."
    - "PATTERNS-surfaced issue 2 (Option A vs Option B 1-line provenance collapse) — the audit's header reaffirms Option A as the chosen shape (`# Source: <path> §<section>`)."
    - "PATTERNS-surfaced issue 3 (em-dash slug behavior — `linter.md` `### Pass 2 — Semantic (read and think)` slugs to `pass-2--semantic-read-and-think`) — explicitly tested in audit notes with the slug string recorded."
  artifacts:
    - path: ".planning/phases/26-plugin-prompt-source-mirror-sync/26-AUDIT.md"
      provides: "Complete D-04 unresolvable-anchor table seeded from PATTERNS.md § Anchor-slug audit + § Critical surfaces, plus prose/docstring/rubric/fragment site rows."
      contains: "current anchor"
  key_links:
    - from: ".planning/phases/26-plugin-prompt-source-mirror-sync/26-AUDIT.md"
      to: ".planning/phases/26-plugin-prompt-source-mirror-sync/26-02-PLAN.md"
      via: "Plan 02 reads the audit and applies the resolution column"
      pattern: "26-AUDIT.md"
---

<objective>
Produce the D-04 audit table at `.planning/phases/26-plugin-prompt-source-mirror-sync/26-AUDIT.md`. This table catalogs every existing anchor pointer in the repository (every `source_anchor=` literal, every `# Source:` comment, every `<!-- Source: -->` HTML header, every prose `Anchors ...` reference, plus the two new `# Source:` comments to be added to Bedrock-only prompt constants), records the proposed new target slug under the canonical surfaces (per D-03 / D-05 / D-06 / D-07), and decides each row's resolution — `re-point` / `restore content` / `drop the check` — per D-04.

Purpose: The audit is the planning input for Plan 02 (re-anchor). It surfaces the load-bearing decision around `CLAUDE.md.template`'s missing `## Log format` / `## Style` headings (PATTERNS § Critical surfaces) and confirms the Option A (1-line collapse) provenance shape uniformly. Without the audit, Plan 02 cannot apply resolutions deterministically.

Output: One new file `26-AUDIT.md` (~80-120 rows). No code edits in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-plugin-prompt-source-mirror-sync/26-CONTEXT.md
@.planning/phases/26-plugin-prompt-source-mirror-sync/26-PATTERNS.md

<interfaces>
Seed data — copy verbatim from PATTERNS.md (do not re-derive). Resolutions:

From PATTERNS.md § Pattern Assignments → Fragment files (8 rows):
- `_fragments/architecture_overview.py` → `plugins/graph-wiki/skills/graph-wiki/SKILL.md §Architecture` (re-point)
- `_fragments/iron_rules.py` → `plugins/graph-wiki/skills/graph-wiki/SKILL.md §Iron rules` (re-point — slug `#iron-rules` verified ✓)
- `_fragments/page_categories.py` → `plugins/graph-wiki/skills/graph-wiki/SKILL.md §Page categories` (re-point — slug `#page-categories` verified ✓)
- `_fragments/claude_md_disambiguation.py` → `plugins/graph-wiki/skills/graph-wiki/SKILL.md §Cross-tool compatibility` (re-point)
- `_fragments/citation_rules.py` → `plugins/graph-wiki/agents/librarian.md §Rules` (re-point)
- `_fragments/frontmatter_rules.py` → `plugins/graph-wiki/agents/ingestor.md §4. Write the source summary` (re-point, slug `#4-write-the-source-summary` verified ✓)
- `_fragments/log_format.py` → **restore content** to `packages/workspace-io/src/workspace_io/assets/CLAUDE.md.template § Log format` (heading currently ABSENT; D-07 mandates the asset IS canonical, so the asset gets the heading + content)
- `_fragments/style_rules.py` → **restore content** to `packages/workspace-io/src/workspace_io/assets/CLAUDE.md.template § Style` (heading currently ABSENT; same rationale)

From PATTERNS.md § Prompt-builder files (6 rows):
- `prompts/scanner.py` L15-17 → `plugins/graph-wiki/agents/scanner.md §Role, §Rules, §Red flags` (re-point — collapse 3 lines to 1)
- `prompts/linter.py` L17 docstring → rebrand path prefix only (docstring not provenance-comment)
- `prompts/linter.py` L26 → `plugins/graph-wiki/agents/linter.md §Rules` (re-point — drop bullet pin per D-05)
- `prompts/linter.py` L55 → `plugins/graph-wiki/agents/linter.md §Pass 2 — Semantic (read and think), §Rules` (re-point — em-dash in section name; slugs to `pass-2--semantic-read-and-think` per PATTERNS § Critical surfaces)
- `prompts/ingestor.py` L5 docstring → rebrand path prefix
- `prompts/librarian.py` L7 docstring → rebrand path prefix

From PATTERNS.md § Bedrock-only prompt constants (2 new comments):
- `prompts/code_reader.py` (add NEW comment) → `agents/graph-wiki-agent/src/graph_wiki_agent/prompts/sources/code_reader.md §Rules, §Outputs, §Red flags` (re-point — new agent-local source per D-06)
- `prompts/synthesizer.py` (add NEW comment) → `agents/graph-wiki-agent/src/graph_wiki_agent/prompts/sources/synthesizer.md §Rules` (re-point — same)

From PATTERNS.md § Eval-harness divergence-rules § source_anchor= (~23 literals):
- divergence/scanner.py (4) → `plugins/graph-wiki/agents/scanner.md#<slug>` (audit case: `#workflow-step-3` → `#3-create-stubs-for-new-packages` per PATTERNS)
- divergence/ingestor.py (4) → 3 to `plugins/graph-wiki/agents/ingestor.md`, 1 to `plugins/graph-wiki/skills/graph-wiki/SKILL.md`
- divergence/librarian.py (4) → 1 SKILL.md, 3 librarian.md
- divergence/linter.py (3) → `plugins/graph-wiki/agents/linter.md` (audit case: `#workflow-pass-3` → `#pass-3--report`)
- divergence/synthesizer.py (4) → `agents/graph-wiki-agent/src/graph_wiki_agent/prompts/sources/synthesizer.md`
- divergence/code_reader.py (4) → `agents/graph-wiki-agent/src/graph_wiki_agent/prompts/sources/code_reader.md`

From PATTERNS.md § Eval-harness divergence-rules § prose `Anchors ...` lines (10 sites):
- divergence/synthesizer.py L20, L36, L49, L66, L89 → path-prefix rewrite to `agents/.../prompts/sources/synthesizer.md`
- divergence/code_reader.py L15, L44, L61, L76, L91 → path-prefix rewrite to `agents/.../prompts/sources/code_reader.md`

From PATTERNS.md § check.py docstring example (1 site):
- check.py L59-60 → `plugins/graph-wiki/skills/graph-wiki/SKILL.md#iron-rules` (example only; mechanical rewrite)

From PATTERNS.md § Rubric files § HTML-comment header (6 rows):
- rubrics/{ingestor,librarian,linter,scanner}.md → `plugins/graph-wiki/agents/<role>.md §Rules, §Red flags`
- rubrics/{synthesizer,code_reader}.md → `agents/.../prompts/sources/<role>.md §Rules, §Red flags`
- ALL 6: also drop the `<!-- Source-commit: -->` line and rebrand any `lattice-wiki` → `graph-wiki` in body content
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer commit → planning artifacts | Untrusted (in the sense of regression-prone) developer-introduced edits cross into the planning artifact tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Tampering | `26-AUDIT.md` row omission | mitigate | acceptance grep gates (see `<acceptance_criteria>`) assert row counts match PATTERNS.md inventory |
| T-26-02 | Repudiation | Audit decisions made without rationale | mitigate | every row has a `notes` column; load-bearing rows (CLAUDE.md.template restoration) carry an explicit one-line rationale |

Severity: low. No external attacker surface; threat is developer regression caught by test gates downstream.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Produce the D-04 anchor audit table</name>
  <files>.planning/phases/26-plugin-prompt-source-mirror-sync/26-AUDIT.md</files>
  <read_first>
    - .planning/phases/26-plugin-prompt-source-mirror-sync/26-CONTEXT.md (D-03, D-04, D-05, D-06, D-07 — anchor rules)
    - .planning/phases/26-plugin-prompt-source-mirror-sync/26-PATTERNS.md (seed tables — every section under "File Classification" and "Pattern Assignments")
    - packages/workspace-io/src/workspace_io/assets/CLAUDE.md.template (verify current headings: `# Graph-Wiki Workspace`, `## Layout`, `## Plugins installed`, `## Conventions for LLM agents` — confirm `## Log format` and `## Style` are ABSENT)
    - plugins/graph-wiki/agents/scanner.md (heading inventory — confirm `## Role` L13, `## Rules` L100, `## Red flags` L108 per PATTERNS; also list ### subheadings under `## Workflow` for the `#workflow-step-3` resolution row)
    - plugins/graph-wiki/agents/linter.md (heading inventory — confirm `### Pass 2 — Semantic (read and think)` L46 and `### Pass 3 — Report` L58 — em-dash slug verification)
    - plugins/graph-wiki/agents/ingestor.md (heading inventory — confirm `### 4. Write the source summary` L49)
    - plugins/graph-wiki/agents/librarian.md (heading inventory — confirm `## Rules` L72)
    - plugins/graph-wiki/skills/graph-wiki/SKILL.md (heading inventory — confirm `## Iron rules` L191, `## Page categories` L140, `## Architecture`, `## Cross-tool compatibility`)
    - packages/eval-harness/src/eval_harness/divergence/scanner.py (every `source_anchor=` literal — should be 4 per PATTERNS)
    - packages/eval-harness/src/eval_harness/divergence/ingestor.py (4 literals)
    - packages/eval-harness/src/eval_harness/divergence/librarian.py (4 literals)
    - packages/eval-harness/src/eval_harness/divergence/linter.py (3 literals)
    - packages/eval-harness/src/eval_harness/divergence/synthesizer.py (4 literals + 5 prose Anchors lines L20,36,49,66,89)
    - packages/eval-harness/src/eval_harness/divergence/code_reader.py (4 literals + 5 prose Anchors lines L15,44,61,76,91)
    - packages/eval-harness/src/eval_harness/divergence/check.py (L59-60 docstring example)
    - packages/eval-harness/src/eval_harness/divergence/rubrics/scanner.md (3-line HTML header L1-3; body for `lattice-wiki` rebrand check)
    - packages/eval-harness/src/eval_harness/divergence/rubrics/{ingestor,librarian,linter,synthesizer,code_reader}.md (same shape)
    - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/iron_rules.py (current 3-line header L1-3)
    - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/_fragments/{architecture_overview,citation_rules,claude_md_disambiguation,frontmatter_rules,log_format,page_categories,style_rules}.py (current headers)
    - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/scanner.py L15-17 (3-line header)
    - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/linter.py L17, L26, L55 (3 sites)
    - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/ingestor.py L5 (docstring)
    - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/librarian.py L7 (docstring)
    - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/code_reader.py L1-3 (confirm no current `# Source:` comment — D-06 adds one)
    - agents/graph-wiki-agent/src/graph_wiki_agent/prompts/synthesizer.py L1-3 (same)
    - packages/prompt-sources/agents/code_reader.md (will become `prompts/sources/code_reader.md` — verify current `lattice` strings via `grep -ni lattice` so the rebrand sweep audit row records them)
    - packages/prompt-sources/agents/synthesizer.md (same)
  </read_first>
  <action>
Create `.planning/phases/26-plugin-prompt-source-mirror-sync/26-AUDIT.md`. Use the structure below — copy seed rows verbatim from PATTERNS.md and add the verification artifacts (current heading state per target file). Re-grep each target file's headings once before recording (do not trust PATTERNS line numbers blindly — files may have drifted).

Document the file with a preamble declaring (a) Option A is the chosen provenance-comment shape (per PATTERNS § Critical surfaces — `# Source: <path> §<section>`); (b) the audit is the input to Plan 02; (c) every row resolves to exactly one of `re-point | restore content | drop the check`.

Then six tables — one per audited surface:

1. **Fragment file `# Source:` headers** (8 rows: `_fragments/{architecture_overview,citation_rules,claude_md_disambiguation,frontmatter_rules,iron_rules,log_format,page_categories,style_rules}.py`). Columns: `file | current 3-line block | proposed new 1-line shape | target heading present? | resolution | notes`. The two CLAUDE.md.template rows MUST carry `restore content` (per D-07; PATTERNS § Critical surfaces makes this the load-bearing decision — record exactly what new heading + content goes into the template).

2. **Prompt-builder `# Source:` comments + docstring rebrands** (6 rows: scanner.py L15-17, linter.py L17/L26/L55, ingestor.py L5, librarian.py L7). Same column shape. For linter.py L55 the proposed new shape uses the em-dash literal (`§Pass 2 — Semantic (read and think)`); record the GitHub-slug as `pass-2--semantic-read-and-think` (double-hyphen) explicitly in the `notes` column to validate D-08 step 2 in Plan 03.

3. **Bedrock-only constants — new `# Source:` comments to ADD** (2 rows: code_reader.py, synthesizer.py). Columns: `file | new comment | target file (NEW agent-local source) | rebrand needed in ported md? | resolution`. Both resolutions: `re-point` (with `restore content` semantic in the sense that the .md source file is created in Plan 02).

4. **Eval-harness `source_anchor=` literals** (~23 rows across 6 files: scanner.py x4, ingestor.py x4, librarian.py x4, linter.py x3, synthesizer.py x4, code_reader.py x4). Columns: `file:line | current anchor | new anchor | target slug verified? | resolution | notes`. Include the two named audit cases from PATTERNS: scanner.md `#workflow-step-3` → `#3-create-stubs-for-new-packages`; linter.md `#workflow-pass-3` → `#pass-3--report`.

5. **Prose `Anchors ...` lines + check.py docstring** (11 rows: synthesizer.py L20/36/49/66/89, code_reader.py L15/44/61/76/91, check.py L59-60). Columns: `file:line | current text | new text | resolution`. All `re-point`.

6. **Rubric HTML headers** (6 rows: rubrics/{ingestor,librarian,linter,scanner,synthesizer,code_reader}.md L1-3 each). Columns: `file | current 3-line block | proposed new 1-line shape | drop Source-commit? | lattice→graph-wiki rebrand needed in body? | resolution`. All resolutions: `re-point`; all `drop Source-commit: yes`; per-file body-rebrand verified via `grep -ni lattice`.

After the six tables, add a "Summary" section with totals: total rows, `re-point` count, `restore content` count, `drop the check` count. The `restore content` count must be exactly 2 (the two CLAUDE.md.template rows); any other `restore content` row requires a one-line rationale.

Do NOT modify any code, prompt, divergence, rubric, or template file in this task — Plan 02 applies resolutions. This task produces only `26-AUDIT.md`.
  </action>
  <verify>
    <automated>test -f .planning/phases/26-plugin-prompt-source-mirror-sync/26-AUDIT.md &amp;&amp; grep -c '^|' .planning/phases/26-plugin-prompt-source-mirror-sync/26-AUDIT.md | awk '$1 &gt;= 60 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/26-plugin-prompt-source-mirror-sync/26-AUDIT.md` succeeds.
    - `grep -E "Option A" .planning/phases/26-plugin-prompt-source-mirror-sync/26-AUDIT.md` returns at least one line (provenance shape declared).
    - `grep -c "restore content" .planning/phases/26-plugin-prompt-source-mirror-sync/26-AUDIT.md` returns exactly 2 (the two CLAUDE.md.template rows: § Log format and § Style — per D-07).
    - `grep -cE "_fragments/(architecture_overview|citation_rules|claude_md_disambiguation|frontmatter_rules|iron_rules|log_format|page_categories|style_rules)\.py" .planning/phases/26-plugin-prompt-source-mirror-sync/26-AUDIT.md | awk '$1 &gt;= 8 {exit 0} {exit 1}'` succeeds (all 8 fragments rowed).
    - `grep -c "source_anchor" .planning/phases/26-plugin-prompt-source-mirror-sync/26-AUDIT.md | awk '$1 &gt;= 20 {exit 0} {exit 1}'` succeeds (all ~23 source_anchor= literals rowed; allow tolerance for grep counting heading lines).
    - `grep -c "rubrics/" .planning/phases/26-plugin-prompt-source-mirror-sync/26-AUDIT.md | awk '$1 &gt;= 6 {exit 0} {exit 1}'` succeeds (all 6 rubric files rowed).
    - `grep -E "pass-2--semantic" .planning/phases/26-plugin-prompt-source-mirror-sync/26-AUDIT.md` returns at least one line (em-dash slug behavior recorded — PATTERNS-surfaced issue 3).
    - `grep -E "#3-create-stubs-for-new-packages" .planning/phases/26-plugin-prompt-source-mirror-sync/26-AUDIT.md` returns at least one line (scanner.md `#workflow-step-3` audit case recorded).
    - `grep -E "#pass-3--report" .planning/phases/26-plugin-prompt-source-mirror-sync/26-AUDIT.md` returns at least one line (linter.md `#workflow-pass-3` audit case recorded).
    - `grep -cE "code_reader\.py|synthesizer\.py" .planning/phases/26-plugin-prompt-source-mirror-sync/26-AUDIT.md | awk '$1 &gt;= 2 {exit 0} {exit 1}'` succeeds (both Bedrock-only constants rowed).
    - No files outside `.planning/phases/26-plugin-prompt-source-mirror-sync/` are modified by this task: `git status --porcelain | grep -vE '^[ ?][ ?]\.planning/phases/26-plugin-prompt-source-mirror-sync/' | wc -l` returns 0.
  </acceptance_criteria>
  <done>
    `26-AUDIT.md` exists with all six tables filled. Exactly 2 rows are `restore content` (the CLAUDE.md.template rows). Every other row is `re-point` with a verified GitHub-slug. Plan 02 can be executed mechanically against this artifact.
  </done>
</task>

</tasks>

<verification>
After completion of this plan, the audit table is the canonical input to Plan 02. No code is changed; the test suite and brand-gate are unaffected by this plan.
</verification>

<success_criteria>
- `26-AUDIT.md` exists at the phase path.
- All acceptance grep gates pass.
- The two CLAUDE.md.template `restore content` rows are explicit and decisioned (D-07).
- No files outside `.planning/phases/26-plugin-prompt-source-mirror-sync/` are touched.
</success_criteria>

<output>
Create `.planning/phases/26-plugin-prompt-source-mirror-sync/26-01-SUMMARY.md` when done — summarize totals, the two `restore content` rows, and any per-row notes that need follow-up in Plan 02.
</output>
