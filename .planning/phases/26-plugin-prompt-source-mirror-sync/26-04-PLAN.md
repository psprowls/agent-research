---
phase: 26-plugin-prompt-source-mirror-sync
plan: 04
type: execute
wave: 4
depends_on:
  - 26-03
files_modified:
  - packages/prompt-sources
  - pyproject.toml
  - scripts/check-brand.sh
  - .brand-grep-allow
  - .planning/todos/resolved/2026-05-21-phase-26-prompt-sources-deletion.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "D-01: The `packages/prompt-sources/` directory is removed via `git rm -r` in this plan's commit. After this plan, `test -d packages/prompt-sources` fails."
    - "D-02: The line `exclude = [\"packages/prompt-sources\"]` is removed from root `pyproject.toml` in the same commit as the deletion. After this plan, `grep -n 'packages/prompt-sources' pyproject.toml` returns no lines."
    - "D-10: A new CHECK 6 block is added to `scripts/check-brand.sh` mirroring the CHECK 5 structural template (header comment + grep invocation + exit-on-match). CHECK 6 is numbered exactly 6 (the next sequential number after the current top of 5 — verified at plan-write time)."
    - "D-11: CHECK 6 blocks the literal string `packages/prompt-sources` under path scope `agents/`, `packages/`, `plugins/`, `scripts/`, `tests/`. `.planning/` is OUT of scope (historical record per CONTEXT)."
    - "D-11: `-F` (fixed-string) is used in the CHECK 6 grep invocation because the pattern has no regex metacharacters and `-F` is faster."
    - "D-11: `.brand-grep-allow` gets at most a small number of new entries. Phase 21's R-04 self-allowlist block already covers `scripts/check-brand.sh` and `.brand-grep-allow` themselves (no new entry needed for those). One new entry IS expected: `agents/graph-wiki-agent/tests/prompts/test_provenance.py` (the test contains the literal `packages/prompt-sources/foo.md` inside the negative `test_disallowed_prefix_rejected` assertion, added in Plan 03). Add a Phase 26 §D-11 rationale block."
    - "D-12: No coverage added for the Python module name `prompt_sources` — the dir was excluded from the uv workspace and no `prompt_sources` import path ever existed. Path-level block in D-11 catches accidental future reintroduction by file location."
    - "PKGCLS pattern parity: a resolved-todo file documents the phase outcome in `.planning/todos/resolved/` (analog to Phase 25's todo move pattern), giving the audit trail."
    - "After this plan: `uv sync` succeeds (workspace remains coherent after the exclude line is dropped); `uv run --package graph-wiki-agent pytest` and `uv run --package eval-harness pytest` both green; `bash scripts/check-brand.sh` exits 0 against the post-deletion tree."
    - "D-13 step 4: deletion + brand-gate is the final ordered milestone — gated by green Plan 03 tests."
  artifacts:
    - path: "scripts/check-brand.sh"
      provides: "CHECK 6 block added, mirroring CHECK 5 structural template (D-10/D-11)."
      contains: "CHECK 6"
    - path: ".brand-grep-allow"
      provides: "Phase 26 §D-11 allowlist entry for test_provenance.py negative-case literal."
      contains: "Phase 26 §D-11"
    - path: "pyproject.toml"
      provides: "`exclude = [\"packages/prompt-sources\"]` line removed (D-02)."
      contains: "[tool.uv.workspace]"
    - path: ".planning/todos/resolved/2026-05-21-phase-26-prompt-sources-deletion.md"
      provides: "Resolved-todo audit trail for the deletion."
      contains: "packages/prompt-sources"
  key_links:
    - from: "scripts/check-brand.sh"
      to: "agents/, packages/, plugins/, scripts/, tests/"
      via: "grep -rEln -F 'packages/prompt-sources'"
      pattern: "CHECK 6"
    - from: "pyproject.toml"
      to: "uv workspace"
      via: "[tool.uv.workspace] exclude list"
      pattern: "tool.uv.workspace"
---

<objective>
Finalize Phase 26 with the hard-cut deletion + brand-gate: `git rm -r packages/prompt-sources/`, remove the `exclude = ["packages/prompt-sources"]` line from root `pyproject.toml`, add a new `CHECK 6` block to `scripts/check-brand.sh` modeled on CHECK 5, seed any required `.brand-grep-allow` entries, and run the full test suite + brand-gate against the resulting tree. Create a resolved-todo audit-trail file mirroring the Phase 25 pattern.

Purpose: This is the moment the mirror invariant from Phase 23 D-08 (`packages/prompt-sources/...` mirrors `plugins/graph-wiki/...`) becomes moot — the second tree disappears. From here, every anchor points at exactly one canonical surface, and the brand-gate prevents future reintroduction.

Output: 1 directory deletion (~17 files removed), 1-line `pyproject.toml` edit, ~20-25 new lines in `check-brand.sh`, 1-3 lines in `.brand-grep-allow`, 1 new resolved-todo file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-plugin-prompt-source-mirror-sync/26-CONTEXT.md
@.planning/phases/26-plugin-prompt-source-mirror-sync/26-PATTERNS.md

<interfaces>
CHECK 6 block — structural template from `scripts/check-brand.sh` L100-120 (CHECK 5). Identical shape, with three differences:
  1. Pattern is a literal (`packages/prompt-sources`), so use `-F` instead of `-E`.
  2. Path scope is `agents/ packages/ plugins/ scripts/ tests/` (NOT `.planning/`, per D-11).
  3. Error tag is `BRAND-PROMPT-SOURCES`.

Block to insert (after CHECK 5, before the final `echo "BRAND-04 OK..."` line):

```
# CHECK 6 — Phase 26 §D-10 / D-11: ban reintroduction of `packages/prompt-sources`
# as a path literal anywhere under agents/, packages/, plugins/, scripts/, tests/.
# Path scope EXCLUDES .planning/ — archived milestones, retrospectives, and phase
# histories legitimately reference the deleted tree as historical record.
# Allowlist applies via .brand-grep-allow for any in-scope self-references.
HITS6=$(grep -rln --exclude-dir=__pycache__ --exclude='*.pyc' -F \
    'packages/prompt-sources' \
    agents/ packages/ plugins/ scripts/ tests/ 2>/dev/null \
    | grep -vF -f <(grep -vE '^[[:space:]]*(#|$)' "$ALLOWLIST") || true)

if [ -n "$HITS6" ]; then
  echo "$HITS6"
  COUNT6=$(printf '%s\n' "$HITS6" | wc -l | tr -d ' ')
  echo "BRAND-PROMPT-SOURCES FAIL: ${COUNT6} unallowlisted hits for packages/prompt-sources" >&2
  exit 1
fi
```

Update the final summary `echo` line to mention BRAND-PROMPT-SOURCES (per PATTERNS § Brand-gate addition):
```
echo "BRAND-04 OK: zero unallowlisted hits (BRAND-04 lattice + BRAND-CMD ... + BRAND-WSEVAL ... + BRAND-PROMPT-SOURCES packages/prompt-sources all clean)"
```
(read the current summary line and extend it; don't replace tokens that have specific phase tags)

`.brand-grep-allow` new entry expected (per Plan 03 negative test):
```
# ---------------------------------------------------------------------------
# Phase 26 §D-11: test_provenance.py contains the literal `packages/prompt-sources/foo.md`
# inside its `test_disallowed_prefix_rejected` assertion. The literal is required
# to prove the whitelist gate rejects the deleted-tree prefix.
# ---------------------------------------------------------------------------
agents/graph-wiki-agent/tests/prompts/test_provenance.py
```

Seeding procedure (per PATTERNS § Allowlist seeding):
  1. Apply the deletion + pyproject edit + CHECK 6 block first.
  2. Run `bash scripts/check-brand.sh`.
  3. Triage each hit. The expected hit is `test_provenance.py` (per Plan 03's negative test). Add the allowlist entry above.
  4. Re-run; should exit 0.

If additional hits surface, triage each: rewrite at source if possible (e.g., comment in a doc that no longer needs the reference); allowlist only if the literal is genuinely load-bearing.

`pyproject.toml` edit: at line 3 (verified at plan-write time), the file currently reads:
```
exclude = ["packages/prompt-sources"]
```
This line is removed entirely (the surrounding `[tool.uv.workspace]` table stays — only the exclude line goes). After removal, `uv sync` must succeed without error.

Resolved-todo file content (mirrors Phase 25's pattern):
```markdown
---
created: 2026-05-21
resolved: <YYYY-MM-DD when executed>
phase: 26
status: resolved
---

# Phase 26 — packages/prompt-sources/ deletion

The `packages/prompt-sources/` upstream-snapshot tree is removed. All anchor pointers
(provenance comments, source_anchor literals, rubric headers, prose Anchors lines)
re-pointed to canonical surfaces:
- `plugins/graph-wiki/` (most pointers)
- `packages/workspace-io/src/workspace_io/assets/CLAUDE.md.template` (log_format, style_rules)
- `agents/graph-wiki-agent/src/graph_wiki_agent/prompts/sources/` (code_reader, synthesizer)

Brand-gate CHECK 6 blocks reintroduction of the literal `packages/prompt-sources`
under agents/, packages/, plugins/, scripts/, tests/.

Resolved by Phase 26 (PLANs 01-04). See `.planning/phases/26-plugin-prompt-source-mirror-sync/`.
```
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository tree → uv workspace coherence | Removing the exclude line could surface a build error if the tree isn't actually gone, or vice versa |
| Brand-gate → future developer commits | CHECK 6 enforces the deletion long-term against accidental reintroduction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-08 | Tampering | Future re-introduction of `packages/prompt-sources/` | mitigate | CHECK 6 fixed-string grep against the path scope; allowlist requires explicit rationale comment |
| T-26-09 | Tampering | uv-workspace resolution failure after exclude removal | mitigate | acceptance criterion runs `uv sync` after edit + deletion together |
| T-26-10 | Repudiation | Deletion without audit trail | mitigate | resolved-todo file in `.planning/todos/resolved/` documents the deletion with date + phase reference |

Severity: low. No external attacker; this is a refactor-finalization gate.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add CHECK 6 to check-brand.sh; seed .brand-grep-allow</name>
  <files>scripts/check-brand.sh, .brand-grep-allow</files>
  <read_first>
    - scripts/check-brand.sh (full file — confirm the highest existing CHECK number is 5 at L100; locate the final summary `echo` line; understand the `$ALLOWLIST` variable convention)
    - .brand-grep-allow (full file — understand the Phase 21 R-04 self-allowlist block at L62-72 and the rationale-comment pattern)
    - agents/graph-wiki-agent/tests/prompts/test_provenance.py (after Plan 03 — confirm the literal `packages/prompt-sources/foo.md` exists in the negative test)
    - .planning/phases/26-plugin-prompt-source-mirror-sync/26-PATTERNS.md § Brand-gate addition (full section)
  </read_first>
  <action>
Append a new CHECK 6 block to `scripts/check-brand.sh` after CHECK 5 (currently ending around L120) and before the final summary `echo` line. Use the block content from `<interfaces>` above verbatim — header comment, grep invocation with `-F` literal, exit-on-match logic, `BRAND-PROMPT-SOURCES` error tag.

Update the final summary `echo` line to append `+ BRAND-PROMPT-SOURCES packages/prompt-sources` to the parenthetical list (extend; do not overwrite the existing pattern tags).

Append the Phase 26 §D-11 allowlist block to `.brand-grep-allow` (use the block content from `<interfaces>`). Place it at the end of the file (after any existing Phase 21/23/24 blocks).

Re-grep the script and verify the CHECK 6 block compiles via shellcheck if available (`shellcheck scripts/check-brand.sh` — best-effort; not a hard gate, the bash logic mirrors a proven block).

DO NOT delete `packages/prompt-sources/` or edit `pyproject.toml` in this task — Task 2 handles those. The brand-gate must be in place before deletion so the post-deletion verification has something to assert against.

After this task, running `bash scripts/check-brand.sh` AGAINST THE CURRENT TREE (which still has `packages/prompt-sources/`) will fail with many CHECK 6 hits. That's expected — Task 2 fixes by deleting the tree.
  </action>
  <verify>
    <automated>grep -E '^# CHECK 6' scripts/check-brand.sh | wc -l | awk '$1 == 1 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E '^# CHECK 6' scripts/check-brand.sh` returns exactly 1 line.
    - `grep -E 'BRAND-PROMPT-SOURCES' scripts/check-brand.sh | wc -l | awk '$1 >= 2 {exit 0} {exit 1}'` succeeds (at least one inside the CHECK 6 fail message and one in the summary echo).
    - `grep -E "grep -rln.*-F[[:space:]]+'packages/prompt-sources'" scripts/check-brand.sh` returns at least 1 line (fixed-string grep is in place).
    - `grep -cE 'HITS6=' scripts/check-brand.sh` returns 1.
    - `grep -cE 'COUNT6=' scripts/check-brand.sh` returns 1.
    - `grep -E 'agents/ packages/ plugins/ scripts/ tests/' scripts/check-brand.sh` returns at least 1 line (path scope correct per D-11).
    - `grep -E 'Phase 26 §D-11' .brand-grep-allow | wc -l | awk '$1 >= 1 {exit 0} {exit 1}'` succeeds (allowlist block present).
    - `grep -E '^agents/graph-wiki-agent/tests/prompts/test_provenance\.py$' .brand-grep-allow` returns 1 line (allowlisted path is correctly listed).
    - `bash -n scripts/check-brand.sh` exits 0 (no syntax errors).
  </acceptance_criteria>
  <done>
    `check-brand.sh` has CHECK 6 inserted before the summary echo. Summary echo mentions BRAND-PROMPT-SOURCES. `.brand-grep-allow` has the Phase 26 §D-11 block allowlisting `test_provenance.py`. No syntax errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete packages/prompt-sources/, remove pyproject.toml exclude, verify the full gate</name>
  <files>packages/prompt-sources, pyproject.toml, .planning/todos/resolved/2026-05-21-phase-26-prompt-sources-deletion.md</files>
  <read_first>
    - pyproject.toml (full file — confirm line 3 is `exclude = ["packages/prompt-sources"]` inside `[tool.uv.workspace]`)
    - packages/prompt-sources/ (directory listing — confirm tree contents before deletion: agents/, references/, SKILL.md, SOURCE-COMMIT, wiki-claude-md-template.md per `ls packages/prompt-sources/`)
    - scripts/check-brand.sh (after Task 1 — verify CHECK 6 is in place)
    - .planning/todos/resolved/ (any existing resolved-todo file from Phase 25 for naming-convention reference)
  </read_first>
  <action>
Run `git rm -r packages/prompt-sources/` to remove the directory and stage the deletion. Verify post-deletion via `test ! -d packages/prompt-sources`.

Edit `pyproject.toml`: locate line 3 (the `exclude = ["packages/prompt-sources"]` line inside the `[tool.uv.workspace]` table) and delete it. Do not touch any other line. The `[tool.uv.workspace]` table stays; only the `exclude` line goes. (If `[tool.uv.workspace]` becomes empty after the edit, leave the empty table — uv treats empty workspace tables as valid; alternatively, if the table only contains the exclude line, remove the whole table for cleanliness. Reader's choice — verify `uv sync` after either approach.)

Create `.planning/todos/resolved/2026-05-21-phase-26-prompt-sources-deletion.md` with the content shown in `<interfaces>`. Adjust the `resolved:` field to today's actual date.

Run the verification suite in this order:
  1. `uv sync` — must succeed; the workspace tree is now coherent without the exclude line.
  2. `uv run --package graph-wiki-agent pytest` — must be fully green; the prompts/ tree no longer has any `packages/prompt-sources/` references (Plan 02) and test_provenance.py passes (Plan 03).
  3. `uv run --package eval-harness pytest` — must be fully green; the divergence tree is re-anchored (Plan 02).
  4. `bash scripts/check-brand.sh` — must exit 0; with the deleted tree, the only in-scope `packages/prompt-sources` literal remaining is in `test_provenance.py` and the allowlist covers it.

If any verification step fails, do NOT proceed to commit. Diagnose: most-likely failures and recovery:
  - `uv sync` fails: the workspace might have a hidden reference to `packages/prompt-sources`. Re-grep `pyproject.toml` and any `packages/*/pyproject.toml` for the literal.
  - `pytest` fails: an anchor in Plan 02 was missed. Re-grep `git grep -l 'packages/prompt-sources' agents/ packages/` (top-level), fix at source, re-run.
  - `check-brand.sh` fails with hits in unexpected files: triage each — rewrite at source if it's a stray reference, allowlist with `.brand-grep-allow` rationale block only if genuinely load-bearing.

After all four checks pass, the phase is done.
  </action>
  <verify>
    <automated>test ! -d packages/prompt-sources &amp;&amp; grep -cE 'packages/prompt-sources' pyproject.toml | awk '$1 == 0 {exit 0} {exit 1}' &amp;&amp; bash scripts/check-brand.sh &gt;/dev/null 2&gt;&amp;1</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -d packages/prompt-sources` succeeds (directory deleted).
    - `grep -n 'packages/prompt-sources' pyproject.toml` returns no lines (exclude line removed).
    - `grep -E '\[tool\.uv\.workspace\]' pyproject.toml` may or may not match depending on cleanup choice — both states valid as long as `uv sync` works.
    - `uv sync 2>&1 | tail -3` shows no error and the workspace resolves.
    - `uv run --package graph-wiki-agent pytest -q --no-header 2>&1 | tail -5` shows a green summary.
    - `uv run --package eval-harness pytest -q --no-header 2>&1 | tail -5` shows a green summary.
    - `bash scripts/check-brand.sh` exits 0 (full brand-gate passes against the post-deletion tree).
    - `test -f .planning/todos/resolved/2026-05-21-phase-26-prompt-sources-deletion.md` succeeds and contains the literal `Resolved by Phase 26`.
    - `git status --porcelain | grep -E '^D  packages/prompt-sources/' | wc -l | awk '$1 >= 1 {exit 0} {exit 1}'` succeeds (git tracks the deletion as staged removals).
    - `grep -rln 'packages/prompt-sources' agents/ packages/ plugins/ scripts/ 2>/dev/null | grep -vE '(check-brand\.sh|test_provenance\.py)$' | wc -l | awk '$1 == 0 {exit 0} {exit 1}'` succeeds (the only in-scope references remaining are check-brand.sh itself and the allowlisted test file).
  </acceptance_criteria>
  <done>
    Tree deleted. `pyproject.toml` exclude line removed. All four verification commands green. Resolved-todo file created. Phase 26 is fully complete.
  </done>
</task>

</tasks>

<verification>
Final phase verification (run end-to-end after both tasks complete):
- `test ! -d packages/prompt-sources`
- `grep -n 'packages/prompt-sources' pyproject.toml` → 0 lines
- `uv sync` → green
- `uv run --package graph-wiki-agent pytest` → green
- `uv run --package eval-harness pytest` → green
- `bash scripts/check-brand.sh` → exit 0
- `git status --porcelain | grep -E '^D  packages/prompt-sources/'` → many deletions
- Phase D-01..D-13 coverage cross-checked against `must_haves` in plans 01-04 (decision-coverage gate)
</verification>

<success_criteria>
- All Task 1 + Task 2 acceptance gates pass.
- `packages/prompt-sources/` no longer exists in the working tree.
- Brand-gate CHECK 6 is in place and passing.
- All v1.4 decisions D-01..D-13 satisfied (each cited by at least one plan's `must_haves.truths` across plans 01-04).
</success_criteria>

<output>
Create `.planning/phases/26-plugin-prompt-source-mirror-sync/26-04-SUMMARY.md` when done. Include:
- Final file/line counts for the deletion
- Confirmation of all four verification commands
- Any unexpected brand-gate hits and their disposition (rewrite vs allowlist)
- The full D-01..D-13 coverage table (cross-reference each decision to its plan(s))
</output>
