---
phase: 260526-d0z-move-graph-wiki-mcp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/graph-wiki-agent/src/graph_wiki_agent/mcp/__init__.py
  - agents/graph-wiki-agent/src/graph_wiki_agent/mcp/server.py
  - agents/graph-wiki-agent/src/graph_wiki_mcp/__init__.py
  - agents/graph-wiki-agent/src/graph_wiki_mcp/server.py
  - agents/graph-wiki-agent/pyproject.toml
  - agents/graph-wiki-agent/tests/unit/test_stdout_guard.py
  - agents/graph-wiki-agent/tests/unit/test_commands_log.py
  - agents/graph-wiki-agent/tests/unit/test_mcp_schema_forbid_extra.py
  - agents/graph-wiki-agent/tests/unit/test_wiki_scan_input.py
  - agents/graph-wiki-agent/tests/unit/test_mcp_query_schema.py
  - agents/graph-wiki-agent/tests/unit/test_mcp_new_tools.py
autonomous: true
requirements: [QUICK-D0Z-01]

must_haves:
  truths:
    - "graph-wiki-mcp console script invokes the MCP server from its new location under graph_wiki_agent.mcp"
    - "All unit tests under agents/graph-wiki-agent/tests/unit/ pass against the new module path"
    - "cg update --full no longer emits the implemented_by warning for graph-wiki-mcp"
    - "The built wheel for graph-wiki-agent includes the MCP server module (no orphaned sibling package)"
  artifacts:
    - path: "agents/graph-wiki-agent/src/graph_wiki_agent/mcp/__init__.py"
      provides: "MCP submodule init"
    - path: "agents/graph-wiki-agent/src/graph_wiki_agent/mcp/server.py"
      provides: "FastMCP server (relocated)"
      contains: "mcp = FastMCP(name=\"graph-wiki-mcp\")"
    - path: "agents/graph-wiki-agent/pyproject.toml"
      provides: "Updated entry point"
      contains: "graph_wiki_agent.mcp.server:main"
  key_links:
    - from: "agents/graph-wiki-agent/pyproject.toml"
      to: "graph_wiki_agent.mcp.server:main"
      via: "[project.scripts] entry point"
      pattern: "graph-wiki-mcp\\s*=\\s*\"graph_wiki_agent\\.mcp\\.server:main\""
    - from: "tests/unit/*.py"
      to: "graph_wiki_agent.mcp.*"
      via: "import statements"
      pattern: "from graph_wiki_agent\\.mcp"
---

<objective>
Move `graph_wiki_mcp` from a sibling package under `src/` into a proper submodule
of `graph_wiki_agent` (`graph_wiki_agent.mcp`), update the console-script entry
point, update all unit-test imports, and remove the old sibling directory.

Purpose: Resolve graph-io's `implemented_by` D-05 warning emitted by
`cg update --full` and fix a latent wheel-packaging bug where `uv_build`'s
default single-module discovery would omit `graph_wiki_mcp` from the built
wheel (the issue is masked by editable workspace installs today).

Output: A relocated MCP server, an updated entry point, updated test imports,
a clean `cg update --full` run, and passing unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@agents/graph-wiki-agent/pyproject.toml
@agents/graph-wiki-agent/src/graph_wiki_mcp/server.py

<interfaces>
The FastMCP server's *public name string* must remain `"graph-wiki-mcp"`
(line 81 of `server.py`):

```python
mcp = FastMCP(name="graph-wiki-mcp")
```

Only the Python module location changes. The console script name
(`graph-wiki-mcp`) and the FastMCP server's `name=` kwarg are unchanged.

The integration tests (`tests/integration/test_mcp_e2e.py`,
`test_mcp_stdio.py`) invoke the script via
`uv run --package graph-wiki-agent graph-wiki-mcp` — they reference the
console-script name, not the module path, so they require no edits.

The server's outbound imports already use the absolute path
`from graph_wiki_agent.commands.query import ...` and continue to work
unchanged after the move.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relocate graph_wiki_mcp into graph_wiki_agent.mcp and update entry point</name>
  <files>
    agents/graph-wiki-agent/src/graph_wiki_agent/mcp/__init__.py,
    agents/graph-wiki-agent/src/graph_wiki_agent/mcp/server.py,
    agents/graph-wiki-agent/src/graph_wiki_mcp/__init__.py,
    agents/graph-wiki-agent/src/graph_wiki_mcp/server.py,
    agents/graph-wiki-agent/pyproject.toml
  </files>
  <action>
    Move the MCP server into a submodule of graph_wiki_agent. Use `git mv` so
    history is preserved.

    1. Create the destination directory:
       `mkdir -p agents/graph-wiki-agent/src/graph_wiki_agent/mcp`
    2. `git mv agents/graph-wiki-agent/src/graph_wiki_mcp/__init__.py agents/graph-wiki-agent/src/graph_wiki_agent/mcp/__init__.py`
    3. `git mv agents/graph-wiki-agent/src/graph_wiki_mcp/server.py agents/graph-wiki-agent/src/graph_wiki_agent/mcp/server.py`
    4. Remove the now-empty old directory:
       `rmdir agents/graph-wiki-agent/src/graph_wiki_mcp`
    5. Edit `agents/graph-wiki-agent/pyproject.toml` line 20: change
       `graph-wiki-mcp   = "graph_wiki_mcp.server:main"` to
       `graph-wiki-mcp   = "graph_wiki_agent.mcp.server:main"` (preserve
       alignment whitespace).
    6. Do NOT modify `server.py` contents. Its outbound imports
       (`from graph_wiki_agent.commands.query import ...`) and the FastMCP
       `name="graph-wiki-mcp"` kwarg both remain correct as-is.

    Do not refactor adjacent code. Do not touch integration tests — they call
    the console script by name, not by import path.
  </action>
  <verify>
    <automated>test -f agents/graph-wiki-agent/src/graph_wiki_agent/mcp/server.py && test -f agents/graph-wiki-agent/src/graph_wiki_agent/mcp/__init__.py && ! test -d agents/graph-wiki-agent/src/graph_wiki_mcp && grep -q 'graph_wiki_agent\.mcp\.server:main' agents/graph-wiki-agent/pyproject.toml && ! grep -q 'graph_wiki_mcp\.server:main' agents/graph-wiki-agent/pyproject.toml</automated>
  </verify>
  <done>
    The `graph_wiki_mcp/` directory no longer exists; `graph_wiki_agent/mcp/`
    contains both files; pyproject.toml entry point references
    `graph_wiki_agent.mcp.server:main`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update unit-test imports from graph_wiki_mcp to graph_wiki_agent.mcp</name>
  <files>
    agents/graph-wiki-agent/tests/unit/test_stdout_guard.py,
    agents/graph-wiki-agent/tests/unit/test_commands_log.py,
    agents/graph-wiki-agent/tests/unit/test_mcp_schema_forbid_extra.py,
    agents/graph-wiki-agent/tests/unit/test_wiki_scan_input.py,
    agents/graph-wiki-agent/tests/unit/test_mcp_query_schema.py,
    agents/graph-wiki-agent/tests/unit/test_mcp_new_tools.py
  </files>
  <action>
    In each of the six unit-test files listed in `<files>`, replace every
    occurrence of the bare module name `graph_wiki_mcp` with
    `graph_wiki_agent.mcp`. This covers:

    - `import graph_wiki_mcp` → `import graph_wiki_agent.mcp`
    - `from graph_wiki_mcp import ...` → `from graph_wiki_agent.mcp import ...`
    - `from graph_wiki_mcp.server import ...` → `from graph_wiki_agent.mcp.server import ...`
    - String references in `monkeypatch.setattr("graph_wiki_mcp...", ...)`
      or `importlib.import_module("graph_wiki_mcp...")` patterns.

    Use the Edit tool per-file (Read first, then Edit each unique occurrence).
    Do NOT use sed/awk. Do NOT modify any non-test files. Do NOT touch
    integration tests under `tests/integration/`.

    Sanity check after edits: `grep -rn 'graph_wiki_mcp' agents/graph-wiki-agent/`
    must return zero results (the substring should appear nowhere in the
    repo after this task except possibly inside `.planning/` historical docs,
    which are out of scope per the orchestrator's blast-radius note).
  </action>
  <verify>
    <automated>! grep -rn 'graph_wiki_mcp' agents/graph-wiki-agent/src/ agents/graph-wiki-agent/tests/ agents/graph-wiki-agent/pyproject.toml</automated>
  </verify>
  <done>
    Zero references to the old module name `graph_wiki_mcp` remain in
    `agents/graph-wiki-agent/src/`, `agents/graph-wiki-agent/tests/`, or
    `agents/graph-wiki-agent/pyproject.toml`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Resync workspace, run unit tests, re-run cg update --full</name>
  <files>(no file edits — verification only)</files>
  <action>
    Re-sync the uv workspace so the relocated module is picked up by the
    editable install, then verify both the test suite and the graph-io
    warning are clean.

    1. Run `uv sync --package graph-wiki-agent` to refresh the editable
       install (the entry point changed, so the script shim needs to be
       regenerated).
    2. Run `uv run --package graph-wiki-agent pytest tests/unit/` — must
       pass with zero failures.
    3. Sanity-check the console script still resolves:
       `uv run --package graph-wiki-agent python -c "import graph_wiki_agent.mcp.server as s; print(s.main)"`
       — must print a function reference, not raise ImportError.
    4. Run `uv run cg update --full` and confirm the warning string
       `cannot resolve implemented_by for graph-wiki-agent entry 'graph-wiki-mcp'`
       does NOT appear in its output.

    If any step fails, surface the failure — do NOT silently retry.
  </action>
  <verify>
    <automated>uv sync --package graph-wiki-agent && uv run --package graph-wiki-agent pytest tests/unit/ -x && uv run --package graph-wiki-agent python -c "import graph_wiki_agent.mcp.server" && uv run cg update --full 2>&1 | { ! grep -q "cannot resolve implemented_by for graph-wiki-agent entry 'graph-wiki-mcp'"; }</automated>
  </verify>
  <done>
    `pytest tests/unit/` passes; `import graph_wiki_agent.mcp.server` succeeds;
    `cg update --full` no longer emits the implemented_by warning for
    `graph-wiki-mcp`.
  </done>
</task>

</tasks>

<verification>
- All three task `<verify>` blocks pass.
- `git status` shows: two files renamed (under git), one pyproject.toml
  modified, six test files modified, no stray files.
- Integration tests under `tests/integration/` remain untouched (orchestrator
  verified they call the console script by name, not by import path).
</verification>

<success_criteria>
- `agents/graph-wiki-agent/src/graph_wiki_mcp/` no longer exists.
- `agents/graph-wiki-agent/src/graph_wiki_agent/mcp/{__init__.py,server.py}` exist.
- `pyproject.toml` entry point reads `graph-wiki-mcp = "graph_wiki_agent.mcp.server:main"`.
- `uv run --package graph-wiki-agent pytest tests/unit/` passes.
- `uv run cg update --full` does NOT emit the implemented_by warning for `graph-wiki-mcp`.
- No string `graph_wiki_mcp` (with underscore) survives in `src/`, `tests/`, or `pyproject.toml`.
</success_criteria>

<output>
Create `.planning/quick/260526-d0z-move-graph-wiki-mcp-under-graph-wiki-age/260526-d0z-SUMMARY.md` when done.
</output>
