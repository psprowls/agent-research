---
phase: quick-260529-na9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/model-adapter/src/model_adapter/models.toml
  - packages/eval-harness/src/eval_harness/judge.py
  - packages/eval-harness/src/eval_harness/pricing.py
autonomous: true
requirements: [SWEEP-04]
must_haves:
  truths:
    - "Every Anthropic Haiku model_id in models.toml uses the global. inference profile, not us."
    - "The six re-swept roles carry exactly the agreed candidate lists (39 cells total)."
    - "Each re-swept role keeps a `# Previous default:` provenance line and a 2026-05-29 queued-set note with no invented scores."
    - "The judge panel in judge.py drops Sonnet and uses Mistral Large 3 (judge_a) + Nova Pro (judge_b) with verified pricing fields."
    - "A --dry-run sweep over the new 39-cell matrix prints a non-negative estimated_cost_usd with zero real Bedrock spend."
  artifacts:
    - path: "packages/model-adapter/src/model_adapter/models.toml"
      provides: "Refreshed role defaults + sweep_candidates + judge role blocks"
      contains: "global.anthropic.claude-haiku-4-5-20251001-v1:0"
    - path: "packages/eval-harness/src/eval_harness/judge.py"
      provides: "Re-pinned JUDGE_PANEL_CONFIG (Mistral Large 3 + Nova Pro)"
      contains: "mistral.mistral-large-3-675b-instruct"
    - path: "packages/eval-harness/src/eval_harness/pricing.py"
      provides: "Pricing entries for all new sweep candidates so estimate_sweep_cost is meaningful"
      contains: "mistral.mistral-large-3-675b-instruct"
  key_links:
    - from: "models.toml sweep_candidates"
      to: "eval_harness.preflight.estimate_sweep_cost via pricing.PRICES"
      via: "cost_for_usage lookup per candidate model_id"
      pattern: "cost_for_usage"
---

<objective>
Refresh the cost-frontier sweep configuration for a new 2026-05-29 sweep run and
validate the plumbing with a no-spend dry run.

Three config surfaces change:
1. `models.toml` — migrate all Anthropic Haiku ids from `us.` → `global.`, replace
   the six `sweep_candidates` arrays with the agreed lists, refresh the sweep-decision
   comment blocks (keep `# Previous default:` provenance, drop stale dated raw-results).
2. `judge.py` — re-pin the two-judge panel: drop Sonnet (Claude self-preference bias),
   add Mistral Large 3 as judge_a, keep Nova Pro as judge_b. Mirror the change in the
   `[roles.judge_a]`/`[roles.judge_b]` blocks of `models.toml` for consistency.
3. `pricing.py` — add the new candidate model ids so the dry-run cost estimate is not
   silently undercounted (estimate_sweep_cost swallows UnknownModelError).

Then run the sweep in `--dry-run` mode and capture `estimated_cost_usd` for the
39-cell matrix BEFORE any real spend.

Purpose: queue an accurate, bias-reduced sweep without incurring Bedrock cost.
Output: edited config files + a captured dry-run cost estimate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

<key_findings>
Discovered during planning — the executor should treat these as authoritative
so it does not re-derive them:

1. The judge panel that the harness ACTUALLY consumes is the hardcoded
   `JUDGE_PANEL_CONFIG` list in `packages/eval-harness/src/eval_harness/judge.py`
   (read by `make_judge` via `cfg["model_id"]` / `cfg["input_price_per_m"]` /
   `cfg["output_price_per_m"]`). The `[roles.judge_a]`/`[roles.judge_b]` blocks
   in models.toml are NOT read by the judge harness — but the spec asks to re-pin
   the judge panel, so edit BOTH: judge.py is the binding change, models.toml is
   kept consistent. Field names in judge.py are exactly `input_price_per_m` and
   `output_price_per_m` (NOT per_1m).

2. There is NO Typer CLI entry point for the sweep. The dry-run path is the
   library function `eval_harness.preflight.estimate_sweep_cost(role_candidates,
   n_cases, repeats)` (and/or `preflight_check(..., skip_bed01=True,
   auto_confirm=True)` which returns the same estimate). `run_full_matrix(...,
   dry_run=True)` skips BED-01 and all live cells. The dry-run validation in
   Task 3 therefore runs a short `uv run python` snippet, not a CLI command.

3. `estimate_sweep_cost` SILENTLY SKIPS models not present in
   `eval_harness.pricing.PRICES` (UnknownModelError swallowed). Most of the new
   candidates are absent from PRICES, so without Task 2's pricing additions the
   39-cell estimate would only price ~2 models and be meaningless. All new prices
   are available in `bedrock-models-considering.json` (verified during planning).

4. Verified prices (USD per 1M, input/output, from bedrock-models-considering.json):
   - global.anthropic.claude-haiku-4-5-20251001-v1:0 = 1.0 / 5.0
   - mistral.mistral-large-3-675b-instruct           = 0.50 / 1.50
   - qwen.qwen3-next-80b-a3b                          = 0.14 / 1.20
   - deepseek.v3.2                                    = 0.62 / 1.85
   - moonshotai.kimi-k2.5                             = 0.60 / 3.00
   - zai.glm-5                                        = 1.0 / 3.20
   - us.deepseek.r1-v1:0                              = 1.35 / 5.40
   - moonshot.kimi-k2-thinking                        = 0.60 / 2.50
   - openai.gpt-oss-120b-1:0                          = 0.15 / 0.60
   - openai.gpt-oss-20b-1:0                           = 0.07 / 0.30
   - minimax.minimax-m2.5                             = 0.30 / 1.20
   - zai.glm-4.7-flash                                = 0.07 / 0.40
   - qwen.qwen3-vl-235b-a22b                          = 0.53 / 2.66
   - mistral.ministral-3-14b-instruct                = 0.20 / 0.20
   - qwen.qwen3-coder-30b-a3b-v1:0                    = 0.15 / 0.60
   - qwen.qwen3-coder-next                            = 0.50 / 1.20
   - mistral.devstral-2-123b                          = 0.40 / 2.00
   - us.amazon.nova-pro-v1:0 = 0.80 / 3.20 (ALREADY in pricing.py and judge.py; NOT in
     the JSON — leave as-is, it is the existing pinned value; do NOT guess a new one).

5. DISCREPANCY to flag (do NOT silently fix): the existing PRICES entry for
   `qwen.qwen3-32b-v1:0` is 0.40 / 1.60, but the JSON now lists 0.15 / 0.60. The
   spec does not ask to re-price existing models. Leave the existing 0.40/1.60
   entry untouched and add a short TODO comment next to it noting the JSON delta.
</key_findings>

@packages/model-adapter/src/model_adapter/models.toml
@packages/eval-harness/src/eval_harness/judge.py
@packages/eval-harness/src/eval_harness/pricing.py
@packages/eval-harness/src/eval_harness/preflight.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refresh models.toml — Haiku→global migration, six candidate lists, decision comments</name>
  <files>packages/model-adapter/src/model_adapter/models.toml</files>
  <action>
Apply these surgical edits to models.toml. Every changed line must trace to the
spec — do not reformat untouched blocks or change region/max_tokens/max_concurrency.

(A) Haiku id migration (Anthropic-only). Replace EVERY occurrence of
`us.anthropic.claude-haiku-4-5-20251001-v1:0` with
`global.anthropic.claude-haiku-4-5-20251001-v1:0`. This affects the role-default
`model_id` lines for: preflight, librarian, code_reader, scanner, narrator,
domain-proposer — AND every Haiku entry inside the sweep_candidates arrays.
Do NOT touch non-Anthropic ids (Nova, Qwen, etc.) — only Anthropic has a global
profile. Also migrate `us.anthropic.claude-sonnet-4-6` → leave UNCHANGED here
(Sonnet is being dropped from judges in Task 2 and is not in any new candidate
list; the only remaining Sonnet references are inside stale sweep_candidates
arrays that this task replaces wholesale anyway).

(B) Replace the six sweep_candidates arrays with these EXACT lists (order as given):
  - librarian (6): global.anthropic.claude-haiku-4-5-20251001-v1:0, qwen.qwen3-32b-v1:0,
    qwen.qwen3-next-80b-a3b, deepseek.v3.2, moonshotai.kimi-k2.5, zai.glm-5
  - synthesizer (8): global.anthropic.claude-haiku-4-5-20251001-v1:0, qwen.qwen3-32b-v1:0,
    zai.glm-5, moonshotai.kimi-k2.5, deepseek.v3.2, qwen.qwen3-next-80b-a3b,
    us.deepseek.r1-v1:0, moonshot.kimi-k2-thinking
  - linter (6): global.anthropic.claude-haiku-4-5-20251001-v1:0, qwen.qwen3-32b-v1:0,
    openai.gpt-oss-120b-1:0, deepseek.v3.2, minimax.minimax-m2.5, zai.glm-4.7-flash
  - ingestor (7): global.anthropic.claude-haiku-4-5-20251001-v1:0, qwen.qwen3-32b-v1:0,
    openai.gpt-oss-120b-1:0, minimax.minimax-m2.5, qwen.qwen3-next-80b-a3b,
    zai.glm-4.7-flash, qwen.qwen3-vl-235b-a22b
  - scanner (6): global.anthropic.claude-haiku-4-5-20251001-v1:0, openai.gpt-oss-20b-1:0,
    zai.glm-4.7-flash, mistral.ministral-3-14b-instruct, qwen.qwen3-32b-v1:0,
    qwen.qwen3-coder-30b-a3b-v1:0
  - code_reader (6): global.anthropic.claude-haiku-4-5-20251001-v1:0,
    qwen.qwen3-coder-30b-a3b-v1:0, qwen.qwen3-coder-next, mistral.devstral-2-123b,
    openai.gpt-oss-120b-1:0, minimax.minimax-m2.5

(C) Role default model_id lines — keep current production picks (per spec), but
note the Haiku-bearing ones move to global per (A):
  - librarian default: global.anthropic.claude-haiku-4-5-20251001-v1:0 (was us. Haiku)
  - scanner default:   global.anthropic.claude-haiku-4-5-20251001-v1:0 (was us. Haiku)
  - code_reader default: global.anthropic.claude-haiku-4-5-20251001-v1:0 (was us. Haiku)
  - ingestor default:  qwen.qwen3-32b-v1:0 (UNCHANGED — in candidate list)
  - synthesizer default: qwen.qwen3-32b-v1:0 (UNCHANGED — in candidate list)
  - linter default:    us.amazon.nova-lite-v1:0 (UNCHANGED — NOT in new candidate list)

(D) Comment refresh for each re-swept role. Replace the stale
"Sweep candidates (run 2026-05-17): raw results ..." comment lines with a short
queued-set note. Keep (or add) a `# Previous default:` provenance line recording
the immediately-prior default id (SWEEP-04 requirement). Use this template per role
(no scores — the sweep has not run):
    # Sweep candidates queued 2026-05-29 (candidate set below; sweep not yet run — no scores).
    # Previous default: <prior default id>
For the LINTER role specifically, ADD this flag line (its incumbent default
us.amazon.nova-lite-v1:0 is NOT in the new candidate list):
    # NOTE: incumbent default us.amazon.nova-lite-v1:0 is NOT in the 2026-05-29 candidate
    # set — default left as-is; needs a decision after the sweep runs.

Do NOT add sweep_candidates to preflight, narrator, domain-proposer, judge_a,
or judge_b (they have none today — preserve that).
  </action>
  <verify>
    <automated>cd /Users/pat/Personal/agent-research && python3 -c "import tomllib; d=tomllib.load(open('packages/model-adapter/src/model_adapter/models.toml','rb'))['roles']; assert not any('us.anthropic.claude-haiku' in str(v) for v in d.values()), 'us. Haiku still present'; counts={'librarian':6,'synthesizer':8,'linter':6,'ingestor':7,'scanner':6,'code_reader':6}; [print(r, len(d[r]['sweep_candidates']), 'OK' if len(d[r]['sweep_candidates'])==n else 'WRONG') or (len(d[r]['sweep_candidates'])==n or (_ for _ in ()).throw(AssertionError(r))) for r,n in counts.items()]; assert all(d[r]['sweep_candidates'][0]=='global.anthropic.claude-haiku-4-5-20251001-v1:0' for r in counts), 'haiku-global not first'; assert d['linter']['model_id']=='us.amazon.nova-lite-v1:0'; assert d['ingestor']['model_id']=='qwen.qwen3-32b-v1:0'; print('ALL OK')"</automated>
  </verify>
  <done>
models.toml parses as valid TOML; no `us.anthropic.claude-haiku` id remains; the
six sweep_candidates arrays match the agreed lists by length and ordering (global
Haiku first in each); linter/ingestor/synthesizer defaults unchanged; each re-swept
role has a `# Previous default:` line and a 2026-05-29 queued note; linter has the
incumbent-not-a-candidate flag comment.
  </done>
</task>

<task type="auto">
  <name>Task 2: Re-pin judge panel (judge.py + models.toml) and add candidate pricing (pricing.py)</name>
  <files>packages/eval-harness/src/eval_harness/judge.py, packages/model-adapter/src/model_adapter/models.toml, packages/eval-harness/src/eval_harness/pricing.py</files>
  <action>
(A) judge.py — re-pin JUDGE_PANEL_CONFIG. Drop the Sonnet entry entirely. New panel
(two dicts, in this order). Use the exact field names the code reads
(`model_id`, `input_price_per_m`, `output_price_per_m`):
  judge_a: model_id="mistral.mistral-large-3-675b-instruct", input_price_per_m=0.50,
           output_price_per_m=1.50
  judge_b: model_id="us.amazon.nova-pro-v1:0", input_price_per_m=0.80,
           output_price_per_m=3.20  (UNCHANGED from current)
Update the module-level comment above JUDGE_PANEL_CONFIG: replace the
"Sonnet for quality ceiling, Nova Pro for cost diversity" rationale with a note that
Sonnet was dropped to remove Claude-family self-preference bias (Haiku is a candidate
in every role), and that judge_a=Mistral Large 3 / judge_b=Nova Pro are both
independent of all candidate families. Add the dated-ARN baseline-drift caveat:
the ideal is a dated revision ARN, but neither id exposes one in the catalog, so we
pin the alias and accept drift risk.
For Nova-Pro pricing: 0.80/3.20 is the EXISTING pinned value (also in pricing.py).
It is NOT in bedrock-models-considering.json; keep it as-is — do NOT guess a new
number. (No TODO needed since it is the already-pinned value.)

(B) models.toml — for consistency only, update the `[roles.judge_a]` and
`[roles.judge_b]` blocks:
  judge_a model_id → mistral.mistral-large-3-675b-instruct
  judge_b model_id → us.amazon.nova-pro-v1:0 (unchanged)
Add a one-line comment above these two blocks noting that the binding judge config
lives in eval_harness/judge.py (JUDGE_PANEL_CONFIG) and these blocks are kept in
sync for reference. Do NOT add pricing fields to the toml judge blocks (the toml
schema has no price fields and the harness does not read them from toml).

(C) pricing.py — add PRICES entries for every NEW candidate/judge model id that is
not already present, so estimate_sweep_cost prices the full 39-cell matrix instead
of silently skipping unknowns. Add (input/output USD per 1M):
  global.anthropic.claude-haiku-4-5-20251001-v1:0 = 1.0 / 5.0
  mistral.mistral-large-3-675b-instruct           = 0.50 / 1.50
  qwen.qwen3-next-80b-a3b                          = 0.14 / 1.20
  deepseek.v3.2                                    = 0.62 / 1.85
  moonshotai.kimi-k2.5                             = 0.60 / 3.00
  zai.glm-5                                        = 1.0 / 3.20
  us.deepseek.r1-v1:0                              = 1.35 / 5.40
  moonshot.kimi-k2-thinking                        = 0.60 / 2.50
  openai.gpt-oss-120b-1:0                          = 0.15 / 0.60
  openai.gpt-oss-20b-1:0                           = 0.07 / 0.30
  minimax.minimax-m2.5                             = 0.30 / 1.20
  zai.glm-4.7-flash                                = 0.07 / 0.40
  qwen.qwen3-vl-235b-a22b                          = 0.53 / 2.66
  mistral.ministral-3-14b-instruct                = 0.20 / 0.20
  qwen.qwen3-coder-30b-a3b-v1:0                    = 0.15 / 0.60
  qwen.qwen3-coder-next                            = 0.50 / 1.20
  mistral.devstral-2-123b                          = 0.40 / 2.00
Each entry is a dict with only `input` and `output` keys (Bedrock non-Claude models
have no cache keys — match the existing Nova/Qwen entry shape). Source-date these in
the module docstring or a trailing comment: "2026-05-29 additions from
bedrock-models-considering.json". Do NOT add cache_read/cache_write to the new
non-Claude entries.
For the EXISTING `qwen.qwen3-32b-v1:0` entry (currently 0.40 / 1.60): LEAVE THE
VALUES UNCHANGED, but add a one-line TODO comment next to it:
  # TODO(2026-05-29): bedrock-models-considering.json now lists 0.15/0.60 for this
  # id; left at 0.40/1.60 (existing pin) — reconcile separately.
  </action>
  <verify>
    <automated>cd /Users/pat/Personal/agent-research && python3 -c "
import importlib, sys
sys.path.insert(0,'packages/eval-harness/src'); sys.path.insert(0,'packages/model-adapter/src')
from eval_harness.judge import JUDGE_PANEL_CONFIG as J
assert len(J)==2, J
assert J[0]['model_id']=='mistral.mistral-large-3-675b-instruct', J[0]
assert J[1]['model_id']=='us.amazon.nova-pro-v1:0', J[1]
assert all('input_price_per_m' in c and 'output_price_per_m' in c for c in J)
assert not any('sonnet' in c['model_id'].lower() for c in J), 'sonnet not dropped'
from eval_harness.pricing import PRICES, cost_for_usage
need=['global.anthropic.claude-haiku-4-5-20251001-v1:0','mistral.mistral-large-3-675b-instruct','qwen.qwen3-next-80b-a3b','deepseek.v3.2','moonshotai.kimi-k2.5','zai.glm-5','us.deepseek.r1-v1:0','moonshot.kimi-k2-thinking','openai.gpt-oss-120b-1:0','openai.gpt-oss-20b-1:0','minimax.minimax-m2.5','zai.glm-4.7-flash','qwen.qwen3-vl-235b-a22b','mistral.ministral-3-14b-instruct','qwen.qwen3-coder-30b-a3b-v1:0','qwen.qwen3-coder-next','mistral.devstral-2-123b']
missing=[m for m in need if m not in PRICES]
assert not missing, ('missing prices: '+repr(missing))
assert PRICES['qwen.qwen3-32b-v1:0']['input']==0.40, 'existing qwen3-32b price changed'
print('ALL OK')
"</automated>
  </verify>
  <done>
JUDGE_PANEL_CONFIG has exactly two judges (Mistral Large 3, Nova Pro), no Sonnet,
correct price field names; models.toml judge blocks mirror those ids with the
"binding config in judge.py" note; pricing.py has entries for all 17 new ids and the
existing qwen3-32b entry is unchanged (0.40/1.60) with a reconcile TODO.
  </done>
</task>

<task type="auto">
  <name>Task 3: Dry-run validation — estimate cost for the 39-cell matrix with zero spend</name>
  <files>packages/eval-harness/src/eval_harness/models.toml</files>
  <action>
Run a NO-SPEND dry run that loads the new sweep_candidates from models.toml and
prints the estimated cost for the full 39-cell matrix. Do NOT run any live
(non-dry-run) sweep — make zero real Bedrock calls.

Use a short `uv run python` snippet (there is no sweep CLI). Build the
role_candidates dict by reading each re-swept role's sweep_candidates via
`model_adapter.loader.load_role_config(role)`, then call
`eval_harness.preflight.estimate_sweep_cost(role_candidates, n_cases, repeats)` and
print the result. Also assert no model in the matrix is missing from pricing.PRICES
(catch any typo introduced in Task 1/2 — if estimate silently skipped a model the
number would be wrong). Use representative `n_cases` and `repeats` matching the
sweep defaults (`repeats=3`; pick `n_cases` to match the eval cases file count, or
use n_cases=4 as the documented estimator baseline and state which you used).

Run from the workspace root, e.g.:
  uv run --package eval-harness python -c "<snippet>"
The snippet must:
  1. Iterate roles ["librarian","synthesizer","linter","ingestor","scanner","code_reader"].
  2. cand[role] = load_role_config(role)["sweep_candidates"].
  3. Assert total cell count == 39 (6+8+6+7+6+6).
  4. For every candidate id, assert id in pricing.PRICES (fail loudly on any miss).
  5. Print estimate_sweep_cost(cand, n_cases=4, repeats=3) as estimated_cost_usd.
  6. Confirm it is below preflight.HARD_CAP_USD (25.0) and print the headroom.

Capture the printed estimated_cost_usd value verbatim in the task summary /
completion notes (this is the deliverable). If any candidate is missing from PRICES
or the cell count != 39, STOP and report rather than papering over it.

Optionally also exercise the real estimator entry point with
`preflight.preflight_check(cand, n_cases=4, repeats=3, skip_bed01=True,
auto_confirm=True)` to confirm it returns the same float without prompting or
pinging Bedrock — this proves the dry-run plumbing end to end. Do NOT call
run_full_matrix without dry_run=True.
  </action>
  <verify>
    <automated>cd /Users/pat/Personal/agent-research && uv run --package eval-harness python -c "
from model_adapter.loader import load_role_config
from eval_harness.preflight import estimate_sweep_cost, preflight_check, HARD_CAP_USD
from eval_harness.pricing import PRICES
roles=['librarian','synthesizer','linter','ingestor','scanner','code_reader']
cand={r:load_role_config(r)['sweep_candidates'] for r in roles}
cells=sum(len(v) for v in cand.values())
assert cells==39, ('expected 39 cells, got '+str(cells))
miss=[m for v in cand.values() for m in v if m not in PRICES]
assert not miss, ('candidates missing from PRICES: '+repr(sorted(set(miss))))
est=estimate_sweep_cost(cand, n_cases=4, repeats=3)
est2=preflight_check(cand, n_cases=4, repeats=3, skip_bed01=True, auto_confirm=True)
assert abs(est-est2)<1e-9, (est, est2)
assert est < HARD_CAP_USD, ('estimate exceeds hard cap', est)
print(f'estimated_cost_usd={est:.4f}  cells={cells}  headroom_to_cap=\${HARD_CAP_USD-est:.2f}')
print('DRY-RUN OK — no Bedrock spend')
"</automated>
  </verify>
  <done>
The dry-run snippet runs with zero Bedrock calls, confirms a 39-cell matrix, every
candidate is priced (no silent skips), prints a concrete `estimated_cost_usd` under
the $25 hard cap, and `preflight_check(..., skip_bed01=True, auto_confirm=True)`
returns the identical estimate without prompting or pinging Bedrock. The
estimated_cost_usd value is recorded in the completion notes.
  </done>
</task>

</tasks>

<verification>
- `models.toml` parses as TOML and contains no `us.anthropic.claude-haiku` id.
- The six sweep_candidates arrays match the agreed lists exactly (length + order).
- `JUDGE_PANEL_CONFIG` = [Mistral Large 3, Nova Pro], no Sonnet, correct field names.
- All 39 candidate ids are present in `pricing.PRICES`.
- Dry-run prints `estimated_cost_usd` < $25 with zero real Bedrock spend.
- No new application code or features added; existing role/judge block structure,
  ordering, and non-swept blocks preserved.
</verification>

<success_criteria>
- Haiku→global migration complete for all Anthropic Haiku ids (defaults + candidates).
- Six candidate arrays + decision/provenance comments refreshed (no invented scores).
- Linter incumbent-not-a-candidate flagged in a comment; default left unchanged.
- Judge panel re-pinned in judge.py (binding) and mirrored in models.toml; drift caveat noted.
- pricing.py extended with all new candidate prices; existing qwen3-32b price flagged but unchanged.
- A concrete dry-run `estimated_cost_usd` for the 39-cell matrix is captured, under the hard cap, with no Bedrock spend.
</success_criteria>

<output>
Create `.planning/quick/260529-na9-refresh-models-toml-sweep-candidates-and/260529-na9-SUMMARY.md` when done.
Record the captured `estimated_cost_usd` value in the summary.
</output>
