---
quick_id: 260519-k9t
slug: preflight-role
status: in-progress
date: 2026-05-19
---

# Quick Task 260519-k9t: Add preflight role, remove sonnet role

## Description

Make `make_llm("preflight")` the explicit handle used by BED-01 connectivity smoke
test instead of repurposing `make_llm("haiku")`. Remove the unused `sonnet` alias
role entirely. `haiku` stays as a generic cheap-model handle for tests.

## Why

- The `sonnet` role in `models.toml` has no production caller — only test fixtures use it.
- `preflight.py:86` calls `make_llm("haiku")` purely because it needs a known-cheap
  handle to ping Bedrock; promoting that to its own role makes the intent explicit
  and decouples preflight from any future `haiku` re-pointing.

## Scope (1 task)

### T1 — Add preflight, drop sonnet, update callers

**Files:**
- `packages/model-adapter/src/model_adapter/models.toml` — add `[roles.preflight]` (haiku-4.5 ARN, low max_tokens), remove `[roles.sonnet]` block.
- `packages/eval-harness/src/eval_harness/preflight.py` — `make_llm("haiku")` → `make_llm("preflight")`; update docstring.
- `packages/model-adapter/tests/test_loader.py` — drop `test_make_llm_sonnet_*` test and `SONNET_ARN` constant; replace `"sonnet"` in `ALL_ROLES` with `"preflight"`; add a `test_make_llm_preflight_*` test.
- `models-claude.toml` — replace `[roles.sonnet]` block with `[roles.preflight]`.
- `models-qwen.toml` — replace `[roles.sonnet]` block with `[roles.preflight]`.

**Action:** Edit the five files above per the plan.

**Verify:**
- `uv run --package model-adapter pytest packages/model-adapter/tests/ -x -q`
- `uv run --package eval-harness pytest packages/eval-harness/tests/ -m "not eval" -k preflight -x -q`
- `grep -rn '"sonnet"\|make_llm("sonnet")' packages/ agents/` returns no production hits.

**Done:** Tests pass; sonnet role no longer present in any `models*.toml`; preflight role used by `preflight_bed01()`.

## must_haves

- `[roles.preflight]` exists in `packages/model-adapter/src/model_adapter/models.toml`
- `[roles.sonnet]` removed from all three TOML files
- `preflight.py` calls `make_llm("preflight")`
- Model-adapter tests pass
- One atomic commit
