---
phase: 260521-gc0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/workspace-io/src/workspace_io/config.py
  - packages/workspace-io/tests/test_config.py
  - packages/wiki-io/src/wiki_io/assets/page-templates/package/overview.md
  - packages/wiki-io/src/wiki_io/lint_wiki.py
  - packages/wiki-io/tests/test_lint_wiki.py
  - packages/wiki-io/src/wiki_io/update_tokens.py
  - packages/wiki-io/tests/test_update_tokens.py
autonomous: true
requirements:
  - W1-repo-discovery
  - W5-pathqual-wikilinks
  - F1-schema-file-exclusion
  - F2-tokens-null-on-unsupported

must_haves:
  truths:
    - "When GRAPH_WIKI_WORKSPACE points at a workspace that is itself a git repo, resolve() respects `repo-directory:` in the workspace manifest instead of binding repo_root to the workspace's own .git."
    - "Package overview pages, when rendered from the asset template, link sub-pages with path-qualified wikilinks (e.g. [[packages/<pkg>/api|api]]) so lint_wiki cannot misclassify them as orphans."
    - "lint_wiki.scan() does NOT flag `CLAUDE.md` or `AGENTS.md` (at any depth) for missing_frontmatter or missing_tokens — they are schema files, not wiki pages."
    - "When the Bedrock CountTokens API rejects a model as unsupported, update_tokens writes `tokens: null` to the page frontmatter (and a legitimate API return of 0 still writes `tokens: 0`)."
  artifacts:
    - path: "packages/workspace-io/src/workspace_io/config.py"
      provides: "resolve() honoring repo-directory: from .graph-wiki.yaml and .graph-wiki.local.yaml in both env-var and discovery branches"
      contains: "repo-directory"
    - path: "packages/wiki-io/src/wiki_io/assets/page-templates/package/overview.md"
      provides: "Sub-pages section using path-qualified wikilinks with display aliases"
      contains: "[[packages/"
    - path: "packages/wiki-io/src/wiki_io/lint_wiki.py"
      provides: "page-enumeration loop excluding CLAUDE.md and AGENTS.md at any depth"
      contains: "CLAUDE.md"
    - path: "packages/wiki-io/src/wiki_io/update_tokens.py"
      provides: "unsupported-model branch writing tokens: null (preserving 0 only for legitimate empty counts)"
      contains: "tokens: null"
  key_links:
    - from: "packages/workspace-io/src/workspace_io/config.py"
      to: ".graph-wiki.yaml / .graph-wiki.local.yaml"
      via: "_local_config.read() for `repo-directory` key"
      pattern: "_local_config\\.read"
    - from: "packages/wiki-io/src/wiki_io/update_tokens.py"
      to: "boto3 bedrock-runtime CountTokens"
      via: "ValidationException branch returning None instead of 0"
      pattern: "ValidationException|unsupported"

---

<objective>
Close four independent lint-driven defects surfaced by today's `/graph-wiki:lint` run on the freshly bootstrapped `wikis/agent-research` vault. Each fix is a small, surgical change isolated to one module + one test file, sequenced in a single quick plan because they share no files.

Purpose: Eliminate spurious lint findings caused by (a) repo discovery binding to the wiki's own `.git`, (b) bare wikilinks in the package overview template, (c) schema files (`CLAUDE.md`, `AGENTS.md`) being treated as pages, and (d) `tokens: 0` conflating "API unsupported" with "empty page". Together these account for ~40+ false-positive lint findings.

Output: Updated source in `packages/workspace-io/` and `packages/wiki-io/` plus four tests covering the new behavior. W4 (stub empty category index files) is intentionally OUT OF SCOPE — it lives in todo `2026-05-21-bootstrap-should-stub-empty-category-index-files.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@packages/workspace-io/src/workspace_io/config.py
@packages/workspace-io/src/workspace_io/_local_config.py
@packages/workspace-io/tests/test_config.py
@packages/wiki-io/src/wiki_io/lint_wiki.py
@packages/wiki-io/src/wiki_io/update_tokens.py
@packages/wiki-io/tests/test_update_tokens.py
@packages/wiki-io/src/wiki_io/assets/page-templates/package/overview.md
@packages/wiki-io/src/wiki_io/assets/page-templates/domain/overview.md

<interfaces>
<!-- Pre-extracted contracts the executor will need. No codebase scavenger hunt required. -->

From `packages/workspace-io/src/workspace_io/config.py`:
- `LOCAL_CONFIG_FILENAME = ".graph-wiki.local.yaml"` and `WORKSPACE_DIRECTORY_KEY = "workspace-directory"`.
- `def resolve(cwd: Path | None = None) -> GraphWikiConfig:` — two branches:
  - env-var branch (lines ~60-65): when `GRAPH_WIKI_WORKSPACE` is set, builds `workspace` then `repo_root = _find_repo_root(workspace) or workspace.parent.resolve()`. This is the buggy path.
  - discovery branch (lines ~67-78): finds repo_root by walking up from cwd, then `resolve_workspace(repo_root)` reads `<repo>/.graph-wiki.local.yaml` for `workspace-directory:` only.
- `def _find_repo_root(start) -> Path | None:` returns the first ancestor (or start itself) containing `.git`.

From `packages/workspace-io/src/workspace_io/_local_config.py`:
- `def read(path: Path) -> dict[str, str]:` returns flat top-level `key: value` pairs from a YAML-ish file. Handles `~`, quotes, inline `#` comments. Returns `{}` if file is missing or malformed. Reuse this for parsing both `.graph-wiki.yaml` and `.graph-wiki.local.yaml`.

From `packages/wiki-io/src/wiki_io/lint_wiki.py`:
- The page enumerator is the `for md in workspace.rglob("*.md"):` loop starting at line 86.
- Already-excluded filenames: `log.md` (line 91), `index.md` (line 102). Dotdir paths (`.graph/`, `.obsidian/`) are excluded at line 89.
- An identical second loop at line 144 (`for md in workspace.rglob("*.md"):`) parses index pages for outbound links — must apply the same exclusion logic for consistency (index.md is the only one that loop currently keeps; CLAUDE.md/AGENTS.md should be skipped there too).

From `packages/wiki-io/src/wiki_io/update_tokens.py`:
- `count_tokens(text, model_id, region)` calls `boto3.client("bedrock-runtime").count_tokens(...)` and returns `response["inputTokens"]`. This is the helper that may raise.
- `update_page(path, ...)` wraps the call in a broad `except Exception` (line 111) and currently returns `("skipped", 0)` on ANY error — this is the source of the bug: unsupported-model errors are lumped with read errors, and the page keeps its existing `tokens: 0` (or never gets one).
- Frontmatter is rewritten line-by-line preserving original YAML formatting (lines 121-141). The `tokens:` field is matched by `line == "tokens:" or line.startswith("tokens: ")`. Writing `tokens: null` must round-trip through this same machinery and through `python-frontmatter`'s `frontmatter.loads()` (which parses `null` as Python `None`).
- `DEFAULT_MODEL_ID = "us.anthropic.claude-haiku-4-5-20251001-v1:0"` — Bedrock CountTokens raises `botocore.exceptions.ClientError` with `error_response["Error"]["Code"] == "ValidationException"` for unsupported model+operation combinations.

From `packages/wiki-io/src/wiki_io/assets/page-templates/package/overview.md`:
- Lines 32-36 are the Sub-pages list with bare wikilinks: `[[api]]`, `[[patterns]]`, `[[work]]`, `[[context]]`.
- This template is rendered by `init_vault.render_template(src, dest, variables)` and by `scan_monorepo` when seeding new package pages. Variables available: `{{PACKAGE_TITLE}}`, `{{DATE}}`. Path interpolation must use a NEW variable (e.g. `{{PACKAGE_SLUG}}`) — but the lint_wiki resolver already handles the folder-shorthand form `[[<container>/<name>]]` (line 124-128) and the explicit path form `[[packages/<pkg>/<sub>]]` directly via `link_targets` set membership. So no renderer change is needed if we use a literal hardcoded prefix that matches the on-disk path layout.
- BUT: package overviews are emitted into `<workspace>/packages/<pkg>/<pkg>.md` (the folder-shorthand form). Sub-pages live at `<workspace>/packages/<pkg>/<sub>.md`. So a wikilink from the overview to a sibling must use the form `[[packages/<pkg>/<sub>|<sub>]]` where `<pkg>` varies per page. This DOES require a render-time variable. See task 2's action for the variable approach.

From `packages/wiki-io/src/wiki_io/init_vault.py` (line 70):
- `def render_template(src, dest, variables):` performs `{{KEY}}` substitution. To add a `{{PACKAGE_SLUG}}` token, callers of `render_template` that emit `package/overview.md` must pass `PACKAGE_SLUG` in the variables dict. Search for `package/overview.md` (or its parent dir use) under `scan_monorepo.py` and `init_vault.py` to enumerate call sites.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: W1 — Workspace manifest `repo-directory:` overrides repo discovery</name>
  <files>packages/workspace-io/src/workspace_io/config.py, packages/workspace-io/tests/test_config.py</files>
  <behavior>
    - Test A (env-var branch, manifest sets repo-directory): set `GRAPH_WIKI_WORKSPACE` to a directory that IS a git repo and ALSO contains `.graph-wiki.yaml` with `repo-directory: /tmp/some-other-repo`. Call `resolve()`. Assert `cfg.repo_root == Path("/tmp/some-other-repo").resolve()` (NOT the workspace itself). Use a `tmp_path`-built target repo.
    - Test B (env-var branch, manifest sets repo-directory with `~`): same as A but `repo-directory: ~/projects/foo`; monkeypatch `HOME`; assert expansion.
    - Test C (env-var branch, no repo-directory key): existing behavior preserved — repo_root is `_find_repo_root(workspace) or workspace.parent.resolve()`.
    - Test D (discovery branch, .graph-wiki.local.yaml sets repo-directory): repo on disk at `/A/repo`, workspace at `/A/repo/graph-wiki`, but `<repo>/.graph-wiki.local.yaml` declares `repo-directory: /B/other-repo`. After discovery walks up from cwd inside `/A/repo` and locates `.git`, the manifest's `repo-directory:` MUST override that finding so `cfg.repo_root == Path("/B/other-repo").resolve()`. (This case is the symmetric override for non-env-var usage.)
    - Test E (discovery branch, no repo-directory): existing behavior preserved.
    - Test F (relative path): `repo-directory: ../sibling-repo` in workspace manifest, expand against the WORKSPACE directory.
  </behavior>
  <action>
    Extend `workspace_io.config.resolve()` so both branches consult a new `repo-directory` key.

    Add a module-level constant `REPO_DIRECTORY_KEY = "repo-directory"` next to the existing `WORKSPACE_DIRECTORY_KEY`.

    Add a helper `_repo_directory_override(workspace: Path, repo_root_default: Path) -> Path:` that:
    1. Reads `<workspace>/.graph-wiki.yaml` via `_local_config.read(...)`.
    2. Reads `<workspace>/.graph-wiki.local.yaml` via `_local_config.read(...)` and merges its keys ON TOP of the manifest (local overrides committed manifest — same precedence pattern as the existing `workspace-directory` convention).
    3. If `REPO_DIRECTORY_KEY` is present and non-empty, expand `~` and resolve relative paths against `workspace`; return the resolved Path.
    4. Otherwise return `repo_root_default` unchanged.

    Env-var branch (currently lines 60-65): after computing the default `repo_root = _find_repo_root(workspace) or workspace.parent.resolve()`, pass it through `_repo_directory_override(workspace, repo_root)` and use the result.

    Discovery branch (currently lines 67-78): after computing `workspace = resolve_workspace(repo_root)`, pass `repo_root` through `_repo_directory_override(workspace, repo_root)` and rebind. The strict-manifest check at lines 72-77 stays unchanged.

    Also: in the discovery branch, the existing `<repo>/.graph-wiki.local.yaml` lookup for `workspace-directory:` stays as-is — `repo-directory:` is read from the WORKSPACE's manifest, not the repo's, by design (the workspace is the authoritative source of "which repo do I describe").

    Update the module docstring to mention `repo-directory:` alongside `workspace-directory:`.

    Add the six tests above to `packages/workspace-io/tests/test_config.py`. Reuse the existing `_make_repo` and `_seed_manifest` helpers; add a `_seed_manifest_with(workspace, keys: dict[str, str])` helper that writes additional lines to `.graph-wiki.yaml`. Tests should monkeypatch `GRAPH_WIKI_WORKSPACE` explicitly (set or `delenv`) to disambiguate the two branches.

    Do NOT touch `resolve_workspace()` or `_find_repo_root()` — the override fits cleanly on top.
  </action>
  <verify>
    <automated>uv run --package workspace-io pytest packages/workspace-io/tests/test_config.py -x -v</automated>
  </verify>
  <done>All six new tests pass. Existing 9 tests in test_config.py still pass. `grep -n "repo-directory" packages/workspace-io/src/workspace_io/config.py` shows the new key handling in both branches.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: W5 — Path-qualified wikilinks in package and domain overview templates</name>
  <files>packages/wiki-io/src/wiki_io/assets/page-templates/package/overview.md, packages/wiki-io/src/wiki_io/assets/page-templates/domain/overview.md, packages/wiki-io/src/wiki_io/init_vault.py, packages/wiki-io/src/wiki_io/scan_monorepo.py, packages/wiki-io/tests/test_scan_monorepo.py</files>
  <behavior>
    - Test: render the package overview template through whatever code path is used in production (`init_vault.render_template` with a `PACKAGE_SLUG` variable, OR the scan-monorepo seeding code). After substitution, assert the rendered file contains `[[packages/<slug>/api|api]]`, `[[packages/<slug>/patterns|patterns]]`, `[[packages/<slug>/work|work]]`, `[[packages/<slug>/context|context]]` (exact path-qualified form with display alias). Bare `[[api]]` etc. must be ABSENT in the rendered output.
    - Test (domain): render the domain overview template with a `DOMAIN_SLUG` variable (or the analog). Assert the rendered file contains `[[domains/<slug>/details|details]]` (path-qualified), NOT bare `[[details]]`.
    - Add the tests next to existing scan/init tests. Reuse `packages/wiki-io/tests/fixtures/` if a suitable fake repo fixture already exists; otherwise build a minimal one in-test.
  </behavior>
  <action>
    Step 1: Audit call sites. Grep `packages/wiki-io/src/wiki_io/` for `package/overview.md` and `domain/overview.md` to enumerate every place these templates are rendered. Expected call sites: `init_vault.render_template(ASSETS_DIR / "package/overview.md", ...)` and the scan-monorepo seeding path (search `scan_monorepo.py` for the package-overview emission — likely uses `render_template` or inlines the same substitution).

    Step 2: Decide variable names. Use `{{PACKAGE_SLUG}}` for the package case (the on-disk folder name under `<workspace>/packages/`) and `{{DOMAIN_SLUG}}` for the domain case. These are different from `{{PACKAGE_TITLE}}` (which is a display string and may contain spaces).

    Step 3: Edit `packages/wiki-io/src/wiki_io/assets/page-templates/package/overview.md` Sub-pages block to:
    ```
    ## Sub-pages
    - [[packages/{{PACKAGE_SLUG}}/api|api]]           — public API, exports, CLI subcommands
    - [[packages/{{PACKAGE_SLUG}}/patterns|patterns]] — key patterns and conventions
    - [[packages/{{PACKAGE_SLUG}}/work|work]]         — bugs, tech debt, features, open questions
    - [[packages/{{PACKAGE_SLUG}}/context|context]]   — concepts, decisions, ADRs, sources
    ```
    Preserve trailing blank line.

    Step 4: Edit `packages/wiki-io/src/wiki_io/assets/page-templates/domain/overview.md` Sub-pages line:
    ```
    ## Sub-pages
    - [[domains/{{DOMAIN_SLUG}}/details|details]] — concepts, dependencies, decisions, sources, contrasts
    ```

    Step 5: Update every call site to pass `PACKAGE_SLUG` / `DOMAIN_SLUG` in the variables dict. The slug is the on-disk folder name — `init_vault.py` and `scan_monorepo.py` already know it (it's the directory they're rendering into). If a call site does not yet have access to the slug, derive it from `dest.parent.name`.

    Step 6: Container-path is hardcoded as `packages/` and `domains/` because the asset template lives at `assets/page-templates/package/overview.md` (template-per-category) and the on-disk layout uses those literal prefixes. If a workspace later remaps containers via `wiki/CLAUDE.md` layout block, that's a separate concern — file a follow-up todo IF AND ONLY IF the audit in Step 1 shows that current code already supports non-default container paths for package/domain emission. Otherwise leave as a future concern (note in SUMMARY).

    Step 7: Add the two render tests described in `<behavior>`. If the existing test_scan_monorepo.py or test_init_vault.py already covers template rendering, extend those; otherwise create `test_overview_template_wikilinks.py`.
  </action>
  <verify>
    <automated>uv run --package wiki-io pytest packages/wiki-io/tests/ -x -v -k "overview or template or wikilink"</automated>
  </verify>
  <done>Both overview templates contain only path-qualified wikilinks in their Sub-pages sections. Rendered output (via tests) confirms the wikilinks resolve via the on-disk path. `grep -n "\[\[api\]\]\|\[\[patterns\]\]\|\[\[work\]\]\|\[\[context\]\]\|\[\[details\]\]" packages/wiki-io/src/wiki_io/assets/page-templates/` returns no matches in the Sub-pages blocks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: F1 — Exclude CLAUDE.md and AGENTS.md from lint page enumeration</name>
  <files>packages/wiki-io/src/wiki_io/lint_wiki.py, packages/wiki-io/tests/test_lint_wiki.py</files>
  <behavior>
    - Test A: build a fake workspace under `tmp_path` containing `wiki/CLAUDE.md` (with arbitrary non-frontmatter content), `wiki/AGENTS.md`, and one legitimate page `wiki/foo.md` with full frontmatter. Run `scan(wiki, stale_days=90, log_gap_days=14, repo_path=None)`. Assert `report["missing_frontmatter"]` does NOT contain `"wiki/CLAUDE.md"[:-3]` or `"wiki/AGENTS.md"[:-3]`. Assert `report["missing_tokens"]` likewise.
    - Test B (any-depth coverage — decision: any-depth, see must_haves notes): also place a `wiki/packages/foo/CLAUDE.md` and assert it is NOT flagged. (Even though this isn't current practice, the planner's decision is to make exclusion forward-compatible.)
    - Test C: assert `report["total_pages"]` reflects the legit page only (excludes CLAUDE.md/AGENTS.md from the count).
  </behavior>
  <action>
    **Planner decision (recorded in must_haves):** Exclude `CLAUDE.md` and `AGENTS.md` at ANY DEPTH within the workspace. Rationale: these are tool-schema files by convention (init_vault.py emits them as such). Forward-compatible if a domain/package ever introduces its own. Strict spirit of the lint matches "wiki content pages only".

    Edit `packages/wiki-io/src/wiki_io/lint_wiki.py` `scan()`:
    1. Define a module-level `SCHEMA_FILENAMES = {"CLAUDE.md", "AGENTS.md"}` constant near the existing `LINTED_TOPS`.
    2. In the first enumeration loop (line 86, `for md in workspace.rglob("*.md"):`), after the dotdir check and BEFORE the existing `if rel.name in {"log.md"}:` filter, add: `if rel.name in SCHEMA_FILENAMES: continue`. Place it BEFORE `link_targets.add(key)` so schema files are also not treated as wikilink targets (they aren't pages by any definition, so resolving `[[CLAUDE]]` should remain broken).
    3. In the second enumeration loop (line 144, the index-link parser), apply the same exclusion symmetrically: `if rel.name in SCHEMA_FILENAMES: continue` near the existing index/work-archived skips.

    Add the three tests above to `packages/wiki-io/tests/test_lint_wiki.py`. Look at existing tests in that file for the `tmp_path` workspace-building idiom; reuse it. Don't mock — build real files and call `scan()` directly.

    Do NOT touch `update_tokens.py`'s skip set (that's already handled separately by the frontmatter check at line 90: files without `---` are skipped). Do NOT touch the `lint/package_sync.py` enumeration (line 26) unless it surfaces in the lint report — quick scan of the file says it only enumerates pages with category=`package`/`app`, which CLAUDE.md never has, so it's already safe.
  </action>
  <verify>
    <automated>uv run --package wiki-io pytest packages/wiki-io/tests/test_lint_wiki.py -x -v</automated>
  </verify>
  <done>All three new tests pass. Existing tests in test_lint_wiki.py still pass. `grep -n "SCHEMA_FILENAMES" packages/wiki-io/src/wiki_io/lint_wiki.py` shows the constant and two usage sites (both loops).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: F2 — `tokens: null` when CountTokens API is unsupported for the model</name>
  <files>packages/wiki-io/src/wiki_io/update_tokens.py, packages/wiki-io/tests/test_update_tokens.py</files>
  <behavior>
    - Test A (unsupported model branch): mock `boto3.client(...).count_tokens(...)` to raise `botocore.exceptions.ClientError({"Error": {"Code": "ValidationException", "Message": "Model does not support count tokens operation"}}, "CountTokens")`. Call `update_page(path, ...)` against a tmp file that already has frontmatter (with `tokens: 5`). Assert the file is rewritten with `tokens: null` (literal four chars). Assert `update_page` returns `("updated", None)` (or a sentinel — see action). Assert `frontmatter.loads(path.read_text()).metadata["tokens"] is None`.
    - Test B (legit zero): mock count_tokens to return `0`. Call update_page. Assert `tokens: 0` is written and `("updated", 0)` (or unchanged if the page already had `tokens: 0`).
    - Test C (idempotency on null): start with a file containing `tokens: null` from a previous run; mock count_tokens to raise ValidationException again. Assert the file is NOT rewritten (`("unchanged", None)`).
    - Test D (non-validation errors still skip): mock count_tokens to raise a generic `RuntimeError("network down")`. Assert behavior is the existing `("skipped", 0)` — no frontmatter mutation.
  </behavior>
  <action>
    Edit `packages/wiki-io/src/wiki_io/update_tokens.py`:

    Step 1: Import `from botocore.exceptions import ClientError` at the top.

    Step 2: Define a helper `def _is_unsupported_model_error(exc: BaseException) -> bool:` that returns True iff `isinstance(exc, ClientError)` AND `exc.response.get("Error", {}).get("Code") == "ValidationException"` AND `"count tokens" in exc.response.get("Error", {}).get("Message", "").lower()` (matching the literal Bedrock message; fall back to just the code if message check is too brittle — pick the stricter version that still matches the real Bedrock response, which is "Model does not support count tokens operation" today).

    Step 3: Refactor `update_page` (around line 110-113):
    - Replace the broad `except Exception as exc:` with two branches:
      1. `except ClientError as exc:` — if `_is_unsupported_model_error(exc)`, set `count = None` and proceed to the rewrite path. Otherwise log and return `("skipped", 0)` (current behavior).
      2. `except Exception as exc:` — log and return `("skipped", 0)` (current behavior preserved).
    - Update the unchanged-check (line 115) to handle `None`: `if post.metadata.get("tokens") == count:` already works for None vs None equality, but be explicit — when count is None, treat the legitimate "missing tokens key" case as different from "tokens: null"; use `("tokens" in post.metadata and post.metadata["tokens"] is None and count is None)` as the unchanged condition for the null case, otherwise fall through to write.

    Step 4: Rewrite the write path (lines 121-141) to emit `tokens: null` when `count is None`:
    - Where the code currently does `updated_lines.append(f"tokens: {count}")`, branch: if `count is None`, append `"tokens: null"`; else append `f"tokens: {count}"`.
    - Same for the append-at-end case (line 134).

    Step 5: Change the return type signature commentary — the function now returns `tuple[str, int | None]`. Update the return type hint accordingly.

    Step 6: Update the strip-baseline logic (lines 103-105): the filter currently strips `line == "tokens:" or line.startswith("tokens: ")` — this already covers `tokens: null` so no change needed. Sanity-check by reading the surrounding code.

    Step 7: Add the four tests above to `packages/wiki-io/tests/test_update_tokens.py`. Build tmp files with `tmp_path`. Mock `boto3.client` at the `wiki_io.update_tokens.boto3` import site, matching the existing test pattern (lines 17-21). For the ClientError construction, use:
    ```
    ClientError(
        error_response={"Error": {"Code": "ValidationException", "Message": "Model does not support count tokens operation"}},
        operation_name="CountTokens",
    )
    ```

    Do NOT modify `update_vault()` or `main()` — the result dict's `("updated", "unchanged", "skipped")` buckets still work; "tokens-was-null" updates are still "updated".
  </action>
  <verify>
    <automated>uv run --package wiki-io pytest packages/wiki-io/tests/test_update_tokens.py -x -v</automated>
  </verify>
  <done>All four new tests pass. Existing two tests in test_update_tokens.py still pass. A page processed under an unsupported model contains `tokens: null` in its frontmatter, round-trips through `frontmatter.loads` as Python `None`, and is reported as "updated" (or "unchanged" on re-run).</done>
</task>

<task type="auto">
  <name>Task 5: Final verification gate — full test suite across both packages</name>
  <files></files>
  <action>
    Run the full pytest suite across both modified packages to catch any cross-test regressions introduced by the four fixes. No code changes in this task — verification only.

    If a test fails, surface the failure with full output and STOP. Do not retroactively edit tests to make them pass — a regression here means one of tasks 1-4 had an unintended side effect that needs explicit reasoning before fixing.
  </action>
  <verify>
    <automated>uv run pytest packages/wiki-io/ packages/workspace-io/ -x</automated>
  </verify>
  <done>Full test suite for wiki-io and workspace-io passes. Exit code 0. No skipped tests beyond the existing pre-quick baseline.</done>
</task>

</tasks>

<verification>
After all five tasks complete:

1. `uv run pytest packages/wiki-io/ packages/workspace-io/ -x` exits 0.
2. `grep -n "repo-directory" packages/workspace-io/src/workspace_io/config.py` shows the new key handling.
3. `grep -n "SCHEMA_FILENAMES" packages/wiki-io/src/wiki_io/lint_wiki.py` shows the constant.
4. `grep -n "tokens: null\|count is None" packages/wiki-io/src/wiki_io/update_tokens.py` shows the null branch.
5. `grep -n "\[\[api\]\]" packages/wiki-io/src/wiki_io/assets/page-templates/package/overview.md` returns no matches (bare wikilinks gone).

Optional sanity check (not gating): point `GRAPH_WIKI_WORKSPACE=/Users/pat/Personal/graph-wiki/agent-research` and re-run `/graph-wiki:lint`. Expected: W1 findings drop to zero (container_drift + package_sync_drift recompute against the agent-research source repo, not the wiki repo); W5 orphan findings for package sub-pages drop to zero AFTER a fresh `/graph-wiki:scan` regenerates overviews (out of scope here — just verify the template change is in place); F1 findings (`wiki/CLAUDE.md`, `wiki/AGENTS.md`) drop from the lint report; F2 — once update_tokens is re-run, pages on unsupported models show `tokens: null` and the lint's `missing_tokens` count reflects only pages with no `tokens:` field at all.
</verification>

<success_criteria>
- [ ] W1: `resolve()` honors `repo-directory:` from `.graph-wiki.yaml` and `.graph-wiki.local.yaml` (workspace-side) in BOTH the env-var and discovery branches, with `~` expansion and relative-path resolution against the workspace.
- [ ] W5: Package overview template (and domain overview template) renders path-qualified wikilinks with display aliases; no bare `[[api]]`-style links remain in Sub-pages sections; renderer call sites pass `PACKAGE_SLUG` / `DOMAIN_SLUG`.
- [ ] F1: `lint_wiki.scan()` ignores `CLAUDE.md` and `AGENTS.md` at any depth in both enumeration loops (page-scan and index-link-scan).
- [ ] F2: `update_tokens.update_page()` writes `tokens: null` when CountTokens raises a ValidationException matching the unsupported-operation pattern; preserves `tokens: 0` for legitimate zero counts; preserves `("skipped", 0)` for unrelated errors; round-trips correctly through `python-frontmatter`.
- [ ] All four fixes have dedicated tests next to their source files.
- [ ] `uv run pytest packages/wiki-io/ packages/workspace-io/` exits 0.
- [ ] No changes outside the listed `files_modified` set.
</success_criteria>

<output>
Create `.planning/quick/260521-gc0-tackle-four-lint-driven-fixes-w1-repo-di/260521-gc0-SUMMARY.md` when done. Summary should record:
- Confirmation of the F1 "any-depth" exclusion decision (planner default; user may revisit if it ever proves wrong).
- The exact ValidationException matching predicate used in F2 (code-only, code-plus-message, or other) and why.
- Any audit findings from W5 step 1 (which call sites emit `package/overview.md` and `domain/overview.md`) — useful breadcrumb for future template work.
- Whether non-default container paths (custom `wiki/CLAUDE.md` layout block) need a follow-up todo for W5, or whether current emission code already handles them.
</output>
