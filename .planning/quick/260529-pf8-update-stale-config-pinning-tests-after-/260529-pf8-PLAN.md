---
phase: quick-260529-pf8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/eval-harness/tests/test_pricing.py
  - packages/eval-harness/tests/test_models_toml_sweep_candidates.py
  - packages/model-adapter/tests/test_loader.py
  - packages/model-adapter/tests/test_narrator_role.py
  - agents/graph-wiki-agent/tests/integration/test_bedrock_iam.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "Full workspace suite is green: `uv run pytest -q` reports 0 failed (skips/xfails allowed)."
    - "All 14 previously-failing assertions now match the na9 ground truth (global Haiku ARN, qwen3 0.15/0.60 pricing, bespoke-per-role sweep candidates)."
    - "No production/source files changed — only the five named test files."
    - "The two sweep-candidate tests assert structural invariants (non-empty lists, Haiku-in-every-list, all candidates priced, judges have no sweep_candidates) rather than the retired D-03 tier-locked lists."
  artifacts:
    - path: "packages/eval-harness/tests/test_pricing.py"
      provides: "qwen3 cost assertion updated to $0.75"
    - path: "packages/eval-harness/tests/test_models_toml_sweep_candidates.py"
      provides: "structural-invariant sweep-candidate tests (D-03 tier lists retired)"
    - path: "packages/model-adapter/tests/test_loader.py"
      provides: "HAIKU_ARN constant updated to global. id"
    - path: "packages/model-adapter/tests/test_narrator_role.py"
      provides: "HAIKU_ARN constant updated to global. id"
    - path: "agents/graph-wiki-agent/tests/integration/test_bedrock_iam.py"
      provides: "HAIKU_ARN constant updated to global. id"
  key_links:
    - from: "test files"
      to: "packages/model-adapter/src/model_adapter/models.toml"
      via: "role defaults read via load_role_config / make_llm"
      pattern: "global\\.anthropic\\.claude-haiku-4-5-20251001-v1:0"
    - from: "test_pricing.py::test_qwen3_cost"
      to: "packages/eval-harness/src/eval_harness/pricing.py"
      via: "cost_for_usage('qwen.qwen3-32b-v1:0', ...)"
      pattern: "0\\.75|0\\.15.*0\\.60"
---

<objective>
Make the workspace test suite green by updating ONLY the 14 stale assertions that pin the OLD config superseded by the 260529-na9 sweep-config refresh. No production-code changes.

Purpose: na9 intentionally moved the Haiku default everywhere from `us.anthropic.claude-haiku-4-5-20251001-v1:0` to `global.anthropic.claude-haiku-4-5-20251001-v1:0`, reconciled qwen3-32b pricing to 0.15/0.60 per 1M, and replaced the six uniform length-4 D-03 tier-locked `sweep_candidates` arrays with bespoke per-role lists. Several tests still pin the old values and fail. These are stale assertions, not regressions (verified: all 14 failures trace purely to the na9 changes; live Bedrock test stays skipped).

Output: Five updated test files; full suite reports 0 failed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

# New ground truth (DO NOT EDIT — read-only reference):
@packages/model-adapter/src/model_adapter/models.toml
@packages/eval-harness/src/eval_harness/pricing.py

<interfaces>
<!-- Verified ground truth from the source files (read-only). Executor uses these directly. -->

New Haiku default ARN (replaces every `us.anthropic.claude-haiku-4-5-20251001-v1:0` *default*):
  global.anthropic.claude-haiku-4-5-20251001-v1:0

Roles whose default model_id is the global Haiku ARN (per models.toml):
  preflight, librarian, code_reader, scanner, narrator, domain-proposer
Roles with a NON-Haiku default (do NOT change to Haiku):
  linter   -> us.amazon.nova-lite-v1:0   (unchanged; NOVA_LITE_ARN stays correct)
  ingestor -> qwen.qwen3-32b-v1:0
  synthesizer -> qwen.qwen3-32b-v1:0      (QWEN_SYNTHESIZER_ARN stays correct)
  judge_a  -> mistral.mistral-large-3-675b-instruct
  judge_b  -> us.amazon.nova-pro-v1:0

qwen3-32b pricing in pricing.py: input 0.15, output 0.60 per 1M
  -> 1M input + 1M output = 0.15 + 0.60 = 0.75

Six in-scope roles (have sweep_candidates): librarian, synthesizer, code_reader, scanner, linter, ingestor
  Lengths are now bespoke (6/8/6/7/6/6) — NOT uniform 4.
  global.anthropic.claude-haiku-4-5-20251001-v1:0 appears in EVERY in-scope role's sweep_candidates list.
  judge_a / judge_b have NO sweep_candidates key.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update mechanical pins — qwen3 price + Haiku ARN constants</name>
  <files>packages/eval-harness/tests/test_pricing.py, packages/model-adapter/tests/test_loader.py, packages/model-adapter/tests/test_narrator_role.py, agents/graph-wiki-agent/tests/integration/test_bedrock_iam.py</files>
  <action>
Apply four surgical edits (test-only; match existing style; touch nothing else):

1. `packages/eval-harness/tests/test_pricing.py` — `test_qwen3_cost` (lines 30-36): change the assertion target from `pytest.approx(2.0)` to `pytest.approx(0.75)`, and update the docstring "Qwen3-32B: $0.40/M input + $1.60/M output -> $2.00 for 1M each." to reflect the reconciled price "$0.15/M input + $0.60/M output -> $0.75 for 1M each." Leave the other pricing tests untouched (they already pass).

2. `packages/model-adapter/tests/test_loader.py` — line 12: change the `HAIKU_ARN` constant value from `"us.anthropic.claude-haiku-4-5-20251001-v1:0"` to `"global.anthropic.claude-haiku-4-5-20251001-v1:0"`. This single change fixes all 7 loader failures (preflight ARN, access-denied-wrap ARN, domain_proposer, librarian values, and the three workspace-fallback tests whose packaged-default expectation is the global Haiku ARN). Do NOT touch `QWEN_SYNTHESIZER_ARN` (line 16) or `NOVA_LITE_ARN` (line 103) — both verified still correct.

3. `packages/model-adapter/tests/test_narrator_role.py` — line 7: change the `HAIKU_ARN` constant value to `"global.anthropic.claude-haiku-4-5-20251001-v1:0"` (narrator default moved to global Haiku in na9). Fixes all 3 narrator failures.

4. `agents/graph-wiki-agent/tests/integration/test_bedrock_iam.py` — line 28: change the `HAIKU_ARN` constant value to `"global.anthropic.claude-haiku-4-5-20251001-v1:0"` (preflight role now uses the global id). The mock test is offline (monkeypatches `_original_invoke`); the live `test_make_llm_preflight_invoke` stays integration-gated/skipped — no Bedrock spend.

If any of these targeted tests asserts something beyond the qwen3 price or the Haiku id that changed (i.e., a real behavior regression rather than a stale pin), STOP and flag it instead of editing — do not paper over.
  </action>
  <verify>
    <automated>uv run pytest -q packages/eval-harness/tests/test_pricing.py packages/model-adapter/tests/test_loader.py packages/model-adapter/tests/test_narrator_role.py agents/graph-wiki-agent/tests/integration/test_bedrock_iam.py 2>&1 | tail -5</automated>
  </verify>
  <done>The four files' targeted tests pass (12 of the 14 failures resolved: 1 pricing + 7 loader + 3 narrator + 1 bedrock_iam). No source files changed.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite sweep-candidate tests to structural invariants (retire D-03 tier model)</name>
  <files>packages/eval-harness/tests/test_models_toml_sweep_candidates.py</files>
  <action>
Rewrite the two failing tests in this file to assert structural invariants that match the new bespoke-per-role design. This is the agreed direction — the D-03 "locked tier-to-role" model is RETIRED; do NOT re-pin the exact new lists.

`test_sweep_candidates_present_for_all_six_roles` (lines 54-63): replace the `len(candidates) == 4` assertion with `len(candidates) >= 1` (non-empty). Keep the present/is-a-list checks. Lengths are now bespoke per role (6/8/6/7/6/6), so a fixed-length assertion is wrong.

`test_tier_to_role_candidate_map` (lines 66-111): REPLACE entirely. The new test (rename to something like `test_haiku_present_in_every_in_scope_role` or keep a single combined structural test) must assert:
  - For each of the six IN_SCOPE_ROLES, `"global.anthropic.claude-haiku-4-5-20251001-v1:0"` is present in that role's `sweep_candidates` (Pat's "always include Haiku" rule). The new toml confirms this holds for all six (linter included).
Remove the now-obsolete `QUALITY_ROLES`/`MID_ROLES`/`CHEAP_FAST_ROLES` tuples and `QUALITY_CANDIDATES`/`MID_CANDIDATES`/`CHEAP_FAST_CANDIDATES` frozensets (lines 24-47), and the sonnet-4-6 / nova-pro / nova-micro spot-checks (lines 95-111).

Update the module docstring (lines 1-10): it currently says "length 4 (D-05)" and "match the D-03 tier-to-role mapping exactly". Rewrite to describe the new invariants: non-empty per-role sweep_candidates, Haiku present in every in-scope role, all candidates priced, judges have no sweep_candidates.

Do NOT modify the tests that already pass in this file and should stay:
  - `test_no_sweep_candidates_for_judges` (lines 114-121) — keep; verifies judge_a/judge_b have no sweep_candidates.
  - `test_all_candidates_have_pricing` (lines 124-135) — keep; this is the "every candidate priced via cost_for_usage, no UnknownModelError" check across the new heterogeneous lists.
  - `test_make_llm_still_works_for_all_roles` (lines 138-149) — keep.
  - `test_code_reader_cases_json_loads` (lines 152-182) — keep the existing 5-6 cases / tag / baseline-case_id checks as-is (still valid).

Note `IN_SCOPE_ROLES` (line 22) stays as-is. Match existing style and assertion-message format.
  </action>
  <verify>
    <automated>uv run pytest -q packages/eval-harness/tests/test_models_toml_sweep_candidates.py 2>&1 | tail -5</automated>
  </verify>
  <done>All tests in test_models_toml_sweep_candidates.py pass; obsolete D-03 tier frozensets/spot-checks removed; module docstring updated; retained tests (judges, pricing, make_llm, code_reader_cases) untouched and still passing.</done>
</task>

</tasks>

<verification>
After both tasks, run the FULL workspace suite to confirm zero failures (this is the contract for this quick task):

```
uv run pytest -q 2>&1 | tail -15
```

Expected: `0 failed` (skips/xfails are fine — e.g. the GRAPH_WIKI_RUN_INTEGRATION-gated live Bedrock test stays skipped). Prior baseline was 14 failed / 1566 passed; target is 0 failed / 1580 passed (or current passing count + 14), with no new failures introduced.

If the full suite surfaces a failure NOT in the original 14 list, investigate whether the edits caused it (unlikely — edits are test-only and scoped). If a remaining/new failure turns out to be a real production bug rather than a stale assertion, STOP and flag it — do not edit the test to mask it.
</verification>

<success_criteria>
- `uv run pytest -q` reports 0 failed.
- Only the five named test files changed; zero production/source files touched (`git diff --name-only` shows only `*/tests/*` paths).
- qwen3 cost assertion = 0.75; all `HAIKU_ARN` constants = `global.anthropic.claude-haiku-4-5-20251001-v1:0`.
- Sweep-candidate tests assert structural invariants, not the retired D-03 tier lists.
- No live Bedrock calls made (no spend).
</success_criteria>

<output>
Create `.planning/quick/260529-pf8-update-stale-config-pinning-tests-after-/260529-pf8-SUMMARY.md` when done.
</output>
