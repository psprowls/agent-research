---
phase: quick-260529-q8r
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/eval-harness/src/eval_harness/sweep.py
  - packages/eval-harness/tests/test_sweep_full_matrix.py
  - packages/eval-harness/tests/eval/test_sweep_eval.py
autonomous: true
requirements: [SWEEP-01]

must_haves:
  truths:
    - "run_full_matrix builds a real DivergenceMetric per divergence-eligible role and passes it to score_two_gate (no longer hardcoded None)"
    - "run_full_matrix passes the package baselines dir to score_two_gate (no longer hardcoded None)"
    - "An offline unit test proves score_two_gate receives a DivergenceMetric instance and a real Path baselines_dir for an in-scope role"
    - "two_gate.py, metric.py, and the divergence package are unchanged"
  artifacts:
    - path: "packages/eval-harness/src/eval_harness/sweep.py"
      provides: "Per-role divergence metric + baselines_dir wiring into the score_two_gate call site"
      contains: "DivergenceMetric"
    - path: "packages/eval-harness/tests/test_sweep_full_matrix.py"
      provides: "Offline capture test asserting non-None divergence_metric_or_none + Path baselines_dir"
      contains: "divergence_metric_or_none"
  key_links:
    - from: "packages/eval-harness/src/eval_harness/sweep.py"
      to: "eval_harness.divergence.metric.DivergenceMetric"
      via: "per-role construction in run_full_matrix second loop"
      pattern: "DivergenceMetric\\(role=role"
    - from: "packages/eval-harness/src/eval_harness/sweep.py"
      to: "score_two_gate"
      via: "divergence_metric_or_none + baselines_dir kwargs"
      pattern: "divergence_metric_or_none=divergence_metric"
---

<objective>
Fix C: wire the per-role divergence metric and baselines directory into `run_full_matrix` so Gate 1 actually executes.

Today the `score_two_gate(...)` call in `run_full_matrix` passes `divergence_metric_or_none=None` and `baselines_dir=None` hardcoded (sweep.py:873,875). Because all 6 in-scope roles are in `ROLES_WITH_DIVERGENCE`, `two_gate.py` then forces `gate1_passed=False` for every candidate — every candidate auto-FAILs Gate 1. `score_two_gate` / `two_gate.py` are CORRECT; the bug is purely the two hardcoded `None`s in the caller.

Purpose: make the cost-frontier sweep's Gate 1 meaningful instead of a guaranteed fail.
Output: a real `DivergenceMetric` + package `baselines/` dir flow into `score_two_gate` per divergence-eligible role; an offline unit test plus a one-block live-test assertion lock the wiring in place.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@.planning/todos/deferred/2026-05-29-sweep-gate1-divergence-never-wired-in-run-full-matrix.md

<interfaces>
<!-- Verified facts from the codebase. Executor uses these directly — no exploration needed. -->

ALREADY imported at module level in sweep.py:
- line 43: `from workspace_io.paths import wiki_dir`
- line 49: `from eval_harness.two_gate import ROLES_WITH_DIVERGENCE, TwoGateOutcome, score_two_gate`
- `Path` is already imported (used throughout, e.g. the run_full_matrix default `output_dir`).

Canonical DivergenceMetric construction (from test_two_gate_scorer.py:37):
```
metric = DivergenceMetric(
    role=role,
    checks=ROLE_CHECKS[role],
    rubric_path=ROLE_RUBRICS[role],
    wiki=wiki_dir(workspace_path),
)
```
Needed extra imports: `from eval_harness.divergence import ROLE_CHECKS, ROLE_RUBRICS`
and `from eval_harness.divergence.metric import DivergenceMetric`.

Package baselines dir (from sweep.py path):
`Path(__file__).resolve().parents[2] / "baselines"`
sweep.py is at packages/eval-harness/src/eval_harness/sweep.py → parents[2] = packages/eval-harness.
On disk (VERIFIED): packages/eval-harness/baselines/ contains
divergence-{librarian,scanner,ingestor,linter}.json.
MISSING (acceptable, out of scope): divergence-synthesizer.json, divergence-code_reader.json.
`metric.load_baseline()` returns {} when the file is absent (0-failure floor) — no crash.

The call site to edit (sweep.py:864-880), inside the SECOND
`for role, candidates in role_candidates.items():` loop (starts line 836):
```
two_gate_outcomes: dict[str, TwoGateOutcome] = {}
for candidate in candidates:
    outputs_by_case: list[tuple[str, object]] = [...]
    outcome = score_two_gate(
        role=role,
        divergence_metric_or_none=None,   # <-- hardcoded None, FIX
        agent_outputs_by_case=outputs_by_case,
        baselines_dir=None,               # <-- hardcoded None, FIX
        panel_mean=panel_means.get(candidate),
        default_panel_mean=default_panel_mean,
        threshold=threshold,
    )
```

run_full_matrix signature (sweep.py:717): async; keyword args include
`dry_run: bool = False`, `skip_bed01`, `auto_confirm`. When `dry_run=True`
the inner sweep loop `continue`s (sweep.py:815) so `role_results` is empty,
BUT the second loop still runs and `score_two_gate` is STILL called once per
candidate — making dry_run the clean offline driver for the capture test.

ROLES_WITH_DIVERGENCE (two_gate.py:35) =
{librarian, ingestor, linter, scanner, code_reader, synthesizer} — all in scope.

Existing live test capture pattern (test_sweep_eval.py:271-286): wraps
`sweep_mod.score_two_gate` with a kwargs-capturing wrapper appending to
`captured_calls`. Its per-call assertion loop (line 339-341) only checks
{role, panel_mean, default_panel_mean, threshold}.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire per-role divergence_metric + baselines_dir into score_two_gate</name>
  <files>packages/eval-harness/src/eval_harness/sweep.py</files>
  <behavior>
    - For a role in ROLES_WITH_DIVERGENCE, score_two_gate receives a DivergenceMetric instance (not None) and a Path baselines_dir (not None).
    - For a hypothetical role NOT in ROLES_WITH_DIVERGENCE, both stay None (preserves the D-08 contract even though no in-scope role hits this branch).
    - two_gate.py / metric.py / divergence package source are byte-for-byte unchanged.
  </behavior>
  <action>
    Edit `run_full_matrix` ONLY. Add the two imports following the file's import style: put `from eval_harness.divergence import ROLE_CHECKS, ROLE_RUBRICS` and `from eval_harness.divergence.metric import DivergenceMetric` at module top alongside the other `eval_harness.*` imports (lines 45-49). Add a module-level constant near the top of the module (after the existing imports / before the first dataclass) named `_BASELINES_DIR` set to `Path(__file__).resolve().parents[2] / "baselines"` (Path already imported; do NOT re-import).

    Inside the SECOND `for role, candidates in role_candidates.items():` loop (begins at line 836), AFTER `threshold = (...)` (line 860-862) and BEFORE the `two_gate_outcomes: dict[...] = {}` line (864), compute per-role values once: if `role in ROLES_WITH_DIVERGENCE`, set `divergence_metric = DivergenceMetric(role=role, checks=ROLE_CHECKS[role], rubric_path=ROLE_RUBRICS[role], wiki=wiki_dir(workspace_path))` and `baselines_dir_for_role = _BASELINES_DIR`; else set both to `None`. Reuse the module-level `wiki_dir` (line 43) — do NOT re-import it.

    In the `score_two_gate(...)` call (871-879) replace `divergence_metric_or_none=None` with `divergence_metric_or_none=divergence_metric` and `baselines_dir=None` with `baselines_dir=baselines_dir_for_role`. Change nothing else: keep the AgentOutputProxy `outputs_by_case` builder as-is (no divergence check reads `output.page_type`), keep `render_role_doc(... divergence_results=None ...)` as-is.

    Add one comment near the per-role construction noting synthesizer + code_reader have no recorded baseline yet, so `load_baseline()` returns an empty 0-failure floor; recording them via `--accept-divergence-baseline` is an explicit non-goal of this fix. Do NOT fabricate baseline JSON files.
  </action>
  <verify>
    <automated>uv run --package eval-harness pytest packages/eval-harness -q -k "not eval" 2>&1 | tail -5; git diff --quiet packages/eval-harness/src/eval_harness/two_gate.py packages/eval-harness/src/eval_harness/divergence/metric.py && echo "PROTECTED-FILES-UNCHANGED" || echo "ERROR: protected file modified"</automated>
  </verify>
  <done>sweep.py constructs a per-role DivergenceMetric + `_BASELINES_DIR` and passes them into score_two_gate; the two hardcoded `None`s are gone; offline eval-harness suite passes; two_gate.py and metric.py show no diff.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Offline capture test + live-test assertion extension</name>
  <files>packages/eval-harness/tests/test_sweep_full_matrix.py, packages/eval-harness/tests/eval/test_sweep_eval.py</files>
  <behavior>
    - Offline (no Bedrock, NOT gated on GRAPH_WIKI_RUN_EVAL): the captured score_two_gate call for an in-scope role ("librarian") has `divergence_metric_or_none` that is a DivergenceMetric instance, and `baselines_dir` that is an existing Path.
    - Live test: for every captured call whose role is in ROLES_WITH_DIVERGENCE, `divergence_metric_or_none is not None` and `baselines_dir is not None`.
  </behavior>
  <action>
    Create `packages/eval-harness/tests/test_sweep_full_matrix.py` (plain async test, NO `@pytest.mark.eval`, no GRAPH_WIKI_RUN_EVAL guard). Use the `fixture_workspace_path` fixture (conftest.py:86) for `workspace_path`. Drive `run_full_matrix` with `dry_run=True` (the inner sweep loop `continue`s on dry_run but score_two_gate is STILL called once per candidate — verified; this avoids mocking run_role_sweep and any live render-with-data path).

    Test steps:
    1. `import eval_harness.sweep as sweep_mod` and `from eval_harness.divergence.metric import DivergenceMetric`.
    2. Capture: `from eval_harness.two_gate import TwoGateOutcome, score_two_gate as original`; define a wrapper `def score_two_gate_wrapper(**kwargs): captured.append(dict(kwargs)); return TwoGateOutcome(qualified=False, gate1_passed=None, gate2_passed=None, divergence_failures=None, panel_mean=None, threshold_used=0.95, notes="stub")` (do NOT call the real scorer — avoids running checks). `monkeypatch.setattr(sweep_mod, "score_two_gate", score_two_gate_wrapper)`.
    3. Build tmp inputs under `tmp_path`: a `query_cases.json` with one valid case, a `code_reader_cases.json` with one valid case, and an `ingestor_source.md` file (any markdown). Match the case schema used by `_load_and_validate_cases` (mirror the shape in test_sweep_eval.py's CASES_PATH / code_reader_cases.json — keys like `query`, `expected_answer`, `case_id`; if a case loader field is unclear, read `_load_and_validate_cases` in sweep.py and match it exactly).
    4. `await run_full_matrix(role_candidates={"librarian": ["model-x"]}, workspace_path=fixture_workspace_path, query_cases_path=..., code_reader_cases_path=..., ingestor_source_path=..., repeats=1, output_dir=tmp_path/"out", dry_run=True, skip_bed01=True, auto_confirm=True)`.
    5. Assert `captured`, find the librarian call, assert `isinstance(call["divergence_metric_or_none"], DivergenceMetric)` and `isinstance(call["baselines_dir"], Path)` and `call["baselines_dir"].is_dir()`.

    Then extend the LIVE test `packages/eval-harness/tests/eval/test_sweep_eval.py::test_full_matrix_live`. In the existing `for call in captured_calls:` loop (line 339-341), after the existing kwarg-presence asserts, add: `if call["role"] in ROLES_WITH_DIVERGENCE:` then assert `call["divergence_metric_or_none"] is not None` and `call["baselines_dir"] is not None`. Import `ROLES_WITH_DIVERGENCE` from `eval_harness.two_gate` if not already imported in that file (check existing imports first; reuse if present).
  </action>
  <verify>
    <automated>uv run --package eval-harness pytest packages/eval-harness/tests/test_sweep_full_matrix.py -q 2>&1 | tail -8</automated>
  </verify>
  <done>New offline test passes without GRAPH_WIKI_RUN_EVAL and proves the librarian call gets a DivergenceMetric + existing-dir Path; the live test gained a divergence-role assertion block; full offline eval-harness suite (`-k "not eval"`) stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → metric loader | `_BASELINES_DIR` and `wiki_dir(workspace_path)` are read by DivergenceMetric; both are test/dev-machine-local Paths, no user-supplied input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-q8r-01 | Tampering | `_BASELINES_DIR` path construction | accept | Constant derived from `__file__`, not external input; same pattern already used in test_two_gate_scorer.py:33 |
| T-q8r-02 | Denial of Service | missing synthesizer/code_reader baseline | accept | `load_baseline()` returns {} (0-failure floor), no crash; recording baselines is an explicit non-goal |
| T-q8r-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies installed; uses only existing in-repo modules |
</threat_model>

<verification>
- `uv run --package eval-harness pytest packages/eval-harness -q -k "not eval"` passes (offline gate).
- `git diff` shows zero changes to two_gate.py, metric.py, and any file under `packages/eval-harness/src/eval_harness/divergence/`.
- The two hardcoded `None`s at the former sweep.py:873,875 are replaced by the per-role variables.
</verification>

<success_criteria>
- run_full_matrix passes a real DivergenceMetric + package baselines Path into score_two_gate for divergence-eligible roles.
- Offline capture test (no Bedrock, no eval gate) proves the wiring with `isinstance` + `is_dir()` assertions.
- Live test gained a divergence-role kwarg assertion.
- No protected source files modified.
- Offline eval-harness suite green.
</success_criteria>

<output>
Create `.planning/quick/260529-q8r-fix-c-sweep-gate-1-divergence-wiring/260529-q8r-SUMMARY.md` when done.
</output>
