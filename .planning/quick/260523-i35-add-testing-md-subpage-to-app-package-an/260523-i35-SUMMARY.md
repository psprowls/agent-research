---
phase: quick-260523-i35
plan: 01
subsystem: vault-io
tags: [templates, scanner, file-map, testing, tdd]
dependency_graph:
  requires: [260523-he3]
  provides: [testing-subpage-templates, build_file_maps, file_map_testing]
  affects: [scan_monorepo, update_index, page-formats, scanner, scan-workflow]
tech_stack:
  added: []
  patterns: [prod-test-file-map-split, _is_test_path-classifier]
key_files:
  created:
    - packages/vault-io/src/vault_io/assets/page-templates/package/testing.md
    - packages/vault-io/src/vault_io/assets/page-templates/app/overview.md
    - packages/vault-io/src/vault_io/assets/page-templates/app/testing.md
    - packages/vault-io/src/vault_io/assets/page-templates/plugin/overview.md
    - packages/vault-io/src/vault_io/assets/page-templates/plugin/testing.md
  modified:
    - packages/vault-io/src/vault_io/assets/page-templates/package/overview.md
    - packages/vault-io/src/vault_io/scan_monorepo.py
    - packages/vault-io/src/vault_io/update_index.py
    - packages/vault-io/tests/test_scan_monorepo.py
    - packages/vault-io/tests/test_overview_template_wikilinks.py
    - plugins/graph-wiki/skills/graph-wiki/references/page-formats.md
    - plugins/graph-wiki/skills/graph-wiki/references/scan-workflow.md
    - plugins/graph-wiki/agents/scanner.md
  deleted:
    - packages/vault-io/src/vault_io/assets/page-templates/app.md
decisions:
  - "Split rule lives in _is_test_path() in scan_monorepo.py — single source of truth"
  - "build_file_map() legacy API now returns prod-only (deliberate breaking change; documented)"
  - "Testing sub-page section in page-formats.md uses unnumbered heading (avoids renumbering existing sections)"
  - "app/overview.md Sub-pages block lists only testing (no other app sections promoted to sub-pages per plan scope)"
  - "Truncation marker appears on BOTH blocks when global truncation fires (simpler than per-block marker)"
metrics:
  duration: "~40 minutes"
  completed: "2026-05-23"
  tasks_completed: 3
  files_changed: 13
---

# Quick Task 260523-i35: Add testing.md sub-page to app, package, and plugin templates

Split prod and test file maps onto separate wiki pages: `overview.md` now carries only prod source + prod config; new `testing.md` sub-page owns test files, test config, and fixtures.

## What Shipped

### Task 1: Template restructure (commit 8ddc4b0)

- `package/overview.md` Sub-pages list gains a fifth entry: `testing` (between `work` and `context`)
- New `package/testing.md` template: Purpose, How to run, File map, Test conventions, Fixtures, Coverage, Open questions
- Flat `app.md` restructured into `app/overview.md` + `app/testing.md` (directory layout matches package/)
- New `plugin/overview.md` + `plugin/testing.md` templates (category: plugin; same 5-item Sub-pages list)
- `update_index.py` SUBPAGE_STEMS gains `"testing"` so testing.md is not listed as a main nav entry
- `test_overview_template_wikilinks.py` extended: new assertions for `[[wiki/packages/myslug/testing|testing]]`

### Task 2: build_file_maps() emitter with prod/test split (TDD, commits b215b27 + a8490c6)

**RED commit** (b215b27): 21 new failing tests (TestIsTestPath, TestBuildFileMaps, TestBuildFileMap regression)

**GREEN commit** (a8490c6):
- `_TEST_DIR_NAMES`, `_TEST_CONFIG_NAMES`, `_TEST_CONFIG_RE` constants
- `_is_test_path(rel)` — single source of truth for prod vs test classification
- `_emit_file_map_block()` — shared internal emitter (lifted from old build_file_map body)
- `build_file_maps()` — paired API returning `(prod_block, test_block)`
- `build_file_map()` — 3-line wrapper delegating to `build_file_maps()[0]` (prod-only, documented breaking change)
- `scan_monorepo main()` updated: calls `build_file_maps()`, sets `w["file_map"]` and `w["file_map_testing"]`
- All 28 scan_monorepo tests pass; full vault-io suite 142 passed

### Task 3: Reference doc updates (commit d09af27)

- `page-formats.md`: two new bullets in File map convention (prod/testing split rule + fixtures-at-non-test-paths)
- `page-formats.md`: new "Testing sub-page" section with frontmatter spec, sections list, and worked example
- `scanner.md §3`: updated to describe two-block emission (`file_map` → overview, `file_map_testing` → testing.md)
- `scanner.md §3 line 48`: added `testing.md` to the sub-page template enumeration
- `scan-workflow.md`: updated File map bullet to describe two-block write; added `testing.md` to Package sub-pages list

## Deviations from Plan

None — plan executed exactly as written.

## Known Stubs

- `app/overview.md` uses `{{APP_SLUG}}` and `{{CONTAINER_DIR}}` variables in the Sub-pages block and the File map prose paragraph; these variables are placeholders that the scanner agent substitutes at render time. No wikilink-test coverage for app templates (plan did not request this — only package/overview wikilinks are tested).
- All testing.md templates have placeholder File map blocks (single synthetic H3 with `— TODO` row) — the scanner overwrites these on first scan per the unfilled-template detection rule.

## Self-Check

### Created files exist:
- packages/vault-io/src/vault_io/assets/page-templates/package/testing.md — FOUND
- packages/vault-io/src/vault_io/assets/page-templates/app/overview.md — FOUND
- packages/vault-io/src/vault_io/assets/page-templates/app/testing.md — FOUND
- packages/vault-io/src/vault_io/assets/page-templates/plugin/overview.md — FOUND
- packages/vault-io/src/vault_io/assets/page-templates/plugin/testing.md — FOUND
- packages/vault-io/src/vault_io/assets/page-templates/app.md — CONFIRMED GONE

### Commits exist:
- 8ddc4b0 — Task 1 (template restructure + SUBPAGE_STEMS + wikilink test)
- b215b27 — Task 2 RED (failing tests)
- a8490c6 — Task 2 GREEN (implementation)
- d09af27 — Task 3 (reference docs)

### Test suite: 142 passed, 1 skipped (integration, expected)

## Self-Check: PASSED
