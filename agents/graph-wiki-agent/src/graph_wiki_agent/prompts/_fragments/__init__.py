"""Private subpackage: shared prompt fragment string constants.

Each module here exports one SCREAMING_SNAKE_CASE constant and carries a
single-line provenance header (Option A per Phase 26 D-05) linking it to the
canonical source under `plugins/graph-wiki/` (or `packages/workspace-io/...`
for the `CLAUDE.md.template`-sourced fragments).  These are pure string-constant
modules — no imports from other Python packages.
"""
