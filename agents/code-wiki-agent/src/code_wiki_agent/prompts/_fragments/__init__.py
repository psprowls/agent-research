"""Private subpackage: shared prompt fragment string constants.

Each module here exports one SCREAMING_SNAKE_CASE constant and carries a
3-line provenance header linking it to the canonical source in
`packages/prompt-sources/`.  These are pure string-constant modules — no imports
from other Python packages, and definitely not from `packages/prompt-sources/`
(which is documentation, not importable Python).
"""
