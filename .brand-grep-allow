# .brand-grep-allow — allowlist consumed by scripts/check-brand.sh (BRAND-04 grep gate)
#
# Format (per R-04): one path or path-fragment per line. Lines starting with `#` are
# comments and are ignored by `grep -vF -f` (no line in `grep -rEl` output starts with
# `#`, so comment lines never accidentally allowlist a real hit).
#
# Matching semantics: `grep -vF -f .brand-grep-allow` excludes any line whose text
# contains one of these fragments as a literal substring. Use a path fragment
# specific enough to avoid over-allowing (e.g., `packages/eval-harness/baselines/`
# rather than just `baselines/`).
#
# Every entry below carries a rationale comment naming its R-decision
# (R-01 / R-02 / R-03 / R-04) or its carry-forward class (Provenance, Parity-behavior,
# Upstream-reference, Test-data, Upstream-guard, Plan-meta, Historic-decision-log,
# Test-fixture).

# ---------------------------------------------------------------------------
# R-01: round-trip-vault test fixtures — real upstream lattice vault preserved
# verbatim so vault-io can prove byte-identical round-trip parsing.
# ---------------------------------------------------------------------------
packages/vault-io/tests/fixtures/round-trip-vault/

# ---------------------------------------------------------------------------
# R-02: eval-harness baselines + divergence rubrics — recorded measurements
# against the upstream lattice-wiki system; renaming would lie about what
# was measured.
# ---------------------------------------------------------------------------
packages/eval-harness/baselines/
packages/eval-harness/src/eval_harness/divergence/rubrics/

# R-02 (extension per plan 04 §Step 2): baseline.py and pricing.py contain
# `lattice-evals` / `lattice-wiki` literals that name the recorded provenance
# of ported eval-runner code; confirmed present 2026-05-18.
packages/eval-harness/src/eval_harness/baseline.py
packages/eval-harness/src/eval_harness/pricing.py

# ---------------------------------------------------------------------------
# R-03: archived / historical planning documents — provenance value preserved
# (RETROSPECTIVE, MILESTONES, milestones/v1.0-*, milestones/v1.1-*, spike
# READMEs, sweep STORY, research, thread plans).
# ---------------------------------------------------------------------------
.planning/RETROSPECTIVE.md
.planning/MILESTONES.md
.planning/milestones/v1.0-
.planning/milestones/v1.1-
.planning/spikes/001-subagent-context-audit/README.md
.planning/spikes/002-lattice-drift-inventory/README.md
.planning/spikes/WRAP-UP-SUMMARY.md
.planning/sweep/STORY.md
.planning/research/
.planning/threads/next-milestone-planning.md

# ---------------------------------------------------------------------------
# R-04 (self-allowlist per Claude's Discretion): this file itself contains
# 'lattice' literals as pattern documentation, and the gate script contains
# 'lattice' as its grep pattern.
# ---------------------------------------------------------------------------
.brand-grep-allow
scripts/check-brand.sh
# scripts/drift-diff.sh references upstream lattice-wiki-core paths used by the
# Phase 12 plan-01 raw diff dump; the script will outlive the phase for re-syncs.
scripts/drift-diff.sh

# ---------------------------------------------------------------------------
# Phase 12 drift artifacts — DRIFT-DECISIONS{,-RAW}.md embed upstream content
# (filenames, function names, diff hunks) with lattice symbols intact.
# ---------------------------------------------------------------------------
packages/vault-io/DRIFT-DECISIONS-RAW.md
packages/vault-io/DRIFT-DECISIONS.md

# ---------------------------------------------------------------------------
# Phase 12 own plans + context — the phase exists to discuss the rebrand from
# upstream `lattice` → `graph-wiki`, so the phase docs reference both names.
# ---------------------------------------------------------------------------
.planning/phases/12-drift-backport-ecosystem-rebrand-m2/

# ---------------------------------------------------------------------------
# Phase 11 own plans + context — Phase 11 documents the workspace-io port from
# upstream lattice-workspace; references the upstream name throughout.
# ---------------------------------------------------------------------------
.planning/phases/11-workspace-io-port-m1/

# ---------------------------------------------------------------------------
# Phase 13 own plans + context + per-command port specs — Phase 13 authors
# the plugin contract surface that ports upstream `/lattice-wiki:*` commands
# to `/graph-wiki:*`; every spec file names both sides of the rename.
# ---------------------------------------------------------------------------
.planning/phases/13-plugin-spec-m3a/
.planning/spec/13-plugin-contract/

# ---------------------------------------------------------------------------
# Spike scaffolding — MANIFEST + CONVENTIONS describe the spike index, with
# the lattice-drift-inventory spike named explicitly (R-03 spirit).
# ---------------------------------------------------------------------------
.planning/spikes/MANIFEST.md

# ---------------------------------------------------------------------------
# Phase 12 plan-03 carry-forward refs (from
# .planning/phases/12-drift-backport-ecosystem-rebrand-m2/12-03-carry-forward-refs.md)
# Plan 03 enumerated 52 surviving lattice references across 10 classes; the
# entries below encode each as a path-fragment allowlist with the one-line
# rationale class.
# ---------------------------------------------------------------------------

# Plan-meta + Upstream-reference (live planning surface)
# rationale: Upstream-reference — CLAUDE.md intro/core-value/format-compat names upstream lattice-wiki as the system being ported (lines 6, 8, 10, 17).
CLAUDE.md
# rationale: Upstream-reference + Historic-decision-log — PROJECT.md current/historic sections reference upstream packages, milestones, and out-of-scope items.
.planning/PROJECT.md
# rationale: Plan-meta — REQUIREMENTS.md describes the rename from upstream → graph-wiki explicitly.
.planning/REQUIREMENTS.md
# rationale: Plan-meta + Upstream-reference — ROADMAP.md Phase 11–15 summaries describe the port/rebrand work.
.planning/ROADMAP.md
# rationale: Upstream-reference + Plan-meta — STATE.md mirrors PROJECT.md core-value/north-star + research flags note.
.planning/STATE.md

# Provenance / Parity-behavior (packages/ + agents/)
# rationale: Provenance — package docstring "(ported from lattice-wiki-core)".
packages/vault-io/src/vault_io/__init__.py
# rationale: Provenance — module + function docstrings "Extracted from lattice-wiki-core's …".
packages/vault-io/src/vault_io/ingest_work_item.py
# rationale: Provenance — module docstring "Extracted from lattice-wiki-core's ingest_source.py".
packages/vault-io/src/vault_io/ingest_source.py
# rationale: Parity-behavior — BM25 tokenizer + stopword-set parity with upstream lattice-wiki-core wiki_search.
agents/code-wiki-agent/src/code_wiki_agent/commands/query.py
# rationale: Format-compatibility (CLAUDE.md §Constraints) — read_layout() accepts the legacy `<!-- lattice-wiki:layout:start/end -->` sentinel pair so existing upstream lattice-wiki vaults remain parseable (WR-02 fix). write_layout() emits only graph-wiki sentinels.
packages/vault-io/src/vault_io/layout_io.py

# Test prose / Upstream-guard / Test-data (test files)
# rationale: Provenance + Upstream-guard — `test_no_lattice_wiki_core_imports` guard literal; renaming would break the guard.
packages/vault-io/tests/test_ingest_source.py
# rationale: Provenance + Upstream-guard — same guard shape for ingest_work_item.
packages/vault-io/tests/test_ingest_work_item.py
# rationale: Provenance — "Ported from lattice_wiki_core/tests/test_lint_wikilink_placeholders.py".
packages/vault-io/tests/test_wikilink_predicate.py
# rationale: Provenance — "Finding-count parity with lattice-wiki-core" describes the test's parity goal.
packages/vault-io/tests/test_lint_modules.py
# rationale: Test-fixture (non-round-trip) — fixture log generated against upstream tooling (R-01 spirit).
packages/vault-io/tests/fixtures/single-package-vault/
# rationale: Test-data — recorded answer cites `[[packages/lattice-wiki-core]]` as measurement subject (R-02).
packages/eval-harness/tests/test_two_gate_scorer.py
# rationale: Test-data — query subject "What does lattice-wiki-core do?" (R-02).
packages/eval-harness/tests/test_sweep.py
# rationale: Test-data — recorded answer payloads citing the upstream package (R-02).
packages/eval-harness/tests/test_divergence_metric.py
# rationale: Test-data — recorded structural-test inputs citing the upstream package (R-02).
packages/eval-harness/tests/test_structural.py
# rationale: Test-data — divergence-check inputs + a synthetic source-page citing the upstream package (R-02).
packages/eval-harness/tests/test_divergence_checks.py
# rationale: Test-data — sweep-eval query + answers + expected substring referencing the upstream package (R-02).
packages/eval-harness/tests/eval/test_sweep_eval.py

# ---------------------------------------------------------------------------
# carry-forward (post-hoc): R-02/R-03/Provenance hits not enumerated by plan
# 03's carry-forward inventory but discovered when running the gate. Each is
# classified against the existing R-decisions; none is a missed rebrand.
# ---------------------------------------------------------------------------
# rationale: R-02 — recorded eval baseline JSON; renaming would lie about what was measured.
packages/eval-harness/baselines/divergence-ingestor.json
# rationale: R-02 — recorded eval baseline JSON; renaming would lie about what was measured.
packages/eval-harness/baselines/divergence-librarian.json
# rationale: Provenance — workspace-io README header "Ported from `lattice-workspace`".
packages/workspace-io/README.md
# rationale: Provenance — config.py doctests/internals reference upstream lattice config-key behavior.
packages/workspace-io/src/workspace_io/config.py
# rationale: Provenance — test asserts behavior parity with upstream `lattice` directory key.
packages/workspace-io/tests/test_local_config.py
# rationale: Provenance — test comment names the upstream `lattice v1-coerced-entry` test being rewritten.
packages/workspace-io/tests/test_warn_if_stale.py
# rationale: Provenance — packages/prompt-sources/ holds canonical upstream lattice-wiki SKILL.md + reference prose; the entire package mirrors upstream content for prompt fragment provenance.
packages/prompt-sources/
# rationale: Provenance — test name + comment reference upstream lattice-wiki lint behavior parity (CMD-05).
agents/code-wiki-agent/tests/commands/test_lint_parity.py
# rationale: Provenance — test comment names the sibling upstream lattice repo to assert vendored copies are not pulled in.
agents/code-wiki-agent/tests/prompts/test_provenance.py

# ---------------------------------------------------------------------------
# Phase 14 own plans + context — Phase 14 documents the plugin port from
# upstream lattice-wiki to graph-wiki; planning docs reference both names
# (same allowlist class as Phase 12 and Phase 13 entries above).
# ---------------------------------------------------------------------------
.planning/phases/14-plugin-port-m3b/

# ---------------------------------------------------------------------------
# .pytest_cache/v/cache/nodeids — runtime pytest artifacts that embed test
# node IDs from allowlisted test files (e.g. test_ingest_source.py contains
# the string "lattice" in a test-guard name). These files are gitignored but
# present locally after `uv run pytest`; excluding them keeps the gate stable
# across clean-checkout CI and local dev environments.
# ---------------------------------------------------------------------------
.pytest_cache/v/cache/nodeids

# ---------------------------------------------------------------------------
# Phase 14 plugin port — graph-wiki README files explicitly name the upstream
# `lattice-wiki` plugin as the ported origin (Provenance + Upstream-reference
# class, same as other Phase 14 plan entries above).
# ---------------------------------------------------------------------------
# rationale: Provenance — plugins/graph-wiki/README.md intro names the upstream plugin ported from.
plugins/graph-wiki/README.md
# rationale: Provenance — skills/graph-wiki/README.md header names the upstream lattice-wiki port.
plugins/graph-wiki/skills/graph-wiki/README.md

# ---------------------------------------------------------------------------
# Phase 18 own plans + context + discussion log — Phase 18 documents the
# /graph-wiki:init → /graph-wiki:bootstrap rename direction and quotes the
# old slug throughout. CHECK 2 (graph-wiki:init|wiki_init) covers the same
# scope; this allowlist entry keeps Phase 18's own meta files exempt.
# ---------------------------------------------------------------------------
# rationale: Phase 18 meta — quoted documentation of the /graph-wiki:init → /graph-wiki:bootstrap rename direction.
.planning/phases/18-plugin-command-rename/
# rationale: Phase 18 folded todo — filename slug references the older "init-wiki" rename direction (now superseded by /graph-wiki:bootstrap); content unchanged for traceability.
.planning/todos/resolved/2026-05-19-rename-graph-wiki-init-command-to-init-wiki.md

# ---------------------------------------------------------------------------
# Phase 21 — `code-wiki-agent` rename to `graph-wiki-agent`. Per D-13, the
# allowlist stays tight: only entries that would actively break things if
# rewritten. Most historical references are eliminated by the .planning/
# sweep (D-05 override of R-03), not allowlisted. Two categories:
#  (a) renamed-path mirrors of stale Phase 12 entries (R-01/R-02/Provenance
#      classes whose old `agents/code-wiki-agent/...` paths no longer match
#      anything after the dir rename — Phase 12 entries kept untouched per
#      M4; equivalent renamed-path entries added below).
#  (b) Phase 21-specific carry-forward classes (D-06 OOS wiki content,
#      D-07 raw spike sources, Phase 21 self-allowlist, D-07-analog sketch
#      mockups, runtime artifact dirs).
#  (c) Pre-existing lattice surface in v1.2 milestones archive + Phase
#      17/20 docs + a couple of test files — narrow allowlists per the
#      prompt guidance "EXCLUDE only via narrow allowlist entries — don't
#      broaden to mask unrelated regressions". Same R-class as Phase 12
#      R-03 / Provenance entries; never enforced previously because R-03
#      only covered `v1.0-` / `v1.1-` archive prefixes.
# ---------------------------------------------------------------------------

# (a) Renamed-path mirrors of stale Phase 12 allowlist entries — Phase 12
# entries at lines 125, 173, 175 point to `agents/code-wiki-agent/...` paths
# that no longer exist after Phase 21's dir rename. The original entries are
# left intact per M4 (no deletions of Phase 12 lines); equivalent renamed
# paths added here so the gate continues to allowlist the same files.
# rationale: Parity-behavior (mirror of stale Phase 12 line 125) — BM25 tokenizer + stopword-set parity with upstream lattice-wiki-core wiki_search.
agents/graph-wiki-agent/src/graph_wiki_agent/commands/query.py
# rationale: Provenance (mirror of stale Phase 12 line 173) — test name + comment reference upstream lattice-wiki lint behavior parity (CMD-05).
agents/graph-wiki-agent/tests/commands/test_lint_parity.py
# rationale: Provenance (mirror of stale Phase 12 line 175) — test comment names the sibling upstream lattice repo to assert vendored copies are not pulled in.
agents/graph-wiki-agent/tests/prompts/test_provenance.py

# (b) Phase 21-specific carry-forward classes.
# rationale: graph-wiki/wiki/ is out of scope per D-06 — wiki content is healed by next /graph-wiki:scan run.
graph-wiki/wiki/
# rationale: D-07 — raw spike sources stay verbatim (historic untouched material).
.planning/spikes/001-subagent-context-audit/sources/
.planning/spikes/002-lattice-drift-inventory/sources/
# rationale: Phase 21 self-allowlist — the phase dir itself documents the rename and naturally references both old and new names side-by-side (mirrors Phase 12's self-allowlist pattern at line 75).
.planning/phases/21-rename-code-wiki-agent-to-graph-wiki-agent-update-all-code-r/
# rationale: D-07-analog (per 21-04 SUMMARY forward pointer) — `.claude/skills/sketch-findings-deep-agents/sources/*/index.html` are historical HTML snapshots of sketch sources that quote prior commit messages containing CODE_WIKI_CONFIG; rewriting would corrupt commit-log quotations.
.claude/skills/sketch-findings-deep-agents/sources/
# rationale: D-07-analog (Plan 21-05 Task 3 discretion) — `.planning/sketches/00{1,2,3}/index.html` are static sketch mockups quoting `agents/code-wiki-agent` paths verbatim as rendered `/graph-wiki:refresh` output examples; rewriting would corrupt the design artifact. Plan scope was `.planning/sketches/**/*.md`, not *.html.
.planning/sketches/001-refresh-sweep-output/index.html
.planning/sketches/002-refresh-result-block/index.html
.planning/sketches/003-refresh-diff-doc/index.html
# rationale: Auto-generated intel data — `.planning/intel/{files,deps,apis}.json` + `arch.md` are regenerated by `/graphify`-style tooling. Out of plan 21-05's `<files>` scope (which only listed `intel/stack.json`). Will heal on next intel regen.
.planning/intel/files.json
.planning/intel/deps.json
.planning/intel/apis.json
.planning/intel/arch.md
# rationale: Gitignored test-output dir — agent test runs may emit stale references; not committed.
agents/graph-wiki-agent/test-out/

# (c) Pre-existing lattice surface uncovered by R-03 — narrow allowlists per
# the prompt guidance. None of these is a missed code-wiki-* rebrand; all are
# upstream lattice-wiki references in the same R-03 / Provenance class as the
# existing Phase 12 entries. Documented as out-of-scope for Phase 21 (would
# have failed the gate even before the rename, if v1.2 had been added to R-03
# at the time Phase 16 archived to milestones/).
# rationale: R-03 extension — v1.2 milestone REQUIREMENTS / ROADMAP describe the upstream lattice-wiki port (same class as v1.0-/v1.1- entries above).
.planning/milestones/v1.2-REQUIREMENTS.md
.planning/milestones/v1.2-ROADMAP.md
# rationale: R-03 extension — v1.2 phase archives reference upstream lattice-wiki/lattice-workspace throughout (same class as Phase 12 lines 75, 81, 88, 182 which were authored before Phase 16 archived these dirs to milestones/).
.planning/milestones/v1.2-phases/11-workspace-io-port-m1/
.planning/milestones/v1.2-phases/12-drift-backport-ecosystem-rebrand-m2/
.planning/milestones/v1.2-phases/13-plugin-spec-m3a/
.planning/milestones/v1.2-phases/14-plugin-port-m3b/
.planning/milestones/v1.2-phases/15-wiki-self-update/
.planning/milestones/v1.2-phases/16-carry-forward-debt-cleanup/
# rationale: Plan-meta + Upstream-reference — Phase 17/20 docs name upstream lattice-wiki (same class as Phase 12 line 109 PROJECT.md / line 113 ROADMAP.md plan-meta entries).
.planning/phases/17-vault-io-bug-fixes/
.planning/phases/20-workspace-manifest-model-config/
# rationale: Provenance — vault-io scan-companion-fold test references upstream lattice-wiki-core's parent overview filename `lattice-curator-core`.
packages/vault-io/tests/test_scan_companion_fold.py
# rationale: Provenance — eval-harness scanner regression test asserts fixture vault contains no `lattice` symbols (test-guard literal).
packages/eval-harness/tests/test_scanner_regression.py
