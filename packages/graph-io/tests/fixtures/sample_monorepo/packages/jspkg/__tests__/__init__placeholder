Placeholder so the __tests__/ directory is git-tracked even if its sole
test file is ever deleted. Required by Phase 30 plan 30-04 D-19a fixture
extension (the directory must exist for test_suites.emit's filesystem walk
to discover it).
