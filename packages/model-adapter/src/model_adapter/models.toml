# Dedicated handle for the BED-01 connectivity ping in eval_harness.preflight.
# Pin a cheap, fast model here — this role is invoked once per sweep purely to
# confirm AWS credentials and bedrock:InvokeModel permission before spending.
[roles.preflight]
model_id        = "global.anthropic.claude-haiku-4-5-20251001-v1:0"
region          = "us-east-1"
max_tokens      = 64
max_concurrency = 1

[roles.librarian]
# Sweep candidates queued 2026-05-29 (candidate set below; sweep not yet run — no scores).
# Previous default: us.anthropic.claude-haiku-4-5-20251001-v1:0
model_id        = "global.anthropic.claude-haiku-4-5-20251001-v1:0"
region          = "us-east-1"
max_tokens      = 2048
max_concurrency = 5
sweep_candidates = [
  "global.anthropic.claude-haiku-4-5-20251001-v1:0",
  "qwen.qwen3-32b-v1:0",
  "qwen.qwen3-next-80b-a3b",
  "deepseek.v3.2",
  "moonshotai.kimi-k2.5",
  "zai.glm-5",
]

[roles.code_reader]
# Sweep candidates queued 2026-05-29 (candidate set below; sweep not yet run — no scores).
# Previous default: us.anthropic.claude-haiku-4-5-20251001-v1:0
# Plan 03-09: vault-thin code-fallback role. Reads source files directly when
# the librarian fan-out returns no useful excerpts. Conservative defaults —
# the fallback is opt-in and bounded (max 5 tool-call iterations per page).
model_id        = "global.anthropic.claude-haiku-4-5-20251001-v1:0"
region          = "us-east-1"
max_tokens      = 2048
max_concurrency = 3
sweep_candidates = [
  "global.anthropic.claude-haiku-4-5-20251001-v1:0",
  "qwen.qwen3-coder-30b-a3b-v1:0",
  "qwen.qwen3-coder-next",
  "mistral.devstral-2-123b",
  "openai.gpt-oss-120b-1:0",
  "minimax.minimax-m2.5",
]

[roles.scanner]
# Sweep candidates queued 2026-05-29 (candidate set below; sweep not yet run — no scores).
# Previous default: us.anthropic.claude-haiku-4-5-20251001-v1:0
model_id        = "global.anthropic.claude-haiku-4-5-20251001-v1:0"
region          = "us-east-1"
max_tokens      = 500
max_concurrency = 10
sweep_candidates = [
  "global.anthropic.claude-haiku-4-5-20251001-v1:0",
  "openai.gpt-oss-20b-1:0",
  "zai.glm-4.7-flash",
  "mistral.ministral-3-14b-instruct",
  "qwen.qwen3-32b-v1:0",
  "qwen.qwen3-coder-30b-a3b-v1:0",
]

# Phase 45 D-06 — prose-only narrative generation for entity pages.
# Initial config matches `scanner` for v1.8; eval-driven refinement deferred
# to v1.9. `max_tokens` is lower than scanner because narrator emits prose
# only (no frontmatter, no headings — just the body between `## Narrative`
# and the next H2 on an entity page).
[roles.narrator]
model_id        = "global.anthropic.claude-haiku-4-5-20251001-v1:0"
region          = "us-east-1"
max_tokens      = 600
max_concurrency = 10

[roles.linter]
# Sweep candidates queued 2026-05-29 (candidate set below; sweep not yet run — no scores).
# Previous default: us.amazon.nova-lite-v1:0
# NOTE: incumbent default us.amazon.nova-lite-v1:0 is NOT in the 2026-05-29 candidate
# set — default left as-is; needs a decision after the sweep runs.
model_id        = "us.amazon.nova-lite-v1:0"
region          = "us-east-1"
max_tokens      = 3000
max_concurrency = 10
sweep_candidates = [
  "global.anthropic.claude-haiku-4-5-20251001-v1:0",
  "qwen.qwen3-32b-v1:0",
  "openai.gpt-oss-120b-1:0",
  "deepseek.v3.2",
  "minimax.minimax-m2.5",
  "zai.glm-4.7-flash",
]

[roles.ingestor]
# Sweep candidates queued 2026-05-29 (candidate set below; sweep not yet run — no scores).
# Previous default: us.anthropic.claude-haiku-4-5-20251001-v1:0
model_id        = "qwen.qwen3-32b-v1:0"
region          = "us-east-1"
max_tokens      = 2048
max_concurrency = 5
sweep_candidates = [
  "global.anthropic.claude-haiku-4-5-20251001-v1:0",
  "qwen.qwen3-32b-v1:0",
  "openai.gpt-oss-120b-1:0",
  "minimax.minimax-m2.5",
  "qwen.qwen3-next-80b-a3b",
  "zai.glm-4.7-flash",
  "qwen.qwen3-vl-235b-a22b",
]

[roles.synthesizer]
# Sweep candidates queued 2026-05-29 (candidate set below; sweep not yet run — no scores).
# Previous default: us.anthropic.claude-sonnet-4-6
model_id        = "qwen.qwen3-32b-v1:0"
region          = "us-east-1"
max_tokens      = 4096
max_concurrency = 3
sweep_candidates = [
  "global.anthropic.claude-haiku-4-5-20251001-v1:0",
  "qwen.qwen3-32b-v1:0",
  "zai.glm-5",
  "moonshotai.kimi-k2.5",
  "deepseek.v3.2",
  "qwen.qwen3-next-80b-a3b",
  "us.deepseek.r1-v1:0",
  "moonshot.kimi-k2-thinking",
]

# Phase 48 D-19 — LLM proposal for `graph propose-domains`. Separate from
# scanner so cost-per-role is trackable and the model can be tuned
# independently. Initial config mirrors scanner; revisit in v1.9 eval.
[roles.domain-proposer]
model_id        = "global.anthropic.claude-haiku-4-5-20251001-v1:0"
region          = "us-east-1"
max_tokens      = 1024
max_concurrency = 5

# NOTE: the binding judge configuration lives in eval_harness/judge.py
# (JUDGE_PANEL_CONFIG). These blocks are kept in sync for reference only
# and are NOT read by the judge harness.
[roles.judge_a]
model_id        = "mistral.mistral-large-3-675b-instruct"
region          = "us-east-1"
max_tokens      = 2048
max_concurrency = 2

[roles.judge_b]
model_id        = "us.amazon.nova-pro-v1:0"
region          = "us-east-1"
max_tokens      = 2048
max_concurrency = 2
